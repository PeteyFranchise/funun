import { NextResponse } from 'next/server'
import { z } from 'zod'
import { isRoomLead } from '@/lib/playbook/entries'
import { requireRoomAccess } from '@/lib/playbook/rooms'
import { createServiceClient } from '@/lib/supabase/server'
const Schema=z.object({roomKey:z.string().min(1).max(80),status:z.enum(['active','monitoring','resolved','closed']),note:z.string().trim().min(1).max(8000)}).strict()
export async function PATCH(request:Request,context:{params:Promise<{id:string}>}){const id=z.string().uuid().safeParse((await context.params).id);const parsed=Schema.safeParse(await request.json().catch(()=>({})));if(!id.success||!parsed.success)return NextResponse.json({error:'Invalid incident update'},{status:400});const auth=await requireRoomAccess(parsed.data.roomKey);if('error'in auth)return NextResponse.json({error:auth.error},{status:auth.status});const service=createServiceClient();const room=await service.from('playbook_rooms').select('id').eq('key',parsed.data.roomKey).maybeSingle();if(!room.data)return NextResponse.json({error:'Room not found'},{status:404});if(auth.staffRole!=='leadership'&&!(await isRoomLead(service,room.data.id,auth.user.id)))return NextResponse.json({error:'Only leadership or the room lead can change incident state'},{status:403});const incident=await service.from('playbook_incidents').select('status').eq('id',id.data).eq('room_id',room.data.id).maybeSingle();if(!incident.data)return NextResponse.json({error:'Incident not found'},{status:404});const update=await service.from('playbook_incidents').update({status:parsed.data.status,resolved_at:['resolved','closed'].includes(parsed.data.status)?new Date().toISOString():null}).eq('id',id.data);if(update.error)return NextResponse.json({error:update.error.message},{status:500});const eventType=parsed.data.status==='resolved'?'resolved':'status_changed';const event=await service.from('playbook_incident_events').insert({incident_id:id.data,event_type:eventType,actor_id:auth.user.id,note:parsed.data.note});if(event.error)return NextResponse.json({error:'Incident changed, but its audit event failed. Contact IT.'},{status:500});return NextResponse.json({data:{status:parsed.data.status}})}
