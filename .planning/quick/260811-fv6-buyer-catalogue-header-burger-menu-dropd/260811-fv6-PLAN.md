---
quick_id: 260811-fv6
slug: buyer-catalogue-header-burger-menu-dropd
type: quick
status: in-progress
date: 2026-08-11
branch: feat/lane1-catalogue-menu-help
lane: Lane 1 (buyer portal) — UI, step 1 of 3
---

# Quick 260811-fv6 — Buyer catalogue header menu + Help page

Wire the buyer catalogue's header (the LIGHT public view, `components/buyer/CatalogBrowserLight.tsx`)
"burger" menu into a real dropdown, wire the other placeholder header controls, and add a public
**Help / How Licensing Works** page. Step 1 of 3 (step 2 = signed-in menu + Contact-AE card;
step 3 = Blog).

## Tasks

1. **`LoginRegisterModal.tsx` — add `initialSource` prop** so a menu item can open the modal
   directly on the "Talk to a sales rep" view (`source='sales_rep'`). Default `'register'`;
   applied in the open-reset effect.

2. **`CatalogBrowserLight.tsx` — header dropdown + wiring**
   - Burger → accessible dropdown (`menuOpen` state, toggle, click-outside close via the existing
     outer `onClick`, `aria-haspopup`/`aria-expanded`).
   - Guest item set: Register, Login (→ `openAuth('login'|'register')`), Contact a sales rep
     (→ `openAuth('register','sales_rep')`), Help (`<a href="/help">`), Blog (`<a href="/blog">` —
     route lands in step 3; link now).
   - Wire cart "License queue" → `router.push('/sync/requests')`.
   - Wire "Browse" → clear filters + scroll to top.
   - `openAuth` helper + `authTab`/`authSource` state; pass `initialTab`/`initialSource` to the modal.
   - Add `.menuwrap`/`.menu`/`.mi`/`.msep` CSS to the component's `CSS` const, matching the `.panel`
     dropdown aesthetic + fnbl tokens (light + dark).

3. **`app/help/page.tsx` — new public Help / How Licensing Works page**
   - Public automatically (not in middleware's protected-prefix allowlist; no middleware change).
   - Wrapped in `.fnbl` + `FNBL_CSS` so it matches the light buyer aesthetic (and dark mode).
   - Content: what sync licensing is; the Funūn flow (browse → license request → AE → signed license
     → delivery); rights-readiness badges (Rights ready / Partial / Contact required) explained; FAQ.

## Out of scope (later steps)
- Signed-in buyer menu variant + Contact-AE card with name/photo (step 2).
- Blog content system + real `/blog` route (step 3).

## Verify
- `tsc --noEmit` clean.
- In the browser (localhost:3000/sync/catalog): burger opens the dropdown; items act correctly;
  `/help` renders in the light aesthetic. Cart routes to the license queue.

## Notes
- Executed inline (no cold subagents) — orchestrator already in full context.
- Committed on `feat/lane1-catalogue-menu-help`; NOT pushed (repo deploys to prod from `main`).
