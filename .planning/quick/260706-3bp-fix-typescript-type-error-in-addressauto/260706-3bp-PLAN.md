---
phase: quick-260706-3bp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - components/profile/AddressAutocomplete.tsx
autonomous: true
requirements: [QUICK-260706-3bp]

must_haves:
  truths:
    - "npx tsc --noEmit reports zero errors referencing components/profile/AddressAutocomplete.tsx"
    - "AddressAutocomplete.tsx uses proper google.maps.* ambient types, not `any` workarounds"
  artifacts:
    - "package.json lists @types/google.maps under devDependencies"
    - "node_modules/@types/google.maps exists after install"
  key_links:
    - "@types/google.maps devDependency provides the ambient `google` namespace that AddressAutocomplete.tsx type annotations resolve against"
---

<objective>
Fix the TypeScript type error in components/profile/AddressAutocomplete.tsx by installing the missing @types/google.maps devDependency and restoring proper Google Maps types, replacing the uncommitted `any`-based workaround currently on disk.

Purpose: The committed (HEAD) file annotates against the ambient `google.maps.*` namespace (e.g. `google.maps.places.PlaceResult`, `google.maps.places.Autocomplete`, `typeof google`) but `@types/google.maps` is not installed, so `npx tsc --noEmit` fails with TS2304/TS2503/TS7006. A local uncommitted diff "fixes" the build by swapping every Maps type for `any` plus eslint-disable comments — the developer rejected that approach and wants the proper types restored.
Output: @types/google.maps installed as a devDependency and AddressAutocomplete.tsx typed correctly with a clean tsc build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@components/profile/AddressAutocomplete.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @types/google.maps as a devDependency</name>
  <files>package.json, package-lock.json</files>
  <action>Run `npm install --save-dev @types/google.maps` from the repo root. This adds the ambient `google` namespace (`google.maps.*`, including `google.maps.places.PlaceResult`, `google.maps.places.Autocomplete`, and the `google.maps.places.PlaceResult.address_components` element type) that AddressAutocomplete.tsx annotates against. Do NOT pin a specific version unless install resolution fails — let npm select the current published range and record it in package.json + package-lock.json. This package is a widely-used, first-party DefinitelyTyped types package (`@types/*` scope, published by the TypeScript community) — no runtime code, types only.</action>
  <verify>
    <automated>test -d node_modules/@types/google.maps && node -e "const p=require('./package.json'); if(!p.devDependencies || !p.devDependencies['@types/google.maps']) process.exit(1)"</automated>
  </verify>
  <done>node_modules/@types/google.maps exists and package.json devDependencies contains an @types/google.maps entry.</done>
</task>

<task type="auto">
  <name>Task 2: Restore proper Google Maps types in AddressAutocomplete.tsx</name>
  <files>components/profile/AddressAutocomplete.tsx</files>
  <action>Revert the uncommitted `any`-based workaround now that the type package is installed. Make these edits:
  (1) Delete the `type GoogleMaps = any` alias and its preceding eslint-disable comment (currently lines 20-21).
  (2) In the `declare global` block, change `google: { maps: GoogleMaps }` to `google: typeof google` so `Window.google` carries the full ambient Maps typing.
  (3) Change `parsePlace`'s parameter type from `any` to `google.maps.places.PlaceResult` and delete the eslint-disable comment above the function signature.
  (4) Inside `parsePlace`, remove the two eslint-disable comments above the `.address_components?.find(...)` callbacks and let the callback parameter be inferred (the annotation `(c: any)` becomes `(c)` — do not annotate; the installed types infer the element type). Do NOT introduce any explicit `any`.
  (5) Change `autocompleteRef` from `useRef&lt;any&gt;(null)` to `useRef&lt;google.maps.places.Autocomplete | null&gt;(null)` and delete the eslint-disable comment above it.
  Do not alter runtime behavior — this is a types-only revert. Do not add any new eslint-disable comments. After edits, no `@typescript-eslint/no-explicit-any` disable comment should remain in the file.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c 'AddressAutocomplete' | grep -qx 0 && grep -v '^#' components/profile/AddressAutocomplete.tsx | grep -c 'no-explicit-any' | grep -qx 0</automated>
  </verify>
  <done>`npx tsc --noEmit` emits zero errors referencing AddressAutocomplete.tsx, the file contains no `@typescript-eslint/no-explicit-any` disable comments and no `type GoogleMaps = any` alias, and the four annotation sites use proper `google.maps.*` types.</done>
</task>

</tasks>

<verification>
Full-file typecheck must be clean for this file:
- `npx tsc --noEmit` shows zero lines matching `AddressAutocomplete`.
- The four annotation sites are restored: `Window.google: typeof google`, `parsePlace(place: google.maps.places.PlaceResult)`, inferred `.find` callback params (no `any`), `useRef<google.maps.places.Autocomplete | null>(null)`.
- No `eslint-disable-next-line @typescript-eslint/no-explicit-any` comments remain in the file; no `type GoogleMaps = any` alias remains.
</verification>

<success_criteria>
- @types/google.maps is a recorded devDependency (package.json + package-lock.json) and installed in node_modules.
- `npx tsc --noEmit` produces no errors referencing components/profile/AddressAutocomplete.tsx.
- AddressAutocomplete.tsx uses the ambient google.maps namespace throughout with no `any` workaround and no eslint-disable comments.
- Runtime behavior is unchanged (types-only edit).
</success_criteria>

<output>
Create `.planning/quick/260706-3bp-fix-typescript-type-error-in-addressauto/260706-3bp-SUMMARY.md` when done.
</output>
