---
phase: 28-industry-accounts-green-room-access
plan: 05
type: execute
wave: 2
depends_on: ["28-01", "28-02", "28-03"]
files_modified:
  - supabase/migrations/085_industry_capability_green_room_gate.sql
  - __tests__/migration-085.test.ts
autonomous: false
requirements: [INDUSTRY-01, INDUSTRY-02, INDUSTRY-06]
# NOTE: provisional requirement IDs — NOT yet registered in .planning/REQUIREMENTS.md.

must_haves:
  truths:
    - "handle_new_user()'s industry branch writes an approved 'industry' capability_grants row (source='signup') atomically with the user_profiles insert, so every future Industry account (admin invite AND curator claim) can pass the Antenna gate."
    - "Existing member_type='industry' accounts lacking an approved industry capability_grant are backfilled one grant each."
    - "green_room_posts INSERT is DB-enforced to authors whose user_profiles.member_type IN ('artist','industry') — the app gate is no longer the only line of defense."
  artifacts:
    - supabase/migrations/085_industry_capability_green_room_gate.sql
  key_links:
    - "The DB capability write (trigger) is the SINGLE writer of the industry grant at creation — no racing app-side insert (RESEARCH/PATTERNS ordering note)."
    - "The RLS WITH CHECK on green_room_posts_insert_own mirrors the app-layer greenRoomPosterGate member_type rule (defense-in-depth)."
  prohibitions:
    - "Claude MUST NOT run `supabase db push` — migrations are HUMAN-GATED in this project; the owner pushes via Codex after review."
    - "The migration MUST NOT delete the handle_new_user() curator branch or the (curator-portal) route group (two-step retirement — deferred pending the live zero-count)."
    - "The migration MUST NOT drop the industry_profiles table (out of scope, RESEARCH Open Question #3)."
    - "The capability_grants insert MUST use source='signup' (a value already allowed by migration 042's CHECK) — do not add a new source enum value."
---

<objective>
Draft the single additive migration that makes the phase's fixes DB-authoritative, then push it via the
HUMAN-GATED checkpoint (this project NEVER runs `supabase db push` from an agent — matches Phases 16/21/25).

The migration (085) does two things:
1. INDUSTRY-01/06 — `CREATE OR REPLACE FUNCTION handle_new_user()` to add, inside the existing INDUSTRY branch,
   an `approved` `capability_grants` insert (source='signup') for the new account, atomic with the
   `user_profiles` insert the branch already owns. This is the SINGLE writer (no racing app-side insert per
   RESEARCH/PATTERNS), and it covers BOTH creation call sites — admin invite and the repointed curator claim
   (Plan 28-03) — for free. Plus a one-time BACKFILL: for every existing `member_type='industry'` row lacking an
   approved industry grant, insert one (mirrors migration 042's backfill; closes the post-042 gap so
   `hasCapability('industry')` — hence the Plan 28-01 Antenna fix — is satisfiable). The curator branch and the
   artist branch are preserved verbatim.
2. INDUSTRY-02 — DROP + re-CREATE `green_room_posts_insert_own` so its `WITH CHECK` requires
   `author_id = auth.uid()` AND an EXISTS on `user_profiles` where `member_type IN ('artist','industry')` — the
   RLS backstop mirroring Plan 28-02's app gate (this repo's doctrine: app-layer checks are defense-in-depth
   only; RLS is authoritative).

The push checkpoint also confirms the live count of existing `role='curator'` accounts (owner believes ~0,
unverified — no DB tool this session) which gates the deferred two-step curator-branch/route-group removal.

Purpose: make the Antenna + Green Room fixes real at the database layer, under human control.
Output: migration 085 (drafted, tested, human-pushed) + confirmed live invariants.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-industry-accounts-green-room-access/28-RESEARCH.md
@.planning/phases/28-industry-accounts-green-room-access/28-PATTERNS.md
@supabase/migrations/039_handle_new_user_industry_branch.sql
@supabase/migrations/042_capability_grants.sql
@supabase/migrations/057_green_room_feed.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing migration-text assertions for migration 085</name>
  <read_first>
    - __tests__/migration-061.test.ts (the readFileSync + toContain/toMatch text-assertion pattern to mirror)
    - supabase/migrations/042_capability_grants.sql:23-28,65-77 (the capability_grants CHECK — source list includes 'signup' — and the migration-042 backfill shape to mirror)
    - supabase/migrations/057_green_room_feed.sql:365-379 (the exact green_room_posts_insert_own policy to replace)
    - supabase/migrations/039_handle_new_user_industry_branch.sql (the industry branch to extend)
  </read_first>
  <behavior>
    - __tests__/migration-085.test.ts (readFileSync of supabase/migrations/085_industry_capability_green_room_gate.sql):
      - asserts the file CREATE OR REPLACEs handle_new_user() and, within the industry branch, INSERTs INTO
        capability_grants with capability 'industry', status 'approved', and source 'signup'.
      - asserts a backfill statement inserts approved 'industry' grants for existing member_type='industry'
        profiles that lack one (a NOT EXISTS / anti-join guard so it is idempotent — no duplicate for accounts
        migration 042 already backfilled).
      - asserts it DROPs and re-CREATEs "green_room_posts_insert_own" with a WITH CHECK that both keeps
        `author_id = auth.uid()` AND adds an EXISTS on user_profiles with `member_type IN ('artist', 'industry')`.
      - asserts it does NOT contain a DROP of the curator branch nor a DROP TABLE industry_profiles (guard against scope creep).
  </behavior>
  <action>
    Create __tests__/migration-085.test.ts RED (the migration file does not exist yet), mirroring
    migration-061.test.ts. Use toContain/toMatch on the migration text. Do not weaken assertions.
  </action>
  <verify>
    <automated>npx jest __tests__/migration-085.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and FAILS (migration 085 not written yet).
    - No new npm packages; Jest config untouched.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write migration 085 (capability write + backfill + Green Room RLS gate) — DO NOT push</name>
  <read_first>
    - supabase/migrations/039_handle_new_user_industry_branch.sql (copy the FULL function body; extend ONLY the industry branch)
    - supabase/migrations/042_capability_grants.sql:20-40,65-80 (capability_grants columns, CHECK, backfill pattern, the capability_grants_active_uniq partial unique index behavior)
    - supabase/migrations/057_green_room_feed.sql:365-379 (green_room_posts_insert_own — replace, don't stack)
  </read_first>
  <action>
    Create supabase/migrations/085_industry_capability_green_room_gate.sql (additive; header comment notes it is
    HUMAN-GATED — run via `supabase db push` by the owner, never by an agent). Contents:
    (a) CREATE OR REPLACE FUNCTION public.handle_new_user() copying the migration-039 body verbatim, preserving
        the curator early-return branch and the artist default branch unchanged; inside the INDUSTRY branch,
        AFTER the user_profiles insert, add an INSERT INTO public.capability_grants (profile_id, capability,
        status, role_slugs, source, decided_at) VALUES (NEW.id, 'industry', 'approved',
        ARRAY(... role_badges ...), 'signup', now()). Wrap this insert in a nested BEGIN/EXCEPTION WHEN OTHERS
        THEN NULL block (same isolation idiom the branch already uses for subscriptions) so a grant-insert hiccup
        cannot orphan the account. Keep SECURITY DEFINER; no CREATE TRIGGER (on_auth_user_created already binds).
    (b) A one-time backfill: INSERT INTO capability_grants (profile_id, capability, status, source, decided_at)
        SELECT id, 'industry', 'approved', 'backfill', now() FROM public.user_profiles up WHERE up.member_type =
        'industry' AND NOT EXISTS (SELECT 1 FROM capability_grants cg WHERE cg.profile_id = up.id AND
        cg.capability = 'industry' AND cg.status = 'approved') — idempotent via the anti-join (safe to re-run;
        also avoids tripping capability_grants_active_uniq).
    (c) DROP POLICY IF EXISTS "green_room_posts_insert_own" ON green_room_posts; then CREATE POLICY
        "green_room_posts_insert_own" ON green_room_posts FOR INSERT TO authenticated WITH CHECK (author_id =
        auth.uid() AND EXISTS (SELECT 1 FROM public.user_profiles WHERE id = auth.uid() AND member_type IN
        ('artist','industry'))). Leave green_room_posts_update_own untouched.
    Verify exact table/column names against the live schema first (migration 076 renamed artist_profiles →
    user_profiles). Do NOT delete the curator branch, do NOT drop industry_profiles, do NOT add a new source enum
    value. Do NOT run supabase db push.
  </action>
  <verify>
    <automated>npx jest __tests__/migration-085.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - migration-085.test.ts GREEN (all text assertions pass; scope-creep guards pass).
    - Migration file present in supabase/migrations/ with the next sequential number (085); NOT pushed.
    - source='signup' used for the trigger insert; 'backfill' for the backfill (both allowed by migration 042's CHECK).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Owner pushes migration 085 + confirms live invariants</name>
  <action>
    HUMAN-GATED — the agent does NOT run `supabase db push`. Pause here and hand off to the owner: they review
    migration 085, confirm the live `role='curator'` account count, push via Codex, verify LOCAL=REMOTE, and run
    the post-push smoke below. The agent resumes only on the "approved" signal (or a described failure).
  </action>
  <what-built>
    Migration 085 (drafted + text-tested): handle_new_user() industry-branch capability_grants insert + backfill
    of existing industry accounts + the green_room_posts insert RLS member_type gate. All application code from
    Plans 28-01/28-02/28-03/28-04 is merged. Nothing has been pushed to the live database.
  </what-built>
  <how-to-verify>
    1. Review supabase/migrations/085_industry_capability_green_room_gate.sql (additive; no curator-branch delete;
       no industry_profiles drop).
    2. BEFORE pushing, confirm the live count of legacy accounts to migrate:
       `SELECT COUNT(*) FROM curators WHERE claimed_by IS NOT NULL;` and cross-check how many of those auth users
       carry app_metadata.role='curator'. Record the number in the SUMMARY (owner expects ~0). If NONZERO, do NOT
       proceed with the deferred curator-branch/route-group removal follow-up until those accounts are migrated;
       the repoint (Plan 28-03) already prevents NEW ones.
    3. Push via Codex: `supabase db push` (applies 085). Then `supabase migration list` — confirm LOCAL=REMOTE
       through 085.
    4. Post-push smoke (live accounts): (a) a freshly-invited Industry account (admin invite) can POST an Antenna
       opportunity (was 403 before — Plan 28-01) and has an approved 'industry' capability_grant; (b) an
       artist AND an industry account can post in the Green Room; (c) a principal without member_type IN
       artist/industry (e.g. a buyer session, or direct PostgREST insert) is REJECTED by RLS on
       green_room_posts insert; (d) a @funun.studio account cannot post (app-layer, Plan 28-02).
    5. Report results in a Codex push report; paste the migration list + smoke outcomes into the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" once 085 is live (LOCAL=REMOTE) and the smoke checks pass, or describe failures.</resume-signal>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- New migration `supabase/migrations/085_industry_capability_green_room_gate.sql` — modifies
  `handle_new_user()` (industry-branch capability_grants insert, source='signup'), backfills existing
  industry accounts, replaces the `green_room_posts_insert_own` RLS policy with a member_type WITH CHECK.
- New test `__tests__/migration-085.test.ts`.
- Confirmed live invariants: LOCAL=REMOTE through 085; the live `role='curator'` account count (gates the
  deferred two-step curator-branch/route-group removal follow-up).
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| direct PostgREST client → `green_room_posts` INSERT | bypasses the Next.js route; only RLS WITH CHECK can stop a non-member here |
| auth.users INSERT (signup/invite) → `handle_new_user()` trigger | SECURITY DEFINER writes user_profiles + capability_grants atomically |
| owner (human) → `supabase db push` | the only actor permitted to apply the migration to the live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-05-01 | Elevation of Privilege | green_room_posts insert (direct PostgREST) | high | mitigate | RLS WITH CHECK requires author's member_type IN ('artist','industry') — DB-authoritative backstop mirroring the app gate (Plan 28-02); a buyer/no-profile PostgREST insert is rejected |
| T-28-05-02 | Elevation of Privilege | industry capability grant write path | high | mitigate | Single writer in the SECURITY DEFINER trigger (source='signup'), atomic with user_profiles; approved-only; backfill anti-join is idempotent (no duplicate-grant / unique-index trip) |
| T-28-05-03 | Tampering | uncontrolled schema change on live DB | high | mitigate | Migration is HUMAN-GATED — drafted + text-tested by the agent, reviewed and pushed by the owner via Codex; blocking checkpoint, never auto-advanced |
| T-28-05-04 | Denial of Service | orphaned account on grant-insert failure | medium | mitigate | capability_grants insert wrapped in a nested EXCEPTION WHEN OTHERS THEN NULL block (same idiom the industry branch uses for subscriptions) — account creation continues even if the grant insert fails |
| T-28-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this plan (Package Legitimacy Audit: N/A). The migration adds no dependency |
</threat_model>

<verification>
- `npx jest __tests__/migration-085.test.ts` green; full suite (`npm test`) + `tsc`/lint clean before the checkpoint.
- Human checkpoint: LOCAL=REMOTE through 085; live smoke (industry can post opportunity; artist+industry can post
  in Green Room; non-member RLS-rejected; @funun.studio blocked); live `role='curator'` count recorded.
</verification>

<success_criteria>
- Migration 085 is live (owner-pushed), additive, and leaves the curator branch + industry_profiles table intact.
- hasCapability('industry') is satisfiable for real Industry accounts; green_room_posts insert is RLS-gated by member_type.
- The deferred curator-branch/route-group removal is unblocked-or-blocked per the recorded live count.
</success_criteria>

<output>
Create `.planning/phases/28-industry-accounts-green-room-access/28-05-SUMMARY.md` when done.
</output>
