---
phase: 28-industry-accounts-green-room-access
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(admin)/layout.tsx
  - app/(artist)/tools/pitchplug/page.tsx
  - __tests__/curator-directory-relocation.test.ts
autonomous: true
requirements: [INDUSTRY-05]
# NOTE: provisional requirement ID — NOT yet registered in .planning/REQUIREMENTS.md.

must_haves:
  truths:
    - "The artist-facing PitchPlug page (/tools/pitchplug) surfaces a discoverable link to the curator directory (/curators), which was previously an orphaned route reachable only by direct URL."
    - "The admin curators entry is presented as a PitchPlug-associated surface (relabel/grouping), not a standalone curator system."
  artifacts:
    - app/(artist)/tools/pitchplug/page.tsx
    - app/(admin)/layout.tsx
  key_links:
    - "The new PitchPlug → /curators link points at the existing browse page; no new data path is created."
  prohibitions:
    - "MUST NOT touch components/curators/PitchComposer.tsx, pitch_history, or the Launchpad pitch-send wiring — this is navigation only (RESEARCH Pitfall 5)."
    - "MUST NOT change the curators table schema, its RLS, or the directory's column-scoped projection."
    - "MUST NOT merge PitchPlugForm (AI copy generator) with the curators/pitch_history send flow."
---

<objective>
Relocate the curator CRM directory under the PitchPlug navigation surface (INDUSTRY-05). Per the owner decision,
the `curators` table is CRM data that "lives under PitchPlug for now" — PitchPlug is the tool that pitches these
contacts. This is a NAVIGATION/PLACEMENT change ONLY: PitchPlug has zero code relationship to the `curators`
table today (the real curator send flow lives in Launchpad's PitchComposer/pitch_history — RESEARCH Pitfall 5),
and this plan must not create one.

Two moves: (1) the artist-facing `/curators` browse page is currently ORPHANED — not referenced in
`ArtistNav.tsx`'s ITEMS list, reachable only by direct URL — so add a discoverable link/section to it from
`app/(artist)/tools/pitchplug/page.tsx`; (2) relabel/group the admin `/admin/curators` nav entry in
`app/(admin)/layout.tsx` so it reads as a PitchPlug-associated directory rather than a standalone system.

Purpose: make the curator directory discoverable from the tool that uses it, without over-building.
Output: two nav edits + a regression guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-industry-accounts-green-room-access/28-RESEARCH.md
@.planning/phases/28-industry-accounts-green-room-access/28-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing relocation regression test + admin nav relabel</name>
  <read_first>
    - __tests__/migration-061.test.ts (readFileSync/path.join source-assertion pattern to mirror)
    - app/(admin)/layout.tsx:35-40 (the /admin/curators nav Link — the one entry to relabel/group)
    - app/(artist)/tools/pitchplug/page.tsx:40-70 (the header/section JSX where the /curators link will be added in Task 2)
  </read_first>
  <behavior>
    - __tests__/curator-directory-relocation.test.ts (source-assertion, mirroring migration-061.test.ts):
      reads app/(artist)/tools/pitchplug/page.tsx as text and asserts it contains a link whose href is
      '/curators' (the directory is discoverable from PitchPlug); reads app/(admin)/layout.tsx as text and
      asserts the /admin/curators Link's visible label associates it with PitchPlug (e.g. contains the substring
      'PitchPlug' in or adjacent to the curators entry). Keep both assertions specific enough to fail today and
      pass only after Tasks 1-2.
  </behavior>
  <action>
    Create __tests__/curator-directory-relocation.test.ts RED. Then relabel the existing `/admin/curators` Link
    in app/(admin)/layout.tsx so it reads as a PitchPlug-associated directory (e.g. label "PitchPlug · Curators"
    or place it directly beneath a PitchPlug grouping heading) — keep the href '/admin/curators' unchanged (route
    stays; only the label/placement changes). Do not remove any other admin nav link. Project style: no
    semicolons, `@/` alias.
  </action>
  <verify>
    <automated>npx jest __tests__/curator-directory-relocation.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists; the pitchplug-link assertion FAILS (link not added yet) while the admin-label assertion may pass after this task.
    - Admin curators nav entry now reads as PitchPlug-associated; href unchanged; no other links removed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add a discoverable curator-directory link/section to the PitchPlug page (INDUSTRY-05)</name>
  <read_first>
    - app/(artist)/tools/pitchplug/page.tsx (full — the header block around lines 40-70 is the natural home for a directory link)
    - app/(artist)/curators/page.tsx:1-40 (confirm the target route is /curators and it is a browse-only directory — do NOT wire its data into PitchPlug)
  </read_first>
  <action>
    In app/(artist)/tools/pitchplug/page.tsx, add a small link/section (a next/link `Link` to '/curators') in the
    header area, labeled to indicate it opens the curator directory of pitch-target contacts (e.g. "Browse the
    curator directory"). This is a plain navigational link — do NOT fetch curators data here, do NOT import
    CuratorDirectory/PitchComposer, do NOT touch pitch_history or Launchpad. Keep the DEMO branch working (the
    link renders regardless of DEMO). Match the page's existing Tailwind/indigo idiom.
  </action>
  <verify>
    <automated>npx jest __tests__/curator-directory-relocation.test.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - curator-directory-relocation.test.ts GREEN (both assertions).
    - PitchPlug page links to /curators; no import of curators data modules / PitchComposer added.
    - tsc/lint clean; `npm run build` succeeds (no broken route/link).
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- Modified `app/(admin)/layout.tsx` — `/admin/curators` nav entry relabeled/grouped as PitchPlug-associated (href unchanged).
- Modified `app/(artist)/tools/pitchplug/page.tsx` — new discoverable `Link` to `/curators`.
- New test `__tests__/curator-directory-relocation.test.ts`.
- No schema/logic/data-path change; PitchComposer, pitch_history, Launchpad untouched.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artist session → `/tools/pitchplug` → `/curators` | both are already authenticated artist routes; only a nav link is added |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-04-01 | Information Disclosure | curator directory exposure via new link | low | accept | The `/curators` page already uses the column-scoped directory projection (no email/claim_token/raw claimed_by) and its own auth; adding a nav link introduces no new data path or exposure |
| T-28-04-02 | Tampering | accidental scope creep into pitch-send flow | medium | mitigate | Task guardrails + regression test forbid importing PitchComposer/pitch_history/CuratorDirectory data wiring into PitchPlug (RESEARCH Pitfall 5) |
| T-28-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this plan (Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx jest __tests__/curator-directory-relocation.test.ts` green; `npm run build` succeeds.
- Manual-Only (record in SUMMARY): from /tools/pitchplug, the curator-directory link is visible and opens /curators; admin nav shows the curators entry under/labeled with PitchPlug.
</verification>

<success_criteria>
- The curator directory is discoverable from PitchPlug (artist) and reads as PitchPlug-associated (admin).
- Zero change to curator data/logic; suite + build green.
</success_criteria>

<output>
Create `.planning/phases/28-industry-accounts-green-room-access/28-04-SUMMARY.md` when done.
</output>
