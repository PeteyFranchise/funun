---
phase: 33
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/playbook/read-doc.ts
  - lib/playbook/markdown-components.tsx
  - components/playbook/MarkdownDoc.tsx
  - next.config.mjs
  - __tests__/playbook-read-doc.test.ts
autonomous: true
requirements: [PLAYBOOK-05, PLAYBOOK-06]

must_haves:
  truths:
    - "readObservabilityDoc('RUNBOOK.md') returns the file's markdown string; a missing/renamed file throws a descriptive error naming the path + the outputFileTracingIncludes hint"
    - "MarkdownDoc renders via react-markdown + remark-gfm through a React-element component map (no raw-HTML injection)"
    - "next.config.mjs outputFileTracingIncludes ships docs/observability/** into the deployed serverless bundle — verified against .next/server/**/*.nft.json"
  artifacts:
    - lib/playbook/read-doc.ts
    - lib/playbook/markdown-components.tsx
    - components/playbook/MarkdownDoc.tsx
    - next.config.mjs
    - __tests__/playbook-read-doc.test.ts
  key_links:
    - "MarkdownDoc -> markdownComponents map -> UI-SPEC Doc-Page Markdown Container Contract (verbatim element styles)"
    - "read-doc.ts fs.readFile(process.cwd()/docs/observability) ↔ next.config.mjs outputFileTracingIncludes (the deploy-time bundling that makes the runtime read resolve on Vercel)"
  prohibitions:
    - "No raw-HTML string injection for the Playbook surface — react-markdown returns React elements"
    - "No literal relative path ('./docs/...') — always path.join(process.cwd(), 'docs', 'observability', ...)"
---

<objective>
Stand up the markdown-rendering + deploy-time file-tracing foundation the 4 IT doc pages depend on — and defuse the phase's single highest deployment risk (RESEARCH #1): `docs/observability/*.md` are read via `fs`+`process.cwd()` at runtime and are invisible to Next.js's file tracer, so without an `outputFileTracingIncludes` entry they 404/throw ONLY in production (D-10, PLAYBOOK-06).

Purpose: this is the wiring every doc page in 33-06 renders through. Sequenced in Wave 1 and build-trace verified so the doc pages never inherit an unverified deploy risk.
Output: `react-markdown` + `remark-gfm` installed, `readObservabilityDoc()` fail-fast reader, the `markdownComponents` element map, the `MarkdownDoc` RSC wrapper, and the `next.config.mjs` tracing entry (build-trace verified).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-RESEARCH.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-PATTERNS.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-UI-SPEC.md
@lib/vault/pdf/fonts.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install react-markdown + remark-gfm; add readObservabilityDoc() fail-fast reader</name>
  <files>package.json, package-lock.json, lib/playbook/read-doc.ts, __tests__/playbook-read-doc.test.ts</files>
  <read_first>
    - lib/vault/pdf/fonts.ts lines 1-45 (the fail-fast `fs` + `path.join(process.cwd(), ...)` precedent this reader mirrors)
    - 33-RESEARCH.md "Package Legitimacy Audit" (react-markdown 10.1.0 + remark-gfm 4.0.1 — both verdict OK / Approved, postinstall null, remarkjs org) and "Pattern 3" (read-doc code)
    - 33-PATTERNS.md section "lib/playbook/read-doc.ts (utility, CREATE)"
  </read_first>
  <action>
    Install the two RESEARCH-approved packages: `npm install react-markdown remark-gfm` (both audited OK in 33-RESEARCH.md's Package Legitimacy Audit — no [ASSUMED]/[SUS] flags, so no legitimacy checkpoint is required). Create `lib/playbook/read-doc.ts` exporting `readObservabilityDoc(filename: string): Promise<string>` that builds `DOCS_DIR = path.join(process.cwd(), 'docs', 'observability')`, checks existence with `fs.existsSync`, throws a descriptive Error naming the resolved path AND pointing at the `outputFileTracingIncludes` deploy hint when missing (mirrors `fonts.ts`'s loud-failure rationale — never a silent blank render), and otherwise returns `fs.promises.readFile(filePath, 'utf-8')`. Write `__tests__/playbook-read-doc.test.ts` FIRST (RED): assert it returns non-empty content for each real file `VENDOR-DIRECTORY.md`, `RUNBOOK.md`, `OPERATING-RHYTHM.md`, `THRESHOLDS-AND-SEVERITY.md`, and that a bogus filename throws an Error whose message contains the path. Do NOT import react-markdown into this test (keep it pure-logic to avoid the ESM-Jest issue — RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>npx jest __tests__/playbook-read-doc.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls react-markdown remark-gfm` shows both installed (react-markdown ^10.1.0, remark-gfm ^4.0.1)
    - `npx jest __tests__/playbook-read-doc.test.ts` passes (4 files return non-empty; bogus filename throws with path in message)
    - The reader builds its docs path from the runtime working directory (mirrors fonts.ts), never a hardcoded relative path — confirm by reading the file
  </acceptance_criteria>
  <done>Packages installed; readObservabilityDoc() reads the 4 docs from process.cwd() and fails loud on a missing file; Wave 0 test green.</done>
</task>

<task type="auto">
  <name>Task 2: markdownComponents element map + MarkdownDoc RSC wrapper</name>
  <files>lib/playbook/markdown-components.tsx, components/playbook/MarkdownDoc.tsx</files>
  <read_first>
    - 33-UI-SPEC.md section "Doc-Page Markdown Container Contract" (the verbatim per-element style table this map must satisfy: h1/h2/h3/p/strong/a/blockquote/table/thead/th/td/code/pre/ul/ol/hr)
    - 33-RESEARCH.md "Pattern 4" (the drafted components map — starting point) and "Pattern 3" (MarkdownDoc wrapper)
    - 33-PATTERNS.md section "components/playbook/MarkdownDoc.tsx + lib/playbook/markdown-components.tsx — NET-NEW"
  </read_first>
  <action>
    Create `lib/playbook/markdown-components.tsx` exporting a typed `markdownComponents: Components` (from `react-markdown`) mapping each markdown tag to a styled React element per the UI-SPEC "Doc-Page Markdown Container Contract" table verbatim — cover h1/h2/h3/p/strong/a/blockquote/table/thead/th/td/code/pre/ul/ol/hr, using the existing console tokens (`var(--lav)`, `var(--hair)`, `var(--card2)`, `var(--indigo)`) and the mockup literal hexes only where the UI-SPEC specifies (blockquote caution accent `#F59E0B`). Add zero new CSS custom properties (D-10, UI-SPEC Design System). Create `components/playbook/MarkdownDoc.tsx` as a plain Server Component (no client directive at the top of the file, so it ships zero client JS) that renders `<ReactMarkdown remarkPlugins={[remarkGfm]} components={markdownComponents}>{content}</ReactMarkdown>` inside a `max-w-[900px]` container per the UI-SPEC. Render is via react-markdown's React-element output — never a raw-HTML string sink.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "markdown-components|MarkdownDoc" || echo "no MarkdownDoc type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ReactMarkdown" components/playbook/MarkdownDoc.tsx` ≥ 1 and `grep -c "remarkGfm\|remark-gfm" components/playbook/MarkdownDoc.tsx` ≥ 1 [positive — renders via react-markdown]
    - MarkdownDoc has NO client directive at the top of the file (it stays a Server Component, zero client JS) — confirm by reading the file's first line
    - `markdownComponents` map defines styled overrides for at least h1,h2,h3,p,a,blockquote,table,th,td,code,pre (grep each tag key)
    - `npx tsc --noEmit` reports no errors from these two files
  </acceptance_criteria>
  <done>markdownComponents satisfies the UI-SPEC element contract; MarkdownDoc is a zero-client-JS RSC rendering trusted markdown via react-markdown + remark-gfm.</done>
</task>

<task type="auto">
  <name>Task 3: Add docs/observability tracing entry to next.config.mjs + BUILD-TRACE verification</name>
  <files>next.config.mjs</files>
  <read_first>
    - next.config.mjs lines 4-18 (the existing `outputFileTracingIncludes` fonts entry — the exact sibling pattern + the inline comment explaining the failure class)
    - 33-RESEARCH.md "Common Pitfall 1" + "Open Questions #1" (the file-path-style vs route-path-style key ambiguity across Next.js versions, and the MANDATORY build-trace verification step)
  </read_first>
  <action>
    Add a second key to `outputFileTracingIncludes` in `next.config.mjs` mirroring the fonts entry: `'app/(admin)/playbook/**/*': ['./docs/observability/**']`, with a one-line comment noting it mirrors the fonts fix for the doc-page `fs.readFile` calls (PLAYBOOK-06). Then run the MANDATORY build-trace verification (RESEARCH Open Question #1, a HARD gate, not "add the line and assume"): `npx next build`, then confirm the doc files are actually listed as bundled dependencies in the emitted trace manifests for the IT-room doc routes — `grep -rl "docs/observability" .next/server` must return ≥ 1 match. If the file-path-style key produces no trace entry, fall back to the route-path-style key per the official docs (the `(admin)` route group is stripped from the URL, so the route-path key likely should NOT include `(admin)`) and re-run the build-trace check until `docs/observability` appears in a `.nft.json`. Do NOT consider this task done until the grep succeeds.
  </action>
  <verify>
    <automated>npx next build >/tmp/33-03-build.log 2>&1; grep -rl "docs/observability" .next/server >/dev/null && echo "TRACE OK: docs/observability bundled" || (echo "TRACE MISSING — doc pages will 404 in production"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.mjs` `outputFileTracingIncludes` has a `docs/observability/**` entry keyed to the playbook routes
    - `npx next build` completes without error
    - `grep -rl "docs/observability" .next/server` returns ≥ 1 `.nft.json` match (the doc-page routes list the `.md` files as traced dependencies) — this is the hard deploy gate for PLAYBOOK-06
    - Existing fonts entry (`'app/api/**/*': ['./assets/fonts/**']`) is preserved unchanged
  </acceptance_criteria>
  <done>docs/observability/** is bundled into the serverless trace (proven via .next/server/**/*.nft.json), closing RESEARCH's #1 deployment risk before any doc page renders from it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed repo file → rendered page | `docs/observability/*.md` is trusted, committed content (not user input) rendered into the IT room |
| npm registry → build | new dependencies (react-markdown, remark-gfm) enter the supply chain |
| build tracer → deployed bundle | files read via runtime-computed paths must be explicitly bundled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-02 | Tampering (injection) | MarkdownDoc rendering committed markdown | low | mitigate | react-markdown outputs React elements through a typed `components` map — no raw-HTML string sink; injection risk is low because content is trusted/committed (`docs/observability/*.md`), not user input, and there is zero user input anywhere in this read-only surface |
| T-33-SC | Tampering | react-markdown + remark-gfm install | high | mitigate | Both packages audited in 33-RESEARCH.md Package Legitimacy Audit: verdict OK/Approved, `postinstall: null`, official `remarkjs` GitHub org, 27M/31M weekly downloads — no [ASSUMED]/[SUS], so no blocking legitimacy checkpoint required; lockfile pins the resolved versions |
| T-33-03 | Information Disclosure | build trace / config | low | accept | No secrets touched; `outputFileTracingIncludes` bundles only public committed docs; no vendor keys/tokens enter the client bundle from this plan |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `react-markdown` + `remark-gfm` dependencies (package.json / package-lock.json)
- `readObservabilityDoc(filename)` fail-fast reader — `lib/playbook/read-doc.ts`
- `markdownComponents` element-style map — `lib/playbook/markdown-components.tsx`
- `MarkdownDoc` zero-client-JS RSC wrapper — `components/playbook/MarkdownDoc.tsx`
- `outputFileTracingIncludes` `docs/observability/**` entry — `next.config.mjs`
- `__tests__/playbook-read-doc.test.ts` — Wave 0 reader test
</artifacts_produced>

<verification>
- `npx jest __tests__/playbook-read-doc.test.ts` green
- `npx next build` succeeds AND `grep -rl "docs/observability" .next/server` ≥ 1 (the deploy hard gate)
- MarkdownDoc renders via react-markdown (positive grep), stays a Server Component (no 'use client')
</verification>

<success_criteria>
The markdown-render + file-tracing foundation is complete and deploy-verified: docs read from `process.cwd()` fail loud when missing, render through react-markdown's React-element path (no raw-HTML injection), and are proven bundled into the Vercel serverless trace so the 33-06 doc pages cannot inherit an unverified production 404.
</success_criteria>

<output>
Create `.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-03-SUMMARY.md` when done.
</output>
