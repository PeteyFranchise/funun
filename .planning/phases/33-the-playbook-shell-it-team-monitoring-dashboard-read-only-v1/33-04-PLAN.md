---
phase: 33
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/playbook/nav.ts
  - components/playbook/PlaybookNavLink.tsx
  - components/playbook/Rail2.tsx
  - components/playbook/ItRoomTopBar.tsx
  - __tests__/playbook-nav.test.ts
autonomous: true
requirements: [PLAYBOOK-02, PLAYBOOK-03]

must_haves:
  truths:
    - "nav.ts is the single source for the 6 Rail-2 rooms (5 coming-soon ghosts + the role-conditional IT room) and the 5 ordered IT sub-pages"
    - "nav.ts maps the 4 doc pages to their exact docs/observability filenames (D-10 page→file map)"
    - "Rail2 renders the IT room row ONLY when its canSeeItRoom prop is true; the 5 non-IT rooms always render as inert non-clickable ghosts"
    - "PlaybookNavLink is active (permanent hover treatment) when the path starts with /admin/playbook"
  artifacts:
    - lib/playbook/nav.ts
    - components/playbook/PlaybookNavLink.tsx
    - components/playbook/Rail2.tsx
    - components/playbook/ItRoomTopBar.tsx
    - __tests__/playbook-nav.test.ts
  key_links:
    - "nav.ts IT_SUBPAGES ↔ the 5 route segments under /admin/playbook/it/*"
    - "nav.ts DOC_PAGE_FILE ↔ docs/observability/*.md (single source of truth, D-10)"
  prohibitions:
    - "A ghost (coming-soon) room must NEVER be an <a>/<button> — it has no destination, so it must not be focusable/clickable"
    - "For non-authorized staff the IT room row is OMITTED from the DOM (not rendered locked/greyed) — D-06"
---

<objective>
Build the Playbook nav data model + the shared, route-independent chrome primitives every Playbook surface reuses: the room/sub-page single-source (`nav.ts`), the Rail 1 active link, the Rail 2 room list, and the shared IT-room top bar (D-05, D-06, D-04).

Purpose: keeping these as Wave 1 leaf primitives (no route dependency) lets the shell (33-05), the doc pages (33-06), and the dashboard (33-08) build in parallel in Wave 2, all importing one source of truth for room structure and the access-chip chrome.
Output: `lib/playbook/nav.ts`, `PlaybookNavLink`, `Rail2`, `ItRoomTopBar`, + a Wave 0 nav-structure test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-CONTEXT.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-UI-SPEC.md
@components/nav/ArtistNav.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/playbook/nav.ts — room + sub-page + doc-file single source</name>
  <files>lib/playbook/nav.ts, __tests__/playbook-nav.test.ts</files>
  <read_first>
    - 33-CONTEXT.md D-05/D-06 (the six rooms; five coming-soon ghosts; IT room role-conditional; the 5 IT sub-pages in order) and D-10 (page→file map)
    - 33-UI-SPEC.md "Component Inventory & States" → "Rail 2 — room list" + "Coming soon ghost rooms"
  </read_first>
  <action>
    Create `lib/playbook/nav.ts` (no import of StaffRole — keep it pure data, decoupled from the role type) exporting: `PLAYBOOK_ROOMS` — the six rooms in mockup order (Company-wide, A&R, AE / Sales, IT Team, TMS, Leadership), each `{ id, label, comingSoon: boolean, itGated: boolean }` where exactly five are `comingSoon:true` and only IT Team has `itGated:true` + `comingSoon:false`; `IT_SUBPAGES` — the 5 IT sub-pages in order `[dashboard, vendor-directory, runbook, operating-rhythm, thresholds]`, each `{ slug, label, href }` with labels `Monitoring Dashboard`, `Vendor Directory`, `Incident Runbook`, `Operating Rhythm`, `Thresholds & Severity` and hrefs `/admin/playbook/it/<slug>`; `DOC_PAGE_FILE` — a map from the 4 doc slugs to their exact `docs/observability` filenames per D-10 (`vendor-directory→VENDOR-DIRECTORY.md`, `runbook→RUNBOOK.md`, `operating-rhythm→OPERATING-RHYTHM.md`, `thresholds→THRESHOLDS-AND-SEVERITY.md`). Follow project naming conventions (`_VALUES`/`_LABELS`-style where natural, camelCase functions, named exports). Write `__tests__/playbook-nav.test.ts` FIRST (RED) asserting: 6 rooms, exactly 5 comingSoon, exactly one itGated (IT Team, not comingSoon), IT_SUBPAGES length 5 in the exact order with dashboard first, and DOC_PAGE_FILE has the 4 correct filename mappings.
  </action>
  <verify>
    <automated>npx jest __tests__/playbook-nav.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/playbook-nav.test.ts` passes (6 rooms / 5 comingSoon / 1 itGated / 5 ordered subpages / 4 doc-file mappings)
    - `grep -c "VENDOR-DIRECTORY.md" lib/playbook/nav.ts` ≥ 1 and all four D-10 filenames present
    - IT_SUBPAGES order begins with the dashboard slug (the index page)
  </acceptance_criteria>
  <done>nav.ts is the single source for rooms/sub-pages/doc-file mapping; Wave 0 structure test green.</done>
</task>

<task type="auto">
  <name>Task 2: PlaybookNavLink (Rail 1 active link) + Rail2 (room list, ghosts, role-conditional IT row)</name>
  <files>components/playbook/PlaybookNavLink.tsx, components/playbook/Rail2.tsx</files>
  <read_first>
    - components/nav/ArtistNav.tsx lines 1,4,107,283-292 (the `'use client'` + `usePathname()` active-state technique to mirror, scoped down)
    - 33-UI-SPEC.md "Rail reconciliation" #1 (Rail 1 single-link active state) + #2 (Rail 2 build to playbook-double-sidebar.html) + "Coming soon ghost rooms"
    - lib/playbook/nav.ts (created Task 1 — PLAYBOOK_ROOMS, IT_SUBPAGES)
  </read_first>
  <action>
    Create `components/playbook/PlaybookNavLink.tsx` (`'use client'`): a single `<Link href="/admin/playbook">` using `usePathname()` — active when `pathname.startsWith('/admin/playbook')`, applying the existing NAV_LINK_CLASS treatment plus the active variant `bg-[color:var(--border)] text-[color:var(--ink)] font-semibold` (UI-SPEC literal); label "The Playbook". Reuse the exact NAV_LINK_CLASS string so it matches the other Rail 1 links. Create `components/playbook/Rail2.tsx` — a component taking `canSeeItRoom: boolean` (and reading `usePathname()` for active room/sub-row styling; mark it `'use client'` or split a small client active-row child, matching the ArtistNav technique). Render Rail 2 to `playbook-double-sidebar.html`'s `.rail2` per the UI-SPEC: header (book icon + "The Playbook" + subhead "Company wiki · SOPs, topics & plays"); then the rooms from `PLAYBOOK_ROOMS`. The 5 `comingSoon` rooms render as inert non-interactive `<div>`s (NEVER `<a>`/`<button>`, no tabindex) with `opacity-[.45]`, `cursor-default`, and a trailing "Coming soon" pill per the UI-SPEC ghost treatment. The IT Team room renders as an enterable `<a class="room">` with its 5 `IT_SUBPAGES` sub-links (Monitoring Dashboard carries the static emerald "Live" badge) ONLY when `canSeeItRoom` is true — when false it is omitted from the DOM entirely (D-06, hidden not locked). Active room/sub-row styling via `usePathname()` + `startsWith()`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "PlaybookNavLink|Rail2" || echo "no Rail2/PlaybookNavLink type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "usePathname" components/playbook/PlaybookNavLink.tsx` ≥ 1 and it links to `/admin/playbook`
    - Rail2 renders the IT room row only under the `canSeeItRoom` prop; the 5 ghost rooms render as `<div>` (not `<a>`/`<button>`) with a "Coming soon" pill — verify no `<a`/`<button` wraps a comingSoon room in the source
    - Rail2 imports rooms/sub-pages from `lib/playbook/nav.ts` (single source), not a re-declared local list
    - `npx tsc --noEmit` reports no errors from these two files
  </acceptance_criteria>
  <done>Rail 1 gains an active-aware Playbook link; Rail 2 renders 5 inert ghosts + the role-conditional IT room with its 5 ordered sub-pages, all from nav.ts.</done>
</task>

<task type="auto">
  <name>Task 3: ItRoomTopBar — shared crumb + access chip (+ optional Live chip)</name>
  <files>components/playbook/ItRoomTopBar.tsx</files>
  <read_first>
    - 33-UI-SPEC.md "Component Inventory & States" → "Top bar (.ctop)" (render identically on all 5 IT-room pages; access chip on every page; Live chip dashboard-only) + "Copywriting Contract" (access chip `🔒 IT + Leadership`, Live chip `Live`)
  </read_first>
  <action>
    Create `components/playbook/ItRoomTopBar.tsx` — a presentational Server Component taking `{ crumb: string; showLiveChip?: boolean }`. Render the sticky `.ctop` bar per the UI-SPEC: left = mono breadcrumb `The Playbook / IT Team / {crumb}` (current segment bolded/lav); right = the access chip `🔒 IT + Leadership` (11px/700/indigo, the UI-SPEC's literal pill styling) always, then the `Live` chip (emerald, pulsing dot) only when `showLiveChip` is true. This is the deliberate gating-consistency extension beyond `playbook-it-team-room.html` (the access chip belongs on every gated page, D-02). Use only existing tokens + the UI-SPEC's literal values; add no new CSS custom properties.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "ItRoomTopBar" || echo "no ItRoomTopBar type errors"</automated>
  </verify>
  <acceptance_criteria>
    - ItRoomTopBar accepts `crumb` and optional `showLiveChip` and renders the `🔒 IT + Leadership` access chip unconditionally
    - The `Live` chip renders only when `showLiveChip` is true (grep the conditional)
    - `npx tsc --noEmit` reports no errors from this file
  </acceptance_criteria>
  <done>A shared IT-room top bar renders the crumb + access chip on every page and the Live chip only on the dashboard — importable by 33-06 and 33-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rail 2 render decision | whether the IT room row appears at all is a visibility signal derived from staff role |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-04 | Information Disclosure | Rail 2 IT-room visibility | medium | mitigate | The IT room row is omitted from the DOM entirely when `canSeeItRoom` is false (D-06) — non-authorized staff cannot even see the room exists in Rail 2; the caller (33-05 layout) computes `canSeeItRoom` server-side from getStaffRole. This is render-time visibility only — the authoritative access control is the per-page inline guard in 33-06/33-08 (D-02), not Rail 2 |
| T-33-04b | Elevation of Privilege | ghost room affordance | low | mitigate | Ghost rooms are inert `<div>`s (no `<a>`/`<button>`/tabindex) so they carry no navigable destination and cannot be a route into deferred surfaces |
| T-33-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `PLAYBOOK_ROOMS`, `IT_SUBPAGES`, `DOC_PAGE_FILE` — `lib/playbook/nav.ts`
- `PlaybookNavLink` (Rail 1 active client link) — `components/playbook/PlaybookNavLink.tsx`
- `Rail2` (room list + ghosts + role-conditional IT room) — `components/playbook/Rail2.tsx`
- `ItRoomTopBar` (shared crumb + access chip + optional Live chip) — `components/playbook/ItRoomTopBar.tsx`
- `__tests__/playbook-nav.test.ts` — Wave 0 nav-structure test
</artifacts_produced>

<verification>
- `npx jest __tests__/playbook-nav.test.ts` green
- `npx tsc --noEmit` clean for the four new files
- Ghost rooms are non-interactive `<div>`s; IT room row is DOM-omitted when canSeeItRoom is false
</verification>

<success_criteria>
The Playbook nav structure has one source of truth (rooms, ordered sub-pages, doc-file map), Rail 1 has an active-aware entry, and Rail 2 renders 5 inert ghosts + a role-conditional IT room — with the shared IT-room top bar chrome ready for the doc pages and dashboard to reuse.
</success_criteria>

<output>
Create `.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-04-SUMMARY.md` when done.
</output>
