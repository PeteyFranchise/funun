---
phase: 33
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/playbook/digest.ts
  - components/playbook/StatusBanner.tsx
  - components/playbook/DigestPanel.tsx
  - components/playbook/ThresholdsPanel.tsx
  - __tests__/playbook-digest.test.ts
  - __tests__/playbook-status-banner.test.tsx
  - __tests__/playbook-thresholds-panel.test.tsx
autonomous: true
requirements: [PLAYBOOK-07, PLAYBOOK-08, PLAYBOOK-09]

must_haves:
  truths:
    - "getDashboardHealth() re-checks /api/health in-process (import GET as checkHealth) and returns healthy/degraded/unknown, never throwing"
    - "buildDigestTodayRow(status) is pure — it classifies via classifyThreshold(metric, undefined) and renders text, and it does not send email"
    - "StatusBanner renders 3 states: healthy (emerald pulsing), degraded (rose steady), unknown/unreachable (rose steady, 'treat as degraded')"
    - "ThresholdsPanel renders exactly 7 rows from THRESHOLDS — excluding monthly_spend_usd AND uptime_consecutive_failures — with warn/crit values from config"
  artifacts:
    - lib/playbook/digest.ts
    - components/playbook/StatusBanner.tsx
    - components/playbook/DigestPanel.tsx
    - components/playbook/ThresholdsPanel.tsx
    - __tests__/playbook-digest.test.ts
    - __tests__/playbook-status-banner.test.tsx
    - __tests__/playbook-thresholds-panel.test.tsx
  key_links:
    - "getDashboardHealth() -> import { GET as checkHealth } from '@/app/api/health/route' (in-process, no self-HTTP)"
    - "ThresholdsPanel / digest -> THRESHOLDS + classifyThreshold from lib/observability/config.ts (no config modification)"
  prohibitions:
    - "Never self-fetch /api/health over HTTP — always the in-process import { GET as checkHealth }"
    - "The digest render must not dispatch the daily alert email — reuse only the classification, never the alert fan-out"
    - "ThresholdsPanel must not add monthly_spend_usd or uptime_consecutive_failures rows (7-row allowlist)"
---

<objective>
Build the Monitoring Dashboard's live-signal core as standalone, dependency-free components + a digest helper: the health re-check + digest "today" row (D-08, PLAYBOOK-08), the global status banner (D-07, PLAYBOOK-07), and the real-values thresholds panel (D-07/D-09, PLAYBOOK-09).

Purpose: these are the "live health + honest reference" pieces. Keeping them as Wave 1 leaf components lets the dashboard page (33-08) compose them. The digest reuses the cron's exact classification logic WITHOUT sending email — the one line of the cron this phase must not reuse.
Output: `lib/playbook/digest.ts`, `StatusBanner`, `DigestPanel`, `ThresholdsPanel` + Wave 0 tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-CONTEXT.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-UI-SPEC.md
@app/api/cron/daily-observability-check/route.ts
@lib/observability/config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/playbook/digest.ts — in-process health re-check + pure digest today-row</name>
  <files>lib/playbook/digest.ts, __tests__/playbook-digest.test.ts</files>
  <read_first>
    - app/api/cron/daily-observability-check/route.ts lines 40-50 (checkHealthStatus try/catch shape) + lines 62-69 (the classify loop) — reuse the classification, NOT the email dispatch
    - app/api/health/route.ts (checkHealth returns { status:'healthy'|'degraded', checkedAt, durationMs }; 200/503; never throws)
    - 33-UI-SPEC.md "Daily digest panel (D-08)" (single today row; tri-state dot; locked copy) + "Pattern 2" in 33-RESEARCH.md
  </read_first>
  <action>
    Create `lib/playbook/digest.ts` exporting: `getDashboardHealth(): Promise<'healthy'|'degraded'|'unknown'>` reusing the cron's exact try/catch shape — `import { GET as checkHealth } from '@/app/api/health/route'`, `await checkHealth()`, `.json()`, return `'healthy'` when `body.status === 'healthy'` else `'degraded'`, and `'unknown'` on any thrown/parse error (never self-fetch over HTTP — RESEARCH Anti-Pattern). And `buildDigestTodayRow(status: 'healthy'|'degraded'|'unknown')` — a PURE function returning `{ dot: 'emerald'|'rose'|'lavdim', text: string, date: string }` using the UI-SPEC's locked copy ("**{Healthy|Degraded|Unknown}.** /api/health re-check: {status}. All threshold metrics: no live telemetry yet (v2 wires the feed).") and today's date. This module reuses only the classification/status logic; it renders data and does not import or invoke the observability alert fan-out (no email is ever sent from a dashboard render, D-08). Write `__tests__/playbook-digest.test.ts` FIRST (RED): mock the health route's GET to assert getDashboardHealth maps 200-healthy → 'healthy', 503-degraded → 'degraded', and a thrown call → 'unknown'; assert buildDigestTodayRow returns the emerald/rose/lavdim dot for each status and the expected text — parity with the cron's tri-state (mirror the shared-fixture parity-anchor approach used in 18-04).
  </action>
  <verify>
    <automated>npx jest __tests__/playbook-digest.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/playbook-digest.test.ts` passes (health mapping tri-state + digest row dot/text parity)
    - digest.ts re-checks health in-process by importing the health route's GET handler (not a self-HTTP request) — confirm by reading the imports
    - `grep -c "fetch(" lib/playbook/digest.ts` == 0 (no self-HTTP)
    - `grep -c "observability/alerts" lib/playbook/digest.ts` == 0 (no alert fan-out import — the digest never sends email)
    - `npx tsc --noEmit` reports no new errors from this file
  </acceptance_criteria>
  <done>getDashboardHealth re-checks health in-process (tri-state, never throws); buildDigestTodayRow is a pure today-row with no email side effect; Wave 0 parity test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatusBanner (3 states) + DigestPanel (single live row + v2 note)</name>
  <files>components/playbook/StatusBanner.tsx, components/playbook/DigestPanel.tsx, __tests__/playbook-status-banner.test.tsx</files>
  <read_first>
    - 33-UI-SPEC.md "Global status banner" (healthy: emerald pulsing dot + "All systems operational"; degraded: rose steady dot + "Degraded — /api/health reporting an issue" + Incident Runbook link; unreachable: same rose treatment + "Health check unavailable — treat as degraded until confirmed") + "Daily digest panel" (single row + muted v2 note) + "Copywriting Contract"
    - lib/playbook/digest.ts (buildDigestTodayRow — Task 1)
  </read_first>
  <action>
    Create `components/playbook/StatusBanner.tsx` — a presentational component taking `status: 'healthy'|'degraded'|'unknown'` and rendering the three UI-SPEC banner states verbatim: healthy = emerald border/gradient + PULSING emerald dot + "All systems operational" + subtext "3/3 uptime monitors up · /api/health healthy · no open incidents"; degraded = rose border/gradient + STEADY (non-pulsing) `#F43F5E` dot + "Degraded — /api/health reporting an issue" + subtext linking to the Incident Runbook sub-page; unknown = the same rose treatment with subtext "Health check unavailable — treat as degraded until confirmed". Create `components/playbook/DigestPanel.tsx` — header "Daily digest" (`.src` "cron · 06:00 UTC"), exactly ONE row rendered from `buildDigestTodayRow(status)` (dot color per status + text + today's date column), and below it the muted note "Full digest history arrives in v2 — this shows today's live check only; the cron's own email digest is the historical record until then." Write `__tests__/playbook-status-banner.test.tsx` asserting the 3 states render their distinct title + (pulsing vs steady) dot semantics.
  </action>
  <verify>
    <automated>npx jest __tests__/playbook-status-banner.test.tsx --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/playbook-status-banner.test.tsx` passes for healthy/degraded/unknown
    - StatusBanner renders the healthy dot as pulsing and the degraded/unknown dot as steady (distinct treatment per UI-SPEC)
    - DigestPanel renders exactly one today row + the v2 history note (grep the note copy)
    - `npx tsc --noEmit` reports no new errors from these files
  </acceptance_criteria>
  <done>StatusBanner covers all 3 health states with honest (pulsing=fine / steady=attention) dot semantics; DigestPanel shows one live today row + the v2 note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ThresholdsPanel — real THRESHOLDS values, 7-row allowlist</name>
  <files>components/playbook/ThresholdsPanel.tsx, __tests__/playbook-thresholds-panel.test.tsx</files>
  <read_first>
    - 33-UI-SPEC.md "Thresholds panel" (the exact 7-row allowlist table with label↔ThresholdMetric mapping; excludes monthly_spend_usd + uptime_consecutive_failures; "awaiting feed" pill; "Live values · v2" header badge; severity legend footer)
    - lib/observability/config.ts lines 66-102 (THRESHOLDS values + the warning/critical fields + classifyThreshold — consumed, not modified)
    - 33-RESEARCH.md "Pitfall 4" (use the UI-SPEC's explicit 7-metric allowlist for the panel, not a blanket all-except-spend filter)
  </read_first>
  <action>
    Create `components/playbook/ThresholdsPanel.tsx` rendering the 7 threshold rows per the UI-SPEC allowlist — the metrics `vercel_5xx_rate`, `function_throttle`, `dynamic_route_p95_ms`, `supabase_cpu_pct`, `db_connections`, `disk_pct`, `auth_api_5xx_rate` with their UI-SPEC labels — explicitly EXCLUDING `monthly_spend_usd` (its own Spend tile) and `uptime_consecutive_failures` (Better Stack's own alerting). Each row: label + a mono value string built from `THRESHOLDS[metric]` as `warn {warning}{unit} · crit {critical}{unit}` (unit inferred: `%` for rate/pct, `ms` for latency, unitless count for throttle/connections) + a trailing "awaiting feed" pill. Header carries the "Live values · v2" fuchsia badge (honest labeling of the readings column, not the values). Static severity-legend footer (rose SEV-1/2, amber SEV-3, emerald healthy). Read THRESHOLDS from `@/lib/observability/config` — do NOT modify config.ts. Write `__tests__/playbook-thresholds-panel.test.tsx` asserting exactly 7 rows render, that neither excluded metric appears, and that a spot-checked row shows its real config warn/crit values.
  </action>
  <verify>
    <automated>npx jest __tests__/playbook-thresholds-panel.test.tsx --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/playbook-thresholds-panel.test.tsx` passes: exactly 7 rows; `monthly_spend_usd` and `uptime_consecutive_failures` absent; a spot-checked row shows the real THRESHOLDS warn/crit
    - ThresholdsPanel imports THRESHOLDS from `@/lib/observability/config` and does not redeclare the values
    - `npx tsc --noEmit` reports no new errors from this file
  </acceptance_criteria>
  <done>ThresholdsPanel renders the real 7-metric threshold values (excluding spend + uptime) with a badged v2 readings column, locked by an automated row-set test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server render → client bundle | dashboard status components must not leak vendor keys/monitoring secrets into the browser bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-03 | Information Disclosure | dashboard status components | high | mitigate | StatusBanner/DigestPanel/ThresholdsPanel render only status enums + committed config threshold numbers — no secrets, tokens, or DSNs; the existing `__tests__/no-public-monitoring-secret.test.ts` repo-wide guard continues to assert no browser-prefixed monitoring secret exists |
| T-33-08 | Tampering | digest email double-send | medium | mitigate | The digest render reuses only the classification loop, never the alert fan-out — `grep observability/alerts lib/playbook/digest.ts == 0` guards against accidentally importing/dispatching the daily email on every page view (D-08) |
| T-33-07 | Spoofing | health signal via self-HTTP | low | mitigate | Health is re-checked in-process via `import { GET as checkHealth }` (no self-`fetch`), so no base-URL resolution or extra network hop can be spoofed/intercepted |
| T-33-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `getDashboardHealth()` + `buildDigestTodayRow()` — `lib/playbook/digest.ts`
- `StatusBanner` (3 health states) — `components/playbook/StatusBanner.tsx`
- `DigestPanel` (single live today row + v2 note) — `components/playbook/DigestPanel.tsx`
- `ThresholdsPanel` (7-row real-values allowlist) — `components/playbook/ThresholdsPanel.tsx`
- Wave 0 tests: `__tests__/playbook-digest.test.ts`, `__tests__/playbook-status-banner.test.tsx`, `__tests__/playbook-thresholds-panel.test.tsx`
</artifacts_produced>

<verification>
- `npx jest __tests__/playbook-digest.test.ts __tests__/playbook-status-banner.test.tsx __tests__/playbook-thresholds-panel.test.tsx` green
- digest.ts uses in-process health re-check (no fetch), no alerts import
- ThresholdsPanel renders exactly the 7 allowlisted metrics
- `npx tsc --noEmit` clean for the new files
</verification>

<success_criteria>
The dashboard's live-signal core is complete and honest: in-process health re-check driving a 3-state banner, a single live digest today-row that never sends email, and a thresholds panel showing the real config values across the 7 allowlisted metrics with a badged v2 readings column.
</success_criteria>

<output>
Create `.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-07-SUMMARY.md` when done.
</output>
