---
phase: 33
plan: 08
type: execute
wave: 2
depends_on: [33-01, 33-04, 33-07]
files_modified:
  - components/playbook/VendorsGrid.tsx
  - components/playbook/QuickLinks.tsx
  - app/(admin)/playbook/it/dashboard/page.tsx
autonomous: true
requirements: [PLAYBOOK-04, PLAYBOOK-07, PLAYBOOK-08, PLAYBOOK-09, PLAYBOOK-10]

must_haves:
  truths:
    - "The dashboard page calls requireStaffPage(['leadership','it']) BEFORE any health re-check / render (fail closed)"
    - "App Health tile + global status banner reflect the live in-process /api/health re-check"
    - "The Uptime tile + panel are a Better Stack link-out to https://funun.betteruptime.com — no fabricated per-route %s or sparklines"
    - "VendorsGrid has 10 cells with real deep-links; site-critical vendors (Vercel/Supabase/DNS) carry the rose dot"
    - "v2-deferred surfaces (Spend $— /$200, per-vendor status dots, threshold readings) are shown with explicit v2 badges, not omitted and not fabricated"
  artifacts:
    - components/playbook/VendorsGrid.tsx
    - components/playbook/QuickLinks.tsx
    - app/(admin)/playbook/it/dashboard/page.tsx
  key_links:
    - "dashboard page -> requireStaffPage (33-01) BEFORE getDashboardHealth (33-07)"
    - "dashboard page -> StatusBanner/DigestPanel/ThresholdsPanel (33-07) + VendorsGrid/QuickLinks (this plan) + ItRoomTopBar (33-04)"
  prohibitions:
    - "No fabricated uptime %s / sparklines / 'updated 12s ago' freshness chrome (D-07 honesty rule)"
    - "No vendor key/token/monitoring secret in the client bundle — vendor cells are public dashboard/status URLs only"
    - "Never widen the page guard to ALL_STAFF_ROLES (D-02)"
---

<objective>
Assemble the bespoke Monitoring Dashboard opening page — the IT room's index — composing the live-signal core (33-07) with the reference panels, under its own inline guard (D-02, PLAYBOOK-04). Ship the vendors grid + quick links, the Better Stack uptime link-out (D-07, PLAYBOOK-10), and the badged v2 placeholders (D-09).

Purpose: this is the "live health + honest reference" board — live where a signal exists (`/api/health`, real thresholds, live digest), honest link-outs / badged placeholders everywhere a live feed does not yet exist. No fake-live numbers (the one honesty line the owner drew).
Output: `VendorsGrid`, `QuickLinks`, and `app/(admin)/playbook/it/dashboard/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-CONTEXT.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-UI-SPEC.md
@docs/observability/VENDOR-DIRECTORY.md
@docs/observability/UPTIME-MONITORING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: VendorsGrid (10 live deep-link cells) + QuickLinks (4 cards)</name>
  <files>components/playbook/VendorsGrid.tsx, components/playbook/QuickLinks.tsx</files>
  <read_first>
    - 33-UI-SPEC.md "Vendors panel" (10 cells 2-col: Vercel, Supabase, DNS/Registrar, Sentry, Better Stack, Stripe, Resend, Anthropic, DocuSeal, Google Places; excludes GitHub; rose dot for Vercel/Supabase/DNS site-critical; footer note verbatim; the real dashboard/status URLs list) + "Quick links (Jump to)" (4 cards)
    - docs/observability/VENDOR-DIRECTORY.md (source of the vendor list + the "⚑ Open gaps to resolve once: DNS registrar · DocuSeal status URL" caveat)
    - docs/observability/UPTIME-MONITORING.md (confirms the status-page URL https://funun.betteruptime.com)
  </read_first>
  <action>
    Create `components/playbook/VendorsGrid.tsx` — a 10-cell 2-column grid (excluding GitHub, which is deploy-time-only, per the UI-SPEC), each cell an `<a>` to that vendor's REAL dashboard/status URL (the UI-SPEC lists them: vercel.com/dashboard + vercel-status.com, supabase.com/dashboard + status.supabase.com, sentry.io + status.sentry.io, betterstack.com + funun.betteruptime.com, dashboard.stripe.com + status.stripe.com, resend.com + status.resend.com, console.anthropic.com + status.anthropic.com, Google Cloud Console + status.cloud.google.com). Vercel/Supabase/DNS carry the rose `.crit` site-critical dot; the rest use the neutral `.norm` dot. For the two flagged open-confirm URLs (DNS registrar, DocuSeal status) do NOT fabricate a URL — carry the VENDOR-DIRECTORY.md caveat forward (a visible "confirm URL" affordance or the vendor's known base, per the doc's own open-gap note), never an invented link. Footer note verbatim from the UI-SPEC. Create `components/playbook/QuickLinks.tsx` — the 4 "Jump to" cards (Incident Runbook, Vendor Directory, Status page → https://funun.betteruptime.com, Operating Rhythm), each an internal deep-link except the external status page. These are static public URLs only — no keys/tokens anywhere.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "VendorsGrid|QuickLinks" || echo "vendors/quicklinks type OK"</automated>
  </verify>
  <acceptance_criteria>
    - VendorsGrid renders 10 cells (GitHub excluded); Vercel/Supabase/DNS carry the site-critical rose dot
    - DNS registrar + DocuSeal status are NOT given fabricated URLs (carry the VENDOR-DIRECTORY open-confirm caveat)
    - QuickLinks renders 4 cards; the Status-page card points to https://funun.betteruptime.com
    - No `NEXT_PUBLIC_`/token/DSN literal appears in either file (public URLs only)
    - `npx tsc --noEmit` reports no new errors from these files
  </acceptance_criteria>
  <done>VendorsGrid + QuickLinks are live deep-link surfaces with honest site-critical marking and no fabricated URLs or secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Monitoring Dashboard page — guard, live tiles/banner, uptime link-out, panels</name>
  <files>app/(admin)/playbook/it/dashboard/page.tsx</files>
  <read_first>
    - 33-UI-SPEC.md "Stat tiles" (4-up: App Health live, Uptime replaced with "View live status →" link-out, Spend `$— / $200 cap` v2, Incidents `0` / "none active") + "Uptime panel" (structural replacement, not a restyle) + "Global status banner" + "Access & Role Gating"
    - lib/admin/gate.ts (requireStaffPage — 33-01), lib/playbook/digest.ts (getDashboardHealth, buildDigestTodayRow — 33-07)
    - components/playbook/{StatusBanner,DigestPanel,ThresholdsPanel}.tsx (33-07), components/playbook/{VendorsGrid,QuickLinks}.tsx (Task 1), components/playbook/ItRoomTopBar.tsx (33-04)
  </read_first>
  <action>
    Create `app/(admin)/playbook/it/dashboard/page.tsx` as a Server Component (the IT room index page): (1) `await requireStaffPage(['leadership','it'])` FIRST — before any health re-check or render (fail closed, D-02; never widen to ALL_STAFF_ROLES); (2) `<ItRoomTopBar crumb="Monitoring Dashboard" showLiveChip />` (the Live chip is honest here — this page reads live data); (3) `const health = await getDashboardHealth()` then `<StatusBanner status={health} />` and the 4-up tiles row — App Health (live from `health`), Uptime (the "View live status →" link-out to https://funun.betteruptime.com — NOT a numeric % tile, D-07/PLAYBOOK-10), Spend (`$— / $200 cap` + "v2 live figure" badge, D-09), Incidents (`0` / "none active", honest static zero); (4) the Uptime panel STRUCTURAL replacement (a single centered "No live per-route data in v1" call-to-action linking to Better Stack — not a per-route row list, not fake "coming soon" route names, D-07); (5) `<ThresholdsPanel />`, `<VendorsGrid />`, `<DigestPanel status={health} />`, `<QuickLinks />`. Drop all fabricated freshness chrome ("updated 12s ago", per-route %s, decorative sparklines) — the "Live" chip is shown only on this health-driven page. Compose to the observability-dashboard.html structure per the UI-SPEC.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "playbook/it/dashboard" || echo "dashboard page type OK"</automated>
  </verify>
  <acceptance_criteria>
    - The page's `requireStaffPage(['leadership','it'])` call appears textually BEFORE the `getDashboardHealth()` call (fail-closed ordering — verify by reading the file)
    - App Health tile + StatusBanner are driven by the live `getDashboardHealth()` result
    - The Uptime tile + Uptime panel are Better Stack link-outs to https://funun.betteruptime.com with NO numeric per-route % or sparkline (grep for absence of a hardcoded "99.9%"-style literal)
    - Spend/Incidents/threshold-readings v2 surfaces carry explicit v2 badges (not omitted, not fabricated)
    - `grep -c "fetch(" "app/(admin)/playbook/it/dashboard/page.tsx"` == 0 for any `/api/health` self-fetch (health comes via getDashboardHealth's in-process import)
    - `npx tsc --noEmit` reports no new errors from this file
  </acceptance_criteria>
  <done>The Monitoring Dashboard renders live health (tile + banner), an honest Better Stack uptime link-out, real thresholds, the vendors grid, a live digest today-row, and quick links — self-guarded to leadership+it, with every non-live surface badged v2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session → dashboard page | untrusted staff identity must be gated before the live health board renders |
| server render → client bundle | vendor links + status board must not leak vendor keys/monitoring secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Elevation of Privilege | forgotten/mis-scoped dashboard guard | high | mitigate | `requireStaffPage(['leadership','it'])` is the first statement, before any render (fail closed, D-02); never widened to ALL_STAFF_ROLES — verifiable by reading the page |
| T-33-03 | Information Disclosure | vendor grid / dashboard secrets in client bundle | high | mitigate | Vendor cells + quick links are public dashboard/status URLs only — no keys, tokens, or DSNs; the existing `__tests__/no-public-monitoring-secret.test.ts` repo-wide guard continues to assert no browser-prefixed monitoring secret exists in source |
| T-33-07 | Spoofing | health signal | low | mitigate | Health is re-checked in-process (getDashboardHealth's `import { GET as checkHealth }`), never via a self-`fetch` the page constructs |
| T-33-09 | Repudiation/Integrity | fabricated live numbers | medium | mitigate | D-07 honesty rule enforced: no fake uptime %s / sparklines / freshness chrome; the Live chip appears only on the health-driven page; v2 surfaces are explicitly badged, not fabricated |
| T-33-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `VendorsGrid` (10 live deep-link cells, site-critical marking) — `components/playbook/VendorsGrid.tsx`
- `QuickLinks` (4 "Jump to" cards) — `components/playbook/QuickLinks.tsx`
- `/admin/playbook/it/dashboard` — the bespoke Monitoring Dashboard opening page — `app/(admin)/playbook/it/dashboard/page.tsx`
</artifacts_produced>

<verification>
- `npx tsc --noEmit` clean for the three files
- Dashboard self-guards before the health re-check; App Health + banner are live; Uptime is a link-out (no fabricated %)
- No secret in the client bundle (existing no-public-monitoring-secret test stays green: `npx jest __tests__/no-public-monitoring-secret.test.ts`)
- Manual UAT: load with /api/health at 200 (healthy chip/banner) and forced 503 (degraded banner/tile)
</verification>

<success_criteria>
The Monitoring Dashboard is a live-health + honest-reference board: live App Health tile/banner + digest, real thresholds, a 10-vendor deep-link grid, a Better Stack uptime link-out with zero fabricated numbers, and badged v2 placeholders — all behind the page's own leadership+it inline guard.
</success_criteria>

<output>
Create `.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-08-SUMMARY.md` when done.
</output>
