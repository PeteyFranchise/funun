---
phase: 33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/admin/staff-role.ts
  - lib/admin/gate.ts
  - __tests__/staff-role-it.test.ts
autonomous: true
requirements: [PLAYBOOK-01, PLAYBOOK-04]

must_haves:
  truths:
    - "getStaffRole({app_metadata:{staff_role:'it'}}) returns 'it'"
    - "ALL_STAFF_ROLES contains 'it' and still contains leadership/ae/bd/anr"
    - "requireStaffPage(['leadership','it']) returns {user,staffRole} for an it or leadership caller and redirects everyone else (fail closed)"
    - "is_admin=true still resolves to 'leadership' (owner bootstrap unaffected)"
  artifacts:
    - lib/admin/staff-role.ts
    - lib/admin/gate.ts
    - __tests__/staff-role-it.test.ts
  key_links:
    - "requireStaffPage() -> getStaffRole() -> app_metadata.staff_role (single authority, no parallel path)"
  prohibitions:
    - "No existing requireStaff()/verifyAdmin()/getStaffRole() signature or behavior changed for leadership/ae/bd/anr"
    - "requireStaffPage() must NOT return a NextResponse/JSON error shape (it is a page-context guard, not an API guard)"
---

<objective>
Ship the access-control primitives the entire IT-room gate depends on: add the new `it` StaffRole (D-01) and a page-context guard helper `requireStaffPage()` that every IT-room page will call inline (D-02, D-04).

Purpose: `it` is a forward-compatible single-slot role (D-03) for a future non-leadership IT hire; the owner reaches the IT room via the leadership branch. `requireStaffPage()` closes RESEARCH Pitfall 2 — `requireStaff()` returns an API-route-shaped `{error,status}` that a Server Component page cannot use, so pages need a `redirect()`-based equivalent.
Output: widened `StaffRole` union + `ALL_STAFF_ROLES` + `getStaffRole()` branch, a new `requireStaffPage()` export, and a Wave 0 test locking all of it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-CONTEXT.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the `it` StaffRole to the union, ALL_STAFF_ROLES, and getStaffRole()</name>
  <files>lib/admin/staff-role.ts, __tests__/staff-role-it.test.ts</files>
  <read_first>
    - lib/admin/staff-role.ts (the file being modified — note the existing `'anr'` addition comment block at lines 9-15 and the four-way `if` in getStaffRole)
    - lib/admin/gate.test.ts (existing getStaffRole test conventions to mirror — typed fixtures, fail-closed cases)
    - 33-PATTERNS.md section "lib/admin/staff-role.ts (config, MODIFY)" (verbatim pattern to replicate)
  </read_first>
  <behavior>
    - ALL_STAFF_ROLES includes 'it' (length 5: leadership, ae, bd, anr, it)
    - getStaffRole({app_metadata:{staff_role:'it'}}) === 'it'
    - getStaffRole for 'leadership'/'ae'/'bd'/'anr' unchanged
    - getStaffRole({app_metadata:{is_admin:true}}) === 'leadership' (unchanged)
    - getStaffRole({}) === null and never throws (fail closed, unchanged)
    - getStaffRole({app_metadata:{staff_role:'superadmin'}}) === null (unrecognized rejected)
  </behavior>
  <action>
    Append `| 'it'` to the `StaffRole` union, add `'it'` to the `ALL_STAFF_ROLES` array, and add a fourth `meta?.staff_role === 'it'` branch to the `if` in `getStaffRole()` — mirroring the existing `'anr'` addition exactly (D-01). Add a header comment block in the same style as the existing `'anr'` block (lines 9-15) explaining that `'it'` ships live before migration `114_it_staff_role.sql` is applied because getStaffRole reads only `app_metadata` (never the DB) so no `funun_staff` row can hold `'it'` until the owner assigns it — cite Phase 33 / D-01 and note `it`'s narrow authority (Playbook IT-room read access only) and that the single-role model stands (D-03: a person is `it` OR `leadership`, never both). Do NOT touch the `is_admin → leadership` fallback. Write the Wave 0 test `__tests__/staff-role-it.test.ts` FIRST (RED) covering every behavior above, then implement (GREEN).
  </action>
  <verify>
    <automated>npx jest __tests__/staff-role-it.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/staff-role-it.test.ts` passes with the behaviors above asserted
    - `grep -c "'it'" lib/admin/staff-role.ts` returns ≥ 2 (union + ALL_STAFF_ROLES) [positive]
    - `npx tsc --noEmit` reports no new errors introduced by this file (ts-jest is transpile-only; type-check separately)
  </acceptance_criteria>
  <done>StaffRole union, ALL_STAFF_ROLES, and getStaffRole() recognize 'it'; existing roles + is_admin fallback unchanged; Wave 0 test green.</done>
</task>

<task type="auto">
  <name>Task 2: Add requireStaffPage() page-context guard to lib/admin/gate.ts</name>
  <files>lib/admin/gate.ts, __tests__/staff-role-it.test.ts</files>
  <read_first>
    - lib/admin/gate.ts (the file being modified — the existing requireStaff() shape and RequireStaffResult type)
    - app/(admin)/layout.tsx lines 14-28 (the canonical page-context idiom: createServerClient + getUser + `if (!user) redirect('/signin')` + `if (!role) redirect('/')`)
    - 33-RESEARCH.md "Common Pitfall 2: Treating requireStaff() as page-ready as-is" (why a page needs redirect(), not a NextResponse)
  </read_first>
  <action>
    Add an exported async `requireStaffPage(allowed: StaffRole[] = ALL_STAFF_ROLES)` to `lib/admin/gate.ts` that mirrors `app/(admin)/layout.tsx`'s idiom, NOT `requireStaff()`'s API shape (D-02, RESEARCH Pitfall 2): resolve the session with `createServerClient()` from `@/lib/supabase/server` (add the import), `getUser()`, then `redirect('/signin')` when there is no user, and `redirect('/')` when `getStaffRole(user)` is null or not in `allowed`. On success return `{ user, staffRole }`. It must never construct or return a `NextResponse`/`{error,status}` object — `redirect()` throws internally and never returns, so callers can treat the return as an authorized result. Leave `requireStaff()` and `verifyAdmin()` untouched (single authority, D-01 of Phase 25). Extend `__tests__/staff-role-it.test.ts` with a `requireStaffPage` describe block: mock `@/lib/supabase/server` (createServerClient) and `next/navigation` (redirect throws a sentinel) — assert an `it` caller and a `leadership` caller both return `{user, staffRole}`, an `ae` caller triggers `redirect('/')`, and a null session triggers `redirect('/signin')`.
  </action>
  <verify>
    <automated>npx jest __tests__/staff-role-it.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/staff-role-it.test.ts` passes including the requireStaffPage cases (it/leadership authorized; ae → redirect('/'); no session → redirect('/signin'))
    - `grep -c "requireStaffPage" lib/admin/gate.ts` returns ≥ 1 [positive]
    - `grep -c "NextResponse" lib/admin/gate.ts` is unchanged from before (requireStaffPage adds no NextResponse usage)
    - `npx tsc --noEmit` introduces no new errors from gate.ts
  </acceptance_criteria>
  <done>requireStaffPage() exists, fail-closed via redirect(), returns {user,staffRole} on success; existing gate exports unchanged; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session → page Server Component | untrusted staff identity (app_metadata.staff_role) crosses into the IT-room render decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Elevation of Privilege | requireStaffPage() page guard | high | mitigate | Guard fails closed via redirect() when role is null or not in `allowed`; getStaffRole reads app_metadata fresh each request (no cache to go stale); Wave 0 test asserts an `ae` caller is redirected, an `it`/`leadership` caller passes |
| T-33-01b | Elevation of Privilege | `it` union widening | medium | mitigate | `it` is additive only; no existing role's authority changes; `it` carries Playbook IT-room read access only (comment-documented, D-01); single-slot model (D-03) prevents role stacking |
| T-33-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan (pure TS edits); supply-chain gate applies to 33-03 where react-markdown/remark-gfm are added (both audited OK in 33-RESEARCH.md) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `StaffRole` union value `'it'` (+ `ALL_STAFF_ROLES` entry) — `lib/admin/staff-role.ts`
- `getStaffRole()` `'it'` recognition branch — `lib/admin/staff-role.ts`
- `requireStaffPage(allowed)` page-context guard helper — `lib/admin/gate.ts`
- `__tests__/staff-role-it.test.ts` — Wave 0 role + guard test
</artifacts_produced>

<verification>
- `npx jest __tests__/staff-role-it.test.ts` green
- `npx tsc --noEmit` introduces no new type errors
- `grep -c "'it'" lib/admin/staff-role.ts` ≥ 2; `grep -c "requireStaffPage" lib/admin/gate.ts` ≥ 1
</verification>

<success_criteria>
`it` is a recognized StaffRole across union/ALL_STAFF_ROLES/getStaffRole; `requireStaffPage()` is a fail-closed page guard returning `{user,staffRole}` for authorized callers and redirecting all others; every existing gate/role behavior is byte-for-byte preserved for leadership/ae/bd/anr and the is_admin bootstrap.
</success_criteria>

<output>
Create `.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-01-SUMMARY.md` when done.
</output>
