---
phase: 33
plan: 05
type: execute
wave: 2
depends_on: [33-01, 33-04]
files_modified:
  - app/(admin)/layout.tsx
  - app/(admin)/playbook/layout.tsx
  - app/(admin)/playbook/page.tsx
autonomous: true
requirements: [PLAYBOOK-02, PLAYBOOK-03]

must_haves:
  truths:
    - "Rail 1 shows a 'The Playbook' link to ALL staff (outside the isLeadership block), as the first nav item"
    - "/admin/playbook renders Rail 2 (double-sidebar) inside the existing admin shell"
    - "The nested layout computes canSeeItRoom = role is leadership or it, server-side, and passes it to Rail2"
    - "/admin/playbook redirects leadership/it staff to /admin/playbook/it/dashboard; other staff see the coming-soon landing (Rail 2 ghosts only)"
  artifacts:
    - app/(admin)/layout.tsx
    - app/(admin)/playbook/layout.tsx
    - app/(admin)/playbook/page.tsx
  key_links:
    - "playbook/layout.tsx getStaffRole(user) -> canSeeItRoom -> Rail2 prop (D-06 render-time visibility)"
    - "PlaybookNavLink mounted in app/(admin)/layout.tsx Rail 1 (all staff, D-04)"
  prohibitions:
    - "Do NOT restyle Rail 1 or add active-state to any other Rail 1 link (only the new Playbook link, via PlaybookNavLink)"
    - "The nested layout's canSeeItRoom is render-time visibility ONLY — it is NOT the access authority (each IT page self-guards, D-02)"
---

<objective>
Wire the Playbook route shell: add the Rail 1 "The Playbook" entry for all staff (D-04, PLAYBOOK-02) and the nested `/admin/playbook` layout that renders Rail 2 (double-sidebar) with the role-conditional IT room (D-05, D-06, PLAYBOOK-03), plus the index redirect.

Purpose: this is the URL-driven, no-client-state nesting that turns 33-04's primitives into the live double-sidebar surface, reusing the existing admin shell gate (which already admits all staff).
Output: modified `app/(admin)/layout.tsx`, new `app/(admin)/playbook/layout.tsx`, new `app/(admin)/playbook/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-CONTEXT.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-UI-SPEC.md
@app/(admin)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount PlaybookNavLink as the first Rail 1 nav item (all staff)</name>
  <files>app/(admin)/layout.tsx</files>
  <read_first>
    - app/(admin)/layout.tsx (the file being modified — the `<nav>` block, the `Admin` label, the `isLeadership && (...)` group, and the existing NAV_LINK_CLASS links)
    - 33-UI-SPEC.md "Rail reconciliation" #1 (placement: first item, directly under the "Admin" label, BEFORE "Checklist Items", OUTSIDE the isLeadership block; do not restyle Rail 1)
    - components/playbook/PlaybookNavLink.tsx (created 33-04)
  </read_first>
  <action>
    Import `PlaybookNavLink` from `@/components/playbook/PlaybookNavLink` and render `<PlaybookNavLink />` as the FIRST nav item, directly after the "Admin" label `<p>` and BEFORE the `{isLeadership && (...)}` group — so it is visible to every staff role (D-04, PLAYBOOK-02). Do not add active-state to any other Rail 1 link and do not restyle the sidebar (UI-SPEC: consistency with the other links outranks the mockup's fancier Rail 1 chrome). Leave the existing gate (createServerClient + getStaffRole + redirects) untouched — it already admits all staff, which is exactly what the Playbook link needs.
  </action>
  <verify>
    <automated>grep -q "PlaybookNavLink" "app/(admin)/layout.tsx" && npx tsc --noEmit 2>&1 | grep -E "app/\(admin\)/layout" || echo "layout OK"</automated>
  </verify>
  <acceptance_criteria>
    - `app/(admin)/layout.tsx` imports and renders `<PlaybookNavLink />`
    - The link is rendered OUTSIDE the `isLeadership && (...)` block (visible to all staff) and before "Checklist Items"
    - No other Rail 1 link gains active-state; the sidebar structure/classes are otherwise unchanged
    - `npx tsc --noEmit` reports no new errors from this file
  </acceptance_criteria>
  <done>Every staff member sees "The Playbook" as the top Rail 1 entry; the existing gate and sidebar styling are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Nested /admin/playbook layout — Rail 2 shell + server-side canSeeItRoom</name>
  <files>app/(admin)/playbook/layout.tsx</files>
  <read_first>
    - app/(admin)/layout.tsx lines 14-28 (the createServerClient + getStaffRole idiom to reproduce; layouts do not share request state with parents so read the role again)
    - 33-UI-SPEC.md "Rail reconciliation" #2 + #3 (Rail 2 width/structure; content column chrome `.ctop`/`.cwrap`) + "Responsive collapse" (<1000px single-column, CSS-only)
    - components/playbook/Rail2.tsx (created 33-04)
  </read_first>
  <action>
    Create `app/(admin)/playbook/layout.tsx` as a Server Component: resolve the session with `createServerClient()` + `getUser()`, compute `const role = getStaffRole(user)` and `const canSeeItRoom = role === 'leadership' || role === 'it'` (D-06), and render a two-column shell — `<Rail2 canSeeItRoom={canSeeItRoom} />` beside `{children}` — inside the existing `.fncon` admin wrapper the parent layout already provides (no new theme provider, no new data-theme logic; inherit ADMIN_CONSOLE_CSS). Apply the `<1000px` single-column CSS reflow per the UI-SPEC (pure CSS media query, no JS toggle — matches "URL-driven, no client state"). Do NOT add a redirect here beyond what the parent admin layout enforces; this layout's only new responsibility is Rail 2 + the render-time IT-room visibility. Note in a comment that `canSeeItRoom` is render-time visibility only and each IT page carries its own inline guard (D-02).
  </action>
  <verify>
    <automated>test -f "app/(admin)/playbook/layout.tsx" && grep -q "canSeeItRoom" "app/(admin)/playbook/layout.tsx" && npx tsc --noEmit 2>&1 | grep -E "playbook/layout" || echo "playbook layout OK"</automated>
  </verify>
  <acceptance_criteria>
    - `app/(admin)/playbook/layout.tsx` exists and renders `<Rail2 canSeeItRoom={...} />` beside `{children}`
    - `canSeeItRoom` is computed server-side as `role === 'leadership' || role === 'it'` (grep the expression)
    - No new `data-theme`/theme provider is introduced (inherits the parent `.fncon`)
    - `npx tsc --noEmit` reports no new errors from this file
  </acceptance_criteria>
  <done>/admin/playbook renders the double-sidebar Rail 2 with the IT room visible only to leadership/it, inside the existing admin shell.</done>
</task>

<task type="auto">
  <name>Task 3: /admin/playbook index — redirect authorized staff to the IT dashboard</name>
  <files>app/(admin)/playbook/page.tsx</files>
  <read_first>
    - 33-CONTEXT.md "Claude's Discretion" (URL structure: index → redirect authorized staff to the IT dashboard) + D-06 (non-authorized staff see only ghosts)
    - app/(admin)/layout.tsx lines 14-28 (getStaffRole idiom)
  </read_first>
  <action>
    Create `app/(admin)/playbook/page.tsx` as a Server Component: resolve the role (createServerClient + getStaffRole), and if `role === 'leadership' || role === 'it'` call `redirect('/admin/playbook/it/dashboard')` (the IT room's index/opening page). Otherwise render a minimal in-`.cwrap` landing body for non-authorized staff — a short "Coming soon" message consistent with the UI-SPEC (Rail 2 already shows the five ghost rooms; The Playbook is entirely "coming soon" for them in v1). No client state.
  </action>
  <verify>
    <automated>test -f "app/(admin)/playbook/page.tsx" && grep -q "/admin/playbook/it/dashboard" "app/(admin)/playbook/page.tsx" && npx tsc --noEmit 2>&1 | grep -E "playbook/page" || echo "playbook page OK"</automated>
  </verify>
  <acceptance_criteria>
    - `/admin/playbook` redirects `leadership`/`it` to `/admin/playbook/it/dashboard`
    - Non-authorized staff get a coming-soon landing (no redirect into the IT room)
    - `npx tsc --noEmit` reports no new errors from this file
  </acceptance_criteria>
  <done>The Playbook index sends authorized staff straight to the Monitoring Dashboard and shows everyone else the coming-soon landing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session → nested layout/page | staff role determines Rail 2 IT-room visibility + the index redirect target |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-04 | Information Disclosure | Rail 2 IT-room visibility (render-time) | medium | mitigate | `canSeeItRoom` computed server-side; when false the IT room is DOM-omitted (D-06). This is visibility only — the authoritative gate is each IT page's inline guard (33-06/33-08, D-02); the index redirect only sends already-authorized staff to the dashboard |
| T-33-01 | Elevation of Privilege | index redirect | low | mitigate | Redirect to the IT dashboard fires only for `leadership`/`it`; the dashboard page independently self-guards, so a mis-scoped redirect cannot grant access on its own |
| T-33-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- Rail 1 "The Playbook" entry (PlaybookNavLink mount) — `app/(admin)/layout.tsx`
- `/admin/playbook` nested layout (Rail 2 shell + server-side canSeeItRoom) — `app/(admin)/playbook/layout.tsx`
- `/admin/playbook` index page (redirect authorized → it/dashboard) — `app/(admin)/playbook/page.tsx`
</artifacts_produced>

<verification>
- `npx tsc --noEmit` clean for the three files
- Rail 1 link visible to all staff (outside isLeadership); Rail 2 renders with canSeeItRoom; index redirects authorized to it/dashboard
- Manual UAT: a non-it/non-leadership staff account sees Rail 2 with only the five ghosts and no IT room
</verification>

<success_criteria>
The double-sidebar Playbook shell is live under `/admin/playbook`: a Rail 1 entry for all staff, a Rail 2 that hides the IT room from non-authorized staff, and an index that routes authorized staff to the Monitoring Dashboard.
</success_criteria>

<output>
Create `.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-05-SUMMARY.md` when done.
</output>
