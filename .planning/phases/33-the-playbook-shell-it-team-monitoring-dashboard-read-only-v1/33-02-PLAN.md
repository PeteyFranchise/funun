---
phase: 33
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/114_it_staff_role.sql
autonomous: false
requirements: [PLAYBOOK-01]
user_setup:
  - service: supabase
    why: "Widen funun_staff.staff_role CHECK to admit 'it' — OWNER-RUN migration (this project never runs `supabase db push` from an agent)"
    dashboard_config:
      - task: "Apply migration 114 via `supabase db push` (or the owner's normal Codex flow), then confirm LOCAL=REMOTE parity with `supabase migration list`"
        location: "Supabase project — owner terminal (non-TTY hint: SUPABASE_ACCESS_TOKEN env var)"

must_haves:
  truths:
    - "supabase/migrations/114_it_staff_role.sql widens the funun_staff staff_role CHECK to ('leadership','ae','bd','anr','it')"
    - "The migration is HUMAN-GATED (owner-run); an agent never pushes it"
    - "Code recognizing 'it' (33-01) is verifiable green WITHOUT this migration applied (the anr/108 precedent) — code verification is decoupled from the push"
  artifacts:
    - supabase/migrations/114_it_staff_role.sql
  key_links:
    - "migration 114 CHECK ↔ lib/admin/staff-role.ts StaffRole union (DB display-copy stays in sync with the authoritative app_metadata.staff_role)"
  prohibitions:
    - "No agent-run `supabase db push` — the owner applies it"
    - "Do NOT gate build/tsc/jest on this migration being applied (no funun_staff row can hold 'it' until the owner assigns it)"
---

<objective>
Author the OWNER-RUN migration that widens the `funun_staff` `staff_role` CHECK to admit `'it'` (D-01), and document its application as a deployment prerequisite for actually assigning the `it` role — decoupled from code verification.

Purpose: this mirrors the `anr`/`108_anr_staff_role.sql` precedent exactly. The `it` code union (33-01) ships live and passes all automated verification WITHOUT this migration, because `getStaffRole()` reads only `app_metadata` and no `funun_staff` row can hold `'it'` until the owner assigns it. The migration only widens the DB-side display-copy CHECK so a future `it` staff row is insertable.
Output: `supabase/migrations/114_it_staff_role.sql` + an owner-run apply checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-CONTEXT.md
@supabase/migrations/108_anr_staff_role.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 114_it_staff_role.sql from the 108 template</name>
  <files>supabase/migrations/114_it_staff_role.sql</files>
  <read_first>
    - supabase/migrations/108_anr_staff_role.sql (the verbatim template — copy structure exactly, swap role name/number/phase refs)
    - 33-PATTERNS.md section "supabase/migrations/114_it_staff_role.sql (migration, CREATE)"
    - 33-CONTEXT.md D-01 (why owner-run, why code-before-migration is safe)
  </read_first>
  <action>
    Create `supabase/migrations/114_it_staff_role.sql` mirroring `108_anr_staff_role.sql`'s exact shape: a header comment block documenting WHY/WHAT/HUMAN-GATED referencing Phase 33 / D-01 and `it`'s narrow authority (Playbook IT-room read access only — never write/curation power); then `ALTER TABLE public.funun_staff DROP CONSTRAINT IF EXISTS funun_staff_staff_role_check;` followed by `ALTER TABLE public.funun_staff ADD CONSTRAINT funun_staff_staff_role_check CHECK (staff_role IN ('leadership', 'ae', 'bd', 'anr', 'it'));` then a `COMMENT ON CONSTRAINT` noting it was widened in migration 114 (Phase 33) to add `'it'` alongside 108's `'anr'`, that `staff_role` remains a DISPLAY COPY of the authoritative `app_metadata.staff_role`, and ending with `NOTIFY pgrst, 'reload schema';`. Keep the HUMAN-GATED disclaimer verbatim in spirit ("this project never runs `supabase db push` from an agent … the owner reviews and pushes"). Confirmed next available migration number is 114 (highest landed is 113). Do NOT edit any landed migration.
  </action>
  <verify>
    <automated>test -f supabase/migrations/114_it_staff_role.sql && grep -q "'leadership', 'ae', 'bd', 'anr', 'it'" supabase/migrations/114_it_staff_role.sql && grep -q "NOTIFY pgrst" supabase/migrations/114_it_staff_role.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/114_it_staff_role.sql` exists
    - Contains the widened CHECK list `('leadership', 'ae', 'bd', 'anr', 'it')`
    - Contains `DROP CONSTRAINT IF EXISTS funun_staff_staff_role_check`, `ADD CONSTRAINT`, and `NOTIFY pgrst, 'reload schema';`
    - Header comment references Phase 33 / D-01 and the HUMAN-GATED (owner-run) convention
    - No landed migration (080-113) is modified (`git status` shows only 114 added)
  </acceptance_criteria>
  <done>Migration 114 authored to the 108 template with the `it` CHECK-widen, HUMAN-GATED header, and pgrst reload — not yet applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner applies migration 114 (supabase db push) — deployment prerequisite, decoupled from code verification</name>
  <files>supabase/migrations/114_it_staff_role.sql</files>
  <action>Owner-run only — an agent never runs `supabase db push` (repo standing convention). The owner reviews migration 114, applies it via `supabase db push` (or the Codex flow used for 108/110-113; non-TTY hint: set `SUPABASE_ACCESS_TOKEN`), and confirms `supabase migration list` shows LOCAL=REMOTE through 114. This is a DEPLOYMENT prerequisite for actually assigning the `it` role to a `funun_staff` account — it does NOT gate build/tsc/jest for the rest of Phase 33 (33-01's code union verifies green without it, the anr/108 precedent). Execution may continue with this pending.</action>
  <what-built>`supabase/migrations/114_it_staff_role.sql` — the CHECK-widen to admit the `it` staff_role. This is a DEPLOYMENT prerequisite for assigning the `it` role to a real account; it is NOT required for any code verification in this phase to pass.</what-built>
  <how-to-verify>
    1. Review `supabase/migrations/114_it_staff_role.sql` (owner sign-off on the CHECK list + comment).
    2. Apply it via your normal flow: `supabase db push` (non-TTY hint: set `SUPABASE_ACCESS_TOKEN`), or the Codex `supabase db push` flow used for 108/110-113.
    3. Confirm parity: `supabase migration list` shows LOCAL=REMOTE through 114.
    4. Explicitly DO NOT block the rest of Phase 33 on this: 33-01's code union + all doc/dashboard verification (build/tsc/jest) pass without the migration applied, by design (the anr/108 precedent — no `funun_staff` row can hold `'it'` until you assign it).
  </how-to-verify>
  <resume-signal>Type "approved" once 114 is applied and `supabase migration list` shows LOCAL=REMOTE through 114 — or "defer" to continue phase execution with the migration pending (code verification is unaffected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner terminal → production DB | schema authority (the widened CHECK) crosses only via the owner-run push, never an agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01c | Elevation of Privilege | funun_staff staff_role CHECK widen | medium | mitigate | `it` is additive; the migration only widens the DB display-copy CHECK, granting no runtime authority itself — authority lives in code guards (33-01/33-06/33-08). Comment documents `it`'s narrow scope |
| T-33-02m | Tampering | agent-run migration push | high | mitigate | Human-gated: the apply step is a blocking checkpoint; an agent never runs `supabase db push` (repo standing convention, migrations 080-113 precedent) |
| T-33-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `supabase/migrations/114_it_staff_role.sql` — owner-run CHECK-widen adding `'it'` to `funun_staff.staff_role`
</artifacts_produced>

<verification>
- Migration file exists with the widened CHECK + NOTIFY pgrst (automated grep)
- Owner-run apply checkpoint is BLOCKING but explicitly decoupled from code verification
- No landed migration modified
</verification>

<success_criteria>
Migration 114 is authored to the exact 108 template shape (CHECK adds `'it'`, HUMAN-GATED header, pgrst reload); the apply step is a blocking owner checkpoint that does not gate build/tsc/jest for the rest of Phase 33.
</success_criteria>

<output>
Create `.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-02-SUMMARY.md` when done.
</output>
