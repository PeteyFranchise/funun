---
phase: 33
plan: 06
type: execute
wave: 2
depends_on: [33-01, 33-03, 33-04]
files_modified:
  - app/(admin)/playbook/it/vendor-directory/page.tsx
  - app/(admin)/playbook/it/runbook/page.tsx
  - app/(admin)/playbook/it/operating-rhythm/page.tsx
  - app/(admin)/playbook/it/thresholds/page.tsx
  - __tests__/playbook-docs-render.test.ts
autonomous: true
requirements: [PLAYBOOK-04, PLAYBOOK-05, PLAYBOOK-06]

must_haves:
  truths:
    - "Each of the 4 doc pages calls requireStaffPage(['leadership','it']) BEFORE reading any doc content (fail closed)"
    - "Each doc page renders its docs/observability/*.md source via MarkdownDoc — the .md file stays the single source of truth (D-10)"
    - "The page-to-file map matches D-10 exactly (vendor-directory=VENDOR-DIRECTORY.md, runbook=RUNBOOK.md, operating-rhythm=OPERATING-RHYTHM.md, thresholds=THRESHOLDS-AND-SEVERITY.md)"
  artifacts:
    - app/(admin)/playbook/it/vendor-directory/page.tsx
    - app/(admin)/playbook/it/runbook/page.tsx
    - app/(admin)/playbook/it/operating-rhythm/page.tsx
    - app/(admin)/playbook/it/thresholds/page.tsx
    - __tests__/playbook-docs-render.test.ts
  key_links:
    - "page -> requireStaffPage(['leadership','it']) (33-01) runs BEFORE readObservabilityDoc()"
    - "page -> readObservabilityDoc(DOC_PAGE_FILE[slug]) (33-03/33-04) -> MarkdownDoc (33-03)"
  prohibitions:
    - "The guard MUST run before the file read (fail closed, not fail open)"
    - "Never widen the guard to ALL_STAFF_ROLES — always the explicit ['leadership','it'] array (D-02)"
    - "No content duplication — pages render from the .md files, never inlined prose"
---

<objective>
Ship the 4 IT-room doc pages that render `docs/observability/*.md` straight from source (D-10, PLAYBOOK-05), each carrying its own inline `requireStaffPage(['leadership','it'])` guard (D-02, PLAYBOOK-04), depending on the deploy-verified file tracing (PLAYBOOK-06).

Purpose: the `.md` files stay the single source of truth — doc edits reflect automatically, no content duplication. Every page self-guards so the layout gate is never the sole authority.
Output: `vendor-directory`, `runbook`, `operating-rhythm`, `thresholds` pages + a Wave 0 page-to-file render test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-CONTEXT.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-UI-SPEC.md
@.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 4 IT doc pages — inline guard + top bar + MarkdownDoc render</name>
  <files>app/(admin)/playbook/it/vendor-directory/page.tsx, app/(admin)/playbook/it/runbook/page.tsx, app/(admin)/playbook/it/operating-rhythm/page.tsx, app/(admin)/playbook/it/thresholds/page.tsx</files>
  <read_first>
    - 33-PATTERNS.md section "IT-room pages (5 files)" (the per-page guard-before-read pattern; never widen to ALL_STAFF_ROLES)
    - 33-RESEARCH.md "Pattern 3" (page shape: guard then readObservabilityDoc then MarkdownDoc)
    - 33-UI-SPEC.md "Doc-Page Markdown Container Contract" (page chrome above the markdown: top bar + a max-width 900px container) + "Page title simplification"
    - lib/admin/gate.ts (requireStaffPage from 33-01), lib/playbook/nav.ts (DOC_PAGE_FILE from 33-04), lib/playbook/read-doc.ts + components/playbook/MarkdownDoc.tsx (from 33-03), components/playbook/ItRoomTopBar.tsx (from 33-04)
  </read_first>
  <action>
    Create the 4 doc page Server Components under `app/(admin)/playbook/it/{vendor-directory,runbook,operating-rhythm,thresholds}/page.tsx`. Each page, in order: (1) `await requireStaffPage(['leadership','it'])` as the FIRST statement — the guard runs BEFORE any content read (fail closed; never widen the array to ALL_STAFF_ROLES, D-02); (2) render `<ItRoomTopBar crumb="{its label}" />` (no Live chip — doc pages are not the dashboard); (3) `const md = await readObservabilityDoc(DOC_PAGE_FILE['{slug}'])` reading the filename from the nav.ts map (never hardcoded per page), then `<MarkdownDoc content={md} />` inside the UI-SPEC max-width 900px container. The source `.md`'s own leading title + first paragraph render as the page h1 + lede via markdownComponents styling (no extraction logic — UI-SPEC "Page title simplification"). Do not inline any doc prose — the source `.md` is the only content.
  </action>
  <verify>
    <automated>npx jest __tests__/playbook-docs-render.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - All 4 page files exist under `app/(admin)/playbook/it/`
    - Each page's guard `requireStaffPage(['leadership','it'])` appears textually BEFORE its `readObservabilityDoc(` call (fail-closed ordering — verify by reading each file)
    - No page passes `ALL_STAFF_ROLES` to the guard; each uses the explicit `['leadership','it']` array
    - Each page reads its filename from `DOC_PAGE_FILE` (nav.ts), not a hardcoded string literal
    - `npx tsc --noEmit` reports no new errors from these files
  </acceptance_criteria>
  <done>The 4 doc pages render their `.md` source via MarkdownDoc, each self-guarded to leadership+it before any read.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test — page-to-file map + content renders for all 4 docs</name>
  <files>__tests__/playbook-docs-render.test.ts</files>
  <read_first>
    - 33-RESEARCH.md "Validation Architecture" (PLAYBOOK-05/06 test map; page-level auth is manual/UAT, doc-render coverage is the mapping + content read)
    - lib/playbook/nav.ts (DOC_PAGE_FILE), lib/playbook/read-doc.ts (readObservabilityDoc)
  </read_first>
  <action>
    Write `__tests__/playbook-docs-render.test.ts` (pure logic, no react-markdown import — avoids the ESM-Jest issue, RESEARCH Pitfall 3): assert `DOC_PAGE_FILE` maps the 4 doc slugs to exactly the D-10 filenames (`VENDOR-DIRECTORY.md`, `RUNBOOK.md`, `OPERATING-RHYTHM.md`, `THRESHOLDS-AND-SEVERITY.md`), and that `await readObservabilityDoc(DOC_PAGE_FILE[slug])` returns non-empty markdown for each of the 4 (proving the single-source-of-truth wiring resolves locally). This is the automated gate for PLAYBOOK-05/06's local behavior; the actual RSC render + production file-tracing are covered by 33-03's build-trace gate + manual UAT.
  </action>
  <verify>
    <automated>npx jest __tests__/playbook-docs-render.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/playbook-docs-render.test.ts` passes: 4 correct filename mappings + 4 non-empty content reads
    - The test imports `DOC_PAGE_FILE` from nav.ts and `readObservabilityDoc` from read-doc.ts (proves the pages' actual wiring, not a duplicate map)
  </acceptance_criteria>
  <done>An automated test locks the D-10 page-to-file map and proves each doc resolves to non-empty content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session → IT doc page | untrusted staff identity must be gated before any observability doc prose (credential locations, vendor detail) renders |
| committed .md → rendered page | trusted, committed content (not user input) rendered via react-markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Elevation of Privilege | forgotten inline guard on a doc page | high | mitigate | D-02: EVERY one of the 4 pages carries its own `requireStaffPage(['leadership','it'])` as the first statement — verifiable by grepping all 4 page files for the guard before the read; never widened to ALL_STAFF_ROLES |
| T-33-03 | Information Disclosure | doc prose leaking before gate | high | mitigate | The guard runs BEFORE `readObservabilityDoc()` (fail closed) — a non-authorized request redirects before any file is read/rendered |
| T-33-02 | Tampering (injection) | markdown render | low | accept | Content is trusted/committed `docs/observability/*.md` (not user input); MarkdownDoc (33-03) uses react-markdown's React-element path, no raw-HTML sink |
| T-33-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan (react-markdown/remark-gfm added in 33-03) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `/admin/playbook/it/vendor-directory` page — renders VENDOR-DIRECTORY.md
- `/admin/playbook/it/runbook` page — renders RUNBOOK.md
- `/admin/playbook/it/operating-rhythm` page — renders OPERATING-RHYTHM.md
- `/admin/playbook/it/thresholds` page — renders THRESHOLDS-AND-SEVERITY.md
- `__tests__/playbook-docs-render.test.ts` — Wave 0 page-to-file + content-read test
</artifacts_produced>

<verification>
- `npx jest __tests__/playbook-docs-render.test.ts` green
- All 4 pages self-guard with `requireStaffPage(['leadership','it'])` before the read (grep each file)
- `npx tsc --noEmit` clean for the 4 pages
- Manual UAT: an `it`/`leadership` account sees rendered doc content; an `ae`/`bd` account is redirected away from `/admin/playbook/it/*`
</verification>

<success_criteria>
The 4 IT doc pages render their committed `.md` source as styled markdown, each fail-closed guarded to leadership+it before any read, with the D-10 page-to-file map locked by an automated test.
</success_criteria>

<output>
Create `.planning/phases/33-the-playbook-shell-it-team-monitoring-dashboard-read-only-v1/33-06-SUMMARY.md` when done.
</output>
