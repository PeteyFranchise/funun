---
phase: 06-playlist-curator-pitching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/030_curators_pitch_history.sql
  - types/index.ts
  - lib/industry-roles.ts
  - package.json
  - package-lock.json
autonomous: false
requirements: [PITCH-01, PITCH-02, PITCH-03, PITCH-04, PITCH-05, PITCH-06, PITCH-07, PITCH-08]
user_setup:
  - service: resend-pitch-domain
    why: "Cold-outreach pitch sends run over a dedicated subdomain to keep transactional mail deliverable (D-22). Not a build blocker — send fails gracefully until live."
    env_vars:
      - name: PITCH_FROM_EMAIL
        source: "Resend Dashboard -> Domains -> add pitch.funun.studio (DKIM/SPF/DMARC) -> ~2-week warmup, then set to e.g. pitches@pitch.funun.studio"
      - name: RESEND_WEBHOOK_SECRET
        source: "Resend Dashboard -> Webhooks -> add endpoint /api/webhooks/resend -> copy the signing secret (whsec_...)"
    dashboard_config:
      - task: "Add pitch.funun.studio sending domain and complete DNS + warmup"
        location: "Resend Dashboard -> Domains"
      - task: "Create webhook endpoint for email.bounced events"
        location: "Resend Dashboard -> Webhooks -> URL = https://<app-domain>/api/webhooks/resend"
  - service: spotify-youtube-reach
    why: "Weekly reach-signal refresh (follower/subscriber counts) on curator records (D-03). Not a build blocker — fetch no-ops gracefully until creds present (D-04)."
    env_vars:
      - name: SPOTIFY_CLIENT_ID
        source: "Spotify Developer Dashboard -> Create app -> Client ID"
      - name: SPOTIFY_CLIENT_SECRET
        source: "Spotify Developer Dashboard -> Create app -> Client Secret"
      - name: YOUTUBE_API_KEY
        source: "Google Cloud Console -> APIs & Services -> Credentials -> API key (enable YouTube Data API v3)"
      - name: CRON_SECRET
        source: "Generate a 16+ char random string; set in Vercel Project -> Settings -> Environment Variables. Vercel Cron auto-sends it as Bearer auth."

must_haves:
  truths:
    - "A `curators` row can be created with directory + email_valid + genre baseline + claim/reach/moderation columns, and RLS is enabled on the table"
    - "A `pitch_history` row can be created per project with a curator+track uniqueness invariant enforced at the DB layer"
    - "A curator auth account created with app_metadata.role='curator' does NOT receive an artist_profiles/subscriptions row (handle_new_user branches)"
    - "'Playlist Curator' renders as a selectable industry role in Settings and persists as slug 'playlist_curator'"
    - "svix is installed and importable for later webhook signature verification"
  artifacts:
    - "supabase/migrations/030_curators_pitch_history.sql"
    - "Curator and PitchHistory TypeScript types in types/index.ts"
    - "'Playlist Curator' role in lib/industry-roles.ts"
    - "svix in package.json dependencies"
  key_links:
    - "handle_new_user() curator branch — if wrong, curators silently acquire artist accounts (RESEARCH Pitfall 1)"
    - "uniq_curator_track_pitch UNIQUE (curator_id, track_id) — backstop for the duplicate-send guard (PITCH-03)"
    - "schema push to live DB — build/typecheck pass WITHOUT it, creating a false-positive verification state"
    - "curators table carries exactly the lean field set (name, email, playlist/channel URL, genre focus, submission notes) plus an auto-fetched reach_signal — never admin-entered directly (D-02)"
---

<objective>
Lay the entire data + type foundation for Phase 6: the `curators` directory table and the `pitch_history` log table (both with RLS-immediately-after-CREATE per project convention), the curator-branch to `handle_new_user()` so magic-link curator accounts never become artists, the TypeScript types every downstream plan imports, the trivial "Playlist Curator" industry role (PITCH-08), and the one new dependency (`svix`) behind a human legitimacy checkpoint.

Purpose: Every other Phase 6 plan (admin directory, artist directory, composer, response/claim, webhook) reads these tables and types. Getting the schema, the uniqueness invariant, and the `handle_new_user()` branch right up front is the single highest-leverage correctness decision in the phase.
Output: Migration 030 pushed to the live DB, extended types, the new role, and svix installed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-playlist-curator-pitching/06-CONTEXT.md
@.planning/phases/06-playlist-curator-pitching/06-RESEARCH.md
@.planning/phases/06-playlist-curator-pitching/06-PATTERNS.md

# Migration conventions + the exact trigger function being extended
@supabase/migrations/028_launchpad_checklist.sql
@supabase/migrations/027_fix_handle_new_user_exception_isolation.sql
@supabase/migrations/009_antenna_notifications.sql
@lib/industry-roles.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify svix package legitimacy before install</name>
  <what-built>Nothing yet — this gate precedes the only new dependency in Phase 6. RESEARCH.md's Package Legitimacy Audit flagged `svix` as `[SUS]` via a heuristic "too-new" seam that fired on the LATEST version's recent publish date (2026-06-25), NOT on package age.</what-built>
  <how-to-verify>
    Review this evidence from RESEARCH.md's Package Legitimacy Audit before approving `npm install svix`:
    - Package: `svix` on npm, source repo `github.com/svix/svix-webhooks` (official Svix organization)
    - First published: 2021-05-20 (~5 years old, not new)
    - Downloads: ~4.88M per week
    - Why needed: Resend signs webhooks using Svix under the hood; the project's pinned `resend@^4.0.0` predates Resend's own `webhooks.verify()` helper. Adding `svix` directly is lower-risk than a 4→6 major-version `resend` upgrade that would touch the working `lib/email/index.ts`.
    - Verdict rationale: the countervailing evidence (5-year-old official-org package, ~4.88M weekly downloads) strongly indicates a false positive from an active release cadence, not a supply-chain risk. Per protocol the `[SUS]` verdict is preserved rather than silently overridden — hence this checkpoint.
    Optionally cross-check https://www.npmjs.com/package/svix (publisher, repo link, download count).
  </how-to-verify>
  <resume-signal>Type "approved" to allow `npm install svix`, or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install svix and add the Playlist Curator industry role</name>
  <files>package.json, package-lock.json, lib/industry-roles.ts</files>
  <read_first>
    - lib/industry-roles.ts (see the INDUSTRY_ROLE_GROUPS structure and the Business group where a curator role fits)
    - package.json (confirm resend stays pinned at ^4.0.0 — do NOT bump it)
  </read_first>
  <action>Run `npm install svix` (only after Task 1 approval) — this adds `svix` to dependencies and updates package-lock.json. Do NOT change the `resend` version (it stays `^4.0.0` per D-15 / RESEARCH.md Standard Stack). Then in lib/industry-roles.ts add a new role `{ slug: 'playlist_curator', label: 'Playlist Curator' }` to the `Business` group's `roles` array within INDUSTRY_ROLE_GROUPS (PITCH-08). No other file needs changing — components/profile/ProfileForm.tsx renders INDUSTRY_ROLE_GROUPS dynamically (line 476) and ALL_INDUSTRY_ROLE_SLUGS derives from the same source, so the role appears in Settings automatically and validates on save.</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.svix) process.exit(1); if(!p.dependencies.resend.startsWith('^4')) process.exit(1); console.log('svix installed, resend still 4.x')"</automated>
    <automated>grep -c "playlist_curator" lib/industry-roles.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains a `svix` entry
    - `package.json` `resend` dependency still starts with `^4`
    - `lib/industry-roles.ts` contains the slug `playlist_curator` with label `Playlist Curator` inside the Business group
    - `npm run build` compiles with no type errors
  </acceptance_criteria>
  <done>svix is installed without touching resend's version; "Playlist Curator" is a selectable role in Settings.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write migration 030 — curators + pitch_history tables, RLS, uniqueness, handle_new_user curator branch</name>
  <files>supabase/migrations/030_curators_pitch_history.sql</files>
  <read_first>
    - supabase/migrations/028_launchpad_checklist.sql (table + RLS + index + trigger conventions to mirror; uuid_generate_v4() default; ALTER TABLE ... ENABLE ROW LEVEL SECURITY immediately after CREATE TABLE)
    - supabase/migrations/027_fix_handle_new_user_exception_isolation.sql (the EXACT current body of handle_new_user() being extended — use CREATE OR REPLACE FUNCTION, do NOT add a new CREATE TRIGGER)
    - supabase/migrations/009_antenna_notifications.sql (notifications table + service-role-insert RLS pattern)
    - .planning/phases/06-playlist-curator-pitching/06-RESEARCH.md (Code Examples: uniq_curator_track_pitch constraint, response-rate query, drift; Pitfall 1 handle_new_user branch)
  </read_first>
  <action>Create supabase/migrations/030_curators_pitch_history.sql. Follow migration 028's structure verbatim (header comment block, uuid_generate_v4() PK default, ENABLE ROW LEVEL SECURITY on the line immediately after each CREATE TABLE per the CVE-2025-48757 convention, reuse the update_updated_at() trigger function from migration 001 for updated_at columns).

CREATE TABLE curators with columns: id UUID PK; name TEXT NOT NULL; email TEXT NOT NULL UNIQUE; playlist_name TEXT; playlist_url TEXT; platform TEXT NOT NULL (one of: spotify, apple_music, youtube_music, soundcloud, blog_other — the finalized enum from UI-SPEC; enforce with a CHECK constraint); genre_focus TEXT[] NOT NULL DEFAULT '{}'; baseline_genre_focus TEXT[] NOT NULL DEFAULT '{}' (drift baseline per D-16); submission_notes TEXT; reach_signal INT (nullable — approximate/hidden per RESEARCH Pitfall 5, store NULL never 0); reach_fetched_at TIMESTAMPTZ; email_valid BOOLEAN NOT NULL DEFAULT true (flips false on hard bounce, D-15); do_not_pitch BOOLEAN NOT NULL DEFAULT false (unsubscribe flag, D-20); drift_flagged BOOLEAN NOT NULL DEFAULT false (D-16/D-17); flagged_inactive BOOLEAN NOT NULL DEFAULT false (admin manual toggle, D-21); claimed_by UUID REFERENCES auth.users(id) (nullable, set on claim, D-18); claim_token TEXT; claim_token_expires_at TIMESTAMPTZ; created_at TIMESTAMPTZ NOT NULL DEFAULT now(); updated_at TIMESTAMPTZ NOT NULL DEFAULT now().

RLS on curators: (a) a SELECT policy `USING (true)` so authenticated artists can browse the directory (admin writes go through createServiceClient which bypasses RLS, matching launchpad_checklist_items); (b) an UPDATE policy for curator self-serve `USING (auth.uid() = claimed_by) WITH CHECK (auth.uid() = claimed_by)` (V4 access control / IDOR mitigation T-06-05). Add a partial index on claim_token WHERE claim_token IS NOT NULL, and an index on email.

CREATE TABLE pitch_history with columns: id UUID PK; project_id UUID NOT NULL REFERENCES vault_projects(id) ON DELETE CASCADE; track_id UUID NOT NULL REFERENCES tracks(id) ON DELETE CASCADE; curator_id UUID NOT NULL REFERENCES curators(id) ON DELETE CASCADE; artist_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE; note TEXT NOT NULL; status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','accepted','declined')) (no 'opened' — dropped per D-10); response_token TEXT NOT NULL UNIQUE; decline_reason TEXT; sent_at TIMESTAMPTZ NOT NULL DEFAULT now(); responded_at TIMESTAMPTZ. Add `CONSTRAINT uniq_curator_track_pitch UNIQUE (curator_id, track_id)` (the duplicate-send backstop, PITCH-03). Indexes: (curator_id, sent_at) for the 90-day response-rate query, (project_id) for per-project history, (response_token) via the unique constraint.

RLS on pitch_history: SELECT policy `USING (auth.uid() = artist_id)` so an artist reads their own pitch history; no INSERT/UPDATE policy for users (all writes are server-side via service-role client — the send route, the token accept/decline routes, and the curator portal read path — matching the notifications table convention in migration 009). Curator portal reads pitches-received via a service-role query filtered to their curator_id, not via RLS.

handle_new_user() branch (RESEARCH Pitfall 1 — critical): CREATE OR REPLACE FUNCTION public.handle_new_user() copying the ENTIRE current body from migration 027 verbatim, then insert as the very FIRST statement inside BEGIN, before the artist_profiles INSERT: `IF (NEW.raw_app_meta_data->>'role') = 'curator' THEN RETURN NEW; END IF;`. This makes curator magic-link accounts skip artist_profiles, subscriptions, and claim_collaborators entirely (Open Question 4 resolution). Keep the existing nested exception block and SECURITY DEFINER. Do NOT add a CREATE TRIGGER — on_auth_user_created (migration 001) already invokes this function and picks up the replaced body automatically (same note migration 027 makes).

Header comment must state: run via `supabase db push`.</action>
  <verify>
    <automated>grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/030_curators_pitch_history.sql</automated>
    <automated>grep -Eq "role.{0,20}curator.*RETURN NEW|IF .*curator.* THEN" supabase/migrations/030_curators_pitch_history.sql && echo "curator branch present"</automated>
    <automated>grep -q "uniq_curator_track_pitch" supabase/migrations/030_curators_pitch_history.sql && echo "unique constraint present"</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly two `CREATE TABLE` statements (curators, pitch_history), each immediately followed by `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` (grep count of ENABLE ROW LEVEL SECURITY is >= 2)
    - `curators.platform` has a CHECK constraint listing spotify, apple_music, youtube_music, soundcloud, blog_other
    - `pitch_history.status` CHECK lists only pending, accepted, declined (no opened)
    - `uniq_curator_track_pitch UNIQUE (curator_id, track_id)` is present
    - `handle_new_user()` is redefined via CREATE OR REPLACE and returns NEW early when `raw_app_meta_data->>'role' = 'curator'`, with NO new CREATE TRIGGER statement
    - curators has a self-serve UPDATE RLS policy scoped `auth.uid() = claimed_by`
  </acceptance_criteria>
  <done>Migration 030 defines both tables with RLS, the uniqueness invariant, and the curator-safe handle_new_user() branch.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add Curator and PitchHistory TypeScript types</name>
  <files>types/index.ts</files>
  <read_first>
    - types/index.ts (existing type-export conventions: PascalCase names, string-union types for enums, nullable fields as `| null`; see VaultProject at line 34 and Track at line 434 for column-to-field mapping style)
    - supabase/migrations/030_curators_pitch_history.sql (the columns just defined — the types must mirror them exactly)
  </read_first>
  <action>In types/index.ts, add exported types mirroring migration 030's columns. Add `export type CuratorPlatform = 'spotify' | 'apple_music' | 'youtube_music' | 'soundcloud' | 'blog_other'` and `export type PitchStatus = 'pending' | 'accepted' | 'declined'`. Add `export type Curator = { ... }` with every curators column typed (id, name, email as string; playlist_name, playlist_url, submission_notes, reach_fetched_at, claim_token, claim_token_expires_at, claimed_by as `string | null`; platform as CuratorPlatform; genre_focus and baseline_genre_focus as `string[]`; reach_signal as `number | null`; email_valid, do_not_pitch, drift_flagged, flagged_inactive as boolean; created_at, updated_at as string). Add `export type PitchHistory = { ... }` mirroring pitch_history columns (status as PitchStatus, decline_reason and responded_at as `string | null`, the rest per schema). Follow the existing convention of grouping related types with a `// ─── Curators ───` section header comment. Do NOT create a duplicate `Pitch` type — one already exists (line 548) for the submissions system; name these Curator / PitchHistory distinctly.</action>
  <verify>
    <automated>grep -Eq "export type Curator =" types/index.ts && grep -Eq "export type PitchHistory =" types/index.ts && grep -Eq "export type PitchStatus =" types/index.ts && echo "types present"</automated>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Curator`, `PitchHistory`, `CuratorPlatform`, `PitchStatus` are exported from types/index.ts
    - Every field name in `Curator` matches a curators column; every field in `PitchHistory` matches a pitch_history column
    - No `export type Pitch =` collision (the existing submissions `Pitch` type is untouched)
    - `npm run build` compiles with no type errors
  </acceptance_criteria>
  <done>Downstream plans can `import type { Curator, PitchHistory, CuratorPlatform, PitchStatus } from '@/types'`.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 5: [BLOCKING] Push migration 030 to the live database</name>
  <files>supabase/migrations/030_curators_pitch_history.sql</files>
  <read_first>
    - supabase/migrations/030_curators_pitch_history.sql (the file to push)
  </read_first>
  <action>Run `supabase db push` to apply migration 030 to the live Supabase database. This step is MANDATORY and cannot be skipped: `npm run build` and the TypeScript type checks pass WITHOUT the push (types are hand-authored in types/index.ts, not generated from the live DB), so build success alone is a false-positive verification state — the curators and pitch_history tables will not exist for any runtime query until this push runs. If the Supabase CLI is not on PATH or requires interactive login, set SUPABASE_ACCESS_TOKEN (non-TTY workaround) or, per RESEARCH Environment Availability, apply the migration SQL via the Supabase dashboard SQL editor (the project already has 29 migrations; some were applied this way). If the push cannot complete autonomously, surface it as a manual-intervention blocker (this plan is autonomous:false for exactly this reason) — do NOT mark the plan complete with the tables unpushed.</action>
  <verify>
    <automated>supabase db push 2>&1 | tail -20 || echo "PUSH REQUIRES MANUAL INTERVENTION — see action notes"</automated>
    <human-check>Confirm in the Supabase dashboard (Table Editor) that `curators` and `pitch_history` tables now exist with RLS enabled.</human-check>
  </verify>
  <acceptance_criteria>
    - `curators` and `pitch_history` tables exist in the live database (verified via dashboard or `supabase db push` reporting the migration applied)
    - RLS is enabled on both tables in the live DB
    - If push could not run autonomously, the plan is left blocked with a clear manual-intervention note — NOT marked complete
  </acceptance_criteria>
  <done>Both tables exist in the live database and downstream runtime queries will resolve.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (for source-grounding / drift exclusion)

New symbols and files created by THIS plan (exclude from drift verification — they are net-new, not pre-existing):
- Files: `supabase/migrations/030_curators_pitch_history.sql`
- DB tables: `curators`, `pitch_history`
- DB columns (curators): `name`, `email`, `playlist_name`, `playlist_url`, `platform`, `genre_focus`, `baseline_genre_focus`, `submission_notes`, `reach_signal`, `reach_fetched_at`, `email_valid`, `do_not_pitch`, `drift_flagged`, `flagged_inactive`, `claimed_by`, `claim_token`, `claim_token_expires_at`
- DB columns (pitch_history): `project_id`, `track_id`, `curator_id`, `artist_id`, `note`, `status`, `response_token`, `decline_reason`, `sent_at`, `responded_at`
- DB constraint: `uniq_curator_track_pitch`
- Modified function: `handle_new_user()` (curator branch added)
- TypeScript types: `Curator`, `PitchHistory`, `CuratorPlatform`, `PitchStatus`
- Industry role slug: `playlist_curator`
- Dependency: `svix`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supply chain → repo | New npm dependency (`svix`) enters the build via package-lock.json |
| new auth account type → existing artist-only DB triggers | Curator magic-link signups cross the `on_auth_user_created` trigger designed for artists |
| client → curators table (RLS) | Authenticated artists read the directory; claimed curators self-edit their own row |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | `npm install svix` | high | mitigate | `[SUS]`-flagged package gated behind a blocking `checkpoint:human-verify` (Task 1) presenting age/downloads/repo evidence; verdict never auto-approved regardless of workflow.auto_advance |
| T-06-01 | Elevation of Privilege | `handle_new_user()` firing for curator signups | high | mitigate | Early `RETURN NEW` when `raw_app_meta_data->>'role' = 'curator'` skips artist_profiles/subscriptions/claim_collaborators inserts (RESEARCH Pitfall 1); app_metadata is service-role-only writable, not client-forgeable |
| T-06-05 | Elevation of Privilege | curators self-serve UPDATE (IDOR) | high | mitigate | RLS UPDATE policy `USING (auth.uid() = claimed_by)` on curators; defense-in-depth `.eq('claimed_by', user.id)` enforced later in the self-serve API route (plan 06-05) |
| T-06-03 | Tampering / Repudiation | pitch_history response tokens | medium | mitigate | `response_token` is UNIQUE and populated with 256-bit `crypto.randomBytes(32)` (plan 06-04/06-05); one-time-use via `status !== 'pending'` guard |
| T-06-DB | Information Disclosure | RLS omission on new tables | high | mitigate | ENABLE ROW LEVEL SECURITY immediately after each CREATE TABLE (CVE-2025-48757 convention); user-facing SELECT scoped to owner (pitch_history) or directory-public (curators) |
</threat_model>

<verification>
- `npm run build` compiles with no type errors after types + role additions
- `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/030_curators_pitch_history.sql` >= 2
- Migration 030 applied to the live DB (dashboard confirms both tables + RLS)
- Settings page renders "Playlist Curator" as a selectable industry role and saves it as slug `playlist_curator`
</verification>

<success_criteria>
- Both tables exist in the live database with RLS enabled and the uniqueness/CHECK constraints defined
- `handle_new_user()` returns early for curator-role signups (no artist rows created)
- `Curator` / `PitchHistory` / `CuratorPlatform` / `PitchStatus` types are importable
- svix installed without a resend version bump; "Playlist Curator" role live in Settings
</success_criteria>

<output>
Create `.planning/phases/06-playlist-curator-pitching/06-01-SUMMARY.md` when done.
</output>
