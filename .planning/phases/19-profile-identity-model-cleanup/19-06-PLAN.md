---
phase: 19-profile-identity-model-cleanup
plan: 06
type: execute
wave: 2
depends_on: ["19-03"]
files_modified:
  - components/contracts/ContractLocker.tsx
  - app/(artist)/split-sheets/[id]/page.tsx
autonomous: true
requirements: [R4]

must_haves:
  truths:
    - "A claimed user viewing their own party row on a FROZEN sheet (esign_pending/executed) in the Contract Locker credit view gets a 'this info is wrong' affordance that submits a structured field + suggested value to POST /api/split-sheets/[id]/correction-flag (D-05/D-07)"
    - "The affordance appears ONLY on frozen sheets and ONLY on the viewing user's own row — not on live sheets (those flow through R3 profile edits) and not on other parties' rows"
    - "The owner's notification deep-link (/split-sheets/[id]?stagedFlag=...) stages the suggested change and shows the correct next step: void-first for esign_pending; a guided pointer to start a correction for executed (D-08)"
    - "For an executed sheet, the guided apply is a POINTER ONLY — no first-class amendment mechanism (lineage/re-sign) is built this phase; the signed PDF/Certificate is never edited or regenerated (SPEC out-of-scope, owner decision 2026-07-24)"
    - "No affordance or code path here writes another user's split_sheet_parties row or any split_percentage/role term (Edge: Cross-user authority, R4)"
  artifacts:
    - "ContractLocker.tsx: per-party 'this info is wrong' entry on the claimed user's own frozen row"
    - "split-sheets/[id]/page.tsx (owner view): stagedFlag deep-link handling — staged suggestion + void-first (esign_pending) or guided-correction pointer (executed)"
  key_links:
    - "The flag entry posts to the 19-03 correction-flag route; the owner deep-link consumes buildIdentityCorrectionFlagNotification's ?stagedFlag= link (19-03)"
    - "Guided apply routes INTO the existing void route for esign_pending; it never bypasses the lifecycle or mutates the signed document"
  prohibitions:
    - statement: "MUST NOT mutate an executed split sheet or regenerate/replace its signed PDF/Certificate"
      status: resolved
      verification: "test — executed guided-apply is a pointer only (no amendment/re-sign built); assert no regeneration path is added; executed edits remain lifecycle-blocked (409)"
    - statement: "MUST NOT let a non-owner write another user's split_sheet_parties row or edit split_percentage/role"
      status: resolved
      verification: "test — the Locker affordance only opens a structured flag submission to the correction-flag route; it exposes no field for terms and no write to another party row"
---

<objective>
Deliver the R4 user-facing surfaces: (1) the "this info is wrong" flag entry in the Contract Locker credit view, scoped to a claimed user's OWN row on a FROZEN sheet (D-05), submitting the structured field + suggested value to the 19-03 route; and (2) the owner-side guided apply — the notification deep-link stages the suggested change and offers the correct lifecycle next step: void-first for esign_pending, a guided pointer to start a correction for executed (no amendment mechanism built this phase, owner decision).

Purpose: Close the loop from "a claimed user spots wrong identity on a frozen sheet" to "the owner is guided to the safe fix path" — without any cross-user or term write and without ever touching the signed document.
Output: A scoped Locker flag affordance + owner-side staged-flag guidance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-profile-identity-model-cleanup/19-SPEC.md
@.planning/phases/19-profile-identity-model-cleanup/19-CONTEXT.md
@.planning/phases/19-profile-identity-model-cleanup/19-RESEARCH.md
@.planning/phases/19-profile-identity-model-cleanup/19-PATTERNS.md

# Analog surfaces
@components/contracts/ContractLocker.tsx
@app/(artist)/split-sheets/[id]/page.tsx
</context>

<artifacts>
## Artifacts this plan produces
- `ContractLocker.tsx`: the claimed-user "this info is wrong" per-party affordance (frozen sheets, own row only)
- `split-sheets/[id]/page.tsx`: `?stagedFlag=` deep-link handling — staged suggestion + void-first (esign_pending) / guided-correction pointer (executed)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Contract Locker "this info is wrong" flag entry (claimed user, frozen, own row)</name>
  <files>components/contracts/ContractLocker.tsx</files>
  <read_first>
    - 19-CONTEXT.md D-05 (flag entry = Contract Locker credit view, claimed user's own row, frozen sheet) + D-07 (structured field + suggested value, no free text)
    - 19-PATTERNS.md "components/contracts/ContractLocker.tsx" (existing per-party sections are the template; affordance placement is Claude's discretion)
    - 19-SPEC.md R4 (frozen = esign_pending/executed; a claimed user can only flag their OWN identity)
    - 17-DUAL-ENTRY-DESIGN.md §10c/§10c-ii (structured-actions doctrine)
  </read_first>
  <action>
    Add a per-party "this info is wrong" affordance in the Contract Locker credit view, shown ONLY when: the sheet is frozen (status esign_pending or executed) AND the row being viewed is the current user's OWN party row (the claimed user's identity). It must NOT appear on live sheets (draft/pending_approval/approved/countered — those corrections flow through the R3 profile-edit live-link) nor on any other party's row. The affordance opens a small structured control: a field selector limited to the closed set (pro, ipi, publisher, administrator, legal_name — the FLAGGABLE_FIELDS from 19-03) and a single suggested-value input; NO free-text message channel. On submit it POSTs { partyId, field, suggestedValue } to /api/split-sheets/[id]/correction-flag and shows a confirmation ("Sent to the sheet owner"). Expose NO control for split_percentage/role and NO way to edit another party's data. Styling/exact placement is Claude's discretion — follow the existing per-party card treatment. Keep the component a client component; use the existing useRouter/refresh conventions.
  </action>
  <verify>
    <automated>grep -q "correction-flag" components/contracts/ContractLocker.tsx && ! grep -iE "split_percentage|<textarea" components/contracts/ContractLocker.tsx && npx tsc --noEmit 2>&1 | grep -E "ContractLocker" || echo "flag entry wired, no term field, no free-text, types clean"</automated>
  </verify>
  <acceptance_criteria>
    - The affordance renders only on frozen sheets and only on the viewer's own party row
    - It submits a structured { field (allowlisted), suggestedValue } to the correction-flag route — no free-text channel
    - No term (split_percentage/role) control and no other-party edit exists
  </acceptance_criteria>
  <done>A claimed user can flag their own wrong identity on a frozen sheet via a structured Locker control that hits the 19-03 route.</done>
</task>

<task type="auto">
  <name>Task 2: Owner-side guided apply — staged flag + correct lifecycle next step</name>
  <files>app/(artist)/split-sheets/[id]/page.tsx</files>
  <read_first>
    - 19-CONTEXT.md D-08 (guided apply: void-first for esign_pending; guided pointer to start a correction for executed; amendment mechanism DEFERRED) + 19-SPEC.md R4 target/Acceptance line 8
    - 19-RESEARCH.md Pitfall 4 + "No Analog Found" (no amendment route exists; default to a guided pointer per D-08/A3, NOT a new lineage-tracked route)
    - app/(artist)/split-sheets/[id]/page.tsx (initiator/owner view; the existing status handling + how it reads the sheet)
    - app/api/split-sheets/[id]/void/route.ts (the existing void route the esign_pending path routes into)
  </read_first>
  <action>
    On the owner's split-sheet view, read the stagedFlag query param (the deep-link target from buildIdentityCorrectionFlagNotification, 19-03). When present and the viewer is the sheet owner, load that flag (field + suggested value) and render a staged-correction panel showing the flagged field, the current value, and the suggested value. Branch the offered next step by sheet status: for esign_pending, guide the owner to VOID the envelope first (link/trigger the existing void route) so the sheet returns to an editable state where the identity can be corrected; for executed, render a GUIDED POINTER ONLY — explain that the signed record is immutable and the correction must be made by starting a new corrected split sheet (there is intentionally NO auto-generated amendment / lineage / re-sign this phase, per the owner decision). Do NOT build an amendment route, an amends_split_sheet_id column, or any regeneration of the executed PDF/Certificate. Do NOT write another party's row from here. The panel is guidance + a route into the EXISTING void flow only. Follow the file's existing server-component + status-copy conventions.
  </action>
  <verify>
    <automated>grep -q "stagedFlag" "app/(artist)/split-sheets/[id]/page.tsx" && ! grep -iE "amends_split_sheet_id|regenerate|re-mint" "app/(artist)/split-sheets/[id]/page.tsx" && npx tsc --noEmit 2>&1 | grep -E "split-sheets/\[id\]/page" || echo "staged flag handled, no amendment/regeneration, types clean"</automated>
  </verify>
  <acceptance_criteria>
    - The owner view consumes ?stagedFlag= and shows the staged suggested value
    - esign_pending routes the owner into the existing void flow; executed shows a guided pointer only
    - No amendment/lineage/regeneration code is added; the signed PDF/Certificate is never touched
  </acceptance_criteria>
  <done>The owner is guided to the correct, immutability-preserving fix path for the flagged identity, with no amendment mechanism built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claimed user (Locker) -> correction-flag route | The UI must only submit structured, allowlisted fields; the server (19-03) re-validates and scopes the write. |
| owner (staged-flag view) -> lifecycle (void) / executed record | Guided apply may route into void but must never mutate the signed executed document or another party's row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-21 | Tampering | Locker affordance exposes term/other-party edit | high | mitigate | Affordance is a structured field+value flag to the correction-flag route only; no split_percentage/role control, no other-party write (verify greps + server-side re-validation in 19-03). |
| T-19-22 | Tampering | executed guided-apply mutates the signed record | high | mitigate | Executed path is a pointer only — no amendment/regeneration code; lifecycle keeps executed edits blocked (409). |
| T-19-23 | Elevation of Privilege | non-owner sees/acts on staged flag | medium | mitigate | Staged-flag panel renders only for the sheet owner; the flag row's RLS SELECT (19-03) restricts reads to owner + flagger. |

*No packages installed this plan — supply-chain threat (T-19-SC) not applicable.*
</threat_model>

<verification>
- Scoped greps (above) confirm the flag entry has no term/free-text control and the owner view adds no amendment/regeneration
- `npx tsc --noEmit` and `npm run lint` clean
- Live round-trip (flag -> owner bell/email -> staged panel -> void) is UAT after the 19-07 push (flags table live)
</verification>

<success_criteria>
A claimed user flags their own wrong identity on a frozen sheet from the Locker; the owner is deep-linked to a staged panel that routes esign_pending into void and points executed at starting a corrected sheet — with no cross-user/term write and no mutation of the signed document.
</success_criteria>

<output>
Create `.planning/phases/19-profile-identity-model-cleanup/19-06-SUMMARY.md` when done.
</output>
