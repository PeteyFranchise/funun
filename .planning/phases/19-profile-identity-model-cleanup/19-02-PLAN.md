---
phase: 19-profile-identity-model-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/split-sheets/agreement.ts
  - lib/vault/pdf/split-sheet.tsx
  - lib/vault/pdf/split-sheet.test.ts
  - app/approve/[token]/page.tsx
  - components/split-sheets/SplitApprovalView.tsx
autonomous: true
requirements: [R5]

must_haves:
  truths:
    - "A newly generated split-sheet PDF contains the note-to-licensees rendered as a boxed callout beside the parties/rights block, framed as informational guidance (no Funūn accuracy warranty) (R5 Acceptance)"
    - "The same note renders on the read-only share/export surface (the /approve/[token] view) so it travels with the record wherever a recipient sees it (D-11)"
    - "The note wording is the exact verbatim CONTEXT D-10 string, held as a single named constant reused by both surfaces"
  artifacts:
    - "NOTE_TO_LICENSEES constant in lib/split-sheets/agreement.ts"
    - "Boxed callout render in lib/vault/pdf/split-sheet.tsx (reusing guidanceBox/guidanceNote styles)"
    - "HTML/JSX note block on the approve/share surface (SplitApprovalView)"
    - "New byte-extraction assertion in lib/vault/pdf/split-sheet.test.ts"
  key_links:
    - "One NOTE_TO_LICENSEES const feeds both the @react-pdf/renderer PDF and the HTML share view — no wording drift between surfaces"
  prohibitions:
    - statement: "MUST NOT regenerate or alter any already-executed split-sheet PDF/Certificate when adding the note"
      status: resolved
      verification: "test — the note is added to the render path only; executed PDFs are never re-rendered. Assert the note const is present and rendered on a freshly generated sample; no backfill/regeneration code is introduced (lib/vault/pdf/split-sheet.test.ts)"
---

<objective>
Add the R5 "note to licensees" — a standard, counsel-pending advisory that ownership shares are fixed at signing but each writer's current PRO/publisher/administrator/payee must be re-verified at license time, framed as guidance and explicitly NOT a Funūn warranty. It appears on newly generated split-sheet PDFs and on the read-only share/export surface. Already-executed documents are never regenerated.

Purpose: Recipients (e.g. music supervisors) are told the contact/payment data may be stale, closing the sync/licensing staleness gap without touching the frozen ownership record.
Output: One shared constant + two render surfaces + a byte-extraction test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-profile-identity-model-cleanup/19-SPEC.md
@.planning/phases/19-profile-identity-model-cleanup/19-CONTEXT.md
@.planning/phases/19-profile-identity-model-cleanup/19-PATTERNS.md

# R5 analog sources
@lib/split-sheets/agreement.ts
@lib/vault/pdf/split-sheet.tsx
</context>

<artifacts>
## Artifacts this plan produces
- `NOTE_TO_LICENSEES` (named const, `lib/split-sheets/agreement.ts`) — verbatim D-10 wording
- PDF boxed-callout render in `lib/vault/pdf/split-sheet.tsx`
- Note render on `SplitApprovalView` (surfaced by `app/approve/[token]/page.tsx`)
- New assertion in `lib/vault/pdf/split-sheet.test.ts`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: NOTE_TO_LICENSEES constant + PDF boxed callout + byte assertion</name>
  <files>lib/split-sheets/agreement.ts, lib/vault/pdf/split-sheet.tsx, lib/vault/pdf/split-sheet.test.ts</files>
  <read_first>
    - 19-PATTERNS.md "lib/split-sheets/agreement.ts + lib/vault/pdf/split-sheet.tsx ... R5 licensee note" (GUIDANCE_NOTES analog at agreement.ts:40-45; guidanceBox/guidanceNote styles at split-sheet.tsx:243-253; parties table render near split-sheet.tsx:338)
    - 19-CONTEXT.md Specific Ideas — the VERBATIM D-10 wording
    - lib/vault/pdf/split-sheet.test.ts — the existing ESIGN-15/P17-08 byte-extraction test style (Node zlib only, no PDF-parsing dependency)
  </read_first>
  <action>
    In agreement.ts add a new named export `NOTE_TO_LICENSEES` set to the EXACT verbatim D-10 string from 19-CONTEXT.md Specific Ideas ("Ownership shares in this split sheet are fixed as of the date signed. ... Funūn provides this record but does not warrant the current accuracy of contact or payment information."). Do NOT append it to GUIDANCE_NOTES (D-09 places it beside the parties/rights block, not at the document foot with the general notes). Mirror the existing `export const GUIDANCE_NOTES` style/section-header convention. In split-sheet.tsx, render a boxed callout immediately after the parties table — reuse the existing `guidanceBox`/`guidanceNote` styles (or clone them as `licenseeNoteBox`/`licenseeNote` if a distinct border tint is preferred, executor discretion per the "Claude's Discretion" note) — as a `@react-pdf/renderer` View wrapping a Text of `NOTE_TO_LICENSEES`, positioned beside/below the parties/rights block per D-09. Do NOT introduce any regeneration/backfill of existing documents; the note simply renders on whatever the renderer is asked to produce next. Extend split-sheet.test.ts with an assertion that the freshly rendered sample PDF's extracted text contains a stable, unmistakable substring of NOTE_TO_LICENSEES (choose a distinctive phrase such as "fixed as of the date signed"), using the file's existing zlib byte-extraction approach.
  </action>
  <verify>
    <automated>npx jest lib/vault/pdf/split-sheet.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - NOTE_TO_LICENSEES exported from agreement.ts, verbatim D-10 wording, framed as guidance (contains the no-warranty clause)
    - The rendered sample PDF's extracted bytes contain the note text (new passing assertion)
    - No code path regenerates or re-renders an already-executed PDF/Certificate
  </acceptance_criteria>
  <done>Newly generated split-sheet PDFs carry the boxed licensee note beside the parties/rights block; the byte-extraction test proves it.</done>
</task>

<task type="auto">
  <name>Task 2: Render the note on the read-only share/export surface (D-11)</name>
  <files>app/approve/[token]/page.tsx, components/split-sheets/SplitApprovalView.tsx</files>
  <read_first>
    - 19-PATTERNS.md "Read-only share/export surface (D-11)" note (approve page is HTML/JSX, not @react-pdf)
    - app/approve/[token]/page.tsx (parties render + SplitApprovalView handoff)
    - components/split-sheets/SplitApprovalView.tsx (PartyRows block — the parties/rights display area)
  </read_first>
  <action>
    Render the same `NOTE_TO_LICENSEES` string on the read-only approval/share surface as a plain styled HTML/JSX block (NOT a `@react-pdf/renderer` View — this surface is React DOM) positioned beside/below the parties block (near `PartyRows` in SplitApprovalView). Import the constant from lib/split-sheets/agreement.ts so wording never diverges from the PDF. Style it as a subtle boxed callout consistent with the surface's existing muted-note styling. This satisfies D-11 (the note travels with the record wherever a recipient sees it). If the SplitApprovalView is the cleanest single choke point that both the approve view and any share render pass through, place it there; otherwise render in the approve page beside the parties list. Do not alter approval/counter/signing logic — this is a display-only addition.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "approve|SplitApprovalView|agreement" || echo "no type errors in touched files"</automated>
  </verify>
  <acceptance_criteria>
    - The approve/share surface renders NOTE_TO_LICENSEES near the parties block, imported from the shared constant
    - No change to approve/counter/sign behavior
    - tsc clean for the touched files
  </acceptance_criteria>
  <done>A recipient viewing the read-only share/export surface sees the identical licensee note (D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Funūn record -> external licensee | The PDF/share view crosses to an untrusted third party (music supervisor). The note manages a legal/accuracy expectation, not an input. No untrusted input flows INTO the system here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-08 | Repudiation | Licensee reliance on stale payee data | low | mitigate | The verbatim counsel-pending note explicitly disclaims accuracy and instructs re-verification — it is guidance framing, not a warranty (D-10/R5). |
| T-19-09 | Tampering | Executed-document immutability | high | mitigate | The note is added to the forward render path only; no regeneration/backfill code is introduced, so no executed PDF/Certificate is mutated (asserted in split-sheet.test.ts). |

*No packages installed this plan — supply-chain threat (T-19-SC) not applicable.*
</threat_model>

<verification>
- `npx jest lib/vault/pdf/split-sheet.test.ts` green with the new note assertion
- `npx tsc --noEmit` and `npm run lint` clean
- Manual/UAT (post-execution): a freshly generated split-sheet PDF and the /approve view both show the note; no already-executed document changed
</verification>

<success_criteria>
One shared NOTE_TO_LICENSEES constant renders as a boxed callout on newly generated PDFs and on the read-only share view, framed as guidance; the byte-extraction test proves the PDF contains it; no executed document is regenerated.
</success_criteria>

<output>
Create `.planning/phases/19-profile-identity-model-cleanup/19-02-SUMMARY.md` when done.
</output>
