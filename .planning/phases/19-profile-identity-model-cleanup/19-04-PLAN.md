---
phase: 19-profile-identity-model-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - supabase/migrations/071_user_profiles_data_rescue.sql
  - supabase/migrations/072_repoint_claim_functions.sql
  - supabase/migrations/073_drop_user_profiles.sql
autonomous: true
requirements: [R1, R2, R3]

must_haves:
  truths:
    - "Migration 071 copies stranded user_profiles values into artist_profiles using semantic-blank rules (NULL / trimmed-empty / {}), maps phone->contact_phone, display_name->artist_name, bio->bio, canonical-wins, and logs pre/post + stranded-value counts via RAISE NOTICE (R1 Acceptance line 3)"
    - "The {}-address and ''-PRO stranded cases are specifically rescued; the mailing_address blank-check uses IS NULL OR = '{}'::jsonb, never IS NULL alone (Pitfall 6)"
    - "Migration 072 re-points BOTH claim_collaborators() AND backfill_claimed_collaborators() to read from artist_profiles (was user_profiles), with phone->contact_phone (Edge: Missed reader, R1 Acceptance line 4)"
    - "Migration 072 adds the private artist_profiles.claim_prefill JSONB column and extends claim_collaborators() with the R2 reverse pre-fill: for each semantic-blank canonical field, pre-fill from claimed collaborators (most-recent by updated_at wins) and record an unconfirmed claim_prefill provenance entry, idempotently (never overwriting confirmed/edited/non-blank) (R2 Acceptance line 5)"
    - "Migration 073 drops user_profiles ONLY — ordered strictly after 071 (rescue) and 072 (re-point) so no reader references it at drop time (Prohibition: no drop before rescue+re-point)"
    - "The migrations do not touch resolvePartyIdentity or the [id]/page.tsx batch loader; the esign_pending/executed freeze boundary is unchanged (R3 preserved)"
  artifacts:
    - "supabase/migrations/071_user_profiles_data_rescue.sql"
    - "supabase/migrations/072_repoint_claim_functions.sql (re-point both fns + claim_prefill column + R2 reverse pre-fill + NOTIFY pgrst)"
    - "supabase/migrations/073_drop_user_profiles.sql"
    - "artist_profiles.claim_prefill JSONB column (private, not in any GRANT SELECT list)"
  key_links:
    - "071 -> 072 -> 073 filename ordering is the enforcement mechanism for rescue-then-repoint-then-drop; all three are separately reviewable files pushed in the 19-07 checkpoint"
    - "Migration 071's rescue logic mirrors lib/profile/semantic-blank.ts (19-01); migration 072's reverse pre-fill mirrors lib/profile/claim-prefill.ts (19-01) — the twins are the parity anchors"
    - "claim_prefill entry shape { confirmed, source_collaborator_id, source_name, filled_at } is written here and read by the 19-05 confirm UI"
  prohibitions:
    - statement: "MUST NOT drop user_profiles before the data-rescue migration runs, nor before both DB readers are re-pointed"
      status: resolved
      verification: "test — migration ordering asserted by filename (071 rescue, 072 re-point, 073 drop); companion assertions in 19-05 verify 072 reads artist_profiles and 073 contains DROP TABLE"
    - statement: "MUST NOT lose non-NULL-but-blank ({} / '') or unmapped (phone, display_name, bio) data in the rescue"
      status: resolved
      verification: "test — semantic-blank twin (19-01) covers the {}-address and ''-text fixtures that migration 071 mirrors; 071 RAISE NOTICE logs a stranded-value count"
    - statement: "MUST NOT accomplish the consolidation by editing historical migrations"
      status: resolved
      verification: "test — all changes land as NEW files 071/072/073; historical migrations (026/051/053) are byte-unchanged in the diff"
---

<objective>
Author the three ordered R1/R2 migrations that fix the duplicate-rights bug at the data layer: (071) a semantic-blank data rescue that copies any values stranded in user_profiles into the canonical artist_profiles before anything is dropped; (072) a re-point of BOTH claim_collaborators() and backfill_claimed_collaborators() to read artist_profiles, plus the new private claim_prefill column and the R2 reverse pre-fill logic inside claim_collaborators(); and (073) the destructive drop of user_profiles, strictly last. All three are authored as separate files (never pushed here) and reviewed/pushed together in the 19-07 human-gated checkpoint.

Purpose: Collapse the duplicate identity table without data loss and make claim/backfill actually populate rights going forward, while adding the provenance substrate R2's confirm UI reads.
Output: migrations 071, 072, 073 — file-authored, twin-mirrored, ordering-enforced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-profile-identity-model-cleanup/19-SPEC.md
@.planning/phases/19-profile-identity-model-cleanup/19-RESEARCH.md
@.planning/phases/19-profile-identity-model-cleanup/19-PATTERNS.md

# Parity twins (from 19-01) + migration analogs
@lib/profile/semantic-blank.ts
@lib/profile/claim-prefill.ts
</context>

<artifacts>
## Artifacts this plan produces
- `supabase/migrations/071_user_profiles_data_rescue.sql`
- `supabase/migrations/072_repoint_claim_functions.sql` — re-points both fns, adds `artist_profiles.claim_prefill` JSONB (private), R2 reverse pre-fill, `NOTIFY pgrst`
- `supabase/migrations/073_drop_user_profiles.sql`
- New DB column `artist_profiles.claim_prefill` (server-owned, never in a GRANT SELECT list)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Migration 071 — semantic-blank data rescue (logged)</name>
  <files>supabase/migrations/071_user_profiles_data_rescue.sql</files>
  <read_first>
    - 19-RESEARCH.md "Pattern 1: Semantic-blank rescue, canonical-wins" (the illustrative full SQL) + Pitfall 2 (display_name->artist_name, bio->bio) + Pitfall 6 ({}-address blank-check)
    - 19-SPEC.md R1 target + Acceptance line 3 (stranded-value count logged; {}-address and ''-PRO covered)
    - lib/profile/semantic-blank.ts (19-01 twin — the exact blank predicates + RESCUE_FIELD_MAP this SQL mirrors)
    - supabase/migrations/066 lines 1-30 (human-gated header) + migrations 021/026 (mailing_address DEFAULT '{}' and the user_profiles column shape)
  </read_first>
  <action>
    Author NEW migration 071. Include the human-gated header stating an executor agent must NEVER run supabase db push; the live push is the 19-07 blocking human checkpoint. In a DO block, UPDATE artist_profiles from a candidates CTE joined on id to user_profiles, applying canonical-wins per field using the twin's rules: for each text field (pro, ipi, publisher) copy the user_profiles value only when the artist_profiles value is semantic-blank via TRIM-based emptiness; map user_profiles.phone into artist_profiles.contact_phone under the same rule; map user_profiles.display_name into artist_profiles.artist_name and user_profiles.bio into artist_profiles.bio under the same rule; for mailing_address use the JSONB blank-check (IS NULL OR equals the empty JSON object) so a real stranded address is rescued over an empty canonical one (Pitfall 6). Capture GET DIAGNOSTICS row count, and compute a stranded-value count for rows where user_profiles held a non-blank value over a semantic-blank canonical value across pro/ipi/publisher/phone/mailing_address/display_name/bio. RAISE NOTICE both counts so they appear in the push output Pete reviews (the audit trail R1 AC line 3 requires). This migration performs NO drop and does NOT reference the functions — rescue only. Its logic must match lib/profile/semantic-blank.ts field-for-field.
  </action>
  <verify>
    <automated>test -f supabase/migrations/071_user_profiles_data_rescue.sql && grep -q "RAISE NOTICE" supabase/migrations/071_user_profiles_data_rescue.sql && grep -q "contact_phone" supabase/migrations/071_user_profiles_data_rescue.sql && grep -q "artist_name" supabase/migrations/071_user_profiles_data_rescue.sql && grep -q "NEVER run" supabase/migrations/071_user_profiles_data_rescue.sql && npx jest __tests__/rescue-semantic-blank.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 071 exists, rescues rights + phone->contact_phone + display_name->artist_name + bio->bio, canonical-wins
    - mailing_address blank-check handles the empty-JSON case; ''-PRO case rescued
    - Pre/post row count and stranded-value count logged via RAISE NOTICE
    - No DROP and no function change in this file
  </acceptance_criteria>
  <done>The rescue migration recovers stranded data with a logged audit trail, mirroring the 19-01 twin.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 072 — re-point both functions + claim_prefill column + R2 reverse pre-fill</name>
  <files>supabase/migrations/072_repoint_claim_functions.sql</files>
  <read_first>
    - 19-PATTERNS.md "supabase/migrations/072_repoint_claim_functions.sql — R1 function re-point" (full CREATE OR REPLACE shape) + "Pattern 3" claim_prefill column
    - 19-RESEARCH.md "Pattern 2: Re-point a SECURITY DEFINER function" + Pitfall 1 (BOTH functions move together) + Pitfall 3 (provenance = inviting artist's name)
    - supabase/migrations/026 lines ~119-146 (backfill_claimed_collaborators original body) and migration 051 (claim_collaborators re-create shape)
    - lib/profile/claim-prefill.ts (19-01 twin — shouldPrefill idempotency, pickWinningSource most-recent-wins, ClaimPrefillEntry shape)
  </read_first>
  <action>
    Author NEW migration 072 with the human-gated header. Add the private column: ALTER TABLE artist_profiles ADD COLUMN IF NOT EXISTS claim_prefill JSONB DEFAULT the empty JSON object, with a comment noting it is private (migration 040 doctrine) and must NOT be added to any GRANT SELECT list — same posture as legal_name_locked_at/administrator. CREATE OR REPLACE claim_collaborators(p_user_id UUID, p_email TEXT): keep the claimed_by stamping; RE-POINT the rights SELECT to read pro/ipi/publisher/contact_phone/mailing_address FROM artist_profiles (was user_profiles), then COALESCE-backfill the claimed collaborators' blank fields exactly as today (forward profile->credit). Then ADD the R2 reverse pre-fill: for each canonical field that is semantic-blank on artist_profiles for this user, select the winning value from that user's claimed collaborators rows (most-recent by collaborators.updated_at — Pitfall 3/twin pickWinningSource), write it into artist_profiles, and record a claim_prefill entry keyed by canonical field name with { confirmed: false, source_collaborator_id: <that collaborators.id>, source_name: <the inviting artist's artist_profiles.artist_name resolved via collaborators.user_id>, filled_at: now }. Make it idempotent per the twin's shouldPrefill: never pre-fill a field whose canonical value is non-blank, and never overwrite a claim_prefill entry already marked confirmed. CREATE OR REPLACE backfill_claimed_collaborators() applying the IDENTICAL source-table re-point (artist_profiles, phone->contact_phone) — BOTH functions must change in THIS file (Pitfall 1, Edge: Missed reader). End with NOTIFY pgrst to reload the schema cache. The idempotency + conflict logic must match lib/profile/claim-prefill.ts.
  </action>
  <verify>
    <automated>test -f supabase/migrations/072_repoint_claim_functions.sql && grep -q "claim_prefill" supabase/migrations/072_repoint_claim_functions.sql && grep -q "artist_profiles" supabase/migrations/072_repoint_claim_functions.sql && grep -q "backfill_claimed_collaborators" supabase/migrations/072_repoint_claim_functions.sql && grep -q "NOTIFY pgrst" supabase/migrations/072_repoint_claim_functions.sql && npx jest __tests__/claim-prefill.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - BOTH claim_collaborators() and backfill_claimed_collaborators() read artist_profiles (phone->contact_phone) — neither references user_profiles
    - claim_prefill JSONB column added, private, defaulting to empty object
    - Reverse pre-fill writes unconfirmed provenance entries with the inviting-artist source_name, most-recent-wins, idempotent (no overwrite of confirmed/non-blank)
    - NOTIFY pgrst present; twin test green
  </acceptance_criteria>
  <done>Both readers point at the canonical table and the claim path now seeds + provenances a new user's blank profile, matching the 19-01 twins.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 073 — drop user_profiles (strictly last)</name>
  <files>supabase/migrations/073_drop_user_profiles.sql</files>
  <read_first>
    - 19-SPEC.md Constraints (drop only after rescue + re-point) + Prohibitions (no drop before both readers re-pointed)
    - 19-RESEARCH.md "Anti-Patterns to Avoid" (separate, separately-reviewable files) + Pitfall 1
    - supabase/migrations/053 (the current user_profiles definition being removed) + migration 066 header convention
  </read_first>
  <action>
    Author NEW migration 073 with the human-gated header. It contains ONLY the destructive removal of user_profiles: DROP TABLE IF EXISTS public.user_profiles CASCADE (which also removes its RLS policies/triggers from 026/027/053). Add a header comment stating this file MUST be pushed only after 071 (rescue) and 072 (re-point) have applied successfully, and that a repo-wide grep for user_profiles outside historical migration files must return zero runtime hits before this is pushed (the 19-05 runtime removal + the 19-07 checkpoint enforce this). Do not recreate user_profiles for any purpose — the freed name is reserved for Phase 20's rename. Author only — never push.
  </action>
  <verify>
    <automated>test -f supabase/migrations/073_drop_user_profiles.sql && grep -q "DROP TABLE" supabase/migrations/073_drop_user_profiles.sql && grep -q "user_profiles" supabase/migrations/073_drop_user_profiles.sql && grep -q "NEVER run" supabase/migrations/073_drop_user_profiles.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 073 drops user_profiles and only that; ordered strictly after 071/072 by filename
    - Header documents the rescue+re-point precondition and the zero-runtime-reference precondition
    - No recreation of user_profiles; historical migrations byte-unchanged
  </acceptance_criteria>
  <done>The drop migration is authored last-in-order and gated on rescue + re-point + zero runtime references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> live remote DB | Destructive/irreversible schema change; crossed only by Pete at the 19-07 human-gated push, never by an executor. |
| claim_collaborators() SECURITY DEFINER -> artist_profiles / collaborators | Elevated-privilege writes; must stay scoped to the claiming user's own id and never fabricate provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-14 | Repudiation | rescue silently loses PII | high | mitigate | 071 RAISE NOTICE pre/post + stranded-value audit log (R1 AC line 3); rescue is a separate file from the drop so a bug is reviewable before 073 runs. |
| T-19-15 | Tampering | drop before re-point strands backfill_claimed_collaborators | high | mitigate | 071/072/073 strict filename ordering; BOTH functions re-pointed in 072; 19-05 removes all runtime refs before 19-07 pushes 073. |
| T-19-16 | Information Disclosure | claim_prefill readable via direct PostgREST | medium | mitigate | claim_prefill added as a private column, excluded from every GRANT SELECT list (migration 040 doctrine), server-owned only. |
| T-19-17 | Tampering | forged provenance in claim_prefill | medium | mitigate | Provenance is written server-side by the SECURITY DEFINER function from server-resolved ids/names; the client never supplies claim_prefill (enforced in 19-05's API). |

*No packages installed this plan — supply-chain threat (T-19-SC) not applicable.*
</threat_model>

<verification>
- Structural greps (scoped above) pass for 071/072/073
- `npx jest __tests__/rescue-semantic-blank.test.ts __tests__/claim-prefill.test.ts` green (parity twins)
- Historical migrations 026/051/053 byte-unchanged (git diff shows only NEW files)
- Live application (rescue counts, function behavior, drop) is verified at the 19-07 human-gated push via `supabase migration list` LOCAL=REMOTE
</verification>

<success_criteria>
Three ordered migrations exist: a logged semantic-blank rescue, a both-readers re-point with the claim_prefill column and R2 reverse pre-fill, and a last-in-order drop — each mirroring its 19-01 twin and gated for the 19-07 human push, with no historical migration edited.
</success_criteria>

<output>
Create `.planning/phases/19-profile-identity-model-cleanup/19-04-SUMMARY.md` when done.
</output>
