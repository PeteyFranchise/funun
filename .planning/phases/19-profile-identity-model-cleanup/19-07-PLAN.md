---
phase: 19-profile-identity-model-cleanup
plan: 07
type: execute
wave: 3
depends_on: ["19-03", "19-04", "19-05"]
files_modified: []
autonomous: false
requirements: [R1, R2, R4]

must_haves:
  truths:
    - "Migrations 071, 072, 073, 074 are applied to the remote DB in strict filename order; the rescue (071) and both-reader re-point (072) apply BEFORE the drop (073); the flags table (074) is additive"
    - "supabase migration list shows LOCAL=REMOTE for all migrations 001 through 074 after the push"
    - "071's RAISE NOTICE stranded-value + rescue counts are captured in the push output as the R1 audit trail"
    - "user_profiles is dropped only after zero runtime references remain (19-05) and both DB readers are re-pointed (19-04/072)"
  artifacts:
    - "Live remote schema: user_profiles dropped; artist_profiles.claim_prefill live; claim_collaborators()/backfill_claimed_collaborators() reading artist_profiles; split_sheet_identity_flags live with RLS"
  key_links:
    - "This checkpoint is the single schema-cutover gate; it depends on migration authoring (19-04, 19-03) AND the runtime removal of the /api/user-profiles writer (19-05) so 073's drop never strands live code"
  prohibitions:
    - statement: "MUST NOT drop user_profiles before the data-rescue migration runs, nor before both DB readers are re-pointed"
      status: resolved
      verification: "test — push order is 071->072->073->074 by filename; verification is supabase migration list LOCAL=REMOTE; the pre-drop grep gate (19-05) already returned zero runtime references"
    - statement: "MUST NOT run supabase db push from an executor agent"
      status: resolved
      verification: "human — this task is autonomous:false; only Pete pushes via Codex, per project convention"
---

<objective>
The single human-gated schema-cutover checkpoint for Phase 19. Every schema change (071 rescue, 072 re-point + claim_prefill + reverse pre-fill, 073 drop, 074 flags table) has been authored as a NEW migration file by the upstream plans; NONE has been pushed. Pete pushes them via Codex in strict filename order and verifies LOCAL=REMOTE. This gate exists AFTER the runtime removal of the /api/user-profiles writer (19-05) so that 073's drop of user_profiles never strands live code.

Purpose: Apply the destructive/irreversible cutover under human control, with the rescue audit trail visible, exactly as every prior phase's migration checkpoint (058/062/063/065/066).
Output: Live remote schema matching the four migrations; verified LOCAL=REMOTE.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-profile-identity-model-cleanup/19-SPEC.md
</context>

<artifacts>
## Artifacts this plan produces
- No new files. Produces the LIVE remote-schema state: `user_profiles` dropped; `artist_profiles.claim_prefill` present; both claim functions reading `artist_profiles`; `split_sheet_identity_flags` present with RLS — all verified LOCAL=REMOTE.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Pre-push sanity gate (executor)</name>
  <files></files>
  <read_first>
    - 19-SPEC.md Constraints (human-gated migrations; drop only after rescue + re-point) + Prohibitions
    - 19-RESEARCH.md Pitfall 1 (grep for user_profiles before the drop is pushed)
  </read_first>
  <action>
    Before handing off to the human push, the executor performs a read-only sanity gate (no db push). Confirm all four migration files exist (071, 072, 073, 074) and are ordered. Confirm a repo-wide grep for user_profiles / user-profiles / UserProfile returns hits ONLY inside supabase/migrations/ historical files and __tests__/ (the migration-content assertions) — zero runtime references under app/components/lib. Confirm the full Jest suite, tsc --noEmit, and lint are green on the branch. Report the results; if any runtime user_profiles reference remains or any migration file is missing, STOP and flag it (do not proceed to the human checkpoint).
  </action>
  <verify>
    <automated>ls supabase/migrations/071_*.sql supabase/migrations/072_*.sql supabase/migrations/073_*.sql supabase/migrations/074_*.sql >/dev/null 2>&1 && [ "$(grep -rln 'user_profiles\|user-profiles\|UserProfile' app components lib --include='*.ts' --include='*.tsx' 2>/dev/null | grep -v __tests__ | wc -l | tr -d ' ')" = "0" ] && echo "PRE-PUSH OK"</automated>
  </verify>
  <acceptance_criteria>
    - All four migration files present and ordered
    - Zero runtime user_profiles references (migrations + tests excepted)
    - Full suite + tsc + lint green
  </acceptance_criteria>
  <done>The branch is proven safe to push: migrations authored, no runtime references, suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human-gated database push for migrations 071/072/073/074</name>
  <action>Stop. Pete applies migrations 071 -> 072 -> 073 -> 074 in filename order via Codex and verifies LOCAL=REMOTE. No executor agent runs supabase db push.</action>
  <what-built>
    Four authored, unpushed migrations that perform the Phase 19 schema cutover:
    - 071_user_profiles_data_rescue.sql — semantic-blank rescue into artist_profiles, logs pre/post + stranded counts
    - 072_repoint_claim_functions.sql — re-points BOTH claim_collaborators() and backfill_claimed_collaborators() to artist_profiles, adds the private claim_prefill column + R2 reverse pre-fill, NOTIFY pgrst
    - 073_drop_user_profiles.sql — DROP TABLE user_profiles (strictly last)
    - 074_split_sheet_identity_flags.sql — new correction-flag table + RLS
    An executor agent must NEVER run supabase db push (project convention). This is Pete's push, via Codex, exactly like the Phase 17/18 migration checkpoints.
  </what-built>
  <how-to-verify>
    1. Review each of 071/072/073/074 (they are additive/ordered; historical migrations are byte-unchanged).
    2. Push via Codex: `supabase db push` — this applies 071 -> 072 -> 073 -> 074 in filename order.
    3. Watch the push output for migration 071's RAISE NOTICE lines: the pre/post rescue row count and the stranded-value count (the R1 audit trail). Note them in the resume signal.
    4. Verify with `supabase migration list` — LOCAL must equal REMOTE for all migrations 001 through 074.
    5. Optional spot-checks (if console access): artist_profiles.claim_prefill exists and is NOT in any GRANT SELECT; user_profiles no longer exists; split_sheet_identity_flags exists with RLS enabled.
  </how-to-verify>
  <resume-signal>Type "approved" with the migration-list confirmation (LOCAL=REMOTE for 001-074) and 071's logged rescue/stranded counts — or describe any failure (e.g. a rescue count that looks wrong) so the migration can be corrected as a NEW file before re-push.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored migration -> live remote DB | The only crossing is Pete's human-gated push; executors never push. A destructive drop is applied here, gated on rescue + re-point + zero runtime references. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-24 | Repudiation | rescue silently loses data at push time | high | mitigate | 071's RAISE NOTICE counts are read from the live push output and recorded in the resume signal; the rescue is reviewable before the drop applies. |
| T-19-25 | Denial of Service | drop strands live code (/api/user-profiles) | high | mitigate | Checkpoint depends on 19-05 (route deleted, zero runtime references) and the pre-push grep gate; 073 applies only after 071/072 by filename order. |
| T-19-26 | Tampering | unauthorized/premature push | high | mitigate | autonomous:false; only the human operator pushes via Codex; executor agents are forbidden from supabase db push by project convention. |

*No packages installed this plan — supply-chain threat (T-19-SC) not applicable.*
</threat_model>

<verification>
- `supabase migration list` shows LOCAL=REMOTE for 001-074 (recorded in the resume signal)
- 071's logged rescue + stranded-value counts captured
- Post-push: user_profiles gone; artist_profiles.claim_prefill live; both claim functions read artist_profiles; split_sheet_identity_flags live with RLS
- Full Jest suite + tsc + lint remained green pre-push
</verification>

<success_criteria>
The four migrations are live and verified LOCAL=REMOTE, the rescue audit trail is captured, the duplicate table is gone with no data loss and no stranded runtime code, and the R2 provenance column + R4 flags table are live — unblocking end-to-end UAT of R1/R2/R4 and freeing the user_profiles name for Phase 20.
</success_criteria>

<output>
Create `.planning/phases/19-profile-identity-model-cleanup/19-07-SUMMARY.md` when done.
</output>
