---
phase: 19-profile-identity-model-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/profile/semantic-blank.ts
  - lib/profile/claim-prefill.ts
  - __tests__/rescue-semantic-blank.test.ts
  - __tests__/claim-prefill.test.ts
  - lib/split-sheets/live-identity.test.ts
autonomous: true
requirements: [R1, R2, R3]

must_haves:
  truths:
    - "Semantic-blank detection treats NULL, trimmed-empty text, and empty-JSON {} as equivalently blank; a meaningful canonical value always wins over a stranded value (Edge: Data-loss non-NULL blanks, R1)"
    - "The rescue twin maps phone->contact_phone, display_name->artist_name, bio->bio, and covers the {}-address and ''-PRO cases specifically (Edge: Data-loss unmapped columns, R1)"
    - "Claim pre-fill conflict resolution is idempotent: re-running never overwrites a confirmed, edited, or pre-existing non-blank value (Edge: Idempotency/re-run, R2)"
    - "On conflicting sources for one field, the most-recent by collaborators.updated_at wins (Edge: Source conflict, R2)"
    - "resolvePartyIdentity returns the frozen snapshot for esign_pending and executed even when a non-null claimed profile is supplied; live states resolve from the claimed profile (Edge: Freeze-boundary regression, R3)"
    - "Email-mismatch claiming (signup email != collaborator email) is out of scope and yields no claim — a documented limitation, not covered here (Edge: Email mismatch, R2 — DISMISSED, surfaced explicitly)"
  artifacts:
    - "lib/profile/semantic-blank.ts — pure semantic-blank + field-mapping twin for migration 071"
    - "lib/profile/claim-prefill.ts — pure conflict-resolution + idempotency twin for migration 072's reverse pre-fill, exporting the shared claim_prefill entry shape"
    - "__tests__/rescue-semantic-blank.test.ts, __tests__/claim-prefill.test.ts, lib/split-sheets/live-identity.test.ts"
  key_links:
    - "lib/profile/claim-prefill.ts is the single source of truth for the claim_prefill JSONB entry shape consumed by BOTH migration 072 (19-04) and the R2 confirm UI (19-05) — they must not drift"
    - "resolvePartyIdentity freeze boundary (esign_pending/executed) is aligned with lifecycle.ts's edit boundary; the R3 test locks it"
  prohibitions:
    - statement: "MUST NOT lose non-NULL-but-blank ({} / '') or unmapped (phone, display_name, bio) data in the rescue"
      status: resolved
      verification: "test — semantic-blank rescue twin exercised over {}-address and ''-text fixtures (__tests__/rescue-semantic-blank.test.ts)"
    - statement: "MUST NOT present claim-pre-filled data as authoritative without the confirm step (locking a stranger's typo as identity)"
      status: resolved
      verification: "judgment — claim-prefill twin emits an unconfirmed provenance entry; confirmed values are never re-overwritten (__tests__/claim-prefill.test.ts)"
---

<objective>
Build the pure-TypeScript parity twins and regression tests that anchor Phase 19's data-migration and reconciliation logic BEFORE the SQL migrations and UI are authored. Jest cannot execute PL/pgSQL, so — following this repo's Phase 18-04 `coverage-fixtures.ts` precedent — the semantic-blank rescue (R1) and the claim pre-fill conflict/idempotency logic (R2) are each extracted into a small, fully-tested pure module that the corresponding migration mirrors exactly. Add the R3 freeze-boundary regression test that locks `resolvePartyIdentity`'s behavior (which R1's table drop must not change).

Purpose: Give downstream migration/UI plans a machine-checked contract for "what blank means," "which source wins," and "what stays frozen" — the three subtle decisions this phase is most likely to get wrong.
Output: Two new pure modules + three test files, all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-profile-identity-model-cleanup/19-SPEC.md
@.planning/phases/19-profile-identity-model-cleanup/19-RESEARCH.md
@.planning/phases/19-profile-identity-model-cleanup/19-PATTERNS.md

# Twin-source patterns
@lib/split-sheets/live-identity.ts
</context>

<artifacts>
## Artifacts this plan produces
- `lib/profile/semantic-blank.ts` — `isSemanticBlankText()`, `isSemanticBlankJson()`, and a `rescueValue()`/mapping helper (canonical-wins) — the pure twin migration 071 mirrors
- `lib/profile/claim-prefill.ts` — `ClaimPrefillEntry` type `{ confirmed: boolean; source_collaborator_id: string; source_name: string; filled_at: string }`, plus `pickWinningSource()` (most-recent by `updated_at`) and `shouldPrefill()` (idempotency guard) — the pure twin migration 072's reverse pre-fill mirrors, and the shape the R2 UI reads
- `__tests__/rescue-semantic-blank.test.ts`, `__tests__/claim-prefill.test.ts`, `lib/split-sheets/live-identity.test.ts`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Semantic-blank rescue twin (R1)</name>
  <files>lib/profile/semantic-blank.ts, __tests__/rescue-semantic-blank.test.ts</files>
  <read_first>
    - 19-RESEARCH.md "Pattern 1: Semantic-blank rescue, canonical-wins" and "Pitfall 6" (the `{}`-address JSONB blank-check)
    - 19-SPEC.md R1 target + Acceptance Criteria line 3 (the `{}`-address and `''`-PRO cases)
    - lib/profile/validate.ts — existing pure-helper + named-export conventions in this directory
  </read_first>
  <action>
    Create a named-export pure module implementing the SPEC's exact semantic-blank definition. Export `isSemanticBlankText(v)` returning true for null/undefined and for a value whose trimmed string is empty; export `isSemanticBlankJson(v)` returning true for null and for the empty object (deep-equal to `{}`), matching Pitfall 6's requirement that an empty JSONB object counts as blank. Export a `rescueValue(canonical, stranded, kind)` helper that returns the stranded value only when the canonical value is semantic-blank by the matching predicate, otherwise the canonical value (canonical-wins). Export a `RESCUE_FIELD_MAP` describing the source->target column mapping the migration will implement: pro->pro, ipi->ipi, publisher->publisher, phone->contact_phone, mailing_address->mailing_address (json), display_name->artist_name, bio->bio. Do NOT build a generic isEmpty utility (per 19-RESEARCH "Don't Hand-Roll"). No I/O, no Supabase import — this is a pure logic twin whose only consumer is its test and (by mirroring) migration 071. Follow project style: 2-space indent, no semicolons, `@/` imports if any, named exports.
    Write the test first (tdd), covering: `''`-PRO over a blank canonical is rescued; a real canonical PRO wins over a stranded one; a `{}` mailing_address is treated blank so a real stranded address is rescued; a non-empty canonical address wins over `{}`; the phone->contact_phone and display_name->artist_name mappings resolve correctly; whitespace-only text is blank.
  </action>
  <behavior>
    - isSemanticBlankText('') === true; isSemanticBlankText('  ') === true; isSemanticBlankText(null) === true; isSemanticBlankText('ASCAP') === false
    - isSemanticBlankJson({}) === true; isSemanticBlankJson(null) === true; isSemanticBlankJson({ line1: '1 Main St' }) === false
    - rescueValue('', 'BMI', 'text') === 'BMI'; rescueValue('ASCAP', 'BMI', 'text') === 'ASCAP'
    - rescueValue({}, { line1: 'x' }, 'json') deep-equals { line1: 'x' }
    - RESCUE_FIELD_MAP maps phone->contact_phone and display_name->artist_name
  </behavior>
  <verify>
    <automated>npx jest __tests__/rescue-semantic-blank.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New module + test both exist and the test passes
    - Text and JSON blank predicates match the SPEC's NULL / trimmed-empty / `{}` definition
    - Field map includes the phone->contact_phone, display_name->artist_name, bio->bio mappings
  </acceptance_criteria>
  <done>The semantic-blank twin is green and covers the `{}`-address and `''`-PRO cases the SPEC calls out.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Claim pre-fill conflict + idempotency twin (R2)</name>
  <files>lib/profile/claim-prefill.ts, __tests__/claim-prefill.test.ts</files>
  <read_first>
    - 19-RESEARCH.md "Pattern 3: Per-field confirm-and-lock" + Assumptions A1/A2 + Open Question 1
    - 19-SPEC.md R2 target + Acceptance Criteria line 5 (idempotent, never overwrite confirmed/edited/non-blank, most-recent wins)
    - 19-CONTEXT.md D-01..D-04 (live-but-flagged, per-field, named provenance = the person who added you)
    - lib/profile/semantic-blank.ts (Task 1 — reuse its blank predicates)
  </read_first>
  <action>
    Create a named-export pure module defining the shared claim-pre-fill entry shape and the two decisions migration 072's SQL must implement identically. Export type `ClaimPrefillEntry = { confirmed: boolean; source_collaborator_id: string; source_name: string; filled_at: string }` — `source_name` is the display name of THE PERSON WHO ADDED YOU (the inviting artist), resolving D-03 and RESEARCH Open Question 1 to the inviting artist's name, NOT the song. Export `pickWinningSource(candidates)` that, given claimed-collaborator source rows each carrying a field value and an `updated_at`, returns the value from the most-recent row (Edge: Source conflict). Export `shouldPrefill(canonicalValue, existingEntry)` implementing the idempotency guard: return false when the canonical value is non-blank (reuse `isSemanticBlankText`/`isSemanticBlankJson`), OR when an existing entry has `confirmed: true` (user confirmed), OR when the field was edited (canonical non-blank implies edited/owned) — return true only when the field is semantic-blank and not already confirmed. This is the exact rule the SQL reverse-pre-fill uses; it is the parity anchor, no Supabase I/O. Named exports, no semicolons, 2-space indent.
    Write the test first: a blank field with a single source pre-fills and yields an unconfirmed entry; re-running with the same inputs does not change a `confirmed: true` entry; a canonical non-blank value is never overwritten; two sources with different `updated_at` resolve to the newer one; provenance carries the inviting-artist `source_name`.
  </action>
  <behavior>
    - shouldPrefill('', undefined) === true (blank, never claimed)
    - shouldPrefill('', { confirmed: true, ... }) === false (already confirmed)
    - shouldPrefill('ASCAP', undefined) === false (non-blank, owned)
    - pickWinningSource([{value:'BMI',updated_at:'2026-01-01'},{value:'ASCAP',updated_at:'2026-06-01'}]).value === 'ASCAP'
    - a produced ClaimPrefillEntry has confirmed:false, a source_collaborator_id, a source_name (inviting artist), and a filled_at
  </behavior>
  <verify>
    <automated>npx jest __tests__/claim-prefill.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Module + test exist and pass
    - Idempotency guard refuses to overwrite confirmed/edited/non-blank fields
    - Conflict resolution returns the most-recent-by-updated_at source
    - `source_name` is documented and tested as the inviting artist's display name (D-03), not the song
  </acceptance_criteria>
  <done>The claim-prefill twin is green and is the single shape/logic contract 19-04's migration and 19-05's UI both build against.</done>
</task>

<task type="auto">
  <name>Task 3: R3 freeze-boundary regression test</name>
  <files>lib/split-sheets/live-identity.test.ts</files>
  <read_first>
    - lib/split-sheets/live-identity.ts (full — `resolvePartyIdentity` signature + freeze boundary)
    - 19-PATTERNS.md "lib/split-sheets/live-identity.ts (utility, transform) — R3, no code change"
    - 19-SPEC.md R3 Acceptance Criteria line 6
  </read_first>
  <action>
    Add a new co-located test file for the existing resolver (there is currently NO test for this load-bearing module). Import `resolvePartyIdentity` and `LivePartyIdentitySource` from the module. Assert the freeze boundary is unchanged from today: for status `esign_pending` and `executed`, the resolver returns the frozen snapshot object EVEN when a non-null claimed profile with different values is supplied; for `draft`, `pending_approval`, `approved`, and `countered`, it resolves from the claimed profile. Do NOT modify live-identity.ts — R3's entire job is "don't break this"; this test is the regression guard that fails loudly if any later change (or Phase 20's rename) moves the boundary. Match the repo convention of a `.test.ts` file next to the source module.
  </action>
  <verify>
    <automated>npx jest lib/split-sheets/live-identity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - esign_pending and executed return frozen even with a conflicting claimed profile
    - draft/pending_approval/approved/countered live-resolve from the claimed profile
    - live-identity.ts source is byte-unchanged (test-only addition)
  </acceptance_criteria>
  <done>A failing-if-boundary-moves regression test exists for resolvePartyIdentity, locking R3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed this plan) | Pure-logic twins + tests; no route, no request input, no DB write. Threats materialize in the plans that consume these twins (19-04 migrations, 19-05 UI). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | claim-prefill twin correctness | medium | mitigate | The twin is the parity contract; a wrong rule here silently propagates into migration 072. Mitigated by explicit unit tests for idempotency + conflict resolution (Task 2). |
| T-19-02 | Repudiation | semantic-blank twin correctness | low | mitigate | Wrong blank-detection would let the rescue drop data; mitigated by `{}`-address and `''`-PRO fixtures (Task 1) that also drive migration 071's audit expectations. |

*No packages installed this plan — supply-chain threat (T-19-SC) is not applicable to Phase 19 (zero external dependencies added).*
</threat_model>

<verification>
- `npx jest __tests__/rescue-semantic-blank.test.ts __tests__/claim-prefill.test.ts lib/split-sheets/live-identity.test.ts` all green
- `npx tsc --noEmit` clean for the two new modules
- No change to lib/split-sheets/live-identity.ts (git diff shows only the new test file for that module)
</verification>

<success_criteria>
Three test files green; two pure twins exported with the documented shapes; the claim_prefill entry shape and the freeze-boundary behavior are now machine-locked for the downstream migration/UI plans to build against.
</success_criteria>

<output>
Create `.planning/phases/19-profile-identity-model-cleanup/19-01-SUMMARY.md` when done.
</output>
