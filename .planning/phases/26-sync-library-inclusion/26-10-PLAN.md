---
phase: 26-sync-library-inclusion
plan: 10
type: execute
wave: 3
depends_on: ["26-05"]
files_modified:
  - components/admin/SyncLibraryAdmin.tsx
  - app/(admin)/admin/sync-library/page.tsx
  - app/(admin)/layout.tsx
autonomous: true
requirements: [SYNCLIB-15, SYNCLIB-08, SYNCLIB-09, SYNCLIB-05]

must_haves:
  truths:
    - "Permissioned staff can invite an artist and work a per-song curation queue (admit/reject) from one admin Sync Library section."
    - "The queue shows invited vs self-applied as metadata on one consistent list — not two flows."
    - "The leadership-only 'Remove from Sync Library' action appears only for leadership on an admitted song."
  artifacts:
    - "components/admin/SyncLibraryAdmin.tsx"
    - "app/(admin)/admin/sync-library/page.tsx"
    - "app/(admin)/layout.tsx (sidebar link)"
  key_links:
    - "SyncLibraryAdmin → /api/sync-library/invite, /api/sync-library/admin/[listingId], .../remove"
    - "curation queue rows → sync_listings joined to artist + song + project"
---

<objective>
Build the staff-facing admin Sync Library section: an invite panel plus the per-song curation queue (admit/reject), with the leadership-only removal action, composing the three existing admin analog patterns.

Purpose: This is where Funūn curates the whole catalogue — the one consistent human gate (decision #2) plus the leadership takedown. It composes CapabilityRequestsAdmin (row action pair), DealsQueue (filter chips + relative time), and BuyerOrgsAdmin (collapsed invite form) so it matches the admin console exactly.

Output: `SyncLibraryAdmin`, its staff-gated admin page, and the sidebar link.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sync-library-inclusion/26-CONTEXT.md
@.planning/phases/26-sync-library-inclusion/26-UI-SPEC.md
@.planning/phases/26-sync-library-inclusion/26-PATTERNS.md
@components/admin/CapabilityRequestsAdmin.tsx
@app/(admin)/admin/capability-requests/page.tsx
@app/(admin)/layout.tsx
@lib/admin/gate.ts
@./.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SyncLibraryAdmin component (invite panel + curation queue + leadership remove)</name>
  <files>components/admin/SyncLibraryAdmin.tsx</files>
  <read_first>
    - components/admin/CapabilityRequestsAdmin.tsx lines 41-60 (optimistic decision handler: setPendingId, fetch, remove-on-success, error banner) and 103-121 (the Approve/Deny row-action pair with `bg-white text-black` / `border-white/10 text-white/60`) — mirror for Admit/Reject.
    - components/admin/DealsQueue.tsx — CHIP_BASE/CHIP_ON/CHIP_OFF filter chips + `formatTimeSince` relative time (reuse verbatim), default filter to "Needs review".
    - components/admin/BuyerOrgsAdmin.tsx line ~285 — the collapsed toggle-form idiom + the exact `mb-4 rounded-lg bg-grad px-4 py-2.5 text-[13px] font-bold text-white shadow` toggle button for "+ Invite artist".
    - 26-UI-SPEC.md Screens F + G + "Per-Surface Copywriting" #6/#7, "Design System" note (ADMIN uses `.fncon` CSS-variable classes — NEVER hardcode bg-ink/text-lav here), and the Admin admission-queue status-chip table.
    - 26-CONTEXT.md decision #1 (rejection reason field), decision #2 (one gate; invited vs self-applied is metadata), the takedown bullet (leadership-only Remove).
  </read_first>
  <action>
    Build components/admin/SyncLibraryAdmin.tsx (client), composing:
    1. Invite panel — collapsed toggle-form (BuyerOrgsAdmin idiom); expanded form heading "Invite an artist to the Sync Library" + a field to pick the artist (id/email) + "Send invite" (gradient) → POST /api/sync-library/invite. Optimistic + error banner.
    2. Curation queue — DealsQueue-style client filter chips defaulting to "Needs review" (oldest first). Each row: artist name, song title, source project, submitted date (formatTimeSince), the Admin admission-queue status chip (Needs review=amber / Ready to admit=indigo / Admitted=emerald / Rejected=rose per UI-SPEC), and actions:
       - For a listing awaiting the gate (pending_admit): "Admit" (`bg-white text-black`) → POST /api/sync-library/admin/[listingId] {decision:'admit'}; "Reject" (neutral outline) → same route {decision:'reject', reason} with an optional reason input (decision #1). pendingId-driven "Working…" disabled state (CapabilityRequestsAdmin convention).
       - For an ADMITTED listing: a "Remove from Sync Library" action → POST .../remove — rendered ONLY when an `isLeadership` prop is true (leadership-only; the route enforces regardless). Use the destructive rose treatment per UI-SPEC (Remove is a real takedown), with the withdrawal-style inline confirm (two-button row), not a browser confirm().
    Empty state copy: "No submissions yet" / "Artist-submitted and invited songs will appear here for review, oldest first." All classes are `.fncon`-scoped CSS-variable classes.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    SyncLibraryAdmin renders the invite toggle-form, a filterable curation queue with Admit/Reject (+ optional reason) mirroring CapabilityRequestsAdmin, and a leadership-only Remove action on admitted rows; uses `.fncon` classes only; tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Staff-gated admin page + sidebar link</name>
  <files>app/(admin)/admin/sync-library/page.tsx, app/(admin)/layout.tsx</files>
  <read_first>
    - app/(admin)/admin/capability-requests/page.tsx — the per-page staff check (`getStaffRole`, redirect) + the per-row email/name enrichment via `service.auth.admin.getUserById` + user_profiles.
    - app/(admin)/layout.tsx lines ~47-89 — the sidebar `<Link className={NAV_LINK_CLASS}>` list where the new "Sync Library" link goes (near Client Partners / Deals).
    - lib/admin/gate.ts (getStaffRole, StaffRole) — resolve the viewer's role to pass `isLeadership` down.
  </read_first>
  <action>
    app/(admin)/admin/sync-library/page.tsx (server): explicit per-page staff check (redirect '/signin' if no user; redirect '/' if `getStaffRole(user)` is null — allow leadership/ae to view the queue, per CONTEXT curation role). Load the curation queue: sync_listings ordered by applied_at ascending, enriched per row with artist name/email (auth admin + user_profiles), song/track title, and source project title. Pass `isLeadership = getStaffRole(user) === 'leadership'` and the rows to <SyncLibraryAdmin>. Render inside the admin content shell (`flex-1 px-9 py-[30px]`, h1 in `.fncon` tokens).

    app/(admin)/layout.tsx: add a `<Link href="/admin/sync-library" className={NAV_LINK_CLASS}>Sync Library</Link>` to the sidebar list, logically near Client Partners / Deals.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    /admin/sync-library is staff-gated (redirects non-staff), lists the curation queue oldest-first with artist/song/project enrichment, passes isLeadership to gate the Remove action, and a "Sync Library" sidebar link exists; tsc clean; `npm run build` green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff session → admin page | A non-staff session could try to reach the curation queue |
| AE/BD → remove action | The Remove action must not be usable by non-leadership |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-34 | Elevation of Privilege | non-staff reaching /admin/sync-library | high | mitigate | Per-page `getStaffRole` redirect (layout + page double check, project convention); the underlying routes (26-05) also `requireStaff` |
| T-26-35 | Elevation of Privilege | AE/BD invoking removal via the UI | high | mitigate | Remove action rendered only when `isLeadership`; the remove route (26-05) independently enforces `requireStaff(['leadership'])` — UI hiding is not the authority |
| T-26-36 | Information Disclosure | queue exposing artist PII beyond need | low | accept | Enrichment mirrors the existing capability-requests admin pattern (name/email for staff review), scoped to the staff-gated page |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` green.
- Full suite `npx jest` green at wave merge.
- Manual (deferred): as AE, confirm no Remove action; as leadership, confirm Remove works and audits.
</verification>

<success_criteria>
Staff can invite and curate from one admin section that matches the console patterns; only leadership sees/uses Remove; the sidebar link is in place.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- **Component:** `components/admin/SyncLibraryAdmin.tsx` (invite panel + curation queue + leadership remove)
- **Page:** `app/(admin)/admin/sync-library/page.tsx` (staff-gated)
- **Nav (MODIFIED):** `app/(admin)/layout.tsx` — "Sync Library" sidebar link

> Requirements SYNCLIB-05/08/09/15 — provisional; register in REQUIREMENTS.md before phase close.
</artifacts>

<output>
Create `.planning/phases/26-sync-library-inclusion/26-10-SUMMARY.md` when done.
</output>
