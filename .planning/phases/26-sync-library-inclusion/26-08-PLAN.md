---
phase: 26-sync-library-inclusion
plan: 08
type: execute
wave: 3
depends_on: ["26-01", "26-05"]
files_modified:
  - components/sync-library/InvitedSpotlightCard.tsx
  - app/(artist)/dashboard/page.tsx
autonomous: true
requirements: [SYNCLIB-05]

must_haves:
  truths:
    - "An invited artist who has not yet added a song sees a non-dismissible spotlight card at the top of their dashboard inviting them into the Sync Library."
    - "The card disappears once the artist has added at least one song to the sync-library flow."
  artifacts:
    - "components/sync-library/InvitedSpotlightCard.tsx"
    - "app/(artist)/dashboard/page.tsx (server-side visibility check + mount)"
  key_links:
    - "card visibility = approved admin_invited sync_library grant AND zero sync_listings rows (resolved server-side)"
    - "'Review invitation' CTA → /vault (add songs to the sync library)"
---

<objective>
Build the invited-path entry surface: a non-dismissible spotlight card on the artist dashboard, shown only to artists Funūn has invited into the sync library who have not yet added a song.

Purpose: The invite is a hand-picked push and "may be one of the first opportunities an artist sees on their Funūn page" (locked CONTEXT). The card is the single entry point for the invited path; the ongoing per-song status then lives on the song row (26-07), not here.

Output: `InvitedSpotlightCard` and its server-side-gated mount at the top of `/dashboard`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sync-library-inclusion/26-CONTEXT.md
@.planning/phases/26-sync-library-inclusion/26-UI-SPEC.md
@app/(artist)/dashboard/page.tsx
@./.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: InvitedSpotlightCard component</name>
  <files>components/sync-library/InvitedSpotlightCard.tsx</files>
  <read_first>
    - 26-UI-SPEC.md Screen B + "Per-Surface Copywriting" #1 — the exact eyebrow ("Invitation"), heading ("You're invited to the Sync Library"), body, and gradient CTA ("Review invitation"). Card visual: `rounded-card border border-hairstrong bg-card2 p-6`, gradient CTA, the "Invitation" eyebrow pill in the dashboard pinned-card family idiom.
    - app/(artist)/dashboard/page.tsx — the existing pinned-card / "Your next moves" visual family to sit beside.
    - 26-CONTEXT.md decision #3 — the card is NOT dismissible (persists until acted on).
  </read_first>
  <action>
    Build components/sync-library/InvitedSpotlightCard.tsx (can be a server component — no client state; it is non-dismissible so no "not now" control). Use the artist dark tokens (bg-card2, border-hairstrong, bg-grad, .gtext) — NOT admin `.fncon` classes. Gradient "Review invitation" CTA links to `/vault` (the add-songs flow — planner's assigned destination per UI-SPEC open assumption #5). font-semibold only (no font-bold). No dismiss affordance.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    InvitedSpotlightCard renders the locked copy with a single gradient CTA to /vault, no dismiss control, using artist dark tokens; tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Server-gated mount at the top of /dashboard</name>
  <files>app/(artist)/dashboard/page.tsx</files>
  <read_first>
    - app/(artist)/dashboard/page.tsx — the server component's existing data fetch + the page header / "Your next moves" placement (mount ABOVE "Your next moves", below the header).
    - app/(artist)/layout.tsx lines 20-29 — the server-side capability_grants read pattern to mirror for the grant + listings check.
  </read_first>
  <action>
    In the dashboard server component: resolve card visibility server-side — the artist has an APPROVED capability_grants row `capability='sync_library'`, `source='admin_invited'` AND has ZERO sync_listings rows (they have not yet added a song). Only when both hold, render <InvitedSpotlightCard> above the "Your next moves" section. Use a service-role read (mirroring the layout's capability read), never a client fetch. The card auto-disappears once the artist adds a song (a sync_listings row then exists), satisfying the locked "disappears once they've added at least one song" behavior without a dismiss flag.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    The card renders only for an invited artist with zero listings; it is absent for uninvited artists and for invited artists who already have ≥1 listing; tsc clean; `npm run build` green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server render → card visibility | Visibility must be resolved from the trusted grant read, not client-trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-29 | Information Disclosure | card leaking that an artist was invited to another account | low | accept | The card renders only for the signed-in artist's own grant, read server-side scoped to `profile_id = user.id` |
| T-26-30 | Elevation of Privilege | card implying submit-eligibility a non-invited artist lacks | low | mitigate | The card is display-only; the actual submit door (26-03) is ungated and independently server-checked, so the card cannot grant anything |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` green.
- Manual (deferred): create an admin_invited grant, confirm the card shows, add a song, confirm it disappears.
</verification>

<success_criteria>
The invited spotlight card appears exactly for invited-but-not-yet-started artists, is non-dismissible, and routes to the add-songs flow.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- **Component:** `components/sync-library/InvitedSpotlightCard.tsx`
- **Page (MODIFIED):** `app/(artist)/dashboard/page.tsx` — server-gated spotlight mount

> Requirement SYNCLIB-05 — provisional; register in REQUIREMENTS.md before phase close.
</artifacts>

<output>
Create `.planning/phases/26-sync-library-inclusion/26-08-SUMMARY.md` when done.
</output>
