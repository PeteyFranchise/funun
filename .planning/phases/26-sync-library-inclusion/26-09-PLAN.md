---
phase: 26-sync-library-inclusion
plan: 09
type: execute
wave: 3
depends_on: ["26-01", "26-02", "26-05"]
files_modified:
  - app/(artist)/layout.tsx
  - components/nav/ArtistNav.tsx
  - components/nav/icons.tsx
  - lib/sync-library/hub-access.ts
  - app/(artist)/sync-library/page.tsx
  - components/sync-library/SyncLibraryCoachMark.tsx
autonomous: true
requirements: [SYNCLIB-11, SYNCLIB-12, SYNCLIB-13, SYNCLIB-14]

must_haves:
  truths:
    - "The Sync Library nav item appears directly under Deals ONLY once the artist has ≥1 admitted song; Split Sheets sits directly under Contract Locker."
    - "The post-admission hub leads with 'In progress' songs, then Admitted songs and the signed agreement."
    - "When the hub first unlocks, the artist notices it — a 'New' dot on the nav item that clears on first visit, plus a one-time coach-mark."
  artifacts:
    - "components/nav/ArtistNav.tsx (reorder + gated Sync Library item + New dot)"
    - "app/(artist)/sync-library/page.tsx (hub, ≥1-admitted gated)"
    - "lib/sync-library/hub-access.ts (server access + hub data)"
    - "components/sync-library/SyncLibraryCoachMark.tsx (reusable newly-unlocked highlight)"
  key_links:
    - "app/(artist)/layout.tsx resolves hasSyncLibraryAccess (≥1 admitted) server-side → ArtistNav prop"
    - "hub 'In progress' anchor = non-admitted listings; 'Admitted' + 'Your agreement' below"
    - "New dot + coach-mark 'seen' flag via localStorage (no second migration)"
---

<objective>
Deliver the post-admission home: the gated Sync Library nav item (with the locked reorder), the hub page anchored on "In progress", and the newly-unlocked-feature highlight (New dot + coach-mark) — the progressive-disclosure reward for getting a first song in.

Purpose: The hub is earned, not given — it appears only after ≥1 admission, and its arrival must be noticed. Nav visibility is a server-side check mirroring how capabilities are already resolved. The highlight is built as a small reusable primitive so future gated features reuse it (CONTEXT's explicit ask).

Output: the `hasSyncLibraryAccess` server gate + ArtistNav reorder/insert + `SyncLibraryIcon`, the hub page, and the `SyncLibraryCoachMark` + New-dot seen-flag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sync-library-inclusion/26-CONTEXT.md
@.planning/phases/26-sync-library-inclusion/26-UI-SPEC.md
@.planning/phases/26-sync-library-inclusion/26-PATTERNS.md
@components/nav/ArtistNav.tsx
@app/(artist)/layout.tsx
@components/nav/NotificationBell.tsx
@./.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav reorder + gated Sync Library item + icon + server access gate</name>
  <files>app/(artist)/layout.tsx, components/nav/ArtistNav.tsx, components/nav/icons.tsx, lib/sync-library/hub-access.ts</files>
  <read_first>
    - components/nav/ArtistNav.tsx lines 25-55 (ITEMS array + Item type) and 89-93 (visibleItems filter) — the reorder + the data-driven gate go here.
    - app/(artist)/layout.tsx lines 20-33 — the server-side capability_grants read that produces `capabilities` and passes it to ArtistNav; mirror it for `hasSyncLibraryAccess`.
    - components/nav/icons.tsx (DealsIcon/LockerIcon) — the 24×24, stroke="currentColor", `gradient?` construction convention for the new SyncLibraryIcon.
    - 26-CONTEXT.md "Full artist nav order" (the exact before/after-admission orders) and 26-UI-SPEC.md Screen A.
  </read_first>
  <action>
    lib/sync-library/hub-access.ts: export `hasAdmittedSyncListing(service, userId): Promise<boolean>` (a count/exists query for sync_listings status='admitted', artist_user_id=userId).

    app/(artist)/layout.tsx: after the existing capabilities read, compute `hasSyncLibraryAccess = await hasAdmittedSyncListing(service, user.id)` and pass it to <ArtistNav hasSyncLibraryAccess={...}>. Server-side only (never client-fetched), mirroring capabilities.

    components/nav/icons.tsx: add `SyncLibraryIcon` in the sibling idiom — a shelf/catalogue glyph visually distinct from DealsIcon and LockerIcon, accepting `gradient?`.

    components/nav/ArtistNav.tsx:
    - Add a `hasSyncLibraryAccess?: boolean` prop.
    - REORDER the ITEMS array: move the Split Sheets entry to sit directly AFTER Contract Locker (currently after Deals).
    - INSERT a new item `{ href:'/sync-library', label:'Sync Library', match:'/sync-library', Icon: SyncLibraryIcon }` directly AFTER Deals, tagged with a data-driven gate (a new optional field, e.g. `requiresSyncLibraryAccess?: true`) rather than a static capability.
    - Extend `visibleItems` to also drop any item tagged `requiresSyncLibraryAccess` when `!hasSyncLibraryAccess`.
    Resulting orders MUST match 26-CONTEXT exactly (before: Sound Vault · Contract Locker · Split Sheets · Deals · Collaborators …; after admission: same with Sync Library directly under Deals).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Split Sheets renders directly under Contract Locker; the Sync Library item renders directly under Deals only when hasSyncLibraryAccess is true; hasSyncLibraryAccess is resolved server-side in the layout; SyncLibraryIcon exists and is distinct; tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync Library hub page (In-progress anchored, ≥1-admitted gated)</name>
  <files>app/(artist)/sync-library/page.tsx, lib/sync-library/hub-access.ts</files>
  <read_first>
    - 26-UI-SPEC.md Screen E + "Per-Surface Copywriting" #4 — page heading/subheading, the three sections (Your agreement / Admitted songs / In progress) and decision #5: "In progress" is the PRIMARY anchor (leads the page); Admitted + Your agreement sit below.
    - app/(artist)/dashboard/page.tsx / app/(artist)/vault/page.tsx — the `mx-auto max-w-6xl px-6 py-10` + `border-b border-white/10 pb-6` header + section-header convention to mirror.
    - lib/esign/provider.ts (readEsignState) for the "Your agreement" signed date + View-PDF link; components/vault/DocumentCard.tsx file_url → "View PDF" link idiom.
  </read_first>
  <action>
    lib/sync-library/hub-access.ts: add `loadSyncLibraryHub(service, userId)` returning { inProgress: SyncListing[], admitted: SyncListing[], agreement: { version, signedAt, signedFileUrl } | null } — joining sync_listings to track/project titles.

    app/(artist)/sync-library/page.tsx (server): if `!hasAdmittedSyncListing(service, user.id)` → redirect('/dashboard') (progressive disclosure — the hub does not exist for zero-admitted artists). Otherwise render, in this vertical order per decision #5: (1) "In progress" section FIRST (any non-admitted listings — pending_admit / agreement_pending / applied / invited / rejected / withdrawn — reusing the Vault Status Chip set so status language is identical everywhere); (2) "Admitted songs" (title + source project link + admitted date + "Live" pill → Vault project); (3) "Your agreement" (signed date + "View agreement PDF" link). CTA (white/black secondary per UI-SPEC Accent) "Submit another song" → /vault. Mount <SyncLibraryCoachMark> (Task 3) on this page for the first-visit highlight.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    /sync-library redirects zero-admitted artists to /dashboard; for admitted artists it renders In-progress FIRST, then Admitted songs, then Your agreement, with a white/black "Submit another song" CTA; tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reusable newly-unlocked highlight — New dot + one-time coach-mark</name>
  <files>components/sync-library/SyncLibraryCoachMark.tsx, components/nav/ArtistNav.tsx</files>
  <read_first>
    - components/nav/NotificationBell.tsx lines ~100-110 — the unread-dot treatment (small solid `bg-brandfuchsia` dot) to match for the nav "New" dot (no count).
    - 26-UI-SPEC.md "New-feature highlight (fires once, on first admission)" #5 — the New dot behavior (clears on first visit) and the coach-mark copy ("Your Sync Library is live", body, single gradient "Got it").
    - 26-CONTEXT.md new-feature-highlight bullet — build a small reusable "newly-unlocked feature" primitive where practical.
  </read_first>
  <action>
    Build components/sync-library/SyncLibraryCoachMark.tsx as a generalized-where-practical one-time highlight primitive: a nav-anchored tooltip with the locked heading/body and a single gradient "Got it" button, shown once on first arrival at /sync-library, dismissed permanently thereafter. Persist the "seen" state with a lightweight per-user localStorage key (e.g. `funun-synclib-seen-<userId>`) — NO second migration (the notification itself is server-side and already fired by 26-05's admit route; this flag only governs the client-side dot + coach-mark). Keep the seen-key + dismiss logic factored so a future gated feature can reuse it with a different key/copy.

    ArtistNav.tsx: render a small `bg-brandfuchsia` "New" dot on the Sync Library item's icon corner when hasSyncLibraryAccess is true AND the localStorage seen-flag is unset; clear it (set the flag) on the first click/navigation to /sync-library. Match NotificationBell's dot scale, no number.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    A "New" dot shows on the Sync Library nav item until the artist first opens the hub, then clears; the coach-mark shows once on first hub visit with the locked copy and a single gradient "Got it"; the seen-flag persists via localStorage (no migration); tsc clean; `npm run build` green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server render → nav visibility + hub access | Whether the item/hub shows must be a trusted server read, not client-trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-31 | Elevation of Privilege | hub reachable by a zero-admitted artist via direct URL | medium | mitigate | The hub page itself re-checks `hasAdmittedSyncListing` server-side and redirects — nav-hiding is not the authority (matches the layout-vs-page double check convention) |
| T-26-32 | Information Disclosure | hub leaking another artist's listings | high | mitigate | `loadSyncLibraryHub` is scoped to `artist_user_id = user.id`; sync_listings RLS `select_own` backs it |
| T-26-33 | Tampering | client seen-flag spoofed to hide/show the dot | low | accept | The seen-flag is cosmetic (dot + coach-mark only); it grants no access and carries no security meaning |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` green.
- Nav order matches 26-CONTEXT exactly (Split Sheets under Contract Locker; Sync Library under Deals when admitted).
- Manual (deferred): admit a first song, confirm the nav item appears, the dot shows, and the coach-mark fires once.
</verification>

<success_criteria>
The Sync Library nav item and hub appear only post-admission, the hub leads with In-progress work, and the unlock is highlighted via a reusable New-dot + coach-mark primitive.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- **Page (MODIFIED):** `app/(artist)/layout.tsx` — `hasSyncLibraryAccess` server gate → ArtistNav
- **Component (MODIFIED):** `components/nav/ArtistNav.tsx` — reorder (Split Sheets under Contract Locker) + gated Sync Library item + New dot
- **Icon:** `components/nav/icons.tsx` — `SyncLibraryIcon`
- **Module:** `lib/sync-library/hub-access.ts` — `hasAdmittedSyncListing`, `loadSyncLibraryHub`
- **Page:** `app/(artist)/sync-library/page.tsx` (hub)
- **Component:** `components/sync-library/SyncLibraryCoachMark.tsx` (reusable newly-unlocked highlight)

> Requirements SYNCLIB-11/12/13/14 — provisional; register in REQUIREMENTS.md before phase close.
</artifacts>

<output>
Create `.planning/phases/26-sync-library-inclusion/26-09-SUMMARY.md` when done.
</output>
