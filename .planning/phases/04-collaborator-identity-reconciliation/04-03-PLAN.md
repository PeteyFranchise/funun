---
phase: 04-collaborator-identity-reconciliation
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/api/collaborators/[id]/route.ts
  - components/collaborators/CollaboratorCard.tsx
  - components/collaborators/CollaboratorPicker.tsx
  - app/(artist)/dashboard/page.tsx
autonomous: true
requirements: [COLLAB-05]
must_haves:
  truths:
    - "A claimed collaborator card shows a Funūn member badge and an Archive button instead of Delete; an unclaimed card still shows Delete"
    - "Hard-deleting a claimed collaborator via the API is blocked (returns 409); archive is the only removal path"
    - "A user can star a collaborator and the starred collaborator appears in a Favorites group at the top of the MetadataStudio picker"
    - "The dashboard shows a compact My Credits preview below the stats cards only when the user has claimed credits"
  artifacts:
    - components/collaborators/CollaboratorCard.tsx
  key_links:
    - "DELETE /api/collaborators/[id] checks claimed_by and returns 409 before deleting, blocking hard-delete of claimed rows"
    - "CollaboratorPicker groups roster into Favorites / Most Recent / All using is_favorite and created_at order"
    - "dashboard server query selects claimed credits and renders a preview card linking to /collaborators?tab=credits"
---

<objective>
Deliver the roster-management UX slice (COLLAB-05, ROADMAP success criteria 4 and the soft-delete/favorites design notes): claimed collaborator cards visibly upgrade to "Funūn member" with an Archive button replacing Delete; hard-delete of claimed rows is blocked at the API; users can star favorites that float to the top of the picker; and the dashboard surfaces a compact Credits preview.

Purpose: This makes the claim status and credit data visible and manageable from the surfaces the artist and the claimed user already use.
Output: Claimed-state CollaboratorCard (badge, archive, favorite star), the claim-aware DELETE guard, Favorites + Most Recent grouping in the picker, and the dashboard Credits preview.

This plan depends on plan 01 for the `claimed_by`/`archived_at`/`is_favorite` collaborator columns, the `CollaboratorProfile` type fields, the `isClaimedCollaborator` predicate, and the credits RLS policy. It does not modify `lib/collaborators/index.ts` (owned by plan 01) and shares no files with plan 02, so it runs in the same wave.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-collaborator-identity-reconciliation/04-CONTEXT.md
@.planning/phases/04-collaborator-identity-reconciliation/04-RESEARCH.md
@.planning/phases/04-collaborator-identity-reconciliation/04-PATTERNS.md
@.planning/phases/04-collaborator-identity-reconciliation/04-UI-SPEC.md
@.planning/phases/04-collaborator-identity-reconciliation/04-01-SUMMARY.md
</context>

## Phase Goal

**As an** artist who credited a collaborator before they joined Funūn, **I want to** see their card upgrade to a Funūn member with archive-instead-of-delete and quick-access favorites, **so that** I never lose a real person's credit record and can re-pick frequent collaborators fast.

<tasks>

<task type="auto">
  <name>Task 1: Claim-aware DELETE guard + claimed-state CollaboratorCard (badge, archive, favorite star)</name>
  <files>app/api/collaborators/[id]/route.ts, components/collaborators/CollaboratorCard.tsx</files>
  <read_first>
    - app/api/collaborators/[id]/route.ts (current PATCH and DELETE handlers; existing ownership .eq('user_id') chain)
    - components/collaborators/CollaboratorCard.tsx (current card: Props, IPI badge block, Edit button)
    - lib/collaborators/index.ts (CollaboratorProfile fields claimed_by/archived_at/is_favorite and isClaimedCollaborator predicate — shipped in plan 01)
    - .planning/phases/04-collaborator-identity-reconciliation/04-01-SUMMARY.md (confirm the type fields and predicate are available)
    - .planning/phases/04-collaborator-identity-reconciliation/04-PATTERNS.md (Ownership + Claim Guard DELETE pattern; CollaboratorCard extension — claimed state, archive, favorite star, badge styling)
    - .planning/phases/04-collaborator-identity-reconciliation/04-UI-SPEC.md (CollaboratorCard state table; favorite star + archive interaction contracts; accessibility aria-labels; copywriting contract)
    - .planning/phases/04-collaborator-identity-reconciliation/04-RESEARCH.md (Pitfall 4 hard delete of claimed rows; Security Domain T-delete-claim)
  </read_first>
  <action>
    (A) Extend the DELETE handler in app/api/collaborators/[id]/route.ts: before the existing .delete() chain, SELECT claimed_by for the row scoped to id + user_id (.eq('id', id).eq('user_id', user.id).maybeSingle()). If the row's claimed_by is set, return NextResponse.json with an archive-suggestion error message and status 409 (mitigates T-delete-claim). Only proceed to .delete() when claimed_by is null. Leave the PATCH handler unchanged (it already routes through sanitizeCollaborator, which plan 01 extended to accept is_favorite and archived_at, so favorite-toggle and archive PATCHes already work through it).
    (B) Extend components/collaborators/CollaboratorCard.tsx: add optional props onArchive, onDelete, onFavoriteToggle and an isClaimed derivation via isClaimedCollaborator(collaborator). Render a Funūn-member badge (indigo chip: inline-flex rounded-full border border-brandindigo/30 bg-brandindigo/10 px-2 py-0.5 text-[10px] font-bold text-brandindigo, copy "Funūn member") when isClaimed. Render a favorite star button at top-right (absolute right-4 top-3) with aria-label "Add to favorites" / "Remove from favorites" — filled text-brandindigo when is_favorite, outline text-white/20 hover:text-white/50 otherwise. Replace the bottom-right control: when isClaimed render an Archive button (text-xs text-white/50 hover:text-amber-300, copy "Archive", aria-label "Archive collaborator") calling onArchive; otherwise render the existing Delete affordance calling onDelete. Keep the Edit button. Apply the archived read-only display style (opacity-50, no controls) when archived_at is set, per the UI-SPEC state table. Use exact copy and aria-labels from 04-UI-SPEC.md.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "collaborators/\[id\]|CollaboratorCard" || echo OK-typecheck; grep -q "409" "app/api/collaborators/[id]/route.ts" && grep -q "claimed_by" "app/api/collaborators/[id]/route.ts" && echo OK-guard; grep -q "Funūn member" components/collaborators/CollaboratorCard.tsx && grep -q "onArchive" components/collaborators/CollaboratorCard.tsx && grep -q "aria-label" components/collaborators/CollaboratorCard.tsx && echo OK-card</automated>
  </verify>
  <acceptance_criteria>
    - DELETE `/api/collaborators/[id]` selects `claimed_by` first and returns 409 with an archive-suggestion message when it is set; it deletes only when `claimed_by` is null
    - The existing `user_id` ownership scoping is preserved on both the select and delete
    - `CollaboratorCard` renders a "Funūn member" indigo badge when `isClaimedCollaborator(collaborator)` is true
    - The card shows an Archive button (not Delete) for claimed rows and a Delete affordance for unclaimed rows
    - A favorite star toggle with `aria-label` ("Add to favorites" / "Remove from favorites") reflects `is_favorite`
    - Archived rows render in the reduced-opacity read-only style with controls suppressed
    - Badge copy and aria-labels match 04-UI-SPEC.md verbatim
    - `npx tsc --noEmit` reports no new errors in the two modified files
  </acceptance_criteria>
  <done>Claimed collaborators are visually upgraded and protected from hard-delete; favorites can be starred; archived rows are shown read-only.</done>
</task>

<task type="auto">
  <name>Task 2: Favorites + Most Recent grouping in CollaboratorPicker</name>
  <files>components/collaborators/CollaboratorPicker.tsx</files>
  <read_first>
    - components/collaborators/CollaboratorPicker.tsx (current: roster fetch on mount, search filter, list render, "Add new collaborator" action)
    - lib/collaborators/index.ts (CollaboratorProfile is_favorite/archived_at fields; assembleDisplayName)
    - app/api/collaborators/route.ts (GET ordering — currently by name; the picker relies on created_at order for Most Recent)
    - .planning/phases/04-collaborator-identity-reconciliation/04-PATTERNS.md (CollaboratorPicker extension — Favorites + Most Recent grouping; group header pattern; picker API fetch ordering note)
    - .planning/phases/04-collaborator-identity-reconciliation/04-UI-SPEC.md (CollaboratorPicker grouped sections table; picker group copy; states required; favorite toggle interaction)
  </read_first>
  <action>
    Extend components/collaborators/CollaboratorPicker.tsx to group the dropdown list when no search is active (per D-12). Filter out archived rows first (c.archived_at falsy). Derive three groups: Favorites (is_favorite true), Most Recent (top 5 non-favorites by created_at descending — sort the roster by created_at desc client-side before slicing, since the GET currently orders by name), and All (remaining non-favorites). Render each group only when it has entries, each preceded by a non-interactive divider label using the group copy from the UI-SPEC ("FAVORITES", "RECENTLY ADDED", "ALL COLLABORATORS") styled px-4 py-1.5 text-[10px] font-bold uppercase tracking-wide text-lavdim. When a search query is present, collapse to a single flat results list with no group headers (existing behavior). Preserve the existing empty-roster trigger, outside-click close, and "Add new collaborator" bottom action. Keep handleSelect/handleNewSaved unchanged. Use the exact group label copy from 04-UI-SPEC.md.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep "CollaboratorPicker" || echo OK-typecheck; grep -q "FAVORITES" components/collaborators/CollaboratorPicker.tsx && grep -q "RECENTLY ADDED" components/collaborators/CollaboratorPicker.tsx && grep -q "is_favorite" components/collaborators/CollaboratorPicker.tsx && echo OK-groups</automated>
  </verify>
  <acceptance_criteria>
    - With no search active, the picker renders up to three groups (Favorites, Most Recent, All) with the UI-SPEC group labels verbatim
    - Each group renders only when it has at least one entry
    - Most Recent shows the top 5 non-favorite collaborators ordered by `created_at` descending
    - Archived collaborators (`archived_at` set) never appear in the picker
    - When a search query is entered, the list collapses to a single flat result set with no group headers
    - The empty-roster trigger and "Add new collaborator" action are preserved
    - `npx tsc --noEmit` reports no new errors in this file
  </acceptance_criteria>
  <done>The MetadataStudio and split-sheet pickers surface starred and recently-added collaborators at the top for fast repeat selection; archived rows are excluded.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard My Credits preview</name>
  <files>app/(artist)/dashboard/page.tsx</files>
  <read_first>
    - app/(artist)/dashboard/page.tsx (current server component: DEMO branch, vault_projects query, StatCard helper, stats grid, Recent projects section)
    - supabase/migrations/018_collaborators_split_sheets.sql (split_sheet_parties.role, split_sheets.song_name for the preview join)
    - .planning/phases/04-collaborator-identity-reconciliation/04-PATTERNS.md (dashboard extension — Credits preview query, StatCard reuse, conditional render)
    - .planning/phases/04-collaborator-identity-reconciliation/04-UI-SPEC.md (Credits preview in dashboard — placement, heading, max 3 rows, View all link copy and target)
  </read_first>
  <action>
    Extend app/(artist)/dashboard/page.tsx: in the non-DEMO branch, after the existing vault_projects query, add a credits-preview query — supabase.from('collaborators').select('id, name, split_sheet_parties(role, split_sheets(song_name))').eq('claimed_by', user?.id ?? '').is('archived_at', null).order('created_at', { ascending: false }).limit(3). This cross-user read is authorized by the "Claimed users see own credits" RLS policy from plan 01. Compute a creditsCount. Render a "My Credits" preview section positioned below the stats cards grid and above Recent projects, only when creditsCount > 0 (per D-05): a section heading (text-sm font-semibold text-white/60 uppercase tracking-wide, copy "My Credits"), up to 3 preview rows (project/song name, crediting context, role), and a right-aligned "View all credits →" link (text-sm text-white/50 hover:text-white) to /collaborators?tab=credits. In the DEMO branch render no credits preview. Reuse the existing StatCard styling idiom for visual consistency. Use the exact heading and link copy from 04-UI-SPEC.md.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep "dashboard/page" || echo OK-typecheck; grep -q "claimed_by" "app/(artist)/dashboard/page.tsx" && grep -q "tab=credits" "app/(artist)/dashboard/page.tsx" && echo OK-preview</automated>
  </verify>
  <acceptance_criteria>
    - The dashboard runs a credits query filtering `claimed_by = user.id` and `archived_at IS NULL`, limited to 3, joining `split_sheet_parties` → `split_sheets`
    - A "My Credits" preview section renders only when the user has at least one claimed credit
    - The preview shows at most 3 rows and a "View all credits →" link to `/collaborators?tab=credits`
    - The DEMO branch renders no credits preview
    - Heading and link copy match 04-UI-SPEC.md verbatim
    - `npx tsc --noEmit` reports no new errors in this file
  </acceptance_criteria>
  <done>A claimed user landing on the dashboard sees a compact preview of their credits with a deep-link into the full Credits tab; users with no credits see no preview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DELETE /api/collaborators/[id] | Untrusted delete; must block hard-delete of claimed rows |
| client → dashboard / picker credits read | Cross-user read scoped by RLS to the claiming user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 (T-delete-claim) | Tampering | DELETE /api/collaborators/[id] | high | mitigate | Handler selects `claimed_by` and returns 409 when set; only unclaimed rows can be hard-deleted; existing `user_id` ownership scoping retained |
| T-04-10 (T-mass-assign) | Tampering | favorite/archive PATCH | medium | mitigate | Toggles route through the existing PATCH + `sanitizeCollaborator` allowlist (extended in plan 01); `claimed_by` is not in the allowlist so it can't be set client-side |
| T-04-11 (T-rls-leak) | Information Disclosure | dashboard/picker credits read | medium | mitigate | Reads scoped by `claimed_by = user.id`; the "Claimed users see own credits" RLS policy (plan 01) enforces row-level access |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- Manual smoke (VALIDATION row 04-API-02): claim a collaborator row; confirm DELETE /api/collaborators/:id returns 409; confirm the card shows Archive (not Delete); archive it and confirm it leaves the active view.
- Manual smoke (VALIDATION favorites row): star a collaborator, hard refresh, confirm the star persists, open the MetadataStudio picker and confirm the starred collaborator appears in the Favorites group at top.
- Manual smoke (VALIDATION 04-UI-02): as a claimed user, confirm the dashboard shows the My Credits preview and "View all credits →" links to /collaborators?tab=credits; as a user with no credits, confirm no preview renders.
- `npx tsc --noEmit` clean for all changed files.
</verification>

<success_criteria>
- ROADMAP success criterion 4 (collaborator card visual upgrade to Funūn member) is satisfied; the soft-delete/archive and favorites/most-recent design notes are implemented.
</success_criteria>

<output>
Create `.planning/phases/04-collaborator-identity-reconciliation/04-03-SUMMARY.md` when done.
</output>
