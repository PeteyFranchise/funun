---
phase: 04-collaborator-identity-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/api/user-profiles/route.ts
  - app/(artist)/settings/page.tsx
  - components/profile/ProfileForm.tsx
autonomous: true
requirements: [COLLAB-05]
must_haves:
  truths:
    - "D-07: user_profiles table exists and the Settings page reads and writes it (not artist_profiles) for PRO, IPI, publisher, phone, address, display_name, bio"
    - "D-08: Back-fill runs on every settings save — PATCH /api/user-profiles triggers backfill_claimed_collaborators(user.id) so claimed rows fill their gaps additively"
    - "D-09: Back-fill is additive-only — COALESCE(existing_col, new_val) ordering ensures no non-NULL collaborator values are overwritten"
    - "The Settings PATCH only accepts allowlisted fields — id, claimed_by, and timestamps in the body are silently dropped"
  artifacts:
    - app/api/user-profiles/route.ts
  key_links:
    - "PATCH /api/user-profiles writes user_profiles then fires backfill_claimed_collaborators(user.id) so claimed rows fill their gaps"
    - "settings page reads user_profiles and ProfileForm routes Rights Identity fields to /api/user-profiles"
---

<objective>
Deliver the Settings back-fill slice end-to-end (COLLAB-05, ROADMAP success criterion 5): a user fills in their rights identity (PRO, IPI, publisher, phone, address) on the Settings page, it persists to the new `user_profiles` table, and the save automatically fills any gaps on every collaborator row they have claimed — so split sheets reflect complete data with no work from the inviting artist.

Purpose: This closes the "no data re-entry" loop. The claim mechanism (plan 01) links rows; this slice makes the claimed user's own data flow into them.
Output: The `user_profiles` GET+PATCH API, a "Rights Identity" section on the Settings page, and ProfileForm wiring those fields to the new endpoint with a fire-and-forget back-fill.

This plan depends on plan 01 for the `user_profiles` table, the `USER_PROFILES_EDITABLE_FIELDS` field shape, and the `backfill_claimed_collaborators` SQL function.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-collaborator-identity-reconciliation/04-CONTEXT.md
@.planning/phases/04-collaborator-identity-reconciliation/04-RESEARCH.md
@.planning/phases/04-collaborator-identity-reconciliation/04-PATTERNS.md
@.planning/phases/04-collaborator-identity-reconciliation/04-UI-SPEC.md
@.planning/phases/04-collaborator-identity-reconciliation/04-01-SUMMARY.md
</context>

## Phase Goal

**As a** newly-joined Funūn collaborator, **I want to** enter my rights identity once in Settings, **so that** every split sheet I'm credited on fills in my PRO, IPI, publisher, phone, and address automatically — without the artist who credited me having to do anything.

<tasks>

<task type="auto">
  <name>Task 1: user_profiles GET + PATCH API with allowlist and fire-and-forget back-fill</name>
  <files>app/api/user-profiles/route.ts</files>
  <read_first>
    - app/api/profile/route.ts (EDITABLE_FIELDS allowlist, sanitize() helper, PATCH handler structure, GET pattern)
    - app/api/collaborators/route.ts (createApiClient + getUser + 401 auth guard; GET pattern with maybeSingle equivalent)
    - lib/supabase/server.ts (createApiClient, createServiceClient)
    - .planning/phases/04-collaborator-identity-reconciliation/04-01-SUMMARY.md (confirm user_profiles columns and backfill_claimed_collaborators signature shipped in plan 01)
    - .planning/phases/04-collaborator-identity-reconciliation/04-PATTERNS.md (user-profiles route section — adaptation notes; EDITABLE_FIELDS sanitizer shared pattern)
    - .planning/phases/04-collaborator-identity-reconciliation/04-RESEARCH.md (Pattern 3 settings back-fill; Security Domain T-mass-assign)
  </read_first>
  <action>
    Create app/api/user-profiles/route.ts following the app/api/profile/route.ts pattern exactly.
    Define USER_PROFILES_EDITABLE_FIELDS = ['pro', 'ipi', 'publisher', 'phone', 'mailing_address', 'display_name', 'bio'] as const (per D-07; this is the mass-assignment allowlist — id, claimed_by, created_at, updated_at must NOT appear in it, mitigating T-mass-assign). Write a local sanitize() that copies only allowlisted keys, trims strings to null-on-empty, accepts mailing_address as an object-or-null (JSONB), and accepts null. Do not echo any forbidden field name into a head comment that a negative grep checks.
    GET handler: createApiClient(); getUser(); 401 if no user; SELECT * FROM user_profiles WHERE id = user.id .maybeSingle(); return { data } (data may be null if the user has never saved — that is valid).
    PATCH handler: createApiClient(); getUser(); 401 if no user; parse body; const update = sanitize(body); 400 if update is empty. Upsert into user_profiles keyed by id (the row may not exist yet on first save) — use supabase.from('user_profiles').upsert({ id: user.id, ...update }, { onConflict: 'id' }).select().single(). On error return 500. After a successful save, fire the back-fill as fire-and-forget so a back-fill failure never blocks the settings save: createServiceClient().rpc('backfill_claimed_collaborators', { p_user_id: user.id }) wrapped so its rejection is swallowed (do not await-throw it into the response path). Return { data }.
    Import createApiClient and createServiceClient from '@/lib/supabase/server'.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep "user-profiles" || echo OK-typecheck; grep -q "USER_PROFILES_EDITABLE_FIELDS" app/api/user-profiles/route.ts && grep -q "backfill_claimed_collaborators" app/api/user-profiles/route.ts && grep -q "auth.getUser" app/api/user-profiles/route.ts && echo OK-route; grep -E "USER_PROFILES_EDITABLE_FIELDS\s*=\s*\[" app/api/user-profiles/route.ts | grep -qv "claimed_by" && echo OK-allowlist</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/user-profiles/route.ts` exports async GET and PATCH handlers, both calling `createApiClient().auth.getUser()` with a 401 guard
    - `USER_PROFILES_EDITABLE_FIELDS` contains exactly the seven fields (pro, ipi, publisher, phone, mailing_address, display_name, bio) and excludes id, claimed_by, and timestamps
    - PATCH rejects an all-non-allowlisted body with 400 (no DB write)
    - PATCH upserts into `user_profiles` keyed by `id = user.id`
    - After a successful save, PATCH calls `backfill_claimed_collaborators` via `createServiceClient().rpc(...)` as fire-and-forget (a rejection does not change the HTTP response)
    - GET returns the user's row or `null` data when none exists
    - `npx tsc --noEmit` reports no new errors in this file
  </acceptance_criteria>
  <done>The user_profiles endpoint reads and writes the new table with a mass-assignment allowlist and triggers the additive back-fill of claimed collaborator rows after every save.</done>
</task>

<task type="auto">
  <name>Task 2: Rights Identity section on Settings page + ProfileForm wiring to /api/user-profiles</name>
  <files>app/(artist)/settings/page.tsx, components/profile/ProfileForm.tsx</files>
  <read_first>
    - app/(artist)/settings/page.tsx (current server component: DEMO branch, artist_profiles fetch, ProfileForm render)
    - components/profile/ProfileForm.tsx (FormState shape incl. pro/ipi/publisher fields, toForm(), set(), the PATCH to /api/profile, pro/ipi/publisher inputs around lines 505-535)
    - app/api/user-profiles/route.ts (the GET+PATCH contract created in Task 1)
    - .planning/phases/04-collaborator-identity-reconciliation/04-UI-SPEC.md (New: user_profiles settings section — Rights Identity heading, sub-label, fields, save button; copywriting contract)
    - .planning/phases/04-collaborator-identity-reconciliation/04-PATTERNS.md (settings/page.tsx extension — fetch user_profiles, pass userProfile to ProfileForm)
  </read_first>
  <action>
    (A) Extend app/(artist)/settings/page.tsx: keep the existing artist_profiles fetch. In the non-DEMO branch, after loading the user, also fetch the user_profiles row: supabase.from('user_profiles').select('*').eq('id', user.id).maybeSingle(). Pass it to ProfileForm as a new optional prop userProfile (default null in DEMO). The page heading/structure stays the same.
    (B) Extend components/profile/ProfileForm.tsx: add a "Rights Identity" section per the UI-SPEC — a section divider (border-t border-white/10 mt-8 pt-8), heading "Rights Identity" (text-lg font-semibold text-white), sub-label "Saved here, auto-filled into every split sheet and contract." (text-sm text-lavdim mt-1). Move the rights-identity fields (PRO dropdown, IPI/CAE input, Publisher input) into this section and add Phone and the mailing address fields, seeded from the userProfile prop (falling back to the existing artist_profile values when userProfile is null, so existing users see their current data). These rights fields must save to /api/user-profiles via PATCH (not /api/profile) — wire a save that PATCHes the allowlisted fields {pro, ipi, publisher, phone, mailing_address} to /api/user-profiles. The non-rights fields (artist_name, genre, links, etc.) continue to PATCH /api/profile unchanged. Use the existing CTA button style (rounded-lg bg-grad px-4 py-2 text-sm font-semibold text-white shadow-cta). Preserve the existing error/saved feedback pattern for the new save.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "settings/page|ProfileForm" || echo OK-typecheck; grep -q "user_profiles" "app/(artist)/settings/page.tsx" && echo OK-fetch; grep -q "/api/user-profiles" components/profile/ProfileForm.tsx && grep -q "Rights Identity" components/profile/ProfileForm.tsx && echo OK-form</automated>
  </verify>
  <acceptance_criteria>
    - `settings/page.tsx` fetches the `user_profiles` row for the logged-in user and passes it to `ProfileForm` as `userProfile`
    - `ProfileForm` renders a "Rights Identity" section with the divider, heading, and sub-label copy verbatim from 04-UI-SPEC.md
    - The PRO, IPI/CAE, Publisher, Phone, and address fields in that section seed from `userProfile`, falling back to the artist profile values when `userProfile` is null
    - Saving the Rights Identity section issues a PATCH to `/api/user-profiles` with only `{pro, ipi, publisher, phone, mailing_address}`
    - Non-rights profile fields still PATCH `/api/profile`
    - `npx tsc --noEmit` reports no new errors in the two modified files
  </acceptance_criteria>
  <done>A user can edit their rights identity in a dedicated Settings section that writes to user_profiles; on save, the back-fill propagates the data into their claimed collaborator rows, leaving artist-entered values untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PATCH /api/user-profiles | Untrusted body; identity from session, fields gated by allowlist |
| /api/user-profiles → backfill_claimed_collaborators (service role) | Server-side RPC that writes the user's own claimed rows additively |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 (T-mass-assign) | Tampering | PATCH /api/user-profiles | high | mitigate | `USER_PROFILES_EDITABLE_FIELDS` allowlist drops id, claimed_by, and timestamps; only the seven identity fields are written |
| T-04-07 (T-backfill) | Tampering | backfill_claimed_collaborators RPC | medium | mitigate | Back-fill UPDATE uses `COALESCE(existing, new)` (shipped in plan 01 migration); the settings save never overwrites artist-entered non-NULL values |
| T-04-08 | Information Disclosure | GET /api/user-profiles | medium | mitigate | Query scoped to `id = user.id`; RLS policy "Users manage own profile" enforces row ownership at the DB layer |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- Manual smoke (VALIDATION row 04-API-01/04-API-03): claim a collaborator row for the test user; in Settings set pro='ASCAP', ipi='12345'; save; query the claimed collaborator row and confirm pro/ipi are now set while any previously non-NULL field is unchanged.
- Manual: PATCH /api/user-profiles with a body containing `claimed_by` and `id` — confirm those are ignored and only allowlisted fields persist.
- `npx tsc --noEmit` clean for all changed files.
</verification>

<success_criteria>
- ROADMAP success criterion 5 (Settings back-fill of PRO/IPI/publisher/phone/address into claimed rows, additive-only) is satisfied.
</success_criteria>

<output>
Create `.planning/phases/04-collaborator-identity-reconciliation/04-02-SUMMARY.md` when done.
</output>
