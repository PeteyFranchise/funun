---
phase: 08-identity-schema-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - supabase/migrations/038_block_enforcement_existing_tables.sql
  - supabase/migrations/039_handle_new_user_industry_branch.sql
autonomous: true
requirements: []
must_haves:
  truths:
    - "SC-2: the INSERT policies on follows, wall_posts, endorsements, dm_threads, and dm_messages each carry an additional AND no_block(auth.uid(), <other_party_id>) clause so a blocked party cannot follow/post-on/endorse/DM the blocker via RLS (D-15)"
    - "SC-5: handle_new_user() has an industry branch that, when NEW.raw_app_meta_data->>'role' = 'industry', INSERTs an artist_profiles row with member_type='industry', artist_name and industry_roles and roles populated from raw_user_meta_data, plus a subscriptions row (tier='free', status='active') (D-18), and does NOT call claim_collaborators()"
    - "SC-5: the industry branch INSERTs a real artist_profiles row (never a bare RETURN NEW) so every industry member has a row to join against (RESEARCH Pitfall 3)"
    - "the industry branch populates both industry_roles (TEXT[]) and roles (JSONB ProfileRole[]) so an invited member renders a role badge on day one (D-08 / RESEARCH Pitfall 4)"
    - "block-enforcement policy edits are additive AND-clauses onto the existing WITH CHECK, not full table/policy rewrites — zero behavior change today because blocks is empty until Phase 13 (D-15)"
  prohibitions:
    - statement: "the industry branch must not create a pending_industry_invites side-channel table — the resolved D-03 uses admin.createUser() with app_metadata.role set atomically, so the trigger keys off app_metadata, not a side-channel table"
      status: resolved
      verification: "grep migration 039 for `pending_industry_invites` returns no match; the branch condition reads `NEW.raw_app_meta_data->>'role'`"
    - statement: "the industry branch must not call claim_collaborators() (an artist-only flow)"
      status: resolved
      verification: "the industry branch in migration 039 contains no `claim_collaborators` call between its branch entry and its RETURN NEW"
  artifacts:
    - supabase/migrations/038_block_enforcement_existing_tables.sql
    - supabase/migrations/039_handle_new_user_industry_branch.sql
  key_links:
    - "no_block() must exist (plan 08-02, migration 035) before migration 038 references it in policy bodies — enforced by depends_on 08-02 and migration ordering 035 < 038"
    - "member_type column must exist (plan 08-01, migration 034) before the industry branch INSERTs it — enforced by depends_on 08-01 and ordering 034 < 039"
    - "the industry branch reads display_name and role_badges from NEW.raw_user_meta_data (set by createIndustryMember via admin.createUser user_metadata), and role from NEW.raw_app_meta_data (set via app_metadata) — the two metadata channels are distinct"
---

<objective>
Wire block enforcement into every existing socially-exposed table (D-15) and add the industry-member branch to `handle_new_user()` (D-01, resolved via D-03). Both are migrations that depend on Wave 1: block enforcement references `no_block()` (plan 08-02); the industry branch INSERTs `member_type` (plan 08-01).

Purpose: (1) Adding `no_block()` to `follows`/`wall_posts`/`endorsements`/`dm_threads`/`dm_messages` INSERT policies now means Phases 10/11/13 inherit RLS block-enforcement for free instead of each shipping a retrofit migration — and it is zero-behavior-change today since `blocks` is empty until Phase 13 (D-15). (2) The industry branch is the identity-race fix (SC-5): because the resolved D-03 uses `admin.createUser()` with `app_metadata.role='industry'` set atomically at creation, the trigger keys off `app_metadata` (NO side-channel table — this supersedes RESEARCH's `pending_industry_invites` recommendation) and builds a correct, differently-shaped `artist_profiles` row (Pitfall 3) with both role columns populated (Pitfall 4) plus a free subscriptions row (D-18).

Output: Migrations `038_block_enforcement_existing_tables.sql`, `039_handle_new_user_industry_branch.sql`.

CRITICAL DIVERGENCE FROM RESEARCH: 08-RESEARCH.md Pitfall 2 recommends a `pending_industry_invites` side-channel table. CONTEXT.md D-03 (RESOLVED 2026-07-04) supersedes this: the user chose `admin.createUser()` + a custom Resend email instead. Do NOT create `pending_industry_invites`. The trigger branches on `app_metadata.role='industry'`, which `createUser()` sets atomically.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-identity-schema-foundation/08-CONTEXT.md
@.planning/phases/08-identity-schema-foundation/08-RESEARCH.md
@.planning/phases/08-identity-schema-foundation/08-PATTERNS.md
@.planning/phases/08-identity-schema-foundation/08-VALIDATION.md
@.planning/phases/08-identity-schema-foundation/08-02-PLAN.md
</context>

<artifacts_this_phase_produces>
Symbols this plan (08-04) creates — exclude from drift/source-grounding verification:
- Migration file `supabase/migrations/038_block_enforcement_existing_tables.sql` (edits existing INSERT policies on follows/wall_posts/endorsements/dm_threads/dm_messages)
- Migration file `supabase/migrations/039_handle_new_user_industry_branch.sql` (replaces the `handle_new_user()` function body with an added industry branch)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 038 — wire no_block() into existing socially-exposed tables' INSERT policies</name>
  <files>supabase/migrations/038_block_enforcement_existing_tables.sql</files>
  <read_first>
    - supabase/migrations/012_social_layer.sql (the exact existing INSERT policies on follows, wall_posts, endorsements, dm_threads, dm_messages — read each WITH CHECK clause so the new AND-clause appends correctly and references the right "other party" column for each table)
    - supabase/migrations/035_connections_blocks.sql (the no_block() signature from plan 08-02 — confirm argument order is no_block(a, b) and that EXECUTE is granted to authenticated)
    - .planning/phases/08-identity-schema-foundation/08-PATTERNS.md (migration 039-analog section: DROP POLICY IF EXISTS + CREATE POLICY additive AND-clause pattern, "not a full policy rewrite")
    - .planning/phases/08-identity-schema-foundation/08-CONTEXT.md (D-15 scope)
  </read_first>
  <action>
    Create supabase/migrations/038_block_enforcement_existing_tables.sql with the standard header banner ("Migration 038: wire no_block() into follows/wall_posts/endorsements/dm_threads/dm_messages INSERT policies (D-15)").

    For EACH of the five tables' existing INSERT policy, DROP POLICY IF EXISTS the existing policy by its exact name (read migration 012 for the names) and re-CREATE it with the same clause PLUS an appended AND no_block(auth.uid(), <other_party_id_column>). Map the "other party" column per table by reading migration 012: follows → followee_id; wall_posts → the target profile/wall owner id column (confirm the column name in 012); endorsements → the endorsed user's id column; dm_threads → the recipient/other-participant id column; dm_messages → the thread's other participant (if dm_messages has no direct recipient column, gate at the dm_threads INSERT layer and add a comment explaining why dm_messages inherits enforcement from its thread). Preserve each policy's existing WITH CHECK identity clause (e.g. follower_id = auth.uid()) exactly; only append the AND no_block(...) — do not rewrite or drop other conditions. Add a comment at the top explaining this is zero-behavior-change until Phase 13 populates blocks, and lists exactly which policies were amended.
  </action>
  <verify>
    <automated>npx supabase db push --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/038_block_enforcement_existing_tables.sql exists
    - File contains `no_block(auth.uid()` appearing at least 4 times (follows, wall_posts, endorsements, dm_threads — dm_messages may inherit via its thread with a documented comment)
    - File contains `DROP POLICY IF EXISTS` for each amended policy (idempotent)
    - Each re-created INSERT policy preserves its original `= auth.uid()` ownership clause (grep confirms the original identity condition is still present alongside the new no_block AND-clause)
    - `npx supabase db push --dry-run` exits 0
  </acceptance_criteria>
  <done>All five socially-exposed tables' INSERT policies carry the no_block() check as an additive AND-clause; dry-run validates.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 039 — handle_new_user() industry branch (member_type row, both role columns, free subscription, no claim_collaborators)</name>
  <files>supabase/migrations/039_handle_new_user_industry_branch.sql</files>
  <read_first>
    - supabase/migrations/030_curators_pitch_history.sql (the CURRENT handle_new_user() body lines 105-127 — copy it VERBATIM and insert the industry branch; note the curator branch is a bare RETURN NEW, and the nested BEGIN...EXCEPTION WHEN OTHERS THEN NULL...END exception-isolation pattern lines 119-123)
    - supabase/migrations/027_fix_handle_new_user_exception_isolation.sql (the exception-isolation convention this function already uses)
    - .planning/phases/08-identity-schema-foundation/08-CONTEXT.md (D-01/D-03 resolved — createUser() + app_metadata.role='industry'; D-04 invite fields; D-08 both-columns rule; D-18 subscriptions row for industry; D-07 member_type framing)
    - .planning/phases/08-identity-schema-foundation/08-RESEARCH.md (Pitfall 3 — must INSERT a row not RETURN NEW; Pitfall 4 — must populate roles JSONB not just industry_roles; the handle_new_user industry-branch skeleton lines ~487-523, but IGNORE its pending_industry_invites keying — use app_metadata per resolved D-03)
    - lib/industry-roles.ts (INDUSTRY_ROLE_GROUPS slugs — the invite role picks are these slugs)
    - types/index.ts (PROFILE_ROLES presets lines 239-256 and ProfileRole shape lines 258-261 — the roles JSONB entries must be {kind:'preset',slug} for a matching preset else {kind:'custom',label})
  </read_first>
  <action>
    Create supabase/migrations/039_handle_new_user_industry_branch.sql with the standard header banner ("Migration 039: handle_new_user() industry branch (D-01/D-03 resolved — app_metadata-keyed, no side-channel table)").

    CREATE OR REPLACE FUNCTION public.handle_new_user() copying migration 030's body VERBATIM (keep the existing curator branch and the default artist branch with claim_collaborators() exactly as-is — this is a full-body replace since 030 already applied). Insert a NEW industry branch AFTER the curator branch and BEFORE the default artist INSERT:

    IF (NEW.raw_app_meta_data->>'role') = 'industry' THEN — set atomically by createUser() (plan 08-06); NOT a side-channel table (resolved D-03). Inside this branch:
    (1) INSERT INTO public.artist_profiles (id, member_type, artist_name, industry_roles, roles) with member_type='industry', artist_name = NEW.raw_user_meta_data->>'display_name', industry_roles = the text[] read from NEW.raw_user_meta_data->'role_badges' (parse the JSON array of slugs into a TEXT[]), and roles = the JSONB ProfileRole[] mapped from those same slugs. Because mapping slug→preset is non-trivial in SQL, the RECOMMENDED approach is: createIndustryMember() (plan 08-06) pre-computes BOTH the industry_roles TEXT[] AND the roles JSONB ProfileRole[] in TypeScript and passes them through user_metadata (e.g. raw_user_meta_data->'role_badges' as the slug array AND raw_user_meta_data->'profile_roles' as the pre-built ProfileRole[] JSONB). The trigger then just copies raw_user_meta_data->'profile_roles' straight into roles and the slug array into industry_roles — no slug→preset mapping logic in SQL. Write the branch to read both keys from raw_user_meta_data; document this contract in a comment so plan 08-06 populates the matching keys. This satisfies D-08 / Pitfall 4 (a badge renders day one) without embedding the mapping table in PL/pgSQL.
    (2) INSERT INTO public.subscriptions (user_id, tier, status) VALUES (NEW.id, 'free', 'active') — D-18 (industry members DO get a free subscriptions row).
    (3) Do NOT call claim_collaborators() (artist-only). RETURN NEW.
    Wrap any non-critical secondary insert in the same nested BEGIN...EXCEPTION WHEN OTHERS THEN NULL...END isolation as the artist branch's claim_collaborators call, so a secondary failure cannot orphan the profile row. Keep LANGUAGE plpgsql SECURITY DEFINER. No CREATE TRIGGER (on_auth_user_created already invokes this function and picks up the replaced body — mirror migration 030's comment).
  </action>
  <verify>
    <automated>npx supabase db push --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/039_handle_new_user_industry_branch.sql exists
    - File contains `CREATE OR REPLACE FUNCTION public.handle_new_user()`
    - File contains the condition `(NEW.raw_app_meta_data->>'role') = 'industry'`
    - File contains `INSERT INTO public.artist_profiles` inside the industry branch with `member_type` set to `'industry'` (NOT a bare RETURN NEW for the industry case — RESEARCH Pitfall 3)
    - The industry branch INSERT lists both `industry_roles` and `roles` columns (D-08 / Pitfall 4)
    - File contains `INSERT INTO public.subscriptions` for the industry branch (D-18)
    - File contains no `pending_industry_invites` reference (resolved D-03 — grep returns nothing)
    - The industry branch contains no `claim_collaborators` call
    - The existing curator branch (`= 'curator'`) and the default artist branch (with `claim_collaborators`) are both still present (full-body replace preserved them)
    - `npx supabase db push --dry-run` exits 0
  </acceptance_criteria>
  <done>handle_new_user() gains an app_metadata-keyed industry branch that builds a correct member_type='industry' row with both role columns and a free subscription, without claim_collaborators or any side-channel table; curator + artist branches preserved; dry-run validates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blocked party → follows/wall_posts/endorsements/dm INSERT | UI-only block enforcement is bypassable via direct PostgREST; enforcement must live in RLS |
| auth.users INSERT (createUser) → handle_new_user() trigger | app_metadata is service-role-only writable (not client-forgeable); the trigger trusts raw_app_meta_data->>'role' |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10 | Elevation of Privilege / Information Disclosure | Block evasion via direct PostgREST bypass of UI-only block enforcement | high | mitigate | no_block() SECURITY DEFINER wired into every socially-exposed table's INSERT policy (D-15); enforcement is in RLS, not app code — a blocked party's follow/post/endorse/DM insert is rejected by the policy regardless of client |
| T-08-11 | Elevation of Privilege / Tampering | Identity race — a phantom or mis-typed artist_profiles row from mistimed app_metadata | high | mitigate | app_metadata.role is set atomically at createUser() time (plan 08-06), before the trigger fires in the same transaction; the trigger branches on raw_app_meta_data->>'role', which is already present — no post-insert UPDATE, no side-channel timing (resolved D-03; RESEARCH Pitfall 2/3) |
| T-08-12 | Spoofing | A client forging app_metadata.role='industry' to self-provision an industry account | medium | accept | app_metadata is writable only via the service-role admin API, never from a client JWT/user_metadata — a client cannot set it; documented as a Supabase platform guarantee (migration 030 curator-branch precedent relies on the same) |
</threat_model>

<verification>
- `npx supabase db push --dry-run` exits 0 after each migration.
- After a real push (plan 08-05 [BLOCKING] push): as two seeded users A (blocker) and B, insert a block A→B, then attempt as B to follow/wall-post/endorse/DM A — all rejected by RLS (SC-2 manual client test).
- SC-5 verified end-to-end in plan 08-06 once createIndustryMember() exists.
</verification>

<success_criteria>
- SC-2 (completes plan 08-02): no_block() wired into all five existing socially-exposed tables.
- SC-5 (schema half): handle_new_user() industry branch present, app_metadata-keyed, building a correct dual-role-column member_type='industry' row + free subscription, no claim_collaborators, no side-channel table.
</success_criteria>

<output>
Create `.planning/phases/08-identity-schema-foundation/08-04-SUMMARY.md` when done.
</output>
