---
phase: 08-identity-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/034_member_identity_wave4.sql
  - types/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "SC-1: artist_profiles has a member_type column (TEXT, CHECK IN ('artist','industry'), DEFAULT 'artist', NOT NULL)"
    - "SC-1: artist_profiles has a search_vector tsvector column, GENERATED ALWAYS AS STORED, built from artist_name, genres, location, industry_roles, handle, bio (D-17)"
    - "SC-1: a GIN index named idx_artist_profiles_search_vector exists on artist_profiles(search_vector)"
    - "featured_project_id can only reference a public/released vault_project; a private-draft reference is rejected at the DB level (D-16)"
    - "when a featured vault_project is unpublished (is_public true->false), every artist_profiles.featured_project_id referencing it is set NULL automatically (D-16)"
    - "types/index.ts ArtistProfile type includes member_type ('artist' | 'industry') and search_vector (string | null) fields"
    - "the existing industry_profiles table (migration 001) is left completely untouched by this phase — no migration, no drop, no data move (D-06; confirmed dead-but-harmless: zero writers anywhere in the app, only read in 2 Antenna files where it always resolves null)"
  artifacts:
    - supabase/migrations/034_member_identity_wave4.sql
  key_links:
    - "search_vector generated expression uses the two-argument to_tsvector('english', ...) form so the column is IMMUTABLE-valid (RESEARCH Pitfall 5)"
    - "array columns (genres, industry_roles) are flattened via array_to_string(col, ' ') inside the search_vector expression (RESEARCH Pitfall 5)"
---

<objective>
Add the two genuinely-new Phase 8 identity columns to `artist_profiles` (`member_type`, `search_vector`) plus the featured-spotlight integrity triggers (D-16), and extend the hand-maintained `ArtistProfile` TypeScript type to match.

Purpose: This is the root of the unified member-identity model. Every Phase 9–13 feature joins against these columns; people-search (Phase 12) consumes `search_vector`; the featured-spotlight profile card (Phase 9 / PROFILE-05) relies on the D-16 public-only integrity guarantee. Only `member_type` and `search_vector` are new — the six "showcase" columns (`banner_url`, `pronouns`, `open_to`, `featured_project_id`, `roles`, `verified`) already shipped in migration 010 and carry live data (RESEARCH finding 1); this migration re-asserts them with `ADD COLUMN IF NOT EXISTS` no-ops purely to document the full identity set without risk.

Output: Migration `034_member_identity_wave4.sql`, updated `ArtistProfile` type.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-identity-schema-foundation/08-CONTEXT.md
@.planning/phases/08-identity-schema-foundation/08-RESEARCH.md
@.planning/phases/08-identity-schema-foundation/08-PATTERNS.md
@.planning/phases/08-identity-schema-foundation/08-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols this plan (08-01) creates — exclude from drift/source-grounding verification:
- Migration file `supabase/migrations/034_member_identity_wave4.sql`
- Column `artist_profiles.member_type` (TEXT)
- Column `artist_profiles.search_vector` (tsvector, GENERATED STORED)
- Index `idx_artist_profiles_search_vector` (GIN)
- DB function `check_featured_project_is_public()` + trigger `artist_profiles_featured_project_check`
- DB function `clear_featured_if_unpublished()` + trigger `vault_projects_clear_featured_on_unpublish`
- New fields on `ArtistProfile` type in `types/index.ts`: `member_type`, `search_vector`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 034 — member_type, search_vector, GIN index, featured-spotlight integrity triggers</name>
  <files>supabase/migrations/034_member_identity_wave4.sql</files>
  <read_first>
    - supabase/migrations/033_social_campaigns.sql (confirm 034 is the next free number; read its header banner style)
    - supabase/migrations/031_curators_column_privileges.sql (migration header banner convention, lines 1-6)
    - supabase/migrations/022_artist_profile_genres_array.sql (ADD COLUMN IF NOT EXISTS with inline CHECK precedent)
    - supabase/migrations/020_artist_profile_rights_fields.sql (additive ALTER TABLE precedent)
    - .planning/phases/08-identity-schema-foundation/08-RESEARCH.md (Code Examples section, migration 034 skeleton, lines ~386-453 — use the search_vector, CHECK, and both trigger functions verbatim; Pitfall 5 for the two-arg to_tsvector requirement)
    - supabase/migrations/001_initial_schema.sql (current artist_profiles column set, and update_updated_at() trigger-function invocation shape referenced by migration 030 line 51-54)
  </read_first>
  <action>
    Create supabase/migrations/034_member_identity_wave4.sql following the migration-header-banner convention ("-- ====", "Funūn — Wave 4: Identity & Schema Foundation", "Migration 034: member_type + search_vector + featured-spotlight integrity", "Run via: supabase db push").

    ALTER TABLE artist_profiles ADD COLUMN IF NOT EXISTS member_type TEXT NOT NULL DEFAULT 'artist' CHECK (member_type IN ('artist', 'industry')). Also ADD COLUMN IF NOT EXISTS for banner_url (TEXT), pronouns (TEXT), open_to (JSONB NOT NULL DEFAULT '[]'::jsonb), featured_project_id (UUID) — these six are documented no-ops (already live from migration 010); include a comment noting they are self-documenting no-ops, do NOT DROP or retype them.

    Add search_vector as a GENERATED ALWAYS AS (...) STORED tsvector column using the two-argument to_tsvector('english', ...) form (single-arg form is not IMMUTABLE — RESEARCH Pitfall 5). Compose it from D-17's exact field set: coalesce(artist_name,'') , array_to_string(genres,' ') , coalesce(location,'') , array_to_string(industry_roles,' ') , coalesce(handle,'') , coalesce(bio,'') — space-joined. Use ADD COLUMN IF NOT EXISTS.

    Create CREATE INDEX IF NOT EXISTS idx_artist_profiles_search_vector ON artist_profiles USING GIN (search_vector). This is a built-in tsvector_ops GIN index, NOT gin_trgm_ops (the ROADMAP's "GIN trigram" phrasing conflates two strategies — RESEARCH Pitfall 5 terminology note; a tsvector column takes plain GIN).

    Add the two D-16 featured-spotlight integrity functions + triggers from RESEARCH Code Examples verbatim: (1) check_featured_project_is_public() BEFORE INSERT OR UPDATE OF featured_project_id — RAISE EXCEPTION if featured_project_id is non-null and does not reference a vault_projects row with is_public=true; (2) clear_featured_if_unpublished() AFTER UPDATE OF is_public ON vault_projects — when is_public flips true->false, UPDATE artist_profiles SET featured_project_id = NULL WHERE featured_project_id = NEW.id. Confirm the vault_projects column used for public status is `is_public` by reading migration 001 / grep vault_projects; if the actual published-status column differs, use the real column name and note the correction in the SUMMARY.
  </action>
  <verify>
    <automated>npx supabase db push --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/034_member_identity_wave4.sql exists
    - File contains `member_type TEXT NOT NULL DEFAULT 'artist'` and `CHECK (member_type IN ('artist', 'industry'))`
    - File contains `search_vector tsvector` and `GENERATED ALWAYS AS` and `to_tsvector('english',` (two-arg form)
    - File contains `array_to_string(genres` and `array_to_string(industry_roles` (array flattening)
    - File contains `USING GIN (search_vector)` and `idx_artist_profiles_search_vector`
    - File contains `check_featured_project_is_public` and `clear_featured_if_unpublished`
    - File contains no bare single-arg `to_tsvector(` call (grep `to_tsvector(` lines all include `'english'`)
    - `npx supabase db push --dry-run` exits 0 (SQL parses/applies cleanly)
  </acceptance_criteria>
  <done>Migration 034 written; dry-run push validates the SQL; member_type, search_vector, GIN index, and both featured triggers are present and correctly formed.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ArtistProfile type with member_type and search_vector</name>
  <files>types/index.ts</files>
  <read_first>
    - types/index.ts (ArtistProfile type, lines 331-381 — see existing field ordering and comment convention; member_type/search_vector go alongside the migration-010 showcase fields near lines 368-378)
    - .planning/phases/08-identity-schema-foundation/08-RESEARCH.md (Runtime State Inventory — the ArtistProfile manual-update requirement)
  </read_first>
  <action>
    In types/index.ts, add two fields to the `ArtistProfile` type: `member_type: 'artist' | 'industry'` and `search_vector: string | null`. Place them alongside the existing migration-010 showcase block (handle, is_public, avatar_url, banner_url, pronouns, verified, roles, open_to, featured_project_id) with a `// Wave 4 identity (migration 034)` comment marking them. Do not touch types/supabase.ts (the generated file) — that is regenerated in a later plan's schema-push task; this is the hand-maintained type only.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - types/index.ts ArtistProfile type contains `member_type: 'artist' | 'industry'`
    - types/index.ts ArtistProfile type contains `search_vector: string | null`
    - `npx tsc --noEmit` exits 0 (no type errors introduced)
  </acceptance_criteria>
  <done>ArtistProfile carries member_type and search_vector; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (authenticated JWT) → PostgREST → artist_profiles | Any authenticated caller can query artist_profiles directly; column exposure is not gated here (gated in plan 08-05) |
| profile owner → featured_project_id → vault_projects | A member can pin a project id; without the D-16 trigger a private-draft id could leak a private project through the public profile spotlight |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | featured_project_id pointing at a private/unreleased vault_project | high | mitigate | check_featured_project_is_public() BEFORE INSERT/UPDATE trigger rejects non-public references; clear_featured_if_unpublished() nulls the reference when a project is later unpublished (D-16) — a visitor can never reach a private draft via a profile spotlight |
| T-08-02 | Tampering | search_vector generated expression non-immutability | low | mitigate | two-argument to_tsvector('english', ...) form guarantees an IMMUTABLE-valid generated column; single-arg form would fail migration and is explicitly avoided (RESEARCH Pitfall 5) |
</threat_model>

<verification>
- `npx supabase db push --dry-run` exits 0 after migration 034 is written.
- After a real push (deferred to plan 08-05's [BLOCKING] push task): `\d artist_profiles` shows `member_type` and `search_vector`; `SELECT * FROM pg_indexes WHERE tablename='artist_profiles' AND indexname LIKE '%search%'` returns a row (SC-1).
- `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- SC-1 (partial): member_type + search_vector columns and GIN index defined in migration 034.
- D-16 featured-spotlight integrity triggers present and dry-run-valid.
- ArtistProfile type extended with the two new fields.
</success_criteria>

<output>
Create `.planning/phases/08-identity-schema-foundation/08-01-SUMMARY.md` when done.
</output>
