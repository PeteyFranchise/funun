---
phase: 08-identity-schema-foundation
plan: 05
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - supabase/migrations/040_artist_profiles_column_privileges.sql
  - app/u/[handle]/page.tsx
  - app/(artist)/settings/page.tsx
  - app/profile/page.tsx
  - app/api/profile/route.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "SC-4: artist_profiles has column-level REVOKE SELECT FROM authenticated, anon plus a GRANT SELECT on exactly the D-11 PUBLIC column set — the PRIVATE columns (legal_first_name, legal_middle_name, legal_last_name, legal_name_suffix, contact_phone, mailing_address, pro, ipi, publisher, mlc_id, soundexchange_id) have no authenticated/anon SELECT grant (D-10/D-11)"
    - "SC-4: artist_profiles has REVOKE UPDATE FROM authenticated plus a GRANT UPDATE on only the owner-editable public columns; the PRIVATE columns are not authenticated-updatable"
    - "SC-4: after the REVOKE lands, a direct PostgREST `SELECT legal_first_name FROM artist_profiles` as the authenticated role fails with 42501 permission denied for column"
    - "SC-4: the public path app/u/[handle]/page.tsx selects an explicit PUBLIC column list (no `.select('*')`), so the page renders without a 42501 error and never leaks a PRIVATE column"
    - "SC-4: the three owner self-service paths (settings 2 sites, /profile, api/profile PATCH) read/write private fields via createServiceClient() AFTER an explicit auth.uid()===id ownership check performed with the session-bound client — so the owner's own Settings/Profile pages still load and save (D-19)"
    - "the column-privilege migration and the D-19 companion code fix ship in the SAME plan/PR so production is never broken between deploys (D-19)"
    - "this migration's column-privilege lockdown (migration 040) is a fixed tier, independent of and non-conflicting with Phase 13's future per-user row-level visibility toggle (SAFETY-04) — the two do not depend on or contradict each other (D-12)"
  prohibitions:
    - statement: "no authenticated or anon caller can read any PRIVATE column of artist_profiles via direct PostgREST"
      status: resolved
      verification: "migration 040 GRANT SELECT list excludes all 11 PRIVATE columns; a manual `SELECT legal_first_name FROM artist_profiles` as authenticated returns 42501 (post-push manual assertion)"
    - statement: "no remaining `.select('*')` or bare `.select()` against artist_profiles using the session-bound (authenticated-role) client may exist after this plan"
      status: resolved
      verification: "project-wide grep `select('\\*')|\\.select()` against artist_profiles surfaces only createServiceClient()-backed or explicit-column-list call sites"
  artifacts:
    - supabase/migrations/040_artist_profiles_column_privileges.sql
  key_links:
    - "the migration 040 GRANT SELECT column list and app/u/[handle]/page.tsx's explicit .select() list must be IDENTICAL (D-11 PUBLIC set) so app-layer projection and DB-layer grant never drift (RESEARCH Pitfall 1)"
    - "member_type and search_vector (plan 08-01) must be in the GRANT SELECT list — enforced by depends_on 08-01 and migration ordering 034 < 040"
    - "owner self-service paths perform the ownership check via the session-bound client's user.id, then query with createServiceClient() which bypasses RLS AND column grants (RESEARCH Pitfall 1 companion fix)"
---

<objective>
Ship the column-privilege lockdown on `artist_profiles` (D-10/D-11) AND its mandatory D-19 companion code fix in the same plan, then run the [BLOCKING] schema push. Retroactively closes a live, pre-existing CRITICAL exposure: `artist_profiles` currently has zero column-level REVOKE/GRANT, so legal names, contact, mailing address, and PRO/IPI/publisher/MLC/SoundExchange IDs are readable today by any authenticated/anon caller via direct PostgREST.

Purpose: This is SC-4 and the phase's headline security deliverable. The REVOKE alone would 500 the owner's own Settings/`/profile` pages, the public `/u/[handle]` page for every visitor, and the profile PATCH route — because Postgres fails the WHOLE `SELECT *` query on a column-restricted table (RESEARCH Pitfall 1). So the migration and the five call-site fixes MUST ship together (D-19). The schema push lives here because this is the highest-stakes, behavior-changing migration and must be validated against the live column grants, not just config-derived types.

Output: Migration `040_artist_profiles_column_privileges.sql`, four modified read/write paths (five sites), applied schema push, regenerated types.

CRITICAL: This plan's migration depends on plan 08-01's columns existing (the GRANT SELECT list includes member_type, search_vector). Migrations 034–039 (plans 08-01 through 08-04) must all be present before the [BLOCKING] push runs, since the push applies the entire migration set. This plan's push task is the single push for the whole phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-identity-schema-foundation/08-CONTEXT.md
@.planning/phases/08-identity-schema-foundation/08-RESEARCH.md
@.planning/phases/08-identity-schema-foundation/08-PATTERNS.md
@.planning/phases/08-identity-schema-foundation/08-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols this plan (08-05) creates — exclude from drift/source-grounding verification:
- Migration file `supabase/migrations/040_artist_profiles_column_privileges.sql` (REVOKE/GRANT column privileges on artist_profiles)
- Edited `.select()` projections in app/u/[handle]/page.tsx, app/(artist)/settings/page.tsx (2 sites), app/profile/page.tsx, app/api/profile/route.ts (createServiceClient swaps + explicit column lists)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 040 — artist_profiles column-privilege REVOKE/GRANT lockdown (D-10/D-11)</name>
  <files>supabase/migrations/040_artist_profiles_column_privileges.sql</files>
  <read_first>
    - supabase/migrations/031_curators_column_privileges.sql (the EXACT REVOKE/GRANT precedent named by D-10 — full file; reuse its structure, comment style explaining why each column is excluded, and the service_role-is-unaffected rationale)
    - .planning/phases/08-identity-schema-foundation/08-RESEARCH.md (Code Examples migration 038 skeleton lines ~456-485 — the PUBLIC GRANT SELECT list, owner-editable GRANT UPDATE list, and PRIVATE-no-grant set, all fully drafted; use verbatim, correcting the migration number to 040)
    - .planning/phases/08-identity-schema-foundation/08-CONTEXT.md (D-10 retroactive-fix framing; D-11 exact PUBLIC and PRIVATE column classification)
    - supabase/migrations/001_initial_schema.sql + migrations 020/021/022/010 (to confirm every column name in the GRANT lists actually exists on artist_profiles — do not GRANT a non-existent column)
  </read_first>
  <action>
    Create supabase/migrations/040_artist_profiles_column_privileges.sql with the standard header banner ("Migration 040: artist_profiles column-privilege lockdown (D-10/D-11) — retroactive PII fix; ships with the D-19 companion code fix in the same plan").

    REVOKE SELECT ON artist_profiles FROM authenticated, anon. GRANT SELECT on exactly the D-11 PUBLIC set (RESEARCH's drafted list): id, artist_name, genres, location, bio, career_stage, instagram_handle, threads_handle, tiktok_handle, spotify_url, monthly_listeners, total_streams, industry_roles, handle, member_type, pronouns, banner_url, open_to, featured_project_id, search_vector, avatar_url, verified, roles, is_public, created_at, updated_at — TO authenticated, anon. Cross-check every name against the live artist_profiles columns (migrations 001/010/020/021/022/034) before writing; if any listed column does not exist, omit it and note it in the SUMMARY.

    REVOKE UPDATE ON artist_profiles FROM authenticated. GRANT UPDATE on only the owner-editable public columns (RESEARCH's drafted list): artist_name, genres, location, bio, career_stage, instagram_handle, threads_handle, tiktok_handle, spotify_url, monthly_listeners, industry_roles, handle, pronouns, banner_url, open_to, featured_project_id, roles — TO authenticated. (member_type and verified are NOT owner-updatable; search_vector is generated; is_public toggling is handled where it is today — leave its grant as the app currently expects, confirm by reading the settings/profile update path.)

    The PRIVATE columns get NO grant at all for authenticated/anon: legal_first_name, legal_middle_name, legal_last_name, legal_name_suffix, contact_phone, mailing_address, pro, ipi, publisher, mlc_id, soundexchange_id. Add a comment (mirroring 031's claim-token-harvesting rationale) explaining these are PII/rights fields readable only by the owner via the service-role path (see this plan's Task 2 companion fix) — never by another authenticated user or anon via direct PostgREST. Add a header comment noting service_role bypasses RLS and column grants entirely, so every server-side write continues to work unchanged.
  </action>
  <verify>
    <automated>npx supabase db push --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/040_artist_profiles_column_privileges.sql exists
    - File contains `REVOKE SELECT ON artist_profiles FROM authenticated, anon`
    - File contains `GRANT SELECT (` listing member_type and search_vector (proving plan 08-01 columns are covered)
    - GRANT SELECT list excludes all 11 PRIVATE columns (grep confirms `legal_first_name`, `contact_phone`, `mailing_address`, `pro`, `ipi`, `publisher`, `mlc_id`, `soundexchange_id` do NOT appear in any GRANT clause)
    - File contains `REVOKE UPDATE ON artist_profiles FROM authenticated` and a `GRANT UPDATE (` list
    - `npx supabase db push --dry-run` exits 0
  </acceptance_criteria>
  <done>Migration 040 REVOKEs blanket column privileges and re-GRANTs only the PUBLIC/owner-editable sets; all 11 PRIVATE PII columns are ungrantable to authenticated/anon; dry-run validates.</done>
</task>

<task type="auto">
  <name>Task 2: D-19 companion code fix — explicit PUBLIC column list (public path) + createServiceClient ownership-checked reads/writes (3 owner paths)</name>
  <files>app/u/[handle]/page.tsx, app/(artist)/settings/page.tsx, app/profile/page.tsx, app/api/profile/route.ts</files>
  <read_first>
    - app/u/[handle]/page.tsx (line ~109 `.select('*')` public read path)
    - app/(artist)/settings/page.tsx (lines ~81 and ~89 — the TWO `.select('*')` owner self-read sites)
    - app/profile/page.tsx (line ~95 `.select('*')` owner self-read)
    - app/api/profile/route.ts (line ~115 `.select()` after `.update(update).eq('id', user.id)` — the owner PATCH read-back; and lines ~100-103 where auth.getUser() ownership is already established; and the EDITABLE_FIELDS allowlist lines ~8-34 — keep it as-is)
    - lib/supabase/server.ts (createServerClient / createApiClient / createServiceClient factories — confirm exact import names)
    - lib/profile/load.ts (buildProfileData() field usage — audit the explicit PUBLIC list against what the profile actually consumes, so u/[handle] does not under- or over-select)
    - .planning/phases/08-identity-schema-foundation/08-PATTERNS.md (the two-client-path split pattern and the exact fix snippets for each of the four files)
    - .planning/phases/08-identity-schema-foundation/08-RESEARCH.md (Pitfall 1 — the required companion fix, item 2)
  </read_first>
  <action>
    PUBLIC PATH — app/u/[handle]/page.tsx: replace the `.select('*')` against artist_profiles with an explicit column list matching migration 040's GRANT SELECT PUBLIC set EXACTLY (same column names, same order-independence): id, artist_name, genres, location, bio, career_stage, instagram_handle, threads_handle, tiktok_handle, spotify_url, monthly_listeners, total_streams, industry_roles, handle, member_type, pronouns, banner_url, open_to, featured_project_id, search_vector, avatar_url, verified, roles, is_public, created_at, updated_at. Audit against buildProfileData()'s consumed fields (lib/profile/load.ts) so nothing rendered is dropped; if buildProfileData reads a column not in the PUBLIC set (e.g. sound_identity, genre, total_streams), that column must be PUBLIC-safe — if it is a legacy PUBLIC field, add it to BOTH this list and migration 040's GRANT SELECT to keep them identical. Note any such addition in the SUMMARY.

    OWNER SELF-SERVICE PATHS — app/(artist)/settings/page.tsx (both sites), app/profile/page.tsx: switch each `.select('*')` against artist_profiles to use createServiceClient() (bypasses RLS + column grants), but ONLY AFTER establishing the caller's identity via the session-bound client (createServerClient()) — get `const { data: { user } } = await supabase.auth.getUser()`, redirect('/signin') if absent, then run the service-role query filtered `.eq('id', user.id)` using user.id from the verified session (never client input). This is the "self-service routes independently re-verify row ownership server-side before using the service-role client" pattern (mirrors admin routes re-verifying is_admin).

    OWNER PATCH PATH — app/api/profile/route.ts PATCH handler: keep the EDITABLE_FIELDS allowlist exactly as-is (correct mass-assignment protection). The auth.getUser() ownership check (lines ~100-103) is already present — swap only the `.update(update).eq('id', user.id).select().single()` call to run on createServiceClient() instead of the session-bound client, so the read-back of the updated row (which may include private columns) does not 42501. Use the already-verified user.id for the .eq filter.

    Run a fresh project-wide grep for any OTHER `.select('*')` or bare `.select()` against artist_profiles beyond these five sites (RESEARCH Pitfall 1 item 2c — new call sites may exist). If found, apply the correct pattern (explicit PUBLIC list if it is a public/other-user read; createServiceClient + ownership check if it is an owner self-read) and list them in the SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/u/[handle]/page.tsx no longer contains `.select('*')` against artist_profiles; it contains an explicit list including `member_type` and `handle` and excluding `legal_first_name`/`contact_phone`/`pro`
    - app/(artist)/settings/page.tsx uses `createServiceClient()` for the two artist_profiles reads, each preceded by an `auth.getUser()`-derived `user.id` ownership filter
    - app/profile/page.tsx uses `createServiceClient()` for its artist_profiles read with a `user.id` ownership filter
    - app/api/profile/route.ts PATCH uses `createServiceClient()` for the `.update(...).select()` read-back; EDITABLE_FIELDS allowlist unchanged
    - project-wide grep for `.select('*')` / bare `.select()` against artist_profiles surfaces only createServiceClient-backed or explicit-PUBLIC-list sites
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The public path selects only PUBLIC columns; all three owner self-service paths read/write private fields via an ownership-checked service-role client; no session-bound `.select('*')` against artist_profiles remains; type-check passes.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Apply the phase schema push + regenerate types</name>
  <files>types/supabase.ts</files>
  <read_first>
    - package.json (confirm the db:push and db:types npm scripts and their exact commands)
    - .planning/phases/08-identity-schema-foundation/08-RESEARCH.md (Environment Availability — npx supabase db push; SUPABASE_ACCESS_TOKEN for non-interactive; SUPABASE_SERVICE_ROLE_KEY present)
    - .planning/phases/08-identity-schema-foundation/08-VALIDATION.md (Wave 0 note on staging vs. production for the behavior-changing REVOKE)
  </read_first>
  <action>
    This is the single [BLOCKING] schema push for Phase 8. It runs AFTER all seven migrations (034–040) from plans 08-01 through 08-05 are written and each has passed `npx supabase db push --dry-run`, and AFTER this plan's Task 2 companion code fix is complete — so the column REVOKE and the code fix land together (D-19), never leaving production broken.

    Ensure SUPABASE_ACCESS_TOKEN is set in the environment for non-interactive push (RESEARCH Environment Availability). Run `npx supabase db push` (NOT --dry-run — the real push). If the push requires an interactive prompt that cannot be suppressed, STOP and flag for manual intervention (this task is gate=blocking / the plan is not fully autonomous for the push step) — do not force it.

    After a successful push, regenerate the generated types: run the project's db:types script (`supabase gen types typescript` per package.json — confirm exact invocation) to refresh types/supabase.ts against the now-live schema (RESEARCH Runtime State Inventory: types/supabase.ts goes stale the moment any migration lands).

    Then run the SC-4 manual assertion smoke set from 08-VALIDATION.md: as the authenticated role `SELECT legal_first_name FROM artist_profiles LIMIT 1` must return 42501; the owner's Settings/`/profile` pages and a public `/u/[handle]` page must load without a 500. Record results in the SUMMARY. If the live push cannot be run in this environment (no token / no live project access), document that explicitly in the SUMMARY as a manual-intervention gap and mark the phase's push as pending human action — do NOT mark the phase verified without it (per the [BLOCKING] schema-push requirement: build/type checks pass without the push, creating a false-positive verification state).
  </action>
  <verify>
    <automated>npx supabase db push --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push` completed successfully (all of 034–040 applied) OR the SUMMARY explicitly records a manual-intervention gap with the exact reason (no token / no live access) and the phase push is marked pending human action
    - types/supabase.ts regenerated after the push (or the regeneration gap documented)
    - Post-push SC-4 smoke assertions recorded in the SUMMARY: `SELECT legal_first_name` as authenticated → 42501; owner Settings/profile + public /u/[handle] load without 500 (or documented as pending human verification)
  </acceptance_criteria>
  <done>All seven Phase 8 migrations are applied to the live database (or a manual-intervention gap is explicitly documented), generated types are refreshed, and the SC-4 lockdown is confirmed to block private-column reads while leaving owner/public paths working.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated/anon JWT → PostgREST → artist_profiles columns | Direct PostgREST access must not read PRIVATE PII columns — the app-layer allowlist alone is bypassable |
| profile owner (session) → own private columns | The owner legitimately reads/writes their own legal/contact/rights fields; column REVOKE is role-scoped, not row-scoped, so this needs the service-role two-client-path split |
| deploy window (migration ↔ code fix) | If the migration deploys before the code fix, every SELECT * call site 500s — the two must ship together |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-13 | Information Disclosure | Direct PostgREST `SELECT *` reading legal name / contact / PRO-IPI-publisher-MLC-SoundExchange PII of any profile (live pre-existing CRITICAL-1 exposure) | critical | mitigate | migration 040 REVOKEs blanket column SELECT and re-GRANTs only the PUBLIC set; the 11 PRIVATE columns have no authenticated/anon grant — a direct `SELECT legal_first_name` returns 42501 (D-10/D-11) |
| T-08-14 | Denial of Service | Column REVOKE 500ing the owner's own Settings/profile pages, the public /u/[handle] page, and the PATCH route (SELECT * fails the whole query) | high | mitigate | D-19 companion fix ships in the SAME plan: public path uses an explicit PUBLIC column list; owner self-service paths use createServiceClient() after an auth.uid()===id ownership check — no SELECT * against a column-restricted table remains (RESEARCH Pitfall 1) |
| T-08-15 | Elevation of Privilege | An owner self-service route using createServiceClient() without re-verifying ownership, letting one user read another's private columns | high | mitigate | every service-role query is filtered `.eq('id', user.id)` where user.id comes from the session-bound client's verified auth.getUser(), never client input — the "independently re-verify ownership server-side" pattern |
| T-08-16 | Tampering | Deploy-ordering race: migration lands before the code fix, breaking production | high | mitigate | migration 040 and the four-file companion fix are in ONE plan/PR (D-19); the [BLOCKING] push (Task 3) runs only after Task 2's code fix is complete |
</threat_model>

<verification>
- Both migration and code-fix tasks pass their automated verifies (`db push --dry-run`, `tsc --noEmit`).
- [BLOCKING] push applies 034–040 to the live DB (or documents a manual-intervention gap).
- SC-4 manual assertions: authenticated `SELECT legal_first_name` → 42501; owner Settings/profile + public /u/[handle] load without 500; public /u/[handle] payload contains no PRIVATE column values.
</verification>

<success_criteria>
- SC-4: column-privilege lockdown + D-19 companion fix shipped together; private PII columns unreadable via direct PostgREST; owner and public paths still function.
- Phase schema push applied (or manual-intervention gap explicitly documented).
</success_criteria>

<output>
Create `.planning/phases/08-identity-schema-foundation/08-05-SUMMARY.md` when done.
</output>
