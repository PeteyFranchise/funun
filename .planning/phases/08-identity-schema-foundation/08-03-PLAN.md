---
phase: 08-identity-schema-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/036_notifications_dm_reads.sql
autonomous: true
requirements: []
must_haves:
  truths:
    - "SC-3: notifications table carries actor-snapshot columns actor_id (UUID), actor_name (TEXT), actor_avatar_url (TEXT), added via ADD COLUMN IF NOT EXISTS"
    - "SC-3: notifications is a member of the supabase_realtime publication (pg_publication_tables shows tablename='notifications')"
    - "SC-3: a dm_thread_reads table exists with (thread_id, user_id) composite PK and a last_read_at TIMESTAMPTZ, RLS enabled"
    - "the notifications realtime ADD TABLE is idempotent — re-running the migration does not error if notifications is already published"
    - "a member can SELECT only their own dm_thread_reads rows (user_id = auth.uid())"
  artifacts:
    - supabase/migrations/036_notifications_dm_reads.sql
  key_links:
    - "the notifications ADD COLUMN and ALTER PUBLICATION must not broadly REVOKE table-level SELECT on notifications, or realtime delivery breaks (RESEARCH Pitfall 6) — this migration only ADDs, it must not touch notifications grants"
---

<objective>
Extend the existing `notifications` table (migration 009) with denormalized actor-snapshot columns, add it to the Supabase Realtime publication for live bell delivery, and create the `dm_thread_reads` table that powers DM unread counts.

Purpose: Phase 10 (Notifications bell, NOTIF-01/02/03) subscribes to `postgres_changes` on `notifications` for live unread badges — that requires the table be in the `supabase_realtime` publication, and each notification row to carry an actor snapshot (name + avatar) so the bell renders "X followed you" without a join. Phase 11 (PRESENCE-03, floating DM widget unread badge) reads `dm_thread_reads.last_read_at` vs. message timestamps to compute unread counts. This plan is pure schema; the emit path and subscription live in Phases 10/11.

Output: Migration `036_notifications_dm_reads.sql`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-identity-schema-foundation/08-CONTEXT.md
@.planning/phases/08-identity-schema-foundation/08-RESEARCH.md
@.planning/phases/08-identity-schema-foundation/08-PATTERNS.md
@.planning/phases/08-identity-schema-foundation/08-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols this plan (08-03) creates — exclude from drift/source-grounding verification:
- Migration file `supabase/migrations/036_notifications_dm_reads.sql`
- New columns on `notifications`: `actor_id`, `actor_name`, `actor_avatar_url`
- `notifications` membership in the `supabase_realtime` publication
- Table `dm_thread_reads` + its RLS policies
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 036 — notifications actor columns + realtime publication + dm_thread_reads</name>
  <files>supabase/migrations/036_notifications_dm_reads.sql</files>
  <read_first>
    - supabase/migrations/009_antenna_notifications.sql (current notifications table schema lines 62-91, its RLS "Users see own notifications" policy, and the "inserts happen server-side via service-role, no INSERT policy for users" comment)
    - supabase/migrations/014_dm_realtime.sql (the exact idempotent ALTER PUBLICATION supabase_realtime ADD TABLE ... guard pattern — mirror it, guarding via pg_publication_tables so re-running does not error)
    - supabase/migrations/012_social_layer.sql (dm_threads / dm_messages shape — dm_thread_reads references a thread; confirm the dm_threads PK column name and type to reference correctly)
    - .planning/phases/08-identity-schema-foundation/08-CONTEXT.md (Claude's-discretion note on the actor-snapshot column set; D-15 realtime framing)
    - supabase/migrations/031_curators_column_privileges.sql (header banner convention)
  </read_first>
  <action>
    Create supabase/migrations/036_notifications_dm_reads.sql with the standard header banner ("Migration 036: notifications actor-snapshot columns + realtime + dm_thread_reads").

    NOTIFICATIONS EXTENSION: ALTER TABLE notifications ADD COLUMN IF NOT EXISTS actor_id UUID REFERENCES auth.users ON DELETE SET NULL, ADD COLUMN IF NOT EXISTS actor_name TEXT, ADD COLUMN IF NOT EXISTS actor_avatar_url TEXT. These are denormalized snapshot columns (the actor's name/avatar at notification time) so the bell renders without a join and survives the actor later renaming. Do NOT add or alter any REVOKE/GRANT on notifications — RESEARCH Pitfall 6: a broad table-level SELECT revoke would silently break realtime delivery. This migration only ADDs.

    REALTIME: add notifications to the supabase_realtime publication using the idempotent guard from migration 014 — wrap in a DO $$ block that checks pg_publication_tables for pubname='supabase_realtime' AND tablename='notifications' and only runs ALTER PUBLICATION supabase_realtime ADD TABLE notifications if absent. (Read migration 014 for the exact guard shape and reuse it verbatim.)

    DM_THREAD_READS: CREATE TABLE IF NOT EXISTS dm_thread_reads (thread_id UUID NOT NULL REFERENCES dm_threads(id) ON DELETE CASCADE, user_id UUID NOT NULL REFERENCES auth.users ON DELETE CASCADE, last_read_at TIMESTAMPTZ NOT NULL DEFAULT now(), PRIMARY KEY (thread_id, user_id)). Confirm dm_threads' actual PK column/type from migration 012 before writing the FK. ENABLE ROW LEVEL SECURITY. Policies (idempotent DROP POLICY IF EXISTS + CREATE POLICY): dm_thread_reads_select_own FOR SELECT USING (user_id = auth.uid()); dm_thread_reads_upsert_own FOR INSERT TO authenticated WITH CHECK (user_id = auth.uid()); dm_thread_reads_update_own FOR UPDATE TO authenticated USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid()) — the widget upserts its own read marker in Phase 11.
  </action>
  <verify>
    <automated>npx supabase db push --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/036_notifications_dm_reads.sql exists
    - File contains `ADD COLUMN IF NOT EXISTS actor_id`, `actor_name`, and `actor_avatar_url` on notifications
    - File contains an idempotent guard checking `pg_publication_tables` before `ALTER PUBLICATION supabase_realtime ADD TABLE notifications`
    - File contains `CREATE TABLE IF NOT EXISTS dm_thread_reads` with `PRIMARY KEY (thread_id, user_id)` and `last_read_at TIMESTAMPTZ`
    - File contains `ENABLE ROW LEVEL SECURITY` on dm_thread_reads and a `USING (user_id = auth.uid())` SELECT policy
    - File contains no `REVOKE` or `GRANT` against notifications (grep confirms neither keyword appears near `notifications`)
    - `npx supabase db push --dry-run` exits 0
  </acceptance_criteria>
  <done>notifications carries actor-snapshot columns and is in the realtime publication (idempotently); dm_thread_reads exists with owner-only RLS; dry-run validates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| realtime subscriber (authenticated) → notifications | RLS filters delivered rows per subscriber; requires table-level SELECT grant to remain intact |
| client (authenticated) → dm_thread_reads | A member must only read/write their own read markers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07 | Information Disclosure | notifications realtime delivering another user's notifications | low | mitigate | existing "Users see own notifications" RLS (migration 009, USING auth.uid()=user_id) filters realtime delivery per subscriber; this migration preserves it and does not touch notifications grants (RESEARCH Pitfall 6) |
| T-08-08 | Tampering | a member overwriting another member's dm_thread_reads marker to fake unread counts | low | mitigate | dm_thread_reads INSERT/UPDATE policies WITH CHECK (user_id = auth.uid()) restrict writes to the caller's own row |
| T-08-09 | Denial of Service | non-idempotent ALTER PUBLICATION erroring on re-run and blocking the whole migration | low | mitigate | pg_publication_tables guard makes the ADD TABLE a no-op if already published (migration 014 precedent) |
</threat_model>

<verification>
- `npx supabase db push --dry-run` exits 0.
- After a real push (plan 08-05 [BLOCKING] push): `SELECT * FROM pg_publication_tables WHERE pubname='supabase_realtime' AND tablename='notifications'` returns a row; `\d dm_thread_reads` succeeds (SC-3).
</verification>

<success_criteria>
- SC-3: notifications actor columns + realtime publication membership + dm_thread_reads all present and dry-run-valid.
</success_criteria>

<output>
Create `.planning/phases/08-identity-schema-foundation/08-03-SUMMARY.md` when done.
</output>
