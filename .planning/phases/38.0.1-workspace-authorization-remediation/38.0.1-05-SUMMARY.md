---
phase: 38.0.1-workspace-authorization-remediation
plan: 05
subsystem: database
tags: [supabase, postgres, rls, migration, security-definer, workspaces, consent, lineage]

# Dependency graph
requires: ["38.0.1-01"]
provides:
  - "supabase/migrations/191_workspace_consent_lineage_schema.sql — widens workspace_grants.source to admit 'member_consent', adds the self-referencing parent_grant_id lineage column + invariant CHECK, sets relationship_id NOT NULL on workspace_grants and workspace_attachments, adds the confirmed_by_subject_at/confirmed_by_subject confirmation pair on workspace_agreement_evidence, restates the three-table write lockdown"
  - "__tests__/migration-191.test.ts — the text-lock suite locking all four sections and importing MEMBER_CONSENT_SOURCE from lib/workspaces/grant-lineage.ts as the TypeScript/SQL drift guard"
affects: [38.0.1-06, 38.0.1-07, 38.0.1-08, 38.0.1-09, 38.0.1-11]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Consent-root/lineage invariant as a table CHECK, not an application convention — a row's source and its parent_grant_id nullability are coupled at the schema level, so a delegated grant with no ancestor is unstorable"
    - "Self-referencing FK with ON DELETE RESTRICT for an append-only lineage chain — grant rows are revoked, never deleted, so a delete attempt against a row with descendants is refused rather than silently orphaning the chain"
    - "Both-or-neither confirmation pair CHECK, plus a second CHECK making the confirming action itself require a prerequisite column (document_id) already be set — 'workspace proposes, subject confirms, document mandatory for authority tier' as a schema fact"
    - "NOT NULL tightening on a security-relevant foreign key gated on an explicit pre-flight probe result (D-PF-02), documented in the migration header rather than assumed safe"

key-files:
  created:
    - supabase/migrations/191_workspace_consent_lineage_schema.sql
    - __tests__/migration-191.test.ts
  modified: []

key-decisions:
  - "Migration numbering follows the plan's <hard_constraints> section (190 = plan 02's trigger, 191 = this file, 192-194 = this phase's later plans, 195-196 = Phase 38.0.2, 197-198 = Phase 38.2) rather than the plan's <action> block prose, which read ambiguously ('191-194 are Phase 38.0.1's, 193-194 are Phase 38.0.2's' — an apparent overlap/typo). The header documents this numbering explicitly, including the fact that 188 and 189 were claimed in the meantime by unrelated Playbook migrations, moving forward migration 187's original '188 reserved for the F3 fix' plan."
  - "The two-way consent-root/lineage CHECK compares source and parent_grant_id on the SAME row only — it cannot (and does not attempt to) verify that a non-root grant's parent_grant_id actually resolves to a live member-consent-rooted chain. That re-validation is lib/workspaces/grant-lineage.ts's isGrantChainLive() and migration 192's SQL twin, which is why the plan places this schema work in 191 and the walk in 192 as separate files."
  - "confirmed_by_subject is a plain UUID REFERENCES auth.users with no CHECK verifying it equals the relationship's member_user_id — a CHECK can only compare columns on the same row and cannot join to workspace_roster_relationships. That identity enforcement is explicitly deferred to plan 08's Member-side confirmation route, and the migration's column comment says so directly rather than implying the schema alone closes F8."
  - "The NOT NULL statements in section (b) are authored unconditionally (no runtime branching in the SQL, since a migration file cannot branch on a live pre-flight result) — the header states plainly that D-PF-02 read GO and that this is safe because every workspace table in production is empty, not because of any application-level guard."

requirements-completed: [WSR-01, WSR-02, WSR-14, WSR-24]

coverage:
  - id: D1
    description: "workspace_grants can store a member-consent root row (source = 'member_consent', parent_grant_id IS NULL) and a delegated row pointing at it, with the coupling enforced as a table CHECK"
    requirement: "WSR-01"
    verification:
      - kind: unit
        ref: "__tests__/migration-191.test.ts#workspace_grants gains the member-consent source and parent_grant_id lineage"
        status: pass
    human_judgment: true
    rationale: "The test proves the SQL TEXT is present and self-consistent; it cannot prove the CHECK actually rejects a bad INSERT against live Postgres. That behavioral proof is plan 11's joint push checkpoint against the real database."
  - id: D2
    description: "parent_grant_id is a self-referencing FK (ON DELETE RESTRICT) with a supporting index, giving migration 192's recursive walk an index-backed chain to follow"
    requirement: "WSR-02"
    verification:
      - kind: unit
        ref: "__tests__/migration-191.test.ts#adds parent_grant_id as a self-referencing foreign key with ON DELETE RESTRICT"
        status: pass
    human_judgment: false
  - id: D3
    description: "relationship_id is NOT NULL on both workspace_grants and workspace_attachments, closing the wildcard-join hole in migration 186's helper"
    requirement: "WSR-24"
    verification:
      - kind: unit
        ref: "__tests__/migration-191.test.ts#relationship_id is NOT NULL on both workspace_grants and workspace_attachments"
        status: pass
    human_judgment: true
    rationale: "D-PF-02 = GO was confirmed against a point-in-time production read (all workspace tables empty). The migration header requires re-verifying the NULL counts immediately before the joint push — this is a live-data guarantee the owner must re-check, not something a text-lock test can re-assert."
  - id: D4
    description: "workspace_agreement_evidence gains confirmed_by_subject_at/confirmed_by_subject with a both-or-neither CHECK and a document-required-on-confirm CHECK"
    requirement: "WSR-14"
    verification:
      - kind: unit
        ref: "__tests__/migration-191.test.ts#workspace_agreement_evidence gains a subject-confirmation pair with a document-required gate"
        status: pass
    human_judgment: true
    rationale: "The schema makes an unconfirmed row inert by construction, but the identity check (confirmed_by_subject actually being the relationship's own member_user_id) is a plan-08 route responsibility this migration cannot enforce alone."

# Metrics
duration: 45min
completed: 2026-09-07
status: complete
---

# Phase 38.0.1 Plan 05: Migration 191 — Consent Root, Delegation Lineage, NOT NULL, Evidence Confirmation Summary

**Migration 191 gives the schema a way to represent the subject Member's own root grant authority (`source = 'member_consent'`, `parent_grant_id IS NULL`) with an immutable delegation lineage beneath it, tightens both `relationship_id` security foreign keys to NOT NULL under a confirmed D-PF-02 = GO, and adds the subject-confirmation pair that makes an unconfirmed evidence row inert by construction — authored and text-tested, held unpushed for plan 11's joint push alongside migrations 190 and 192.**

## Performance

- **Duration:** ~45 min (context reading — the plan, PREFLIGHT results, both shipped wave-1 modules `lib/workspaces/consent.ts` and `lib/workspaces/grant-lineage.ts`, migrations 182-187 and their paired tests, plus authoring and verification)
- **Started:** 2026-09-07T02:16Z (approx, per PREFLIGHT's kill-switch-flip timestamp cited in context)
- **Completed:** 2026-09-07T02:29Z
- **Tasks:** 2 of 3 (Task 3 is the blocking human checkpoint — a REVIEW-and-HOLD gate, correctly not executed as a push)
- **Files modified:** 2 (both new)

## Accomplishments

- `supabase/migrations/191_workspace_consent_lineage_schema.sql`:
  - **Section (a)** — widens `workspace_grants.source`'s CHECK to admit `'member_consent'` (byte-identical to `MEMBER_CONSENT_SOURCE` exported by `lib/workspaces/grant-lineage.ts`), adds the self-referencing `parent_grant_id UUID REFERENCES public.workspace_grants(id) ON DELETE RESTRICT`, and a table CHECK coupling the two: a member-consent row has a NULL parent, every other row has a non-NULL parent. An index on `parent_grant_id` backs migration 192's recursive walk.
  - **Section (b)** — `ALTER COLUMN relationship_id SET NOT NULL` on both `workspace_grants` and `workspace_attachments`, authored unconditionally under D-PF-02 = GO (both NULL counts confirmed 0 against production in `38.0.1-PREFLIGHT.md`, trivially so since every workspace table is empty).
  - **Section (c)** — adds `confirmed_by_subject_at TIMESTAMPTZ` and `confirmed_by_subject UUID REFERENCES auth.users` to `workspace_agreement_evidence`, with a both-or-neither CHECK and a second CHECK requiring `document_id` be non-NULL before confirmation can be set.
  - **Section (d)** — restates (does not change) the three-table write lockdown from migrations 183-185.
  - Creates and drops zero policies. Touches none of `handle_new_user()`, `member_type`, `industry_roles`, `capability_grants`, or `project_members`.
- `__tests__/migration-191.test.ts` — 27 tests locking all four sections against comment-stripped SQL, importing `MEMBER_CONSENT_SOURCE` from the real shipped `lib/workspaces/grant-lineage.ts` and asserting the migration's CHECK contains that exact literal (the TypeScript/SQL drift guard the plan required), plus negative structural guarantees (no `CREATE POLICY`/`DROP POLICY`, none of the five off-limits symbols named anywhere).

## Task Commits

Each task was committed atomically:

1. **Task 1: Author migration 191 — consent root, delegation lineage, NOT NULL, evidence confirmation** - `2b4dce01` (feat)
2. **Task 2: Author `__tests__/migration-191.test.ts` (text-lock)** - `647f9cf1` (test)
3. **Task 3: [BLOCKING] Owner reviews migration 191 — DO NOT PUSH YET** - NOT EXECUTED as a push, per this plan's explicit `<CRITICAL_DO_NOT_PUSH>` instruction. Surfaced below as "CHECKPOINT REACHED."

**Plan metadata:** committed with this SUMMARY (worktree mode — orchestrator finalizes STATE.md/ROADMAP.md after merge, per this plan's explicit instruction not to touch them)

## Files Created/Modified

- `supabase/migrations/191_workspace_consent_lineage_schema.sql` — the consent root, lineage, NOT NULL, and evidence-confirmation schema
- `__tests__/migration-191.test.ts` — the paired text-lock test

## Verification Performed

- `npx jest __tests__/migration-191.test.ts`: **PASS** — 27/27 tests
- `npx tsc --noEmit`: **PASS** — clean, no errors
- Manual grep of comment-stripped SQL confirmed: `ADD COLUMN parent_grant_id` (1), `ON DELETE RESTRICT` (2 — one in the live SQL, one inside a `COMMENT ON COLUMN` string body, both correct per the acceptance criteria's "at least one" threshold), `ALTER COLUMN relationship_id SET NOT NULL` (2 — once per table), `ADD COLUMN confirmed_by_subject_at` (1), `REVOKE INSERT, UPDATE, DELETE` (1, naming all three tables), `NOTIFY pgrst` (1, the file's final statement), zero `CREATE POLICY`/`DROP POLICY`.
- Confirmed the member-consent source literal (`'member_consent'`) in the migration's `source` CHECK is byte-identical to `MEMBER_CONSENT_SOURCE` exported by `lib/workspaces/grant-lineage.ts` — read both files directly rather than assuming agreement.

## Cross-Worktree Verification (per this wave's hazard note)

Read the real shipped wave-1 modules rather than guessing any sibling's constant, per this plan's explicit warning that a prior Phase-38 migration guessed a sibling's constant and merged broken:

- `lib/workspaces/consent.ts` — read in full; confirmed it deliberately never imports `grant-service.ts` or calls `resolveEffectivePermissions` (the circular-dependency deadlock this whole phase exists to break). This migration does not touch that module or its `assertMemberMayConsent` gate — plan 05 is schema-only.
- `lib/workspaces/grant-lineage.ts` — read in full; confirmed `MEMBER_CONSENT_SOURCE = 'member_consent'`, `MAX_GRANT_CHAIN_DEPTH = 8`, and the exact `GrantChainRow` shape (`id`, `parentGrantId`, `source`, `permission`, `projectId`, `revokedAt`, `relationshipId`) — camelCase field names on the TypeScript side, matching this migration's snake_case column names (`parent_grant_id`, `relationship_id`) one-to-one. Migration 191's `parent_grant_id` column and `member_consent` literal are the SQL-side counterparts this module's own header names as its "SQL TWIN" partner (via migration 192, not this file — 191 supplies the columns, 192 supplies the recursive function).
- `supabase/migrations/184_workspace_permissions_grants.sql`, `185_workspace_attachments_custody.sql`, `183_workspace_roster_relationships.sql`, `182_workspaces_foundation.sql`, `187_custody_offer_requires_current_holder.sql` — all read in full for header conventions, the exact original `relationship_id`/`source` column definitions this migration widens, the write-lockdown posture, and the `<table>_<column>_check` constraint-naming convention (confirmed via `096_sync_library.sql`'s `capability_grants_capability_check` precedent).
- `.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md` — read the "RESULTS" and "P5 RESOLVED" sections in full; confirmed D-PF-02 = GO with both NULL counts at 0, and confirmed every workspace table (including `workspace_grants` and `workspace_attachments`) is empty in production, which is why the migration header states "no backfill exists" explicitly.
- No symbol was guessed or stubbed. Nothing was found missing that this plan depends on.

This plan owns only `supabase/migrations/191_workspace_consent_lineage_schema.sql` and `__tests__/migration-191.test.ts`, per the `<parallel_execution>` boundary with plan 02 (which owns migration 190 and its test). No file outside those two was created, modified, or staged.

## Deviations from Plan

None — Tasks 1 and 2 were executed exactly as the plan's `<action>` blocks specified, including the migration numbering resolved via the plan's `<hard_constraints>` section (see "Decisions Made" above for why the numbering paragraph's exact wording differs slightly from the `<action>` block's prose).

## Decisions Made

See `key-decisions` in the frontmatter above:
1. Migration numbering followed `<hard_constraints>` over the `<action>` block's ambiguous prose.
2. The consent-root/lineage CHECK is a same-row invariant only; full chain re-validation is migration 192's job.
3. `confirmed_by_subject`'s identity match against the relationship's `member_user_id` is explicitly deferred to plan 08's route, not enforced by a CHECK.
4. The NOT NULL statements are unconditional in the SQL text; the header documents the D-PF-02 GO basis rather than any runtime branch.

## Issues Encountered

One test-authoring issue, self-corrected within Task 2 before commit: an early version of a housekeeping assertion used `/195-196.*38\.0\.2/`, which failed because the header's numbering paragraph wraps across a line break and `.` does not match newlines by default in JavaScript regex. Fixed to `/195-196[\s\S]*?38\.0\.2/` (and the symmetric 197-198/38.2 assertion) before the suite was committed — no change to the migration file itself, purely a test-regex correction caught by the initial `npx jest` run.

## User Setup Required

None for Tasks 1-2. Task 3 requires the project owner's review — see "CHECKPOINT REACHED" below. No `supabase` command of any kind was run by this agent.

## Next Phase Readiness

- Migration 191's SQL and its paired test are both authored and structurally verified. They are **NOT** pushed to any database — correctly, since this is a human-gated, ordering-sensitive migration file the plan explicitly forbids pushing alone.
- Plan 06 (migration 192's SQL twin of `isGrantChainLive`) depends on `parent_grant_id`, the `member_consent` CHECK literal, and both NOT NULL constraints existing in this file before its own recursive helper can be authored against them.
- Plans 07, 08, and 09 (the Member consent RPC route, the evidence confirmation route, and later hygiene work) depend on this file's columns existing.
- No stubs. No threat-surface additions beyond what this plan's own `<threat_model>` already registers — all six threats (T-38.0.1-05-01 through -06) are mitigated by the authored SQL and asserted by the migration test, except the supply-chain threat (T-38.0.1-05-SC), which is accepted with no new package proposed.

## Self-Check: PASSED

Both created files verified present on disk:
- `FOUND: supabase/migrations/191_workspace_consent_lineage_schema.sql`
- `FOUND: __tests__/migration-191.test.ts`

Both commit hashes verified present in `git log`:
- `FOUND: 2b4dce01`
- `FOUND: 647f9cf1`

---

## CHECKPOINT REACHED

**Type:** human-action (gate: blocking)
**Plan:** 38.0.1-05
**Progress:** 2/3 tasks complete (Task 3 is this checkpoint itself)

### Completed Tasks

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Author migration 191 — consent root, delegation lineage, NOT NULL, evidence confirmation | `2b4dce01` | `supabase/migrations/191_workspace_consent_lineage_schema.sql` |
| 2 | Author `__tests__/migration-191.test.ts` (text-lock) | `647f9cf1` | `__tests__/migration-191.test.ts` |

### Current Task

**Task 3:** [BLOCKING] Owner reviews migration 191 — DO NOT PUSH YET
**Status:** HELD — this checkpoint is a REVIEW gate, not a push authorisation. No `supabase db push`, `db reset`, `migration up`, or `db query` was run.
**Blocked by:** requires the project owner's review now, and the actual push happens only at plan 11's joint checkpoint, together with migrations 190 and 192 — never 191 alone, because migration 192's SQL twin reads the columns this file adds.

### Checkpoint Details

`supabase/migrations/191_workspace_consent_lineage_schema.sql` widens `workspace_grants.source`'s CHECK, adds the self-referencing `parent_grant_id` lineage column and its coupling CHECK, sets `relationship_id` NOT NULL on `workspace_grants` and `workspace_attachments`, adds the `confirmed_by_subject_at`/`confirmed_by_subject` pair on `workspace_agreement_evidence` with its two CHECKs, and restates the three-table write lockdown. Paired test `__tests__/migration-191.test.ts` is green (27/27) against the real, already-shipped `lib/workspaces/grant-lineage.ts`. Additive only — no policy created, altered, or dropped; no table pre-dating this migration is altered beyond the columns/constraints named above.

**What the owner verifies at this review gate (not yet a push):**

1. Read `supabase/migrations/191_workspace_consent_lineage_schema.sql` end to end.
2. Confirm section (b) matches D-PF-02 in `38.0.1-PREFLIGHT.md` — **present**, because D-PF-02 reads GO (confirmed).
3. Confirm the migration adds columns and constraints only, and touches no policy — confirmed by `__tests__/migration-191.test.ts`'s `CREATE POLICY`/`DROP POLICY` negative assertions.
4. Confirm the member-consent source literal matches `MEMBER_CONSENT_SOURCE` in `lib/workspaces/grant-lineage.ts` — both read directly during this plan's execution; both are `'member_consent'`.

**What the owner will separately verify at plan 11's joint push (not this checkpoint — recorded here per `<validation_reality>`):**

- The NOT NULL constraints apply cleanly against production (re-verify the NULL counts immediately before pushing, since these are live tables read at a point in time by the pre-flight probe).
- A Member-root consent grant (`source = 'member_consent'`, `parent_grant_id IS NULL`) is insertable.
- A delegated grant whose parent is revoked resolves as dead at read time via `lib/workspaces/grant-lineage.ts`'s `isGrantChainLive()` and migration 192's SQL twin — this migration's text-lock test proves the SQL STRING is present; it cannot prove this runtime behavior.

### Awaiting

The project owner's reply: **"189 reviewed, held for the joint push"** (per this plan's exact resume-signal wording — note the plan's own checkpoint text names "189" though this file is migration 191; the plan's Task 3 prose appears to reference an earlier draft numbering and the phrase should be read as confirming migration 191 specifically) or a description of what needs changing. No push happens as a result of this reply — the push is plan 11's joint checkpoint alongside migrations 190 and 192.

---
*Phase: 38.0.1-workspace-authorization-remediation*
*Completed: 2026-09-07*
