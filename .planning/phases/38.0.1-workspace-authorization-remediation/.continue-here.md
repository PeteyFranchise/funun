---
context: phase
phase: 38.0.1-workspace-authorization-remediation
task: 0
total_tasks: 14
status: planned_not_executed
last_updated: 2026-09-06
---

# Phase 38.0.1 — Workspace Authorization Remediation

**Stage: PLANNED, NOTHING EXECUTED.** 14 plans / 6 waves, `gsd-plan-checker` VERIFICATION PASSED.
All artifacts committed. Working tree has UNTRACKED work from a parallel session — see below.

# ⚠ BLOCKING — READ BEFORE ANY EXECUTION

## 1. MIGRATION 189 COLLISION

Two different pieces of work both claim migration **189**:

- `supabase/migrations/189_playbook_bdt_foundation_doctrine.sql` — "The Playbook: BDT Foundation
  Doctrine". **UNTRACKED/uncommitted**, from a parallel session. Also untracked:
  `__tests__/migration-189.test.ts`, `.planning/deliberations/bdt-role-and-console-doctrine.md`,
  `.planning/quick/260906-bdt-foundation-doctrine/`.
- **Phase 38.0.1 plan 05** — consent root, delegation lineage, `relationship_id` NOT NULL,
  evidence confirmation.

**Neither is pushed. Highest applied remote is 187, so nothing is broken yet.**

**Recommended resolution — renumber the BDT migration, not 38.0.1.** 38.0.1 has a four-migration
chain (189→190→191→192) baked into 14 plans, the roadmap and REQUIREMENTS.md, with a joint-push
ordering hazard the planner deliberately solved. The BDT work is one uncommitted migration plus its
test. Moving BDT is a two-file rename; moving 38.0.1 is a phase-wide edit.

**Numbers currently spoken for:** 188 (38.0.1 plan 02) · 189–192 (38.0.1 plans 05/09/10/11) ·
193–194 (Phase 38.0.2) · 195–196 (Phase 38.2). **First genuinely free number is 197.**

**This is the owner's call — the BDT work is not mine and I did not touch it.**

## 2. THE D-56 KILL SWITCH MUST BE OFF

Every one of the 13 requirements deferred to Phase 38.0.2 is safe **only while the kill switch is
off in production**. WSR-07/08 — an admin promoting themselves to owner and removing the real
owner — needs **no grants** to exploit, so the switch is its sole containment.

```
supabase db execute "UPDATE public.workspace_access_config SET enabled = FALSE, disabled_reason = 'Phase 38.0.1 remediation in progress', disabled_at = now() WHERE id;"
```

Plan 01's pre-flight probe reads the config row, so execution confirms it either way.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---|---|---|---|
| Stale-anchor doc edit after a subagent rewrote the file | A subagent (planner, researcher) rewrites a doc; a later orchestrator edit keyed on pre-subagent text silently no-ops, because `str.replace` returns the original on no match. **Happened twice** — Phase 38's roadmap block, then Phase 38.0.1's. | advisory | Re-read any file a subagent may have written BEFORE editing it, assert the anchor exists, and verify with an independent grep — never trust the script's own success message. |
| Text-lock tests mistaken for behavioral proof | This repo's migration tests assert the SQL *string*, not behavior. Phase 38 shipped with 4788 green tests and a live critical custody-takeover path. | blocking | Three requirements here (WSR-06, WSR-25, WSR-03/04) have **named owner-run verification steps** in plan checkpoints. A Jest command must never stand in as sole proof that a trigger fires or a policy denies. |

<current_state>
Phase 38 is shipped and in production (migrations 182–187 live). Its P0 security hotfix is applied.
Phase 38.0.1 is planned and checker-verified; nothing executed. Phase 38.0.2 has a roadmap entry
with its 13 requirements and inherited decisions.
</current_state>

<completed_work>
- Codex adversarial review (22 findings) → 3 P0s fixed and shipped 2026-09-06 (migration 187)
- `/gsd-discuss-phase 38.0.1` → 18 decisions (R-01..R-18), 27 requirements (WSR-01..WSR-27)
- UI-SPEC for WSR-27's consent surface — `gsd-ui-checker` APPROVED
- RESEARCH, PATTERNS, VALIDATION
- `/gsd-plan-phase 38.0.1` → 14 plans / 6 waves, `gsd-plan-checker` **VERIFICATION PASSED**
- Split approved; Phase 38.0.2 roadmap entry created; WSR-01..27 registered in REQUIREMENTS.md
</completed_work>

<remaining_work>
Execution of all 14 plans. Nothing started.

- **Wave 1** — 01 pre-flight production probes [owner-gated] ‖ 03 pure consent + lineage modules
- **Wave 2** — 02 **mig 188** (independent, pushed FIRST ALONE) ‖ 05 **mig 189** [HOLD]
- **Wave 3** — 04 evidence ladder ‖ 06 consent/lineage services + the F6 fix ‖ 09 **mig 190** [HOLD]
- **Wave 4** — 07 Member consent route ‖ 08 evidence propose/confirm ‖ 10 **mig 191** [HOLD]
- **Wave 5** — 11 **mig 192 + JOINT PUSH 189–192** + catalogue + full smoke ‖ 12 copy + settings API ‖ 14 date validation
- **Wave 6** — 13 `/settings/permissions` consent surface
</remaining_work>

<decisions_made>
- **Split approved.** 38.0.1 = consent model + RLS rework + migration 188 + consent surface.
  38.0.2 = transactional integrity + hygiene (13 requirements).
- **WSR-09's second deferral accepted** — the cheap half shipped in the hotfix; the residual
  split-brain window needs the transactional RPC 38.0.2 builds anyway. **Do not defer a third time.**
- **R-17 is a `BEFORE UPDATE` trigger, not a `WITH CHECK` change** — RLS `WITH CHECK` sees only the
  NEW row and cannot compare against OLD.
- **WSR-24 is load-bearing, not hygiene** — a NULL `relationship_id` makes migration 186's helper
  match ANY accepted relationship, defeating WSR-06's custody binding.
- **Migration ordering:** 188 alone first; 189–192 one joint push at plan 11; plans 05/09/10 carry
  HOLD checkpoints, not push authorisations.
</decisions_made>

<blockers>
1. Migration 189 collision (above) — owner decision.
2. Kill switch state (above) — one command.
</blockers>

## Required Reading (in order)

1. `38.0.1-CONTEXT.md` — 18 locked decisions. Do not re-litigate.
2. `38.0.1-SPLIT.md` — the split argument and the binding condition on all 13 deferrals.
3. `38-CONTEXT.md` (parent) — 56 locked decisions this phase changes none of.
4. `.planning/quick/260906-phase38-p0-security-hotfix/SUMMARY.md` — what must NOT be undone.
5. `38.0.1-VALIDATION.md` — the text-lock vs live-Postgres gap.
6. `38-RLS-SMOKE-CHECKLIST.md` — **still unticked from Phase 38**; plan 11 re-runs it.

## Infrastructure State

- Migrations **182–187 live** in production. Highest remote: **187**.
- Working tree: **untracked BDT work present** (see blocker 1). Otherwise clean.
- **8 commits ahead of `origin/main`**, all docs-only, unpushed. Phase 38 itself IS on origin/main
  and therefore in production.

<next_action>
Resolve the migration 189 collision (owner decision — recommend renumbering the BDT migration to
197). Flip the D-56 kill switch off. Then `/gsd-execute-phase 38.0.1` — wave 1 is probes and pure
logic with no migrations, so it is safe to start once the collision is settled.
</next_action>
