---
phase: 38.0.1-workspace-authorization-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md
autonomous: false
migration: none
requirements: [WSR-24, WSR-25]
must_haves:
  truths:
    - "The owner has run six read-only production probes and their literal results are recorded in PREFLIGHT.md."
    - "Migration 192's trigger role list is chosen from an observed `current_user` value, not an assumption."
    - "The NOT NULL constraint in migration 193 is authorised only after a NULL count of 0 is observed on both tables."
  artifacts:
    - ".planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md"
  key_links:
    - "PREFLIGHT.md P1 result -> migration 192's `current_user IN (...)` role list (plan 02)."
    - "PREFLIGHT.md P2 result -> the go/no-go on migration 193's NOT NULL statements (plan 05)."
    - "PREFLIGHT.md P6 result -> the index premise behind migration 194's custody join (plan 09)."
---

<objective>
Gather the six live-Postgres facts that this phase's migrations depend on, before any migration is
authored. RESEARCH's Assumptions A2, A3 and A4 are all "medium risk, verify at planning time"
items whose failure mode is a migration that either breaks a legitimate path (A2) or fails
mid-push against production data (A3). CONTEXT risk 2 states the same rule for R-16 explicitly.

Purpose: this repo has no live-DB test harness. Every behavioural claim about Postgres in this
phase is settled by an owner-run query or it is not settled at all (VALIDATION.md). This plan is
the first of the three named owner-run verifications and it gates two migrations.

Output: `38.0.1-PREFLIGHT.md`, holding the exact SQL for each probe, a results table the owner
fills in, and the two derived go/no-go decisions plans 02 and 05 read.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-CONTEXT.md
@.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-RESEARCH.md
@.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 38.0.1-PREFLIGHT.md with the six probes and an empty results table</name>
  <files>.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md</files>
  <read_first>
    - `.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-RESEARCH.md` — the
      "Assumptions Log" table (A2, A3, A4), the "Runtime State Inventory" table, "Code Examples >
      Verifying the actual Postgres role names in this project", and Pitfall 8's
      `information_schema.role_table_grants` query. These supply the literal SQL for P1, P2, P4.
    - `.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-VALIDATION.md` —
      the "Manual-Only Verifications" table; this document is the artifact that table's
      first, fourth and fifth rows point at.
    - `supabase/migrations/182_workspaces_foundation.sql` lines 236-245 — the existing
      `REVOKE UPDATE, DELETE ON public.workspace_audit_log FROM PUBLIC;` statement and its comment,
      which P4 exists to test the sufficiency of.
    - `supabase/migrations/184_workspace_permissions_grants.sql` `CREATE TABLE public.workspace_grants`
      block — confirms `relationship_id` is nullable today.
    - `supabase/migrations/185_workspace_attachments_custody.sql` `CREATE TABLE
      public.workspace_attachments` block — same, for the second table P2 counts.
  </read_first>
  <action>
    Create the file with YAML frontmatter (`phase: 38.0.1`, `slug:
    workspace-authorization-remediation`, `status: awaiting-owner`, `created: 2026-09-06`) and a
    top section stating that every query below is READ-ONLY, is run by the owner against the
    production database, and that no agent runs any of them.

    Write one numbered section per probe. Each section states the probe id, what it is being run
    for (requirement id and RESEARCH assumption id), the exact SQL as a fenced `sql` block, and
    what each possible result means for the migration that depends on it.

    P1 — Postgres role identity (WSR-25, assumption A2). Query: `SELECT current_user, session_user;`
    Run it TWICE: once through an ordinary authenticated browser session (via PostgREST / the
    session client) and once through `createServiceClient()`. Record both answers separately.
    Interpretation: migration 192's trigger blocks a `user_id` change only for the role names
    observed on the authenticated run; the service-client run must return a DIFFERENT role name,
    otherwise the trigger would block `app/api/vault/custody-transfers/route.ts`'s legitimate
    accept branch and the trigger design must be reconsidered before authoring.

    P2 — NULL security foreign keys (WSR-24, assumption A3, CONTEXT risk 2). Two queries:
    `SELECT count(*) FROM public.workspace_grants WHERE relationship_id IS NULL;` and
    `SELECT count(*) FROM public.workspace_attachments WHERE relationship_id IS NULL;`
    Interpretation: both zero means migration 193 may add the NOT NULL constraints directly.
    Any non-zero count means each offending row needs an explicit backfill-or-delete decision from
    the owner FIRST — plan 05 must not author an unconditional NOT NULL against violating data.

    P3 — Grant-table population (CONTEXT risk 3). Query:
    `SELECT count(*) AS total, count(*) FILTER (WHERE revoked_at IS NULL) AS live FROM public.workspace_grants;`
    Interpretation: the F6 bootstrap deadlock predicts zero. A non-zero count means grants were
    issued by some path this phase has not accounted for, and the R-01 consent-root rework needs a
    data-migration decision before plan 05 lands.

    P4 — Audit-log privilege reality (WSR-26 / S1, deferred to Phase 38.0.2 but captured now
    because it costs one read). Query:
    `SELECT grantee, privilege_type FROM information_schema.role_table_grants WHERE table_schema = 'public' AND table_name = 'workspace_audit_log' AND grantee = 'service_role';`
    Interpretation: any row naming UPDATE or DELETE confirms that migration 182's REVOKE FROM
    PUBLIC did not reach `service_role`, and Phase 38.0.2 must author the explicit revoke plus a
    rejecting trigger. Record the result verbatim so 38.0.2 can plan without re-asking.

    P5 — Containment premise (R-03). Query:
    `SELECT id, enabled, updated_at FROM public.workspace_access_config;`
    Interpretation: this phase's entire deferral argument (see the phase split note in
    38.0.1-SPLIT.md, written by the planner) rests on `enabled` being FALSE in production. If it
    reads TRUE, STOP the phase and flip it before any further work.

    P6 — Custody-join index premise (WSR-06, assumption A4). Query:
    `SELECT indexname, indexdef FROM pg_indexes WHERE schemaname = 'public' AND tablename = 'vault_projects';`
    Interpretation: migration 194 adds a join on `vault_projects (id, user_id)` inside a helper
    called from every RLS policy evaluation on that table. `idx_vault_projects_user_id` and the
    primary key on `id` must both be present; if either is missing in production the plan-09
    EXPLAIN gate becomes a blocker rather than a formality.

    End the file with a "Results" table (columns: Probe, Run at, Result, Derived decision) left
    empty for the owner, and a "Derived decisions" section with two headed placeholders:
    "D-PF-01 — migration 192 trigger role list" and "D-PF-02 — migration 193 NOT NULL go/no-go".

    Follow the repo's planning-document conventions: no emoji, section headings in sentence case,
    every claim traceable to a requirement id or a RESEARCH assumption id.
  </action>
  <verify>
    <automated>test -f .planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md &amp;&amp; grep -c 'P[1-6] —' .planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md` exits 0.
    - `grep -c 'P[1-6] —' .planning/phases/.../38.0.1-PREFLIGHT.md` returns at least 6.
    - `grep -c 'role_table_grants' .planning/phases/.../38.0.1-PREFLIGHT.md` returns at least 1.
    - `grep -c 'relationship_id IS NULL' .planning/phases/.../38.0.1-PREFLIGHT.md` returns at least 2.
    - `grep -c 'D-PF-01' .planning/phases/.../38.0.1-PREFLIGHT.md` returns at least 1 and the same
      for `D-PF-02`.
  </acceptance_criteria>
  <done>PREFLIGHT.md exists, carries all six probes with runnable SQL and an interpretation for each, and has an empty results table plus two named derived-decision placeholders.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Owner runs the six production probes</name>
  <read_first>
    - `.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md` — the file
      authored in Task 1; its six probe sections are the script for this checkpoint.
  </read_first>
  <what-built>`38.0.1-PREFLIGHT.md` with six read-only SQL probes and an empty results table.</what-built>
  <how-to-verify>
    STOP. An agent must never open a connection to the production database or run
    `supabase db push`, `supabase db reset`, or `supabase migration up`. This is the standing
    convention stated verbatim in the headers of migrations 078, 080, 136, 177 and 181-189.

    1. Open `.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md`.
    2. Run P1 twice, as described in its section — once through an authenticated browser session
       and once through the service-role client. Record both `current_user` values.
    3. Run P2, P3, P4, P5 and P6 in the Supabase SQL editor against production.
    4. Paste each literal result into the Results table.
    5. If P5 reports `enabled = true`, flip it to FALSE (R-03) before continuing and note when.
    6. If P2 reports any non-zero count, do not approve — say so, and describe the offending rows,
       so plan 05 can be replanned around them.

    Report back either "probes complete" with the six results, or the specific blocker.
  </how-to-verify>
  <action>
    Halt execution and surface the how-to-verify block to the owner verbatim. Run no database
    command of any kind. Do not proceed to Task 3 until the owner has reported the six results or
    named a blocker. If the owner reports a blocker on P2 or P5, stop the plan and report a
    deviation rather than continuing with assumed values.
  </action>
  <acceptance_criteria>
    - No database connection was opened and no `supabase` CLI command was run by the agent
      (the checkpoint transcript is the evidence).
    - The owner's reply contains a value for each of P1 (both client types), P2 (both tables), P3,
      P4, P5 and P6, or names a specific blocker.
  </acceptance_criteria>
  <resume-signal>Paste the six probe results, or describe the blocker.</resume-signal>
  <done>The owner has reported all six probe results (or a blocker), and no agent touched the database.</done>
</task>

<task type="auto">
  <name>Task 3: Record results and derive D-PF-01 and D-PF-02</name>
  <files>.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md</files>
  <read_first>
    - `.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md` — the file
      authored in Task 1, now to be completed from the owner's reported results.
    - `.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-RESEARCH.md` — Pattern 5's
      worked trigger body, so D-PF-01 records the exact role-list literal plan 02 will use.
  </read_first>
  <action>
    Fill the Results table with the owner's literal values, timestamped, and set `status: complete`
    in the frontmatter.

    Write D-PF-01 as a one-line decision naming the exact set of role names migration 192's trigger
    will refuse a `user_id` change for, plus the role name(s) it must NOT refuse (the observed
    service-client identity). If the observed authenticated role name is not the one RESEARCH
    assumed, say so explicitly and state that plan 02 uses the observed value.

    Write D-PF-02 as GO or NO-GO for migration 193's two NOT NULL statements, citing the two counts.
    On NO-GO, list what plan 05 must do instead (author the constraint behind a documented backfill
    step the owner runs first).

    Add a short "Carried to Phase 38.0.2" subsection holding P4's verbatim result, so WSR-26 can be
    planned there without re-running anything.
  </action>
  <verify>
    <automated>grep -c 'D-PF-01\|D-PF-02' .planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md &amp;&amp; grep -c 'status: complete' .planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'status: complete' .planning/phases/.../38.0.1-PREFLIGHT.md` returns 1.
    - The Results table contains a non-placeholder value for every one of P1-P6 (no row still reads
      the empty-cell placeholder authored in Task 1).
    - D-PF-01 names at least one role literal that the trigger refuses and at least one it exempts.
    - D-PF-02 reads exactly `GO` or `NO-GO` and cites both counts from P2.
    - A `Carried to Phase 38.0.2` heading exists and contains P4's result.
  </acceptance_criteria>
  <done>PREFLIGHT.md is complete: six recorded results, two derived decisions, and P4's data carried forward for WSR-26.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> production database | An agent must never cross it; this plan's whole design is to keep the crossing human-only. |
| planning artifact -> migration author | A wrong recorded value here becomes a wrong role list or a failed push later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.0.1-01-01 | Tampering | An agent running `supabase db push` / `db reset` against production | critical | mitigate | Every probe is read-only and lives in a document; the only execution path is the blocking owner checkpoint in Task 2, whose action text begins with STOP. |
| T-38.0.1-01-02 | Denial of Service | Migration 193's NOT NULL failing mid-push on violating rows | high | mitigate | P2 counts NULLs on both tables and D-PF-02 gates plan 05 on the result. |
| T-38.0.1-01-03 | Elevation of Privilege | Migration 192's trigger written against an assumed role name, silently exempting the class it was meant to block | high | mitigate | P1 observes `current_user` from both client types before the trigger is authored; D-PF-01 records the observed literal. |
| T-38.0.1-01-04 | Information Disclosure | Probe results pasted into a committed planning file could carry row data | low | accept | Every probe returns counts, role names, index definitions or privilege names — no user rows, no PII, no secrets. |
| T-38.0.1-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install occurs in this plan or anywhere in this phase (RESEARCH: "no npm install required"). Nothing to audit. |
</threat_model>

<verification>
- `.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-PREFLIGHT.md` exists with
  `status: complete`.
- Every probe P1-P6 has a recorded result.
- D-PF-01 and D-PF-02 are both written and unambiguous.
- No database connection was opened by an agent at any point (checkpoint transcript is the evidence).
</verification>

<success_criteria>
Plans 02 and 05 can be executed without any remaining assumption about production state: the trigger
role list and the NOT NULL go/no-go are both decided from observed values.
</success_criteria>

<artifacts_this_phase_produces>
| Symbol / artifact | Kind | Location |
|---|---|---|
| `38.0.1-PREFLIGHT.md` | planning artifact | `.planning/phases/38.0.1-workspace-authorization-remediation/` |
| `D-PF-01` | derived decision (trigger role list) | inside PREFLIGHT.md |
| `D-PF-02` | derived decision (NOT NULL go/no-go) | inside PREFLIGHT.md |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/38.0.1-workspace-authorization-remediation/38.0.1-01-SUMMARY.md` when done.
</output>
