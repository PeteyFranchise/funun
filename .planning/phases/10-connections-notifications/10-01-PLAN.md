---
phase: 10-connections-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/connections.test.ts
  - __tests__/notification-triggers.test.ts
  - __tests__/notifications-api.test.ts
  - lib/social/connections.ts
  - lib/social/notifications.ts
  - lib/notifications/index.ts
  - types/index.ts
autonomous: true
requirements: [CONNECT-01, CONNECT-02, NOTIF-01, NOTIF-03]
must_haves:
  truths:
    - "Every phase-owned notification type (new_follower, connection_request, connection_accepted, wall_post, endorsement, release_comment) has a pure builder returning a correctly-shaped createNotification() payload (title/link/data/actor fields)"
    - "Connect request/respond/withdraw payload builders enforce valid state transitions and validate note length (<=200 chars) before any DB write"
    - "createNotification() accepts and persists actor_id/actor_name/actor_avatar_url"
    - "The Notification TypeScript type declares actor_id/actor_name/actor_avatar_url"
  artifacts:
    - "__tests__/connections.test.ts (RED then GREEN)"
    - "__tests__/notification-triggers.test.ts (RED then GREEN)"
    - "__tests__/notifications-api.test.ts (RED then GREEN)"
    - "lib/social/connections.ts"
    - "lib/social/notifications.ts (NOTIFICATION_TYPES catalog + NotificationType + per-type builders)"
  key_links:
    - "lib/notifications/index.ts createNotification() args extended with actorId/actorName/actorAvatarUrl and written into .insert()"
    - "types/index.ts Notification type extended in the SAME task that first passes actor data (tsc excess-property check will otherwise break the build — RESEARCH Pitfall 3)"
---

<objective>
Build the pure, testable foundation for Phase 10: the notification-type catalog + per-type payload builders, the connections request/respond/withdraw payload builders with note validation, the extension of `createNotification()` and the `Notification` type for actor-snapshot columns, and the three Wave-0 Jest scaffolds that machine-check all of the above. Everything here is pure functions and type extensions — no DB, no network, no React — so it unblocks every downstream plan (migration, API routes, UI) without depending on any of them.

Purpose: Per RESEARCH.md, Phase 10 is "almost entirely wiring, not new infrastructure" — but two pure-logic pieces (the notification builders and the connect state-transition builders) are the shared contract every route and component consumes. Getting them tested-first and correct here means the API and UI plans are thin adapters over already-verified logic. The `createNotification()`/`Notification` actor extension (RESEARCH Pitfall 3) MUST land before any call site passes actor data, or the build breaks mid-phase.
Output: `lib/social/notifications.ts`, `lib/social/connections.ts`, extended `lib/notifications/index.ts` + `types/index.ts`, and three green test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-connections-notifications/10-RESEARCH.md
@.planning/phases/10-connections-notifications/10-PATTERNS.md
@.planning/phases/10-connections-notifications/10-VALIDATION.md
@.planning/phases/10-connections-notifications/10-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 RED test scaffolds for connections, notification triggers, and notifications API</name>
  <task_kind_note>
    This is a standard `type="auto"` test-creation task, NOT a TDD task — it authors the RED scaffolds only. It intentionally has no `tdd="true"` attribute and no `<implementation>` element: the modules that turn these RED tests GREEN are the deliverables of Tasks 2-4 in this same plan (`lib/social/notifications.ts` -> Task 2, `lib/social/connections.ts` -> Task 3, the `createNotification()`/`Notification` extension -> Task 4). RED (failing on missing-module import) IS the acceptance for THIS task; GREEN is proven by Tasks 2-4.
  </task_kind_note>
  <files>__tests__/connections.test.ts, __tests__/notification-triggers.test.ts, __tests__/notifications-api.test.ts</files>
  <read_first>
    - __tests__/capability-grant.test.ts (THE canonical mock pattern — copy its jest.mock('@/lib/supabase/server', ...) chain-mock + expect.objectContaining assertions verbatim in style)
    - .planning/phases/10-connections-notifications/10-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — the exact behaviors each test must cover)
    - .planning/phases/10-connections-notifications/10-RESEARCH.md (Validation Architecture section — quick-run command, no-live-DB constraint)
    - jest.config.js (ts-jest preset + @/* alias)
  </read_first>
  <action>
    Create three RED test files under `__tests__/`. They import from modules that do not yet exist in Task 2/3/4, so they MUST fail on first run (RED) — that failing run is the acceptance for this task, then Tasks 2-4 turn them GREEN.

    (a) `__tests__/notification-triggers.test.ts` — imports the six per-type builder functions from `@/lib/social/notifications` (built in Task 2). One `describe` block per phase-owned type: `new_follower`, `connection_request`, `connection_accepted`, `wall_post`, `endorsement`, `release_comment`. Each asserts the builder returns an object with the correct `type`, a `title` templated on the actor name (e.g. `new_follower` produces `${actorName} started following you`), the correct `link` per the UI-SPEC deep-link catalog (e.g. `new_follower` -> `/u/${actorHandle}`, `release_comment` -> `/r/${projectId}#comments`, `wall_post` -> `/u/${ownHandle}#wall`, `endorsement` -> `/u/${ownHandle}#endorsements`, `connection_request`/`connection_accepted` -> `/u/${actorHandle}`), and `actorId`/`actorName`/`actorAvatarUrl` passthrough. Add one test asserting `NOTIFICATION_TYPES` catalog has an entry for every phase-owned type plus the two pre-existing types (`antenna_match`, `application_received`) and that `connection_request` is the only phase-owned entry whose `inlineAction` is truthy. Use `-t "new_follower"` filterable test names (VALIDATION.md references this).

    (b) `__tests__/connections.test.ts` — imports the request/respond/withdraw payload builders from `@/lib/social/connections` (built in Task 3). Assert: buildConnectRequest(requesterId, addresseeId, note) returns `{ requester_id, addressee_id, note }` with note trimmed, and returns note=null for empty/whitespace note; note longer than 200 chars is rejected (builder returns an error result or throws — pick one shape and assert it); requester_id === addressee_id is rejected. Assert the respond builder maps action `accept`->status `accepted`, `decline`->`declined`, `withdraw`->`withdrawn`, and rejects any other action string.

    (c) `__tests__/notifications-api.test.ts` — covers the mark-all-read mutation scoping helper. Since the API route itself is thin (Plan 03), this file tests the pure query-shape helper if one exists, OR (if mark-all-read logic lives inline in the route) assert against a small exported pure helper `buildMarkAllReadFilter(userId)` returning `{ user_id, read: false }` that Plan 03's route uses. Define that helper as living in `lib/social/notifications.ts` (Task 2) so this test is pure. Assert it scopes to the passed userId and read=false only.

    Do NOT put a literal that a later negative-grep gate depends on into these files. These are additive test files with no comment-text-discipline concern.
  </action>
  <verify>
    <automated>npx jest __tests__/connections.test.ts __tests__/notification-triggers.test.ts __tests__/notifications-api.test.ts 2>&1 | grep -Eq 'Cannot find module|is not a function|fail' && echo RED_CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exist under `__tests__/`.
    - Running them fails (RED) because `lib/social/notifications.ts` and `lib/social/connections.ts` do not yet exist — the failure is module-resolution / undefined-import, not a syntax error in the test itself.
    - Test names in notification-triggers include filterable substrings matching each of the 6 phase-owned types.
  </acceptance_criteria>
  <done>Three RED Jest files exist and fail on the expected missing-module/undefined-export grounds (not test-authoring errors).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Notification type catalog + per-type payload builders (GREEN for notification-triggers.test.ts)</name>
  <files>lib/social/notifications.ts</files>
  <read_first>
    - .planning/phases/10-connections-notifications/10-RESEARCH.md (Pattern 3 — the discriminated-union catalog shape is canonical; Pattern 2 — actor-snapshot fields; Open Question #1 — suppress new_follower for trigger-seeded rows, only fire connection_accepted on accept)
    - .planning/phases/10-connections-notifications/10-UI-SPEC.md (section 6 — Notification type catalog: exact icon keys, tile treatment, and per-type link targets; Copywriting Contract — exact notification title strings)
    - __tests__/notification-triggers.test.ts (the RED contract from Task 1 — these builders must satisfy it exactly)
    - lib/social/wall.ts (data-shape module convention; artist_profiles column names artist_name/avatar_url — NOT display_name, a real footgun per RESEARCH Pattern 2)
  </read_first>
  <behavior>
    - Test: NOTIFICATION_TYPES contains new_follower, connection_request, connection_accepted, wall_post, endorsement, release_comment, antenna_match, application_received; each entry has an `icon` string and an `inlineAction` field.
    - Test: only connection_request has a truthy inlineAction ('connection_respond') among phase-owned types.
    - Test: buildNewFollowerNotification({ recipientId, actorId, actorName, actorAvatarUrl, actorHandle }) returns { userId: recipientId, type: 'new_follower', title: `${actorName} started following you`, link: `/u/${actorHandle}`, actorId, actorName, actorAvatarUrl }.
    - Test: buildConnectionRequestNotification(...) -> type 'connection_request', title `${actorName} sent you a connection request`, body = the note (or null), link `/u/${actorHandle}`, data carries connectionId.
    - Test: buildConnectionAcceptedNotification(...) -> type 'connection_accepted', title `${actorName} accepted your connection request`, link `/u/${actorHandle}`.
    - Test: buildWallPostNotification(...) -> type 'wall_post', title `${actorName} posted on your wall`, link `/u/${ownHandle}#wall`.
    - Test: buildEndorsementNotification(...) -> type 'endorsement', title `${actorName} endorsed you`, link `/u/${ownHandle}#endorsements`.
    - Test: buildReleaseCommentNotification(...) -> type 'release_comment', title `${actorName} commented on "${trackTitle}"`, link `/r/${projectId}#comments`.
    - Test: buildMarkAllReadFilter(userId) returns { user_id: userId, read: false }.
  </behavior>
  <action>
    Create `lib/social/notifications.ts`. Export a `NOTIFICATION_TYPES` const object keyed by notification type string, each value `{ icon: string; inlineAction: 'connection_respond' | null }`, matching UI-SPEC section 6's icon names (`user-plus`, `link`, `check`, `message-square`, `star`, `message-circle`, `radio`, `inbox`) and marking ONLY `connection_request` with `inlineAction: 'connection_respond'`. Export `type NotificationType = keyof typeof NOTIFICATION_TYPES`. Keep it `as const`. Keep `type` a plain string at the DB layer (no enum/CHECK) per RESEARCH Pattern 3 — the catalog is the TypeScript-layer safety net, extensible by Phase 11 appending message_request/new_dm.

    Export six pure builder functions (one per phase-owned type) that each return a plain object shaped for `createNotification()`'s args (`{ userId, type, title, body?, link, data?, actorId, actorName, actorAvatarUrl }`). Use the EXACT title strings from UI-SPEC's Copywriting Contract and the EXACT link targets from UI-SPEC section 6. Do NOT emit a `new_follower` builder call path for the trigger-seeded rows — per RESEARCH Open Question #1, connect-accept fires ONLY `connection_accepted` (the DB trigger seeds follows silently, no new_follower notification). Document that suppression choice in a header comment.

    Also export `buildMarkAllReadFilter(userId: string)` returning `{ user_id: userId, read: false }` (consumed by Plan 03's mark-all-read PATCH; kept here so it is pure-unit-testable).
  </action>
  <verify>
    <automated>npx jest __tests__/notification-triggers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/notification-triggers.test.ts` passes (GREEN).
    - `lib/social/notifications.ts` exports NOTIFICATION_TYPES, NotificationType, six buildX builders, and buildMarkAllReadFilter.
    - Every builder's title/link string matches UI-SPEC's Copywriting Contract + section 6 verbatim (asserted by the test).
    - Header comment states that new_follower is suppressed for connect-accept trigger-seeded follows (RESEARCH Open Question #1).
  </acceptance_criteria>
  <done>Notification catalog + six builders + mark-all-read filter exist; notification-triggers.test.ts is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Connect state-transition payload builders (GREEN for connections.test.ts)</name>
  <files>lib/social/connections.ts</files>
  <read_first>
    - __tests__/connections.test.ts (the RED contract from Task 1)
    - supabase/migrations/035_connections_blocks.sql (status enum: pending/accepted/declined/withdrawn; requester_id/addressee_id columns; the two-policy UPDATE split — accept/decline are addressee actions, withdraw is requester action)
    - .planning/phases/10-connections-notifications/10-CONTEXT.md (D-02/D-03/D-04 — Connect->Pending->Connected states, withdraw transition, optional <=200 char note)
    - lib/social/wall.ts (module-shape convention for a lib/social/*.ts service module)
  </read_first>
  <behavior>
    - Test: buildConnectRequest(requesterId, addresseeId, note) returns { requester_id, addressee_id, note } with note trimmed; empty/whitespace note -> note: null.
    - Test: note > 200 chars -> rejected (error result / throw, whichever shape the test asserts).
    - Test: requesterId === addresseeId -> rejected.
    - Test: buildRespondTransition('accept') -> 'accepted'; 'decline' -> 'declined'; 'withdraw' -> 'withdrawn'; unknown -> rejected.
  </behavior>
  <action>
    Create `lib/social/connections.ts`. Export `buildConnectRequest(requesterId, addresseeId, note?)` returning the INSERT payload `{ requester_id, addressee_id, note }` — trim `note`, coerce empty/whitespace to `null`, reject (consistent error shape — match whatever the Task-1 test asserts, e.g. return `{ error }` or throw a descriptive Error per this codebase's error convention) when `note` exceeds 200 chars or when requesterId === addresseeId. Note length is validated here in TS as a friendly pre-check; the Postgres CHECK constraint (Plan 02 migration) is the hard backstop.

    Export `buildRespondTransition(action)` mapping the string action to the target `status` value (`accept`->`accepted`, `decline`->`declined`, `withdraw`->`withdrawn`), rejecting any other action. Do NOT perform any DB I/O in this module — it is pure payload/transition logic that the Plan 03 API route consumes. Keep the RLS enforcement (who may accept vs. withdraw) in the DB policies (migration 035's two-policy split); this module only shapes the intended status value, it does not re-implement authorization.
  </action>
  <verify>
    <automated>npx jest __tests__/connections.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/connections.test.ts` passes (GREEN).
    - `lib/social/connections.ts` exports buildConnectRequest and buildRespondTransition, both pure (no imports of supabase clients).
    - Note validation caps at 200 chars and coerces empty note to null; self-request rejected.
  </acceptance_criteria>
  <done>Connect payload/transition builders exist and are pure; connections.test.ts is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend createNotification() + Notification type for actor-snapshot columns</name>
  <files>lib/notifications/index.ts, types/index.ts</files>
  <read_first>
    - lib/notifications/index.ts (full current file — extend the args type + the .insert() object in place; do NOT wrap it to swallow errors, do NOT change its signature shape beyond adding the three optional args)
    - types/index.ts (Notification type — extend in place; RESEARCH Pitfall 3: tsc excess-property check breaks the build if a call site passes actor fields before the type declares them)
    - .planning/phases/10-connections-notifications/10-PATTERNS.md (the exact extension diff for both files is spelled out under "lib/notifications/index.ts (extend in place)" and "types/index.ts — Notification type")
    - supabase/migrations/036_notifications_dm_reads.sql context (actor_id/actor_name/actor_avatar_url already live on the table since Phase 8 — this task only closes the TS/helper drift)
    - __tests__/notifications-api.test.ts (buildMarkAllReadFilter is already covered by Task 2's module; this task adds no new test file but must keep the full suite green)
  </read_first>
  <behavior>
    - Behavior: createNotification() accepts optional actorId/actorName/actorAvatarUrl and writes them to notifications.actor_id/actor_name/actor_avatar_url (null when omitted).
    - Behavior: the Notification type includes actor_id/actor_name/actor_avatar_url as `string | null`.
    - Behavior: `npx tsc --noEmit` passes and `npm test` is fully green after the extension.
  </behavior>
  <action>
    Extend `createNotification()` in `lib/notifications/index.ts`: add `actorId?: string | null`, `actorName?: string | null`, `actorAvatarUrl?: string | null` to the `args` type, and add `actor_id: args.actorId ?? null`, `actor_name: args.actorName ?? null`, `actor_avatar_url: args.actorAvatarUrl ?? null` to the `.insert({...})` object. Leave the email logic and the plain `{ ok, error }` return untouched — every call site still wraps in its own try/catch (RESEARCH Pattern 1), so the helper is NOT modified to swallow errors internally.

    Extend the `Notification` type in `types/index.ts` with `actor_id: string | null`, `actor_name: string | null`, `actor_avatar_url: string | null`. This MUST be in the same plan (here, before any call site in Plan 03 passes actor data) so the downstream API-route plan's actor-populated createNotification() calls typecheck.

    If a `notifications-api.test.ts` case asserts createNotification writes actor columns via a mocked service client, satisfy it here; otherwise this task's proof is the green full suite + clean tsc.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -Eq 'notifications/index|types/index' && echo TYPE_ERR || npm test</automated>
  </verify>
  <acceptance_criteria>
    - `lib/notifications/index.ts` args type and `.insert()` both carry the three actor fields; grep confirms `actor_id`, `actor_name`, `actor_avatar_url` present in the insert object.
    - `types/index.ts` Notification type declares actor_id/actor_name/actor_avatar_url as string|null.
    - `npx tsc --noEmit` reports no error in these two files.
    - `npm test` is fully green (all three Phase-10 test files + the pre-existing suite).
  </acceptance_criteria>
  <done>createNotification() and the Notification type carry actor-snapshot columns; tsc clean; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API route | Untrusted note text and action strings cross here (validated by builders in this plan + Postgres CHECK in Plan 02) |
| API route → notifications table | Cross-user notification writes use the service-role client (bypasses RLS by design); actor-snapshot fields are attacker-influenceable display text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | connect-request note (client input) | medium | mitigate | `buildConnectRequest()` trims + rejects note > 200 chars in TS (this plan); Postgres `CHECK (char_length(note) <= 200)` is the hard backstop (Plan 02) |
| T-10-02 | Tampering | connect action string (accept/decline/withdraw) | low | mitigate | `buildRespondTransition()` rejects any action not in the known set (this plan); DB two-policy UPDATE split enforces WHO may perform WHICH transition (migration 035, unchanged) |
| T-10-03 | Information Disclosure | notification title/body rendered in panel | low | mitigate | Titles/bodies are rendered as plain React text (JSX auto-escapes) — no `dangerouslySetInnerHTML` anywhere in profile components; builders produce plain strings only |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit: N/A — zero new deps); no install task exists to gate |
</threat_model>

<verification>
- `npm test` fully green (3 new files + existing suite).
- `npx tsc --noEmit` clean for `lib/notifications/index.ts` and `types/index.ts`.
- `lib/social/notifications.ts` and `lib/social/connections.ts` exist and are pure (no supabase client imports).
</verification>

<success_criteria>
- Notification type catalog + six per-type builders + mark-all-read filter shipped and unit-tested.
- Connect request/respond/withdraw builders shipped, note-length validated, unit-tested.
- createNotification() + Notification type carry actor-snapshot columns; build is green.
- All three Wave-0 test files GREEN.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (whole-phase symbol index)

Every new symbol / file / route Phase 10 creates, across all six plans — so downstream phases (esp. Phase 11) know exactly what to extend.

### New files
- `lib/social/notifications.ts` — `NOTIFICATION_TYPES` const catalog, `NotificationType` type, `buildNewFollowerNotification`, `buildConnectionRequestNotification`, `buildConnectionAcceptedNotification`, `buildWallPostNotification`, `buildEndorsementNotification`, `buildReleaseCommentNotification`, `buildMarkAllReadFilter` (Plan 01)
- `lib/social/connections.ts` — `buildConnectRequest`, `buildRespondTransition` (Plan 01)
- `supabase/migrations/050_connections_note.sql` — `connections.note` column, re-created `connections_insert_own` policy (+ no_block), `public.connections_seed_follows()` function, `connections_on_accept` trigger (Plan 02)
- `app/api/connections/route.ts` — `POST` (create request), `PATCH` (accept/decline/withdraw) (Plan 03)
- `app/api/notifications/route.ts` — `GET` (list + unread count), `PATCH` (mark-all-read) (Plan 03)
- `components/nav/NotificationBell.tsx` — default/named `NotificationBell` client component (props `{ userId }`) (Plan 05)
- `components/nav/NotificationPanel.tsx` — `NotificationPanel` client component (props `{ userId, unreadCount, onMarkedAllRead, onRespondedToRequest }`) (Plan 05)
- `components/profile/ConnectButton.tsx` — `ConnectButton` client component (+ inline note-composer subcomponent) (Plan 06)
- `__tests__/connections.test.ts`, `__tests__/notification-triggers.test.ts`, `__tests__/notifications-api.test.ts` (Plan 01)

### Modified files / new exported symbols
- `lib/notifications/index.ts` — `createNotification()` args gain `actorId?/actorName?/actorAvatarUrl?`; insert writes `actor_id/actor_name/actor_avatar_url` (Plan 01)
- `types/index.ts` — `Notification` type gains `actor_id/actor_name/actor_avatar_url: string | null` (Plan 01)
- `components/profile/ProfileView.tsx` — new exported `ConnectState` type + optional `connect?: ConnectState` prop; `#wall` / `#endorsements` section anchors (Plan 06)
- `app/u/[handle]/page.tsx` — derives + passes `connect: ConnectState` (Plan 06)
- `app/(artist)/layout.tsx` — new sticky header row hosting `<NotificationBell />` (Plan 05)
- `app/api/follows/route.ts`, `app/api/wall/route.ts`, `app/api/endorsements/route.ts`, `app/api/release-comments/route.ts` — each gains a best-effort `createNotification()` side effect (Plan 04)

### Notification types introduced (extensible catalog — Phase 11 appends message_request/new_dm)
`new_follower`, `connection_request`, `connection_accepted`, `wall_post`, `endorsement`, `release_comment` (+ pre-existing `antenna_match`, `application_received` kept in catalog)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/10-connections-notifications/10-01-SUMMARY.md` when done.
</output>
