---
phase: 22-buyer-catalogue-light-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [catalogue-browse, audio-player]
must_haves:
  truths:
    - "The buyer Browse Catalogue renders the faithful Claude Design light layout (top nav, tab bar, search, filter pills, results table, tri-state rights badge, License button)."
    - "Filters (Vocals/Mood/Dynamics/Energy/Length/Instruments/Genres/Rights) open, multi-select, narrow results; search matches title+artist; Sort-by reorders; active chips remove/clear-all; live count + 'N filters active' + empty state all work over the fixture."
    - "The sticky audio player (simulated playhead) and the License request modal render and operate over the representative fixture."
    - "The built slice compiles and type-checks clean on this branch."
  artifacts:
    - path: "components/buyer/CatalogBrowserLight.tsx"
      provides: "The in-repo canonical light catalogue (slices 1/2a/2b), Claude Design CSS ported scoped under .fnbl"
      contains: "export function CatalogBrowserLight"
    - path: "lib/deals/catalog-sample.ts"
      provides: "CatalogRow view-model + representative fixture + mapCardsToLightRows"
      contains: "SAMPLE_CATALOG_ROWS"
    - path: "app/(buyer-portal)/buyers/catalog/page.tsx"
      provides: "Server-rendered catalogue page wiring loadCatalogPage -> fixture fallback -> CatalogBrowserLight"
      contains: "CatalogBrowserLight"
  key_links:
    - from: "app/(buyer-portal)/buyers/catalog/page.tsx"
      to: "components/buyer/CatalogBrowserLight.tsx"
      via: "renders <CatalogBrowserLight rows={rows} /> with live-rows-or-fixture fallback"
      pattern: "CatalogBrowserLight"
    - from: "lib/deals/catalog-sample.ts"
      to: "components/buyer/CatalogBrowserLight.tsx"
      via: "CatalogRow type import + SAMPLE_CATALOG_ROWS feeding the page"
      pattern: "SAMPLE_CATALOG_ROWS"
---

<objective>
Formally record that slices 1/2a/2b of the buyer Browse Catalogue are ALREADY BUILT on
branch `feat/buyer-catalogue-light`, so the phase's history is honest and downstream plans
have a verified baseline. This plan writes NO new source code — it verifies the shipped
artifacts exist, compile, and are wired, then records the baseline and the explicit
deferrals in the SUMMARY.

Purpose: Establish an honest baseline. The faithful light catalogue, working
filters/search/sort/chips/count/empty, the sticky player, and the License modal were built
against a representative fixture before this planning pass. The remaining phase scope
(2c wiring, dark toggle, re-theme, live data) is planned in 22-02..22-05.

Output: `22-01-SUMMARY.md` recording the built slices, the fixture it renders, and the
recorded deferrals (real preview audio, logo adoption, live-data/inclusion — all gated or
missing-prerequisite).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-buyer-catalogue-light-ui/22-CONTEXT.md

# The built artifacts this plan records (read to confirm they shipped)
@components/buyer/CatalogBrowserLight.tsx
@lib/deals/catalog-sample.ts
@app/(buyer-portal)/buyers/catalog/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the built light catalogue + fixture artifacts exist and are wired</name>
  <read_first>
    - components/buyer/CatalogBrowserLight.tsx (the built slice — confirm CatalogBrowserLight + CatalogRow + RightsBadge + the sticky player + the License modal are present)
    - lib/deals/catalog-sample.ts (SAMPLE_CATALOG_ROWS fixture + mapCardsToLightRows)
    - app/(buyer-portal)/buyers/catalog/page.tsx (renders CatalogBrowserLight with live-or-fixture fallback)
  </read_first>
  <files>(none — verification only)</files>
  <action>
    Confirm — do NOT modify — that the three built artifacts exist and are wired together.
    This is a read-and-assert task establishing the baseline the rest of the phase builds on.
    Assert: CatalogBrowserLight.tsx exports the `CatalogBrowserLight` component and the
    `CatalogRow` type and renders the tri-state `RightsBadge`, the filter dropdowns, the
    sticky mini player, and the License request modal; catalog-sample.ts exports
    `SAMPLE_CATALOG_ROWS` and `mapCardsToLightRows`; the catalog page imports
    `CatalogBrowserLight` and falls back to the fixture when there are no live rows. If any
    assertion fails, STOP and report — the recorded baseline is wrong and later plans must not
    build on it.
  </action>
  <verify>
    <automated>test "$(grep -c 'export function CatalogBrowserLight' components/buyer/CatalogBrowserLight.tsx)" -ge 1 && test "$(grep -c 'export type CatalogRow' components/buyer/CatalogBrowserLight.tsx)" -ge 1 && test "$(grep -c 'SAMPLE_CATALOG_ROWS' lib/deals/catalog-sample.ts)" -ge 1 && test "$(grep -c 'export function mapCardsToLightRows' lib/deals/catalog-sample.ts)" -ge 1 && test "$(grep -c 'CatalogBrowserLight' 'app/(buyer-portal)/buyers/catalog/page.tsx')" -ge 1</automated>
  </verify>
  <done>All five greps return a positive count; the built catalogue, fixture, and page wiring are confirmed present and unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the built slice type-checks + record baseline and deferrals</name>
  <read_first>
    - .planning/phases/22-buyer-catalogue-light-ui/22-CONTEXT.md ("Already built" + "Deferred Ideas" sections)
    - .planning/deliberations/buyer-catalogue-inclusion-model.md (the gate on live-data wiring)
  </read_first>
  <files>(none — verification + SUMMARY authoring only)</files>
  <action>
    Prove the built slice compiles under the project's strict typecheck (tsconfig has
    noEmit), then record the baseline in the SUMMARY. In the SUMMARY enumerate exactly what
    shipped (slice 1 faithful light layout with real album art from public/buyer-catalogue/;
    slice 2a working browse — filters/search/sort/chips/count/empty over the fixture; slice 2b
    experience — simulated-playhead sticky player + License modal with a demo success toast),
    and enumerate the RECORDED DEFERRALS so they are not silently lost:
    (1) real preview audio — deferred, missing prerequisite (no preview URLs / storage exist yet);
    (2) logo adoption — deferred idea (pick one of the 5 wordmark explorations later);
    (3) Similarity Search / Funūn Playlists tabs — present in the design as tabs, not built;
    (4) live-data wiring + tri-state rights definitions + the inclusion model — GATED on
    `.planning/deliberations/buyer-catalogue-inclusion-model.md` (planned as 22-05, blocked).
    Do NOT write code. Do NOT edit the built files.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0 (built slice type-checks clean); SUMMARY records the shipped slices and the four recorded deferrals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes no source and adds no trust boundary — it records an existing baseline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01-01 | Repudiation | recorded baseline | low | mitigate | Baseline is asserted by automated greps + tsc, not by assertion prose — a false "built" claim fails the verify gate. |
| T-22-01-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; nothing to audit. |
</threat_model>

<verification>
- Task 1 greps confirm the built catalogue, fixture, and page wiring are present.
- `npx tsc --noEmit` confirms the built slice compiles under strict mode.
- No source files are modified by this plan.
</verification>

<success_criteria>
- The three built artifacts (CatalogBrowserLight.tsx, catalog-sample.ts, catalog page) are confirmed present, wired, and type-clean.
- `22-01-SUMMARY.md` records the shipped slices and the four deferrals.
</success_criteria>

## Artifacts this phase produces

This plan produces no source artifacts. It records (does not create) the baseline:
- `components/buyer/CatalogBrowserLight.tsx` — canonical light catalogue (already built)
- `lib/deals/catalog-sample.ts` — CatalogRow view-model + fixture (already built)
- `app/(buyer-portal)/buyers/catalog/page.tsx` — catalogue page (already built)

<output>
Create `.planning/phases/22-buyer-catalogue-light-ui/22-01-SUMMARY.md` when done.
</output>
