---
phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/130_playbook_rbac.sql
  - supabase/migrations/131_plays.sql
  - supabase/migrations/132_selects_engagement.sql
  - __tests__/migration-130.test.ts
  - __tests__/migration-131.test.ts
  - __tests__/migration-132.test.ts
autonomous: false
requirements: [R9, R13]
user_setup:
  - service: supabase
    why: "Migrations 130/131/132 must be applied to prod (project wgfjakfiyeewzfuxkgyo) by the owner — agents never run supabase db push"
    dashboard_config:
      - task: "Run `supabase db push` and confirm 130/131/132 applied via `supabase migration list` (LOCAL=REMOTE)"
        location: "Owner's local terminal against prod project wgfjakfiyeewzfuxkgyo"

must_haves:
  truths:
    - "The data-driven RBAC model exists: playbook_rooms (6 rooms seeded to reproduce Phase 33) + playbook_room_role_grants (per-role view grants, leadership NEVER stored) + playbook_room_leads + playbook_sub_groups + playbook_entries (SOP/Topic, draft→publish) (D-31.2-01/02/03/04/05)"
    - "The IT room seed reproduces Phase 33's exact behavior: it-team is grantable to role 'it' and leadership passes structurally (no leadership grant row) — day-one access is unchanged (Pitfall 6 backward-compat)"
    - "The Plays model exists: plays (one active at a time, enforced by a partial unique index), play_assignments (client_targeted | general_task), play_assignment_completions (unique per assignment+AE) (D-31.2-08/09/10/11)"
    - "The Selects telemetry model exists: selects_track_engagement (raw per-track+viewer delta rows, delta bounded 0<Δ≤15 at the DB) + selects_opens, with an abuse-cap trigger authored IN THIS migration (R13/D-31.2-12)"
    - "Every new table is RLS-enabled with ZERO policies AND REVOKE'd from authenticated+anon; leadership is structurally excluded from playbook_room_role_grants via a CHECK role <> 'leadership'"
    - "Each migration file ends with NOTIFY pgrst reload; migrations 130/131/132 are applied to prod by the owner at the blocking checkpoint before any DB-dependent verification runs"
  artifacts:
    - "supabase/migrations/130_playbook_rbac.sql"
    - "supabase/migrations/131_plays.sql"
    - "supabase/migrations/132_selects_engagement.sql"
    - "__tests__/migration-130.test.ts"
    - "__tests__/migration-131.test.ts"
    - "__tests__/migration-132.test.ts"
  key_links:
    - "playbook_rooms.key ('it-team' etc.) is the roomKey requireRoomAccess() (plan 03) reads; playbook_room_role_grants(room_id, role) is what the access editor (plan 07) writes"
    - "plays partial-unique-active index is the one-active invariant plays.ts (plan 06) relies on; play_assignment_completions(assignment_id, ae_user_id) unique is the idempotent-complete key"
    - "selects_track_engagement raw delta rows are what engagement.ts (plan 02) SUMs at read time to derive the ≥30s qualified-listen (never a stored running total)"
  prohibitions:
    - "role='leadership' MUST NOT be insertable into playbook_room_role_grants (enforced by a DB CHECK); leadership access is structural, never row-data (Pitfall 5)"
    - "No new table may declare a CREATE POLICY — zero-policy + REVOKE is the only posture (staff service-role routes only)"
    - "No new table's columns may appear in any GRANT SELECT ... TO authenticated/anon statement — private by default"
    - "The selects_track_engagement abuse-cap trigger MUST ship in migration 132 itself — it MUST NOT be deferred to a follow-up (telemetry is leadership-facing from day one)"
    - "An executor agent MUST NOT run `supabase db push` — the push is the owner's blocking checkpoint"
---

<objective>
Author the three gating migrations for the entire phase, then hand them to the owner for the human-gated `supabase db push`. Migration 130 creates the data-driven RBAC + Playbook-authoring model (rooms, per-role grants, room-leads, sub-groups, SOP/Topic entries). Migration 131 creates the Plays model (one-active play, assignments, per-AE completions). Migration 132 creates the Selects telemetry model (raw audible-time delta rows + open events) with its abuse-cap trigger authored in the same file.

Purpose: every downstream surface (RBAC gate, authoring, Plays banner, engagement rollups) reads tables that do not exist yet. TypeScript types come from config, not the live DB, so downstream verification is a false-positive until these are applied — hence the blocking push checkpoint. Migrations follow the 128/129 zero-policy + REVOKE doctrine exactly.
Output: three migration SQL files + three text-lock test files, applied to prod.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-CONTEXT.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-RESEARCH.md
@supabase/migrations/128_ae_console_health.sql
@supabase/migrations/117_selects_reaction_cap.sql
@__tests__/migration-128.test.ts
@lib/playbook/nav.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 130 (playbook RBAC + authoring model, seeded to reproduce Phase 33)</name>
  <files>supabase/migrations/130_playbook_rbac.sql</files>
  <read_first>
    - supabase/migrations/128_ae_console_health.sql — the HUMAN-GATED header wording + gen_random_uuid() defaults + the ENABLE ROW LEVEL SECURITY / REVOKE / NOTIFY doctrine to mirror exactly
    - lib/playbook/nav.ts — the 6 Phase-33 room ids/labels/order (company-wide, ar, ae-sales, it-team, tms, leadership) and which is IT (leadership+it today)
    - lib/admin/staff-role.ts — the StaffRole value set the role CHECK constrains, and why 'leadership' must be excluded from grants (Pitfall 5)
    - .planning/phases/31.2-.../31.2-RESEARCH.md § Architecture Patterns + Assumptions Log A1/A4 (recommended table shapes)
  </read_first>
  <action>
    Create `supabase/migrations/130_playbook_rbac.sql`. Repo tip is 129 (128/129 taken) — 130 is next. Open with the HUMAN-GATED header stating an executor agent never runs `supabase db push` (mirror 128's header). Use gen_random_uuid() pk defaults.

    (a) `public.playbook_rooms` (id uuid pk, key text UNIQUE with a CHECK constraining key to lower alphanumerics + hyphen, label text not null, sort_order int not null, sensitive boolean not null default false, coming_soon boolean not null default true, created_at, updated_at). Seed the six Phase-33 rooms in nav.ts order: company-wide, ar, ae-sales, tms as coming_soon=true sensitive=false; it-team as coming_soon=false sensitive=true; leadership as coming_soon=true sensitive=true. (D-31.2-03: it-team + leadership are the sensitive/view-gated rooms.)

    (b) `public.playbook_room_role_grants` (id uuid pk, room_id uuid not null REFERENCES public.playbook_rooms ON DELETE CASCADE, role text not null, created_by uuid REFERENCES auth.users, created_at) with a UNIQUE(room_id, role) and a CHECK that role is one of the non-leadership StaffRole values AND role <> 'leadership' (structural last-admin protection — leadership is never stored, Pitfall 5). Seed grants that reproduce today's behavior + the D-31.2-03 default: grant role 'it' on it-team (leadership passes structurally → net leadership+it, unchanged from Phase 33); grant every non-leadership operational role (ae, bd, anr, legal, tms, accounting, marketing) on company-wide, ar, ae-sales, tms (readable by all Team Members); leave the leadership room with ZERO grant rows (leadership-only, structural). ASSUMPTION (research open-Q1, default chosen): seed all six rooms now — leadership can configure grants before content ships.

    (c) `public.playbook_room_leads` (id uuid pk, room_id uuid not null REFERENCES public.playbook_rooms ON DELETE CASCADE, user_id uuid not null REFERENCES auth.users ON DELETE CASCADE, created_by uuid REFERENCES auth.users, created_at) with UNIQUE(room_id, user_id). This is the per-room room-lead assignment layer (D-31.2-02) — distinct from the role×room view grants.

    (d) `public.playbook_sub_groups` (id uuid pk, room_id uuid not null REFERENCES public.playbook_rooms ON DELETE CASCADE, key text not null, label text not null, sort_order int not null default 0, created_at, updated_at) with UNIQUE(room_id, key). (D-31.2-04 sub-groups.)

    (e) `public.playbook_entries` (id uuid pk, room_id uuid not null REFERENCES public.playbook_rooms ON DELETE CASCADE, sub_group_id uuid REFERENCES public.playbook_sub_groups ON DELETE SET NULL, entry_type text not null CHECK entry_type IN ('sop','topic'), title text not null, content jsonb not null default '{}', draft_content jsonb, status text not null default 'draft_pending' CHECK status IN ('draft_pending','published'), author_id uuid REFERENCES auth.users, approved_by uuid REFERENCES auth.users, created_at, updated_at) + index on (room_id, entry_type, status). This is the SOP/Topic draft→publish store (D-31.2-05/06); content is the published body, draft_content is the pending edit.

    (f) For every new table: ENABLE ROW LEVEL SECURITY, create NO policies, REVOKE SELECT, INSERT, UPDATE, DELETE ... FROM authenticated, anon. End the file with the PostgREST schema-cache reload notification as the last statement. Do not modify migrations 001-129.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/130_playbook_rbac.sql','utf8'); const need=['playbook_rooms','playbook_room_role_grants','playbook_room_leads','playbook_sub_groups','playbook_entries','ENABLE ROW LEVEL SECURITY',\"role <> 'leadership'\",\"NOTIFY pgrst, 'reload schema';\"]; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('MISSING',miss);process.exit(1)} if((s.match(/CREATE POLICY/g)||[]).length){console.error('policy present');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File contains CREATE TABLE for playbook_rooms, playbook_room_role_grants, playbook_room_leads, playbook_sub_groups, playbook_entries
    - playbook_room_role_grants has a CHECK excluding role 'leadership'; six rooms seeded; it-team seeded grantable to 'it'
    - Every new table is RLS-enabled and REVOKE'd from authenticated, anon; no CREATE POLICY appears; file ends with the schema-cache reload
  </acceptance_criteria>
  <done>Migration 130 authored to the 128 doctrine; the RBAC + authoring tables exist, seeded to reproduce Phase 33, leadership structurally excluded from grants.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 131 (plays) + migration 132 (selects engagement, cap trigger inline)</name>
  <files>supabase/migrations/131_plays.sql, supabase/migrations/132_selects_engagement.sql</files>
  <read_first>
    - supabase/migrations/128_ae_console_health.sql — doctrine + header (again, for both files)
    - supabase/migrations/117_selects_reaction_cap.sql — the SECURITY DEFINER cap-trigger shape to mirror for the engagement table
    - supabase/migrations/111_selects.sql — selects / selects_tracks table + column names the engagement FKs reference
    - .planning/phases/31.2-.../31.2-RESEARCH.md § Code Examples (cap trigger) + Common Pitfalls #2 (per-delta ceiling)
  </read_first>
  <action>
    Author BOTH files with the HUMAN-GATED header + gen_random_uuid() defaults + the zero-policy/REVOKE/NOTIFY doctrine.

    `supabase/migrations/131_plays.sql`:
    (a) `public.plays` (id uuid pk, title text not null, note text, status text not null default 'active' CHECK status IN ('active','retired'), published_by uuid REFERENCES auth.users, published_at, created_at). Add a PARTIAL UNIQUE INDEX guaranteeing AT MOST ONE row with status='active' (the one-active-play invariant, D-31.2-08) — a unique index on a constant expression WHERE status='active'.
    (b) `public.play_assignments` (id uuid pk, play_id uuid not null REFERENCES public.plays ON DELETE CASCADE, kind text not null CHECK kind IN ('client_targeted','general_task'), title text not null, note text, health_band text, pipeline_stage_key text, link_url text, attachment_url text, content jsonb, sort_order int not null default 0, created_at). client_targeted rows use health_band/pipeline_stage_key (D-31.2-09a); general_task rows use title/note/link_url/attachment_url/content — the content-carrying, posting-ready-but-posting-deferred directive (D-31.2-10).
    (c) `public.play_assignment_completions` (id uuid pk, assignment_id uuid not null REFERENCES public.play_assignments ON DELETE CASCADE, ae_user_id uuid not null REFERENCES auth.users, note text, completed_at timestamptz not null default now()) with UNIQUE(assignment_id, ae_user_id) — the idempotent per-(assignment, AE) completion key (D-31.2-11).
    (d) RLS + zero-policy + REVOKE on all three; end with the schema-cache reload.

    `supabase/migrations/132_selects_engagement.sql`:
    (e) `public.selects_track_engagement` (id uuid pk, selects_id uuid not null REFERENCES public.selects ON DELETE CASCADE, selects_track_id uuid not null REFERENCES public.selects_tracks ON DELETE CASCADE, viewer_key text, delta_seconds numeric(7,2) not null CHECK delta_seconds > 0 AND delta_seconds <= 15, event text not null CHECK event IN ('heartbeat','pause','ended','unload'), created_at timestamptz not null default now()) + index on (selects_track_id, viewer_key). These are RAW delta rows (never a stored running total) — the ≤15 DB ceiling is the abuse defense mirroring the per-heartbeat clamp (Pitfall 2).
    (f) `public.selects_opens` (id uuid pk, selects_id uuid not null REFERENCES public.selects ON DELETE CASCADE, viewer_key text, opened_at timestamptz not null default now()) + index on (selects_id, viewer_key). Open/view events (D-31.2-12).
    (g) A SECURITY DEFINER cap trigger on selects_track_engagement mirroring enforce_selects_reaction_cap (migration 117): count existing rows for the same (selects_track_id, viewer_key) and RAISE EXCEPTION with ERRCODE 'check_violation' above a sane ceiling (e.g. 5000 rows). This MUST be in THIS migration, not deferred.
    (h) RLS + zero-policy + REVOKE on both tables; end with the schema-cache reload.

    Do not modify migrations 001-130.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const a=fs.readFileSync('supabase/migrations/131_plays.sql','utf8'); const b=fs.readFileSync('supabase/migrations/132_selects_engagement.sql','utf8'); const na=['plays','play_assignments','play_assignment_completions','UNIQUE',\"status = 'active'\",\"NOTIFY pgrst, 'reload schema';\"]; const nb=['selects_track_engagement','selects_opens','delta_seconds','SECURITY DEFINER','RAISE EXCEPTION',\"NOTIFY pgrst, 'reload schema';\"]; const m=[...na.filter(t=>!a.includes(t)).map(t=>'131:'+t),...nb.filter(t=>!b.includes(t)).map(t=>'132:'+t)]; if((a.match(/CREATE POLICY/g)||[]).length||(b.match(/CREATE POLICY/g)||[]).length){console.error('policy present');process.exit(1)} if(m.length){console.error('MISSING',m);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 131 creates plays (with a partial-unique active index), play_assignments (kind check client_targeted|general_task), play_assignment_completions (unique assignment+AE)
    - 132 creates selects_track_engagement (delta 0<Δ≤15 check) + selects_opens + a SECURITY DEFINER cap trigger in the same file
    - Both files are RLS-enabled + REVOKE'd, no CREATE POLICY, end with the schema-cache reload
  </acceptance_criteria>
  <done>Migrations 131 + 132 authored to doctrine; Plays + telemetry models exist, the engagement cap trigger ships inline with its table.</done>
</task>

<task type="auto">
  <name>Task 3: Text-lock migrations 130/131/132</name>
  <files>__tests__/migration-130.test.ts, __tests__/migration-131.test.ts, __tests__/migration-132.test.ts</files>
  <read_first>
    - __tests__/migration-128.test.ts — the exact readFileSync + toContain / not.toMatch mirroring convention (HUMAN-GATED assertion, NOTIFY-at-end assertion, negated GRANT assertions live only in the test file)
    - the three SQL files authored in Tasks 1-2
  </read_first>
  <action>
    Create one text-lock test per migration mirroring `__tests__/migration-128.test.ts` (readFileSync + assertions). Assert, per file:
    - 130: each of the five tables is created; the role<>'leadership' CHECK is present on the grants table; the six rooms + the it-team 'it' grant are seeded; every new table is RLS-enabled AND REVOKE'd from authenticated, anon (negated-grep the authenticated GRANT pattern for each — these not.toMatch assertions live only in this test file, which no acceptance gate greps); no CREATE POLICY targets a new table; the HUMAN-GATED provenance line is present; the file ends with the schema-cache reload. Include the Pitfall-6 seed-parity assertion: it-team is grantable to 'it' (day-one behavior == Phase 33's leadership+it).
    - 131: plays / play_assignments / play_assignment_completions created; the partial-unique active-play index is present; the completions UNIQUE(assignment_id, ae_user_id) is present; RLS + REVOKE + no policy + NOTIFY end.
    - 132: selects_track_engagement (with the delta ≤15 CHECK) + selects_opens created; the cap trigger function + trigger are present IN THIS FILE; RLS + REVOKE + no policy + NOTIFY end.
    Reference forbidden literal tokens by concept where an assertion would otherwise embed one.
  </action>
  <verify>
    <automated>npm test -- __tests__/migration-130.test.ts __tests__/migration-131.test.ts __tests__/migration-132.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three migration test suites pass
    - Each suite fails if its migration ever gains a policy, drops the RLS/REVOKE posture, grants a new column to authenticated, or moves the NOTIFY off the last line
    - migration-130 fails if the it-team seed parity or the role<>'leadership' CHECK regresses; migration-132 fails if the cap trigger is removed
  </acceptance_criteria>
  <done>All three migrations are text-locked; a regression in RLS/REVOKE/seed/cap posture fails jest.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: [BLOCKING] Owner applies migrations 130/131/132 to prod (supabase db push)</name>
  <files>supabase/migrations/130_playbook_rbac.sql, supabase/migrations/131_plays.sql, supabase/migrations/132_selects_engagement.sql</files>
  <action>
    STOP — owner action required; an executor agent MUST NOT run `supabase db push`. This checkpoint runs AFTER the three migrations + tests are authored and BEFORE any DB-dependent verification in plans 03-10. The owner reviews the three migrations, runs `supabase db push` against prod (project wgfjakfiyeewzfuxkgyo), then confirms `supabase migration list` shows LOCAL=REMOTE through 132 with no errors on any table create, the plays partial-unique index, or the engagement cap trigger. Types come from config, not the live DB, so downstream verification is a false-positive until this is done.
  </action>
  <what-built>Migrations 130/131/132 (ten staff-only tables + the plays one-active index + the engagement cap trigger) are authored and text-locked by jest. Nothing is applied to the database — this repo never runs `supabase db push` from an agent.</what-built>
  <how-to-verify>
    1. Review the three migration files.
    2. Run `supabase db push` against prod (project wgfjakfiyeewzfuxkgyo).
    3. Run `supabase migration list` and confirm LOCAL=REMOTE through 132.
    4. Confirm no errors on any table create, the plays partial-unique active index, or the engagement cap trigger.
  </how-to-verify>
  <verify>Owner confirms `supabase migration list` shows migrations 130/131/132 live (LOCAL=REMOTE).</verify>
  <done>Migrations 130/131/132 are applied to prod; all ten new tables exist live.</done>
  <resume-signal>Type "130-132 applied" once `supabase migration list` shows all three live, or describe any push error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated non-staff client → Postgres (PostgREST) | A non-staff caller could attempt to read/write the new staff-only tables directly |
| public /selects/[token] viewer → selects_track_engagement | An anonymous viewer's telemetry write path lands rows in a new table |
| leadership → playbook_room_role_grants | The access matrix must never be able to lock leadership out |
| owner → prod DB | The human-gated `supabase db push` is the only path that mutates live schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.2-01 | Information Disclosure (V4/V13) | all 10 new tables | high | mitigate | RLS-enabled + zero-policy + REVOKE from authenticated/anon on every new table; text-locked by the migration tests |
| T-31.2-02 | Denial of Service (staff lockout) | playbook_room_role_grants | high | mitigate | DB CHECK role <> 'leadership' makes leadership structurally unstorable — last-admin lockout is impossible at the data layer (Pitfall 5) |
| T-31.2-03 | Tampering | selects_track_engagement | high | mitigate | delta_seconds CHECK 0<Δ≤15 (per-heartbeat ceiling) + a SECURITY DEFINER per-(track,viewer) cap trigger authored in migration 132 itself, mirroring migration 117 |
| T-31.2-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A — no new dependencies) |
</threat_model>

<verification>
- `npm test -- __tests__/migration-130.test.ts __tests__/migration-131.test.ts __tests__/migration-132.test.ts` green.
- `supabase migration list` shows LOCAL=REMOTE through 132 (owner-confirmed at the checkpoint).
</verification>

<success_criteria>
- All ten new tables exist under the zero-policy + REVOKE doctrine; leadership is structurally excluded from grants; the engagement cap trigger ships with its table; the migrations are applied to prod.
</success_criteria>

<output>
Create `.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-01-SUMMARY.md` when done.
</output>
