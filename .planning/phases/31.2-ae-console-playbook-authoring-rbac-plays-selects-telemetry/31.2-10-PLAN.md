---
phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry
plan: 10
type: execute
wave: 4
depends_on: ["31.2-02", "31.2-05", "31.2-09"]
files_modified:
  - app/api/admin/client-partners/selects/[id]/engagement/route.ts
  - app/api/admin/client-partners/selects/[id]/engagement/route.test.ts
  - app/api/admin/client-partners/engagement-rollup/route.ts
  - components/admin/EngagementPanel.tsx
  - app/(admin)/admin/selects/[id]/page.tsx
  - components/admin/ClientPartnersRoom.tsx
  - app/(admin)/admin/client-partners/page.tsx
autonomous: true
requirements: [R13]

must_haves:
  truths:
    - "An AE per-Selects engagement readout (EngagementPanel) shows per-track plays / audible-seconds / qualified-listens (≥30s) / distinct replays + a Selects-level summary, for a Selects the AE owns — staff-only, own-Selects scoped (D-31.2-13/14, R13 acceptance)"
    - "A leadership aggregate rollup across the team's book shows who's getting listens / which Selects land, reusing the batched loadWholeBookWithCoverage-style read (D-31.2-13)"
    - "All aggregation is computed on read by SUMming the raw selects_track_engagement delta rows via plan-02's engagement.ts — no stored totals; engagement is NEVER shown to the client (D-31.2-14)"
    - "The leadership rollup extends the existing Client Partners leadership tower rather than a new page — ASSUMPTION (research open-Q3 default)"
  artifacts:
    - "app/api/admin/client-partners/selects/[id]/engagement/route.ts"
    - "app/api/admin/client-partners/engagement-rollup/route.ts"
    - "components/admin/EngagementPanel.tsx"
  key_links:
    - "aggregatePerTrack / per-Selects rollup come from plan-02 engagement.ts; the raw rows come from plan-05's write path (migration-132 tables)"
    - "the AE route is own-Selects scoped (canAccessOrgContacts precedent); the leadership rollup is leadership-only (verifyAdmin) across the whole book"
    - "EngagementPanel mounts on app/(admin)/admin/selects/[id]/page.tsx (the AE's Selects detail view); the leadership rollup extends ClientPartnersRoom (the tower)"
  prohibitions:
    - "MUST NOT expose engagement data on any client-facing surface — staff-only (D-31.2-14); the AE route is staff-gated, the leadership route leadership-gated"
    - "MUST NOT store or read a pre-computed running total — SUM the raw delta rows at read time (D-06 compute-on-read)"
    - "MUST NOT let an AE read another AE's Selects engagement — own-Selects/own-book scoping (404 not 403, 31.1 precedent)"
---

<objective>
Surface R13's telemetry in the two places D-31.2-13 requires: an AE per-Selects engagement readout (per-track plays / audible-seconds / qualified-listens / replays + a Selects summary) on the AE's Selects detail view, and a leadership aggregate rollup across the team's book on the existing Client Partners tower. All aggregation SUMs the raw delta rows at read time via plan-02's engagement.ts; nothing is ever shown to the client.

Purpose: this is the payoff of the capture path — the coaching signal ("they replayed track 3 — lead with it") reaches the AE and leadership. Own-Selects / own-book scoping + staff-only gating uphold D-31.2-14. This runs last (wave 4) because it extends the same tower files plan 09 modifies.
Output: the AE per-Selects route + EngagementPanel (mounted), the leadership rollup route, the tower extension.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-CONTEXT.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-RESEARCH.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-PATTERNS.md
@lib/selects/engagement.ts
@app/(admin)/admin/selects/[id]/page.tsx
@components/admin/ClientPartnersRoom.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AE per-Selects engagement route + EngagementPanel + mount on the Selects detail view</name>
  <files>app/api/admin/client-partners/selects/[id]/engagement/route.ts, app/api/admin/client-partners/selects/[id]/engagement/route.test.ts, components/admin/EngagementPanel.tsx, app/(admin)/admin/selects/[id]/page.tsx</files>
  <read_first>
    - lib/selects/engagement.ts (plan 02) — aggregatePerTrack + the per-Selects rollup to call over the raw rows
    - app/api/admin/client-partners/[orgId]/game-plan/route.ts — the staff GET + own-book scoping (canAccessOrgContacts, 404-not-403) convention to mirror for own-Selects scoping
    - app/(admin)/admin/selects/[id]/page.tsx — the AE's Selects detail RSC where EngagementPanel mounts
    - .planning/phases/31.2-.../31.2-PATTERNS.md § EngagementPanel (No Analog — the per-track readout shape)
  </read_first>
  <action>
    Create `app/api/admin/client-partners/selects/[id]/engagement/route.ts` — a staff GET that resolves the Selects, verifies the caller owns it (own-Selects scoping via the AE→org book, 404 not 403 for an out-of-book Selects), reads the raw selects_track_engagement + selects_opens rows and returns per-track { plays, audibleSeconds, qualifiedListens, replays } + a Selects-level summary computed by engagement.ts's aggregation (SUM at read time, never a stored total). Create `components/admin/EngagementPanel.tsx` ('use client', data + string-action props only) rendering the per-track readout + summary. Mount EngagementPanel on `app/(admin)/admin/selects/[id]/page.tsx` (the AE's Selects detail view), feeding it the route's data. Add `route.test.ts`: the aggregation matches engagement.ts (≥30s → one qualified listen, replay distinct); an out-of-book Selects → 404; the response is staff-shaped (no client exposure).
  </action>
  <verify>
    <automated>npm test -- app/api/admin/client-partners/selects/[id]/engagement/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The AE readout shows per-track plays/audible-seconds/qualified-listens/replays + a Selects summary; ≥30s counts one qualified listen; replays distinct
    - An out-of-book Selects → 404; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The AE sees how each client engaged with the Selects they sent — own-Selects-scoped, staff-only.</done>
</task>

<task type="auto">
  <name>Task 2: Leadership engagement-rollup route (aggregate across the team's book)</name>
  <files>app/api/admin/client-partners/engagement-rollup/route.ts</files>
  <read_first>
    - lib/client-partners/signals.ts — loadWholeBookWithCoverage's batched no-N+1 leadership read pattern to mirror
    - lib/selects/engagement.ts (plan 02) — the aggregation to fold across many Selects
    - lib/admin/gate.ts — verifyAdmin (leadership-only)
  </read_first>
  <action>
    Create `app/api/admin/client-partners/engagement-rollup/route.ts` — a leadership-only (verifyAdmin) GET that aggregates engagement across the team's book (who's getting listens, which Selects land), reusing the batched loadWholeBookWithCoverage-style read (no N+1) and engagement.ts's read-time aggregation. Return a per-AE / per-Selects rollup shape the tower can render. Machine-verify (mirroring 31.1's gate test) that this leadership cross-book read is never reachable by a non-leadership role.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Leadership-only; aggregates across the team's book via a batched read (no N+1); non-leadership → 403
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Leadership can see, in aggregate, which Selects land and who's getting listens.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the leadership Client Partners tower with the engagement rollup</name>
  <files>components/admin/ClientPartnersRoom.tsx, app/(admin)/admin/client-partners/page.tsx</files>
  <read_first>
    - components/admin/ClientPartnersRoom.tsx — the existing leadership tower (All tab / By-AE) where the rollup section mounts (after plan 09's Today's Play banner — this plan runs in wave 4 to avoid a same-file conflict)
    - app/(admin)/admin/client-partners/page.tsx — loadClientPartnersRoomData, where to fetch the engagement rollup for leadership
    - .planning/phases/31.2-.../31.2-RESEARCH.md Open Question #3 (extend the tower — the chosen default)
  </read_first>
  <action>
    Extend `components/admin/ClientPartnersRoom.tsx`'s leadership view with an engagement rollup section (per-AE / which-Selects-land), fed from the plan-2/2 route via app/(admin)/admin/client-partners/page.tsx's leadership-only load (research open-Q3 default: extend the existing tower, not a new page). Guard the rollup load to leadership only (hide-not-filter, 31.1 discipline — never call the whole-book engagement read for an AE/BD). Preserve plan 09's Today's Play banner + the existing My/All tabs untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The leadership tower shows the engagement rollup; the whole-book engagement read is never invoked for a non-leadership role (machine-verifiable, 31.1 hide-not-filter precedent)
    - Plan 09's banner + existing tabs preserved; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The leadership tower surfaces the team-wide engagement rollup — D-31.2-13's second surface is live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AE → another AE's Selects engagement | Per-Selects readout must be own-Selects scoped |
| non-leadership staff → whole-book engagement rollup | The aggregate is leadership-only |
| engagement data → any client-facing surface | Telemetry is staff-only, never client-visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.2-26 | Information Disclosure | AE per-Selects route | high | mitigate | Own-Selects/own-book scoping (404 not 403, 31.1 precedent); staff-gated |
| T-31.2-27 | Information Disclosure | leadership rollup | high | mitigate | verifyAdmin leadership-only; hide-not-filter so the whole-book read never runs for AE/BD (31.1 discipline) |
| T-31.2-28 | Information Disclosure | engagement → client | high | mitigate | Both routes staff-gated; the public player contract test (plan 05) locks the client render path out of the engagement tables (D-31.2-14) |
| T-31.2-SC | Tampering (supply chain) | npm installs | low | accept | No new packages this phase |
</threat_model>

<verification>
- `npm test -- app/api/admin/client-partners/selects/[id]/engagement/route.test.ts` green.
- `npx tsc --noEmit` clean; full suite + `npm run build` clean at the phase gate.
- Manual UAT (VALIDATION): play a Selects preview with pause/seek; confirm the AE readout's audible seconds match real listening (not idle), and the leadership rollup reflects it.
</verification>

<success_criteria>
- R13's engagement signal surfaces to the AE (per-Selects) and to leadership (aggregate), computed on read from raw deltas, staff-only — D-31.2-12/13/14 delivered end to end.
</success_criteria>

<output>
Create `.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-10-SUMMARY.md` when done.
</output>
