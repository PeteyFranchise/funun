---
phase: 31.2
slug: ae-console-playbook-authoring-rbac-plays-selects-telemetry
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-24
---

# Phase 31.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | jest 29.x (+ `npx tsc --noEmit` typecheck) |
| **Config file** | `package.json` / jest config (existing — no Wave 0 install) |
| **Quick run command** | `npm test -- <path-or-pattern>` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~10–15 seconds (full suite ~2600+ tests) |

---

## Sampling Rate

- **After every task commit:** Run `npm test -- <touched files>` (+ `npx tsc --noEmit` for TS changes)
- **After every plan wave:** Run `npm test` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green + `tsc --noEmit` clean
- **Max feedback latency:** ~15 seconds

---

## Per-Task Verification Map

> Populated by the planner — each task's `<verify><automated>` command maps here. Every behavior-adding task must carry an automated jest/tsc check; pure DB-migration tasks are text-locked by a `__tests__/migration-<n>.test.ts` (mirrors migrations 128/129).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 31.2-01-01 | 01 | 1 | R9 | T-31.2-* | staff/role-gated writes; zero-policy tables | unit | `npm test -- __tests__/migration-*.test.ts` | ⬜ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements — jest + tsc are already set up and load-bearing (2600+ tests). No new framework/install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Audible-time accuracy against a real listen session | R13 | Real playback timing (play/pause/seek) can't be fully asserted in jsdom | Play a Selects preview, pause/seek, confirm accumulated listen-seconds match wall-clock listening (not idle time) in the AE per-Selects view |
| Data-driven access editor changes access live | R9 | Requires a leadership session + role toggles against the applied schema | As leadership, grant/revoke a role on a room in the matrix; confirm a member of that role gains/loses the room without a deploy; confirm last-admin/leadership can't be locked out |
| Migration(s) applied to prod | R9/R13 | Human-gated `supabase db push` (agents never push) | Owner runs `supabase db push`; `supabase migration list` shows LOCAL=REMOTE through the new migration(s) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
