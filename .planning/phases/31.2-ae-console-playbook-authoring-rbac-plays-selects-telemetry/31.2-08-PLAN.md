---
phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry
plan: 08
type: execute
wave: 3
depends_on: ["31.2-04"]
files_modified:
  - components/playbook/EntryEditor.tsx
  - app/(admin)/admin/playbook/[room]/page.tsx
  - lib/client-partners/game-plan.ts
  - lib/client-partners/game-plan.test.ts
  - app/api/admin/client-partners/[orgId]/game-plan/route.ts
autonomous: true
requirements: [R9]

must_haves:
  truths:
    - "A room content page lists that room's published SOP/Topic entries and, for members with room access, mounts EntryEditor to draft/edit — publishing directly for leadership/room-leads, submitting-for-approval otherwise (D-31.2-05/06), gated by requireRoomAccessPage"
    - "The Game-Plan topic picker shows the 31.1 SEEDED_GAME_PLAN_TOPICS AND the authored published Playbook Topics TOGETHER — authored topics augment, never replace, the seeded starters (D-31.2-07)"
    - "The seeded topic constant is untouched; authored topics are a SEPARATE source merged at read time in the game-plan GET route (never migrated into the DB, never merged inside buildDefaultGamePlanTopics) (Pitfall 4)"
  artifacts:
    - "components/playbook/EntryEditor.tsx"
    - "app/(admin)/admin/playbook/[room]/page.tsx"
  key_links:
    - "EntryEditor POSTs/PATCHes the plan-04 entries routes; the room page guards via requireRoomAccessPage (plan 03)"
    - "the game-plan GET route (31.1) merges SEEDED_GAME_PLAN_TOPICS + published playbook_entries(entry_type='topic') at read time — feeding the existing GamePlanPanel picker"
  prohibitions:
    - "MUST NOT migrate or re-derive SEEDED_GAME_PLAN_TOPICS from the DB — it stays a code constant; authored topics are concatenated at read time (Pitfall 4)"
    - "MUST NOT merge authored topics inside buildDefaultGamePlanTopics() — that function's contract is 'no game_plans row exists yet' seeding, unrelated to the picker menu (Pitfall 4)"
    - "MUST NOT break 31.1's existing game-plan tests (covered-summary, 0-of-N edge) — extend, do not rewrite"
    - "MUST NOT let a non-room-member author — the room page + EntryEditor writes go through requireRoomAccess (Pitfall 6)"
---

<objective>
Ship the Playbook authoring UI (a room content page + the SOP/Topic EntryEditor over plan 04's routes) and wire dynamic Game-Plan topic sourcing so authored Playbook Topics augment — never replace — the 31.1 seeded starters in the call Game-Plan picker.

Purpose: this makes R9's "editable SOP library" and D-31.2-05/07 real for the user: leadership/ops author content in-app, and the AE's Game-Plan picker grows over time from The Playbook while always keeping the seeded defaults. The augment-not-replace merge (Pitfall 4) protects 31.1's tested behavior.
Output: EntryEditor.tsx, a room content page, the game-plan read-time merge, extended game-plan tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-CONTEXT.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-RESEARCH.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-PATTERNS.md
@lib/client-partners/game-plan.ts
@app/api/admin/client-partners/[orgId]/game-plan/route.ts
@app/(admin)/tips/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: EntryEditor.tsx + room content page</name>
  <files>components/playbook/EntryEditor.tsx, app/(admin)/admin/playbook/[room]/page.tsx</files>
  <read_first>
    - app/(admin)/tips/page.tsx — the Tips authoring UI to mirror (draft/publish affordances, list render)
    - app/api/admin/playbook/entries/route.ts + [id]/route.ts (plan 04) — the POST/PATCH contract EntryEditor calls
    - lib/playbook/rooms.ts (plan 03) — requireRoomAccessPage + loadRooms for the room resolution
    - components/playbook/Rail2.tsx — the room key/labels + chrome style to match
  </read_first>
  <action>
    Create `app/(admin)/admin/playbook/[room]/page.tsx` — an RSC that resolves the room by key, guards via requireRoomAccessPage(roomKey) (fail-closed self-guard, never rely on the nav), lists that room's published SOP/Topic entries (+ the caller's own drafts), and mounts EntryEditor for the room. Create `components/playbook/EntryEditor.tsx` ('use client', data + string-action props only) mirroring the Tips authoring UI: a member drafts/edits an entry (title + entry_type sop|topic + content) and Save either publishes directly (leadership/room-lead — the server decides) or submits a draft for approval; an approver sees approve/reject affordances on pending drafts. Reflect the server-derived status the routes return; router.refresh() after each write. Do not re-implement the publish-gate decision client-side — it is the route's server call (plan 04).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The room page guards via requireRoomAccessPage and lists entries; EntryEditor drafts/edits/approves via the plan-04 routes
    - No client-side publish-gate decision; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Leadership/ops author SOPs + Topics in-app; regular members draft-for-approval — R9's authoring UI is live.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dynamic Game-Plan topic sourcing — authored Topics augment seeded (read-time merge)</name>
  <files>lib/client-partners/game-plan.ts, lib/client-partners/game-plan.test.ts, app/api/admin/client-partners/[orgId]/game-plan/route.ts</files>
  <read_first>
    - lib/client-partners/game-plan.ts — SEEDED_GAME_PLAN_TOPICS + buildDefaultGamePlanTopics + the "31.2 plugs in here" doc comment (do NOT touch buildDefaultGamePlanTopics's contract)
    - lib/client-partners/game-plan.test.ts — the existing covered-summary / 0-of-N edge tests to extend (not rewrite)
    - app/api/admin/client-partners/[orgId]/game-plan/route.ts — the GET handler where the picker option set is assembled
    - .planning/phases/31.2-.../31.2-RESEARCH.md Pitfall 4 (merge two sources, never migrate the seed)
  </read_first>
  <behavior>
    - buildPickerTopics(seeded, authored) concatenates the seeded starter topics AND the authored published Topics into one option list; neither replaces the other; an empty authored list yields exactly the seeded starters (31.1 behavior preserved)
    - Authored topics carry a distinct source label (e.g. 'playbook:<id>') so the picker can attribute them; seeded stay source 'seeded'
  </behavior>
  <action>
    Add a pure `buildPickerTopics(seededTopics, authoredTopics)` merge helper to `lib/client-partners/game-plan.ts` (concatenate; authored augment seeded; distinct source label for authored) — leave SEEDED_GAME_PLAN_TOPICS and buildDefaultGamePlanTopics UNTOUCHED (Pitfall 4). Add a thin loader for published playbook_entries where entry_type='topic'. In the game-plan GET route, when building the picker's option set (NOT the saved-plan seeding path), merge the seeded starters with the authored Topics via buildPickerTopics and include them in the response the picker reads. Extend `lib/client-partners/game-plan.test.ts` (RED first): empty authored → seeded only (existing 31.1 behavior intact); non-empty authored → both appear together, seeded never dropped. Then implement (GREEN).
  </action>
  <verify>
    <automated>npm test -- lib/client-partners/game-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - buildPickerTopics returns seeded-only when authored is empty, and both together otherwise (seeded never replaced)
    - The existing 31.1 covered-summary / 0-of-N tests still pass; the route merges at read time, not in buildDefaultGamePlanTopics
  </acceptance_criteria>
  <done>The Game-Plan picker grows from authored Playbook Topics while always keeping the seeded starters — D-31.2-07 satisfied without breaking 31.1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-room-member → authoring | Authoring a room's entries must be room-scoped, server-checked |
| authored topics → the Game-Plan picker | Merging must not corrupt or replace the tested seeded defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.2-22 | Elevation of Privilege | EntryEditor / room page | high | mitigate | requireRoomAccessPage on the page + server-derived publish gate (plan 04) — the client never decides publish authority |
| T-31.2-23 | Tampering (behavior regression) | game-plan merge | medium | mitigate | buildPickerTopics keeps seeded untouched; 31.1 tests extended not rewritten (Pitfall 4) |
| T-31.2-SC | Tampering (supply chain) | npm installs | low | accept | No new packages — reuse react-markdown/markdownComponents if long-form bodies render |
</threat_model>

<verification>
- `npm test -- lib/client-partners/game-plan.test.ts` green (incl. preserved 31.1 tests).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- SOPs + Topics author in-app through the room content page, and authored Topics augment the seeded Game-Plan starters at read time — R9's authoring + D-31.2-05/07 are delivered without regressing 31.1.
</success_criteria>

<output>
Create `.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-08-SUMMARY.md` when done.
</output>
