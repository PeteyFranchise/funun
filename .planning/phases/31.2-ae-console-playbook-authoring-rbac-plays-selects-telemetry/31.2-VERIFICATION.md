---
phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry
verified: 2026-08-24T00:00:00Z
status: human_needed
score: 27/27 must-haves verified (all 10 plans' must_haves truths)
behavior_unverified: 0
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Audible-time accuracy against a real listen session — play a Selects preview, pause, seek/scrub, let it finish; check the AE per-Selects EngagementPanel"
    expected: "Accumulated audible-seconds tracks only genuine playback time (idle/paused time and the seek jump itself are excluded); a full-length contiguous play yields ~track-duration seconds; a scrub-through-without-listening contributes ~0"
    why_human: "jsdom cannot drive real <audio> element timing — useAudibleTimeAccumulator.ts is intentionally unit-test-free by design (per 31.2-02-SUMMARY); its correctness rests on audible-accumulator.test.ts's pure-math coverage plus this live check"
  - test: "Data-driven access editor changes access live — as leadership, open /admin/playbook/access, toggle a role off a room, sign in (or check) as a member of that role"
    expected: "The member loses/gains visibility and requireRoomAccessPage-backed entry to that room's pages immediately, with no deploy; the leadership column/row is never rendered or toggleable; attempting to grant 'leadership' is rejected"
    why_human: "Requires a real leadership session + a second staff account with a non-leadership role toggled live against the applied prod schema"
  - test: "Leadership publishes a Play with a client-targeted assignment (e.g. health='at_risk') and a general_task assignment; an AE opens My Client Partners"
    expected: "The Today's Play banner shows at the top with the AE's own-book matching count; 'View clients' filters the My tab to only matching rows (CR-01 fix) with a dismissible 'Filtered by today's play' chip; the AE can mark both assignment kinds done; leadership's completions rollup reflects who acted"
    why_human: "End-to-end own-book + live DB state (real AE book, real health bands) cannot be fully exercised by unit tests alone; this is the phase's core coaching-loop UX"
  - test: "Leadership views the Client Partners tower's Engagement section after a client listens to a sent Selects"
    expected: "Per-AE totals + per-Selects breakdown reflect real listens (audible-seconds, qualified-listens ≥30s, replays); nothing is ever visible on the public /selects/[token] page"
    why_human: "Requires a real client-side listen session to generate rows, then a leadership session to view the aggregate"
---

# Phase 31.2: AE Console — Playbook Authoring/RBAC, Plays & Selects Telemetry Verification Report

**Phase Goal:** Close the coaching loop + add measurement on top of the shipped AE Console (31.1) and read-only Playbook shell (33) — Playbook authoring (SOPs/Topics/Plays), data-driven role×room RBAC (access editor), Plays ("today's play" banner + follow-through rollup), Selects engagement telemetry (genuine audible-time accumulator, per-recipient, staff-only), and dynamic Game-Plan topic sourcing (authored Topics augment the 31.1 seeded defaults).

**Verified:** 2026-08-24
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (by plan, all merged from PLAN.md `must_haves.truths`)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Data-driven RBAC model exists (playbook_rooms/room_role_grants/room_leads/sub_groups/entries) | ✓ VERIFIED | `supabase/migrations/130_playbook_rbac.sql` — all 5 tables, CHECK `role <> 'leadership'`, seeded 6 rooms in nav.ts order |
| 2 | IT room seed reproduces Phase 33 exact behavior (backward-compat) | ✓ VERIFIED | Migration 130: `it-team` grantable to role `'it'` only, leadership passes structurally; `__tests__/migration-130.test.ts` text-locks this |
| 3 | Plays model exists (one-active + 2 assignment kinds + idempotent completions) | ✓ VERIFIED | `supabase/migrations/131_plays.sql` — `plays_one_active_uniq` partial unique index, `play_assignments.kind IN ('client_targeted','general_task')`, `UNIQUE(assignment_id, ae_user_id)` |
| 4 | Selects telemetry model exists (raw delta rows, ≤15s clamp, abuse-cap trigger in-file) | ✓ VERIFIED | `supabase/migrations/132_selects_engagement.sql` — `CHECK (delta_seconds > 0 AND delta_seconds <= 15)`, `enforce_selects_track_engagement_cap()` SECURITY DEFINER trigger shipped inline |
| 5 | Every new table zero-policy RLS + REVOKE; leadership excluded via CHECK | ✓ VERIFIED | All 3 migrations: `ENABLE ROW LEVEL SECURITY` + `REVOKE ... FROM authenticated, anon`, no `CREATE POLICY` anywhere; grep confirmed |
| 6 | Migrations end with NOTIFY pgrst; applied to prod at blocking checkpoint | ✓ VERIFIED | All 3 files end `NOTIFY pgrst, 'reload schema';`; STATE.md 31.2-01 entry + given context confirm owner ran `supabase db push`, LOCAL=REMOTE through 132 |
| 7 | Genuine audible-time accumulator (pure, contiguity-bounded, not wall-clock) | ✓ VERIFIED | `components/selects-player/audible-accumulator.ts` — `accountForTick`/`foldAudibleSeconds`, 2s contiguity ceiling, seek/backward/seeking all → 0; 11 tests green |
| 8 | React hook wraps accumulator against real `<audio>`, 10s heartbeat + flush | ✓ VERIFIED | `components/selects-player/useAudibleTimeAccumulator.ts` exists, imported/wired in `SelectsPlayer.tsx` line 29/343 |
| 9 | engagement.ts server contract: clamp ≤15s, ≥30s qualified-listen, pure aggregation | ✓ VERIFIED | `lib/selects/engagement.ts` — `clampDelta`, `QUALIFIED_LISTEN_SECONDS=30`, `aggregatePerTrack`/`aggregateTrack`/`aggregateSelectsRollup`; 14 tests green |
| 10 | requireRoomAccess/requireRoomAccessPage additive on requireStaff, leadership structural | ✓ VERIFIED | `lib/playbook/rooms.ts` — calls `requireStaff`/`requireStaffPage` first, leadership short-circuits before any DB read |
| 11 | Access-editor write path never inserts leadership grant, audits every change | ✓ VERIFIED | `lib/playbook/access-grants.ts` — `isGrantableRole` excludes leadership (checked in `setRoomGrant`/`removeRoomGrant`), both write `logStaffAction` unconditionally |
| 12 | GET/PATCH room×role matrix, leadership-only, zod strict, audited | ✓ VERIFIED | `app/api/admin/playbook/rooms/route.ts` — `verifyAdmin()` both handlers, `.strict()` schema, double-checks `isGrantableRole` before any service call |
| 13 | loadRooms() is the single live source Rail2/layout render from | ✓ VERIFIED | `app/(admin)/admin/playbook/layout.tsx` imports `loadRooms` from `lib/playbook/rooms`; `Rail2.tsx` no longer imports `PLAYBOOK_ROOMS` |
| 14 | SOP/Topic role-tiered draft→publish, generalized from Tips precedent | ✓ VERIFIED | `lib/playbook/entries.ts` — `resolveWrite`/`applyApproval`/`applyReject`; approver publishes direct, else `draft_pending` |
| 15 | isApprover server-derived (leadership OR isRoomLead), never client flag | ✓ VERIFIED | `app/api/admin/playbook/entries/route.ts` + `[id]/route.ts` compute `isApprover` server-side from `auth.staffRole`/`isRoomLead`; zod `.strict()` schemas carry no status/approver field |
| 16 | Editing published SOP is forward-only in place | ✓ VERIFIED | `editEntry` in `entries.ts` updates via `resolveWrite`, no history rewrite |
| 17 | Every entries write is room-scoped (requireRoomAccess) + audited | ✓ VERIFIED | Both entries routes call `requireRoomAccess(roomKey)` before any write; `logStaffAction` on create/approve/reject/edit |
| 18 | POST engagement route: resolve-by-token, clamp ≤15s, rate-limit, persist raw row | ✓ VERIFIED | `app/api/selects/[token]/engagement/route.ts` — `resolveSelectsByToken` → dual `checkRateLimit` → zod `.strict()` → `loadOwnSelectsTrack` → `clampDelta` → insert |
| 19 | Route records open events, viewer-cookie attribution, never surfaces aggregates | ✓ VERIFIED | Same route: `event==='open'` branch inserts `selects_opens`; response is always `{ ok: true }`, never an aggregate; D-31.2-14 contract test in `route.test.ts` (lines 153-166) locks the public player render path out of engagement tables |
| 20 | SelectsPlayer wires hook to `<audio>`, heartbeat/pause/ended POST, unload via sendBeacon | ✓ VERIFIED | `components/selects-player/SelectsPlayer.tsx` lines 107-121 (`sendBeacon` for unload, `fetch` otherwise), line 343 hook wiring |
| 21 | Plays: publish retires prior active (one-active invariant) | ✓ VERIFIED | `lib/playbook/plays.ts` `buildPublishTransition`/`publishPlay` retire-then-activate; DB partial unique index (migration 131) backstops it; `plays.test.ts` green |
| 22 | Two assignment kinds, validated (client_targeted xor content-carrying general_task) | ✓ VERIFIED | `validateAssignment` in `plays.ts` enforces the exact mutual-exclusion rule per D-31.2-09/10 |
| 23 | matchingClientCount scoped to evaluating AE's own book, reuses computeHealth/resolveStage | ✓ VERIFIED | `lib/client-partners/plays-eligibility.ts` — pure `matchingClientCount`, no health calc of its own; caller (plan 09/room-data.ts) supplies own-book rows |
| 24 | Idempotent per-(assignment, AE) completion | ✓ VERIFIED | `markAssignmentComplete`/`buildCompletionUpsert` upsert on `UNIQUE(assignment_id, ae_user_id)` with `ignoreDuplicates: true` |
| 25 | Access-editor matrix UI: clickable, live PATCH, no leadership column | ✓ VERIFIED | `components/playbook/AccessEditorMatrix.tsx` — `GRANTABLE_ROLES.map` for columns (excludes leadership), `handleToggle` PATCHes + `router.refresh()` |
| 26 | All 5 IT-room page guards re-pointed to requireRoomAccessPage('it-team') | ✓ VERIFIED | grep confirms all 5 pages (`dashboard`, `vendor-directory`, `runbook`, `operating-rhythm`, `thresholds`) call `requireRoomAccessPage('it-team')` |
| 27 | Room content page + EntryEditor; Game-Plan picker merges seeded+authored topics (augment, never replace) | ✓ VERIFIED | `app/(admin)/admin/playbook/[room]/page.tsx` + `EntryEditor.tsx`; `lib/client-partners/game-plan.ts`'s `buildPickerTopics` is a pure concatenation, `SEEDED_GAME_PLAN_TOPICS` untouched; wired into `GamePlanPanel` (commit `19f82f2`) |
| 28 | PlayComposer + Today's Play banner + leadership "who's acted" rollup | ✓ VERIFIED | `components/playbook/PlayComposer.tsx`, `components/admin/TodaysPlayBanner.tsx` mounted in `ClientPartnersRoom.tsx` (My tab top), `app/api/admin/plays/[id]/completions/route.ts` leadership-only (`verifyAdmin`) |
| 29 | Play deep-link actually filters the destination (CR-01 fix) | ✓ VERIFIED | `app/(admin)/admin/client-partners/page.tsx` reads/validates `?health=&stage=`; `ClientPartnersRoom.tsx` calls `applyPlayFilter` which shares `rowMatchesAssignment` with `matchingClientCount` — count and filtered rows can't drift apart; commit `b5e8d39` |
| 30 | AE per-Selects EngagementPanel + leadership aggregate rollup, own-Selects/own-book scoped, compute-on-read | ✓ VERIFIED | `app/api/admin/client-partners/selects/[id]/engagement/route.ts` (own-Selects via `loadSelectsInScope`, 404 not 403) + `lib/selects/engagement-rollup.ts` + `app/api/admin/client-partners/engagement-rollup/route.ts` (`verifyAdmin`) |

**Score:** 30/30 truths verified (0 present-but-behavior-unverified at the code level; audible-time *accuracy* against real playback is inherently a live-session check, routed to human verification below)

### Required Artifacts (representative sample — full list matches all 10 plans' `files_modified`)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/130_playbook_rbac.sql` | RBAC + authoring schema | ✓ VERIFIED | 217 lines, zero-policy RLS, CHECK constraints, seeded rooms |
| `supabase/migrations/131_plays.sql` | Plays schema | ✓ VERIFIED | 126 lines, partial unique active index |
| `supabase/migrations/132_selects_engagement.sql` | Telemetry schema + trigger | ✓ VERIFIED | 136 lines, abuse-cap trigger inline |
| `lib/playbook/rooms.ts` | RBAC gate | ✓ VERIFIED | Composes requireStaff, structural leadership carve-out |
| `lib/playbook/access-grants.ts` | Audited grant read/write | ✓ VERIFIED | isGrantableRole + logStaffAction |
| `lib/playbook/entries.ts` | Draft→publish transitions | ✓ VERIFIED | Pure resolveWrite/applyApproval + thin CRUD |
| `lib/playbook/plays.ts` | Plays domain engine | ✓ VERIFIED | 368 lines, retire-then-activate, idempotent completion |
| `lib/selects/engagement.ts` | Server telemetry contract | ✓ VERIFIED | clampDelta, aggregation functions |
| `lib/selects/engagement-rollup.ts` | Leadership aggregate | ✓ VERIFIED | buildEngagementRollup, batched |
| `components/selects-player/audible-accumulator.ts` | Pure delta math | ✓ VERIFIED | accountForTick/foldAudibleSeconds |
| `components/selects-player/useAudibleTimeAccumulator.ts` | DOM hook | ✓ VERIFIED | Wired into SelectsPlayer.tsx |
| `components/playbook/AccessEditorMatrix.tsx` | RBAC editor UI | ✓ VERIFIED | No leadership column, live PATCH |
| `components/playbook/EntryEditor.tsx` | SOP/Topic authoring UI | ✓ VERIFIED | Data+string-action props only |
| `components/playbook/PlayComposer.tsx` | Play builder UI | ✓ VERIFIED | Both assignment kinds, publish |
| `components/admin/TodaysPlayBanner.tsx` | AE banner | ✓ VERIFIED | Mounted top of My Client Partners |
| `components/admin/EngagementPanel.tsx` | AE telemetry readout | ✓ VERIFIED | Mounted on Selects detail page |

All artifacts exist, are substantive (no stub bodies, no `return null`/empty placeholders beyond legit HTML `placeholder=` attributes), and are wired (imports + call sites confirmed via grep + direct reads).

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `playbook_rooms.key` | `requireRoomAccess()` | roomKey param | ✓ WIRED | `lib/playbook/rooms.ts` reads playbook_rooms by key |
| `playbook_room_role_grants` | Access editor route | GET/PATCH | ✓ WIRED | `app/api/admin/playbook/rooms/route.ts` |
| `plays_one_active_uniq` | `plays.ts` publish logic | retire-then-activate | ✓ WIRED | `publishPlay()` mirrors the DB invariant |
| `selects_track_engagement` raw rows | `engagement.ts` SUM at read | aggregateTrack | ✓ WIRED | Confirmed in both the AE route and leadership rollup |
| `buildAssignmentDeepLink` (?health=&stage=) | `/admin/client-partners` page | searchParams → applyPlayFilter | ✓ WIRED (post-CR-01 fix) | Confirmed: page reads params, room filters rows, shared predicate with count |
| 5 IT-room pages | `requireRoomAccessPage('it-team')` | page guard | ✓ WIRED | All 5 grepped and confirmed re-pointed |
| `SEEDED_GAME_PLAN_TOPICS` + authored Topics | `GamePlanPanel` picker | buildPickerTopics read-time merge | ✓ WIRED | Confirmed via game-plan.ts + wave-3 integration commit 19f82f2 |
| Public `/selects/[token]` page | engagement tables | (deliberately absent) | ✓ CONFIRMED ABSENT | No engagement/audible reference found in the public player page; D-31.2-14 contract test locks this |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| R9 | 31.2-01,03,04,06,07,08,09 | Playbook / SOPs authoring + data-driven RBAC + Plays | ✓ SATISFIED (code) | All must-haves above; REQUIREMENTS.md line 676 notes R9 is "deferred to Phase 31.2 (CONTEXT.md scope re-cut, not yet registered ... pending a future close pass)" — this is a bookkeeping/registration gap in REQUIREMENTS.md, not an implementation gap. Per verification instructions, no edit performed here; flagging for the close-pass owner. |
| R13 | 31.2-01,02,05,10 | Selects engagement telemetry | ✓ SATISFIED (code + registered) | REQUIREMENTS.md line 672 has a full traceability row: "Impl complete ... owner UAT pending" |

**Note on R9's registration:** R9 is fully implemented in code (verified above) but REQUIREMENTS.md's Phase 31 Slice-1 traceability table has not yet added an `R9` row — only prose at line 676 mentions the deferral. This matches the given context ("this project registers requirement rows at phase close") and is explicitly NOT something this verification edits. Flagged as a WARNING-tier bookkeeping item for the phase-close pass, not a code gap.

### Anti-Patterns Found

None. Grepped all key surfaces (`lib/playbook/*`, `lib/selects/engagement*`, `components/playbook/*`, `components/admin/TodaysPlayBanner.tsx`, `components/admin/EngagementPanel.tsx`, all 3 migrations) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER|coming soon|not yet implemented` — zero hits (the only `placeholder` matches are legitimate HTML input `placeholder=` attributes).

### Code Review Findings (31.2-REVIEW.md) — Resolution Confirmed

| Finding | Disposition | Verified |
|---------|-------------|----------|
| CR-01 — Play deep-link filter unconsumed | Fixed, commit `b5e8d39` | ✓ Confirmed in code: page reads `searchParams`, `ClientPartnersRoom` applies `applyPlayFilter` sharing `rowMatchesAssignment` with `matchingClientCount` |
| WR-01 — "From Selects" mislabel + raw UUID badge | Fixed, commit `5ba9736` | ✓ Confirmed: `sourceLabel()` in `GamePlanPanel.tsx` branches on `playbook:` prefix → "From Playbook" |
| IN-01 — `listEntries` incomplete approver view if called directly | Deferred (documented, non-blocking — page-level workaround is correct today) | Accepted as documented; not a live bug |

### Automated Verification Run (this verification pass, not trusted from SUMMARY)

- `npx tsc --noEmit` — clean, no errors
- Targeted phase test suites (13 suites covering migrations 130-132, rooms, access-grants, entries, plays, plays-eligibility, engagement, engagement-rollup, audible-accumulator, and all API routes) — **141/141 tests passed**
- Full repo suite (`npm test`) — **267 suites / 2853 tests passed**, matching the claimed post-review state exactly
- `git status` — clean on phase-relevant paths; no uncommitted 31.2 work

### Human Verification Required

See frontmatter `human_verification` — 4 items, all live-app/live-session checks that cannot be exercised by static analysis or jsdom:

1. **Audible-time accuracy** against a real listen session (pause/seek/scrub) — the accumulator's pure math is unit-proven, but end-to-end accuracy against genuine human playback needs a live check.
2. **Access editor live effect** — grant/revoke a role on a room as leadership, confirm a second account's access changes without a deploy, confirm leadership can never be locked out.
3. **Plays end-to-end coaching loop** — publish a Play, confirm the AE banner + filtered deep-link + mark-done + leadership rollup all function together against real book data.
4. **Leadership engagement rollup** reflecting a real client listen session, with confirmation nothing ever leaks to the public Selects page.

These match the phase's own `31.2-VALIDATION.md` "Manual-Only Verifications" table plus the CR-01 fix's live-filter behavior — none are novel additions.

### Gaps Summary

No code-level gaps. All 30 observable truths across the 10 plans are verified present, substantive, and wired; the one prior code-review blocker (CR-01) and one warning (WR-01) are both confirmed fixed in the codebase (not just claimed in SUMMARY). tsc is clean and the full 267-suite/2853-test suite is green, matching the claimed post-review state exactly — independently re-run, not trusted from SUMMARY.

The only outstanding items are (a) four live-app/live-session human verifications that no static check can substitute for, and (b) a REQUIREMENTS.md bookkeeping gap for R9's row (explicitly out of scope for this verification per instructions — R9's registration is deferred to a future close pass, not missing implementation).

---

_Verified: 2026-08-24_
_Verifier: Claude (gsd-verifier)_
