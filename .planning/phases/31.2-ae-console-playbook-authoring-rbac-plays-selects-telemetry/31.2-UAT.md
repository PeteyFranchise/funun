---
status: blocked
phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry
source: [31.2-VERIFICATION.md]
started: 2026-08-25T03:30:00Z
updated: 2026-08-25T03:30:00Z
---

## Current Test

number: 1
name: Audible-time accuracy against a real listen session
expected: |
  Play a Selects preview, pause, seek/scrub, let it finish, then check the AE
  per-Selects EngagementPanel. Accumulated audible-seconds tracks only genuine
  playback time (idle/paused time and the seek jump itself are excluded); a
  full-length contiguous play yields ~track-duration seconds; a
  scrub-through-without-listening contributes ~0.
awaiting: seed data (fake client accounts + songs) — owner may have Codex seed test-only data

## Tests

### 1. Audible-time accuracy against a real listen session
expected: Play a Selects preview, pause, seek/scrub, let it finish; the AE per-Selects EngagementPanel shows audible-seconds for genuine playback only (paused/idle time and seek jumps excluded); a full contiguous play ≈ track duration; a scrub-through ≈ 0.
result: [blocked - no client accounts or songs seeded in the system yet]

### 2. Data-driven access editor changes access live
expected: As leadership, open /admin/playbook/access and toggle a role off/on a room; a member of that role loses/gains room visibility AND requireRoomAccessPage-backed entry immediately, no deploy. The leadership column/row is never rendered or toggleable; granting 'leadership' is rejected.
result: [blocked - no client accounts or songs seeded in the system yet]

### 3. Plays coaching loop end-to-end
expected: Leadership publishes a Play with a client-targeted assignment (e.g. health=at_risk) and a general/social task. The AE's My Client Partners shows the Today's Play banner with their own-book matching count; "View clients" filters the My tab to only matching rows with a dismissible "Filtered by today's play" chip; the AE marks both assignment kinds done; leadership's completions rollup reflects who acted.
result: [blocked - no client accounts or songs seeded in the system yet]

### 4. Leadership engagement rollup reflects a real listen
expected: After a client listens to a sent Selects, the Client Partners tower's Engagement section shows per-AE totals + per-Selects breakdown (audible-seconds, qualified-listens ≥30s, replays). Nothing engagement-related is ever visible on the public /selects/[token] page.
result: [blocked - no client accounts or songs seeded in the system yet]

## Summary

total: 4
passed: 0
issues: 0
pending: 0
skipped: 0
blocked: 4

## Gaps
