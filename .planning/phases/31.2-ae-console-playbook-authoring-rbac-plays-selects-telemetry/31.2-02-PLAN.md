---
phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/selects/engagement.ts
  - lib/selects/engagement.test.ts
  - components/selects-player/audible-accumulator.ts
  - components/selects-player/audible-accumulator.test.ts
  - components/selects-player/useAudibleTimeAccumulator.ts
autonomous: true
requirements: [R13]

must_haves:
  truths:
    - "A genuine audible-time accumulator exists as a PURE, separately-testable function that counts only small forward contiguous currentTime deltas (0<Δ≤2s) and discards seek/scrub jumps — NOT a wall-clock timer (D-31.2-12, phase watch-out)"
    - "The React hook useAudibleTimeAccumulator wraps the pure accumulator against a real <audio> element, flushing pending audible seconds on a 10s heartbeat + on pause/ended/unload (sendBeacon-ready)"
    - "engagement.ts owns the server-side contract: a per-delta clamp (≤15s), the ≥30s qualified-listen threshold, and pure read-time aggregation that SUMs raw delta rows into per-track audible-seconds + qualified-listen + distinct replay counts (R13 acceptance: past-threshold audible == exactly one qualified listen; scrub != listen; replay distinct)"
  artifacts:
    - "lib/selects/engagement.ts"
    - "components/selects-player/audible-accumulator.ts"
    - "components/selects-player/useAudibleTimeAccumulator.ts"
  key_links:
    - "audible-accumulator.ts's pure delta math is imported by useAudibleTimeAccumulator.ts (plan 05 wires the hook into SelectsPlayer)"
    - "engagement.ts's clampDelta + QUALIFIED_LISTEN_SECONDS are consumed by the engagement POST route (plan 05) and its aggregation by the read rollups (plan 10)"
  prohibitions:
    - "MUST NOT accumulate listen time from setInterval/Date.now() wall-clock deltas — only from consecutive audio.currentTime readings (Pitfall 1, the phase's explicit watch-out)"
    - "MUST NOT accept a raw client delta without clamping to the ≤15s per-flush ceiling (Pitfall 2)"
    - "MUST NOT store a running total — aggregation is computed on read by summing raw delta rows (D-06 compute-on-read doctrine)"
---

<objective>
Build R13's pure, testable core with no DB and no UI coupling: the genuine audible-time accumulator (the phase's watch-out capability) and the server-side engagement contract (clamp, ≥30s qualified-listen threshold, read-time aggregation). Extract the accumulator's delta math into a pure function so the "scrub does not count / replay is distinct" acceptance bar is unit-tested directly, exactly as computeHealth() is pure and tested apart from its caller.

Purpose: de-risk the hardest correctness surface early and in parallel with the migrations, so plan 05 (the write route + player wiring) and plan 10 (read rollups) only assemble already-tested primitives.
Output: engagement.ts (server contract + aggregation), audible-accumulator.ts (pure math), useAudibleTimeAccumulator.ts (hook), + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-CONTEXT.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-RESEARCH.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-PATTERNS.md
@lib/client-partners/health.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure audible-time delta math + tests</name>
  <files>components/selects-player/audible-accumulator.ts, components/selects-player/audible-accumulator.test.ts</files>
  <read_first>
    - .planning/phases/31.2-.../31.2-PATTERNS.md § useAudibleTimeAccumulator (the reference delta logic to extract as pure)
    - .planning/phases/31.2-.../31.2-RESEARCH.md Pattern 4 + Pitfall 1 (the genuine-accumulator guard)
    - lib/client-partners/health.ts — the "pure function, separately tested from its RSC caller" precedent shape/style (no-semicolon, named exports)
  </read_first>
  <behavior>
    - accountForTick(lastTime, currentTime, seeking) returns the audible delta to add: 0 when lastTime is null, 0 when seeking, 0 when the delta is negative, 0 when the delta exceeds the contiguity ceiling (2s), else the delta itself
    - A forward 0.25s tick during normal playback returns ~0.25; a 40s forward jump (scrub) returns 0; a backward jump (rewind) returns 0; a tick while seeking returns 0
    - A helper that folds a sequence of (time, seeking) samples into a total accumulated audible-seconds, resetting contiguity after any seek — a scrub-through-without-play sample sequence totals ~0
  </behavior>
  <action>
    Create `components/selects-player/audible-accumulator.ts` exporting the pure delta math extracted from RESEARCH Pattern 4: a named function that, given the previous currentTime, the current currentTime, and whether the element is seeking, returns the audible delta to accumulate — only a small forward contiguous delta (strictly >0 and ≤ a 2s contiguity ceiling) counts; a negative delta, a large jump (seek/scrub), or the seeking flag yields 0. Export the 2s ceiling as a named constant. Also export a pure fold helper that reduces an ordered array of playback samples into total audible seconds (breaking the contiguity chain on each seek) — this is what the test drives to prove "scrub without audible playback totals ~0". No React, no DOM, no I/O. Match health.ts's style (no semicolons, named exports, 2-space indent). Write the test first (RED), then the implementation (GREEN).
  </action>
  <verify>
    <automated>npm test -- components/selects-player/audible-accumulator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The suite proves: normal-playback ticks accumulate; a seek/scrub jump accumulates 0; a backward jump accumulates 0; the seeking flag suppresses accumulation
    - The fold helper totals ~0 for a pure scrub-through sample sequence and ~track-duration for a contiguous full play
  </acceptance_criteria>
  <done>The genuine-accumulator math is pure and unit-proven against the scrub-excluded acceptance bar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server engagement contract + read-time aggregation + tests</name>
  <files>lib/selects/engagement.ts, lib/selects/engagement.test.ts</files>
  <read_first>
    - .planning/phases/31.2-.../31.2-RESEARCH.md § Validation Architecture (R13 test map: qualified listen, scrub-excluded, replay distinct) + Pitfall 2 (per-delta ceiling)
    - .planning/phases/31.1-.../31-SPEC.md acceptance for R13 (default ≥30s qualified-listen threshold, replay counted distinctly)
    - lib/client-partners/health.ts — pure-aggregation style precedent
  </read_first>
  <behavior>
    - clampDelta(n): returns n when 0<n≤15, clamps anything >15 to 15, rejects (returns 0 or throws per the chosen contract) n≤0 or non-finite
    - QUALIFIED_LISTEN_SECONDS === 30 (from the 31-SPEC R13 default)
    - aggregatePerTrack(rows): given raw delta rows for a (track, viewer), returns total audible seconds (sum of deltas), a boolean/whether qualified (total ≥ 30), and a distinct replay count derived from 'ended' events (a replay is distinct from a resume-after-pause)
    - A track with 29.9s summed audible time is NOT qualified; 30.0s IS qualified exactly once; two 'ended' events for the same viewer count as two replays
  </behavior>
  <action>
    Create `lib/selects/engagement.ts` exporting: a `QUALIFIED_LISTEN_SECONDS` constant = 30 (cite 31-SPEC R13); a `clampDelta` function enforcing the 0<Δ≤15 per-flush ceiling (Pitfall 2) that the POST route (plan 05) will call before persist; and pure read-time aggregation functions that take the RAW delta rows (and open/ended events) and compute per-track audible-seconds, qualified-listen (≥30s → exactly one qualified listen per track+viewer), and a distinct replay count from 'ended' events — plus a per-Selects roll-up that folds the per-track numbers. Keep every aggregation pure over in-memory rows (compute-on-read, no running totals). Add typed row shapes matching migration 132's columns. The DB read/query functions that FETCH these rows may also live here as thin service-client selects, but the tested surface is the pure aggregation. Write the test first (RED) with in-memory rows, then implement (GREEN).
  </action>
  <verify>
    <automated>npm test -- lib/selects/engagement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - clampDelta clamps >15 to 15 and rejects ≤0 / non-finite
    - aggregatePerTrack marks ≥30s audible as exactly one qualified listen, <30s as none, and counts each 'ended' as a distinct replay
    - The per-Selects rollup sums per-track audible seconds without a stored total
  </acceptance_criteria>
  <done>The server engagement contract + read-time aggregation are pure and prove the R13 qualified-listen / replay-distinct acceptance bar.</done>
</task>

<task type="auto">
  <name>Task 3: useAudibleTimeAccumulator hook (wraps the pure math against a real <audio> element)</name>
  <files>components/selects-player/useAudibleTimeAccumulator.ts</files>
  <read_first>
    - .planning/phases/31.2-.../31.2-PATTERNS.md § useAudibleTimeAccumulator (full reference implementation)
    - components/selects-player/audible-accumulator.ts (Task 1's pure math to import)
    - components/selects-player/SelectsPlayer.tsx lines ~250-300, 939 — the existing audioRef + <audio> element the hook attaches to (do NOT modify SelectsPlayer here — plan 05 wires it)
  </read_first>
  <action>
    Create `components/selects-player/useAudibleTimeAccumulator.ts` — the React hook from RESEARCH Pattern 4, importing Task 1's pure delta math (never re-deriving it). It takes an audioRef and an onFlush(deltaSeconds, event) callback; on each timeupdate it feeds (lastTime, currentTime, seeking) through the pure function and accumulates the returned delta; on seeking it breaks the contiguity chain; it flushes accumulated pending seconds on a 10s heartbeat and on pause/ended/beforeunload, then zeroes pending. It removes all listeners + clears the interval on cleanup. No fetch here — onFlush is the injection point plan 05 supplies (heartbeat POST + sendBeacon-on-unload). This hook is not unit-tested (jsdom cannot drive real audio timing — see the VALIDATION manual-only row); its correctness rests on Task 1's pure test. Verify it compiles.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` is clean
    - The hook imports the pure math from audible-accumulator.ts (no duplicated delta logic) and never uses Date.now()/setInterval as the time source
  </acceptance_criteria>
  <done>The hook is ready for plan 05 to attach to SelectsPlayer's <audio> element with a real onFlush.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser player → client-computed delta | The audible delta is computed client-side; correctness (not trust) is the concern here — the server clamp is the trust boundary, enforced in plan 05 via clampDelta |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.2-04 | Tampering | client-reported deltaSeconds | high | mitigate | clampDelta (defined here, ≤15s ceiling) is the pure contract the plan-05 route enforces before persist, backing the DB CHECK + cap trigger (defense in depth, Pitfall 2) |
| T-31.2-05 | Information Disclosure (correctness) | audible-accumulator math | medium | mitigate | Pure function proven to discard scrub/seek/backward jumps so a skim never fabricates a qualified listen (Pitfall 1) |
| T-31.2-SC | Tampering (supply chain) | npm installs | low | accept | No new packages — native <audio> DOM events + existing deps only (RESEARCH: no install) |
</threat_model>

<verification>
- `npm test -- components/selects-player/audible-accumulator.test.ts lib/selects/engagement.test.ts` green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- The genuine audible-time accumulator + the server engagement contract are pure, tested, and prove R13's scrub-excluded / replay-distinct / ≥30s-qualified acceptance bar with zero DB or UI coupling.
</success_criteria>

<output>
Create `.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-02-SUMMARY.md` when done.
</output>
