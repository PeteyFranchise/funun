---
phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry
plan: 09
type: execute
wave: 3
depends_on: ["31.2-06"]
files_modified:
  - components/playbook/PlayComposer.tsx
  - components/admin/TodaysPlayBanner.tsx
  - components/admin/ClientPartnersRoom.tsx
  - app/(admin)/admin/client-partners/page.tsx
  - app/api/admin/plays/[id]/completions/route.ts
autonomous: true
requirements: [R9]

must_haves:
  truths:
    - "Leadership composes a Play as a container of assignments (client-targeted health/stage segments + general checkable directives) via PlayComposer, structurally like GamePlanPanel holds topics (D-31.2-08/09/10)"
    - "A 'Today's Play' banner mounts at the TOP of My Client Partners: a client-targeted assignment shows the AE's own matching count + deep-links into the 31.1 filtered list; a general_task shows the directive; the AE marks each done (D-31.2-09/10/11)"
    - "The banner is always visible while a play is active and reflects per-assignment done/not-done state (no snooze) — ASSUMPTION (research open-Q2 default); the AE reads-and-marks the social/general directive (posting deferred, D-31.2-10)"
    - "Leadership sees a 'who's acted' completion rollup across the team's book (the measurement half, D-31.2-11)"
  artifacts:
    - "components/playbook/PlayComposer.tsx"
    - "components/admin/TodaysPlayBanner.tsx"
    - "app/api/admin/plays/[id]/completions/route.ts"
  key_links:
    - "PlayComposer POSTs /api/admin/plays (plan 06); the banner reads the active play (GET) + POSTs the complete route (plan 06)"
    - "matchingClientCount + buildAssignmentDeepLink (plan 06 plays-eligibility) power the client-targeted count + deep-link into the 31.1 My-Client-Partners filters"
    - "the completions rollup route reads play_assignment_completions (migration 131) for the leadership 'who's acted' view"
  prohibitions:
    - "MUST NOT implement in-app social posting — the general/social assignment is a read-and-mark-done directive; posting is a deferred fast-follow (D-31.2-10)"
    - "MUST NOT compute a client-targeted count against a global list — it uses the AE's OWN book (plan 06 own-book scoping)"
    - "MUST NOT invent a new filtered-list route for the deep-link — reuse the 31.1 My-Client-Partners health/stage filter query-string"
---

<objective>
Build the Plays user surfaces on the plan-06 engine: the leadership PlayComposer (compose + publish the one active team play), the AE-facing "Today's Play" banner at the top of My Client Partners (own-book counts, deep-links, mark-done), and the leadership "who's acted" completion rollup. This is the visible coaching loop + its measurement half.

Purpose: D-31.2-11's follow-through closes the loop — the AE acts from the banner, leadership sees who acted. The banner reuses the 31.1 filters for deep-linking (no new list UI) and the social/general assignment stays a read-and-mark directive (posting deferred, D-31.2-10).
Output: PlayComposer.tsx, TodaysPlayBanner.tsx mounted in ClientPartnersRoom, the completions rollup route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-CONTEXT.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-RESEARCH.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-PATTERNS.md
@components/admin/ClientPartnersRoom.tsx
@app/(admin)/admin/client-partners/page.tsx
@lib/client-partners/plays-eligibility.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlayComposer.tsx (leadership) + completions rollup route</name>
  <files>components/playbook/PlayComposer.tsx, app/api/admin/plays/[id]/completions/route.ts</files>
  <read_first>
    - .planning/phases/31.2-.../31.2-PATTERNS.md § PlayComposer (the Game-Plan panel container-of-items analog)
    - app/api/admin/plays/route.ts (plan 06) — the publish POST body shape { title, note?, assignments: [...] }
    - lib/playbook/plays.ts (plan 06) — validateAssignment kinds + loadCompletions
    - lib/admin/gate.ts — verifyAdmin for the leadership-only rollup route
  </read_first>
  <action>
    Create `components/playbook/PlayComposer.tsx` ('use client', data + string-action props only) — a leadership Play-builder mirroring the Game-Plan panel's container-of-items pattern: add assignments of either kind (client_targeted → pick a health band or pipeline stage; general_task → title + note + optional link/attachment/content), reorder, then Publish (POST /api/admin/plays) which retires the prior active. Create `app/api/admin/plays/[id]/completions/route.ts` — a leadership-only (verifyAdmin) GET returning the "who's acted" rollup (loadCompletions across the team's book: per-assignment completed-by AE list + not-yet counts). Mount PlayComposer in the leadership Playbook chrome (reachable from the access/Plays area).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - PlayComposer builds both assignment kinds and publishes via the plan-06 route; the general_task carries content but offers no posting action
    - The completions route is leadership-only and returns the who's-acted rollup; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Leadership composes/publishes the one active team play and sees who has acted.</done>
</task>

<task type="auto">
  <name>Task 2: TodaysPlayBanner.tsx + mount at top of My Client Partners</name>
  <files>components/admin/TodaysPlayBanner.tsx, components/admin/ClientPartnersRoom.tsx, app/(admin)/admin/client-partners/page.tsx</files>
  <read_first>
    - components/admin/ClientPartnersRoom.tsx — the existing room layout + where a top banner mounts (mirror the OnboardingTasksPanel always-render pattern)
    - app/(admin)/admin/client-partners/page.tsx — loadClientPartnersRoomData + how data is fed to ClientPartnersRoom (add the active play + the AE's own-book counts here)
    - lib/client-partners/plays-eligibility.ts (plan 06) — matchingClientCount + buildAssignmentDeepLink
    - app/api/admin/plays/[id]/assignments/[assignmentId]/complete/route.ts (plan 06) — the mark-done POST
  </read_first>
  <action>
    Create `components/admin/TodaysPlayBanner.tsx` ('use client', data + string-action props only) — renders the active play at the TOP of My Client Partners (D-31.2-11): for each client_targeted assignment, the AE's OWN matching count + a deep-link (buildAssignmentDeepLink → the existing 31.1 filtered list) + a mark-done control; for each general_task, the directive (title/note/link/attachment) + a mark-done control (NO posting action — deferred, D-31.2-10). Always visible while a play is active; each row reflects its done/not-done state (no snooze — research open-Q2 default). Mark-done POSTs the plan-06 complete route + router.refresh(). Mount it at the top of ClientPartnersRoom; feed the active play + the caller's own-book match counts from app/(admin)/admin/client-partners/page.tsx (server-computed via matchingClientCount against the AE's already-loaded book — own-book scoping). Do not alter the existing room list/tabs.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The banner mounts at the top of My Client Partners, shows own-book counts + deep-links for client-targeted assignments and the directive for general tasks, and marks each done idempotently
    - Always-visible-with-done-state (no snooze); no posting action; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The AE sees today's play at the top of their home, acts on it, and marks assignments done — the coaching loop's visible half is live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-leadership staff → play composer / completions rollup | Composing + the who's-acted rollup are leadership-only |
| AE → own-book eligibility | The banner's counts must be the AE's own book, not a global list |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.2-24 | Elevation of Privilege | PlayComposer / completions route | high | mitigate | verifyAdmin (leadership-only) on publish + the completions rollup |
| T-31.2-25 | Information Disclosure | banner eligibility counts | medium | mitigate | Counts computed server-side via matchingClientCount against the AE's own book only (plan 06) |
| T-31.2-SC | Tampering (supply chain) | npm installs | low | accept | No new packages this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; full suite green at the wave merge.
- Manual UAT (VALIDATION): as leadership publish a play; as an AE confirm the banner shows own-book counts + deep-links + marks done.
</verification>

<success_criteria>
- Leadership publishes the one active team play; the AE acts on it from the top of My Client Partners and marks assignments done; leadership sees who's acted — D-31.2-08..11 delivered.
</success_criteria>

<output>
Create `.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-09-SUMMARY.md` when done.
</output>
