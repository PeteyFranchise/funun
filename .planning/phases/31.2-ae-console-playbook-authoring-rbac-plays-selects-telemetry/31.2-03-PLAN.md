---
phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry
plan: 03
type: execute
wave: 2
depends_on: ["31.2-01"]
files_modified:
  - lib/playbook/rooms.ts
  - lib/playbook/rooms.test.ts
  - lib/playbook/access-grants.ts
  - lib/playbook/access-grants.test.ts
  - app/api/admin/playbook/rooms/route.ts
  - app/api/admin/playbook/rooms/route.test.ts
autonomous: true
requirements: [R9]

must_haves:
  truths:
    - "requireRoomAccess(roomKey) / requireRoomAccessPage(roomKey) exist as an ADDITIVE gate composed on top of requireStaff/requireStaffPage — leadership always passes WITHOUT a DB read (structural), every other role passes only if playbook_room_role_grants holds its role for the room (D-31.2-01/03, Pattern 1)"
    - "The access-editor write path (setRoomGrant/removeRoomGrant) never inserts a leadership grant row and writes exactly one staff_audit_log entry per grant change via logStaffAction (D-31.2-01 audit guardrail)"
    - "GET /api/admin/playbook/rooms returns the room×role grant matrix; PATCH toggles one (room, role) grant — leadership-only (verifyAdmin), zod .strict(), audited (D-31.2-01)"
    - "loadRooms() reads playbook_rooms as the single live source Rail2/layout render from (was the hardcoded PLAYBOOK_ROOMS array) (D-31.2-01)"
  artifacts:
    - "lib/playbook/rooms.ts"
    - "lib/playbook/access-grants.ts"
    - "app/api/admin/playbook/rooms/route.ts"
  key_links:
    - "requireRoomAccessPage('it-team') (rooms.ts) is what plan 07 re-points the 5 IT-room page guards + PlaybookLayout to"
    - "loadRooms() feeds plan 07's Rail2/layout DB-driven render; the GET/PATCH matrix feeds plan 07's AccessEditorMatrix"
    - "playbook_room_role_grants(room_id, role) written here is the exact table migration 130 created"
  prohibitions:
    - "MUST NOT insert or persist a role='leadership' grant row — leadership access is structural, never row-data (Pitfall 5); rely on the migration-130 DB CHECK AND guard in code"
    - "MUST NOT let Rail2 nav-visibility be the access authority — requireRoomAccess must be independently callable by every room route/page (Pitfall 6)"
    - "MUST NOT trust a client-supplied role/grant flag — the PATCH derives leadership-only authority from the session (verifyAdmin), never the body"
---

<objective>
Build the data-driven authorization layer that generalizes Phase 33's hardcoded `requireStaffPage(['leadership','it'])` room gates into DB-backed grants — WITHOUT replacing the base staff gate. Ships the `requireRoomAccess`/`requireRoomAccessPage` helper (leadership structural, others grant-checked), the access-grant read/write module (leadership never stored, every change audited), and the leadership-only room×role matrix route.

Purpose: this is the heaviest dependency in the phase — Rail2, the access editor UI, the IT-room re-point (plan 07), and every future authored room read through it. Getting the leadership-is-structural invariant and the audit guardrail right here makes "last-admin protection" a structural impossibility rather than a runtime check.
Output: lib/playbook/rooms.ts, lib/playbook/access-grants.ts, the rooms matrix route, + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-CONTEXT.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-RESEARCH.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-PATTERNS.md
@lib/admin/gate.ts
@lib/admin/staff-role.ts
@lib/staff/audit.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/playbook/rooms.ts — loadRooms + requireRoomAccess/requireRoomAccessPage</name>
  <files>lib/playbook/rooms.ts, lib/playbook/rooms.test.ts</files>
  <read_first>
    - .planning/phases/31.2-.../31.2-PATTERNS.md § lib/playbook/rooms.ts (the compose-not-replace reference shape)
    - lib/admin/gate.ts — requireStaff / requireStaffPage / verifyAdmin / RequireStaffPageResult exact signatures + the OPERATIONAL_STAFF_ROLES default
    - lib/admin/staff-role.ts — StaffRole union + getStaffRoles (multi-role) + why leadership is special
  </read_first>
  <behavior>
    - A pure canAccessRoom(staffRoles, grantRolesForRoom) helper returns true when the caller holds 'leadership' (regardless of grants) OR any of the caller's roles is in the room's granted-role set; false otherwise
    - Leadership passes even when the grant set is empty; a role with no matching grant is denied; a multi-role member passes if ANY held role is granted
  </behavior>
  <action>
    Create `lib/playbook/rooms.ts` composing (never replacing) lib/admin/gate.ts. Export: a pure `canAccessRoom(roles, grantedRoles)` predicate (leadership short-circuits true; else set-intersection) — this is the unit-tested surface; `loadRooms(service)` reading playbook_rooms (id, key, label, sort_order, sensitive, coming_soon) ordered by sort_order; `requireRoomAccess(roomKey)` for routes and `requireRoomAccessPage(roomKey)` for pages — each first calls the base requireStaff/requireStaffPage, returns immediately if the resolved role is leadership (no DB read — structural, Pitfall 5), else reads the room + its playbook_room_role_grants and denies (403 for routes, redirect('/admin/playbook') for pages) when no held role has a grant. Match the PATTERNS reference exactly. Write the pure-predicate test first (RED), then implement (GREEN); the DB-backed require* functions are exercised by the route test in Task 3 (their DB reads compile now, run after the plan-01 push).
  </action>
  <verify>
    <automated>npm test -- lib/playbook/rooms.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - canAccessRoom: leadership passes with empty grants; non-granted role denied; any-held-granted-role passes
    - `npx tsc --noEmit` clean; require* compose requireStaff/requireStaffPage (do not reimplement staff auth)
  </acceptance_criteria>
  <done>The additive room-access gate exists; leadership is structurally never gated; the access predicate is unit-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/playbook/access-grants.ts — grant read/write with structural leadership carve-out + audit</name>
  <files>lib/playbook/access-grants.ts, lib/playbook/access-grants.test.ts</files>
  <read_first>
    - lib/staff/audit.ts — logStaffAction signature (actorId, action, targetType, targetId, changes) — call unconditionally, never throws
    - lib/admin/staff-role.ts — the non-leadership role set a grant may target
    - .planning/phases/31.2-.../31.2-RESEARCH.md Pitfall 5 (never render/persist a leadership row)
  </read_first>
  <behavior>
    - A pure isGrantableRole(role) returns false for 'leadership' and any non-StaffRole, true for the other roles
    - buildGrantMatrix(rooms, grantRows) folds rooms + grant rows into a room→{role→granted} matrix that NEVER includes a leadership column
  </behavior>
  <action>
    Create `lib/playbook/access-grants.ts` exporting: pure `isGrantableRole(role)` (rejects 'leadership' + non-roles) and pure `buildGrantMatrix(rooms, grantRows)` (produces the room×role boolean matrix with NO leadership column) — the tested surface; plus `readRoomGrants(service)` (all grant rows), `setRoomGrant(service, {roomId, role, actorId})` and `removeRoomGrant(service, {roomId, role, actorId})` that (a) reject a leadership target up front via isGrantableRole, (b) insert/delete the grant row, and (c) call logStaffAction once with a descriptive action ('grant_playbook_room_role' / 'revoke_playbook_room_role') and the (room, role) in changes. Write the pure-helper test first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npm test -- lib/playbook/access-grants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - isGrantableRole rejects 'leadership' and unknown roles; buildGrantMatrix never emits a leadership column
    - setRoomGrant/removeRoomGrant reject a leadership target and are shaped to call logStaffAction exactly once per change
  </acceptance_criteria>
  <done>Grants read/write with leadership structurally excluded and every change auditable.</done>
</task>

<task type="auto">
  <name>Task 3: app/api/admin/playbook/rooms/route.ts — leadership-only room×role matrix GET/PATCH</name>
  <files>app/api/admin/playbook/rooms/route.ts, app/api/admin/playbook/rooms/route.test.ts</files>
  <read_first>
    - app/api/admin/tips/[itemKey]/route.ts — the verifyAdmin + service-client + zod .strict() + JSON error-shape convention
    - lib/admin/gate.ts — verifyAdmin (leadership-only) exact return shape
    - lib/playbook/access-grants.ts (Task 2) + lib/playbook/rooms.ts (Task 1) — loadRooms/readRoomGrants/buildGrantMatrix/setRoomGrant/removeRoomGrant to call
    - lib/admin/gate.test.ts — the role-gate test convention to mirror for route.test.ts
  </read_first>
  <action>
    Create `app/api/admin/playbook/rooms/route.ts`. GET: verifyAdmin (leadership-only, 403 otherwise), then return buildGrantMatrix(loadRooms(), readRoomGrants()) — the matrix the editor renders. PATCH: verifyAdmin, parse a zod `.strict()` body { roomId: uuid, role: string, granted: boolean }, reject role 'leadership' with 400 (never a grantable target), then call setRoomGrant or removeRoomGrant (each audits). Return the updated matrix. Never trust a client-supplied authority flag — leadership-only comes from verifyAdmin's session resolution. Add `app/api/admin/playbook/rooms/route.test.ts` mirroring lib/admin/gate.test.ts: assert a non-leadership caller is 403 on GET and PATCH; assert a PATCH targeting role 'leadership' is 400; assert a valid grant PATCH is shaped to audit.
  </action>
  <verify>
    <automated>npm test -- app/api/admin/playbook/rooms/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Non-leadership caller → 403 on GET and PATCH; PATCH with role 'leadership' → 400
    - A valid grant toggle updates the matrix and triggers exactly one audit log
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The leadership-only access-editor write path is live and audited; the matrix is readable for plan 07's UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated staff (non-leadership) → access-editor route | Only leadership may read/write the room×role matrix |
| leadership → playbook_room_role_grants | The editor must never be able to lock leadership out of any room |
| any staff role → a sensitive room's content | Access must be enforced server-side per request, not via nav hiding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.2-06 | Elevation of Privilege | PATCH /api/admin/playbook/rooms | high | mitigate | verifyAdmin (leadership-only) derives authority from the session; body-supplied flags ignored; zod .strict() |
| T-31.2-07 | Denial of Service (staff lockout) | setRoomGrant / matrix | high | mitigate | isGrantableRole + a 400 on role 'leadership' + the migration-130 DB CHECK — leadership can never be stored/removed (Pitfall 5) |
| T-31.2-08 | Information Disclosure | requireRoomAccess | high | mitigate | Additive server-side gate every room route/page calls independently; nav hide is UX only (Pitfall 6) |
| T-31.2-09 | Repudiation | grant changes | medium | mitigate | logStaffAction writes exactly one staff_audit_log row per grant change (D-31.2-01 audit guardrail) |
| T-31.2-SC | Tampering (supply chain) | npm installs | low | accept | No new packages this phase |
</threat_model>

<verification>
- `npm test -- lib/playbook/rooms.test.ts lib/playbook/access-grants.test.ts app/api/admin/playbook/rooms/route.test.ts` green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- The data-driven room-access gate + the audited, leadership-structurally-protected access-editor write path exist and are the single authority plan 07 renders and re-points onto.
</success_criteria>

<output>
Create `.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-03-SUMMARY.md` when done.
</output>
