---
phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry
plan: 06
type: execute
wave: 2
depends_on: ["31.2-01"]
files_modified:
  - lib/client-partners/plays-eligibility.ts
  - lib/client-partners/plays-eligibility.test.ts
  - lib/playbook/plays.ts
  - lib/playbook/plays.test.ts
  - app/api/admin/plays/route.ts
  - app/api/admin/plays/[id]/assignments/[assignmentId]/complete/route.ts
  - app/api/admin/plays/route.test.ts
autonomous: true
requirements: [R9]

must_haves:
  truths:
    - "Publishing a Play retires the prior active one — exactly one active team-wide play at a time (D-31.2-08), enforced by the migration-131 partial-unique index AND the publish transition (retire-then-activate)"
    - "A Play is a container of assignments of two kinds: client-targeted (health band OR pipeline stage, evaluated per-AE against their OWN book) and general_task (a checkable directive carrying copy + link + attachment, posting-deferred) (D-31.2-09/10)"
    - "matchingClientCount scopes a client-targeted assignment to ONLY the evaluating AE's own book, reusing computeHealth()/resolveStage() — never a second health calculator (D-31.2-09, Pattern 3)"
    - "An AE marking an assignment done writes exactly one completion row per (assignment, AE) — idempotent re-click (D-31.2-11), backed by the migration-131 UNIQUE(assignment_id, ae_user_id)"
  artifacts:
    - "lib/client-partners/plays-eligibility.ts"
    - "lib/playbook/plays.ts"
    - "app/api/admin/plays/route.ts"
    - "app/api/admin/plays/[id]/assignments/[assignmentId]/complete/route.ts"
  key_links:
    - "plays / play_assignments / play_assignment_completions are migration-131 tables; the health bands + stage keys reuse lib/client-partners/health.ts + stages.ts"
    - "matchingClientCount + the deep-link filter feed plan 09's TodaysPlayBanner; the completion rows feed plan 09's leadership 'who's acted' rollup"
  prohibitions:
    - "MUST NOT build a second health calculator — always call computeHealth() (lib/client-partners/health.ts) + resolveStage() (Don't Hand-Roll)"
    - "MUST NOT evaluate a client-targeted assignment against a global list — own-book scoping only (D-31.2-09, Pattern 3)"
    - "MUST NOT allow two active plays — the publish path retires the prior active before/while activating, and the DB partial-unique index backstops it (D-31.2-08)"
    - "MUST NOT implement in-app posting for the general/social assignment — 31.2 stores the content-carrying directive only; posting is a deferred fast-follow (D-31.2-10)"
---

<objective>
Build the Plays domain: the one-active-play publish invariant, the two assignment kinds (client-targeted health/stage segments + general checkable directives), per-AE own-book eligibility matching, and idempotent AE completion. This is the coaching-loop engine leadership drives.

Purpose: plan 09 renders the "Today's Play" banner + the leadership completion rollup on top of this. Keeping eligibility on computeHealth()/resolveStage() and own-book scoping (never a global list) preserves the 31.1 own-book discipline; the general/social assignment carries content but stays posting-deferred per D-31.2-10.
Output: plays-eligibility.ts (pure), plays.ts (domain + DB), the plays publish route + the assignment-complete route, + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-CONTEXT.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-RESEARCH.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-PATTERNS.md
@lib/client-partners/health.ts
@lib/client-partners/stages.ts
@lib/client-partners/game-plan.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/client-partners/plays-eligibility.ts — own-book assignment matching (pure)</name>
  <files>lib/client-partners/plays-eligibility.ts, lib/client-partners/plays-eligibility.test.ts</files>
  <read_first>
    - .planning/phases/31.2-.../31.2-PATTERNS.md § plays-eligibility.ts (the exact matchingClientCount reference) + § Pattern 3 (own-book scoping)
    - lib/client-partners/health.ts — HealthState/HealthValue + computeHealth (the ONLY health source)
    - lib/client-partners/stages.ts — resolveStage/pipelineStageKey shape
    - lib/client-partners/signals.ts — the ClientPartnerRow / book shape own-book scoping produces
  </read_first>
  <behavior>
    - matchingClientCount(book, { healthBand?, pipelineStageKey? }) filters an AE's own book: a row matches when it satisfies the healthBand (if set) AND the pipelineStageKey (if set); no filter set → whole book
    - A client-targeted assignment against health 'at_risk' returns only the AE's at_risk rows; the same assignment yields a different count for a different AE's book (own-book scoping)
    - A deep-link filter builder maps { healthBand, pipelineStageKey } → the SAME query-string the 31.1 My Client Partners filters already accept (e.g. health/stage params) — no new filter UI
  </behavior>
  <action>
    Create `lib/client-partners/plays-eligibility.ts` exporting the pure `matchingClientCount(book, assignment)` from the PATTERNS reference (filter on health + pipelineStageKey), plus a pure `buildAssignmentDeepLink(assignment)` returning the existing My-Client-Partners filtered-list query string (reuse the 31.1 health/stage filter param names — do not invent a new route). Both pure over an in-memory book (the caller supplies the AE's own book). Write the test first (RED) proving own-book scoping (two different books → two different counts) and the health/stage filters, then implement (GREEN).
  </action>
  <verify>
    <automated>npm test -- lib/client-partners/plays-eligibility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - matchingClientCount respects health band, pipeline stage, both, and neither; counts differ per book (own-book scoping)
    - buildAssignmentDeepLink emits the existing 31.1 filter query-string, not a new route
  </acceptance_criteria>
  <done>Client-targeted eligibility is pure, own-book-scoped, and reuses the existing health/stage model + filters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/playbook/plays.ts — one-active invariant + assignment container + idempotent completion</name>
  <files>lib/playbook/plays.ts, lib/playbook/plays.test.ts</files>
  <read_first>
    - .planning/phases/31.2-.../31.2-PATTERNS.md § plays (the Game-Plan container-of-items shape to mirror; separate tables NOT a JSONB blob)
    - lib/client-partners/game-plan.ts — the container/type-shape precedent (topics → assignments)
    - supabase/migrations/131_plays.sql (plan 01) — the plays/assignments/completions columns + the partial-unique active index this relies on
  </read_first>
  <behavior>
    - validateAssignment(input): a client_targeted assignment requires at least one of healthBand/pipelineStageKey and no directive content; a general_task requires a title and may carry note/link/attachment/content; a malformed kind is rejected
    - buildPublishTransition(currentActive, newPlay): describes retiring the current active play (status→retired) and activating the new one — exactly one active results
    - completionIsIdempotent: marking the same (assignment, AE) twice yields one logical completion (the DB UNIQUE backs this; the helper returns an upsert-on-conflict-do-nothing shape)
  </behavior>
  <action>
    Create `lib/playbook/plays.ts` exporting: Play / PlayAssignment / PlayAssignmentCompletion types (mirroring the game-plan container shape); a pure `validateAssignment(input)` enforcing the two-kind discriminant (client_targeted → health/stage, no posting; general_task → title + optional copy/link/attachment/content, D-31.2-09/10); a pure `buildPublishTransition` (retire prior active → activate new, one-active invariant D-31.2-08); and thin service-client functions `publishPlay` (retire-then-insert-active in one path, relying on the partial-unique index as the backstop), `loadActivePlay`, `markAssignmentComplete` (insert-on-conflict-do-nothing on (assignment_id, ae_user_id) — idempotent, D-31.2-11), and `loadCompletions` (for plan 09's rollup). Write the pure-helper tests first (RED: validateAssignment kinds, one-active transition, idempotent shape), then implement (GREEN).
  </action>
  <verify>
    <automated>npm test -- lib/playbook/plays.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - validateAssignment enforces client_targeted (health/stage, no posting) vs general_task (title + optional content); rejects malformed kinds
    - buildPublishTransition yields exactly one active play; markAssignmentComplete is idempotent per (assignment, AE)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The Plays domain enforces one-active + two-kind assignments + idempotent completion in pure, tested logic.</done>
</task>

<task type="auto">
  <name>Task 3: Plays publish route (leadership) + assignment-complete route (AE)</name>
  <files>app/api/admin/plays/route.ts, app/api/admin/plays/[id]/assignments/[assignmentId]/complete/route.ts, app/api/admin/plays/route.test.ts</files>
  <read_first>
    - app/api/admin/tips/route.ts — the staff CRUD + logStaffAction convention
    - lib/admin/gate.ts — verifyAdmin (leadership-only publish) + requireStaff (AE complete)
    - lib/playbook/plays.ts (Task 2) — publishPlay / markAssignmentComplete / validateAssignment
    - lib/staff/audit.ts — logStaffAction
  </read_first>
  <action>
    Create `app/api/admin/plays/route.ts`: POST publishes a new Play (verifyAdmin — leadership-only), parsing a zod `.strict()` body { title, note?, assignments: [...] } where each assignment is validated by validateAssignment; call publishPlay (retire prior active + activate) and logStaffAction ('publish_play'). GET returns the active play (requireStaff, so any AE can read the current team play). Create `app/api/admin/plays/[id]/assignments/[assignmentId]/complete/route.ts`: POST marks the calling AE's completion (requireStaff), calls markAssignmentComplete (idempotent) and logStaffAction ('complete_play_assignment'); a re-click returns success without a duplicate. Add `route.test.ts`: a non-leadership publish → 403; publishing retires the prior active; a double-complete yields one completion.
  </action>
  <verify>
    <automated>npm test -- app/api/admin/plays/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Non-leadership POST to publish → 403; publishing a new play retires the prior active
    - An AE double-clicking complete produces exactly one completion; every write audits
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Leadership publishes the one active team play; AEs mark assignments done idempotently — the coaching loop's write half is live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-leadership staff → play publish | Only leadership composes/publishes the team play |
| AE → another AE's book | Client-targeted eligibility must never leak another AE's clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.2-16 | Elevation of Privilege | POST /api/admin/plays | high | mitigate | verifyAdmin (leadership-only) on publish; AE routes limited to their own completion |
| T-31.2-17 | Information Disclosure | client-targeted eligibility | medium | mitigate | matchingClientCount scopes to the evaluating AE's own book only (Pattern 3) — never a global list |
| T-31.2-18 | Tampering (data integrity) | one-active invariant + completion | medium | mitigate | Migration-131 partial-unique active index + UNIQUE(assignment, AE) backstop the app-level transition (idempotent) |
| T-31.2-SC | Tampering (supply chain) | npm installs | low | accept | No new packages this phase |
</threat_model>

<verification>
- `npm test -- lib/client-partners/plays-eligibility.test.ts lib/playbook/plays.test.ts app/api/admin/plays/route.test.ts` green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- One active team play at a time, two assignment kinds, own-book eligibility, and idempotent AE completion are live in pure-tested logic + leadership/AE routes — R9's Plays engine is ready for plan 09's surfaces.
</success_criteria>

<output>
Create `.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-06-SUMMARY.md` when done.
</output>
