---
phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry
plan: 06
subsystem: api
tags: [plays, client-partners, health, zod, jest, playbook, coaching-loop]

# Dependency graph
requires:
  - phase: 31.2-01
    provides: migrations 130/131/132 applied to prod (plays / play_assignments / play_assignment_completions tables + plays_one_active_uniq partial index)
provides:
  - "matchingClientCount / buildAssignmentDeepLink — own-book client-targeted eligibility + deep-link builder"
  - "validateAssignment / buildPublishTransition / buildCompletionUpsert — the Plays domain's pure invariants"
  - "publishPlay / loadActivePlay / markAssignmentComplete / loadCompletions — the Plays domain's DB I/O layer"
  - "POST/GET /api/admin/plays — leadership publish + any-staff read of the active team play"
  - "POST /api/admin/plays/[id]/assignments/[assignmentId]/complete — idempotent AE mark-done"
affects: [31.2-09]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Plays domain mirrors the Game Plan container-of-items shape (lib/client-partners/game-plan.ts) — separate tables, not a JSONB blob, since Plays carry a one-active invariant + per-AE completion that Game Plans don't"
    - "buildPublishTransition/buildCompletionUpsert are pure description functions the I/O layer carries out — same 'describe then execute' split as computeHealth()/assembleRows()"

key-files:
  created:
    - lib/client-partners/plays-eligibility.ts
    - lib/client-partners/plays-eligibility.test.ts
    - lib/playbook/plays.ts
    - lib/playbook/plays.test.ts
    - app/api/admin/plays/route.ts
    - app/api/admin/plays/route.test.ts
    - app/api/admin/plays/[id]/assignments/[assignmentId]/complete/route.ts
  modified: []

key-decisions:
  - "buildAssignmentDeepLink targets /admin/client-partners with health/stage query params — no filter-UI convention existed yet in the codebase to reuse verbatim, so these two param names were chosen as the deep-link contract for plan 09's TodaysPlayBanner to consume (D-31.2-09a intent, no new route)"
  - "PlaysEligibilityRow extends ClientPartnerRow with an optional pipelineStageKey field, defined locally in plays-eligibility.ts rather than added to columns.ts — this plan is pure-eligibility-logic scope only; wiring resolveStage(...).key through loadBook()'s row assembly is production plumbing left to plan 09, which owns the TodaysPlayBanner caller"

patterns-established:
  - "Plays domain mirrors the Game Plan container-of-items shape (separate tables, not JSONB)"

requirements-completed: [R9]

coverage:
  - id: D1
    description: "matchingClientCount filters an AE's own book by health band and/or pipeline stage key (AND semantics), matching the whole book when no filter is set"
    requirement: R9
    verification:
      - kind: unit
        ref: "lib/client-partners/plays-eligibility.test.ts#matchingClientCount"
        status: pass
    human_judgment: false
  - id: D2
    description: "Own-book scoping: the same assignment yields a different matchingClientCount for a different AE's book — never a global list"
    requirement: R9
    verification:
      - kind: unit
        ref: "lib/client-partners/plays-eligibility.test.ts#matchingClientCount own-book scoping"
        status: pass
    human_judgment: false
  - id: D3
    description: "buildAssignmentDeepLink emits the existing My Client Partners route's health/stage query string, never a new route"
    requirement: R9
    verification:
      - kind: unit
        ref: "lib/client-partners/plays-eligibility.test.ts#buildAssignmentDeepLink"
        status: pass
    human_judgment: false
  - id: D4
    description: "validateAssignment enforces the client_targeted (health/stage required, no directive content) vs general_task (title required, optional content) two-kind discriminant, rejecting malformed kinds"
    requirement: R9
    verification:
      - kind: unit
        ref: "lib/playbook/plays.test.ts#validateAssignment"
        status: pass
    human_judgment: false
  - id: D5
    description: "buildPublishTransition + publishPlay retire the prior active play before activating the new one — exactly one active play results (D-31.2-08)"
    requirement: R9
    verification:
      - kind: unit
        ref: "lib/playbook/plays.test.ts#buildPublishTransition"
        status: pass
      - kind: unit
        ref: "app/api/admin/plays/route.test.ts#publishing a new play retires the prior active one"
        status: pass
    human_judgment: false
  - id: D6
    description: "markAssignmentComplete/buildCompletionUpsert is idempotent per (assignment, AE) — a re-click produces exactly one completion row"
    requirement: R9
    verification:
      - kind: unit
        ref: "lib/playbook/plays.test.ts#buildCompletionUpsert"
        status: pass
      - kind: unit
        ref: "app/api/admin/plays/route.test.ts#an AE double-clicking complete produces exactly one completion row"
        status: pass
    human_judgment: false
  - id: D7
    description: "POST /api/admin/plays publish is leadership-only (verifyAdmin) — a non-leadership caller gets 403 and no write occurs"
    requirement: R9
    verification:
      - kind: unit
        ref: "app/api/admin/plays/route.test.ts#returns 403 for a non-leadership caller and never publishes"
        status: pass
    human_judgment: false
  - id: D8
    description: "Every publish and every completion write calls logStaffAction (publish_play / complete_play_assignment) unconditionally"
    requirement: R9
    verification:
      - kind: unit
        ref: "app/api/admin/plays/route.test.ts#logStaffAction assertions"
        status: pass
    human_judgment: false

duration: 55min
completed: 2026-08-25
status: complete
---

# Phase 31.2 Plan 06: Plays Domain — One-Active Invariant, Two Assignment Kinds, Own-Book Eligibility Summary

**Built the Plays coaching-loop engine: leadership publishes one team-wide play at a time (retire-then-activate), assignments target either an AE's own-book health/stage segment or carry a posting-deferred general directive, and AEs mark completions idempotently.**

## Performance

- **Duration:** 55 min
- **Started:** 2026-08-25T01:27:00Z (approx, first task read)
- **Completed:** 2026-08-25T02:22:35Z
- **Tasks:** 3/3 completed
- **Files modified:** 7 created, 0 modified

## Accomplishments

- `lib/client-partners/plays-eligibility.ts` — pure own-book matching (`matchingClientCount`) and deep-link builder (`buildAssignmentDeepLink`), reusing `computeHealth()`'s output rather than a second health calculator
- `lib/playbook/plays.ts` — the Plays domain: `validateAssignment` (two-kind discriminant), `buildPublishTransition` (one-active invariant), `buildCompletionUpsert` (idempotent completion shape), plus the thin I/O layer (`publishPlay`, `loadActivePlay`, `markAssignmentComplete`, `loadCompletions`)
- `app/api/admin/plays/route.ts` + `.../complete/route.ts` — leadership-only publish, any-staff read, and idempotent AE mark-done, each write audited via `logStaffAction`

## Task Commits

Each task was committed atomically:

1. **Task 1: lib/client-partners/plays-eligibility.ts — own-book assignment matching (pure)** - `bc9e34e` (feat)
2. **Task 2: lib/playbook/plays.ts — one-active invariant + assignment container + idempotent completion** - `a0f5620` (feat)
3. **Task 3: Plays publish route (leadership) + assignment-complete route (AE)** - `589ab38` (feat)

**Plan metadata:** committed by orchestrator after wave merge (worktree-isolated execution — STATE.md/ROADMAP.md are not touched by this agent)

_Note: all three tasks were TDD (RED test file first, confirmed failing on missing module, then GREEN implementation) except Task 3's routes, which were implemented directly with a full route.test.ts covering both endpoints._

## Files Created/Modified

- `lib/client-partners/plays-eligibility.ts` - Pure own-book client-targeted eligibility matching + deep-link builder
- `lib/client-partners/plays-eligibility.test.ts` - Tests proving own-book scoping and health/stage filter semantics
- `lib/playbook/plays.ts` - Plays domain types, pure invariant helpers, and service-client I/O functions
- `lib/playbook/plays.test.ts` - Pure-helper tests for validateAssignment/buildPublishTransition/buildCompletionUpsert
- `app/api/admin/plays/route.ts` - GET (any staff, active play) + POST (leadership-only, publish)
- `app/api/admin/plays/route.test.ts` - Route tests for both GET/POST and the nested complete route
- `app/api/admin/plays/[id]/assignments/[assignmentId]/complete/route.ts` - POST (any staff, idempotent own-completion)

## Decisions Made

- **Deep-link param names (`health`, `stage`):** no existing filter-query-string convention was found in the codebase for My Client Partners (`app/(admin)/admin/client-partners`) to reuse verbatim — `buildAssignmentDeepLink` establishes `health`/`stage` as the contract for plan 09's TodaysPlayBanner to eventually consume, per the plan's "e.g. health/stage params" guidance. No new filter UI or route was built (out of scope here).
- **`PlaysEligibilityRow` extends `ClientPartnerRow` locally** (in plays-eligibility.ts) with an optional `pipelineStageKey` field rather than editing `lib/client-partners/columns.ts` — this plan's `files_modified` scope is the eligibility/domain/route layer only; wiring `resolveStage(...).key` through `loadBook()`'s row assembly (`lib/client-partners/signals.ts`) is production plumbing that belongs to plan 09, the actual caller of `matchingClientCount` against a live book.

## Deviations from Plan

None - plan executed exactly as written. `pipelineStageKey` typing above is an additive, in-scope-file decision (not a deviation) since it only touches `plays-eligibility.ts`, one of the plan's own `files_modified` entries.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. Migrations 130/131/132 are already applied to prod (per plan 01's SUMMARY).

## Next Phase Readiness

- Plan 09's TodaysPlayBanner can call `loadActivePlay` for the active play + assignments, `matchingClientCount`/`buildAssignmentDeepLink` against the AE's own book (via `loadBook()`, enriched with `pipelineStageKey`), and `markAssignmentComplete` for the mark-done action.
- Plan 09's leadership completion rollup can call `loadCompletions(service, assignmentIds)`.
- No blockers. The one open wiring item (populating `pipelineStageKey` on `ClientPartnerRow` in production) is explicitly plan 09's responsibility, documented above.

---
*Phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry*
*Completed: 2026-08-25*

## Self-Check: PASSED

All 7 created files verified present on disk; all 3 task commits (`bc9e34e`, `a0f5620`, `589ab38`) verified present in git log. Full plan verification suite (`lib/client-partners/plays-eligibility.test.ts lib/playbook/plays.test.ts app/api/admin/plays/route.test.ts`) green (34/34 tests), and `npx tsc --noEmit` clean.
