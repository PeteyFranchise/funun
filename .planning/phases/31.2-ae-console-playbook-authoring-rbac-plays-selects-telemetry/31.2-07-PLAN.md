---
phase: 31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry
plan: 07
type: execute
wave: 3
depends_on: ["31.2-03"]
files_modified:
  - components/playbook/AccessEditorMatrix.tsx
  - app/(admin)/admin/playbook/access/page.tsx
  - components/playbook/Rail2.tsx
  - app/(admin)/admin/playbook/layout.tsx
  - app/(admin)/admin/playbook/it/dashboard/page.tsx
  - app/(admin)/admin/playbook/it/vendor-directory/page.tsx
  - app/(admin)/admin/playbook/it/runbook/page.tsx
  - app/(admin)/admin/playbook/it/operating-rhythm/page.tsx
  - app/(admin)/admin/playbook/it/thresholds/page.tsx
autonomous: true
requirements: [R9]

must_haves:
  truths:
    - "Leadership sees a clickable room×role access-editor matrix; toggling a cell PATCHes the grant and the change takes effect live (no deploy) — the leadership column is NEVER rendered (D-31.2-01, Pitfall 5)"
    - "Rail2 + PlaybookLayout render from DB rooms (loadRooms) instead of the hardcoded PLAYBOOK_ROOMS array; the IT room's visibility is derived from requireRoomAccess-backed grants, not a hardcoded canSeeItRoom boolean (D-31.2-01/03)"
    - "All 5 IT-room page guards are re-pointed from requireStaffPage(['leadership','it']) to requireRoomAccessPage('it-team') — the matrix now actually controls the IT room's real gate, day-one behavior unchanged (Pitfall 6)"
  artifacts:
    - "components/playbook/AccessEditorMatrix.tsx"
    - "app/(admin)/admin/playbook/access/page.tsx"
  key_links:
    - "AccessEditorMatrix reads/PATCHes GET/PATCH /api/admin/playbook/rooms (plan 03); the access page is leadership-gated via verifyAdmin"
    - "requireRoomAccessPage('it-team') (plan 03 rooms.ts) is the new authority behind the 5 IT pages + the layout's IT visibility"
    - "loadRooms (plan 03) feeds Rail2 through the layout as props (Rail2 stays a client component fed server-side)"
  prohibitions:
    - "MUST NOT render or persist a leadership row/column in the matrix (Pitfall 5)"
    - "MUST NOT leave any IT-room page on the hardcoded requireStaffPage(['leadership','it']) literal — that would make the matrix cosmetic (Pitfall 6)"
    - "MUST NOT let Rail2 visibility be the access authority — each IT page keeps an inline server self-guard (now requireRoomAccessPage), never relying on the nav hide"
---

<objective>
Make the data-driven RBAC real end to end: build the leadership room×role access-editor matrix UI, convert Rail2 + the Playbook layout to render from DB rooms, and re-point all five IT-room page guards from Phase 33's hardcoded `requireStaffPage(['leadership','it'])` literal to `requireRoomAccessPage('it-team')`. The migration-130 seed reproduces today's leadership+it grant, so day-one behavior is unchanged — but now a leadership toggle in the matrix actually controls the IT room's real gate.

Purpose: this is the payoff of D-31.2-01 — without the re-point (Pitfall 6) the matrix would be cosmetic. Keeping the leadership column unrendered (Pitfall 5) preserves the structural last-admin protection.
Output: AccessEditorMatrix.tsx, the access page, DB-driven Rail2 + layout, 5 re-pointed IT guards.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-CONTEXT.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-RESEARCH.md
@.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-PATTERNS.md
@components/playbook/Rail2.tsx
@app/(admin)/admin/playbook/layout.tsx
@lib/playbook/rooms.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AccessEditorMatrix.tsx + leadership access-editor page</name>
  <files>components/playbook/AccessEditorMatrix.tsx, app/(admin)/admin/playbook/access/page.tsx</files>
  <read_first>
    - .planning/phases/31.2-.../31.2-PATTERNS.md § AccessEditorMatrix (No Analog Found — build from the StaffRole list + PATCH-per-cell; NEVER a leadership column)
    - lib/admin/staff-role.ts — ALL_STAFF_ROLES for the columns (exclude 'leadership')
    - app/api/admin/playbook/rooms/route.ts (plan 03) — the GET matrix + PATCH { roomId, role, granted } contract to call
    - app/(admin)/admin/playbook/layout.tsx — the leadership gate + chrome style to match for the new page
  </read_first>
  <action>
    Create `components/playbook/AccessEditorMatrix.tsx` ('use client') — a room×role grid: rooms as rows, the non-leadership StaffRole values as columns (NEVER a leadership column — Pitfall 5), each cell a toggle that PATCHes `/api/admin/playbook/rooms` with { roomId, role, granted } and router.refresh()es on success. Data + string-action props only (no function props). Create `app/(admin)/admin/playbook/access/page.tsx` — a leadership-gated RSC (verifyAdmin/requireStaffPage(['leadership'])) that GETs the matrix from plan 03's route and mounts AccessEditorMatrix. Add a Rail 1 / Rail 2 entry point to reach it (a leadership-only "Access" link in the Playbook chrome). Do not render the leadership row/column anywhere.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The matrix renders rooms × non-leadership roles with no leadership column; a cell toggle PATCHes the grant + refreshes
    - The access page is leadership-only; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Leadership can edit room×role access from a clickable matrix; the leadership column is structurally absent.</done>
</task>

<task type="auto">
  <name>Task 2: DB-driven Rail2 + PlaybookLayout</name>
  <files>components/playbook/Rail2.tsx, app/(admin)/admin/playbook/layout.tsx</files>
  <read_first>
    - components/playbook/Rail2.tsx — the current PLAYBOOK_ROOMS import + canSeeItRoom prop + GhostRoom/ItRoomEntry render (preserve the visual treatment)
    - app/(admin)/admin/playbook/layout.tsx — the current getStaffRole/canSeeItRoom computation to replace with DB rooms + grant-derived visibility
    - lib/playbook/rooms.ts (plan 03) — loadRooms + canAccessRoom/requireRoomAccessPage
  </read_first>
  <action>
    Modify `app/(admin)/admin/playbook/layout.tsx` to call loadRooms(service) and compute per-room visibility from the caller's roles against the DB grants (leadership sees all; others see a room only if granted OR it's a non-sensitive room per D-31.2-03) — replacing the hardcoded canSeeItRoom boolean — then pass the visible rooms list down to Rail2 as a prop. Modify `components/playbook/Rail2.tsx` to accept the rooms (and per-room visibility) as props instead of importing PLAYBOOK_ROOMS, preserving the existing GhostRoom / ItRoomEntry / collapse visual treatment (nav.ts may stay as a typing/fallback source but is no longer the live authority). Nav visibility remains UX only — enforcement lives in Task 3's page guards.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Rail2 + layout render from DB rooms (loadRooms), not the hardcoded array; the IT room's visibility follows the grants
    - Visual treatment (ghost rooms, IT sub-pages, collapse) preserved; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The Playbook nav is data-driven; a grant change reflects in the rail without a deploy.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point the 5 IT-room page guards to requireRoomAccessPage('it-team')</name>
  <files>app/(admin)/admin/playbook/it/dashboard/page.tsx, app/(admin)/admin/playbook/it/vendor-directory/page.tsx, app/(admin)/admin/playbook/it/runbook/page.tsx, app/(admin)/admin/playbook/it/operating-rhythm/page.tsx, app/(admin)/admin/playbook/it/thresholds/page.tsx</files>
  <read_first>
    - app/(admin)/admin/playbook/it/vendor-directory/page.tsx — the current inline requireStaffPage(['leadership','it']) self-guard shape (identical across the 5 pages)
    - lib/playbook/rooms.ts (plan 03) — requireRoomAccessPage('it-team') return shape
    - __tests__/migration-130.test.ts (plan 01) — the seed-parity lock that guarantees it-team == leadership+it day one (so this re-point is behavior-preserving)
  </read_first>
  <action>
    In each of the 5 IT-room pages, replace the inline `requireStaffPage(['leadership','it'])` call with `requireRoomAccessPage('it-team')` (imported from lib/playbook/rooms.ts) — a mechanical one-line guard swap per page, keeping the guard BEFORE any content read (fail-closed, D-02 discipline preserved). Behavior is unchanged because migration 130 seeds it-team grantable to 'it' and leadership passes structurally (parity already asserted by the plan-01 migration-130 test — no new test needed here). Do not alter the doc-render bodies.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- __tests__/migration-130.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 IT pages guard via requireRoomAccessPage('it-team'); none retains the hardcoded requireStaffPage(['leadership','it']) literal
    - The migration-130 seed-parity test stays green (day-one IT access unchanged); `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The IT room's real gate is now data-driven — the access matrix controls it; Pitfall 6 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-leadership staff → access-editor page | Only leadership may reach/edit the matrix |
| any staff → IT-room content | Enforcement must be the data-driven room gate, not the nav hide |
| matrix UI → leadership access | The UI must never offer to toggle leadership's access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.2-19 | Denial of Service (staff lockout) | AccessEditorMatrix | high | mitigate | No leadership row/column rendered or persisted (Pitfall 5); the plan-03 route + migration CHECK backstop |
| T-31.2-20 | Information Disclosure | IT-room pages | high | mitigate | Each page re-pointed to requireRoomAccessPage('it-team') server-side self-guard; nav hide is UX only (Pitfall 6) |
| T-31.2-21 | Elevation of Privilege | access page | high | mitigate | Leadership-only RSC gate (verifyAdmin/requireStaffPage(['leadership'])) |
| T-31.2-SC | Tampering (supply chain) | npm installs | low | accept | No new packages this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm test -- __tests__/migration-130.test.ts` green (seed parity).
- Full suite green at the wave merge.
</verification>

<success_criteria>
- The room×role matrix edits access live, the Playbook nav is DB-driven, and the IT room's real gate is controlled by the grants — D-31.2-01's data-driven RBAC is complete with leadership structurally protected.
</success_criteria>

<output>
Create `.planning/phases/31.2-ae-console-playbook-authoring-rbac-plays-selects-telemetry/31.2-07-SUMMARY.md` when done.
</output>
