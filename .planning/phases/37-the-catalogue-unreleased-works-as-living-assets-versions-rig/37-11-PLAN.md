---
phase: 37-the-catalogue-unreleased-works-as-living-assets-versions-rig
plan: 11
type: execute
wave: 3
depends_on: [37-04, 37-05]
files_modified:
  - components/catalogue/WorkHeader.tsx
  - components/catalogue/WorkHeader.test.tsx
  - components/catalogue/WorkRoster.tsx
  - components/catalogue/WorkRoster.test.tsx
autonomous: true
requirements: [S-02, S-01]

must_haves:
  truths:
    - "The title is a live input — click, type, autosaved — because identity is the work id and the title is presentation (RENAME RULE)"
    - "The header carries the three-state vocal setting, and choosing instrumental removes every who-sings prompt from the page (DEFAULT-PERFORMER RULE)"
    - "With a primary performer set, the header says sections inherit unless tagged, so a solo artist is never nagged"
    - "The header shows contributor chips and the splits status, matching sketch 001's header row"
    - "Adding someone to the work reuses the existing quick-invite shape — a first name and an email — and returns a copyable link even when email delivery fails"
    - "The roster states plainly that being on the work is not being on the splits (S-02, doctrine)"
    - "Marking someone a writer is a separate, explicit action, and the UI never shows or asks for a percentage"
    - "Only an administer-tier member sees the membership controls"
  artifacts:
    - "components/catalogue/WorkHeader.tsx"
    - "components/catalogue/WorkRoster.tsx"
  key_links:
    - "The title input and vocal control PATCH plan 05's /api/works/[workId]"
    - "The roster posts to plan 05's /api/works/[workId]/members and surfaces the invite link it returns"
    - "Tier labels and the capability predicates come from `lib/catalogue/membership.ts` (plan 04)"
    - "The vocal state this header sets is what plan 08's LyricBlockCard reads to suppress its singer cluster"
  prohibitions:
    - "MUST NOT render a percentage input, a percentage suggestion, or any number next to a person's name (CAT-Q1a)"
    - "MUST NOT add someone to the split sheet as part of adding them to the work"
    - "MUST NOT show membership controls to a contribute-tier member"
    - "MUST NOT build a second invite form — the field shape matches the existing quick-invite path"
    - "MUST NOT use raw hex colours, and MUST NOT spend a second gradient on this surface"
    - "MUST NOT run `npm run build`"
---

<objective>
Build the work's header and its roster — the two surfaces where identity and membership live.

Implements: the header row of `.claude/skills/sketch-findings-funun/sources/001-work-page-diary/
variant-a.html` and `.../variant-c.html` (title, contributor chips, splits status), plus the pad
header's live title input and vocal control from `.claude/skills/sketch-findings-funun/sources/
006-lyrics-pad/variant-a.html`. The governing rules — RENAME, DEFAULT-PERFORMER and PERFORMER — are
recorded in `.claude/skills/sketch-findings-funun/references/catalogue-hygiene-ui.md`.

`WorkHeader.tsx` is the title input, the three-state vocal control, the contributor chips and the
splits-status chip. `WorkRoster.tsx` is add-a-collaborator, the tier picker and the separate writer
promotion.

Purpose: S-02 is the point where a song stops being one person's file. The header is where the
artist declares who sings — a setting with real downstream consequences, since instrumental makes
the Crate vocal check pass by construction and strips every who-sings prompt from the pad. The
roster is where the doctrine's sharpest line has to be visible in the interface, not just true in
the database: membership grants access, the sheet grants ownership.

Output: two components and two `renderToStaticMarkup` suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-CONTEXT.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-04-SUMMARY.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-05-SUMMARY.md
@.planning/deliberations/the-catalogue-unreleased-works.md
@.claude/skills/sketch-findings-funun/references/catalogue-hygiene-ui.md
@.claude/skills/sketch-findings-funun/sources/001-work-page-diary/variant-a.html
@.claude/skills/sketch-findings-funun/sources/006-lyrics-pad/variant-a.html
@components/ui/LearnWhy.tsx
@tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: components/catalogue/WorkHeader.tsx — the live title, the three vocal states, the chips</name>
  <files>components/catalogue/WorkHeader.tsx, components/catalogue/WorkHeader.test.tsx</files>
  <action>
    Read sketch 001-A's header card and 006-A's header input, and build one header that carries both.

    The title is a bare live input styled as a heading — no border until focus, no save button —
    debounced and PATCHed to plan 05's route. Comment the RENAME RULE where it is implemented:
    identity is the work id, the title is presentation, which is why renaming is free at any time and
    why the diary logs it with both names. Note the rest of the rule for the reader: former titles
    stay findable through those diary entries so a collaborator asking for the old name still finds
    the song, and graduation carries the current title into the release where it takes its final
    form.

    Beneath the title, the identity line from the sketches: the owner's handle, the contributor names,
    and the splits status as a chip. Keep the splits chip to a state word — draft — with no
    percentage anywhere. Comment CAT-Q1a's people-not-numbers rule right there, because this header is
    the single most tempting place in the product to show a percentage next to a name.

    The vocal control has three states and each does real work. State one is a named primary
    performer, defaulting to the creator, presented in the reference file's own words — sections
    inherit unless tagged. State two is varies, meaning per-block tags do the work. State three is
    instrumental. Comment what instrumental actually causes, so nobody later treats it as a label:
    every who-sings prompt disappears and blocks stay pure structure in producer vocabulary, the
    Crate vocal check passes by definition because there are no vocals for the hard no to trigger on,
    DDEX exports omit vocal performer roles, and it is the same fact as an instrumental tag.

    Fold the explanation of the two inheritance guardrails behind `components/ui/LearnWhy.tsx` rather
    than printing them: a default fills the plan and never the record (a credit becomes fact only
    when a version carries that performance, and the human-take registry requires an actual take),
    and an AI vocal can never hide under the default because the add-audio flow asks regardless and a
    declared fact always beats inheritance.

    Accept the viewer's tier and hide the vocal control and the title input from a viewer who cannot
    edit — though in 37.1 both tiers can, so this is a prop the component honours rather than a case
    it currently exercises. Comment that, so the prop does not read as dead.

    Leave the destination lights out. Sketch 004's Crate, Release, Registration and Distribution
    chips are 37.2 — comment that the header's right-hand slot is deliberately reserved for them so
    the layout does not have to be redrawn when they land.

    Write a `renderToStaticMarkup` suite: the title renders as an input carrying the current value;
    the splits chip renders a state word and the markup contains no percent character next to a
    name; each of the three vocal states renders its own affordance and the inheritance line appears
    only for the primary state; the LearnWhy content is collapsed on first paint; no raw hex appears.
  </action>
  <verify>
    <automated>npx jest components/catalogue/WorkHeader.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>The title is a live debounced input; the three vocal states each render distinctly with the inheritance line on the primary state only; the splits chip shows a state and no number; the guardrail explanation is folded behind LearnWhy.</done>
</task>

<task type="auto">
  <name>Task 2: components/catalogue/WorkRoster.tsx — who is on the song, and who is on the sheet</name>
  <files>components/catalogue/WorkRoster.tsx, components/catalogue/WorkRoster.test.tsx</files>
  <action>
    Build the roster panel. It lists the work's members with their tier and, separately, shows which
    of them are on the living split sheet.

    Make the distinction visible, not just true. Render the two facts as two columns or two clearly
    labelled groupings, and carry a short line stating the doctrine plainly: being on the work means
    you can add to it; being on the sheet means you own part of it. Comment that this is the
    interface expression of Pitfall 3 and that collapsing the two groupings into one list would
    reintroduce exactly the confusion the separation exists to prevent.

    Adding someone uses the field shape the existing quick-invite path already accepts — a first name
    and an email — plus a tier choice from `lib/catalogue/membership.ts`'s label record. Comment that
    the field shape is deliberately identical to the existing standalone invite so an artist meets one
    invite form in this product, not two.

    Surface the invite link plan 05's route returns alongside the success state, with a copy control.
    Comment why the link is shown rather than assumed delivered: the underlying invite helper returns
    a usable link even when email delivery fails, and showing it is what keeps a delivery outage from
    reading as a failed invite.

    Mark someone a writer as a SEPARATE action on an existing member — never a checkbox inside the
    add form. Comment the reason at that control: adding a member and granting a share are different
    decisions made at different moments, and a single combined form makes the second one accidental.

    When a writer is promoted, show what happened in words, not numbers: the sheet was redrafted to
    equal shares. Comment CAT-Q1a: equal is the default, writers move it from there themselves if
    they choose, and the system never proposes a contribution-weighted split. This component renders
    no percentage input and no suggestion.

    Gate every membership control behind `canManageMembership()` from plan 04, using the viewer's tier
    and ownership. A contribute-tier member sees the roster read-only. Comment that the server
    enforces the same rule and that this is presentation, not protection.

    Show pending invitees distinctly — someone invited but not yet signed up has no user id yet.
    Comment that migration 136's claim bridge fills that in automatically the moment they sign up, so
    the state resolves itself and needs no manual reconciliation.

    Show the writer and singer badges beside each person, matching the pad's badge vocabulary so one
    person reads the same in both places.

    Write a `renderToStaticMarkup` suite: the two groupings render with their explanatory line; the
    add form carries exactly a name field, an email field and a tier choice; the writer promotion is
    outside the add form; a contribute-tier viewer sees no add form and no promote control; the
    markup contains no percentage input; a pending invitee renders its own state.
  </action>
  <verify>
    <automated>npx jest components/catalogue/WorkRoster.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npx jest</automated>
  </verify>
  <done>Membership and splits render as two distinct groupings with the plain-words explanation; the add form matches the existing invite shape and surfaces the returned link; writer promotion is a separate action reported in words; a contribute-tier viewer sees the roster read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roster UI → membership mutation | The interface that decides who may write into someone else's song |
| collaborator-entered names → rendered chips and headings | Free text from another account renders in the owner's header |
| viewer tier → visible controls | What a contribute-tier member is shown they may do |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-65 | Elevation of Privilege | A contribute-tier member using a hidden control | high | mitigate | The controls are gated by `canManageMembership()` for presentation, and plan 05's route independently requires the administer tier — the component's comment states plainly that hiding is not protecting. |
| T-37-66 | Elevation of Privilege | A member acquiring a share by being added | high | mitigate | Writer promotion is a separate control on an existing member, never a field in the add form, so the two decisions cannot be made in one accidental click. |
| T-37-67 | Tampering | The product proposing a contribution-weighted split | high | mitigate | No percentage input or suggestion exists anywhere in these components, and the suite asserts the markup contains none. Promotion outcomes are reported in words. |
| T-37-68 | Tampering | XSS through a collaborator-entered name in a chip or heading | medium | mitigate | React's default JSX escaping covers every rendered string; no raw-HTML injection is used. |
| T-37-69 | Information Disclosure | An invite link shown to the wrong viewer | medium | mitigate | The link renders only in the success state of an add performed by a viewer who passed the administer gate on both the client and the server. |
| T-37-70 | Repudiation | An inherited singer credit read as a performance record | medium | mitigate | The header states that sections inherit unless tagged and folds the guardrail behind LearnWhy: a default fills the plan, never the record, and the human-take registry requires an actual take. |
| T-37-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan — zero new packages in Phase 37.1. |
</threat_model>

<verification>
- `npx jest components/catalogue` green.
- `npx tsc --noEmit` reports 0 errors.
- `npm run lint` clean (the script already carries `--max-warnings=0`).
- `npx jest` full suite green — no regression against the current baseline (288 test files).
- Component tests use `renderToStaticMarkup`; this repo runs jest in a node environment with no jsdom.
- **DO NOT run `npm run build`.** The owner keeps a dev server on :3000 and a production build clobbers `.next` underneath it. Use `npx tsc --noEmit`.
</verification>

<success_criteria>
- The title renames live and the diary records it; the three vocal states are all reachable and instrumental visibly changes the page.
- The roster shows membership and splits as two facts, with the difference said in plain words.
- Adding a collaborator uses one invite shape and always yields a usable link.
- Writer promotion is separate, explicit, and reported without a number.
- Membership controls are invisible to a contribute-tier viewer and enforced independently on the server.
</success_criteria>

<output>
Create `.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-11-SUMMARY.md` when done.
Every commit message explains WHY and ends with:
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
</output>
