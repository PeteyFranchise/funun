---
phase: 37-the-catalogue-unreleased-works-as-living-assets-versions-rig
plan: 13
type: execute
wave: 5
depends_on: [37-12]
files_modified:
  - components/catalogue/WorkCard.tsx
  - components/catalogue/WorkCard.test.tsx
  - components/catalogue/CatalogueShelf.tsx
  - components/catalogue/CatalogueShelf.test.tsx
  - app/(artist)/vault/page.tsx
  - app/(artist)/vault/new/page.tsx
autonomous: false
requirements: [S-03, S-01, S-02, S-04]
user_setup:
  - service: browser-devices
    why: "MediaRecorder playback compatibility could not be verified from research alone (37-RESEARCH assumption A2, Open Question 3) — only a real device pass answers it"
    dashboard_config:
      - task: "Record a hum on Chrome desktop and play it back on iPhone Safari, then record on iPhone Safari and play back on Chrome desktop"
        location: "The owner's own devices against the deployed app"

must_haves:
  truths:
    - "The Sound Vault is two shelves — My Catalogue and Releases — under one roof (S-03, the IA decision)"
    - "The Releases shelf is untouched: same query, same cards, same counts, same empty state (S-03)"
    - "The catalogue shelf lists works the artist owns, works they are a member of, and any legacy project typed unreleased — with no data migration"
    - "The create flow is two doors — start a song and start a release — and the unreleased type no longer appears as a choice"
    - "The unreleased type is retired from the picker ONLY; the database CHECK constraint is unchanged and the existing prod row still validates (RESEARCH Pitfall 4)"
    - "Start a song creates the work and lands the artist in the composer room"
    - "Start a release opens the existing four-type form and posts where it always did"
    - "The owner has verified hum record-and-playback across Chrome desktop and iPhone Safari before the phase is called shippable (researcher Open Question 3)"
  artifacts:
    - "components/catalogue/WorkCard.tsx"
    - "components/catalogue/CatalogueShelf.tsx"
    - "app/(artist)/vault/page.tsx"
    - "app/(artist)/vault/new/page.tsx"
  key_links:
    - "The catalogue shelf's owned and member queries mirror the existing owned/shared split already in app/(artist)/vault/page.tsx"
    - "The 🎵 door posts to plan 05's POST /api/works and redirects to plan 12's /vault/works/[workId]"
    - "The 🚀 door keeps posting to the existing /api/vault route with the four remaining types"
    - "A legacy project typed unreleased surfaces on the catalogue shelf and still links to its existing project room"
  prohibitions:
    - "MUST NOT change the Releases shelf's query, its card component, its counts, or its empty state"
    - "MUST NOT alter `vault_projects.type`'s CHECK constraint or migrate any existing project row"
    - "MUST NOT add a My Catalogue entry to the artist nav — it is a shelf inside the vault, and the rail stays tight (the IA decision)"
    - "MUST NOT render the sketch-004 destination doors, a labels filter, or a volume/table view — all 37.2"
    - "MUST NOT use raw hex colours, and MUST NOT spend a second gradient on the vault page"
    - "MUST NOT run `npm run build`"
---

<objective>
Give the catalogue its real home, replace the five-type picker with two doors, and put the phase in
front of the owner's own devices.

Implements the information-architecture decision recorded in
`.planning/deliberations/the-catalogue-unreleased-works.md` — one roof, two shelves; two doors
replacing the five types; "Song" as the face and "work" as the backbone.

`WorkCard.tsx` and `CatalogueShelf.tsx` are the new shelf. `app/(artist)/vault/page.tsx` gains the
shelf above the existing Releases grid. `app/(artist)/vault/new/page.tsx` becomes the two-door
picker. The final task is the owner's blocking device test.

Purpose: the junk-drawer checkbox that started this entire design is replaced by the thing it was
always trying to be. And the one question research could not answer from documentation — whether a
hum recorded in one browser plays back in another — is answered the only way it can be, on real
devices, before the owner, Thomas and Eric start writing real songs in production.

Output: two components, two modified pages, and one verified device matrix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-CONTEXT.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-RESEARCH.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-05-SUMMARY.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-09-SUMMARY.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-12-SUMMARY.md
@.planning/deliberations/the-catalogue-unreleased-works.md
@app/(artist)/vault/page.tsx
@app/(artist)/vault/new/page.tsx
@components/vault/VaultProjectCard.tsx
@types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: The My Catalogue shelf — WorkCard, CatalogueShelf, and the vault page</name>
  <files>components/catalogue/WorkCard.tsx, components/catalogue/WorkCard.test.tsx, components/catalogue/CatalogueShelf.tsx, components/catalogue/CatalogueShelf.test.tsx, app/(artist)/vault/page.tsx</files>
  <action>
    Build `WorkCard.tsx` as the catalogue's card, deliberately lighter than
    `components/vault/VaultProjectCard.tsx`. A work has no artwork, no release date, no distributor
    and no ISRC — comment that, because the temptation is to reuse the release card and end up
    showing an artist four empty gates on a song that is three days old. The card shows: the title,
    the version count with the latest version's numeral, the block count or a lyrics indicator, the
    contributor avatars, the splits state as a word, and a relative last-activity time. When the row
    is a legacy project typed unreleased, render the same card shape with a small marker and link to
    its existing project room instead of the composer.

    Comment the readiness decision for this phase: the doctrine's rights-readiness scorecard is scope
    item 1 and is NOT in 37.1 — no score ring appears on a work card yet, and the slot is left for it
    rather than filled with a release-readiness number that would be meaningless here.

    Build `CatalogueShelf.tsx` as the section: the heading, a short subtitle in the artist-side
    possessive voice, the grid, and an empty state that repeats the pitch — start a song with a hum,
    thirty seconds of melody makes it real and provably yours — with the 🎵 door as its action.
    Comment that the shelf is where a catalogue-heavy producer effectively lives, per the IA
    decision that the vault may land on whichever shelf was used last (the last-used memory itself
    is 37.2; note that so the omission is deliberate).

    Modify `app/(artist)/vault/page.tsx` with the smallest possible diff. Add, in the same
    `Promise.all` the page already runs, two more queries mirroring the owned/shared pattern already
    there: works owned by the viewer, and work memberships for the viewer resolving to works they are
    on. Then filter the existing project list for rows typed unreleased and pass those to the shelf
    alongside the works. Comment RESEARCH Pitfall 4 at that filter: the merge happens in application
    code, not SQL, precisely so no data migration and no CHECK-constraint change is needed, and the
    one existing production row typed unreleased surfaces on the new shelf untouched.

    Render the catalogue shelf ABOVE the Releases grid and leave everything below it exactly as it is
    — same cards, same counts, same subtitle, same shared-with-me section. Comment S-03's constraint
    verbatim in substance: Releases untouched. The only permitted change to the existing Releases code
    path is excluding unreleased-typed rows from its grid if they currently appear there, and if they
    do, say so in the comment.

    Update the page's top-line subtitle so it describes two shelves rather than only releases, and
    keep the existing New project action pointing at the create route, which becomes the two doors in
    task 2.

    Handle demo mode the way the page already does: the demo path has no works, so the shelf renders
    its empty state rather than erroring. Comment it.

    Write `renderToStaticMarkup` suites for both components: the card renders a title, a version count
    and a splits word, and renders no score ring and no release-only field; a legacy unreleased row
    renders its marker and its project link; the shelf renders its grid for a populated list and the
    hum pitch for an empty one; no raw hex appears.
  </action>
  <verify>
    <automated>npx jest components/catalogue/WorkCard.test.tsx components/catalogue/CatalogueShelf.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>grep -c 'CatalogueShelf' 'app/(artist)/vault/page.tsx' | grep -qvx 0</automated>
    <automated>grep -c 'VaultBrowser' 'app/(artist)/vault/page.tsx' | grep -qvx 0</automated>
  </verify>
  <done>The vault page renders My Catalogue above an unchanged Releases grid; the shelf lists owned works, member works and legacy unreleased projects merged in application code; the card omits release-only fields and the readiness ring; demo mode renders the empty state.</done>
</task>

<task type="auto">
  <name>Task 2: The two doors — app/(artist)/vault/new/page.tsx</name>
  <files>app/(artist)/vault/new/page.tsx</files>
  <action>
    Replace the five-type picker with the two-door question the IA decision specifies. The page asks
    "What are you starting?" and offers two large doors: 🎵 Start a song, which leads to My Catalogue,
    and 🚀 Start a release, which leads to the existing single, snippet, EP and album flow.

    Comment the decision and its history at the top of the file: the unreleased PROJECT TYPE retires
    from the create flow because it was the junk-drawer checkbox that started this entire design, and
    it is replaced by the thing it was always trying to be. Note the naming rule alongside it —
    "Song" is the face and "work" is the backbone, so the button says start a song while split
    sheets, registration and doctrine copy keep saying work where precision matters.

    The 🎵 door collects at most a title (optional — comment that the empty state is the pitch and a
    song can start nameless), posts to plan 05's POST /api/works, and redirects to
    /vault/works/[workId]. On failure, surface the error in the existing style rather than silently
    staying put.

    The 🚀 door reveals the existing form unchanged in behaviour, minus the unreleased option: the
    same title, type, genre and target-release fields, posting to the same /api/vault route. Comment
    RESEARCH Pitfall 4 here too: this is a UI and routing change only, and `vault_projects.type`'s
    CHECK constraint keeps accepting unreleased so existing rows continue to validate.

    Keep the page's back link and cancel affordance. Spend the single gradient on the door the artist
    is most likely to want — the song door, per the phase's own priority — and use the plain treatment
    for the release door.

    Make the two doors keyboard-reachable and labelled, matching the accessibility level the rest of
    this codebase's forms hold.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>grep -c "'/api/works'" 'app/(artist)/vault/new/page.tsx' | grep -qvx 0</automated>
    <automated>grep -c "'/api/vault'" 'app/(artist)/vault/new/page.tsx' | grep -qvx 0</automated>
    <automated>npx jest</automated>
  </verify>
  <done>The create page asks one question and offers two doors; the song door creates a work and lands in the composer; the release door keeps the existing four-type form and endpoint; the unreleased option no longer renders anywhere in the picker and no schema change was made.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Owner device test — hum record and playback across Chrome desktop and iPhone Safari</name>
  <files>lib/catalogue/hum-capture.ts, components/catalogue/HumCaptureButton.tsx</files>
  <action>
    STOP — owner action required. This is the phase's shippability gate and it exists because
    37-RESEARCH.md could not close it from documentation. Assumption A2 (that Safari can play back
    WebM audio recorded elsewhere) came from search results that historically conflate WebM video
    support with WebM audio support, and the researcher flagged it explicitly as Open Question 3:
    needs a real device test before shipping.

    Nothing in the code can be changed to pre-empt this. The runtime codec pick already does the right
    thing on the RECORDING side — it asks the browser rather than sniffing it. What no test in this
    repo can answer is whether a take recorded in one browser plays back in another, which is exactly
    the situation a shared diary creates the moment a collaborator opens a song on a different device.

    If a gap is found, the fix is a follow-up, not a blocker on the rest of the phase: candidate
    reordering, a per-browser preferred type, or a server-side transcode step. Record what was
    observed either way.
  </action>
  <what-built>The whole 37.1 slice is in place: the two-shelf vault, the two-door create flow, the composer room with its diary, the lyrics pad, hum capture and file upload, the AI-entry flow with its receipts, and the shared roster. Hum capture picks its codec by asking the browser at runtime and falls back to file upload when nothing is supported. What has NOT been verified is cross-browser playback of a recorded take.</what-built>
  <how-to-verify>
    1. On Chrome desktop, open a song, record a short hum, and confirm it appears in the diary with a
       play control that plays back in place.
    2. On iPhone Safari, open the SAME song and play that Chrome-recorded take. This is the assertion
       that matters most — it is the one research could not answer.
    3. On iPhone Safari, record a hum on that song and confirm it plays back on the phone.
    4. Back on Chrome desktop, play the Safari-recorded take.
    5. Confirm the microphone indicator goes off on both devices as soon as each take ends, and again
       after cancelling a take mid-recording.
    6. Decline the microphone permission once on each device and confirm you get an inline message
       with a way forward rather than a dead button.
    7. While you are in there, walk the slice end to end once on each device: start a song from the 🎵
       door, write two sections in the pad, drag one above the other and confirm the numbers swap,
       copy the full lyric in both flavours, add an audio file, answer the AI question, and read the
       receipt. Confirm the diary recorded every one of those without you asking it to.
    8. Record which browser produced which format and whether any playback failed — that observation
       is the deliverable, whatever it says.
  </how-to-verify>
  <verify>Owner confirms a hum recorded on Chrome desktop plays back on iPhone Safari and vice versa, or reports precisely which direction failed and on what OS version.</verify>
  <done>The record-and-playback matrix is verified on the owner's own devices in both directions, the microphone releases correctly on both, and the permission-denied path degrades usefully. Any gap is recorded with the browser and OS version so the follow-up is a known, scoped fix rather than a mystery.</done>
  <resume-signal>Type "device test passed" if both directions play back, or describe exactly which direction failed and on which browser and OS version.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault page → two independent listing queries | The owned and member reads that decide what an artist sees on their own shelf |
| create flow → two different creation endpoints | One picker now writes to two different tables |
| recorded audio → another person's browser | A shared diary means one artist's device produces bytes another artist's device must play |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-78 | Information Disclosure | The catalogue shelf listing a work the viewer is not on | high | mitigate | The shelf's two queries mirror the page's existing owned/shared split and run under the viewer's own session, so migration 136's policies are the enforcement; a membership query returning ids is intersected with works the same policies already allow. |
| T-37-79 | Tampering | The Releases shelf changing behaviour as collateral | medium | mitigate | The plan's prohibition names the Releases query, card, counts and empty state as untouchable, and the diff is additive above the existing grid. The verify gate asserts the existing browser component is still mounted. |
| T-37-80 | Tampering | Retiring the unreleased type breaking existing rows | high | mitigate | The retirement is a picker change only. No migration alters `vault_projects.type`, and the legacy row surfaces through an application-code filter rather than a data migration (RESEARCH Pitfall 4). |
| T-37-81 | Denial of Service | An unplayable recording format silently breaking the shared diary | medium | mitigate | The blocking device test in task 3 is the control. The runtime codec pick already avoids the recording-side failure; the playback-side answer is empirical and is gated before the phase is called shippable. |
| T-37-82 | Information Disclosure | Microphone left live after a take on a real device | high | mitigate | Enforced in code (plan 09) and verified on hardware in step 5 of the device test, including the cancel path that unit tests cannot exercise. |
| T-37-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan — zero new packages in Phase 37.1. |
</threat_model>

<verification>
- `npx jest components/catalogue` green.
- `npx tsc --noEmit` reports 0 errors.
- `npm run lint` clean (the script already carries `--max-warnings=0`).
- `npx jest` full suite green — no regression against the current baseline (288 test files).
- The owner's device matrix is confirmed at the task 3 checkpoint.
- **DO NOT run `npm run build`.** The owner keeps a dev server on :3000 and a production build clobbers `.next` underneath it. Use `npx tsc --noEmit`.
</verification>

<success_criteria>
- The Sound Vault shows two shelves, and Releases behaves exactly as it did before.
- Legacy unreleased projects appear on the catalogue shelf with no data migration and no schema change.
- One question, two doors — and the song door lands the artist in the composer room.
- Hum record-and-playback is verified in both directions on the owner's own devices, or the exact gap is recorded.
- No 37.2 surface (destination doors, labels, volume view) has leaked into 37.1.
</success_criteria>

<output>
Create `.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-13-SUMMARY.md` when done.
Every commit message explains WHY and ends with:
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
</output>
