---
phase: 37-the-catalogue-unreleased-works-as-living-assets-versions-rig
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/135_works_core.sql
  - supabase/migrations/136_work_members.sql
  - supabase/migrations/137_split_sheets_work_link.sql
  - supabase/migrations/138_work_diary_events.sql
  - __tests__/migration-135.test.ts
  - __tests__/migration-136.test.ts
  - __tests__/migration-137.test.ts
  - __tests__/migration-138.test.ts
autonomous: false
requirements: [S-01, S-02, S-03, S-04]
user_setup:
  - service: supabase
    why: "Migrations 135–138 must be applied to prod by the owner — an agent never runs `supabase db push` on this project"
    dashboard_config:
      - task: "Review 135–138, run `supabase db push` once (all four together), then confirm `supabase migration list` shows LOCAL=REMOTE through 138"
        location: "Owner's local terminal against the prod Supabase project"

must_haves:
  truths:
    - "A composition (`works`) exists as its own entity, separate from `vault_projects` — with a title, a three-state vocal setting, and a 37.2 graduation seam, and nothing about release readiness (S-03)"
    - "A work accumulates recordings (`work_versions`) whose vN numeral is NEVER stored — only `created_at` ordering exists to derive it from (RENUMBERING RULE, RESEARCH Pitfall 5)"
    - "A work accumulates structure blocks (`lyric_blocks`) whose section numeral is NEVER stored, whose authorship binds to the row id, and whose repeats are a self-FK link rather than a duplicated row (REPEAT RULE)"
    - "AI contributions (`ai_entries`) are zero-split rows in DDEX component vocabulary, attached at work level or version level, each carrying a plain-words citation and an optional pointer to the human take it cites (CAT-Q3)"
    - "Work membership (`work_members`) can exist for a person who has not signed up yet, and is backfilled the moment `collaborators.claimed_by` is set — the one verified-identity signal in this codebase (S-02)"
    - "Every diary-worthy mutation lands in `work_diary_events` from a database trigger, so capture never depends on which route performed the write (CAT-Q1)"
    - "`work_diary_events` is INSERT-only to clients — no UPDATE or DELETE path exists for authenticated or anon, which is what makes a citation auditable after the fact"
    - "A work's living split sheet is reachable via `split_sheets.work_id` and there is no reverse column on `works` (researcher Open Question 1, resolved: sheet→work only)"
    - "`vault_projects.type`'s CHECK constraint is untouched, so the one existing prod project typed `unreleased` still validates (RESEARCH Pitfall 4)"
    - "Migrations 135–138 are applied to prod by the owner in one push, before any wave-2 plan runs"
  artifacts:
    - "supabase/migrations/135_works_core.sql"
    - "supabase/migrations/136_work_members.sql"
    - "supabase/migrations/137_split_sheets_work_link.sql"
    - "supabase/migrations/138_work_diary_events.sql"
    - "__tests__/migration-135.test.ts"
    - "__tests__/migration-136.test.ts"
    - "__tests__/migration-137.test.ts"
    - "__tests__/migration-138.test.ts"
  key_links:
    - "`work_member_tier()` is created in 136 and is the single authorization primitive every wave-2 API route calls through `lib/catalogue/access.ts` (plan 04)"
    - "`reorder_lyric_blocks()` is created in 138 and is the only writer of `lyric_blocks.position` in bulk — plan 07's reorder route calls it via the service role"
    - "`split_sheets.work_id` is written by plan 05's POST /api/works at work-creation time and read by plan 12's work page to resolve the living draft"
    - "The `capture_*` triggers are what plan 10's DiaryFeed renders and what plan 04's `lib/catalogue/diary.ts` types"
    - "`collaborators.claimed_by` is the trigger source that backfills `work_members.user_id`, exactly as migration 079 does for `project_members`"
  prohibitions:
    - "MUST NOT add `works.split_sheet_id` — Open Question 1 is resolved in favour of the sheet-side FK only (this is a deliberate choice, not an oversight)"
    - "MUST NOT add a labels column to `works` or `work_versions` — Open Question 2 is resolved: labels DEFER to 37.2 (additive later, no backfill, no 37.1 surface consumes them)"
    - "MUST NOT alter `vault_projects.type`'s CHECK constraint — `unreleased` retires from the create-flow UI only (RESEARCH Pitfall 4)"
    - "MUST NOT store a version numeral or a block numeral column — both are derived at read time (RESEARCH Pitfall 5)"
    - "MUST NOT widen the `track-audio` storage.objects RLS policies from migration 004 — access is gated at the API-route level (RESEARCH Pitfall 2)"
    - "MUST NOT create any UPDATE or DELETE policy on `work_diary_events`, and MUST NOT grant those verbs to authenticated or anon"
    - "MUST NOT add an INSERT/UPDATE/DELETE grant on `work_members` to authenticated or anon — every membership write goes through a service-role route (migration 078(b) posture)"
    - "An executor agent MUST NOT run `supabase db push` — the push is the owner's blocking checkpoint"
    - "MUST NOT run `npm run build`"
---

<objective>
Author the entire database layer of Phase 37.1 — four migrations and their four text-lock
tests — then hand the set to the owner for one human-gated `supabase db push`.

135 creates the composition layer (`works`, `work_versions`, `lyric_blocks`, `ai_entries`)
with RLS enabled and deliberately no policies. 136 creates `work_members`, the
`is_work_owner`/`work_member_tier` SECURITY DEFINER helper pair, every policy for all five
tables, and the claimed-collaborator bridge trigger. 137 links a work to its living split
sheet with one nullable column. 138 creates the diary and the triggers that fill it, plus
the atomic block-reorder RPC that emits its own single diary event.

Purpose: everything in waves 2 through 5 reads a table or calls a function that does not
exist until this push lands, and this project's TypeScript types come from config rather
than from the live database — so downstream verification would be a false positive without
it. The push is a gate, not a formality.

Output: four migration files, four text-lock test files, applied to prod as one push.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-CONTEXT.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-RESEARCH.md
@.planning/deliberations/the-catalogue-unreleased-works.md
@supabase/migrations/078_project_members.sql
@supabase/migrations/079_project_membership_auto.sql
@supabase/migrations/013_readiness_activity_trigger.sql
@supabase/migrations/127_atomic_checklist_reorder.sql
@supabase/migrations/067_split_sheet_song_attachment.sql
@supabase/migrations/134_handle_format_and_backfill.sql
@__tests__/migration-134.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migrations 135 (composition core) and 136 (membership + RLS)</name>
  <files>supabase/migrations/135_works_core.sql, supabase/migrations/136_work_members.sql</files>
  <action>
    135 is the next free number — 134 is the highest existing. Write both files in this task
    because they are one logical unit split for reviewability: 135 creates tables with RLS
    enabled and NO policies, 136 creates the helper pair those policies must call. Say that
    explicitly in 135's header and again in 136's: the two files are pushed together in a
    single checkpoint, so there is never a live window in which the core tables are enabled
    but unreachable. Splitting the other way — policies in 135 — is impossible, because a
    policy body cannot call `work_member_tier()` before `work_members` exists.

    **Both headers.** Follow migration 134's header shape. Carry, verbatim in substance:
    the standing line that an executor agent must NEVER run `supabase db push` on this
    project and that the live push is a human-gated checkpoint; the RLS doctrine paragraph;
    and the UUID note from migration 078's header — every primary key default here is
    `gen_random_uuid()`, never `uuid_generate_v4()`, because uuid-ossp lives in the
    `extensions` schema and is not on the migration session's search path.

    **135 — section 1, `public.works`.** Columns: `id UUID PRIMARY KEY DEFAULT
    gen_random_uuid()`; `user_id UUID REFERENCES auth.users ON DELETE CASCADE NOT NULL`
    (the creator and default owner); `title TEXT NOT NULL DEFAULT 'Untitled'`; `vocal_state
    TEXT NOT NULL DEFAULT 'primary' CHECK (vocal_state IN ('primary','varies',
    'instrumental'))`; `primary_performer JSONB` (nullable; shape
    `{kind,collaborator_id,user_id,name}`); `graduated_project_id UUID REFERENCES
    public.vault_projects ON DELETE SET NULL` (the 37.2 seam, written by nothing in 37.1);
    `created_at TIMESTAMPTZ DEFAULT NOW()`; `updated_at TIMESTAMPTZ DEFAULT NOW()`. Index on
    `user_id`. Attach migration 001's existing `update_updated_at()` trigger function BEFORE
    UPDATE (migration 028's header records that reuse convention — do not define a second
    copy of it).

    Comment the three-state `vocal_state` against the DEFAULT-PERFORMER RULE: `instrumental`
    is not cosmetic — it makes every who-sings prompt disappear, makes the Crate vocal check
    pass by construction, and makes DDEX omit vocal performer roles.

    Comment the two columns that are deliberately ABSENT and why, so a later reader does not
    read them as oversights. First: there is no reverse pointer from a work to its split
    sheet. The researcher's Open Question 1 is resolved in favour of the sheet-side FK only
    (137) — it matches the existing `split_sheets.vault_project_id` / `track_id` direction
    from migration 067, and it turns work creation into one insert per row instead of an
    insert-then-update. A work's living sheet is resolved by selecting from `split_sheets`
    where the work matches and status is `draft`. Second: there is no artist-labels column.
    Open Question 2 is resolved as DEFER to 37.2 — a plain TEXT array is cheap to add later
    against a table that will still have few rows, and 37.1 ships no surface (the volume
    view is deferred) that would consume it.

    **135 — section 2, `public.work_versions`.** Columns: `id` (as above); `work_id UUID
    REFERENCES public.works ON DELETE CASCADE NOT NULL`; `user_id UUID REFERENCES auth.users
    NOT NULL` — whoever created THIS version, which may be a collaborator rather than the
    work owner; `source TEXT NOT NULL CHECK (source IN ('hum','upload'))`; `audio_path TEXT
    NOT NULL`; `audio_ext TEXT NOT NULL`; `audio_size BIGINT`; `duration_seconds INTEGER`;
    `label TEXT` (optional artist free text such as "acoustic take"); `performers JSONB NOT
    NULL DEFAULT '[]'` (declared per-recording credits, the authoritative ones per the
    PERFORMER RULE); `created_at TIMESTAMPTZ DEFAULT NOW()`. Composite index on
    (`work_id`, `created_at`).

    Comment `audio_path`'s convention and its reason: the path is `{work_id}/{version_id}.
    {ext}` in the existing `track-audio` bucket, with NO owner-id prefix. RESEARCH Pitfall 2
    is the argument — every real upload and signed-URL read in this codebase already goes
    through the service-role client, and migration 004's owner-folder storage policies were
    never widened for shared projects when Phase 21 shipped. An owner-id prefix would make
    the (currently inert) defense-in-depth layer reject a legitimate collaborator upload the
    moment anyone exercised it directly. This migration does not widen those storage
    policies; access is gated at the API route via `work_member_tier()`.

    State clearly that no version numeral column exists and none may be added: vN is derived
    at read time by row-number over `created_at` within a work (RESEARCH Pitfall 5).

    **135 — section 3, `public.lyric_blocks`.** Columns: `id`; `work_id ... ON DELETE
    CASCADE NOT NULL`; `block_type TEXT NOT NULL CHECK (block_type IN ('verse','pre_chorus',
    'chorus','bridge','intro','outro','hook','custom'))`; `custom_label TEXT` (only
    meaningful when the type is custom); `position INTEGER NOT NULL`; `text TEXT NOT NULL
    DEFAULT ''`; `author_kind TEXT NOT NULL DEFAULT 'human' CHECK (author_kind IN ('human',
    'ai'))`; `author_user_id UUID REFERENCES auth.users`; `performers JSONB NOT NULL DEFAULT
    '[]'`; `repeat_of_block_id UUID REFERENCES public.lyric_blocks ON DELETE SET NULL`;
    `created_at`; `updated_at`. Composite index on (`work_id`, `position`). Attach
    `update_updated_at()`.

    Comment the three rules this shape encodes. RENUMBERING RULE: `position` is the absolute
    drag order and the ONLY ordering fact stored; "Verse 2" is derived from position among
    same-type siblings at read time, so dragging a verse renumbers instantly and authorship —
    which binds to the row id — cannot smudge. Custom sections never renumber. REPEAT RULE:
    `repeat_of_block_id` is a link, not a copy — a repeated chorus displays the source text
    and inherits the source's author, and "detach to vary" is copy-on-write that clears this
    column and gives the row its own authorship from that point. PERFORMER RULE: `author_user_id`
    is the automatic writer badge (it moves splits); `performers` is the declared singer
    cluster (it moves credits, never splits) and a blank one inherits the work's
    `primary_performer` — an inherited badge fills the PLAN, never the RECORD.

    **135 — section 4, `public.ai_entries`.** Columns: `id`; `work_id ... ON DELETE CASCADE
    NOT NULL`; `level TEXT NOT NULL CHECK (level IN ('work','version'))`; `version_id UUID
    REFERENCES public.work_versions ON DELETE CASCADE`; `block_id UUID REFERENCES
    public.lyric_blocks ON DELETE SET NULL`; `component TEXT NOT NULL CHECK (component IN
    ('vocal','instrument','lyric','melody','full'))`; `mode TEXT NOT NULL CHECK (mode IN
    ('performance','generate'))`; `citation TEXT NOT NULL`; `human_source_version_id UUID
    REFERENCES public.work_versions ON DELETE SET NULL`; `created_by UUID REFERENCES
    auth.users NOT NULL`; `created_at`. Table-level CHECK: level `version` requires a
    version, level `work` requires none. Index on `work_id`.

    Comment against CAT-Q3: AI entries carry zero splits by construction — there is no
    percentage column and none may be added. `mode` is the swap-versus-generate distinction:
    `performance` is a tool singing or playing something a human wrote (ownership untouched),
    `generate` is the tool inventing material (owned by no one). `human_source_version_id` is
    the when-in-doubt rule made structural — the maximal-ownership citation is only offered
    when it can point at a real diary-anchored take, so the label stays TRUE.

    **135 — section 5.** `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` on all four tables, and
    a comment stating that policies are deliberately deferred to 136 for the reason given in
    the header. End the file with `NOTIFY pgrst, 'reload schema';` as the last statement.

    **136 — section 1, `public.work_members`.** Columns: `id`; `work_id UUID REFERENCES
    public.works ON DELETE CASCADE NOT NULL`; `user_id UUID REFERENCES auth.users ON DELETE
    CASCADE` (set immediately for the owner, NULL until an invitee claims); `collaborator_id
    UUID REFERENCES public.collaborators ON DELETE SET NULL` (NULL only for the owner's own
    row); `tier TEXT NOT NULL CHECK (tier IN ('contribute','administer'))`; `added_by UUID
    REFERENCES auth.users`; `created_at`. Partial unique index on (`work_id`,`user_id`) where
    user_id is not null; partial unique index on (`work_id`,`collaborator_id`) where
    collaborator_id is not null; plain indexes on `work_id` and on `user_id`.

    Enable RLS, then `REVOKE INSERT, UPDATE, DELETE ON public.work_members FROM authenticated,
    anon;` — migration 078(b)'s exact posture. Comment why: membership decides who can write
    audio and lyrics into someone else's song, so it is never writable over raw PostgREST;
    every write goes through a service-role API route that has already proved the caller's
    tier.

    **136 — section 2, the helper pair.** Create `public.is_work_owner(p_work_id UUID, p_uid
    UUID) RETURNS BOOLEAN LANGUAGE sql STABLE SECURITY DEFINER SET search_path = ''` selecting
    an EXISTS over `public.works`, and `public.work_member_tier(p_work_id UUID, p_uid UUID)
    RETURNS TEXT` with the same modifiers selecting `tier` from `public.work_members` for that
    work and user. Follow each with `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated;`
    then `GRANT EXECUTE ... TO authenticated;` — migration 078's exact wording.

    Comment the reason this pair exists at all, copying migration 078's own argument: a naive
    pair of cross-table EXISTS subqueries between `works` and `work_members` recurses at
    Postgres REWRITE time with 42P17 — the bug migration 018 hit and migrations 064 and 078
    both had to fix. It is shipped from day one here rather than rediscovered in production.
    Add a COMMENT ON FUNCTION for each, in the style of migration 064's, stating they are
    intended for RLS USING clauses wrapped as a scalar subselect, not as client RPCs.

    **136 — section 3, the policies.** Every policy body wraps a helper call as a scalar
    subselect. Create:
    - on `work_members`, SELECT where the caller owns the work or the row is the caller's own;
    - on `works`: SELECT where owner or any tier; UPDATE where owner or any tier; INSERT with
      check that `user_id` is the caller; DELETE where owner only;
    - on `work_versions`, `lyric_blocks` and `ai_entries`: SELECT where the caller owns the
      parent work or holds any tier on it, and ALL (write) under the same condition.

    Comment the tier decision explicitly. In 37.1 both tiers may add versions, edit lyrics and
    file AI entries — that is what CONTRIBUTE means in the doctrine (play versions, add their
    own iterations, edit the pad, annotate). ADMINISTER is not a row-write distinction in this
    phase; it gates membership changes (enforced by the REVOKE above plus plan 05's route
    check) and, in 37.2, the money and release doors. Say that the 37.2 doors will need their
    own administer-only gate on `graduated_project_id`, and that leaving both tiers writable
    on the content tables today is deliberate rather than an omission.

    **136 — section 4, the claimed-collaborator bridge.** Create
    `public.sync_work_membership_on_claim() RETURNS TRIGGER LANGUAGE plpgsql SECURITY DEFINER
    SET search_path = ''` which updates `public.work_members` setting `user_id` to the newly
    claimed value for every row whose `collaborator_id` matches the claimed row and whose
    `user_id` is still NULL, then returns NEW. Drop and create the trigger AFTER UPDATE OF
    `claimed_by` ON `public.collaborators` FOR EACH ROW WHEN the new value is distinct from
    the old.

    Comment migration 079's finding as the reason this keys off `claimed_by` and nothing else:
    `split_sheet_parties.user_id` is read in three places in this codebase and written nowhere
    — a dead signal — while `collaborators.claimed_by` is set exclusively by
    `claim_collaborators()` on signup via a case-insensitive match against the signing-up
    user's Supabase-Auth-verified account email. Note the simplification versus 079: that
    migration needed three fire sites because a party can be linked before or after either
    side exists; a `work_members` row always carries `collaborator_id` at creation time
    (plan 05's route picks or creates a roster row first), so one fire site is sufficient.

    End 136 with `NOTIFY pgrst, 'reload schema';` as the last statement.
  </action>
  <verify>
    <automated>test -f supabase/migrations/135_works_core.sql && test -f supabase/migrations/136_work_members.sql</automated>
    <automated>grep -cE 'CREATE TABLE( IF NOT EXISTS)? public\.(works|work_versions|lyric_blocks|ai_entries)' supabase/migrations/135_works_core.sql | grep -qx 4</automated>
    <automated>grep -c 'gen_random_uuid()' supabase/migrations/135_works_core.sql | grep -qvx 0</automated>
    <automated>grep -c 'is_work_owner' supabase/migrations/136_work_members.sql | grep -qvx 0</automated>
    <automated>grep -c 'work_member_tier' supabase/migrations/136_work_members.sql | grep -qvx 0</automated>
    <automated>grep -c "REVOKE INSERT, UPDATE, DELETE ON public.work_members FROM authenticated, anon" supabase/migrations/136_work_members.sql | grep -qx 1</automated>
    <automated>grep -c "NOTIFY pgrst, 'reload schema';" supabase/migrations/135_works_core.sql | grep -qx 1</automated>
    <automated>grep -c "NOTIFY pgrst, 'reload schema';" supabase/migrations/136_work_members.sql | grep -qx 1</automated>
  </verify>
  <done>Both migration files exist. 135 creates the four composition tables with RLS enabled and no policies, reusing `update_updated_at()`. 136 creates `work_members` with the write REVOKE, the SECURITY DEFINER helper pair with their REVOKE/GRANT pair, every policy for all five tables, and the one-fire-site claim bridge. Both end with the schema reload.</done>
</task>

<task type="auto">
  <name>Task 2: Author migrations 137 (sheet↔work link) and 138 (the diary + atomic reorder)</name>
  <files>supabase/migrations/137_split_sheets_work_link.sql, supabase/migrations/138_work_diary_events.sql</files>
  <action>
    Both files carry the same header conventions as task 1's (human-gated push line, RLS
    doctrine paragraph).

    **137 — the sheet-side link, and nothing else.** `ALTER TABLE public.split_sheets ADD
    COLUMN IF NOT EXISTS work_id UUID REFERENCES public.works ON DELETE SET NULL;` plus
    `CREATE INDEX IF NOT EXISTS` on it. That is the whole schema change.

    Comment three things. First, the direction decision (researcher Open Question 1, resolved):
    the FK points from the sheet to the work, matching how migration 067 already attached a
    sheet to a project and to a track. There is no reverse column and there will not be one —
    a work's living draft is found by selecting from `split_sheets` where the work matches and
    status is `draft`, which is exactly as cheap as a stored id and avoids the insert-then-
    update dance at work-creation time.

    Second, that this migration deliberately does NOT touch `split_sheets` or
    `split_sheet_parties` RLS. That table pair is the most delicate in this codebase — migration
    064 exists solely to fix the recursion between their two policy sets, using the
    `is_split_sheet_initiator` / `is_split_sheet_party` SECURITY DEFINER helpers. Phase 37.1
    needs a work member to READ the living draft in order to render the splits state and the
    people-not-numbers nudge, and it gets that through a service-role read in a lib helper
    that has already proved work membership (plan 05's `lib/catalogue/splits-io.ts`), which is
    the same posture every signed-URL read in this codebase already takes. Adding a fourth
    policy to that pair to save one service-role call would be a bad trade.

    Third, that `vault_projects.type`'s CHECK constraint is untouched by this phase. `unreleased`
    retires from the create-flow UI only (plan 13); the value must keep validating because the
    one existing prod row typed that way surfaces on the new catalogue shelf without a data
    migration (RESEARCH Pitfall 4).

    End with the schema reload.

    **138 — section 1, `public.work_diary_events`.** Columns: `id UUID PRIMARY KEY DEFAULT
    gen_random_uuid()`; `work_id UUID REFERENCES public.works ON DELETE CASCADE NOT NULL`;
    `kind TEXT NOT NULL CHECK (kind IN ('version','lyric_edit','roster','sheet','ai_entry',
    'rename','reorder','detach','note'))`; `actor_user_id UUID REFERENCES auth.users`;
    `payload JSONB NOT NULL DEFAULT '{}'`; `created_at TIMESTAMPTZ DEFAULT NOW()`. Index on
    (`work_id`, `created_at DESC`).

    Enable RLS. Create ONE policy — SELECT, where the caller owns the work or holds any tier
    on it, using the helper pair from 136. Then `REVOKE INSERT, UPDATE, DELETE ON
    public.work_diary_events FROM authenticated, anon;`.

    Comment why the diary is read-only to every client: an entry is evidence. The citation on
    an AI entry and the timestamp on a hum are only worth something if nobody can go back and
    edit or remove them afterwards. Writes arrive from SECURITY DEFINER triggers (which run as
    the function owner and are unaffected by the revoke) and, for the single `note` kind, from
    a service-role route. Also record the anti-pattern this table exists to avoid, from
    RESEARCH Pitfall 1: `activity_events` is a PUBLIC wall feed with a closed four-value kind
    enum and a best-effort emitter that is explicitly allowed to swallow errors. Its render
    component is worth copying; its table and its emit function are not. Nothing in
    `app/api/works/**` may import `emitActivity`.

    **138 — section 2, the capture triggers.** Write one `SECURITY DEFINER ... SET search_path
    = ''` plpgsql function per source, each inserting exactly one row into
    `public.work_diary_events`, modelled on migration 013's trigger-emit precedent:
    - AFTER INSERT ON `work_versions` → kind `version`, actor is the row's `user_id`, payload
      carries the version id and its source.
    - AFTER INSERT ON `lyric_blocks` → kind `lyric_edit`, payload carries the block id, its
      type and an operation of `added`. AFTER UPDATE OF `text`, `block_type`, `custom_label`
      ON `lyric_blocks`, fired only when one of those actually changed → kind `lyric_edit`,
      operation `edited`. AFTER DELETE → kind `lyric_edit`, operation `removed`. AFTER UPDATE
      OF `repeat_of_block_id` fired only when the old value was non-null and the new value is
      null → kind `detach`.
    - AFTER INSERT ON `work_members` → kind `roster`, payload carries the member id, the tier
      and the collaborator id.
    - AFTER INSERT ON `ai_entries` → kind `ai_entry`, payload carries the entry id, level,
      component, mode and the citation string.
    - AFTER UPDATE OF `title` ON `works`, fired only when the title actually changed → kind
      `rename`, payload carries the old and the new title.
    - AFTER INSERT ON `split_sheet_parties` → kind `sheet`, but ONLY when the parent sheet's
      `work_id` is non-null. Look the sheet up inside the function and return early otherwise.
      Comment that this makes the trigger a strict no-op for every split sheet that exists
      today and for every non-work sheet created later, which is why it is safe to attach a
      new trigger to a table the split-sheet builder already writes on every save.

    Comment the whole section against CAT-Q1: auto-capture must never depend on discipline, so
    it lives at the database tier where it fires regardless of which route — including routes
    not yet written — performed the write. Note the one deliberate exception: kind `note` has
    no trigger, because a note is a free-standing annotation with no underlying row to trigger
    from; plan 06's notes route inserts it directly through the service role. That is the only
    app-authored diary kind and it must stay the only one.

    Note also that the lyric-edit trigger fires per saved UPDATE, not per keystroke — the pad
    debounces before it PATCHes, so section-level history is what lands ("Ben added Verse 2"),
    not a wall of "lyrics changed".

    **138 — section 3, `public.reorder_lyric_blocks(p_work_id UUID, p_order JSONB)`.** Model it
    directly on migration 127's `reorder_launchpad_checklist()`, adapted: `RETURNS INT LANGUAGE
    plpgsql SECURITY DEFINER SET search_path = ''`. Validate that the payload is a JSON array;
    cap its length at 200; require every element to be an object carrying a string `id` that
    parses as a uuid and a numeric `position`; reject duplicate ids; require the positions to
    be unique and contiguous from zero. Then `LOCK TABLE public.lyric_blocks IN SHARE ROW
    EXCLUSIVE MODE`, count the blocks belonging to `p_work_id`, and reject unless the payload
    names every one of that work's current blocks exactly once. Apply the new positions with a
    single set-based UPDATE joined against `jsonb_to_recordset`, scoped to `p_work_id`. Compare
    the resulting row count against the expected count and raise a serialization failure if
    they differ. Finish by inserting ONE `reorder` diary event for the work, then return the
    updated count.

    Comment why the reorder event is emitted from inside this function instead of from a
    per-row trigger on `position`: a set-based update of N rows would fire N times and bury the
    diary under one entry per block for a single drag. One drag is one event.

    Comment why the RPC lives in 138 rather than in 137, which is where RESEARCH.md sketched it:
    the function body inserts into `work_diary_events`, and placing it in the later file keeps
    the migration set free of forward references to a table that does not exist yet.

    Finish with `REVOKE ALL ON FUNCTION public.reorder_lyric_blocks(UUID, JSONB) FROM PUBLIC,
    anon, authenticated;` and `GRANT EXECUTE ... TO service_role;` — migration 127's exact
    posture, because the caller is plan 07's route, which has already proved work membership.

    End 138 with `NOTIFY pgrst, 'reload schema';` as the last statement.
  </action>
  <verify>
    <automated>test -f supabase/migrations/137_split_sheets_work_link.sql && test -f supabase/migrations/138_work_diary_events.sql</automated>
    <automated>grep -c 'ADD COLUMN IF NOT EXISTS work_id' supabase/migrations/137_split_sheets_work_link.sql | grep -qx 1</automated>
    <automated>grep -cE 'CREATE TABLE( IF NOT EXISTS)? public\.work_diary_events' supabase/migrations/138_work_diary_events.sql | grep -qx 1</automated>
    <automated>grep -c "REVOKE INSERT, UPDATE, DELETE ON public.work_diary_events FROM authenticated, anon" supabase/migrations/138_work_diary_events.sql | grep -qx 1</automated>
    <automated>grep -c 'reorder_lyric_blocks' supabase/migrations/138_work_diary_events.sql | grep -qvx 0</automated>
    <automated>grep -c 'LOCK TABLE public.lyric_blocks IN SHARE ROW EXCLUSIVE MODE' supabase/migrations/138_work_diary_events.sql | grep -qx 1</automated>
    <automated>grep -c "NOTIFY pgrst, 'reload schema';" supabase/migrations/137_split_sheets_work_link.sql | grep -qx 1</automated>
    <automated>grep -c "NOTIFY pgrst, 'reload schema';" supabase/migrations/138_work_diary_events.sql | grep -qx 1</automated>
  </verify>
  <done>137 adds exactly one nullable column plus its index and changes no RLS. 138 creates the diary table with a single SELECT policy and the write REVOKE, one capture function and trigger per source (including the sheet-party trigger that no-ops for non-work sheets), and the atomic reorder RPC that takes the table lock, validates contiguity, and emits exactly one reorder event. Both end with the schema reload.</done>
</task>

<task type="auto">
  <name>Task 3: Text-lock tests for migrations 135–138</name>
  <files>__tests__/migration-135.test.ts, __tests__/migration-136.test.ts, __tests__/migration-137.test.ts, __tests__/migration-138.test.ts</files>
  <action>
    Write all four in the established style of `__tests__/migration-134.test.ts`: read the SQL
    with `readFileSync`, build a comment-stripped view of the executable statements (drop every
    line whose trimmed form starts with the SQL comment marker) so "the migration does not do X"
    assertions cannot be defeated by prose, and copy that file's `normalizeWhitespace()` helper
    plus an unwrapped view of the header comments for the assertions about documented reasoning.
    These migrations are human-gated and never pushed by an agent, so these four files ARE the
    pre-push review evidence.

    **migration-135.test.ts.** Assert: all four tables are created; every primary key default is
    `gen_random_uuid()` and the string `uuid_generate_v4` appears nowhere; `works.vocal_state`'s
    CHECK names exactly the three states; `works` carries no column whose name ends in
    `split_sheet_id` (Open Question 1) and no labels/tags column (Open Question 2); neither
    `work_versions` nor `lyric_blocks` declares any column whose name contains `number` or
    `numeral` (Pitfall 5); `lyric_blocks.repeat_of_block_id` is a self-referencing FK with ON
    DELETE SET NULL; `ai_entries` declares no percentage or split column (CAT-Q3 zero-split) and
    carries the level/version table CHECK; RLS is enabled on all four tables and the file
    contains no policy-creation statement at all (with a header comment explaining that
    deferral); `update_updated_at` is referenced rather than redefined; the storage path
    convention is documented in a comment and the file contains no ALTER on `storage.objects`;
    the last non-empty statement is the schema reload.

    **migration-136.test.ts.** Assert: `work_members` exists with both partial unique indexes;
    the write REVOKE from authenticated and anon is present verbatim; both helper functions are
    declared `STABLE SECURITY DEFINER` with the empty search path and each is followed by a
    REVOKE-then-GRANT pair; every policy body that references a helper wraps the call as a
    scalar subselect (structural check: for each helper call site inside a USING or WITH CHECK
    clause, the character immediately preceding the function name resolves to a subselect
    opening, not a bare expression) — this is the 42P17 guard migration 078's header describes;
    policies exist for all five tables; the claim-bridge trigger fires AFTER UPDATE OF
    `claimed_by` on `collaborators` with a WHEN clause that requires the value to have changed;
    the bridge function updates only rows whose `user_id` is still null (idempotency); the file
    contains no INSERT, UPDATE or DELETE grant on `work_members` to authenticated or anon; the
    header documents that this file carries the policies for 135's tables and why; the last
    non-empty statement is the schema reload.

    **migration-137.test.ts.** Assert: exactly one ALTER TABLE statement and it targets
    `split_sheets`; it adds exactly one column, idempotently; the file contains no policy
    creation, no policy drop, and no reference to `split_sheet_parties`; the file contains no
    ALTER of `vault_projects` and no reference to the project-type CHECK constraint (Pitfall 4);
    the header documents the FK-direction decision and the deliberate non-change to the
    split-sheet RLS pair; the last non-empty statement is the schema reload.

    **migration-138.test.ts.** Assert: the diary table's kind CHECK lists exactly the nine kinds;
    exactly one policy exists on the table and it is a SELECT policy; the write REVOKE from
    authenticated and anon is present verbatim, and no UPDATE or DELETE policy exists; every
    capture function is declared SECURITY DEFINER with the empty search path; a trigger exists
    for each of the documented sources, and the update-driven ones carry a column list plus a
    change guard; the sheet-party trigger's function body tests the parent sheet's work link
    before inserting; the string `emitActivity` and the identifier `activity_events` appear
    nowhere in the file; `reorder_lyric_blocks` takes the SHARE ROW EXCLUSIVE table lock, raises
    on a non-contiguous or incomplete order, inserts exactly one diary row, and is granted to
    `service_role` only after a REVOKE from PUBLIC, anon and authenticated; the header documents
    the note-kind exception and the reason the reorder event is emitted from the function rather
    than a per-row trigger; the last non-empty statement is the schema reload.

    Every one of these four files must run green with no database connection — this repo has
    never run `supabase db push` from a test and must not start.
  </action>
  <verify>
    <automated>npx jest __tests__/migration-135.test.ts __tests__/migration-136.test.ts __tests__/migration-137.test.ts __tests__/migration-138.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All four text-lock suites pass with no database connection, covering: the two resolved open questions, the derived-numeral prohibition, the zero-split shape of AI entries, the scalar-subselect wrapping of every helper call in a policy, the diary's INSERT-only posture, and the reorder RPC's lock/contiguity/grant profile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: [BLOCKING] Owner applies migrations 135–138 to prod (one `supabase db push`)</name>
  <files>supabase/migrations/135_works_core.sql, supabase/migrations/136_work_members.sql, supabase/migrations/137_split_sheets_work_link.sql, supabase/migrations/138_work_diary_events.sql</files>
  <action>
    STOP — owner action required. An executor agent MUST NOT run `supabase db push` on this
    project. This checkpoint runs after all four migrations and all four text-lock tests are
    authored, and BEFORE any wave-2 plan (37-05, 37-06, 37-07) runs. Every wave-2 route writes a
    table or calls a function that does not exist until this push lands, and TypeScript types in
    this project come from config rather than from the live database, so downstream verification
    would be a green false positive without it.
  </action>
  <what-built>Four migrations, authored and text-locked by jest, not yet applied: 135 the composition core (`works`, `work_versions`, `lyric_blocks`, `ai_entries`, RLS on, no policies); 136 `work_members` with the write REVOKE, the `is_work_owner`/`work_member_tier` SECURITY DEFINER helper pair, every policy for all five tables, and the claimed-collaborator bridge; 137 the single `split_sheets.work_id` column; 138 the diary table, its capture triggers, and the atomic `reorder_lyric_blocks()` RPC. Nothing has been applied to the database.</what-built>
  <how-to-verify>
    1. Review the four files. Two things deserve a real read: in 136, every helper call inside a
       policy must be wrapped as a scalar subselect (this is the 42P17 recursion guard migration
       078's header explains), and the REVOKE on `work_members` must name INSERT, UPDATE and
       DELETE for both authenticated and anon. In 138, `work_diary_events` must have exactly one
       policy and it must be SELECT-only.
    2. Confirm the text-lock suites are green — `npx jest __tests__/migration-13` runs all four
       and is the evidence for everything else in this list.
    3. Run `supabase db push` against the prod project. All four go in one push, in numeric order.
    4. Run `supabase migration list` and confirm LOCAL=REMOTE through 138.
    5. Confirm no error on any table create, any policy create, the helper-pair grants, the
       trigger creates, or the reorder function create.
    6. Smoke the shape: confirm `works`, `work_versions`, `lyric_blocks`, `work_members`,
       `ai_entries` and `work_diary_events` all exist and are empty, and that `split_sheets` now
       carries a nullable work link. Confirm your existing split sheets and vault projects are
       untouched — nothing in this set alters an existing row.
  </how-to-verify>
  <verify>Owner confirms `supabase migration list` shows migrations 135, 136, 137 and 138 live (LOCAL=REMOTE) with no push errors.</verify>
  <done>All four migrations are applied to prod; the six new tables exist, the helper pair and the reorder RPC are callable, and the diary's capture triggers are attached.</done>
  <resume-signal>Type "135-138 applied" once `supabase migration list` shows all four live, or describe any push error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated → new tables via PostgREST | A signed-in artist can query any of the six new tables directly, bypassing every API route |
| collaborator (contribute tier) → another artist's work rows | An invited member holds a real session and legitimately writes into someone else's song |
| `collaborators.claimed_by` UPDATE → `work_members.user_id` | A SECURITY DEFINER trigger grants access to a work as a side effect of a signup |
| service_role → `work_diary_events` | The one write path clients cannot reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Information Disclosure | `works` / `work_versions` / `lyric_blocks` / `ai_entries` SELECT | high | mitigate | Owner-or-member policies built on the `is_work_owner` / `work_member_tier` SECURITY DEFINER pair (136), never a raw cross-table EXISTS — which is both a correctness fix (42P17) and the reason the policy cannot be tricked into a wider read. |
| T-37-02 | Denial of Service | RLS policy recursion between `works` and `work_members` | high | mitigate | The helper pair ships in the first migration that needs it, wrapped as scalar subselects in every policy body. The 136 text-lock test asserts the wrapping structurally, so a later hand-edit that inlines an EXISTS fails CI rather than production. |
| T-37-03 | Elevation of Privilege | Direct PostgREST write to `work_members` | high | mitigate | `REVOKE INSERT, UPDATE, DELETE ... FROM authenticated, anon` (migration 078(b) posture). Membership is the capability that lets someone write into another artist's song, so it is service-role-only by construction, not by route convention. |
| T-37-04 | Repudiation | Editing or deleting a diary entry after the fact | high | mitigate | `work_diary_events` gets one SELECT policy and a REVOKE of INSERT, UPDATE and DELETE from authenticated and anon. Writes arrive only from SECURITY DEFINER triggers and one service-role route. An AI citation and a hum timestamp are only evidence if they are immutable. |
| T-37-05 | Tampering | A diary-worthy write performed by a future route that forgets to log | medium | mitigate | Capture lives in database triggers, not app code (CAT-Q1). A route written next year still logs. The single exception (`note`) has no underlying row and is documented as the only app-authored kind. |
| T-37-06 | Elevation of Privilege | `reorder_lyric_blocks()` called directly by a client to reshuffle another work | high | mitigate | REVOKE ALL from PUBLIC, anon and authenticated; GRANT EXECUTE to `service_role` only (migration 127 posture). The only caller is plan 07's route, which proves work membership first. |
| T-37-07 | Tampering | Concurrent reorder losing or duplicating a block position | medium | mitigate | SHARE ROW EXCLUSIVE table lock plus completeness, uniqueness and contiguity validation before a single set-based UPDATE, with a row-count check that raises a serialization failure on drift — migration 127's proven shape. |
| T-37-08 | Spoofing | Membership backfilled to the wrong person on claim | high | mitigate | The bridge keys exclusively off `collaborators.claimed_by`, which `claim_collaborators()` sets from the signing-up user's Supabase-Auth-verified account email — never from a party's free-typed email. `split_sheet_parties.user_id` is a dead column and is not consulted (migration 079's finding). |
| T-37-09 | Information Disclosure | Widening `track-audio` storage RLS for collaborators | medium | accept | Not done in 37.1. Every upload and signed-URL read already goes through the service role, so the migration-004 policies are inert defense-in-depth; widening them would add migration surface for a direct-to-storage client path that does not exist anywhere in this codebase (RESEARCH Pitfall 2). Revisit only if a future phase ships direct client uploads. |
| T-37-10 | Tampering | New trigger on `split_sheet_parties` disturbing the existing split-sheet builder | medium | mitigate | The capture function returns early unless the parent sheet carries a work link, making it a strict no-op for every sheet that exists today and every non-work sheet created later. The 138 text-lock test asserts the guard is present in the function body. |
| T-37-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan or anywhere in Phase 37.1 — 37-RESEARCH.md's Package Legitimacy Audit records "not applicable, zero new packages". |
</threat_model>

<verification>
- `npx jest __tests__/migration-135.test.ts __tests__/migration-136.test.ts __tests__/migration-137.test.ts __tests__/migration-138.test.ts` green.
- `npx tsc --noEmit` reports 0 errors.
- `npm run lint` clean (the script already carries `--max-warnings=0`).
- `npx jest` full suite green — no regression against the current baseline (288 test files).
- `supabase migration list` shows LOCAL=REMOTE through 138 (owner-confirmed at the task 4 checkpoint).
- **DO NOT run `npm run build`.** The owner keeps a dev server on :3000 and a production build clobbers `.next` underneath it. Use `npx tsc --noEmit`.
</verification>

<success_criteria>
- Six new tables exist, all RLS-enforced through one SECURITY DEFINER helper pair.
- No numeral is stored anywhere; no reverse sheet pointer exists on `works`; no labels column exists.
- `work_members` is unwritable over PostgREST; `work_diary_events` is unwritable and unerasable over PostgREST.
- Every diary-worthy mutation has a trigger, and the one exception is documented as such.
- `vault_projects` and the split-sheet RLS pair are provably untouched.
- All four migrations are live in prod before wave 2 begins.
</success_criteria>

<output>
Create `.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-01-SUMMARY.md` when done.
Every commit message explains WHY and ends with:
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
</output>
