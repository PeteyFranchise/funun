---
phase: 37-the-catalogue-unreleased-works-as-living-assets-versions-rig
plan: 12
type: execute
wave: 4
depends_on: [37-05, 37-06, 37-07, 37-08, 37-09, 37-10, 37-11]
files_modified:
  - app/(artist)/vault/works/[workId]/page.tsx
  - components/catalogue/WorkPage.tsx
  - components/catalogue/WorkPage.test.tsx
autonomous: true
requirements: [S-01, S-02, S-03, S-04]

must_haves:
  truths:
    - "A work has a page at /vault/works/[workId], and a person with no access to it gets the same not-found response as a person asking for a work that does not exist"
    - "Desktop renders the two-column layout — versions sticky left, diary right, header spanning (sketch 001-C)"
    - "Mobile renders the single-stream diary by default with a Diary and Versions toggle reaching the version cards (sketch 001-A, the decided mobile default)"
    - "The composer card leads the page and exactly one guiding line sits between it and the diary (sketch 005-C)"
    - "Playback URLs are signed server-side in one batch call with a short TTL"
    - "The hum-first deliberate minute fires once per song, before that song's first AI entry, and never again"
    - "The AI-entry flow runs conversationally for the account's first-ever entry and as the two-door form after"
    - "Every hygiene moment fires INSIDE an add flow, never as a separate chore (sketch 005-C)"
    - "A brand-new work renders the empty-state pitch rather than an empty shell"
  artifacts:
    - "app/(artist)/vault/works/[workId]/page.tsx"
    - "components/catalogue/WorkPage.tsx"
  key_links:
    - "The page resolves access with `resolveWorkAccess()` before it loads anything, and passes the viewer's tier down"
    - "The guiding line's snapshot is assembled here from data already fetched for the render — the resolver performs no I/O of its own"
    - "`loadWorkSplits()` (plan 05) is the service-role read that supplies the splits state and the people the nudge may name"
    - "`signVersionUrls()` (plan 06) mints the playback URLs the diary and the versions column use"
    - "Every component built in plans 08 through 11 is mounted here, and nowhere else"
  prohibitions:
    - "MUST NOT fetch inside a leaf component — the page fetches, the components render"
    - "MUST NOT show more than one guiding line or stack nudges anywhere on the page"
    - "MUST NOT show the hum-first moment more than once per song"
    - "MUST NOT render the sketch-004 destination doors — they are 37.2"
    - "MUST NOT construct a storage URL client-side; every playback URL is signed server-side"
    - "MUST NOT spend the indigo-to-fuchsia gradient more than once on this page"
    - "MUST NOT run `npm run build`"
---

<objective>
Mount the composer room: the page that turns eleven plans' worth of parts into the room an artist
actually writes in.

Implements the assembled layout of `.claude/skills/sketch-findings-funun/sources/001-work-page-diary/
variant-c.html` (desktop two-column) and `.../variant-a.html` (mobile single-stream with the
Diary/Versions toggle), with `.../005-the-composer/variant-c.html` as the page's spine.

`app/(artist)/vault/works/[workId]/page.tsx` is the server component: it resolves access, loads
everything in one pass, signs the playback URLs, assembles the guiding-line snapshot, and hands the
result down. `components/catalogue/WorkPage.tsx` is the client shell that lays it out, switches
treatment by breakpoint, and owns the modal state for the hygiene moments.

Purpose: this is the plan where the doctrine's most important sentence has to be visibly true —
creation leads, and evidence is the exhaust. Every hygiene moment fires inside an add flow rather
than as a separate chore, the diary stays clean, and the song gets one sentence. If this page reads
as a compliance form, the phase has failed even with every other plan green.

Output: one server page, one client shell, one structural suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-CONTEXT.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-05-SUMMARY.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-06-SUMMARY.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-08-SUMMARY.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-09-SUMMARY.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-10-SUMMARY.md
@.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-11-SUMMARY.md
@.claude/skills/sketch-findings-funun/references/catalogue-hygiene-ui.md
@.claude/skills/sketch-findings-funun/sources/001-work-page-diary/variant-a.html
@.claude/skills/sketch-findings-funun/sources/001-work-page-diary/variant-c.html
@.claude/skills/sketch-findings-funun/sources/005-the-composer/variant-c.html
@app/(artist)/vault/[projectId]/page.tsx
@components/layout/Topbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: app/(artist)/vault/works/[workId]/page.tsx — one server pass, signed URLs, one snapshot</name>
  <files>app/(artist)/vault/works/[workId]/page.tsx</files>
  <action>
    Create the server component, following the shape of `app/(artist)/vault/[projectId]/page.tsx` —
    `force-dynamic`, `createServerClient()` under the viewer's own session, a `Promise.all` for the
    parallel reads, and the service client used only for signing URLs.

    Resolve access FIRST with `resolveWorkAccess()` (plan 04). On a refusal, call Next's not-found
    for the 404 case and redirect to sign-in for the 401 case. Comment that a work the viewer is not
    on must be indistinguishable from a work that does not exist, which is why the 404 path is used
    rather than an explanatory error page.

    Then load, in one parallel pass: the work row; its versions ordered by creation; its lyric blocks
    ordered by position; its members with the display names their collaborator or profile rows carry;
    its AI entries; and its diary events newest first, bounded to a sensible page size. Load the
    living split sheet and its parties through `loadWorkSplits()` (plan 05) — comment that this is the
    one service-role read on this page besides URL signing, that its precondition (access already
    resolved) is satisfied two statements above, and that it exists instead of a new RLS policy on the
    split-sheet table pair for the reasons migration 137's header records.

    Sign the playback URLs in ONE batch call via `signVersionUrls()` (plan 06), matching the existing
    project page's batch pattern and its short TTL. Comment that a per-version signing loop would turn
    a page render into N storage calls.

    Derive presentation server-side where a pure function already exists: version numerals via
    `deriveVersionNumerals()`, block labels via `deriveBlockNumerals()`, repeat resolution via
    `resolveRepeat()`, and each diary entry's three parts via `describeDiaryEvent()` with the
    display-name context this page has just assembled. Comment that doing this on the server keeps
    the components dumb and keeps the numbers identical to what an export would produce.

    Assemble the guiding-line snapshot and call `resolveGuidingLine()` (plan 03) here — version count,
    block count, members, the writers missing from the sheet via `writersMissingFromSheet()`, AI
    entries awaiting a re-author decision, the dismissed step keys, the contributors the splits nudge
    has already fired for, and the artist's reminder setting. Comment that the resolver performs no
    I/O by design and that every input above is already in hand for the page's own render, so the
    guiding line costs nothing extra.

    For 37.1, persist the dismissal and fired-for sets in the simplest durable place available that
    does not require a migration — comment the choice explicitly and note that a dedicated column or
    table is a 37.2 tidy-up when the destination doors add their own per-work state.

    Render `WorkPage` with everything resolved. Add a Topbar consistent with the rest of the artist
    surface, with a back link to the Sound Vault.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>grep -c 'resolveWorkAccess' 'app/(artist)/vault/works/[workId]/page.tsx' | grep -qvx 0</automated>
    <automated>grep -c 'signVersionUrls' 'app/(artist)/vault/works/[workId]/page.tsx' | grep -qx 1</automated>
    <automated>grep -c 'resolveGuidingLine' 'app/(artist)/vault/works/[workId]/page.tsx' | grep -qx 1</automated>
  </verify>
  <done>The page resolves access before loading anything, loads every entity in one parallel pass, signs playback URLs in a single batch, derives every numeral and entry description server-side, and resolves exactly one guiding line from data it already had.</done>
</task>

<task type="auto">
  <name>Task 2: components/catalogue/WorkPage.tsx — the shell, the breakpoint treatments, and the add flows</name>
  <files>components/catalogue/WorkPage.tsx</files>
  <action>
    Build the client shell that lays out the room and owns the flow state.

    **Layout.** Desktop follows 001-C: the header card spans the top, then a two-column grid with the
    versions list sticky in a narrow left column and the diary in the wide right column. Mobile
    follows 001-A: a single stream with a Diary and Versions segmented toggle, defaulting to Diary.
    Comment the decision so the treatment is not "simplified" later: 001 was decided as C on desktop
    and A on mobile, and the mobile version-cards view is reached through the toggle rather than lost.

    **Order.** WorkHeader, then ComposerCard, then the single GuidingLine, then the diary. Comment
    005-C's rule: creation leads, the song gets one sentence, and the diary follows clean.

    **The four verbs.** Wire each tile to its flow. Hum it opens the capture component inline. Write
    lyrics opens or scrolls to the LyricsPad. Add audio opens a file picker that posts to the same
    versions route with the upload source. Note opens a small composer posting to the notes route.

    **The AI question, inline.** After an add-audio (and after a hum where it is plausible), ask
    whether AI was involved as part of that add flow — never as a separate chore. Comment the rule
    from 005-C: AI questions and the hum-first moment fire INSIDE the add flows. When the artist says
    yes, mount AiEntryFlow, choosing its conversational mode when this is the account's first-ever
    entry.

    **The hum-first moment.** Before this song's FIRST AI entry, mount HumFirstMoment full-screen —
    once per song. Track that with the same per-work state the page persists, and comment that the
    once-per-song rule is the whole reason the deliberate minute is tolerable: a moment that fired on
    every entry would be a wall.

    **The re-author prompt.** When an AI entry has generated material awaiting a decision, mount
    ReauthorPrompt inline where the artist meets that entry — recurring and low ceremony, per the
    003 split. Do NOT staple it onto a diary row; the diary stays clean.

    **Empty state.** When the work has no versions and no blocks, render ComposerCard's empty variant
    with the hum-first hero instead of the standard bar, and render no guiding line — the hero already
    is the guidance. Comment that this is the owner's pitch and the first thing a new artist sees.

    **Refresh.** After every mutation, refresh the server data rather than hand-patching local state
    for anything the database derives — numerals, diary entries and the guiding line all come from
    the server, and a local guess would drift. Local optimism is fine for the pad's in-progress text.

    **Budget.** One gradient on this page. It belongs to the composer's primary action (or the empty
    state's hum button, which is the same slot). The guiding line uses the tinted border and wash, not
    the full gradient.

    Leave a commented placeholder in the header's right-hand slot for the sketch-004 destination
    lights, stating they are 37.2 and that the slot is reserved so the layout does not move when they
    arrive.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>The shell renders the header, composer, single guiding line and diary in that order; desktop is two-column and mobile is single-stream with a working toggle; all four verbs are wired; the AI question and the hum-first moment fire inside add flows; the empty state is the hum pitch; one gradient is spent.</done>
</task>

<task type="auto">
  <name>Task 3: components/catalogue/WorkPage.test.tsx — the structural proof</name>
  <files>components/catalogue/WorkPage.test.tsx</files>
  <action>
    Write the suite with `renderToStaticMarkup`, following `lib/vault/pdf/split-sheet.test.tsx`'s
    JSX-in-Jest precedent. There is no jsdom in this repo, so the assertions are on markup structure
    and on prop-driven branches — not on clicks.

    Assert, at minimum:
    - A populated work renders the header, the composer card, the diary and the versions list.
    - Exactly one guiding line renders when a step was supplied, and none renders when null was
      supplied. Count occurrences rather than asserting presence, so a future change that renders a
      second one fails here.
    - A work with no versions and no blocks renders the empty-state hero copy and renders no guiding
      line.
    - A version supplied with a signed URL renders a play control; one supplied without renders none.
    - The mobile treatment renders the Diary and Versions toggle and the desktop treatment does not.
    - The markup contains exactly one element carrying the gradient class.
    - The markup contains no destination-door chips, since those are 37.2.
    - The markup contains no raw hex colour.
    - A contribute-tier viewer's render carries no membership-management control.

    Add one regression assertion with a comment explaining it: no diary entry renders an action
    button. That is the machine-checkable form of the "diary stays clean" rule, and it is the rule
    most likely to be eroded by a later well-meant change.
  </action>
  <verify>
    <automated>npx jest components/catalogue/WorkPage.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npx jest</automated>
  </verify>
  <done>The suite proves the layout order, the at-most-one guiding line by count, the empty state, conditional playback, both breakpoint treatments, the single gradient, the absence of 37.2 doors, and the clean diary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path → a work's entire contents | The page is the widest read surface in the phase |
| service-role reads on a server page | Split-sheet contents and signed URLs fetched with elevated privilege |
| viewer tier → rendered affordances | What a contribute-tier member is shown they may do |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-71 | Information Disclosure | Reading a work the viewer is not on | high | mitigate | `resolveWorkAccess()` runs before any load, and a refusal renders not-found so the response cannot distinguish a private work from a nonexistent one. The row-level policies from migration 136 are the second layer under the viewer's own session client. |
| T-37-72 | Elevation of Privilege | Service-role reads running before the access check | high | mitigate | The only two elevated calls are `loadWorkSplits()` and `signVersionUrls()`, both placed after the access resolution, both documented with that precondition in their own modules. |
| T-37-73 | Information Disclosure | Long-lived or leaked playback URLs | medium | mitigate | URLs are batch-signed server-side with the same short TTL the existing project page uses, and nothing client-side constructs a storage path. |
| T-37-74 | Elevation of Privilege | Affordances shown to a tier that cannot use them | medium | mitigate | The viewer's tier is passed down and gates the membership controls; every corresponding route independently enforces the same rule, so the UI gate is presentation rather than protection. |
| T-37-75 | Tampering | Local state drifting from the derived record | medium | mitigate | Numerals, diary entries and the guiding line are all server-derived and refetched after every mutation; local optimism is confined to in-progress pad text. |
| T-37-76 | Repudiation | A hygiene moment being bypassed by page structure | medium | mitigate | The hum-first moment and the AI question are mounted inside the add flows rather than beside them, so the flow that creates the evidence is the flow that asks the question. |
| T-37-77 | Denial of Service | A large diary or version list rendering unbounded | low | mitigate | The diary query is bounded to a page size; comment records that pagination beyond the first page is 37.2's volume-view work. |
| T-37-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan — zero new packages in Phase 37.1. |
</threat_model>

<verification>
- `npx jest components/catalogue` green.
- `npx tsc --noEmit` reports 0 errors.
- `npm run lint` clean (the script already carries `--max-warnings=0`).
- `npx jest` full suite green — no regression against the current baseline (288 test files).
- Migrations 135–138 must already be live (37-01's checkpoint) — this page reads all six tables.
- Component tests use `renderToStaticMarkup`; this repo runs jest in a node environment with no jsdom.
- **DO NOT run `npm run build`.** The owner keeps a dev server on :3000 and a production build clobbers `.next` underneath it. Use `npx tsc --noEmit`.
</verification>

<success_criteria>
- A work opens at its own URL, and a non-member gets a not-found.
- Desktop is 001-C's two columns; mobile is 001-A's single stream with a working toggle.
- The page reads composer first, one guiding line, then a clean diary.
- Every hygiene moment fires inside an add flow; the deliberate minute fires once per song.
- A brand-new work shows the hum pitch, not an empty shell.
</success_criteria>

<output>
Create `.planning/phases/37-the-catalogue-unreleased-works-as-living-assets-versions-rig/37-12-SUMMARY.md` when done.
Every commit message explains WHY and ends with:
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
</output>
