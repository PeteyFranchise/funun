---
context: phase
phase: 38-member-organization-team-workspaces
task: 0
total_tasks: 13
status: planned_not_executed
last_updated: 2026-09-05T23:26:04.193Z
---

# Phase 38 — Member Organization & Team Workspaces

**Stage: PLANNED, NOTHING EXECUTED.** No code and no migrations have been written. All work so far
is planning artifacts, fully committed. Working tree clean.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale-anchor doc edit after a subagent already rewrote the file | The `gsd-planner` subagent rewrote the ROADMAP Phase 38 block as part of its own run. A later orchestrator edit keyed on the pre-planner text silently no-op'd — `str.replace` returns the original on no match, so the "updated" message printed while nothing changed. | advisory | Re-read any file a subagent may have written BEFORE editing it, and assert the anchor exists (`assert anchor in s`) rather than trusting the replace. Verify with a grep afterwards, not with the script's own success message. |

<current_state>
Phase 38 is discussed, researched, pattern-mapped, planned and checker-verified. The owner paused
here to change locations. The immediate next action is execution, which has not begun.
</current_state>

<completed_work>
- Discussion: all 12 owner-specified decision areas → **56 locked decisions (D-01..D-56)**, **31 requirement IDs (WS-01..WS-31)** → `38-CONTEXT.md`
- `38-RESEARCH.md` (862 lines), `38-PATTERNS.md` (30 files classified, 26 with analogs), `38-VALIDATION.md`
- **13 plans, 7 waves** (`38-01` … `38-13-PLAN.md`); `gsd-plan-checker` returned **VERIFICATION PASSED** (19/19 requirements, wave file-disjointness, threat models, artifacts sections)
- Owner approved the phase split → `Phase 38.1` and `Phase 38.2` roadmap entries created
- **D-56** added after planning (disable control) → CONTEXT amended, plan 38-11 extended with a build task, threat register T-38-11-09 flipped from *transfer* to *mitigate*
- `WS-01..WS-31` registered in `.planning/REQUIREMENTS.md` with per-phase assignment + traceability
- `STATE.md` updated; 3 docs-only commits on `main` (local, unpushed)
</completed_work>

<remaining_work>
Execution of all 13 plans. Nothing started.

- **Wave 1** — 38-01 permission catalogue + subset check (pure logic, no migration)
- **Wave 2** — 38-02 state machines ‖ 38-03 **mig 182** [owner push]
- **Wave 3** — 38-04 **mig 183** [owner push] ‖ 38-05 access gate + audit + routes
- **Wave 4** — 38-06 invitations ‖ 38-07 roster lifecycle ‖ 38-08 **mig 184** [owner push]
- **Wave 5** — 38-09 grant issuance + use-time resolver ‖ 38-10 **mig 185** [owner push]
- **Wave 6** — 38-11 **mig 186 — RLS branch + disable control** [owner push, pushed ALONE]
- **Wave 7** — 38-12 attachment + catalogue ‖ 38-13 Appears-on shelf + custody transfer
</remaining_work>

<decisions_made>
- **Split approved** — Phase 38 = Slices A–D; 38.1 = E–G; 38.2 = H–I. Boundary after D rather than C so the permission model and its RLS consumer stay inside one security review.
- **D-56** — a *working* platform-wide disable control ships with Phase 38, not merely the `workspace_access_enabled()` seam. Supersedes the "seam only" reading of D-55; adds **WS-31**.
- **WS-20 and WS-25 pulled forward** into Phase 38 against the research's slice map — payout exclusion is a property of the permission catalogue, and audit must exist in every service-role write route from day one.
- **D-28** — `vault_projects.user_id` means **record custody, never rights ownership**. Resolves both the label-owns-masters and performs-on-someone-else's-record cases without a schema fork.
</decisions_made>

<blockers>
None. Execution is unblocked.
</blockers>

## Required Reading (in order)

1. `.planning/phases/38-member-organization-team-workspaces/38-CONTEXT.md` — **56 locked decisions. Do NOT re-litigate any of them.**
2. `docs/architecture/ACCOUNT-TYPES.md` — BINDING account doctrine (owner-approved 2026-09-04). Workspaces are contexts, **not** a fourth account class.
3. `.planning/deliberations/sound-vault-master-custody.md` D-01 and D-02 — BINDING, LOCKED 2026-09-01. Bounds D-08, D-10 and D-40. D-01's preview/clean-master separation must not regress.
4. `38-RESEARCH.md` — architecture patterns, the RLS pitfalls, code examples
5. `38-PATTERNS.md` — concrete analogs with line numbers; also names the 3 no-analog files
6. `38-VALIDATION.md` — Wave 0 test requirements and the manual-only verifications

## Human Actions Pending (all blocking)

1. **Migrations 182–185** — additive, batchable at one owner checkpoint. An executor agent must **NEVER** run `supabase db push`.
2. **Migration 186 — pushed ALONE.** Then the six-account adversarial RLS smoke checklist, the SQLSTATE 42P17 recursion check across five tables, an `EXPLAIN ANALYZE` at 200+ attached projects, and the D-56 disable drill (off / on / fail-closed).
3. Optional: push the 3 local docs commits to `origin/main`.

## Infrastructure State

- Working tree **clean**. 3 commits on `main` ahead of `origin/main`, **docs-only, unpushed — nothing deployed.**
- Migration numbers **182–186 pre-assigned** to Phase 38; **187–188 reserved for Phase 38.2.** Latest applied migration is 181.
- No dev server, no background processes, no async jobs.

<context>
This session started as "what does an A&R see when onboarding members," which surfaced that Funūn
had no *relationship* layer — `project_members` has four flat roles and no management API, and the
account switcher is a sign-out between two separate logins. That gap became Phase 38.

The A&R-facing CRM itself was deliberately NOT planned. Its findings are preserved in
`.planning/todos/pending/2026-09-05-anr-member-crm-and-role-vocabulary.md` — including two owner
decisions already taken (assigned book like the AE model; consolidate role vocabulary first) and a
live inconsistency worth an independent `/gsd-quick`: A&R can approve Sync Library tags via
`app/api/sync-library/tag-approve/route.ts:31` but the page is gated `leadership|ae` and has no nav
entry for `anr`.

The single highest risk in this phase is the D-48 RLS workspace branch in migration 186. A defect
there grants third parties access to artists' catalogues platform-wide. That is why 186 is pushed
alone, and why the disable control (D-56) was added.
</context>

<next_action>
Run `/gsd-execute-phase 38`.

Wave 1 (38-01, permission catalogue + subset check) is pure logic with no migration, so it is safe
to start any time. **Wave 2 reaches the first owner migration checkpoint (182)** — begin when you
can review a migration in the same sitting, or execution will stall there.
</next_action>
