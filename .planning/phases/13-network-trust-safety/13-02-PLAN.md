---
phase: 13-network-trust-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01-PLAN.md
autonomous: true
requirements:
  - DISCOVER-04
---

# Plan 13-02: Network Tab

<objective>
Build the member-facing Network tab for following, followers, connections, pending requests, and blocked members.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add network query API</name>
  <acceptance_criteria>
    - Requires authenticated viewer.
    - Lists following, followers, accepted connections, pending inbound/outbound requests, and viewer-owned blocklist.
    - Uses explicit public-safe profile columns.
    - Does not expose who blocked the viewer.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Network tab UI</name>
  <acceptance_criteria>
    - Desktop and mobile tabs are usable.
    - State-specific actions are clear.
    - Block/unblock uses inline confirmation.
  </acceptance_criteria>
</task>

</tasks>

<verify>
  <automated>npm test -- --runInBand __tests__/network-api.test.ts</automated>
  <automated>npm run lint</automated>
  <automated>npx tsc --noEmit</automated>
</verify>

