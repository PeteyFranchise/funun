---
phase: 13-network-trust-safety
plan: 01
type: execute
wave: 1
autonomous: true
requirements:
  - DISCOVER-04
  - SAFETY-01
  - SAFETY-02
  - SAFETY-03
  - SAFETY-04
---

# Plan 13-01: Trust & Safety Contracts

<objective>
Create the core trust/safety contract layer and schema plan for network lists, block state, report targets, verification authority, and profile visibility.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Define pure contracts</name>
  <acceptance_criteria>
    - Network list item shapes are typed.
    - Report target types and reason values are typed.
    - Profile visibility and open-to visibility values are typed.
    - Verification admin action types are typed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Draft schema migration</name>
  <acceptance_criteria>
    - Reports table is private by default.
    - Profile visibility fields are additive.
    - Admin verification audit fields are additive.
    - RLS policies prevent reported-user access to reports.
  </acceptance_criteria>
</task>

</tasks>

<verify>
  <automated>npm test -- --runInBand __tests__/trust-safety-contracts.test.ts</automated>
  <automated>npm run lint</automated>
  <automated>npx tsc --noEmit</automated>
</verify>

