---
phase: 13-network-trust-safety
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01-PLAN.md
requirements:
  - SAFETY-01
autonomous: false
---

# Plan 13-03: Hard Block Enforcement Audit

<objective>
Make blocking authoritative across public profiles, search/discovery, feed, DMs, follows, connections, comments, reactions, and reposts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add block/unblock API</name>
  <acceptance_criteria>
    - Authenticated users can block/unblock another member.
    - Self-block is rejected.
    - Duplicate block is idempotent.
    - Blocklist reads only return blocker-owned rows.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Audit and patch read/write surfaces</name>
  <acceptance_criteria>
    - Public profile route excludes blocked pairs.
    - People Search excludes blocked pairs.
    - Green Room feed excludes blocked pairs.
    - DM send and request paths reject blocked pairs.
    - Follow/connect paths reject blocked pairs.
  </acceptance_criteria>
</task>

</tasks>

<verify>
  <automated>npm test -- --runInBand __tests__/block-enforcement.test.ts</automated>
  <automated>npm run lint</automated>
  <automated>npx tsc --noEmit</automated>
  <manual>Live DB RLS smoke test: blocked user cannot discover or directly fetch blocker profile.</manual>
</verify>

