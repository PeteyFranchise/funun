---
phase: 15-account-capability-model
plan: 04
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - app/(admin)/admin/capability-requests/page.tsx
  - components/admin/CapabilityRequestsAdmin.tsx
autonomous: true
requirements: [D-03, D-11]
user_setup: []

must_haves:
  truths:
    - "An admin can browse a queue of pending artist→industry capability requests, each showing the requester and their pre-picked role badges (D-03/D-11)"
    - "An admin can approve or deny a pending request; approve flips it and attaches the pre-picked badge, deny marks it denied (D-03)"
    - "The queue is admin-gated per-page (is_admin re-verified server-side), matching the /admin/members convention"
  artifacts:
    - app/(admin)/admin/capability-requests/page.tsx
    - components/admin/CapabilityRequestsAdmin.tsx
  key_links:
    - "The page re-verifies is_admin server-side then loads pending grants via createServiceClient (mirrors admin/members/page.tsx)"
    - "The component's approve/deny buttons POST to /api/capabilities/approve/[grantId] (Plan 02)"
    - "role_slugs are displayed read-only (D-11: badge already chosen at request time; no admin override UI needed — RESEARCH Open Question 3)"
---

<objective>
Build the admin approval queue that turns pending artist→industry requests into approved capabilities. It extends the exact `/admin/members` pattern (per-page `is_admin` gate, service-client load, optimistic list) with approve/deny actions wired to Plan 02's approve route. The requester's pre-picked role badges (collected at request time by Plan 03's CapabilityCta, D-11) are shown read-only so the admin sees what badge will attach on approval.

Purpose: Complete the D-01/D-03 request→approve loop with a real in-app admin surface — not an out-of-band process. This is the admin-facing counterpart to Plan 03's requester-facing CTA.
Output: One admin server page + one client component.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-account-capability-model/15-CONTEXT.md
@.planning/phases/15-account-capability-model/15-RESEARCH.md
@.planning/phases/15-account-capability-model/15-PATTERNS.md

# Exact pattern to copy: per-page admin gate + service load; optimistic list + chip picker
@app/(admin)/admin/members/page.tsx
@components/admin/MembersAdmin.tsx
@lib/admin/gate.ts
@lib/industry/roleMapping.ts
@lib/supabase/server.ts
# The route this component calls (Plan 02)
@app/api/capabilities/approve/[grantId]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin capability-requests page (server, admin-gated)</name>
  <files>app/(admin)/admin/capability-requests/page.tsx</files>
  <read_first>
    - app/(admin)/admin/members/page.tsx (copy verbatim: `export const dynamic = 'force-dynamic'`, the createServerClient user + is_admin redirect gate, the createServiceClient list load, the per-row email attach via `service.auth.admin.getUserById(row.id)`)
    - lib/supabase/server.ts (createServerClient, createServiceClient)
    - types (ProfileRole, PROFILE_ROLE_LABELS if the component renders badges)
  </read_first>
  <action>
    Create `app/(admin)/admin/capability-requests/page.tsx` as an async server component. Head it with `export const dynamic = 'force-dynamic'`. Copy the admin gate from admin/members/page.tsx verbatim: `createServerClient().auth.getUser()`, `if (!user) redirect('/signin')`, `const isAdmin = (user.app_metadata as { is_admin?: boolean })?.is_admin === true; if (!isAdmin) redirect('/')`. Then load pending requests with `createServiceClient().from('capability_grants').select('id, profile_id, capability, role_slugs, requested_at').eq('status', 'pending').order('requested_at', { ascending: true })`. For each row, attach the requester's display info: read `artist_name` from `artist_profiles` (`.from('artist_profiles').select('artist_name').eq('id', row.profile_id).maybeSingle()`) and the email via `service.auth.admin.getUserById(row.profile_id)` (same pattern as admin/members). Shape each into a `CapabilityRequest` object `{ grantId, profileId, artistName, email, capability, roleSlugs, requestedAt }`. Render `<h1>Capability Requests</h1>` and `<CapabilityRequestsAdmin initialRequests={requests} />` inside the same `flex-1 px-9 py-[30px]` container admin/members uses.
  </action>
  <verify>
    <automated>test -f "app/(admin)/admin/capability-requests/page.tsx" && npx tsc --noEmit 2>&1 | grep -c "capability-requests" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Page re-verifies is_admin server-side and redirects non-admins (grep: `is_admin` and `redirect('/')`)
    - Page loads only `status = 'pending'` grants, ordered by requested_at ascending
    - Each request carries grantId, profileId, artistName, email, capability, roleSlugs, requestedAt
    - `npx tsc --noEmit` no errors in the page
  </acceptance_criteria>
  <done>The admin page is per-page admin-gated, lists pending capability requests with requester identity + pre-picked role slugs, and renders the client queue component.</done>
</task>

<task type="auto">
  <name>Task 2: CapabilityRequestsAdmin component (approve/deny queue)</name>
  <files>components/admin/CapabilityRequestsAdmin.tsx</files>
  <read_first>
    - components/admin/MembersAdmin.tsx (copy the state-machine shape: useState list, per-row action, addError→actionError pattern, formatJoined date helper, roleLabel badge render, the chip-render style for role display)
    - lib/industry/roleMapping.ts (mapSlugsToProfileRoles / label helpers — only if the component needs to render slug→label; otherwise render role_slugs via the industry-role label helper)
    - app/api/capabilities/approve/[grantId]/route.ts (the endpoint shape: POST with `{ decision: 'approve' | 'deny' }`, returns `{ data: { grantId, status } }`)
  </read_first>
  <action>
    Create `components/admin/CapabilityRequestsAdmin.tsx` (`'use client'`). Export type `CapabilityRequest = { grantId: string; profileId: string; artistName: string | null; email: string; capability: 'artist' | 'industry'; roleSlugs: string[]; requestedAt: string }`. Component signature `CapabilityRequestsAdmin({ initialRequests }: { initialRequests: CapabilityRequest[] })`. Hold `const [requests, setRequests] = useState(initialRequests)`, `const [pendingId, setPendingId] = useState<string | null>(null)` (which row is mid-action), and `const [actionError, setActionError] = useState<string | null>(null)`. Render each request as a card (reuse MembersAdmin's card styling: `rounded-xl border border-white/10 p-4`) showing artist_name, `{email} · Requested {formatDate(requestedAt)}`, the requested capability, and the pre-picked role_slugs as read-only chips (D-11 / RESEARCH Open Question 3 — display only, no override UI). Add two buttons per card: "Approve" and "Deny". On click, set `pendingId`, POST `{ decision }` to `/api/capabilities/approve/${grantId}`, and on success remove the row from `requests` (optimistic) and clear pendingId; on failure set `actionError` from the response's `error` field and clear pendingId. Reuse MembersAdmin's date-format helper and its error-banner styling. Show an empty state ("No pending capability requests.") when the list is empty, matching MembersAdmin's empty state. Do NOT render a role-edit chip picker here — the badge was already chosen at request time (D-11); the queue is display + approve/deny only.
  </action>
  <verify>
    <automated>test -f "components/admin/CapabilityRequestsAdmin.tsx" && grep -q "/api/capabilities/approve/" components/admin/CapabilityRequestsAdmin.tsx && npx tsc --noEmit 2>&1 | grep -c "CapabilityRequestsAdmin" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Component POSTs `{ decision: 'approve' | 'deny' }` to `/api/capabilities/approve/[grantId]`
    - role_slugs render read-only (no chip-toggle picker in the admin queue)
    - Approving/denying removes the row optimistically; failures surface actionError
    - Empty state renders when there are no pending requests
    - `npx tsc --noEmit` no errors in the component
  </acceptance_criteria>
  <done>The queue component lists pending requests with read-only pre-picked badges and approve/deny actions wired to the Plan 02 approve route, with optimistic removal and error surfacing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin capability-requests page | Only admins may load it; a non-admin could try to reach the URL directly |
| client CapabilityRequestsAdmin → /api/capabilities/approve/[grantId] | The component is a UI over an admin-gated route; the real authority check is server-side (verifyAdmin in Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-13 | Elevation of Privilege | Non-admin loading the approval queue page | high | mitigate | Per-page server-side is_admin re-verification with redirect('/') for non-admins (copied verbatim from admin/members/page.tsx; layout gating alone is not relied upon — T-05-02 doctrine) |
| T-15-14 | Elevation of Privilege | A non-admin calling the approve route directly, bypassing this UI | high | mitigate | The approve route itself runs verifyAdmin() as its first line (Plan 02 T-15-06); this UI is not the boundary |
| T-15-15 | Information Disclosure | Requester email/identity exposed to a non-admin | medium | mitigate | Requester email is attached server-side via service.auth.admin.getUserById behind the is_admin gate; a non-admin never reaches the page load |
| T-15-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages introduced; nothing to slopcheck |
</threat_model>

<verification>
- `npx tsc --noEmit` no new errors
- `npx jest` full suite green (no lib changes here)
- Manual (per 15-VALIDATION.md): as an admin, load `/admin/capability-requests`, approve a pending artist→industry request, and confirm (a) the row disappears, (b) the target account's industry capability becomes 'approved' (verifiable via the DB or by that account then seeing the Antenna Post affordance), and (c) the pre-picked badge attaches to their profile. As a non-admin, confirm the page redirects to `/`.
</verification>

<success_criteria>
- In-app admin approval queue exists (D-03) — no out-of-band process
- Pre-picked badge shown read-only; approve attaches it (D-11)
- Per-page admin gate matches project doctrine
- Wired to the Plan 02 approve route
</success_criteria>

<output>
Create `.planning/phases/15-account-capability-model/15-04-SUMMARY.md` when done. Record the admin approve→badge-attach flow in the Manual-Only section per 15-VALIDATION.md.
</output>
