---
phase: 21-cross-account-collaboration-sheet-sync
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - "components/vault/VaultProjectCard.tsx"
  - "components/vault/SharedProjectBadge.tsx"
  - "components/vault/SharedProjectBadge.test.tsx"
  - "app/(artist)/vault/page.tsx"
autonomous: true
requirements:
  - "③-mine-vs-shared"

must_haves:
  truths:
    - "The vault page renders a separate 'Shared with me' section, distinct from the owned grid"
    - "A shared card is badged with the owner's name and the viewer's own role (e.g. 'Shared · Maya's project · You're a viewer')"
    - "The owned-projects query is unchanged (still .eq('user_id', me)) — shared rows never mix into the owned grid"
  prohibitions:
    - "A shared card renders NO edit affordance a viewer cannot use (the card links to a read-safe surface; no owner-only new/edit CTA on shared cards)"
    - "The shared lane does NOT alter the owned-grid counts or ordering"
  artifacts:
    - "components/vault/SharedProjectBadge.tsx"
    - "app/(artist)/vault/page.tsx (shared-with-me query + section)"
  key_links:
    - "shared query: project_members WHERE user_id = me → vault_projects (+ children) — RLS from 21-01 makes the rows visible"
    - "owner name resolved from vault_projects.user_id → user_profiles.artist_name; viewer role from the project_members row"
---

<objective>
Keep "mine" and "shared" straight in the vault (③): add a separate "Shared with me" lane, badge each
shared card with the owner's name and the viewer's role, and leave the owned grid — and its scoreboard
math — completely untouched.

Purpose: Surface the shared projects that 21-01's RLS now makes visible, without mixing them into the
owned grid and without offering edit affordances a viewer cannot use. The owned `.eq('user_id', me)`
query stays exactly as-is (RESEARCH: ③'s scoreboard-exclusion is satisfied "for free" because the
owner query excludes shared rows by construction).

Output: a `SharedProjectBadge` component, an extended `VaultCard` type with optional shared fields, and
a new "Shared with me" query + render section on the vault page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-cross-account-collaboration-sheet-sync/21-SPEC.md
@.planning/phases/21-cross-account-collaboration-sheet-sync/21-PATTERNS.md

@app/(artist)/vault/page.tsx
@components/vault/VaultProjectCard.tsx
@lib/vault/membership.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SharedProjectBadge component + VaultCard shared fields</name>
  <read_first>
    - components/vault/VaultProjectCard.tsx (VaultCard type lines 10-23; absolute-positioned status-chip idiom lines 91-97; the readiness ring pattern)
    - lib/vault/membership.ts (ProjectRole type + PROJECT_ROLE_LABELS for the role label)
    - 21-PATTERNS.md "VaultProjectCard.tsx / SharedProjectBadge.tsx" section (badge text per ③)
  </read_first>
  <behavior>
    - SharedProjectBadge renders "Shared · {ownerName}'s project" and a role line "You're a {roleLabel}"
    - When ownerName is null it degrades to "Shared project" without throwing
    - The badge maps role to its label via PROJECT_ROLE_LABELS (viewer → "viewer", etc.)
  </behavior>
  <files>components/vault/SharedProjectBadge.tsx, components/vault/SharedProjectBadge.test.tsx, components/vault/VaultProjectCard.tsx</files>
  <action>
    Extend the VaultCard type in components/vault/VaultProjectCard.tsx with two OPTIONAL fields so all
    existing owned-card callers keep compiling unchanged: `sharedBy?: { ownerName: string | null }` and
    `viewerRole?: ProjectRole` (import ProjectRole from lib/vault/membership). When `sharedBy` is
    present, render SharedProjectBadge in the card following the existing absolute-positioned status-chip
    idiom (do not disturb the owned-card status chip / readiness ring). Create
    components/vault/SharedProjectBadge.tsx as a small presentational component taking ownerName and role,
    rendering the ③ badge text via PROJECT_ROLE_LABELS. Write SharedProjectBadge.test.tsx first covering
    the three behaviors above (render assertion on the composed text + null-owner degrade). Keep the owned
    (non-shared) card render path byte-identical for callers that pass no shared fields.
  </action>
  <verify>
    <automated>npx jest components/vault/SharedProjectBadge.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>SharedProjectBadge renders the ③ text, degrades on null owner, and existing owned-card callers compile unchanged (tsc clean).</done>
  <acceptance_criteria>
    - SharedProjectBadge.test.tsx green
    - VaultCard shared fields are optional; no existing caller of VaultProjectCard needs a change to compile
    - tsc --noEmit clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: "Shared with me" query + render section on the vault page</name>
  <read_first>
    - app/(artist)/vault/page.tsx (existing owned query lines 35-50; card-mapping lines 57-80; empty/error render)
    - components/vault/VaultProjectCard.tsx (updated VaultCard shape from Task 1)
    - lib/vault/readiness.ts (readinessItemsForProject — reuse for the shared cards' gate counts)
  </read_first>
  <files>app/(artist)/vault/page.tsx</files>
  <action>
    In app/(artist)/vault/page.tsx add a SECOND, parallel query alongside the existing owned query (do
    NOT modify the owned `.eq('user_id', me)` query — it must keep excluding shared rows by construction,
    which satisfies ③'s scoreboard exclusion). The shared query reads project_members for the current
    user (WHERE user_id = me), joining through to vault_projects and the same child selections
    (tracks/vault_assets/vault_documents/tool_outputs) the owned query uses, plus the owner's display
    name (vault_projects.user_id → user_profiles.artist_name). RLS from 21-01 already scopes visibility;
    this query just asks for the shared rows explicitly. Map shared rows to VaultCard using the same
    readinessItemsForProject → card shape as the owned mapping, but set `artist` to the OWNER's name,
    populate `sharedBy: { ownerName }` and `viewerRole` from the membership row, and render them in a NEW
    labeled "Shared with me" section beneath the owned VaultBrowser — not inside it. The shared section
    renders only when there is at least one shared card; the owned empty-state and error handling stay
    as-is. Shared cards must not surface an owner-only new/edit CTA. Exclude the viewer's own owner-role
    backfilled rows from the shared lane (a project you own is not "shared with you") by filtering out
    memberships whose project_id you also own / whose role is 'owner'.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>The vault page renders a distinct "Shared with me" section fed by the project_members query, badged per ③, with the owned query and grid untouched; build + tsc clean.</done>
  <acceptance_criteria>
    - Owned query unchanged (grep confirms the existing .eq('user_id', ...) owned select is intact)
    - Shared section is a separate render block, only shown when non-empty, excluding owner-role rows
    - tsc --noEmit + npm run build clean
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → shared-with-me query → project_members / vault_projects | Reads rows RLS makes visible; must not surface edit affordances a viewer lacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-13 | Information Disclosure / EoP-perception | shared card affordances | medium | mitigate | Shared cards show badge (own role only) + read-safe link; no owner-only new/edit CTA; role label from the viewer's own membership row |
| T-21-16 | Information Disclosure | shared-with-me query scope | medium | mitigate | Query relies on 21-01 RLS (owner OR member); no service-role escalation; owner-role/own-project rows filtered out of the shared lane |
| T-21-12 | Information Disclosure (correctness) | scoreboard exclusion | low | mitigate | Owned query left untouched — shared rows never enter owned counts/ordering (③ satisfied by construction) |
| T-21-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A) |

Block-on-high: no high/critical threats introduced by this UI/read plan; access enforcement lives in
21-01's RLS.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (exclude from drift verification)

- Component: `components/vault/SharedProjectBadge.tsx`
- VaultCard optional fields: `sharedBy`, `viewerRole` (in components/vault/VaultProjectCard.tsx)
- New render section "Shared with me" + shared-with-me query in `app/(artist)/vault/page.tsx`
- Test file: `components/vault/SharedProjectBadge.test.tsx`
</artifacts_produced>

<verification>
- `npx jest components/vault/SharedProjectBadge.test.tsx` green
- `npx tsc --noEmit` + `npm run build` clean
- `npm test` full suite green
- Manual: an account with a viewer membership sees the shared project only in the "Shared with me"
  lane, badged with owner name + "You're a viewer", with no edit CTA; the owned grid and its stats are
  unchanged
</verification>

<success_criteria>
Shared projects appear in a separate, badged "Shared with me" lane showing the owner's name and the
viewer's role, with no unusable edit affordances, and the owned grid + scoreboard math are byte-for-byte
unaffected.
</success_criteria>

<output>
Create `.planning/phases/21-cross-account-collaboration-sheet-sync/21-03-SUMMARY.md` when done.
</output>
