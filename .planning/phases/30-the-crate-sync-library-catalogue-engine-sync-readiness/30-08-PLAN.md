---
phase: 30-the-crate-sync-library-catalogue-engine-sync-readiness
plan: 08
type: execute
wave: 3
depends_on: [30-07, 30-01, 30-03]
files_modified:
  - components/buyer/CatalogBrowserLight.tsx
  - lib/deals/catalog-query.ts
  - app/sync/catalog/page.tsx
autonomous: true
requirements: [CRATE-07, CRATE-08]
must_haves:
  truths:
    - "On the SAME /sync/catalog surface, a logged-in staff member sees staff-only layers (rights detail, readiness status, artist notes, in-progress) while a buyer sees the clean storefront — same component, not a fork."
    - "Staff visibility is resolved SERVER-SIDE via getStaffRole; a client-supplied flag can never reveal staff layers."
    - "Staff-layer chrome renders in the Crate's own light (.fnbl) theme — it does not reach for .fncon dark tokens."
  artifacts:
    - "CatalogRow gains an optional staff field; CatalogBrowserLight gains a staffMode prop and conditional staff-layer rendering."
    - "loadCatalogPage optionally attaches per-row staff layer data when a server-resolved staffMode is passed."
    - "app/sync/catalog/page.tsx resolves staffMode and passes staff layers only for staff."
  key_links:
    - "Staff readinessStatus derives from syncReadinessForTrack (30-01); rightsDetail from stage3/rightsBadge; inProgress + artistNotes(staff_notes) from sync_listings (incl. 30-03 columns)."
    - "The buyer render path is byte-identical when staffMode is absent."
  prohibitions:
    - "MUST NOT fork CatalogBrowserLight or add a second catalogue component."
    - "MUST NOT trust a client-supplied staffMode/isStaff — resolve via getStaffRole server-side."
    - "MUST NOT render staff-layer chrome using .fncon dark tokens inside the .fnbl light component (they will not resolve)."
    - "MUST NOT show any staff layer to a buyer or logged-out visitor."
---

<objective>
Make The Crate role-aware on the one `/sync/catalog` surface: logged-in staff see staff-only layers (rights detail, readiness status, artist notes, in-progress tracks) on the very page buyers browse; buyers see the clean storefront. One component, server-resolved visibility, light-theme-native staff chrome.

Purpose: This is the "same surface, two faces" decision. It is the layered Crate AEs will build Selects from in Phase 31. It must reuse CatalogBrowserLight (never fork) and must resolve staff role server-side.
Output: extended `components/buyer/CatalogBrowserLight.tsx`, `lib/deals/catalog-query.ts`, `app/sync/catalog/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-CONTEXT.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-RESEARCH.md
@components/buyer/CatalogBrowserLight.tsx
@components/buyer/fnbl-theme.ts
@lib/deals/catalog-query.ts
@lib/deals/catalog.ts
@app/sync/catalog/page.tsx
@lib/sync-library/readiness.ts
@lib/sync-library/gate.ts
@lib/admin/gate.ts
@lib/admin/staff-role.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-side staff-layer resolution in loadCatalogPage</name>
  <read_first>
    - lib/deals/catalog-query.ts (as enriched by 30-07) — loadCatalogPage's batched sync_listings admission query, the per-project stage3, and the card assembly loop; note it already resolves admittedProjectIds and per-project documents.
    - lib/sync-library/readiness.ts (30-01) — syncReadinessForTrack for the readiness status string.
    - lib/sync-library/gate.ts (30-01) — rightsBadge/rights signal for a richer staff rights detail.
    - supabase/migrations/107_sync_listings_quality_review.sql (30-03) — staff_notes column to surface as the staff-layer "artist notes".
    - 30-RESEARCH.md "Pattern 5" + "Anti-Patterns" (no fork; single query) and "Pitfall 4" (theme scoping).
  </read_first>
  <files>lib/deals/catalog-query.ts</files>
  <action>
    Add an optional staffMode parameter to loadCatalogPage (default null). When staffMode is a non-null server-resolved role, additionally batch-load the sync_listings rows for the page's projects (status + track_id + staff_notes — one batched query, reusing/extending the admission query already present) and, for each card, attach an optional staff object: readinessStatus (a short string derived from syncReadinessForTrack for a representative track — e.g. 'admitted' | 'pending_admit' | 'needs_completion'), rightsDetail (a human string from the stage3 already computed), artistNotes (from staff_notes, may be null), inProgress (true when a non-admitted, non-terminal listing exists for the project). When staffMode is null, attach nothing — the returned cards are byte-identical to today's buyer output. Never expand the buyer-visible fields; the staff object is separate and only populated for staff. Keep it the single query (no fork).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>DB round-trip: call loadCatalogPage with staffMode='leadership' → cards carry a staff object with readinessStatus/rightsDetail/artistNotes/inProgress; call with staffMode=null → cards have no staff object (identical to buyer output).</manual>
  </verify>
  <done>loadCatalogPage attaches per-row staff layers only when a server-resolved staffMode is passed; single query preserved; tsc green.</done>
  <acceptance_criteria>
    - staffMode defaults to null; buyer output is unchanged when it is null.
    - Staff layer fields derive from syncReadinessForTrack / stage3 / sync_listings (incl. staff_notes) via batched queries.
    - loadCatalogPage is not forked; existing admission/visibility gates intact.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Optional staff layers in CatalogBrowserLight (staffMode prop, light-theme chrome)</name>
  <read_first>
    - components/buyer/CatalogBrowserLight.tsx — the CatalogRow type (lines ~42–63), the top-level props ({ rows, isPublic, embedded }), and a row render block where an extra staff panel can be conditionally inserted.
    - components/buyer/fnbl-theme.ts — the .fnbl light token names to use for the staff-layer chrome (Pitfall 4: do NOT use .fncon dark tokens here).
    - 30-RESEARCH.md "Pattern 5" — optional staff field + staffMode prop, buyer path unchanged when absent.
  </read_first>
  <files>components/buyer/CatalogBrowserLight.tsx</files>
  <action>
    Extend the CatalogRow type with an optional staff field: { readinessStatus: string; rightsDetail: string; artistNotes: string | null; inProgress: boolean }. Add an optional staffMode prop ('leadership' | 'ae' | null, default undefined/null) to the CatalogBrowserLight props. When staffMode is set AND a row carries a staff object, render an additional, visually-distinct staff-only panel per row (rights detail, readiness status, artist notes, an in-progress marker) using ONLY .fnbl light-theme tokens so it reads correctly against the light Crate surface. When staffMode is absent, render exactly the current buyer markup (no staff panel, no layout shift). Keep it the same component; do not branch into a separate render tree beyond the small conditional panel.
    Optionally surface in-progress rows for staff (rows a buyer would not see) ONLY if loadCatalogPage supplies them for staff; do not change the buyer's row set.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Browser-preview: mount with staffMode set + staff-bearing rows → the staff panel renders in light theme with readable contrast; mount without staffMode → the buyer render is unchanged (no staff panel).</manual>
  </verify>
  <done>CatalogRow.staff + staffMode prop + conditional light-theme staff panel shipped; buyer path unchanged; tsc green.</done>
  <acceptance_criteria>
    - staffMode absent → buyer render is unchanged (no staff panel, no layout shift).
    - Staff panel uses .fnbl light tokens (no .fncon references inside this component).
    - The component is not forked; one CatalogRow type serves both faces.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Resolve staffMode server-side on /sync/catalog</name>
  <read_first>
    - app/sync/catalog/page.tsx — the current session branching (no user → public; buyer member → embedded); how loadCatalogPage + mapCardsToLightRows + the fixture fallback are wired.
    - lib/admin/gate.ts / lib/admin/staff-role.ts — getStaffRole(user) to resolve a staff role from the session (the SAME authority /admin/sync-library uses).
    - lib/deals/catalog-sample.ts — mapCardsToLightRows must carry through the staff object onto CatalogRow (extend it if needed, or attach staff alongside the mapped row).
  </read_first>
  <files>app/sync/catalog/page.tsx</files>
  <action>
    In the catalog page, after loading the session, resolve a staff role via getStaffRole(user). When the visitor is staff, call loadCatalogPage with the resolved staffMode and pass staffMode + the staff-bearing rows to CatalogBrowserLight (so staff see the layered Crate on the same URL). When the visitor is a buyer or logged-out, keep the existing clean-storefront behavior exactly (staffMode null/absent). Ensure the mapping to CatalogRow carries the staff object through for staff (adjust mapCardsToLightRows or map the staff field alongside). Never derive staffMode from a query param, cookie the client controls, or any client input — only from the server-resolved session role.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Browser-preview per VALIDATION.md: load /sync/catalog as a buyer (clean, no staff layers) and as a logged-in staff member (staff layers visible — rights/readiness/notes/in-progress). Confirm a buyer cannot force staff layers via any URL/param.</manual>
  </verify>
  <done>/sync/catalog is role-aware: staff see layers on the same surface, buyers stay clean; staffMode is server-resolved; tsc green.</done>
  <acceptance_criteria>
    - Staff session → layered Crate on /sync/catalog; buyer/logged-out → clean storefront (unchanged).
    - staffMode is resolved only from getStaffRole(session user), never from client input.
    - The staff object flows through the view-model mapping to the rendered rows.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| session → staff layers | Only a server-resolved staff role may receive staff-layer data; buyers/anon must not. |
| client flag → staff visibility | No client-supplied flag may unlock staff layers. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Information Disclosure | staff layers on /sync/catalog | critical | mitigate | staffMode resolved via getStaffRole server-side; the staff object is attached server-side only when a role is present; no client flag path exists. |
| T-30-12 | Information Disclosure | in-progress rows | high | mitigate | Non-admitted/in-progress rows are only added to the staff result set server-side; the buyer row set is unchanged. |
| T-30-13 | Usability/Correctness (theme) | staff chrome | low | mitigate | Staff-layer chrome uses .fnbl light tokens (Pitfall 4) so it renders visibly on the light surface. |
| T-30-SC | Tampering | npm installs | low | accept | No new packages. |
</threat_model>

## Artifacts this plan produces
- **Files:** `components/buyer/CatalogBrowserLight.tsx`, `lib/deals/catalog-query.ts`, `app/sync/catalog/page.tsx` (all extended).
- **Symbols:** `CatalogRow.staff` (optional), `staffMode` prop, loadCatalogPage `staffMode` param.
- **Migrations:** none.

<verification>
- `npx tsc --noEmit` green; `npx next build` green at wave end.
- grep confirms no new catalogue component file and no `.fncon` token inside CatalogBrowserLight.
- Browser-preview confirms buyer-clean vs staff-layered on the same URL; staffMode cannot be client-forced.
</verification>

<success_criteria>
- One /sync/catalog surface serves both faces: staff-layered for team members, clean for buyers, with server-resolved visibility and light-theme-native staff chrome — no fork.
</success_criteria>

<output>
Create `.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-08-SUMMARY.md` when done.
</output>
