---
phase: 30-the-crate-sync-library-catalogue-engine-sync-readiness
plan: 09
type: execute
wave: 3
depends_on: [30-05, 30-04]
files_modified:
  - components/admin/SyncReadinessWorklist.tsx
  - app/(admin)/admin/sync-library/page.tsx
  - components/admin/SyncLibraryAdmin.tsx
autonomous: true
requirements: [CRATE-03, CRATE-05]
must_haves:
  truths:
    - "Sync Library shows a Sync Readiness worklist: every incomplete track with exactly what's missing, where staff work the gaps down and can set the quality review + guidance notes."
    - "Admit/Reject controls render for leadership only; an AE sees the queue and the worklist (browse) but no admit/reject/quality controls (the route enforces this regardless)."
    - "The backstage stays in the dark .fncon Team Console theme."
  artifacts:
    - "components/admin/SyncReadinessWorklist.tsx (new) renders the worklist rows + gaps in .fncon."
    - "app/(admin)/admin/sync-library/page.tsx loads the worklist and passes it + role into the console."
    - "components/admin/SyncLibraryAdmin.tsx gates admit/reject on leadership and surfaces gate signals."
  key_links:
    - "The worklist UI consumes GET /api/sync-library/worklist (30-05); admit/reject + quality controls call the leadership-only routes (30-04)."
    - "Access UX mirrors the route: isLeadership hides admit/reject/quality for AE (defense-in-depth, not the security boundary)."
  prohibitions:
    - "MUST NOT show admit/reject/quality controls to an AE (UX mirror of the leadership-only routes)."
    - "MUST NOT treat hiding controls as the security boundary — the routes remain the authority."
    - "MUST NOT use .fnbl light tokens in the admin console (it is .fncon dark)."
    - "MUST NOT re-implement readiness/worklist logic in the client — render what the worklist route returns."
---

<objective>
Build the Sync Library backstage UI: a Sync Readiness worklist where staff see every incomplete track with exactly what's missing (and set the quality review + guidance notes), plus the access-model UX so admit/reject/quality controls appear for leadership only while AEs browse.

Purpose: This is the room where staff guide artists to sync-ready. It surfaces the worklist (30-05), wires the leadership-only curation + quality routes (30-04), and reconciles the visible controls with the locked access model.
Output: new `components/admin/SyncReadinessWorklist.tsx`; extended `app/(admin)/admin/sync-library/page.tsx` and `components/admin/SyncLibraryAdmin.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-CONTEXT.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-RESEARCH.md
@app/(admin)/admin/sync-library/page.tsx
@components/admin/SyncLibraryAdmin.tsx
@lib/sync-library/worklist.ts
@app/api/sync-library/worklist/route.ts
@lib/admin/gate.ts
@lib/admin/staff-role.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync Readiness worklist component (components/admin/SyncReadinessWorklist.tsx)</name>
  <read_first>
    - components/admin/SyncLibraryAdmin.tsx — the .fncon token vocabulary (CHIP_BASE/CHIP_ON/CHIP_OFF, QUEUE_CHIP color classes, formatTimeSince, parseError) and the optimistic decision-state idiom to mirror; note "never bg-ink/text-lav here — .fncon only".
    - lib/sync-library/worklist.ts (30-05) — the WorklistRow shape (missing[], qualityOk, staffNotes) the component renders.
    - app/api/sync-library/admin/[listingId]/quality/route.ts (30-04) — the quality/notes POST contract the component calls.
  </read_first>
  <files>components/admin/SyncReadinessWorklist.tsx</files>
  <action>
    Create a client component SyncReadinessWorklist that takes the worklist rows (WorklistRow[]) and the isLeadership flag as props and renders, in the dark .fncon theme, one entry per incomplete track: song + project + artist, its appliedAt age (reuse formatTimeSince idiom), and the exact missing[] items as chips/list ("exactly what's missing"). For leadership, render inline controls to set the quality review (quality_ok pass/fail + optional note) and a guidance staff_notes field, POSTing to the quality route (30-04) with optimistic state + parseError handling. For an AE, render the same rows READ-ONLY (browse) with no quality/notes controls. Use ONLY .fncon CSS-variable tokens.
    Keep all readiness/gap content driven by the passed rows — do not recompute readiness client-side.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Browser-preview: as leadership, the worklist lists incomplete tracks with their missing items and a working quality/notes control (POST persists, re-read confirms); as an AE, the same list renders read-only (no quality/notes controls).</manual>
  </verify>
  <done>SyncReadinessWorklist renders gaps in .fncon with leadership-only quality/notes controls; tsc green.</done>
  <acceptance_criteria>
    - Each row shows the exact missing[] items from the worklist route (no client-side readiness recompute).
    - Quality/notes controls render only when isLeadership; AE sees read-only rows.
    - Only .fncon tokens are used (no .fnbl/bg-ink/text-lav).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire the worklist into the Sync Library page + pass role</name>
  <read_first>
    - app/(admin)/admin/sync-library/page.tsx — the existing staff check (leadership + ae), isLeadership, the batched listing/track/project/artist loads, and how SyncLibraryAdmin is mounted; note the page currently allows leadership+ae in.
    - app/api/sync-library/worklist/route.ts (30-05) — but prefer server-side: the page can build the worklist directly via buildWorklist using the data it already loads, OR fetch the route; choose the server-side buildWorklist path to avoid a client round-trip (mirror the existing server-render pattern).
    - lib/sync-library/worklist.ts — buildWorklist inputs.
  </read_first>
  <files>app/(admin)/admin/sync-library/page.tsx</files>
  <action>
    Extend the Sync Library page to also assemble the Sync Readiness worklist server-side (reuse the buildWorklist shaper over the data the page already batch-loads, plus per-project documents needed for readiness — extend the existing loads minimally) and render the new SyncReadinessWorklist alongside the existing curation queue (e.g. a "Sync Readiness" section/tab under the "Sync Library" heading). Pass isLeadership through to the worklist so AE gets browse-only. Keep the page's existing staff gate (leadership + ae may view the room); curation/quality writes remain leadership-only at the routes. Preserve the current queue rendering.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Browser-preview: /admin/sync-library shows the curation queue AND a Sync Readiness worklist section; leadership sees quality/notes controls, AE sees the worklist read-only.</manual>
  </verify>
  <done>Sync Library page renders the worklist + curation queue with role passed through; tsc green.</done>
  <acceptance_criteria>
    - The worklist section renders on /admin/sync-library with accurate incomplete-track rows.
    - isLeadership is passed to both the queue and the worklist.
    - The existing curation queue behavior is preserved.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Access-model UX in SyncLibraryAdmin (leadership-only admit/reject + gate signals)</name>
  <read_first>
    - components/admin/SyncLibraryAdmin.tsx — where Admit/Reject actions render on pending_admit rows, and how isLeadership is already used to conditionally render the leadership-only Remove action (mirror that exact pattern for admit/reject).
    - app/api/sync-library/admin/[listingId]/route.ts (30-04) — admit/reject is now leadership-only (403 for AE) and gate-guarded; the UI must mirror this and handle the new 409 needs-completion response.
  </read_first>
  <files>components/admin/SyncLibraryAdmin.tsx</files>
  <action>
    Gate the Admit/Reject controls on isLeadership exactly as the Remove action already is — for an AE, do NOT render admit/reject (they browse the queue only). Handle the admit route's new 409 "needs completion" response: on a 409, show a clear inline message directing staff to the Sync Readiness worklist (the track must finish its checklist / pass quality review) rather than a generic error, and keep the row in place (not admitted, not rejected). Optionally surface a compact gate-signal indicator per pending_admit row (rights/quality/metadata) if the data is available on the row, so leadership can see why admit is blocked. Keep all styling in .fncon. Add a header note that hiding admit/reject for AE is a UX mirror; the route is the security authority.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Browser-preview: as leadership, admit/reject render; admitting a gate-failing track shows the "finish the checklist" message (row stays). As an AE, admit/reject are absent (browse only). Confirm an AE attempting the API directly still gets 403 (route authority).</manual>
  </verify>
  <done>Admit/reject are leadership-only in the UI; the 409 needs-completion response is handled gracefully; tsc green.</done>
  <acceptance_criteria>
    - Admit/Reject controls are hidden for AE (mirroring Remove) and shown for leadership.
    - A 409 needs-completion response is surfaced as a clear "finish the checklist / quality review" message; the row is not marked admitted or rejected.
    - Styling stays .fncon; the header notes the route is the security boundary.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AE session → curation controls | AE must not perform curation/quality writes; the UI mirrors this but the routes enforce it. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-06 | Elevation of Privilege | admit/reject/quality UX | high | mitigate | Controls hidden for AE as defense-in-depth; the leadership-only routes (30-04) remain the actual authority (403 for AE). |
| T-30-01 | Elevation of Privilege | worklist page | medium | mitigate | The page keeps its existing staff gate; the worklist route is requireStaff() gated. |
| T-30-05 | Repudiation | quality/notes writes from the worklist | medium | mitigate | Writes go through the audited quality route (logStaffAction). |
| T-30-SC | Tampering | npm installs | low | accept | No new packages. |
</threat_model>

## Artifacts this plan produces
- **Files:** `components/admin/SyncReadinessWorklist.tsx` (new); `app/(admin)/admin/sync-library/page.tsx`, `components/admin/SyncLibraryAdmin.tsx` (extended).
- **Behavior:** the Sync Readiness worklist + leadership-only curation UX in the backstage.
- **Migrations:** none.

<verification>
- `npx tsc --noEmit` green; `npx next build` green at wave end (phase gate).
- grep confirms admit/reject render is guarded by `isLeadership` in SyncLibraryAdmin (mirroring the existing Remove guard) and the worklist component uses only .fncon tokens.
- Browser-preview confirms leadership vs AE control visibility and the 409 needs-completion message.
</verification>

<success_criteria>
- Staff work incomplete tracks down from a real worklist showing exactly what's missing; leadership curates (admit/reject/quality) while AEs browse; the backstage stays dark-themed and honest to the route-level access model.
</success_criteria>

<output>
Create `.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-09-SUMMARY.md` when done.
</output>
