---
phase: 30-the-crate-sync-library-catalogue-engine-sync-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/107_sync_listings_quality_review.sql
  - supabase/migrations/108_anr_staff_role.sql
  - lib/admin/staff-role.ts
autonomous: false
requirements: [CRATE-09, CRATE-10]
user_setup:
  - service: supabase
    why: "Migrations are owner-run — an agent never executes supabase db push."
    dashboard_config:
      - task: "Review migrations 107 + 108 then run supabase db push (owner-only)"
        location: "Local Supabase CLI / project — via the owner's Codex push flow (mirrors migrations 096/106)"
must_haves:
  truths:
    - "sync_listings gains a persisted, auditable manual quality-review signal and a per-listing staff-notes field (migration 107)."
    - "A new staff role `anr` (A&R) exists alongside leadership/ae/bd: funun_staff.staff_role's CHECK is widened (migration 108) and lib/admin/staff-role.ts's StaffRole union + ALL_STAFF_ROLES include 'anr'."
    - "Migration 107 adds columns ONLY — it does NOT widen sync_listings.status or its CHECK constraint (no new 'needs_completion' status). Migration 108 widens ONLY the funun_staff role CHECK (additive)."
    - "The new sync_listings columns are REVOKE-locked from authenticated/anon and written only via the service role (RLS doctrine)."
  artifacts:
    - "supabase/migrations/107_sync_listings_quality_review.sql (quality-review + staff_notes columns) — text-tested, human-gated."
    - "supabase/migrations/108_anr_staff_role.sql (funun_staff staff_role CHECK widened to add 'anr') — text-tested, human-gated."
    - "lib/admin/staff-role.ts — StaffRole union + ALL_STAFF_ROLES (+ any label map) include 'anr'."
  key_links:
    - "quality_ok feeds the inclusion gate's qualityOk signal (30-04); staff_notes feeds the worklist + Crate staff layer."
    - "The 'anr' StaffRole is the tag-approval authority the 30-06 approve route gates on (requireStaff(['leadership','anr']))."
  prohibitions:
    - "MUST NOT run `supabase db push` from the agent — owner reviews and pushes BOTH migrations (matches migrations 080/081/096/106 convention)."
    - "MUST NOT widen the sync_listings status enum / add a status value — incomplete is a COMPUTED readiness signal, not a stored status."
    - "MUST NOT grant authenticated/anon write access to the new sync_listings columns."
    - "MUST NOT give A&R (anr) admit/reject/remove curation power in this phase — its only Phase-30 authority is approving/rejecting AE tag proposals."
---

<objective>
Draft the two owner-run migrations this phase needs and the paired role-model code change: (107) additive quality-review + staff-notes columns on `sync_listings` so the inclusion gate's manual quality judgment (v1) is persisted and auditable; (108) widen `funun_staff.staff_role`'s CHECK to introduce the new `anr` (A&R) staff role; and update `lib/admin/staff-role.ts` so `StaffRole`/`ALL_STAFF_ROLES` include `anr`.

Purpose: The gate's third check — the quality bar — is a manual staff judgment for v1 that needs a home on the per-song row (deliberately additive-columns-only: no status/CHECK widening, so the state machine can't drift). Separately, the owner's tag-approval workflow introduces an A&R role that approves AE tag proposals — a cross-cutting role-model addition (funun_staff CHECK + the StaffRole union). Both DB changes are owner-run and share one blocking-human push gate.
Output: `supabase/migrations/107_sync_listings_quality_review.sql`, `supabase/migrations/108_anr_staff_role.sql`, and the `lib/admin/staff-role.ts` change — drafted + text-tested, then OWNER-pushed and verified live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-CONTEXT.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-RESEARCH.md
@supabase/migrations/096_sync_library.sql
@supabase/migrations/089_funun_staff_and_audit.sql
@lib/admin/staff-role.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft migration 107 — sync_listings quality-review + staff-notes columns</name>
  <read_first>
    - supabase/migrations/096_sync_library.sql — the FULL file: the sync_listings CREATE, the human-gated header banner, the RLS block (REVOKE from authenticated/anon, service-role-only writes), the COMMENT ON conventions, and the T-26-XX inline STRIDE comments.
    - 30-RESEARCH.md "Project Constraints" (owner-run migration convention; T-30-XX inline threat comments) and "Assumptions Log A4" (modeling incomplete OUTSIDE status — no enum widening).
  </read_first>
  <files>supabase/migrations/107_sync_listings_quality_review.sql</files>
  <action>
    Draft migration 107 with the SAME human-gated header banner migration 096 uses (state plainly: this project never runs supabase db push from an agent; owner reviews + pushes; drafted + text-tested only).
    ALTER TABLE sync_listings ADD COLUMN (all nullable, additive): quality_ok BOOLEAN (null = not yet reviewed, true = passed the sync-fit/quality bar, false = failed), quality_note TEXT, quality_reviewed_by UUID REFERENCES auth.users(id), quality_reviewed_at TIMESTAMPTZ, and staff_notes TEXT (per-listing staff guidance shown in the worklist + the Crate staff layer).
    Add COMMENT ON COLUMN lines explaining each field's role, matching 096's commenting style, and note that quality_ok is the manual v1 quality bar (no automated audio analysis) that feeds the inclusion gate.
    Reapply the RLS/grant doctrine so the new columns are only writable via the service role: confirm sync_listings already REVOKEs write from authenticated/anon (it does, per 096) and add an explicit reaffirming comment (columns inherit the table's existing RLS — no per-column grants). Do NOT touch the status column or its CHECK constraint. Add inline T-30-05 (Repudiation — quality writes audited via logStaffAction at the route) and T-30-06 (EoP — leadership-only writers) STRIDE comments referencing the routes that will enforce them.
    Text-test only: read the SQL back for correctness; DO NOT execute it.
  </action>
  <verify>
    <automated>test -f supabase/migrations/107_sync_listings_quality_review.sql && grep -q "HUMAN-GATED" supabase/migrations/107_sync_listings_quality_review.sql && echo OK</automated>
    <manual>Read the SQL: five additive nullable columns; NO ALTER of status/CHECK; header banner present; COMMENTs present; RLS reaffirmed. Do not push.</manual>
  </verify>
  <done>Migration 107 drafted with the owner-run banner, additive columns only, RLS reaffirmed, text-tested — NOT pushed.</done>
  <acceptance_criteria>
    - File exists and carries the HUMAN-GATED / "never db push from an agent" banner.
    - Only ADD COLUMN statements against sync_listings; no status/CHECK/enum change anywhere.
    - Every new column has a COMMENT; quality_ok documented as the manual v1 quality bar.
    - The agent did not execute supabase db push (no live schema change performed by the agent).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Draft migration 108 (A&R role CHECK widen) + StaffRole code change</name>
  <read_first>
    - supabase/migrations/089_funun_staff_and_audit.sql — the funun_staff table, its `staff_role TEXT NOT NULL CHECK (staff_role IN ('leadership','ae','bd'))`, the COMMENT noting staff_role is a DISPLAY COPY of the authoritative app_metadata.staff_role, and the zero-RLS/service-role-only shape.
    - supabase/migrations/096_sync_library.sql — the DROP CONSTRAINT / ADD CONSTRAINT constraint-widening convention (used there for capability_grants/vault_documents.type) to mirror for the staff_role CHECK.
    - lib/admin/staff-role.ts — the StaffRole union ('leadership' | 'ae' | 'bd'), ALL_STAFF_ROLES, and getStaffRole (which must recognize 'anr'); note gate.ts re-exports these.
    - 30-CONTEXT.md resolved-after-research: "NEW staff role A&R (anr)" — A&R's Phase-30 power is approving/rejecting AE tag proposals only; admit/reject stays leadership-only.
  </read_first>
  <files>supabase/migrations/108_anr_staff_role.sql, lib/admin/staff-role.ts</files>
  <action>
    Draft migration 108 with the SAME human-gated header banner (never db push from an agent; owner reviews + pushes; drafted + text-tested only). Widen funun_staff.staff_role's CHECK to add 'anr' using the DROP CONSTRAINT then ADD CONSTRAINT constraint-widening pattern migration 096 uses (find the existing named check constraint on staff_role, drop it, re-add it as `CHECK (staff_role IN ('leadership','ae','bd','anr'))`). Add a COMMENT/inline note recording that A&R is a tag-approval authority (Phase 30), that staff_role here is still a DISPLAY COPY of app_metadata.staff_role, and inline T-30-06 (EoP) STRIDE note. Text-test only; DO NOT execute.
    In the SAME task, update lib/admin/staff-role.ts (code, ships live — no gate): add 'anr' to the StaffRole union and to ALL_STAFF_ROLES, extend getStaffRole to recognize meta.staff_role === 'anr', and add an 'anr' entry to any staff-role label map that exists (or add one if the file/UI expects labels). This is safe to ship before the migration because getStaffRole reads app_metadata (no DB dependency) and no funun_staff row holds 'anr' until 108 is applied. Confirm gate.ts's re-exports still compile.
  </action>
  <verify>
    <automated>test -f supabase/migrations/108_anr_staff_role.sql && grep -q "HUMAN-GATED" supabase/migrations/108_anr_staff_role.sql && npx tsc --noEmit</automated>
    <manual>Read migration 108: DROP/ADD CHECK adds only 'anr' (additive), owner-run banner present, no other change. grep confirms lib/admin/staff-role.ts's StaffRole union + ALL_STAFF_ROLES include 'anr' and getStaffRole returns it. Do not push.</manual>
  </verify>
  <done>Migration 108 drafted (additive CHECK widen for 'anr', owner-run banner, text-tested, NOT pushed); StaffRole union/ALL_STAFF_ROLES/getStaffRole include 'anr'; tsc green.</done>
  <acceptance_criteria>
    - 108 carries the HUMAN-GATED banner and only DROP/ADD the staff_role CHECK to add 'anr' (no other schema change).
    - lib/admin/staff-role.ts StaffRole union + ALL_STAFF_ROLES include 'anr'; getStaffRole recognizes it; gate.ts re-exports still compile (tsc green).
    - The agent did not execute supabase db push.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: OWNER-RUN gate — owner applies migrations 107 + 108 (agent never pushes)</name>
  <action>STOP. This is a blocking human gate. The agent MUST NOT run `supabase db push`. Wait for the owner to review + apply BOTH migration 107 and migration 108 and confirm "pushed" before running the live-verification task. This mirrors the migrations 080/081/096/106 owner-run convention.</action>
  <what-built>Migration 107 (additive sync_listings quality-review + staff_notes columns) and migration 108 (funun_staff staff_role CHECK widened to add 'anr') are drafted and text-tested. Neither is applied yet — this project applies migrations owner-side only. The lib/admin/staff-role.ts code change ships with the plan commit (no gate).</what-built>
  <how-to-verify>
    1. Review supabase/migrations/107_sync_listings_quality_review.sql (additive columns only, no status/CHECK change) and supabase/migrations/108_anr_staff_role.sql (DROP/ADD staff_role CHECK, additive 'anr' only) — owner-run banners present.
    2. Apply both via your normal owner push flow (the Codex `supabase db push` path used for migrations 096/106).
    3. Confirm LOCAL=REMOTE (migration history shows 107 + 108 applied with no drift).
    4. Reply "pushed" (or describe any error) so the agent can run the live verification task.
  </how-to-verify>
  <resume-signal>Type "pushed" once migrations 107 + 108 are applied, or describe the error.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Live verification of migrations 107 + 108 (service-role round-trip)</name>
  <read_first>
    - lib/supabase/server.ts — createServiceClient() factory used for the round-trip.
    - supabase/migrations/107_sync_listings_quality_review.sql — the exact column names to probe.
    - supabase/migrations/108_anr_staff_role.sql — the widened staff_role CHECK.
  </read_first>
  <files>supabase/migrations/107_sync_listings_quality_review.sql, supabase/migrations/108_anr_staff_role.sql</files>
  <action>
    After the owner confirms "pushed", verify both migrations are live WITHOUT further code change, via the service-role client (a throwaway node/tsx one-liner or an existing admin read path):
    (a) SELECT quality_ok, quality_note, quality_reviewed_by, quality_reviewed_at, staff_notes from sync_listings LIMIT 1 and confirm the query succeeds (columns resolve) rather than erroring with "column does not exist".
    (b) Confirm the funun_staff staff_role CHECK now admits 'anr' — e.g. read the constraint definition, or perform a service-role no-op that would violate the OLD constraint but satisfy the new one (do NOT leave test rows behind; roll back / delete any probe row).
    Record both round-trip results in the SUMMARY. Do not seed persistent data.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Service-role SELECT of the five sync_listings columns returns without a "column does not exist" error; the funun_staff staff_role CHECK accepts 'anr'. Downstream plans (30-04/30-05 quality; 30-06 A&R approval) may now rely on both.</manual>
  </verify>
  <done>Both migrations confirmed live via service-role round-trips; downstream gate/worklist/tag-approval plans unblocked.</done>
  <acceptance_criteria>
    - A service-role SELECT of quality_ok/quality_note/quality_reviewed_by/quality_reviewed_at/staff_notes succeeds against the live DB.
    - The funun_staff staff_role CHECK admits 'anr' live (verified without leaving probe rows behind).
    - No agent-run supabase db push occurred; both schema changes were owner-applied.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| authenticated/anon → sync_listings | Buyers/artists must never write curation columns; only the service role (staff routes) may. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-06 | Elevation of Privilege | new sync_listings columns + anr role | high | mitigate | sync_listings columns inherit its REVOKE-from-authenticated/anon RLS (096), writes via the leadership-gated route (30-04); the 'anr' role is additive and carries ONLY tag-approval authority (30-06), never admit/reject. |
| T-30-05 | Repudiation | quality/notes writes | medium | mitigate | Every write logs via logStaffAction (enforced in 30-04); inline T-30-05 comment records the requirement. |
| T-30-SC | Tampering | schema push (107 + 108) | high | mitigate | OWNER-RUN human checkpoint (blocking-human, never auto-approved) — the agent never runs db push; owner reviews + applies BOTH migrations. |
</threat_model>

## Artifacts this plan produces
- **Migrations (OWNER-RUN):** `supabase/migrations/107_sync_listings_quality_review.sql` — additive columns `quality_ok`, `quality_note`, `quality_reviewed_by`, `quality_reviewed_at`, `staff_notes` on `sync_listings`; `supabase/migrations/108_anr_staff_role.sql` — widen `funun_staff.staff_role` CHECK to add `'anr'`.
- **Files/Symbols:** `lib/admin/staff-role.ts` — `StaffRole` union + `ALL_STAFF_ROLES` (+ label map) include `'anr'`; `getStaffRole` recognizes it.

<verification>
- Both migration files present with owner-run banners; 107 additive columns only (no status/CHECK change); 108 DROP/ADD staff_role CHECK adding only 'anr'.
- `npx tsc --noEmit` green with the widened StaffRole union; grep confirms 'anr' in the union + ALL_STAFF_ROLES.
- Post-push: service-role SELECT of the five sync_listings columns succeeds live; funun_staff CHECK admits 'anr'.
</verification>

<success_criteria>
- The manual quality-bar signal + staff notes have a persisted, RLS-protected home on the per-song row (no status-machine change), and the A&R (`anr`) role exists in both the DB CHECK and the StaffRole union — all owner-applied and verified live.
</success_criteria>

<output>
Create `.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-03-SUMMARY.md` when done.
</output>
