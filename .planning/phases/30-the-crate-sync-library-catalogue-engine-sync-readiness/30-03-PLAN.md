---
phase: 30-the-crate-sync-library-catalogue-engine-sync-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/107_sync_listings_quality_review.sql
autonomous: false
requirements: [CRATE-09]
user_setup:
  - service: supabase
    why: "Migration is owner-run — an agent never executes supabase db push."
    dashboard_config:
      - task: "Review migration 107 then run supabase db push (owner-only)"
        location: "Local Supabase CLI / project — via the owner's Codex push flow (mirrors migrations 096/106)"
must_haves:
  truths:
    - "sync_listings gains a persisted, auditable manual quality-review signal and a per-listing staff-notes field."
    - "The migration adds columns ONLY — it does NOT widen sync_listings.status or its CHECK constraint (no new 'needs_completion' status)."
    - "The new columns are REVOKE-locked from authenticated/anon and written only via the service role (RLS doctrine)."
  artifacts:
    - "supabase/migrations/107_sync_listings_quality_review.sql exists, text-tested, human-gated."
  key_links:
    - "quality_ok feeds the inclusion gate's qualityOk signal (30-04); staff_notes feeds the worklist + Crate staff layer."
  prohibitions:
    - "MUST NOT run `supabase db push` from the agent — owner reviews and pushes (matches migrations 080/081/096/106 convention)."
    - "MUST NOT widen the status enum / add a status value — incomplete is a COMPUTED readiness signal, not a stored status."
    - "MUST NOT grant authenticated/anon write access to the new columns."
---

<objective>
Draft the one owner-run migration this phase needs: additive quality-review and staff-notes columns on `sync_listings` so the inclusion gate's manual quality judgment (v1) is persisted and auditable, and staff can attach guidance notes per listing.

Purpose: The gate's third check — the quality bar — is a manual staff judgment for v1. To make it a real, structured, worklist-visible gate signal (not an implicit side effect of clicking Admit) it needs a home on the per-song row. Deliberately additive-columns-only: no status/CHECK widening, so the state machine and its LEGAL_TRANSITIONS table cannot drift.
Output: `supabase/migrations/107_sync_listings_quality_review.sql`, drafted + text-tested, then OWNER-pushed and verified live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-CONTEXT.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-RESEARCH.md
@supabase/migrations/096_sync_library.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft migration 107 — sync_listings quality-review + staff-notes columns</name>
  <read_first>
    - supabase/migrations/096_sync_library.sql — the FULL file: the sync_listings CREATE, the human-gated header banner, the RLS block (REVOKE from authenticated/anon, service-role-only writes), the COMMENT ON conventions, and the T-26-XX inline STRIDE comments.
    - 30-RESEARCH.md "Project Constraints" (owner-run migration convention; T-30-XX inline threat comments) and "Assumptions Log A4" (modeling incomplete OUTSIDE status — no enum widening).
  </read_first>
  <files>supabase/migrations/107_sync_listings_quality_review.sql</files>
  <action>
    Draft migration 107 with the SAME human-gated header banner migration 096 uses (state plainly: this project never runs supabase db push from an agent; owner reviews + pushes; drafted + text-tested only).
    ALTER TABLE sync_listings ADD COLUMN (all nullable, additive): quality_ok BOOLEAN (null = not yet reviewed, true = passed the sync-fit/quality bar, false = failed), quality_note TEXT, quality_reviewed_by UUID REFERENCES auth.users(id), quality_reviewed_at TIMESTAMPTZ, and staff_notes TEXT (per-listing staff guidance shown in the worklist + the Crate staff layer).
    Add COMMENT ON COLUMN lines explaining each field's role, matching 096's commenting style, and note that quality_ok is the manual v1 quality bar (no automated audio analysis) that feeds the inclusion gate.
    Reapply the RLS/grant doctrine so the new columns are only writable via the service role: confirm sync_listings already REVOKEs write from authenticated/anon (it does, per 096) and add an explicit reaffirming comment (columns inherit the table's existing RLS — no per-column grants). Do NOT touch the status column or its CHECK constraint. Add inline T-30-05 (Repudiation — quality writes audited via logStaffAction at the route) and T-30-06 (EoP — leadership-only writers) STRIDE comments referencing the routes that will enforce them.
    Text-test only: read the SQL back for correctness; DO NOT execute it.
  </action>
  <verify>
    <automated>test -f supabase/migrations/107_sync_listings_quality_review.sql && grep -q "HUMAN-GATED" supabase/migrations/107_sync_listings_quality_review.sql && echo OK</automated>
    <manual>Read the SQL: five additive nullable columns; NO ALTER of status/CHECK; header banner present; COMMENTs present; RLS reaffirmed. Do not push.</manual>
  </verify>
  <done>Migration 107 drafted with the owner-run banner, additive columns only, RLS reaffirmed, text-tested — NOT pushed.</done>
  <acceptance_criteria>
    - File exists and carries the HUMAN-GATED / "never db push from an agent" banner.
    - Only ADD COLUMN statements against sync_listings; no status/CHECK/enum change anywhere.
    - Every new column has a COMMENT; quality_ok documented as the manual v1 quality bar.
    - The agent did not execute supabase db push (no live schema change performed by the agent).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: OWNER-RUN gate — owner applies migration 107 (agent never pushes)</name>
  <action>STOP. This is a blocking human gate. The agent MUST NOT run `supabase db push`. Wait for the owner to review + apply migration 107 and confirm "pushed" before running the live-verification task. This mirrors the migrations 080/081/096/106 owner-run convention.</action>
  <what-built>Migration 107 (additive sync_listings quality-review + staff_notes columns) is drafted and text-tested. It is NOT yet applied — this project applies migrations owner-side only.</what-built>
  <how-to-verify>
    1. Review supabase/migrations/107_sync_listings_quality_review.sql — confirm additive columns only, no status/CHECK change, owner-run banner present.
    2. Apply it via your normal owner push flow (the Codex `supabase db push` path used for migrations 096/106).
    3. Confirm LOCAL=REMOTE (migration history shows 107 applied with no drift).
    4. Reply "pushed" (or describe any error) so the agent can run the live verification task.
  </how-to-verify>
  <resume-signal>Type "pushed" once migration 107 is applied, or describe the error.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Live verification of migration 107 (service-role round-trip)</name>
  <read_first>
    - lib/supabase/server.ts — createServiceClient() factory used for the round-trip.
    - supabase/migrations/107_sync_listings_quality_review.sql — the exact column names to probe.
  </read_first>
  <files>supabase/migrations/107_sync_listings_quality_review.sql</files>
  <action>
    After the owner confirms "pushed", verify the columns are live WITHOUT a code change: using the service-role client (a throwaway node/tsx one-liner or an existing admin read path), SELECT quality_ok, quality_note, quality_reviewed_by, quality_reviewed_at, staff_notes from sync_listings LIMIT 1 and confirm the query succeeds (columns resolve) rather than erroring with "column does not exist". This is the DB round-trip verification standard (no unit framework).
    Record the round-trip result in the SUMMARY. Do not seed data.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Service-role SELECT of the five new columns from sync_listings returns without a "column does not exist" error (columns are live). Downstream plans (30-04/30-05) may now read/write them.</manual>
  </verify>
  <done>The five new columns confirmed live via a service-role SELECT; downstream gate/worklist plans unblocked.</done>
  <acceptance_criteria>
    - A service-role SELECT of quality_ok/quality_note/quality_reviewed_by/quality_reviewed_at/staff_notes succeeds against the live DB.
    - No agent-run supabase db push occurred; the schema change was owner-applied.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| authenticated/anon → sync_listings | Buyers/artists must never write curation columns; only the service role (staff routes) may. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-06 | Elevation of Privilege | new sync_listings columns | high | mitigate | Columns inherit sync_listings' existing REVOKE-from-authenticated/anon RLS (migration 096); writes go only through the leadership-gated route in 30-04. |
| T-30-05 | Repudiation | quality/notes writes | medium | mitigate | Every write logs via logStaffAction (enforced in 30-04); inline T-30-05 comment records the requirement. |
| T-30-SC | Tampering | schema push | high | mitigate | OWNER-RUN human checkpoint (blocking-human, never auto-approved) — the agent never runs db push; owner reviews + applies. |
</threat_model>

## Artifacts this plan produces
- **Migration:** `supabase/migrations/107_sync_listings_quality_review.sql` (OWNER-RUN) — additive columns `quality_ok`, `quality_note`, `quality_reviewed_by`, `quality_reviewed_at`, `staff_notes` on `sync_listings`.
- **Files/Symbols:** none beyond the migration.

<verification>
- Migration file present with owner-run banner; additive columns only; no status/CHECK change.
- Post-push: service-role SELECT of the five columns succeeds live.
</verification>

<success_criteria>
- The manual quality-bar signal and staff notes have a persisted, RLS-protected home on the per-song row, applied by the owner and verified live — with zero change to the sync_listings status state machine.
</success_criteria>

<output>
Create `.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-03-SUMMARY.md` when done.
</output>
