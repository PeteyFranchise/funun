---
phase: 30-the-crate-sync-library-catalogue-engine-sync-readiness
plan: 04
type: execute
wave: 2
depends_on: [30-01, 30-03]
files_modified:
  - app/api/sync-library/admin/[listingId]/route.ts
  - app/api/sync-library/admin/[listingId]/quality/route.ts
autonomous: true
requirements: [CRATE-04, CRATE-05, CRATE-09]
must_haves:
  truths:
    - "admit/reject is LEADERSHIP-ONLY; an AE calling it is Forbidden (403) — AE keeps browse & pull only."
    - "A track can only be admitted when the inclusion gate returns admit_eligible (rights clear + quality ok + metadata complete); otherwise the admit is refused and the track stays in the completion pipeline (never auto-rejected)."
    - "Staff set the manual quality judgment + guidance notes through a dedicated leadership-gated route; every write is audited."
  artifacts:
    - "app/api/sync-library/admin/[listingId]/route.ts tightened to requireStaff(['leadership']) and gate-aware before admit."
    - "app/api/sync-library/admin/[listingId]/quality/route.ts (new) writes quality_ok/quality_note/staff_notes, leadership-only, audited."
  key_links:
    - "The admit branch composes evaluateInclusionGate() over computeStage3() (rights), sync_listings.quality_ok (quality), and syncReadinessForTrack() metadata completeness."
    - "isValidTransition() remains the single legal-transition authority — the gate is an ADDITIONAL admit precondition, not a replacement."
  prohibitions:
    - "MUST NOT leave requireStaff(['leadership','ae']) on the admit/reject route — tighten to ['leadership'] (resolved-after-research: access fix)."
    - "MUST NOT reject an incomplete track — a failed gate returns a 409/needs_completion refusal that keeps the track in the pipeline."
    - "MUST NOT trust any client-supplied readiness/quality/gate value — recompute server-side from DB-loaded data."
    - "MUST NOT spread the request body into the DB write — fixed allowlisted columns only."
---

<objective>
Wire the inclusion gate into the real curation flow and ship the access fix: tighten admit/reject to leadership-only, refuse to admit a track that fails the gate (routing it into the completion pipeline instead of live), and add a leadership-only route for staff to record the manual quality judgment and guidance notes.

Purpose: This is where the gate stops being theory and becomes the actual admission control. It also reconciles the shipped Phase 26 code with the locked access model (AE = browse & pull only; leadership = full curation).
Output: extended `app/api/sync-library/admin/[listingId]/route.ts`; new `app/api/sync-library/admin/[listingId]/quality/route.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-CONTEXT.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-RESEARCH.md
@app/api/sync-library/admin/[listingId]/route.ts
@lib/sync-library/gate.ts
@lib/sync-library/readiness.ts
@lib/vault/stage3.ts
@lib/admin/gate.ts
@lib/staff/audit.ts
@lib/deals/catalog-query.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten admit/reject to leadership-only + compose the inclusion gate before admit</name>
  <read_first>
    - app/api/sync-library/admin/[listingId]/route.ts — the FULL current route: requireStaff(['leadership','ae']), the decision validation, the DB-loaded target, isValidTransition() double-decide guard, the admit/reject write blocks with fixed allowlisted columns, the first-admit notification, and the UNCONDITIONAL logStaffAction.
    - lib/sync-library/gate.ts + lib/sync-library/readiness.ts (from 30-01) — evaluateInclusionGate, GateSignal, syncReadinessForTrack, isSyncMetadataComplete.
    - lib/vault/stage3.ts — computeStage3(project, tracks, documents, readinessScore) → canContinue (rights signal).
    - lib/deals/catalog-query.ts — the PROJECT_COLUMNS select + how it loads a project's tracks + vault_documents in one query (mirror this batched load to gather the gate inputs for the listing's project).
  </read_first>
  <files>app/api/sync-library/admin/[listingId]/route.ts</files>
  <action>
    Change the FIRST-statement gate from requireStaff(['leadership','ae']) to requireStaff(['leadership']) — admit/reject is now leadership-only (resolved-after-research access fix; AE keeps browse & pull, no curation writes). Keep it as the first statement before any DB read.
    In the admit branch ONLY (reject is unchanged — a human reject is always allowed), before performing the admitted write: load the listing's project with its tracks + vault_documents (mirror catalog-query's batched select and stage3 usage), compute the gate signals server-side — rightsClear from computeStage3(...).canContinue; metadataComplete from isSyncMetadataComplete(syncReadinessForTrack(thisTrack, sharedDocs, projectType)); qualityOk from the listing row's quality_ok === true — then call evaluateInclusionGate(). If it returns 'needs_completion', DO NOT admit and DO NOT reject: return a 409 with a clear message that the track must finish the Sync Readiness checklist (and/or pass the quality review) before it can go live, and keep its current non-terminal status. Only when it returns 'admit_eligible' proceed to the existing admitted write path.
    Preserve every existing safeguard: isValidTransition() stays as the legal-transition authority (the gate is an additional precondition, not a substitute); the admitted write keeps its fixed allowlisted column set; the first-admit notification and the UNCONDITIONAL logStaffAction stay. Extend the log 'changes' to record the gate verdict for the admit path.
    Update the route's header threat comments to T-30-06 (EoP — leadership-only) and note the gate precondition.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>DB round-trip via the running app: as leadership, admit a fully-ready admitted-eligible listing → 200 + status admitted. As leadership, attempt to admit an incomplete listing (missing metadata or quality_ok not true) → 409, status unchanged (NOT rejected). As an AE session, POST admit → 403 Forbidden.</manual>
  </verify>
  <done>Route is leadership-only; admit is gate-guarded (incomplete → 409, never rejected); reject unchanged; audit + notification preserved; tsc green.</done>
  <acceptance_criteria>
    - requireStaff(['leadership']) is the first statement; an AE receives 403 on admit and reject.
    - An admit on a gate-failing track returns 409 and leaves the status non-terminal (not 'rejected', not 'admitted').
    - An admit on a gate-passing track still writes status 'admitted' via the fixed allowlisted columns and logs the action.
    - Gate signals are computed from DB-loaded data, never from the request body.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Leadership-only quality-review + staff-notes write route</name>
  <read_first>
    - app/api/sync-library/admin/[listingId]/route.ts — the staff-gate-first + DB-loaded-target + fixed-allowlisted-columns + UNCONDITIONAL logStaffAction doctrine to mirror.
    - supabase/migrations/107_sync_listings_quality_review.sql (from 30-03) — the exact column names (quality_ok, quality_note, quality_reviewed_by, quality_reviewed_at, staff_notes).
    - lib/staff/audit.ts — logStaffAction signature.
  </read_first>
  <files>app/api/sync-library/admin/[listingId]/quality/route.ts</files>
  <action>
    Create a new POST route app/api/sync-library/admin/[listingId]/quality/route.ts. First statement: requireStaff(['leadership']) (quality/notes are curation writes → leadership-only, matching Task 1). Load the listing by the path param (never from the body). Validate the body against a fixed shape: an optional boolean quality_ok, an optional trimmed/length-capped quality_note, an optional trimmed/length-capped staff_notes — reject unknown/mistyped values (mirror the existing route's hand-rolled allowlist validation). Write ONLY those columns plus quality_reviewed_by = auth.user.id and quality_reviewed_at = now() (when quality_ok is provided) via the service-role client. Call logStaffAction UNCONDITIONALLY after the write (action e.g. 'sync_library.quality_review', targetType 'sync_listing', targetId listingId, changes recording the new values). Return the updated fields.
    Degrade safely on DB errors with NextResponse.json({ error }, { status }) — never throw uncaught.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>DB round-trip: as leadership, POST quality_ok=true + staff_notes to a listing → 200; re-read the row and confirm quality_ok/quality_reviewed_by/quality_reviewed_at/staff_notes persisted. As an AE, POST → 403. A malformed body (e.g. quality_ok:'yes') is rejected 400.</manual>
  </verify>
  <done>Quality/notes route ships leadership-only, allowlisted, audited; persistence confirmed via round-trip; tsc green.</done>
  <acceptance_criteria>
    - requireStaff(['leadership']) first; AE → 403.
    - Only quality_ok/quality_note/staff_notes/quality_reviewed_by/quality_reviewed_at are written; body is not spread.
    - logStaffAction is called after every successful write.
    - A round-trip confirms the values persist and are readable by the gate (Task 1's qualityOk).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| staff client → curation routes | Only leadership may admit/reject/set-quality; AE must be blocked from all curation writes. |
| request body → sync_listings write | Body must never dictate the target or the written columns. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-06 | Elevation of Privilege | admit/reject + quality routes | critical | mitigate | requireStaff(['leadership']) as the first statement on every write route; AE receives 403. |
| T-30-03 | Tampering | admit transition | high | mitigate | isValidTransition() remains the single legal-transition authority; the gate is an additional server-computed precondition; target loaded from DB by path param. |
| T-30-09 | Tampering | gate-signal spoofing | high | mitigate | rightsClear/metadataComplete/qualityOk are recomputed server-side from DB-loaded rows; no client value is trusted. |
| T-30-05 | Repudiation | curation + quality writes | medium | mitigate | logStaffAction called UNCONDITIONALLY after every write. |
| T-30-SC | Tampering | npm installs | low | accept | No new packages. |
</threat_model>

## Artifacts this plan produces
- **Files:** `app/api/sync-library/admin/[listingId]/route.ts` (extended: leadership-only + gate precondition), `app/api/sync-library/admin/[listingId]/quality/route.ts` (new).
- **Behavior:** admit is gate-guarded; access model reconciled to the locked decision.
- **Migrations:** none (consumes 30-03's columns).

<verification>
- `npx tsc --noEmit` green; `npx next build` green at wave end.
- grep confirms the admit route now calls `requireStaff(['leadership'])` (no `'ae'` in the admit/reject/quality gates).
- Round-trips confirm: AE blocked (403); incomplete admit → 409 non-terminal; complete admit → 'admitted'; quality write persists.
</verification>

<success_criteria>
- Leadership-only curation is enforced server-side; the gate genuinely governs admission; incomplete tracks stay in the pipeline; quality judgments persist and feed the gate; every write is audited.
</success_criteria>

<output>
Create `.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-04-SUMMARY.md` when done.
</output>
