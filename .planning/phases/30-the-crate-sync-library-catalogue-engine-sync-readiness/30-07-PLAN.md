---
phase: 30-the-crate-sync-library-catalogue-engine-sync-readiness
plan: 07
type: execute
wave: 2
depends_on: [30-02]
files_modified:
  - lib/deals/catalog.ts
  - lib/deals/catalog-query.ts
  - lib/deals/catalog-sample.ts
autonomous: true
requirements: [CRATE-08]
must_haves:
  truths:
    - "A LIVE catalogue row renders its REAL authored tags — artist, mood, energy, vocal, instruments, and tri-state rights — not blanks and not the fixture."
    - "The tri-state rights badge on a live row is derived from the same rights signal the gate uses (rightsBadge), not hardcoded to 'ok'."
    - "The single loadCatalogPage remains the one query both the buyer page and API use — enrichment is additive, not a fork."
  artifacts:
    - "lib/deals/catalog.ts CatalogCard carries artist/mood/energy/vocal/instruments/rights (enriched shape)."
    - "lib/deals/catalog-query.ts populates the enriched fields from real track descriptors + rights."
    - "lib/deals/catalog-sample.ts mapCardsToLightRows maps the real fields (no blank placeholders)."
  key_links:
    - "Enriched fields come from readDescriptors(track.metadata) (moods/energy/vocal/instruments) + rightsBadge(computeStage3) — reusing 30-02's descriptor v2 and 30-01's rights badge."
    - "mapCardsToLightRows stops synthesizing blank artist/mood/energy/instruments."
  prohibitions:
    - "MUST NOT fork loadCatalogPage or add a parallel query module (single-implementation doctrine, T-26-24)."
    - "MUST NOT hardcode rights to 'ok' on live rows — derive the tri-state from the rights signal."
    - "MUST NOT exceed the minimal 22-05 slice — only the enrichment needed so live rows show real tags + support the staff layers; defer broader 22-05 scope."
    - "MUST NOT expose owner contact details or non-public signals on CatalogCard (D-14a)."
---

<objective>
Ship the minimal live-data slice (the part of Phase 22's unbuilt "22-05" that Phase 30 needs): enrich the single catalogue query so LIVE rows carry their real authored tags and a real tri-state rights badge, and map those through to the Crate view-model — instead of rendering blanks and falling back to the fixture.

Purpose: The role-aware Crate and the visible tagging work cannot be demonstrated on fixtures. Live rows must show real mood/energy/vocal/instruments/artist + real rights so the staff layers (30-08) attach to real data. This is deliberately scoped to the minimum; broader 22-05 (player audio, pagination polish, other buyer surfaces) stays out.
Output: enriched `lib/deals/catalog.ts` (CatalogCard shape), `lib/deals/catalog-query.ts` (population), `lib/deals/catalog-sample.ts` (mapping).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-CONTEXT.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-RESEARCH.md
@lib/deals/catalog.ts
@lib/deals/catalog-query.ts
@lib/deals/catalog-sample.ts
@lib/metadata/schema.ts
@lib/sync-library/gate.ts
@lib/vault/stage3.ts
@components/buyer/CatalogBrowserLight.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich CatalogCard with the real authored fields (lib/deals/catalog.ts)</name>
  <read_first>
    - lib/deals/catalog.ts — the CatalogCard type (id/title/type/genre/coverArtUrl/tracks) and the D-14a "no owner contact details / no non-public signals" note; CatalogRights ('ok'|'part'|'req'); the readDescriptors import already present.
    - lib/metadata/schema.ts — readDescriptors output (moods/energy/vocal/instruments) + the label maps (MOOD_LABELS/ENERGY_LABELS/VOCAL_LABELS/INSTRUMENT_LABELS) for display strings.
    - components/buyer/CatalogBrowserLight.tsx (CatalogRow, lines ~42–63) — the display fields the Crate expects (artist/energy/mood/vocal/instruments/rights) so the enriched CatalogCard can feed them.
  </read_first>
  <files>lib/deals/catalog.ts</files>
  <action>
    Extend the CatalogCard type additively with the real authored display fields the Crate needs: artist (string), mood (string — a display label derived from the track's confirmed moods), energy (string display), vocal (string display), instruments (string[] display labels), and rights (CatalogRights). Keep the existing id/title/type/genre/coverArtUrl/tracks fields. Do NOT add any owner contact detail or non-public signal (D-14a).
    Add small pure helpers here (or reuse label maps) to turn a track's descriptors into the display strings the Crate row expects. Export whatever the query needs to populate these. Keep everything pure.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Reason through: CatalogCard now types artist/mood/energy/vocal/instruments/rights; the shape still excludes owner contact fields.</manual>
  </verify>
  <done>CatalogCard enriched additively with authored display fields + rights; pure helpers exported; tsc green.</done>
  <acceptance_criteria>
    - CatalogCard gains artist/mood/energy/vocal/instruments/rights and no private/owner fields.
    - Any new helper is pure and derives display strings from controlled-vocab descriptors.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Populate the enriched fields in the one query (lib/deals/catalog-query.ts)</name>
  <read_first>
    - lib/deals/catalog-query.ts — loadCatalogPage: PROJECT_COLUMNS (tracks already include metadata; add artist name source), the per-project stage3 computation already performed for isRightsReady, and the final card push where the enriched fields must be set.
    - lib/sync-library/gate.ts (30-01) — rightsBadge(stage3) → tri-state, and the CatalogRights map, to set card.rights from the SAME stage3 already computed in the loop.
    - lib/metadata/schema.ts — readDescriptors for moods/energy/vocal/instruments; note descriptors are track-level so pick a representative (e.g. the project's first track with descriptors) for the row display.
    - app/(admin)/admin/sync-library/page.tsx — the user_profiles artist_name batched lookup pattern to resolve artist names by owner id.
  </read_first>
  <files>lib/deals/catalog-query.ts</files>
  <action>
    In loadCatalogPage, resolve artist names for the page's owner ids via ONE batched user_profiles select (mirror the admin page's artistNameById map) — no per-row query. When building each CatalogCard, populate: artist from that map; mood/energy/vocal/instruments from readDescriptors of a representative track (first track carrying descriptors) turned into display strings via the Task-1 helpers; and rights from rightsBadge(stage3) mapped to CatalogRights — reusing the stage3 already computed for the isRightsReady gate in the same loop (do not recompute). Keep loadCatalogPage the single query; do not fork it. Preserve all existing privacy/admission/filter gates exactly.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>DB round-trip: seed/confirm at least one admitted, rights-ready live project with authored descriptors, then load /sync/catalog — the live row shows the real artist name + mood/energy/vocal/instruments and a rights badge that reflects its actual rights (not a blanket 'ok').</manual>
  </verify>
  <done>loadCatalogPage populates enriched authored fields + real tri-state rights via one batched query; no fork; tsc green.</done>
  <acceptance_criteria>
    - Artist names resolved via one batched query (no N+1).
    - Enriched fields populated from real descriptors; rights from rightsBadge(stage3), not hardcoded.
    - loadCatalogPage remains the single implementation; existing gates unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Map real fields into the Crate view-model (lib/deals/catalog-sample.ts)</name>
  <read_first>
    - lib/deals/catalog-sample.ts — mapCardsToLightRows (currently synthesizes blank artist/energy/mood/vocal/instruments and hardcodes rights:'ok'); SAMPLE_CATALOG_ROWS fixture semantics.
    - components/buyer/CatalogBrowserLight.tsx (CatalogRow) — the exact field names/types the row expects.
  </read_first>
  <files>lib/deals/catalog-sample.ts</files>
  <action>
    Update mapCardsToLightRows to map the now-real CatalogCard fields into CatalogRow: artist from card.artist; mood/energy/vocal/instruments from the enriched card fields; rights from card.rights (the real tri-state) instead of the hardcoded 'ok'. Keep the gradientFor fallback for missing cover art and keep the vaultProjectId/tracks wiring for the License modal. Update the header comment to note live rows now carry real authored tags (the minimal 22-05 slice); the SAMPLE fixture remains only as the empty-state fallback when there are zero live rows.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Browser-preview: with live rows present, /sync/catalog renders them with real tags + real rights badges; with zero live rows, the SAMPLE fixture still renders (empty-state fallback unchanged).</manual>
  </verify>
  <done>mapCardsToLightRows maps real authored fields + real rights; fixture is fallback-only; tsc green.</done>
  <acceptance_criteria>
    - Live rows show real artist/mood/energy/vocal/instruments and the real rights badge (no blanks, no forced 'ok').
    - The fixture fallback path is unchanged for the zero-live-rows case.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| catalogue query → buyer client | Only public, admitted, rights-ready data may reach the buyer; no owner contact/private signals. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-10 | Information Disclosure | enriched CatalogCard | high | mitigate | Enrichment adds only public display fields (D-14a); loadCatalogPage's existing admission/visibility/block gates are preserved unchanged. |
| T-30-11 | Tampering | rights badge | medium | mitigate | rights derived from rightsBadge(stage3), the same rights authority the gate uses — never a hardcoded 'ok'. |
| T-30-SC | Tampering | npm installs | low | accept | No new packages. |
</threat_model>

## Artifacts this plan produces
- **Files:** `lib/deals/catalog.ts`, `lib/deals/catalog-query.ts`, `lib/deals/catalog-sample.ts` (all extended).
- **Behavior:** live catalogue rows render real authored tags + real tri-state rights (minimal 22-05 slice).
- **Migrations:** none.

<verification>
- `npx tsc --noEmit` green; `npx next build` green at wave end.
- grep confirms no new `loadCrateStaffPage`/parallel query module; `mapCardsToLightRows` no longer hardcodes `rights: 'ok'` for live rows.
- Browser-preview confirms real tags on live rows.
</verification>

<success_criteria>
- Live catalogue rows show their real authored tags and real rights, through the single query — giving the role-aware Crate (30-08) real data to layer on.
</success_criteria>

<output>
Create `.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-07-SUMMARY.md` when done.
</output>
