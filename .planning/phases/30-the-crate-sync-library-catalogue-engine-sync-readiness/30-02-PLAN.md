---
phase: 30-the-crate-sync-library-catalogue-engine-sync-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/metadata/schema.ts
  - lib/tagging/ai-tag.ts
  - lib/tagging/tag-merge.ts
autonomous: true
requirements: [CRATE-06]
must_haves:
  truths:
    - "AI-suggested tags land in a distinct field and NEVER overwrite the artist-confirmed descriptors.moods/energy/vocal."
    - "AI output is constrained to the existing controlled vocab (moods/energy/vocal/genres) plus a new INSTRUMENT vocab — off-vocabulary values are dropped silently."
    - "The instrument controlled vocabulary exists as a shared export (not just the Crate's UI-only filter fixture)."
  artifacts:
    - "lib/metadata/schema.ts exports INSTRUMENT_VALUES + INSTRUMENT_LABELS and an extended TrackDescriptors carrying optional instruments + ai_suggested provenance."
    - "lib/tagging/ai-tag.ts exports suggestTrackTags() (Anthropic, degrades gracefully with no key)."
    - "lib/tagging/tag-merge.ts exports a pure mergeTags() that preserves artist-confirmed values."
  key_links:
    - "suggestTrackTags reuses lib/buyer/brief-ai.ts's prompt-for-JSON + tolerant extractJson pattern and the shared lib/anthropic constants."
    - "readDescriptors/sanitizeDescriptors keep reading the SAME descriptors JSONB shape, now additive for instruments/ai_suggested."
  prohibitions:
    - "MUST NOT overwrite descriptors.moods/energy/vocal with AI output (TrackDescriptors is artist-authored, never inferred)."
    - "MUST NOT let an off-vocabulary AI value reach the descriptors shape — coerce against *_VALUES, drop the rest."
    - "MUST NOT throw at import when ANTHROPIC_API_KEY is absent — degrade gracefully like brief-ai.ts (never like lib/anthropic/index.ts, which throws at import)."
    - "MUST NOT change the existing descriptors read/write behavior for callers that do not use the new fields (additive only)."
---

<objective>
Build the layered-tagging foundation: close the missing INSTRUMENT controlled-vocabulary gap, extend the track descriptors shape so AI suggestions and staff refinements are provenance-tracked alongside (never on top of) the artist-confirmed values, and add the pure AI-suggest + merge modules.

Purpose: Tagging (mood/genre/energy/instrument) is what the Crate filters, search, and brief-matching run on. This plan makes AI suggestions safe (vocab-constrained, non-destructive) and gives instruments a real shared vocab so AI-suggested instruments are filter-visible rather than free-text.
Output: extended `lib/metadata/schema.ts`, new `lib/tagging/ai-tag.ts`, new `lib/tagging/tag-merge.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-CONTEXT.md
@.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-RESEARCH.md
@lib/metadata/schema.ts
@lib/buyer/brief-ai.ts
@lib/anthropic/index.ts
@lib/genres.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: INSTRUMENT controlled vocab + descriptor v2 (additive) in lib/metadata/schema.ts</name>
  <read_first>
    - lib/metadata/schema.ts — the MOOD_LABELS/MOOD_VALUES, ENERGY_LABELS/ENERGY_VALUES, VOCAL_LABELS/VOCAL_VALUES pattern; the TrackDescriptors type; and readDescriptors/sanitizeDescriptors (the tolerant read + drop-off-vocab write).
    - 30-RESEARCH.md "Layered Tagging — Data Model Detail" — the proposed TrackDescriptorsV2 shape and the explicit "instruments has no controlled vocab today" gap (CatalogBrowserLight FILTER_OPTIONS.Instruments is a UI-only fixture).
  </read_first>
  <behavior>
    - readDescriptors on a blob with a valid instruments array returns those instruments (off-vocab dropped, capped); on a blob with none returns an empty/absent instruments field without breaking existing moods/energy/vocal reads.
    - readDescriptors on a blob carrying ai_suggested returns the ai_suggested sub-object (moods/energy/vocal/instruments/suggested_at/model) coerced to vocab; the confirmed moods/energy/vocal are unchanged.
    - sanitizeDescriptors still returns null for a fully-untagged input and never emits an ai_suggested field from artist input (AI suggestions are written by the AI route only, not this sanitizer).
  </behavior>
  <files>lib/metadata/schema.ts</files>
  <action>
    Following the exact MOOD/ENERGY/VOCAL registry pattern, add INSTRUMENT_LABELS (a Record) and derive INSTRUMENT_VALUES from its keys, seeding it with at least the Crate's existing instrument fixtures (Piano, Strings, Guitar, Synth, Brass) plus a modest superset (e.g. drums, bass, keys, vocals-as-instrument, percussion, woodwinds) — SCREAMING_SNAKE_CASE constants, PascalCase Instrument type. Keep the value tokens lowercase like other vocabs.
    Extend TrackDescriptors additively: add optional `instruments?: Instrument[]` (capped, deduped) and an optional `ai_suggested?` sub-object holding vocab-coerced moods/energy/vocal/instruments plus suggested_at + model strings, and an optional `staff_refined_by?: string` marker. Do NOT change the existing moods/energy/vocal fields.
    Update readDescriptors to also parse instruments (coerce against INSTRUMENT_VALUES, cap like MOODS_MAX) and, when present, the ai_suggested sub-object (each field coerced to its vocab). Keep the confirmed moods/energy/vocal parsing byte-identical to today.
    Update sanitizeDescriptors to accept + normalize an optional instruments array (drop off-vocab) but NEVER to write ai_suggested (that write path belongs to the AI route in 30-06). Preserve the "return null when nothing is set" behavior.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Round-trip in a node/tsx scratch or by reasoning: a descriptors blob with instruments ['piano','banjo-not-in-vocab'] reads back ['piano']; a blob with an ai_suggested object reads it back without altering confirmed moods; a legacy blob (no instruments/ai_suggested) reads back exactly as before.</manual>
  </verify>
  <done>INSTRUMENT_VALUES/INSTRUMENT_LABELS + descriptor v2 shipped additively; readDescriptors/sanitizeDescriptors updated; tsc green; existing callers unbroken.</done>
  <acceptance_criteria>
    - INSTRUMENT_VALUES and INSTRUMENT_LABELS are exported and follow the _VALUES/_LABELS convention.
    - TrackDescriptors gains optional instruments + ai_suggested + staff_refined_by only (no removed/renamed fields).
    - readDescriptors coerces instruments and ai_suggested to controlled vocab, dropping off-vocab silently.
    - sanitizeDescriptors never emits ai_suggested and still returns null for untagged input.
    - lib/deals/catalog.ts (which imports from schema.ts) still type-checks unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: AI tag suggestion module (lib/tagging/ai-tag.ts)</name>
  <read_first>
    - lib/buyer/brief-ai.ts — the full draftBriefFromProse pattern: inline Anthropic client, graceful no-key return { ok:false, error }, extractJson tolerant parser, list()/buildPrompt() vocab-constrained prompt, coerce-against-known-vocab result.
    - lib/anthropic/index.ts — the shared MODEL/MAX_TOKENS constants (import these; do NOT hardcode a third model id — see 30-RESEARCH Open Q #5).
    - lib/metadata/schema.ts — MOOD_VALUES/ENERGY_VALUES/VOCAL_VALUES/INSTRUMENT_VALUES to constrain output; lib/genres.ts ALL_GENRE_SLUGS for genre suggestions.
  </read_first>
  <files>lib/tagging/ai-tag.ts</files>
  <action>
    Create lib/tagging/ai-tag.ts mirroring brief-ai.ts's structure. Construct the Anthropic client inline from process.env.ANTHROPIC_API_KEY and return a graceful { ok:false, error:'The tagging assistant is offline right now.' } when the key is absent — never import lib/anthropic/index.ts's eager singleton (it throws at import). Import MODEL/MAX_TOKENS values from lib/anthropic where practical, or re-declare a single shared constant referencing the same id; do NOT invent a third model id.
    Export suggestTrackTags(input) where input carries the track title plus any artist-supplied text (lyrics/notes) available to the caller. Build a prompt that constrains the model to ONLY the enumerated vocab (list() helper over MOOD_VALUES, ENERGY_VALUES, VOCAL_VALUES, INSTRUMENT_VALUES, ALL_GENRE_SLUGS) and asks for a JSON object of suggested moods/energy/vocal/instruments/genres. Reuse the fenced-code-tolerant extractJson pattern verbatim.
    Coerce the parsed result: keep only vocab members (MOOD_VALUES.includes etc.), dedupe, cap counts, drop everything else silently. Return { ok:true, suggestion } where suggestion is shaped to slot into descriptors.ai_suggested (moods/energy/vocal/instruments + suggested_at + model). This is the Tampering/prompt-injection defense (T-30-04): constrain to known vocab, never let free text through.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>With ANTHROPIC_API_KEY unset, suggestTrackTags returns { ok:false, error } (no throw). Reason through: a model reply containing an off-vocab mood is dropped; a reply wrapped in a fenced json code block still parses via extractJson; a non-JSON reply returns a friendly error.</manual>
  </verify>
  <done>suggestTrackTags exported, vocab-constrained, key-graceful; tsc green.</done>
  <acceptance_criteria>
    - No key → { ok:false, error } (never throws at import or call).
    - Output values are all members of the controlled vocab; off-vocab values cannot appear.
    - The module reuses the extractJson tolerant-parse idiom from brief-ai.ts (not a new bespoke parser).
    - No third hardcoded model id is introduced.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Provenance-preserving tag merge (lib/tagging/tag-merge.ts)</name>
  <read_first>
    - lib/metadata/schema.ts — the descriptor v2 shape from Task 1 (moods/energy/vocal confirmed; ai_suggested; staff_refined_by).
    - 30-RESEARCH.md "Pattern 4" + "Anti-Patterns to Avoid" — AI suggestions must never overwrite artist-confirmed values; staff refine is a third write path over the SAME shape with a provenance marker.
  </read_first>
  <files>lib/tagging/tag-merge.ts</files>
  <action>
    Create lib/tagging/tag-merge.ts as a pure module. Export mergeAiSuggestion(current, suggestion) that returns a new TrackDescriptors with ai_suggested set to the suggestion, leaving confirmed moods/energy/vocal/instruments untouched (the artist still owns the confirmed values).
    Export applyStaffRefinement(current, refined, staffUserId) that lets a staff member set/adjust the CONFIRMED descriptors (moods/energy/vocal/instruments), stamps staff_refined_by = staffUserId, and preserves any ai_suggested sub-object for provenance display. Both functions are pure, vocab-safe (route inputs through the schema coercion), and never mutate their inputs.
    Document that "AI-suggested, unconfirmed" vs "artist-confirmed" vs "staff-adjusted" are distinguishable from the returned shape (ai_suggested present but not promoted = unconfirmed; staff_refined_by present = staff-adjusted).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Reason through: mergeAiSuggestion on a track whose confirmed moods are ['driving'] and an AI suggestion of ['chill'] returns confirmed moods still ['driving'] and ai_suggested.moods ['chill']; applyStaffRefinement to ['cinematic'] sets confirmed moods ['cinematic'] and staff_refined_by = the staff id.</manual>
  </verify>
  <done>mergeAiSuggestion + applyStaffRefinement exported, pure, non-destructive; tsc green.</done>
  <acceptance_criteria>
    - mergeAiSuggestion never changes confirmed moods/energy/vocal/instruments.
    - applyStaffRefinement stamps staff_refined_by and preserves ai_suggested.
    - Both functions are pure (no I/O, no input mutation) and vocab-safe.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| artist text → AI prompt | A track's artist-supplied lyrics/notes feed the Anthropic prompt — untrusted input crosses into the model. |
| AI output → descriptors JSONB | Model output must not reach the stored shape unconstrained. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04 | Tampering (prompt injection) | suggestTrackTags | high | mitigate | Constrain output to enumerated *_VALUES vocab and drop anything else silently (brief-ai.ts coerceBrief discipline); AI values never write directly to a column and are never rendered unescaped. |
| T-30-08 | Tampering | mergeAiSuggestion | medium | mitigate | AI output lands only in ai_suggested; confirmed artist values are structurally protected by the merge function. |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages (reuse @anthropic-ai/sdk only). SC gate not applicable. |
</threat_model>

## Artifacts this plan produces
- **Files:** `lib/tagging/ai-tag.ts`, `lib/tagging/tag-merge.ts` (new); `lib/metadata/schema.ts` (extended).
- **Symbols:** `INSTRUMENT_VALUES`, `INSTRUMENT_LABELS`, `Instrument`, extended `TrackDescriptors` (instruments/ai_suggested/staff_refined_by), `suggestTrackTags()`, `mergeAiSuggestion()`, `applyStaffRefinement()`.
- **Migrations:** none (descriptors is unconstrained JSONB — extended without a migration).

<verification>
- `npx tsc --noEmit` green (including lib/deals/catalog.ts which imports schema.ts).
- grep confirms `lib/tagging/ai-tag.ts` does NOT import the eager `lib/anthropic/index.ts` singleton (it builds the client inline for graceful degradation).
- grep confirms no off-vocab path: AI suggestion coercion routes through `*_VALUES.includes`.
</verification>

<success_criteria>
- Instruments have a shared controlled vocabulary; descriptors carry provenance additively; AI suggest + merge are pure, vocab-safe, and non-destructive to artist-confirmed tags.
</success_criteria>

<output>
Create `.planning/phases/30-the-crate-sync-library-catalogue-engine-sync-readiness/30-02-SUMMARY.md` when done.
</output>
