---
phase: 20-profile-table-rename-artist-profiles-to-user-profiles
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-03"]
files_modified: []
autonomous: false
requirements: [D-02, D-05]
user_setup:
  - service: supabase
    why: "Human-gated migration push #2 (migration 077, drop the compat view) — executor must NEVER run supabase db push"
    dashboard_config:
      - task: "Push migration 077 via Codex after the soak, then confirm LOCAL=REMOTE and the view is gone"
        location: "Supabase remote project (wgfjakfiyeewzfuxkgyo) via Codex / supabase CLI"
must_haves:
  truths:
    - "The compat view is kept for a short soak after the smoke gate passes, until old warm serverless instances drain / a low-traffic window (D-05)"
    - "Migration 077 drops ONLY the artist_profiles view; user_profiles and all its data/RLS/functions are untouched (D-02)"
    - "After the drop, supabase migration list shows LOCAL=REMOTE through 077 and the app still works — all 6 functions now resolve against user_profiles, not the dropped view"
  artifacts: []
  key_links:
    - "Dropping the view before the soak elapses races a straggler warm instance still calling artist_profiles → 404 (RESEARCH Pitfall 6)"
    - "The 6 SECURITY DEFINER functions were already repointed to user_profiles in 076, so the drop does not break them (RESEARCH Pitfall 3)"
---

<objective>
Execute the second half of the D-02 two-push cutover: after the D-04 smoke gate (plan 20-03) passed, keep the compatibility view alive for a short D-05 soak, then the human pushes migration 077 to drop it, and both parties confirm the app still works.

This plan is NOT autonomous — both tasks are human-gated checkpoints. The executor does NOT run `supabase db push`; it presents the soak criteria and the drop verification, then blocks for the human.

Purpose: Retire the transitional view safely, completing the rename with no straggler-instance breakage.
Output: A live remote DB where `artist_profiles` no longer exists as any relation; only `user_profiles` remains.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-profile-table-rename-artist-profiles-to-user-profiles/20-CONTEXT.md
@.planning/phases/20-profile-table-rename-artist-profiles-to-user-profiles/20-RESEARCH.md
@supabase/migrations/077_drop_artist_profiles_compat_view.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: D-05 soak — hold the compat view until old instances drain</name>
  <action>BLOCK for the human. Do NOT push 077 yet. Present the soak criteria below; the human confirms the wait window elapsed / traffic is low. Do not proceed to the drop until Pete confirms "soak complete".</action>
  <what-built>Plan 20-03 landed migration 076 and deployed the renamed code; the D-04 smoke gate passed. The `artist_profiles` compat view is still live so any straggler old serverless instance keeps working.</what-built>
  <how-to-verify>
    Hold the view for the D-05 soak — do NOT push 077 yet. Wait until EITHER old warm Vercel instances have drained (a defined wait window after the deploy) OR a low-traffic window is reached. During the soak, monitor Vercel/PostgREST logs and confirm there is NO reliance you would break: specifically watch for old-code `.from('artist_profiles')` traffic tapering to zero. A spike of `42P01` (relation does not exist) later would indicate a straggler — the soak exists precisely to let those instances recycle first (RESEARCH Pitfall 6).
  </how-to-verify>
  <resume-signal>Type "soak complete" once the wait window has elapsed / traffic is low and old-code artist_profiles calls have drained — or describe why the soak should extend.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human push #2 — apply migration 077 (drop view) + verify (D-02)</name>
  <action>BLOCK for the human. Only after "soak complete", the human pushes migration 077 via Codex; the executor does NOT run `supabase db push`. Present the post-drop verification below. Do not mark this plan complete until Pete confirms "077 verified".</action>
  <what-built>Migration 077 was authored in plan 20-01 (single `DROP VIEW IF EXISTS public.artist_profiles;` + `NOTIFY pgrst`). It is NOT yet applied — the push is human-only.</what-built>
  <how-to-verify>
    Only after "soak complete": Pete pushes migration 077 via Codex (applied in filename order after 076). Then confirm:
    1. `supabase migration list` shows LOCAL=REMOTE through 077.
    2. The view is gone: `SELECT 1 FROM pg_class WHERE relname = 'artist_profiles';` returns no rows (neither table nor view named artist_profiles exists).
    3. `user_profiles` is intact with its data: a `SELECT count(*) FROM public.user_profiles;` matches the pre-rename row count.
    4. The app still works end-to-end: re-run a quick subset of the D-04 checks (one signup, one public profile load, one split-sheet read) and a Green Room feed load — confirming the 6 repointed functions resolve against `user_profiles` now that the view is dropped (RESEARCH Pitfall 3 — this is the "worked in transition, breaks after cleanup" trap the function repoints in 076 prevent).
    5. No `42P01`/`PGRST` relation-not-found spike in logs after the drop.
  </how-to-verify>
  <resume-signal>Type "077 verified" once LOCAL=REMOTE through 077, the view is gone, user_profiles is intact, and the app still works — or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| straggler old serverless instance → PostgREST → artist_profiles | The only remaining consumer of the view; the soak drains it before the drop |
| Codex/human operator → remote Supabase DB | Privileged migration push; the only path allowed to run `supabase db push` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-08 | Denial of Service | Dropping the view before warm instances drain | medium | mitigate | D-05 soak checkpoint gates the 077 push on drained old-code traffic / low-traffic window + log monitoring (RESEARCH Pitfall 6) |
| T-20-11 | Denial of Service | A missed function repoint surfacing only after the drop | high | mitigate | All 6 functions were repointed to user_profiles in 076 (verified statically in 20-01 and behaviorally in the 20-03 smoke gate); 077's post-drop re-check of signup + Green Room feed confirms no time bomb (RESEARCH Pitfall 3) |
| T-20-12 | Tampering | 077 accidentally dropping/altering more than the view | high | mitigate | 20-01 authored 077 as a single `DROP VIEW IF EXISTS` with no ALTER/GRANT (asserted by grep at authoring); post-push check confirms user_profiles row count is intact |
| T-20-SC | Tampering (supply chain) | package installs | low | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- Soak elapsed / old-code artist_profiles traffic drained before the drop (D-05).
- `supabase migration list` shows LOCAL=REMOTE through 077.
- No relation named `artist_profiles` exists (pg_class returns zero rows).
- `user_profiles` row count unchanged from pre-rename (no data loss — the whole phase is data-preserving).
- Post-drop app checks (signup, public profile, split sheet, Green Room feed) pass; no 42P01/PGRST spike.
</verification>

<success_criteria>
- Compat view soaked then dropped via human-gated push #2 (D-02/D-05).
- `artist_profiles` no longer exists as a relation; `user_profiles` is the sole canonical profile table with all data intact.
- The rename is complete end-to-end with zero downtime and zero data change.
</success_criteria>

<output>
Create `.planning/phases/20-profile-table-rename-artist-profiles-to-user-profiles/20-04-SUMMARY.md` when done.
</output>
