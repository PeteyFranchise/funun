---
phase: 20-profile-table-rename-artist-profiles-to-user-profiles
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-04]
user_setup:
  - service: supabase
    why: "Human-gated migration push #1 (migration 076) — the executor must NEVER run supabase db push (project convention)"
    dashboard_config:
      - task: "Push migration 076 via Codex, then confirm LOCAL=REMOTE with supabase migration list"
        location: "Supabase remote project (wgfjakfiyeewzfuxkgyo) via Codex / supabase CLI"
  - service: vercel
    why: "Deploy the renamed code (merge main) — the synchronization point between push #1 and push #2"
    dashboard_config:
      - task: "Confirm the main deploy is live and serving user_profiles-reading code"
        location: "Vercel dashboard / funun.studio"
must_haves:
  truths:
    - "Migration 076 is applied to the live remote DB and supabase migration list shows LOCAL=REMOTE through 076 (D-01/D-02)"
    - "The artist_profiles view exists WITH security_invoker=on and its column grants match the table's pre-rename grants exactly (verified against information_schema, not assumed)"
    - "The renamed code is deployed to production AFTER 076 landed — new code reads user_profiles; old warm instances read artist_profiles through the view (D-02)"
    - "The full D-04 smoke set passes: signup on all 3 handle_new_user branches, /u/[handle], /r/[projectId], a split sheet, a Settings rights save, a metadata/registration read, and the pgrst reload is confirmed (D-04)"
  artifacts: []
  key_links:
    - "Order is strict: push 076 → verify DB → deploy code → smoke gate. Deploying before 076 lands would 404 new code against a not-yet-renamed table"
    - "The security_invoker + grant-parity verification is the gate that proves no RLS-bypass window opened (RESEARCH Pitfall 1/2)"
---

<objective>
Execute the first half of the D-02 two-push cutover: the human pushes migration 076, both parties verify the live DB is correct (no RLS bypass, grant parity, pgrst reloaded), the renamed code deploys, and the full D-04 smoke-test gate passes — all BEFORE the view is dropped (that is plan 20-04).

This plan is NOT autonomous — every task is a human-gated checkpoint. The executor authored the artifacts (20-01, 20-02) but does NOT run `supabase db push` or trigger the production deploy; it presents the exact steps and verification queries, then blocks for the human.

Purpose: Land the rename on the live database with zero downtime and prove correctness before the point of no return.
Output: A live remote DB with user_profiles + the security_invoker compat view, a deployed renamed frontend, and a passed smoke gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-profile-table-rename-artist-profiles-to-user-profiles/20-CONTEXT.md
@.planning/phases/20-profile-table-rename-artist-profiles-to-user-profiles/20-RESEARCH.md
@supabase/migrations/076_rename_artist_profiles_to_user_profiles.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human push #1 — apply migration 076 + verify live DB (D-01/D-02)</name>
  <action>BLOCK for the human. Present the push + verification steps below; the executor does NOT run `supabase db push`. Do not proceed to the deploy task until Pete confirms "076 verified".</action>
  <what-built>Migration 076 was authored in plan 20-01 (rename + security_invoker compat view + 6 function repoints + column-scoped grants + defensive service_role grant + NOTIFY). It is NOT yet applied to the remote database — that push is human-only per project convention.</what-built>
  <how-to-verify>
    Pete performs push #1 via Codex and both parties confirm the live-DB verification (this closes RESEARCH Assumptions A1/A2 and Open Question 1, per D-01/D-02):
    1. BEFORE the push, run `SELECT version();` against the remote DB — confirm PostgreSQL 15.x or later (A2). If it is somehow older, STOP — `security_invoker` would be a parameter error; do not proceed.
    2. Push migration 076 (`supabase db push`, human-run via Codex), applied in filename order after 075.
    3. `supabase migration list` shows LOCAL=REMOTE through 076.
    4. Confirm the view exists AND is security_invoker: `SELECT relname, reloptions FROM pg_class WHERE relname = 'artist_profiles' AND relkind = 'v';` — `reloptions` must include `security_invoker=on` (RESEARCH Pitfall 1 — this is the highest-severity check; if absent, the view is an RLS bypass and 076 must be corrected and re-pushed).
    5. Grant parity (Open Question 1): run `SELECT grantee, privilege_type, column_name FROM information_schema.role_column_grants WHERE table_name = 'artist_profiles' ORDER BY grantee, column_name;` and compare column-for-column against the same query the migration reproduced from 040/043/054. Any missing/narrowed column must be reconciled before proceeding.
    6. service_role on the view (Assumption A1): `SELECT grantee, privilege_type FROM information_schema.role_table_grants WHERE table_name = 'artist_profiles' AND grantee = 'service_role';` — confirm the defensive grant took (or that ambient default privileges cover it).
    7. Confirm the `NOTIFY pgrst, 'reload schema'` reload took effect — a `.from('artist_profiles')` read via the old (still-deployed) code returns rows, not a stale-schema 404/500 (RESEARCH Pitfall 5).
    Report the outputs of steps 1/4/5/6 in the resume signal.
  </how-to-verify>
  <resume-signal>Type "076 verified" once LOCAL=REMOTE, the view is confirmed security_invoker=on, grant parity holds, and the pgrst reload is confirmed — OR describe the discrepancy (which triggers a 076 fix + re-push before continuing).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy the renamed code to production (strictly after 076 lands — D-02)</name>
  <action>BLOCK for the human. Only after "076 verified", the human merges main so Vercel deploys the renamed code. Do not proceed to the smoke gate until Pete confirms "deployed".</action>
  <what-built>The renamed code (plan 20-02) reads `user_profiles` directly. It must deploy ONLY after 076 has landed — old warm instances keep working through the security_invoker view during the cutover.</what-built>
  <how-to-verify>
    Only after "076 verified": deploy the renamed code to production (merge to main → Vercel auto-deploy of funun.studio). Confirm the deploy completed and the site loads. New serverless instances now read `user_profiles`; any still-warm old instances continue reading `artist_profiles` through the compat view (this is the intended zero-downtime overlap, D-02).
  </how-to-verify>
  <resume-signal>Type "deployed" once the main deploy is live and the site loads, or describe the deploy failure (rollback posture per D-02: redeploy the prior code — it still reads artist_profiles via the view; nothing to un-migrate).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-04 full smoke-test gate (before the irreversible view drop)</name>
  <action>BLOCK for the human. Present the full D-04 smoke set below and the cross-user RLS spot-check; the human runs them against production. Do not mark this plan complete until Pete confirms "smoke passed".</action>
  <what-built>With 076 live and the renamed code deployed, the app now exercises `user_profiles` end-to-end through the new code and `artist_profiles`→view through any straggler old code. The full D-04 smoke set proves both paths and all 6 repointed functions work before the view is dropped.</what-built>
  <how-to-verify>
    Run the FULL D-04 smoke set against production (all must pass — this is the gate before the irreversible view drop):
    1. Signup on ALL 3 handle_new_user branches — artist, industry, and curator — and confirm the correct profile row (or curator early-return) is created for each (RESEARCH Pitfall 4: both INSERT branches were repointed; miss one and one member type silently breaks).
    2. Public profile pages load: `/u/[handle]` and `/r/[projectId]` for a real handle/project.
    3. Open and interact with a split sheet (resolvePartyIdentity path reads the renamed relation).
    4. Save a rights change in Settings and confirm it persists.
    5. Read a metadata/registration view that pulls profile data.
    6. Confirm no `permission denied for view "artist_profiles"` (42501), no `relation does not exist` (42P01), and no stale-schema errors in Vercel/PostgREST logs during the run.
    Cross-user safety spot-check (closes T-20-01): while signed in as user A, confirm you CANNOT read another user's private profile fields (e.g. contact_phone) through any public/old-code path — proves security_invoker is actually enforcing RLS through the view.
  </how-to-verify>
  <resume-signal>Type "smoke passed" once every D-04 item passes and the cross-user check confirms RLS holds through the view — or list the failing item(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated/anon client → PostgREST → artist_profiles view → user_profiles | The live cutover window; RLS-through-the-view is the gate proven by the smoke check |
| Codex/human operator → remote Supabase DB | Privileged migration push; the only path allowed to run `supabase db push` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06 | Info Disclosure/Elevation | Live compat view during the deploy gap | high | mitigate | Push #1 checkpoint verifies `reloptions` includes `security_invoker=on` via pg_class AND the smoke gate's cross-user spot-check proves RLS holds through the view before the deploy proceeds |
| T-20-03b | Info Disclosure | Live view grant parity | high | mitigate | Push #1 checkpoint runs the `information_schema.role_column_grants` parity query against the live DB and reconciles any drift before deploy (Open Question 1) |
| T-20-07 | Elevation/DoS | service_role grant on the view (A1) | low | mitigate | Push #1 checkpoint queries `role_table_grants` for service_role; the defensive `GRANT ALL ... TO service_role` in 076 removes the ambiguity |
| T-20-10 | DoS | Deploying new code before 076 lands | high | mitigate | Strict task ordering — deploy checkpoint is gated on "076 verified"; new code cannot 404 against a not-yet-renamed table |
| T-20-SC | Tampering (supply chain) | package installs | low | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- `supabase migration list` shows LOCAL=REMOTE through 076.
- pg_class confirms the `artist_profiles` view carries `security_invoker=on`.
- information_schema confirms column-grant parity + service_role access on the view.
- The full D-04 smoke set is green, including the 3 signup branches and the cross-user RLS spot-check.
- The renamed code is live in production, deployed strictly after 076 landed.
</verification>

<success_criteria>
- Migration 076 applied and verified correct on the live remote DB (D-01/D-02).
- Renamed code deployed after the migration (D-02 ordering honored).
- D-04 smoke gate fully passed; no RLS-bypass, no 42501/42P01, cross-user isolation confirmed (D-04).
- Ready for the D-05 soak + view drop (plan 20-04).
</success_criteria>

<output>
Create `.planning/phases/20-profile-table-rename-artist-profiles-to-user-profiles/20-03-SUMMARY.md` when done.
</output>
