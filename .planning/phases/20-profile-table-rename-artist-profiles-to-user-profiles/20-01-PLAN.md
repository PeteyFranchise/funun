---
phase: 20-profile-table-rename-artist-profiles-to-user-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/076_rename_artist_profiles_to_user_profiles.sql
  - supabase/migrations/077_drop_artist_profiles_compat_view.sql
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "Migration 076 renames artist_profiles to user_profiles (OID-preserving ALTER TABLE), so RLS/triggers/FKs/indexes follow automatically with zero re-creation (D-01)"
    - "The compat relation is a security_invoker=on view over user_profiles, so RLS is evaluated as the INVOKING role — no cross-user bypass during the deploy gap (D-01)"
    - "All 6 SECURITY DEFINER function bodies point at public.user_profiles, so nothing breaks when the view is dropped in 077"
    - "The view carries the exact column-scoped SELECT/UPDATE grants (never GRANT ALL), so old code keeps working AND exposure is not widened"
    - "Migration 077 drops ONLY the view (single DROP VIEW + NOTIFY), gated by a header precondition citing D-04/D-05"
  artifacts:
    - supabase/migrations/076_rename_artist_profiles_to_user_profiles.sql
    - supabase/migrations/077_drop_artist_profiles_compat_view.sql
  key_links:
    - "CREATE VIEW artist_profiles WITH (security_invoker = on) — the single highest-severity clause; omission is an RLS bypass"
    - "The 6 CREATE OR REPLACE FUNCTION bodies (handle_new_user both branches, clear_featured_if_unpublished, green_room_post_matches_custom_audience, green_room_can_view_post, claim_collaborators, backfill_claimed_collaborators) all repointed to public.user_profiles"
    - "claim_collaborators/backfill_claimed_collaborators CREATE OR REPLACE must NOT re-add GRANT EXECUTE — preserves migration 075's service_role-only lockdown"
---

<!-- planner-discipline-allow: artist_profiles -->

<objective>
Author (do NOT push) the two human-gated migration files that perform the zero-downtime table rename per D-01.

Migration 076 does five things in one file: (1) `ALTER TABLE artist_profiles RENAME TO user_profiles`; (2) `CREATE OR REPLACE` the 6 functions whose bodies literally reference the old table, repointed to `public.user_profiles`; (3) `CREATE VIEW artist_profiles WITH (security_invoker = on) AS SELECT * FROM user_profiles`; (4) re-issue the exact column-scoped SELECT/UPDATE grants on the view plus a defensive `service_role` grant; (5) `NOTIFY pgrst, 'reload schema'`. Migration 077 drops the view after the D-04 smoke gate and D-05 soak.

Purpose: These files are the entire DB-layer surface of the rename. Their correctness is verified by static file assertions here and by the human at push time (plan 20-03/20-04). The executor NEVER runs `supabase db push`.
Output: `076_rename_artist_profiles_to_user_profiles.sql`, `077_drop_artist_profiles_compat_view.sql`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-profile-table-rename-artist-profiles-to-user-profiles/20-CONTEXT.md
@.planning/phases/20-profile-table-rename-artist-profiles-to-user-profiles/20-RESEARCH.md
@.planning/phases/20-profile-table-rename-artist-profiles-to-user-profiles/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 076 (rename + 6 function repoints + security_invoker compat view + grants + NOTIFY)</name>
  <files>supabase/migrations/076_rename_artist_profiles_to_user_profiles.sql</files>
  <read_first>
    - supabase/migrations/075_phase19_privilege_hardening.sql (lines 1-43) — the human-gated header/footer banner style to copy: "Funūn — Wave 2: Rights & Registration Rails (Phase 20: Profile Table Rename)" project line, prose rationale, and the "An executor agent must NEVER run `supabase db push` for this migration" line placed IN the header block.
    - supabase/migrations/072_repoint_claim_functions.sql (lines 45-244) — the CREATE OR REPLACE table-repoint precedent (RE-POINTED comment convention, `SECURITY DEFINER SET search_path = ''`, `public.`-qualified refs); this migration's claim_collaborators + backfill_claimed_collaborators are the current live bodies. Repoint direction here is the REVERSE of 072 (artist_profiles → user_profiles).
    - supabase/migrations/039_handle_new_user_industry_branch.sql — current live handle_new_user() body. BOTH the industry branch INSERT and the default/artist branch INSERT target the old table (Pitfall 4 — miss one and one member type's signup silently breaks).
    - supabase/migrations/034_member_identity_wave4.sql (clear_featured_if_unpublished, ~lines 129-142) — `UPDATE public.artist_profiles SET featured_project_id = NULL`.
    - supabase/migrations/060_green_room_block_visibility_and_audience_roles.sql (green_room_post_matches_custom_audience, LEFT JOIN line) and supabase/migrations/059_green_room_feed_author_publicness.sql (green_room_can_view_post, FROM line).
    - supabase/migrations/040_artist_profiles_column_privileges.sql (lines 83-119) — the base GRANT SELECT + GRANT UPDATE column lists; supabase/migrations/043_profile_allow_resharing.sql (GRANT SELECT (allow_resharing)) and supabase/migrations/054_dm_request_status_presence.sql (GRANT SELECT (last_seen_at)) — the two column additions to fold into the view's SELECT grant.
    - 20-RESEARCH.md "Code Examples" (migration 076 shape) and 20-PATTERNS.md "The 6 Functions Requiring CREATE OR REPLACE" table + "security_invoker view" section.
  </read_first>
  <action>
    Create the new file `076_rename_artist_profiles_to_user_profiles.sql` (076 is the next free number; 075 is latest; historical migrations are immutable — new file only). Follow the 075/073 human-gated header banner exactly, with a prose rationale describing the D-01 zero-downtime compat-view rename and the "executor must NEVER run supabase db push" line in the header.

    Body, in this order:
    1. `ALTER TABLE artist_profiles RENAME TO user_profiles;` — OID-preserving; RLS policies, triggers, FKs (capability_grants, verification_audit_log), indexes and the table's own grants all follow automatically. Do NOT rename index/constraint/trigger names (cosmetic only — leave them; renaming adds lock risk for zero benefit).
    2. Six `CREATE OR REPLACE FUNCTION public.<name>(...)` blocks — copy each function's CURRENT live body verbatim, changing ONLY the old table identifier to `public.user_profiles`, and add a one-line `-- RE-POINTED (Phase 20): -> public.user_profiles` comment at each changed line. Preserve every `SECURITY DEFINER`, `LANGUAGE`, and `SET search_path = ''` clause byte-for-byte. The six: handle_new_user (BOTH INSERT branches — Pitfall 4), clear_featured_if_unpublished, green_room_post_matches_custom_audience, green_room_can_view_post, claim_collaborators, backfill_claimed_collaborators. For claim_collaborators and backfill_claimed_collaborators, do NOT add any `GRANT EXECUTE` line — CREATE OR REPLACE preserves migration 075's service_role-only EXECUTE lockdown, and re-adding a grant would re-widen a cross-user-write DEFINER function (per 075's own comment).
    3. `CREATE VIEW artist_profiles WITH (security_invoker = on) AS SELECT * FROM user_profiles;` — the security_invoker clause is NON-NEGOTIABLE (without it the view runs as owner and bypasses RLS, a cross-user read/write hole — RESEARCH Pitfall 1). This is net-new Postgres-15 machinery; there is no repo analog, use RESEARCH's Code Examples template.
    4. `REVOKE ALL ON artist_profiles FROM PUBLIC, authenticated, anon;` then re-issue the EXACT column-scoped grants on the view: `GRANT SELECT (<union of 040's SELECT list + 043's allow_resharing + 054's last_seen_at>) ON artist_profiles TO authenticated, anon;` and `GRANT UPDATE (<040's UPDATE list>) ON artist_profiles TO authenticated;`. NEVER a blanket `GRANT ALL ... TO authenticated/anon`. No INSERT/DELETE grant (the table never granted these to authenticated/anon — writes go through createServiceClient which bypasses column grants). Add a defensive `GRANT ALL ON artist_profiles TO service_role;` (RESEARCH Open Question 2 / Assumption A1 — costs nothing, removes the ambiguity; service_role already has equivalent access via other paths).
    5. Add an inline comment on the GRANT block marking the column lists as `[ASSUMED from migrations 040/043/054 — verified live against information_schema.role_column_grants at the 20-03 push #1 checkpoint]` (RESEARCH Open Question 1). Do not guess additional columns.
    6. `NOTIFY pgrst, 'reload schema';` as the final statement.
  </action>
  <verify>
    <automated>test -f supabase/migrations/076_rename_artist_profiles_to_user_profiles.sql && grep -q "ALTER TABLE artist_profiles RENAME TO user_profiles" supabase/migrations/076_rename_artist_profiles_to_user_profiles.sql && grep -Eq "CREATE VIEW[[:space:]]+artist_profiles[[:space:]]+WITH[[:space:]]*\(security_invoker[[:space:]]*=[[:space:]]*on\)" supabase/migrations/076_rename_artist_profiles_to_user_profiles.sql && [ "$(grep -c 'CREATE OR REPLACE FUNCTION' supabase/migrations/076_rename_artist_profiles_to_user_profiles.sql)" -ge 6 ] && grep -q "NOTIFY pgrst" supabase/migrations/076_rename_artist_profiles_to_user_profiles.sql && grep -q "NEVER run \`supabase db push\`" supabase/migrations/076_rename_artist_profiles_to_user_profiles.sql && ! grep -Eq "GRANT ALL ON artist_profiles TO (authenticated|anon)" supabase/migrations/076_rename_artist_profiles_to_user_profiles.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `076_...sql` exists with the human-gated header banner and the "never db push" line.
    - Contains `ALTER TABLE artist_profiles RENAME TO user_profiles`, exactly the 6 `CREATE OR REPLACE FUNCTION` blocks (handle_new_user with both INSERT branches repointed), `CREATE VIEW artist_profiles WITH (security_invoker = on)`, `REVOKE ALL` + column-scoped `GRANT SELECT`/`GRANT UPDATE` on the view, a defensive `GRANT ALL ... TO service_role`, and `NOTIFY pgrst, 'reload schema'`.
    - Contains NO blanket `GRANT ALL ON artist_profiles TO authenticated`/`anon`, and NO new `GRANT EXECUTE` for claim_collaborators/backfill_claimed_collaborators.
    - Every DEFINER function block preserves `SECURITY DEFINER SET search_path = ''` and uses `public.user_profiles` for the repointed reference.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author migration 077 (drop the compat view)</name>
  <files>supabase/migrations/077_drop_artist_profiles_compat_view.sql</files>
  <read_first>
    - supabase/migrations/073_drop_user_profiles.sql (full header + body) — the precondition-gated single-DROP + NOTIFY shape and human-gated footer to mirror; 077 is structurally identical but `DROP VIEW IF EXISTS` instead of `DROP TABLE`.
    - 20-RESEARCH.md "Migration 077 (after soak)" Code Example.
  </read_first>
  <action>
    Create `077_drop_artist_profiles_compat_view.sql` (next free number after 076). Mirror 073's human-gated header banner. The header PRECONDITION block must state explicitly that this file is pushed ONLY after (a) the D-04 smoke-test gate passed and (b) the D-05 soak elapsed (old warm serverless instances drained / low-traffic window). Body is exactly two statements: `DROP VIEW IF EXISTS public.artist_profiles;` then `NOTIFY pgrst, 'reload schema';`. Nothing else — no table changes, no grant changes. Include the "executor must NEVER run supabase db push" line in the header.
  </action>
  <verify>
    <automated>test -f supabase/migrations/077_drop_artist_profiles_compat_view.sql && grep -q "DROP VIEW IF EXISTS public.artist_profiles" supabase/migrations/077_drop_artist_profiles_compat_view.sql && grep -q "NOTIFY pgrst" supabase/migrations/077_drop_artist_profiles_compat_view.sql && grep -qi "D-04" supabase/migrations/077_drop_artist_profiles_compat_view.sql && grep -qi "D-05" supabase/migrations/077_drop_artist_profiles_compat_view.sql && ! grep -q "ALTER TABLE" supabase/migrations/077_drop_artist_profiles_compat_view.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `077_...sql` exists with the human-gated header, a precondition block referencing D-04 (smoke gate) and D-05 (soak), and the "never db push" line.
    - Body is exactly `DROP VIEW IF EXISTS public.artist_profiles;` + `NOTIFY pgrst, 'reload schema';` — no ALTER TABLE, no GRANT, no other statements.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated/anon client → PostgREST → compat view → user_profiles | Untrusted signed-in caller reaches profile rows through the old `artist_profiles` name during the deploy gap; RLS is the gate |
| createServiceClient() (service_role) → view/table | Server-side privileged writes; bypasses RLS + column grants by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Elevation/Info Disclosure | CREATE VIEW artist_profiles | critical | mitigate | View created `WITH (security_invoker = on)` so RLS is evaluated as the invoking role, not the owner; asserted by grep in Task 1 verify (RESEARCH Pitfall 1) |
| T-20-02 | Elevation | claim_collaborators / backfill_claimed_collaborators CREATE OR REPLACE | high | mitigate | No `GRANT EXECUTE` line added — CREATE OR REPLACE preserves migration 075's service_role-only lockdown; Task 1 verify asserts no new grant |
| T-20-03 | Info Disclosure/Elevation | View GRANT statements | high | mitigate | Exact column-scoped SELECT/UPDATE lists reproduced from 040/043/054; grep asserts no blanket `GRANT ALL ... TO authenticated/anon` |
| T-20-04 | Denial of Service | View missing grants → 42501 for old code | medium | mitigate | Explicit `REVOKE ALL` then column-scoped re-grant on the view; defensive service_role grant (A1) |
| T-20-SC | Tampering (supply chain) | package installs | low | accept | Zero new dependencies this phase — pure rename; confirmed by RESEARCH Runtime State Inventory + unchanged package.json |
</threat_model>

<verification>
- Both migration files exist under `supabase/migrations/` with the next free numbers 076/077 and immutable historical files untouched (`git status` shows only the two new files added).
- 076 static assertions (Task 1 verify) all pass; 077 static assertions (Task 2 verify) all pass.
- No `supabase db push` was run by the executor (project convention — pushes are the human checkpoints in 20-03/20-04).
- Live DB correctness (grant parity, security_invoker in effect, LOCAL=REMOTE) is NOT verifiable here — it is the human push #1 checkpoint in plan 20-03. That is by design (human-gated migrations).
</verification>

<success_criteria>
- Migration 076 authored: rename + 6 repointed functions + security_invoker view + column-scoped grants + service_role defensive grant + NOTIFY, with no blanket GRANT ALL and no re-widened EXECUTE grants (D-01).
- Migration 077 authored: single DROP VIEW + NOTIFY, precondition-gated on D-04/D-05.
- Both files carry the "executor must NEVER run supabase db push" header line.
</success_criteria>

<output>
Create `.planning/phases/20-profile-table-rename-artist-profiles-to-user-profiles/20-01-SUMMARY.md` when done.
</output>
