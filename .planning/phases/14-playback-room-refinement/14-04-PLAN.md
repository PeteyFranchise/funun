---
phase: 14-playback-room-refinement
plan: 04
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - lib/vault/export-pack.ts
  - lib/vault/pdf/credits-sheet.tsx
  - lib/vault/pdf/metadata-sheet.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-10: a pure buildExportManifest() lists exactly the bundle members that exist for a project (master WAV, share MP3, stems ZIP, instrumental) — never a member whose file is absent"
    - "D-10: renderCreditsSheet() produces a real PDF Buffer/stream containing per-track credits + splits from readComposers()"
    - "D-10: renderMetadataSheet() produces a real PDF Buffer/stream containing ISRC/ISWC/BPM/key/language per track"
    - "the manifest builder is pure (no Storage/DB I/O) — I/O stays in the export route (Plan 06)"
  artifacts:
    - lib/vault/export-pack.ts
    - lib/vault/pdf/credits-sheet.tsx
    - lib/vault/pdf/metadata-sheet.tsx
  key_links:
    - "buildExportManifest() is the single source of truth for WHICH files go in the ZIP — the export route (Plan 06) iterates its output; if a file is absent the manifest omits it (D-08 hide-when-absent applied to bundle contents)"
    - "the two PDF renderers consume readComposers() + the same field set as the onesheet — data shape reused, render target (@react-pdf/renderer <Document>) is new (PATTERNS §credits-sheet note)"
---

<objective>
Build the pure assembly layer for the Export Pack (D-10): a `buildExportManifest()` that gathers a project + its tracks into a typed list of "which bundle members actually exist" (master WAV, share MP3, stems ZIP, instrumental, plus the two generated PDFs), and two `@react-pdf/renderer` templates that render the credits/splits sheet and the metadata sheet as real PDF files.

Purpose: Separating pure assembly (this plan) from I/O (the export route in Plan 06) matches CLAUDE.md's "pure functions in lib/" convention and keeps the route thin. Rendering PDFs server-side (not the onesheet's "Print to PDF" client trick) is what lets them be bundled into the ZIP.
Output: One lib module + two PDF template components.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-playback-room-refinement/14-RESEARCH.md
@.planning/phases/14-playback-room-refinement/14-PATTERNS.md
@lib/metadata/bundle.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildExportManifest() — pure "which files exist" assembler</name>
  <files>lib/vault/export-pack.ts</files>
  <read_first>
    - lib/metadata/bundle.ts (the `buildBundle(project, tracks, artistName)` pure-transform template — same "gather rows into one typed object" shape; note ProjectRow/TrackRow types + sortTracks())
    - lib/metadata/schema.ts (readComposers, readMasterAudio, and the new readStems/readInstrumental from Plan 01 — the four file-ref readers the manifest checks for presence)
    - .planning/phases/14-playback-room-refinement/14-PATTERNS.md (§lib/vault/export-pack.ts — "pure transform, no I/O; I/O stays in the route")
    - .planning/phases/14-playback-room-refinement/14-CONTEXT.md (D-10 — the exact bundle contents list)
  </read_first>
  <behavior>
    - buildExportManifest(project, tracks) with a track having master + share + stems + instrumental → files list includes one entry per existing ref with { path, filename, kind } (kind: 'master'|'share'|'stems'|'instrumental'), plus flags creditsSheet:true, metadataSheet:true
    - a track with only a share MP3 → files list contains only the share entry (no master/stems/instrumental entries) — absent refs are OMITTED, never emitted with a null path (D-08 applied to bundle contents)
    - a project with zero tracks having any master → hasMaster:false in the returned manifest (the route/UI uses this for the no-master gate)
    - filenames are human-readable and collision-safe across tracks (e.g. include track number/title slug so a 5-track album's masters don't overwrite each other in the ZIP)
    - the function performs NO Storage download and NO Supabase query — it only reads the already-fetched rows
  </behavior>
  <action>
    Create `lib/vault/export-pack.ts`. Export a typed `ExportManifest` and a pure `buildExportManifest(project, tracks)` function following `buildBundle`'s signature/shape convention. For each track (sorted by track_number like `sortTracks`), use `readMasterAudio(t.metadata)`, `readStems(t.metadata)`, `readInstrumental(t.metadata)` and the track's `audio_file_url` (share MP3) to determine which of the four file refs exist; for each existing ref push `{ path, filename, kind }` into a `files` array where `filename` is a slugified, collision-safe name (include the track number + a sanitized title, e.g. `01-song-title.master.wav`, using the ref's `ext`/`name`). Set `hasMaster` true if any track has a master ref. Set `creditsSheet: true` and `metadataSheet: true` (the two PDFs are always generated from whatever data exists). Return `{ files, hasMaster, creditsSheet, metadataSheet, tracks: <typed rows the PDF renderers need> }`. Keep it PURE — no `createServiceClient`, no `storage`, no `await` on I/O. camelCase, named exports, no semicolons, 2-space indent, `is`/`has` prefix for predicates.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/vault/export-pack.ts','utf8'); if(!/export function buildExportManifest/.test(s)) throw new Error('no builder'); if(/createServiceClient|\.storage\.|\.download\(/.test(s)) throw new Error('manifest builder must be pure — no Storage I/O'); if(!/readStems|readInstrumental|readMasterAudio/.test(s)) throw new Error('does not read the file refs'); console.log('PASS')"</automated>
    <human-check>Not required for this pure function — the node verify + tsc cover it.</human-check>
  </verify>
  <acceptance_criteria>
    - `lib/vault/export-pack.ts` exports `buildExportManifest` and an `ExportManifest` type.
    - The function contains NO `createServiceClient`, `.storage.`, or `.download(` (purity enforced by node verify).
    - It uses `readMasterAudio`/`readStems`/`readInstrumental` to detect existing refs and omits absent ones (no null-path entries).
    - Filenames in `files` are collision-safe across tracks (include track number/title slug).
    - node verify prints `PASS`; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>A pure manifest builder lists exactly the bundle members that exist, with collision-safe filenames, ready for the export route to iterate (D-10).</done>
</task>

<task type="auto">
  <name>Task 2: credits-sheet.tsx — @react-pdf/renderer credits/splits PDF template</name>
  <files>lib/vault/pdf/credits-sheet.tsx</files>
  <read_first>
    - .planning/phases/14-playback-room-refinement/14-PATTERNS.md (§credits-sheet — "copy the data shape and field selection from onesheet + readComposers(), NOT the render technique")
    - app/(artist)/vault/[projectId]/metadata/onesheet/page.tsx (the field selection + credits/splits data layout to mirror as a document, NOT the print mechanism)
    - lib/metadata/schema.ts (readComposers + COMPOSER_ROLE_LABELS — the credits data source; Composer type at line 64)
    - .planning/phases/14-playback-room-refinement/14-RESEARCH.md (§Standard Stack — @react-pdf/renderer 4.5.1; §Anti-Patterns — do NOT use Puppeteer/Print)
  </read_first>
  <action>
    Create `lib/vault/pdf/credits-sheet.tsx`. Build and export a `renderCreditsSheet(manifestOrBundle)` async function that returns a PDF as a Node stream or Buffer using `@react-pdf/renderer`'s `renderToStream`/`renderToBuffer` and a `<Document>` component tree (`<Document>/<Page>/<View>/<Text>` — react-pdf primitives, NOT HTML tags). The document shows: a header (release title + artist), then per track a section listing each composer's name, role (via `COMPOSER_ROLE_LABELS[c.role]`), PRO, IPI (when present), and split %, plus the per-track split total (flag when != 100%). Read credits via `readComposers(track.metadata)` — do NOT re-derive composer data (PATTERNS: "call it directly"). Define a small `StyleSheet.create` for layout following the two-sheet, tabular structure. Export both the `<Document>` component (for testability) and the `renderCreditsSheet()` async renderer the route calls. This is a `.tsx` file rendering react-pdf JSX; TypeScript strict — type the props explicitly, no implicit any. No console.log.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/vault/pdf/credits-sheet.tsx','utf8'); if(!/@react-pdf\/renderer/.test(s)) throw new Error('must use @react-pdf/renderer'); if(!/export .*renderCreditsSheet/.test(s)) throw new Error('no renderCreditsSheet export'); if(!/readComposers/.test(s)) throw new Error('does not use readComposers'); if(/puppeteer|playwright|window\.print/.test(s)) throw new Error('forbidden print/headless-browser technique'); console.log('PASS')"</automated>
    <human-check>Generate a pack on a project with real credits → open credits-and-splits.pdf → confirm every composer, role, PRO, IPI, split, and the split total render with real data (not blank/placeholder) (VALIDATION D-10).</human-check>
  </verify>
  <acceptance_criteria>
    - `lib/vault/pdf/credits-sheet.tsx` uses `@react-pdf/renderer` and exports `renderCreditsSheet`.
    - It reads credits via `readComposers` (not re-derived) and uses `COMPOSER_ROLE_LABELS` for role labels.
    - It contains NO `puppeteer`/`playwright`/`window.print` (node verify enforces).
    - node verify prints `PASS`; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>A server-side credits/splits PDF renderer producing a real PDF file from readComposers() data (D-10).</done>
</task>

<task type="auto">
  <name>Task 3: metadata-sheet.tsx — @react-pdf/renderer metadata PDF template</name>
  <files>lib/vault/pdf/metadata-sheet.tsx</files>
  <read_first>
    - lib/vault/pdf/credits-sheet.tsx (the sibling from Task 2 — reuse the same StyleSheet/Document scaffolding, `renderToStream`/`renderToBuffer` pattern, and export shape)
    - app/(artist)/vault/[projectId]/metadata/onesheet/page.tsx (PROJECT_COLS/TRACK_COLS field selection at lines ~11-14 — ISRC/ISWC/BPM/key/language are the exact fields; reuse this column set per PATTERNS)
    - .planning/phases/14-playback-room-refinement/14-CONTEXT.md (D-10 — metadata sheet fields: ISRC/ISWC/BPM/key/language)
  </read_first>
  <action>
    Create `lib/vault/pdf/metadata-sheet.tsx` mirroring credits-sheet.tsx's scaffolding. Export `renderMetadataSheet(manifestOrBundle)` returning a PDF stream/Buffer via `@react-pdf/renderer`. The document shows a header (release title + artist) then a per-track table of ISRC, ISWC, BPM, key signature, language, and duration — the same field set the onesheet already selects (D-10). Reuse the `<Document>/<Page>/<View>/<Text>` + `StyleSheet.create` approach from credits-sheet. No `puppeteer`/`playwright`/print technique. TypeScript strict, explicit prop types, no console.log.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/vault/pdf/metadata-sheet.tsx','utf8'); if(!/@react-pdf\/renderer/.test(s)) throw new Error('must use @react-pdf/renderer'); if(!/export .*renderMetadataSheet/.test(s)) throw new Error('no renderMetadataSheet export'); if(!/ISRC|isrc/.test(s)) throw new Error('missing metadata fields'); if(/puppeteer|playwright|window\.print/.test(s)) throw new Error('forbidden print technique'); console.log('PASS')"</automated>
    <human-check>Open metadata.pdf from a generated pack → confirm ISRC/ISWC/BPM/key/language/duration render per track with real data (VALIDATION D-10).</human-check>
  </verify>
  <acceptance_criteria>
    - `lib/vault/pdf/metadata-sheet.tsx` uses `@react-pdf/renderer`, exports `renderMetadataSheet`, includes the ISRC/ISWC/BPM/key/language field set.
    - No `puppeteer`/`playwright`/print technique (node verify enforces).
    - node verify prints `PASS`; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>A server-side metadata PDF renderer producing a real PDF file with the ISRC/ISWC/BPM/key/language field set (D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new at runtime) | This plan produces pure functions + PDF templates; it performs no I/O and crosses no auth boundary. The auth/ownership boundary is enforced by the export route that consumes these (Plan 06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Information Disclosure | PDF renderers embedding project data | low | mitigate | The renderers only receive already-owner-scoped rows passed in by the export route (Plan 06 does the `.eq('user_id', user.id)` fetch); the renderers never query the DB themselves, so they cannot leak another user's data. Enforced by keeping these functions pure (node verify forbids Storage/DB calls in export-pack.ts). |
| T-14-08 | Denial of Service | @react-pdf/renderer bundle-size / cold-start | low | accept | @react-pdf/renderer is pure-JS (no ~300MB Chromium binary), chosen precisely to stay under Vercel's ~250MB deployment bundle limit (RESEARCH §Alternatives Considered). Accept: this is the mitigation-by-library-choice already made in research. |
</threat_model>

<verification>
- All three node verifies print `PASS` (pure manifest builder; both PDF renderers use @react-pdf/renderer and the correct data sources; no forbidden print technique).
- `npx tsc --noEmit` passes.
- Human: generated PDFs render real credits + metadata (deferred to Plan 06's integration, since rendering needs the route).
</verification>

<success_criteria>
- Pure manifest builder lists exactly the existing bundle members (D-10).
- Two server-side PDF renderers produce real files from the same data the onesheet uses (D-10).
- Zero I/O / zero auth logic in this layer — cleanly consumable by the export route.
</success_criteria>

<output>
Create `.planning/phases/14-playback-room-refinement/14-04-SUMMARY.md` when done. Record the `ExportManifest` type shape, the `buildExportManifest` signature, and the `renderCreditsSheet`/`renderMetadataSheet` signatures (stream vs Buffer return type) so Plan 06's route wires them correctly with `archiver.append()`.

## Artifacts this phase produces (Plan 04)
- `lib/vault/export-pack.ts` — exports `buildExportManifest`, `ExportManifest` (type)
- `lib/vault/pdf/credits-sheet.tsx` — exports `renderCreditsSheet` (+ Document component)
- `lib/vault/pdf/metadata-sheet.tsx` — exports `renderMetadataSheet` (+ Document component)
</output>