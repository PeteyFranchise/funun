---
phase: 14-playback-room-refinement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/vault/VaultProjectCard.tsx
  - components/vault/ProjectTabs.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-01: clicking a Sound Vault project card navigates to /vault/{id}/play (the playback room), not /vault/{id} (management page)"
    - "D-01: the management page still has a discoverable link/tab into the playback room (/vault/{id}/play)"
  artifacts:
    - components/vault/VaultProjectCard.tsx
    - components/vault/ProjectTabs.tsx
  key_links:
    - "VaultProjectCard's <Link href> is the single entry point from the vault grid into a project — flipping it to /play makes the playback room the primary landing (D-01)"
---

<objective>
Implement D-01's navigation change: the Sound Vault project card (`VaultProjectCard.tsx`) links directly to the playback room (`/vault/{id}/play`) as the primary landing page, and the management page's `ProjectTabs` surfaces a link back to the playback room so `/play` is no longer orphaned dead code.

Purpose: Today `/vault/{id}/play` is unreachable from the UI (no nav, no link — per CONTEXT.md's codebase findings). This plan makes the playback room the front door and keeps the management page reachable, closing the "orphaned page" gap. It is intentionally scoped to routing/link edits only — the playback room's UI rework (uploads, toggle, readiness widgets, export) is Plan 05/06.
Output: Two edited components.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-playback-room-refinement/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point the Sound Vault project card at the playback room (D-01)</name>
  <files>components/vault/VaultProjectCard.tsx</files>
  <read_first>
    - components/vault/VaultProjectCard.tsx (line 70 — the `<Link href={`/vault/${card.id}`}>` that currently targets the management page; this is the exact single line to change)
    - .planning/phases/14-playback-room-refinement/14-CONTEXT.md (D-01 — card links to the playback room as primary landing)
  </read_first>
  <action>
    In `components/vault/VaultProjectCard.tsx`, change the card's `<Link>` target (line ~70) from `href={`/vault/${card.id}`}` to `href={`/vault/${card.id}/play`}` per D-01. Change ONLY the href value — do not alter the card's markup, classes, readiness ring, status chip, or any other prop. This is the single routing edit that makes the playback room the primary landing page.
  </action>
  <verify>
    <automated>grep -q "/vault/\${card.id}/play" components/vault/VaultProjectCard.tsx && ! grep -q "href={\`/vault/\${card.id}\`}" components/vault/VaultProjectCard.tsx && echo PASS</automated>
    <human-check>From `/vault`, click any project card → confirm it lands on the playback room (`/vault/{id}/play`), not the management page (VALIDATION D-01).</human-check>
  </verify>
  <acceptance_criteria>
    - `VaultProjectCard.tsx` contains `href={`/vault/${card.id}/play`}` and no longer contains the bare `href={`/vault/${card.id}`}` link.
    - The grep verify prints `PASS`.
    - No other line in the file changed (git diff shows a single-line href edit).
  </acceptance_criteria>
  <done>Clicking a project card in the Sound Vault grid navigates to the playback room (D-01).</done>
</task>

<task type="auto">
  <name>Task 2: Add a playback-room link to the management page ProjectTabs (D-01)</name>
  <files>components/vault/ProjectTabs.tsx</files>
  <read_first>
    - components/vault/ProjectTabs.tsx (the full component — it renders a `role="tablist"` header; confirmed by PATTERNS.md to have zero links to /play today; it does NOT receive projectId today, so a prop must be threaded)
    - components/vault/VaultProjectCard.tsx (the `next/link` import + Link usage pattern to reuse)
    - app/(artist)/vault/[projectId]/page.tsx (lines 383-421 — the call site that renders `<ProjectTabs items={...} />`; the caller has `project.id` available to pass a new prop)
    - .planning/phases/14-playback-room-refinement/14-CONTEXT.md (D-01 — management page becomes secondary but reachable; the playback room must be reachable FROM it)
  </read_first>
  <action>
    Give `ProjectTabs` an optional `playbackHref?: string` prop and render, in the `role="tablist"` header row (right-aligned, after the mapped tab buttons), a `next/link` "Playback room →" affordance pointing at `playbackHref` when provided. Use `import Link from 'next/link'`. Style it as a subtle secondary link consistent with the file's existing `text-white/50 hover:text-white/80 text-sm font-medium` tab-button treatment (do not use the accent gradient — this is a secondary nav link, not a CTA). Then update the single call site in `app/(artist)/vault/[projectId]/page.tsx` (the `<ProjectTabs items={[...]} />` render, ~line 383) to pass `playbackHref={`/vault/${project.id}/play`}`. Do NOT change the `TabItem` type, the active-tab state logic, or the panel-visibility toggle. Keep the prop optional so any other caller (if one exists) is unaffected.
  </action>
  <verify>
    <automated>grep -q "playbackHref" components/vault/ProjectTabs.tsx && grep -q "next/link" components/vault/ProjectTabs.tsx && grep -q "playbackHref={\`/vault/\${project.id}/play\`}" "app/(artist)/vault/[projectId]/page.tsx" && echo PASS</automated>
    <human-check>Open a project's management page (`/vault/{id}`) → confirm a "Playback room →" link is present in the tabs row and navigates to `/vault/{id}/play`.</human-check>
  </verify>
  <acceptance_criteria>
    - `ProjectTabs.tsx` accepts an optional `playbackHref` prop and renders a `next/link` to it when present.
    - The management page (`page.tsx`) passes `playbackHref={`/vault/${project.id}/play`}`.
    - The grep verify prints `PASS`.
    - `TabItem` type, active-tab `useState`, and panel toggle logic are unchanged.
  </acceptance_criteria>
  <done>The management page has a discoverable link into the playback room, so `/play` is no longer orphaned (D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only changes client-side navigation targets between two already-owner-gated routes; no new data, auth, or upload surface is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Information Disclosure | /vault/{id}/play route access | low | accept | The playback room route already enforces the same owner-only gate as the management page (`.eq('user_id', ...)` in `play/page.tsx`, preserved in Plan 05); changing the card's link target does not widen access — a non-owner following the link still hits `notFound()`. No new exposure. |
</threat_model>

<verification>
- VaultProjectCard grep verify prints `PASS` (links to `/play`, no bare management link).
- ProjectTabs grep verify prints `PASS` (accepts `playbackHref`, uses `next/link`, call site passes the href).
- Human: card click lands on playback room; management page shows a working "Playback room →" link.
</verification>

<success_criteria>
- Project card is the front door to the playback room (D-01).
- Playback room is reachable from the management page (D-01).
</success_criteria>

<output>
Create `.planning/phases/14-playback-room-refinement/14-02-SUMMARY.md` when done. Record the new `playbackHref` prop signature on `ProjectTabs` (in case any future caller needs it) and confirm the card link flip.

## Artifacts this phase produces (Plan 02)
- `components/vault/VaultProjectCard.tsx` — `<Link>` href now `/vault/{id}/play`
- `components/vault/ProjectTabs.tsx` — new optional prop `playbackHref?: string` + a "Playback room →" link
</output>