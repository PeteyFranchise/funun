---
phase: 14-playback-room-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/041_track_audio_stems_config.sql
  - lib/metadata/schema.ts
  - package.json
  - package-lock.json
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-07: track-audio bucket accepts uploads up to 250MB (not just 50MB)"
    - "D-03/D-07: track-audio bucket accepts application/zip and application/x-zip-compressed MIME types"
    - "D-03/D-04: a typed reader returns a track's stems ZIP reference {path,size,name} from tracks.metadata.stems"
    - "D-04/D-06: a typed reader returns a track's instrumental audio reference {path,size,ext} from tracks.metadata.instrumental"
    - "D-10: archiver, @react-pdf/renderer, and tus-js-client are installed and resolvable"
  artifacts:
    - supabase/migrations/041_track_audio_stems_config.sql
    - lib/metadata/schema.ts
    - package.json
  key_links:
    - "readStems()/readInstrumental() in schema.ts are the single canonical way every route + page reads stems/instrumental refs — mirrors readMasterAudio()"
    - "bucket file_size_limit=262144000 and the zip MIME allowlist are the AUTHORITATIVE server-side gate for the direct-to-storage stems upload (Plan 05); client checks are UX-only"
---

<objective>
Lay the storage + type foundation for Phase 14: raise the `track-audio` bucket's `file_size_limit` to 250MB and add ZIP MIME types (D-07/Pitfall 4), add canonical typed readers for the new `tracks.metadata.stems` and `tracks.metadata.instrumental` JSONB sub-objects (D-03/D-04, mirroring the existing `readMasterAudio()`), and install the three new libraries the Export Pack and large-upload paths need (D-10).

Purpose: Every downstream plan (upload routes, playback UI, export route) depends on the bucket accepting large ZIPs and on a single canonical way to read the new metadata shapes. Doing this first prevents each plan from inventing its own reader or hitting a silent storage-layer rejection.
Output: Migration `041`, extended `lib/metadata/schema.ts`, three installed npm packages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-playback-room-refinement/14-RESEARCH.md
@.planning/phases/14-playback-room-refinement/14-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm npm packages resolve to the intended packages (not typo-squats)</name>
  <read_first>
    - .planning/phases/14-playback-room-refinement/14-RESEARCH.md (§Package Legitimacy Audit — all three verdicts are OK/Approved; §Standard Stack pins the exact versions)
  </read_first>
  <what-built>
    Nothing is built yet — this is a pre-install verification gate. RESEARCH.md's Package Legitimacy Audit returned OK/Approved for all three packages (archiver 8.0.0, @react-pdf/renderer 4.5.1, tus-js-client 4.3.1), but the package NAMES carry [ASSUMED] provenance (sourced from training knowledge), so a human must confirm the install resolves to the real packages before bytes hit node_modules.
  </what-built>
  <how-to-verify>
    1. Open npmjs.com/package/archiver, npmjs.com/package/@react-pdf/renderer, and npmjs.com/package/tus-js-client.
    2. Confirm each is the actively-maintained, high-download package (archiver ~30M/wk by archiverjs/node-archiver; @react-pdf/renderer ~4M/wk by diegomura/react-pdf; tus-js-client ~1M/wk by tus/tus-js-client).
    3. Confirm none is a look-alike/typo-squat (e.g. `archivers`, `react-pdf-renderer` without scope).
  </how-to-verify>
  <acceptance_criteria>
    - Human types "approved" confirming the three package names on npmjs.com match RESEARCH.md's Package Legitimacy Audit table (real repos, high downloads, no typo-squat).
  </acceptance_criteria>
  <resume-signal>Type "approved" or name the package(s) that look wrong</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install archiver, @react-pdf/renderer, tus-js-client (+ @types/archiver)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependency set — confirm none of the three are already present)
    - .planning/phases/14-playback-room-refinement/14-RESEARCH.md (§Standard Stack — exact versions: archiver 8.0.0, @react-pdf/renderer 4.5.1, tus-js-client 4.3.1)
  </read_first>
  <action>
    Run `npm install archiver@8.0.0 @react-pdf/renderer@4.5.1 tus-js-client@4.3.1` then `npm install --save-dev @types/archiver`. Pin the exact versions RESEARCH.md verified against the live npm registry on 2026-07-06. These are runtime deps for the Export Pack (archiver = streaming ZIP, @react-pdf/renderer = server-side PDF) and the 250MB stems upload (tus-js-client = resumable direct-to-storage). Do NOT add any other package. Do NOT run `npm audit fix` or upgrade unrelated deps.
  </action>
  <verify>
    <automated>node -e "require('archiver'); require('tus-js-client'); require('@react-pdf/renderer'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `archiver`, `@react-pdf/renderer`, `tus-js-client` and devDependencies include `@types/archiver`.
    - The node -e require check prints `ok` (all three resolve).
    - No unrelated dependency version in package.json changed (git diff of package.json shows only the four added lines).
  </acceptance_criteria>
  <done>The three libraries + @types/archiver are installed at the RESEARCH-pinned versions and resolve at require time.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 041 — raise bucket size limit to 250MB and add ZIP MIME types</name>
  <files>supabase/migrations/041_track_audio_stems_config.sql</files>
  <read_first>
    - supabase/migrations/004_track_audio_storage.sql (the exact bucket row + `on conflict (id) do update set` idempotent pattern to mirror; current file_size_limit=52428800, current allowed_mime_types audio-only)
    - .planning/phases/14-playback-room-refinement/14-RESEARCH.md (§Pitfall 4 — the bucket allowlist is the authoritative server-side gate; §Confirmed Constraints)
  </read_first>
  <action>
    Create `supabase/migrations/041_track_audio_stems_config.sql`. Use a single idempotent `insert into storage.buckets (...) values ('track-audio', 'track-audio', false, 262144000, array[...]) on conflict (id) do update set file_size_limit = excluded.file_size_limit, allowed_mime_types = excluded.allowed_mime_types;` — mirror migration 004's exact statement shape so re-running is safe. Set `file_size_limit` to `262144000` (250 * 1024 * 1024, add an inline SQL comment stating "-- 250 MB, D-07"). Set `allowed_mime_types` to the existing nine audio types (`audio/mpeg`, `audio/mp3`, `audio/wav`, `audio/x-wav`, `audio/mp4`, `audio/aac`, `audio/flac`, `audio/ogg`, `audio/webm`) PLUS `application/zip` and `application/x-zip-compressed` (the type some Windows zip tools emit, per Pitfall 4). Do NOT touch the four RLS policies from 004 — they already scope inserts to `(storage.foldername(name))[1] = auth.uid()::text`, which is exactly the owner-path constraint the direct-to-storage upload relies on (RESEARCH §Security Domain). Do NOT `drop policy`/`create policy` here. Add a header comment block in the `// ─── Section ───` house style explaining this is an additive config bump (not a rename, no new bucket) per RESEARCH §Runtime State Inventory.
  </action>
  <verify>
    <automated>grep -q "262144000" supabase/migrations/041_track_audio_stems_config.sql && grep -q "application/zip" supabase/migrations/041_track_audio_stems_config.sql && grep -q "application/x-zip-compressed" supabase/migrations/041_track_audio_stems_config.sql && grep -q "on conflict (id) do update" supabase/migrations/041_track_audio_stems_config.sql && echo PASS</automated>
    <human-check>Run `npx supabase db push --dry-run` (VALIDATION §Wave 0) and confirm migration 041 parses/applies cleanly. This is deployment-dependent; the sandbox has no linked project (see STATE.md Blockers) so a human runs this against the real project.</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/041_track_audio_stems_config.sql` exists.
    - The grep verify prints `PASS` (contains `262144000`, `application/zip`, `application/x-zip-compressed`, and the `on conflict (id) do update` idempotent clause).
    - The migration alters no access-control DDL from 004 (those rules stay intact): stripping comment lines, `grep -v '^--' supabase/migrations/041_track_audio_stems_config.sql | grep -ciE 'create polic|drop polic'` returns 0.
    - `npx supabase db push --dry-run` reports no SQL parse/apply error for 041.
  </acceptance_criteria>
  <done>Bucket `track-audio` config is bumped to 250MB and accepts ZIP MIME types via an idempotent migration; no RLS policy is altered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add readStems() and readInstrumental() typed readers to schema.ts</name>
  <files>lib/metadata/schema.ts</files>
  <read_first>
    - lib/metadata/schema.ts (lines 259-272 — the existing `MasterAudio` type + `readMasterAudio()` reader; this is the EXACT shape/defensive-parse pattern to mirror; also confirm the `// ─── Section ───` comment style and named-export convention)
    - .planning/phases/14-playback-room-refinement/14-PATTERNS.md (§`metadata` JSONB sub-object extension — the stems/instrumental key shapes)
    - components/vault/PlaybackView.tsx (lines 5-17 — TrackView already consumed downstream; these readers feed it in Plan 05)
  </read_first>
  <behavior>
    - readStems({ stems: { path: 'u/p/t.stems.zip', size: 1000, name: 'a.zip' } }) returns { path: 'u/p/t.stems.zip', size: 1000, name: 'a.zip' }
    - readStems({}) and readStems(null) and readStems(undefined) return null
    - readStems({ stems: { path: '' } }) returns null (empty path is not a valid ref)
    - readStems({ stems: { path: 'x' } }) returns { path: 'x', size: 0, name: 'stems.zip' } (size defaults to 0, name defaults to 'stems.zip' when absent/wrong-typed)
    - readInstrumental({ instrumental: { path: 'u/p/t.instrumental.mp3', size: 500, ext: 'mp3' } }) returns { path, size: 500, ext: 'mp3' }
    - readInstrumental({}) / null / undefined return null
    - readInstrumental({ instrumental: { path: 'x' } }) returns { path: 'x', size: 0, ext: 'mp3' } (ext defaults to 'mp3')
  </behavior>
  <action>
    In `lib/metadata/schema.ts`, directly below the existing `readMasterAudio()` block (~line 272), add two exported types and two exported reader functions following the `readMasterAudio` template verbatim (same defensive `typeof raw.path !== 'string' || !raw.path` guard, same null-return-on-absence). Add `export type StemsFile = { path: string; size: number; name: string }` and `export function readStems(metadata: Record<string, unknown> | null | undefined): StemsFile | null` reading `metadata?.stems` — default `size` to 0 when not a number, default `name` to `'stems.zip'` when not a non-empty string. Add `export type InstrumentalFile = { path: string; size: number; ext: string }` and `export function readInstrumental(metadata: Record<string, unknown> | null | undefined): InstrumentalFile | null` reading `metadata?.instrumental` — default `size` to 0, default `ext` to `'mp3'` (mirrors readMasterAudio's `ext` default of `'wav'`). Use camelCase, named exports, 2-space indent, no semicolons, `// ─── Section ───` header comments per CLAUDE.md. Do NOT modify `readMasterAudio` or any existing reader.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/metadata/schema.ts','utf8'); if(!/export function readStems\(/.test(s)||!/export function readInstrumental\(/.test(s)||!/export type StemsFile/.test(s)||!/export type InstrumentalFile/.test(s)) throw new Error('missing readers/types'); console.log('PASS')"</automated>
    <human-check>Run `npx tsc --noEmit` (or `npm run build`) — confirm strict-mode type-checking passes with the two new readers/types.</human-check>
  </verify>
  <acceptance_criteria>
    - `lib/metadata/schema.ts` exports `readStems`, `readInstrumental`, `StemsFile`, `InstrumentalFile` (node grep verify prints `PASS`).
    - Each reader returns `null` for `null`/`undefined`/`{}`/empty-path input and a fully-defaulted object for a valid path (matches the `<behavior>` cases exactly).
    - `readMasterAudio` and all pre-existing readers are byte-identical to before (git diff shows only additions after line ~272).
    - TypeScript strict-mode check passes.
  </acceptance_criteria>
  <done>schema.ts has canonical `readStems()`/`readInstrumental()` readers mirroring `readMasterAudio()`, ready for the routes (Plan 03), export lib (Plan 04), and playback UI (Plan 05) to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Package names carry [ASSUMED] provenance (sourced from training knowledge); install brings third-party code into the build |
| browser client → Supabase Storage (future, enabled by this migration) | The 250MB/ZIP allowlist set here becomes the authoritative server-side gate for the direct-to-storage upload in Plan 05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-SC | Tampering | npm install (archiver, @react-pdf/renderer, tus-js-client) | high | mitigate | Blocking human checkpoint (Task 1) verifies each package name on npmjs.com against RESEARCH.md's Package Legitimacy Audit before install; all three returned OK/no-postinstall-scripts |
| T-14-01 | Tampering | track-audio bucket allowed_mime_types | medium | mitigate | Migration 041 adds only `application/zip`/`application/x-zip-compressed` to the existing audio allowlist — the bucket allowlist stays the authoritative server-side MIME gate (RESEARCH §Security Domain); client-declared MIME is never trusted alone |
| T-14-02 | Elevation of Privilege | track-audio bucket RLS (path ownership) | high | accept | Migration 041 deliberately does NOT touch the four owner-path RLS policies from migration 004 (`(storage.foldername(name))[1] = auth.uid()::text`); leaving them intact preserves the per-user path constraint the direct upload depends on. Rationale for accept: no change = no new risk surface on this boundary. |
</threat_model>

<verification>
- `node -e` require check for all three packages prints `ok`.
- Migration 041 grep verify prints `PASS`; contains no RLS policy statements.
- schema.ts exports the four new symbols; TypeScript strict check passes.
- `npx supabase db push --dry-run` (human, deployment-dependent) applies 041 cleanly.
</verification>

<success_criteria>
- track-audio bucket accepts 250MB uploads and ZIP MIME types (D-07).
- Canonical `readStems()`/`readInstrumental()` readers exist and are strict-mode clean (D-03/D-04).
- archiver, @react-pdf/renderer, tus-js-client installed at pinned versions, human-verified as legitimate (D-10).
</success_criteria>

<output>
Create `.planning/phases/14-playback-room-refinement/14-01-SUMMARY.md` when done. Record: the exact `file_size_limit` value written, the final `allowed_mime_types` array, the signatures of `readStems`/`readInstrumental` (for Plans 03/04/05 to consume), and whether the `supabase db push --dry-run` was actually run or deferred to a human (per STATE.md's Phase 8 sandbox blocker).

## Artifacts this phase produces (Plan 01)
- `supabase/migrations/041_track_audio_stems_config.sql` (new migration)
- `lib/metadata/schema.ts` exports: `readStems`, `readInstrumental`, `StemsFile` (type), `InstrumentalFile` (type)
- `package.json` deps: `archiver`, `@react-pdf/renderer`, `tus-js-client`; devDeps: `@types/archiver`
</output>