---
phase: 25-funun-team-accounts-ae
plan: 06
type: execute
wave: 3
depends_on: [25-01, 25-04, 25-05]
files_modified:
  - app/(admin)/layout.tsx
  - app/(admin)/admin/team-members/page.tsx
  - components/admin/StaffAdmin.tsx
  - app/(admin)/admin/my-client-partners/page.tsx
  - components/admin/MyCompanies.tsx
autonomous: true
requirements: [TEAM-07, TEAM-06]
must_haves:
  truths:
    - "app/(admin)/layout.tsx admits ANY staff role (getStaffRole(user) !== null) instead of is_admin-only, and redirects non-staff to '/' (leadership-tier via the is_admin fallback still passes)"
    - "the sidebar is role-aware: leadership-only links ('Team Members' management + the existing leadership admin sections) are hidden from AE/BD; EVERY staff role sees 'My Client Partners' and 'Directory' (the all-roles Team Member Directory)"
    - "/admin/staff is a leadership-only page listing staff + a create form calling POST /api/admin/staff"
    - "/admin/my-companies is the AE/BD work queue: it lists only buyer orgs assigned to the caller (ae_user_id === caller), leadership sees all"
    - "every EXISTING leadership-only admin page keeps its own inline leadership self-guard, so an AE/BD that navigates directly is cleanly redirected (Pitfall 3 — no broken empty page)"
  artifacts:
    - "app/(admin)/layout.tsx (widened gate + role-aware sidebar)"
    - "app/(admin)/admin/staff/page.tsx + components/admin/StaffAdmin.tsx"
    - "app/(admin)/admin/my-companies/page.tsx + components/admin/MyCompanies.tsx"
  key_links:
    - "layout gate widening + role-aware sidebar + existing pages' inline leadership guards together close Pitfall 3 — none may be skipped"
    - "my-companies data is scoped by ae_user_id in the server component exactly like the scoped GET route (Pitfall 4)"
  prohibitions:
    - "MUST NOT build a separate /team route group — extend /admin (RESEARCH Open Question 3, D-01 generalize-don't-duplicate)"
    - "MUST NOT remove or weaken any existing admin page's inline leadership self-guard"
    - "MUST NOT show leadership-only sidebar links to AE/BD"
    - "MUST NOT let /admin/my-companies show orgs the caller is not assigned to"
---

<objective>
**Naming (owner 2026-08-05 — apply globally; see 25-CONTEXT "Naming & vocabulary"):** user-facing labels are
**"Team Members"** (not "Staff") and **"Client Partners"** (not "Buyer Orgs"/"companies"). **User-visible page
routes** are **`/admin/team-members`**, **`/admin/my-client-partners`**, and the existing Buyer Orgs link is
relabeled **"Client Partners"** (its route `/admin/buyer-orgs` stays). **Keep internal names unchanged**: API
routes (`/api/admin/staff`, `/api/admin/buyer-orgs`), component files (`StaffAdmin.tsx`, `MyCompanies.tsx`),
tables (`funun_staff`, `buyer_orgs`, `staff_role`). Where this plan's body still says "Staff"/"My Companies"/
"buyer orgs" as a *label or page route*, read it as the renamed value; internal identifiers are literal.

Surface the Team Member model in the existing /admin area: widen the admin gate to admit AE/BD, make the sidebar
role-aware, add the leadership-only **Team Members** management page (`/admin/team-members`), and add the AE/BD
**"My Client Partners"** work queue (`/admin/my-client-partners`).

Decision IDs: D-01 = generalize/extend /admin (not a new /team); D-02 = leadership manages staff.
TEAM-06 (work queue surface) + TEAM-07 (admin surface widening). Pitfall 3 (layout widened but pages must
still self-guard) is closed here.

Purpose: Give AE/BD a real, scoped home inside the product and give leadership the staff-management UI —
without exposing leadership-only pages to AE/BD.
Output: widened layout + role-aware sidebar, /admin/staff, /admin/my-companies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-funun-team-accounts-ae/25-PATTERNS.md
@.planning/phases/25-funun-team-accounts-ae/25-01-SUMMARY.md
@.planning/phases/25-funun-team-accounts-ae/25-04-SUMMARY.md
@.planning/phases/25-funun-team-accounts-ae/25-05-SUMMARY.md

# Verified analogs
@app/(admin)/layout.tsx
@app/(admin)/admin/buyer-orgs/page.tsx
@app/(admin)/admin/members/page.tsx
@components/admin/BuyerOrgsAdmin.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen app/(admin)/layout.tsx gate + role-aware sidebar</name>
  <files>app/(admin)/layout.tsx</files>
  <read_first>
    - app/(admin)/layout.tsx (current inline is_admin gate lines 6-15 + the flat sidebar link list lines 19-92)
    - lib/admin/gate.ts (getStaffRole — import and use it instead of the inline is_admin read)
    - 25-PATTERNS.md section "app/(admin)/layout.tsx (MODIFY IN PLACE)" (sidebar + Pitfall 3 conditional-links note)
  </read_first>
  <action>
    Modify app/(admin)/layout.tsx: replace the inline `is_admin === true` check with
    const role = getStaffRole(user); if (!role) redirect('/'). Compute a boolean isLeadership = role ===
    'leadership'. Keep every existing sidebar link but wrap the leadership-only ones (all current links —
    Checklist, Tips, Curators, Industry Members, Client Partners (the existing Buyer Orgs link, relabeled),
    Deals, GTM Metrics, Green Room Placements, Reports, Verification, E-Sign Usage — plus the new
    "Team Members" management link) so they render only when isLeadership. Always render — for EVERY staff
    role — a "My Client Partners" link (href /admin/my-client-partners) AND a "Directory" link
    (href /admin/directory, the all-roles Team Member Directory from plan 25-10); render the "Team Members"
    management link (href /admin/team-members) only for leadership. Keep the SignOutButton footer. Preserve the exact link
    classes. This — combined with each existing page keeping its own inline leadership guard (verified in
    Task 3) — closes Pitfall 3: AE/BD never see leadership links, and direct navigation is bounced by the page.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app/\(admin\)/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Layout imports getStaffRole and redirects only when role is null
    - Leadership-only links are wrapped in an isLeadership conditional; My Companies renders for all staff
    - tsc + eslint clean
  </acceptance_criteria>
  <done>Admin gate widened to any staff role; sidebar role-aware; Staff link leadership-only, My Companies for all.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/staff page + StaffAdmin component (leadership-only)</name>
  <files>app/(admin)/admin/staff/page.tsx, components/admin/StaffAdmin.tsx</files>
  <read_first>
    - app/(admin)/admin/members/page.tsx (server-component shape: force-dynamic, per-page leadership guard,
      service-client read, pass rows to a client component)
    - components/admin/BuyerOrgsAdmin.tsx (client-component list + create-form pattern, fetch to the admin route)
  </read_first>
  <action>
    Create app/(admin)/admin/staff/page.tsx as a server component with export const dynamic = 'force-dynamic'
    and a per-page LEADERSHIP guard (fetch user, if getStaffRole(user) !== 'leadership' redirect('/') — matches
    the codebase convention that pages self-guard, not rely on the layout). Read the funun_staff list via the
    service client (column-explicit) with per-row email attached (mirror members page), and render
    components/admin/StaffAdmin.tsx. Create components/admin/StaffAdmin.tsx ('use client') mirroring
    BuyerOrgsAdmin: a list of staff (email · role · joined) and a create form (email, display name, role select
    over the three staff roles) that POSTs to /api/admin/staff, surfacing 400/409 errors inline and refreshing
    on success. Follow project style (no semicolons, named exports, `@/` imports).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Page self-guards to leadership (redirects AE/BD)
    - Create form posts to /api/admin/staff and handles 400/409 inline
    - tsc clean; role select is limited to the three staff roles
  </acceptance_criteria>
  <done>Leadership-only staff management UI shipped, wired to the staff routes.</done>
</task>

<task type="auto">
  <name>Task 3: /admin/my-companies work queue + confirm existing pages self-guard</name>
  <files>app/(admin)/admin/my-companies/page.tsx, components/admin/MyCompanies.tsx</files>
  <read_first>
    - app/(admin)/admin/buyer-orgs/page.tsx (server-component service-client read + per-row member count to mirror)
    - lib/staff/scope.ts / 25-05-SUMMARY.md (the ae_user_id scoping the queue must mirror)
  </read_first>
  <action>
    Create app/(admin)/admin/my-companies/page.tsx as a server component (force-dynamic) with a per-page
    guard that admits ANY staff role (getStaffRole(user) !== null, else redirect('/')). Read buyer_orgs via
    the service client; when getStaffRole(user) !== 'leadership', scope with .eq('ae_user_id', user.id) so an
    AE/BD sees only assigned companies; leadership sees all (this is the read-side twin of 25-05's scoped GET,
    Pitfall 4). Render components/admin/MyCompanies.tsx ('use client'): a list of assigned companies with a
    link/inline editor that PATCHes the allowed field(s) to /api/admin/buyer-orgs/[id] (name for v1), surfacing
    the 404-on-unassigned and 400 responses. THEN, as part of this task, run a grep over app/(admin)/**/page.tsx
    to CONFIRM every existing admin page still contains its own leadership self-guard (inline is_admin OR a
    getStaffRole==='leadership' check); if any page is found without one, add a leadership self-guard to it
    (record which, if any, in the SUMMARY). Do not otherwise modify existing pages.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -rL "is_admin\|getStaffRole" app/\(admin\)/admin/*/page.tsx app/\(admin\)/*/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - my-companies scopes by ae_user_id for non-leadership; leadership sees all
    - The grep lists NO existing admin page lacking a self-guard (empty result = pass), except newly-added guards
    - Editor PATCHes the allowlisted field and handles 404/400; tsc clean
  </acceptance_criteria>
  <done>AE/BD work queue shipped and scoped; every admin page confirmed self-guarding (Pitfall 3 closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AE/BD browser → /admin route group | layout admits any staff role; leadership-only pages self-guard and bounce AE/BD; sidebar hides leadership links |
| my-companies server read → buyer_orgs | scoped by ae_user_id === caller for non-leadership, mirroring the write-side scope |
| client role (session JWT) → UI | role decides which nav/actions to render (hints only); every write is re-gated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-11 | Elevation of Privilege | AE/BD reaching a leadership-only page | medium | mitigate | Role-aware sidebar hides leadership links; every existing leadership page keeps its inline self-guard (verified by grep) so direct navigation redirects — layout widening never stands alone (Pitfall 3) |
| T-25-08 | Information Disclosure | my-companies leaking unassigned orgs | high | mitigate | Server component scopes buyer_orgs by ae_user_id for non-leadership, identical to the scoped GET route (Pitfall 4) |
| T-25-12 | Information Disclosure | client trusting app_metadata for sensitive render | low | mitigate | Role used only for nav/affordance hints; the server re-checks on every write; no sensitive data is gated by client role alone |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds
- The Task 3 grep returns no existing admin page lacking a self-guard
- Live AE/BD navigation + scoped-queue visual smoke is exercised at the 25-07 post-push checkpoint (needs staff accounts + ae_user_id live)
</verification>

<success_criteria>
- /admin admits all staff roles with a role-aware sidebar; leadership-only pages remain self-guarded
- Leadership-only Staff management UI + AE/BD scoped My Companies queue shipped
- No /team route group introduced
</success_criteria>

## Artifacts this phase produces (Plan 25-06)

| Route / Component | File | Kind |
|-------------------|------|------|
| widened admin gate + role-aware sidebar | app/(admin)/layout.tsx | modified layout |
| `/admin/staff` | app/(admin)/admin/staff/page.tsx | page (leadership-only) |
| `StaffAdmin` | components/admin/StaffAdmin.tsx | client component |
| `/admin/my-companies` | app/(admin)/admin/my-companies/page.tsx | page (all staff, scoped) |
| `MyCompanies` | components/admin/MyCompanies.tsx | client component |

<output>
Create `.planning/phases/25-funun-team-accounts-ae/25-06-SUMMARY.md` when done. Record any page that needed
a self-guard added (expected: none).
</output>
