---
phase: 25-funun-team-accounts-ae
plan: 09
type: execute
wave: 4
depends_on: [25-02, 25-05, 25-06]
files_modified:
  - lib/staff/notifications.ts
  - app/api/admin/buyer-orgs/[id]/ae/route.ts
  - app/(admin)/admin/buyer-orgs/page.tsx
  - components/admin/BuyerOrgsAdmin.tsx
  - __tests__/staff-buyer-orgs-api.test.ts
autonomous: true
requirements: [TEAM-04, TEAM-07]
must_haves:
  truths:
    - "a Leadership Admin can REASSIGN any Client Partner to a different AE from the Client Partners admin list — a per-row AE control (current AE + a picker of AEs) that PATCHes /api/admin/buyer-orgs/[id]/ae"
    - "the /ae route is reassignment-aware: it reads the PRIOR ae_user_id before writing, and when the assignment CHANGES away from a previous AE it notifies BOTH the newly-assigned AE (gained) AND the previous AE (lost) — not only the new one"
    - "the reassign control is leadership-only (the route already gates requireStaff(['leadership']); the UI control renders only for leadership) — AE/BD cannot reassign"
    - "the AE picker options come from funun_staff where staff_role = 'ae' (the assignable pool), plus an 'Unassign' option (ae_user_id = null)"
    - "every reassignment is audited exactly once via logStaffAction('assign_ae') (already in 25-05) — reassignment reuses that path, and the previous AE's scoped access to that Client Partner ends immediately (scoping is by ae_user_id)"
  artifacts:
    - "lib/staff/notifications.ts (buildAeUnassignedNotification added alongside buildAeAssignedNotification)"
    - "app/api/admin/buyer-orgs/[id]/ae/route.ts (reassignment-aware: prior-AE read + notify-both)"
    - "app/(admin)/admin/buyer-orgs/page.tsx + components/admin/BuyerOrgsAdmin.tsx (leadership AE column + reassign control)"
  key_links:
    - "reassignment is the SAME write path as assignment (buyer_orgs.ae_user_id via the leadership-only /ae route) — do not fork a second endpoint"
    - "notify-both requires reading the old ae_user_id in the SAME handler before the update (a read-then-write in one request)"
  prohibitions:
    - "MUST NOT expose the reassign control to AE/BD — leadership-only (the route stays requireStaff(['leadership']))"
    - "MUST NOT create a second assignment endpoint — enhance the existing /ae route"
    - "MUST NOT block the response on notification delivery — both notifications are best-effort side effects (.catch(() => {}))"
    - "MUST NOT skip the previous-AE notification when the assignment changes from one AE to another"
---

<objective>
Deliver the Leadership capability: **reassign a Client Partner to a different AE** (owner 2026-08-05). The write
path already exists (25-05's leadership-only `PATCH /api/admin/buyer-orgs/[id]/ae` sets `ae_user_id`); this plan
adds (1) the **Leadership reassign UI** on the Client Partners admin list, and (2) makes the route
**reassignment-aware** by notifying BOTH the new and the previous AE when an assignment changes hands.

Requirements: TEAM-04 (AE assignment) + TEAM-07 (admin surface) — no new requirement ID; reassignment is the
assignment capability exercised by leadership through a UI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-funun-team-accounts-ae/25-CONTEXT.md
@.planning/phases/25-funun-team-accounts-ae/25-02-SUMMARY.md
@.planning/phases/25-funun-team-accounts-ae/25-05-SUMMARY.md
@.planning/phases/25-funun-team-accounts-ae/25-06-SUMMARY.md

# Verified analogs
@lib/staff/notifications.ts
@app/api/admin/buyer-orgs/[id]/ae/route.ts
@components/admin/BuyerOrgsAdmin.tsx
@app/(admin)/admin/buyer-orgs/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the /ae route reassignment-aware (notify both AEs)</name>
  <files>lib/staff/notifications.ts, app/api/admin/buyer-orgs/[id]/ae/route.ts, __tests__/staff-buyer-orgs-api.test.ts</files>
  <read_first>
    - app/api/admin/buyer-orgs/[id]/ae/route.ts (the 25-05 handler: requireStaff(['leadership']), validate uuid|null, update, notify new AE)
    - lib/staff/notifications.ts (buildAeAssignedNotification — mirror its shape for buildAeUnassignedNotification)
    - lib/notifications/index.ts (createNotification signature)
  </read_first>
  <action>
    Add buildAeUnassignedNotification(orgId, orgName) to lib/staff/notifications.ts — a pure builder mirroring
    buildAeAssignedNotification, phrased "A Client Partner was reassigned away from you" for the losing AE.
    In app/api/admin/buyer-orgs/[id]/ae/route.ts, BEFORE the update, fetch the org's current ae_user_id
    (service client, select ae_user_id). After the successful update, compute changed = prevAeId && prevAeId !==
    newAeId; when changed, also createNotification for prevAeId via buildAeUnassignedNotification (best-effort,
    .catch(() => {})), in addition to the existing new-AE notification. Keep the single logStaffAction('assign_ae')
    call. Extend __tests__/staff-buyer-orgs-api.test.ts: a reassignment from AE-A to AE-B notifies BOTH; a first
    assignment (prev null) notifies only the new AE; unassign (null) notifies only the previous AE.
  </action>
  <verify>
    <automated>npx jest __tests__/staff-buyer-orgs-api.test.ts -t "assign"</automated>
  </verify>
  <acceptance_criteria>
    - Reassign A→B triggers two createNotification calls (B assigned, A unassigned); first-assign triggers one
    - The prior ae_user_id is read in the same handler before the update
    - logStaffAction called exactly once; tsc/jest clean
  </acceptance_criteria>
  <done>The /ae route notifies both the gaining and losing AE on reassignment; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Leadership reassign control on the Client Partners admin list</name>
  <files>app/(admin)/admin/buyer-orgs/page.tsx, components/admin/BuyerOrgsAdmin.tsx</files>
  <read_first>
    - app/(admin)/admin/buyer-orgs/page.tsx (existing Client Partners list server component — how it reads orgs)
    - components/admin/BuyerOrgsAdmin.tsx (the client list to extend with an AE column + control)
    - lib/admin/gate.ts (getStaffRole — the page/control is leadership-only)
  </read_first>
  <action>
    In app/(admin)/admin/buyer-orgs/page.tsx: alongside the org list, when getStaffRole(user) === 'leadership',
    also read the assignable AE pool (funun_staff where staff_role = 'ae' → {user_id, display_name}) and each
    org's current ae_user_id; pass both to the component. In components/admin/BuyerOrgsAdmin.tsx: add a
    "Account Exec" column showing the current AE (or "Unassigned"); render a leadership-only reassign control —
    a select of the AE pool plus an "Unassign" option — that on change PATCHes /api/admin/buyer-orgs/[id]/ae with
    { ae_user_id } and refreshes the row, surfacing errors inline. Render the control only when isLeadership
    (passed from the page); AE/BD see the column read-only or not at all. Follow project style (no semicolons,
    named exports, `@/` imports). Reuse the console theme tokens (25-08) so it reads in light + dark.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Leadership sees a current-AE column + a reassign picker; the picker PATCHes /api/admin/buyer-orgs/[id]/ae
    - The control is gated to leadership (not rendered/actionable for AE/BD)
    - AE options come from funun_staff staff_role='ae' + an Unassign option; tsc + build clean
  </acceptance_criteria>
  <done>Leadership can reassign any Client Partner to a different AE from the Client Partners list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leadership browser → /ae route | reassignment is a privileged write gated requireStaff(['leadership']); the UI control is a hint, the route is the authority |
| reassign write → buyer_orgs.ae_user_id | changing ae_user_id immediately re-scopes the previous AE OUT and the new AE IN (read scoping is by ae_user_id) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-15 | Elevation of Privilege | AE/BD attempting a reassign (self-grant a Client Partner) | high | mitigate | The /ae route stays requireStaff(['leadership']); the reassign control renders only for leadership — a forged client request still 403s at the route |
| T-25-16 | Repudiation | reassignment with no trail | medium | mitigate | Every assignment/reassignment logs 'assign_ae' via logStaffAction (25-05) with the changed ae_user_id — who reassigned what, when |
| T-25-17 | Information Disclosure | notifying the wrong/previous AE with sensitive data | low | mitigate | Notifications carry only org name + a reassignment message; the previous AE loses read access at the same write |
| T-25-SC | Tampering | npm/pip installs | low | accept | No new dependencies — reuses createNotification + the existing route |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds; the reassign jest cases green
- Live (at the 25-07 checkpoint): leadership reassigns a Client Partner A→B; B gains it in "My Client Partners",
  A loses it, both are notified, and a staff_audit_log 'assign_ae' row is written
</verification>

<success_criteria>
- Leadership can reassign any Client Partner to a different AE from the Client Partners list
- Reassignment notifies both the gaining and losing AE and is audited; AE/BD cannot reassign
- No second endpoint; the existing leadership-only /ae route is the single write path
</success_criteria>

## Artifacts this phase produces (Plan 25-09)

| Symbol / Surface | File | Kind |
|------------------|------|------|
| `buildAeUnassignedNotification` | lib/staff/notifications.ts | pure builder (added) |
| reassignment-aware `/ae` PATCH (notify-both) | app/api/admin/buyer-orgs/[id]/ae/route.ts | modified route |
| leadership AE column + reassign control | components/admin/BuyerOrgsAdmin.tsx | modified client component |
| AE-pool + current-AE read | app/(admin)/admin/buyer-orgs/page.tsx | modified page |

<output>
Create `.planning/phases/25-funun-team-accounts-ae/25-09-SUMMARY.md` when done.
</output>
