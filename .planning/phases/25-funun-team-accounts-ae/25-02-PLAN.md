---
phase: 25-funun-team-accounts-ae
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/staff/audit.ts
  - lib/staff/audit.test.ts
  - lib/staff/notifications.ts
  - lib/staff/notifications.test.ts
autonomous: true
requirements: [TEAM-05, TEAM-06]
must_haves:
  truths:
    - "logStaffAction(service, args) inserts exactly one staff_audit_log row per call, including idempotent/no-op edits (unconditional-log discipline, D-04)"
    - "logStaffAction returns { ok:false, error } on insert failure and never throws — the caller decides whether a log failure blocks the primary write (D-04 prefers: never fail the primary write on a log error)"
    - "resolveLeadRecipient(org) returns the assigned ae_user_id, else the leadership fallback id — an unassigned company's lead still routes somewhere"
    - "buildLeadRoutedNotification / buildAeAssignedNotification are pure builders returning a createNotification-compatible payload (no I/O)"
  artifacts:
    - "lib/staff/audit.ts exports logStaffAction()"
    - "lib/staff/notifications.ts exports resolveLeadRecipient() + the pure notification builders"
    - "lib/staff/audit.test.ts and lib/staff/notifications.test.ts green"
  key_links:
    - "logStaffAction is the ONE write-through call every staff write (25-04, 25-05) invokes — centralizing default #4 into a single code-review surface"
    - "notification builders feed the existing createNotification(service, ...) — reuse, do not re-implement in-app/email fan-out"
  prohibitions:
    - "MUST NOT build a new notification table or queue — reuse the existing notifications table + createNotification (RESEARCH Don't Hand-Roll)"
    - "MUST NOT scatter ad-hoc .insert() audit calls across routes — one helper only"
---

<objective>
Ship the two write-through helpers every staff write depends on: the audit trail (D-04) and the
lead/work-routing notification builders (goal deliverable #4). Both are pure/mockable and land in Wave 1 so
Wave 2 routes can call them.

Decision IDs: D-04 = audit staff writes to client data (reuse verification_audit_log pattern). Lead routing
is ROADMAP goal deliverable #4 (route new-buyer signups + buyer activity to the right employee, in-app +
email). Provisional requirements TEAM-05 (audit), TEAM-06 (lead/work routing).

Purpose: Make "log every staff write" and "notify the assigned AE" enforceable from a single file each,
rather than N routes.
Output: lib/staff/audit.ts (logStaffAction), lib/staff/notifications.ts (resolveLeadRecipient + builders),
with unit tests using a mocked service client (the staff_audit_log table is authored in 25-03; live insert
is verified after the 25-07 push checkpoint).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-funun-team-accounts-ae/25-PATTERNS.md

# Verified analogs
@lib/notifications/index.ts
@lib/deals/notifications.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: logStaffAction write-through helper (lib/staff/audit.ts)</name>
  <files>lib/staff/audit.ts, lib/staff/audit.test.ts</files>
  <read_first>
    - lib/notifications/index.ts (createNotification: service-client-first-arg, { ok, error } non-throwing
      return, insert-and-return-ok shape to copy)
    - 25-PATTERNS.md section "lib/staff/audit.ts — logStaffAction()"
  </read_first>
  <behavior>
    - logStaffAction(mockService, { actorId, action:'edit_buyer_org', targetType:'buyer_org', targetId, changes })
      calls service.from('staff_audit_log').insert once with those mapped columns and returns { ok:true }
    - A no-op/idempotent edit (empty changes) still produces exactly one insert (unconditional log, D-04)
    - When the mocked insert returns an error, logStaffAction returns { ok:false, error } and does NOT throw
    - actorId is required; targetId and changes are optional and default to null / {}
  </behavior>
  <action>
    Create lib/staff/audit.ts exporting async logStaffAction(service: SupabaseClient, args: { actorId: string;
    action: string; targetType: string; targetId?: string | null; changes?: Record<string, unknown> }):
    Promise<{ ok: boolean; error?: string }>. Mirror createNotification's shape exactly: service client is
    the first arg, insert into staff_audit_log mapping actor_id/action/target_type/target_id/changes
    (changes defaulting to {}), return { ok: !error, error: error?.message }, never throw. Add
    lib/staff/audit.test.ts using a hand-rolled mock service client (an object whose from().insert() records
    the call and returns a configurable { error }) — mirror the repo's mocked-Supabase test convention.
    Assert the unconditional-insert behavior and the non-throwing error path.
  </action>
  <verify>
    <automated>npx jest lib/staff/audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test proves exactly one insert per call, including the empty-changes case
    - Test proves { ok:false } (not a throw) on a mocked insert error
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>logStaffAction exported, single-insert unconditional, non-throwing; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lead-routing notification builders + recipient resolver (lib/staff/notifications.ts)</name>
  <files>lib/staff/notifications.ts, lib/staff/notifications.test.ts</files>
  <read_first>
    - lib/deals/notifications.ts (pure-builder convention: no client, no I/O, returns a
      createNotification-compatible payload; caller does the try/catch side-effect insert)
    - lib/notifications/index.ts (the createNotification args shape the builders must match)
  </read_first>
  <behavior>
    - resolveLeadRecipient({ ae_user_id: 'ae1' }, 'lead1') returns 'ae1'
    - resolveLeadRecipient({ ae_user_id: null }, 'lead1') returns 'lead1' (leadership fallback)
    - resolveLeadRecipient(null, 'lead1') returns 'lead1'
    - buildAeAssignedNotification({ recipientId, orgId, orgName, actorId }) returns a payload with
      type:'ae_assigned', a title naming the company, a link to the company's admin surface, userId=recipientId
    - buildLeadRoutedNotification({ recipientId, orgId, orgName, ... }) returns a payload with
      type:'lead_routed' and userId=recipientId
    - builders are pure — calling them twice with the same args returns equal objects, no I/O
  </behavior>
  <action>
    Create lib/staff/notifications.ts. Export a pure resolveLeadRecipient(org, leadershipFallbackId) that
    returns org?.ae_user_id ?? leadershipFallbackId (so an unassigned company still routes to leadership —
    goal deliverable #4). Export pure builders buildAeAssignedNotification(args) and
    buildLeadRoutedNotification(args) that each return an object matching createNotification's args
    (userId, type, title, body, link, data, actorId) — mirror lib/deals/notifications.ts exactly (no
    Supabase client, no I/O; the notifications.type column is unconstrained TEXT so no migration is needed
    for the new 'ae_assigned'/'lead_routed' types). HTML/text templating and the actual DB write stay in
    createNotification at the call site (25-05 wires buildAeAssignedNotification after AE assignment; the
    buyer-signup lead-routing call site lands in Phase 23, so leave buildLeadRoutedNotification wired only
    to any buyer-creation event that exists today and document the Phase 23 hook in the SUMMARY). Add
    lib/staff/notifications.test.ts covering the behavior bullets.
  </action>
  <verify>
    <automated>npx jest lib/staff/notifications.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - resolveLeadRecipient fallback proven (assigned → AE, unassigned/null → leadership)
    - builders return createNotification-compatible payloads and perform no I/O
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>resolveLeadRecipient + both builders exported, pure, tested; Phase 23 signup hook documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff route → staff_audit_log | audit rows are written only via the service-role client inside logStaffAction; clients can never write or read this table (enforced by 25-03's zero-RLS/REVOKE table) |
| notification recipient resolution | recipientId is derived server-side from ae_user_id / leadership fallback, never from a client-supplied target |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03 | Repudiation | staff_audit_log write path | high | mitigate | Single logStaffAction helper called unconditionally after every staff write (D-04); "forgot to log" becomes a one-file review surface; table itself is zero-RLS/REVOKE-ALL (25-03) so client writes are structurally impossible |
| T-25-14 | Tampering | audit changes payload | low | mitigate | changes is a JSONB snapshot written by the server from the sanitized update object, never from raw request body |
| T-25-15 | Information Disclosure | lead-routing recipient | low | mitigate | resolveLeadRecipient only ever returns ae_user_id or the configured leadership id — never a client-chosen recipient |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx jest lib/staff/audit.test.ts lib/staff/notifications.test.ts` green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- logStaffAction is the single, unconditional, non-throwing audit write-through
- resolveLeadRecipient + pure notification builders exist and reuse the existing notifications infra
</success_criteria>

## Artifacts this phase produces (Plan 25-02)

| Symbol | File | Kind |
|--------|------|------|
| `logStaffAction(service, args)` | lib/staff/audit.ts | async write-through |
| `resolveLeadRecipient(org, fallbackId)` | lib/staff/notifications.ts | pure resolver |
| `buildAeAssignedNotification(args)` | lib/staff/notifications.ts | pure builder |
| `buildLeadRoutedNotification(args)` | lib/staff/notifications.ts | pure builder |

<output>
Create `.planning/phases/25-funun-team-accounts-ae/25-02-SUMMARY.md` when done
</output>
