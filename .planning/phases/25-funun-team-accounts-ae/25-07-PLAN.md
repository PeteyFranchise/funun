---
phase: 25-funun-team-accounts-ae
plan: 07
type: execute
wave: 5
depends_on: [25-03, 25-04, 25-05, 25-06]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TEAM-01, TEAM-02, TEAM-03, TEAM-04, TEAM-05, TEAM-06, TEAM-07, TEAM-08]
user_setup:
  - service: supabase
    why: "Push migrations 085 + 086 (human-gated) and seed the bootstrap leadership account"
    dashboard_config:
      - task: "Run `supabase db push` (via Codex) to apply 085 + 086, then `supabase migration list` to confirm LOCAL=REMOTE through 086"
        location: "Supabase project wgfjakfiyeewzfuxkgyo (production)"
      - task: "Seed/confirm the bootstrap leadership account — set app_metadata.staff_role='leadership' on the owner account (or accept the is_admin→leadership fallback for beta, decision A1)"
        location: "Supabase dashboard / service role (same way is_admin is set today)"
must_haves:
  truths:
    - "migrations 085 + 086 are LIVE on the remote DB (supabase migration list shows LOCAL=REMOTE through 086)"
    - "the bootstrap leadership account can reach /admin (via explicit staff_role='leadership' OR the is_admin fallback), and the A1 fallback decision is recorded"
    - "post-push live smoke confirms: authenticated/anon cannot read funun_staff or staff_audit_log; ae_user_id is not readable by a buyer/authenticated client; an AE cannot edit an org it isn't assigned to (404); a staff write produces exactly one staff_audit_log row"
    - "Phase 25 requirement IDs TEAM-01..TEAM-08 are registered in .planning/REQUIREMENTS.md (closing the pre-existing gap pattern, not extending it)"
  artifacts:
    - ".planning/REQUIREMENTS.md Phase 25 section (TEAM-01..TEAM-08)"
    - "25-07-SUMMARY.md recording push confirmation, the A1 decision, and live-smoke results"
  key_links:
    - "no live-DB verification in this phase may run before this push checkpoint completes"
    - "app_metadata.staff_role remains authoritative for the gate even after funun_staff exists (Pitfall 1)"
  prohibitions:
    - "Claude MUST NOT run `supabase db push` — the owner runs it via Codex after review; this task only confirms"
    - "MUST NOT invent requirement text that overstates delivery — register TEAM-01..07 against the behaviors actually shipped"
---

<objective>
Close Phase 25: the human-gated migration push (the ONE thing Claude cannot do), the bootstrap leadership
seed (D-02), the deferred live-DB behavioral smoke, and registration of the provisional requirement IDs.

Decision IDs: D-02 = bootstrap seeded like is_admin (owner via Supabase/service role); D-04 = audit trail
live-verified; A1 = the is_admin→leadership fallback decision is confirmed/recorded here. This plan carries
all provisional requirements TEAM-01..TEAM-08 for registration.

Purpose: Migrations are HUMAN-GATED in this project (prior phases 16/21 handled pushes exactly this way).
Everything built in Waves 1-3 passes tsc/build/jest WITHOUT the push (types come from query strings, not
the live DB); this plan is where live data is touched and the phase's requirement IDs are recorded.
Output: live migrations, a reachable leadership account, live smoke results, and a REQUIREMENTS.md Phase 25 section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-funun-team-accounts-ae/25-CONTEXT.md
@.planning/phases/25-funun-team-accounts-ae/25-03-SUMMARY.md
@.planning/phases/25-funun-team-accounts-ae/25-04-SUMMARY.md
@.planning/phases/25-funun-team-accounts-ae/25-05-SUMMARY.md
@.planning/phases/25-funun-team-accounts-ae/25-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Human migration push (085 + 086) + bootstrap leadership seed</name>
  <action>
    HUMAN-GATED — Claude does not run this. The owner reviews migrations 085 + 086, pushes them to the
    production Supabase project via Codex (`supabase db push`), confirms LOCAL=REMOTE through 086 via
    `supabase migration list`, seeds the bootstrap leadership account (app_metadata.staff_role='leadership')
    or accepts the is_admin→leadership fallback (A1), and confirms /admin still loads. Claude only records
    the outcome. No live-DB verification in this phase runs before this task completes.
  </action>
  <what-built>
    Migrations 085 (funun_staff + staff_audit_log, zero-RLS/REVOKE-ALL) and 086 (buyer_orgs.ae_user_id
    private column) are authored and their migration-text test is green. The staff gate, provisioning
    routes, scoped buyer-org routes, and admin UI are all built and pass tsc/build/jest against mocked
    clients. Nothing has touched the live database yet.
  </what-built>
  <how-to-verify>
    1. Review migrations 085 + 086 for correctness (zero-policy + REVOKE-ALL on both new tables; ae_user_id
       absent from the buyer_orgs GRANT SELECT list).
    2. Via Codex, run `supabase db push` to apply 085 + 086 to the production project.
    3. Run `supabase migration list` and confirm LOCAL = REMOTE through migration 086.
    4. Seed the bootstrap leadership account: set app_metadata.staff_role='leadership' on the owner account
       the same way is_admin is set today (Supabase dashboard / service role) — OR decide to rely on the
       is_admin→leadership fallback for beta (A1). Record which choice you made.
    5. Confirm the owner account can still load /admin after deploy.
  </how-to-verify>
  <resume-signal>Type "pushed" once 085/086 are live (LOCAL=REMOTE through 086) and the leadership account reaches /admin; state your A1 choice (explicit re-seed vs fallback).</resume-signal>
  <verify><human-check>Owner confirms LOCAL=REMOTE through 086 and /admin loads for the leadership account</human-check></verify>
  <done>085 + 086 live; leadership account reachable; A1 decision recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Post-push live behavioral smoke of the security mitigations</name>
  <action>
    HUMAN-GATED — run after Task 1. The owner exercises the six live checks below against the running app /
    live DB and reports results. These are the mitigations that could only be tested against real tables
    (service-role-only reachability, private ae_user_id, assignment scope 404, one-audit-row-per-action,
    scoped My Companies, leadership-page redirect). Claude records pass/fail and routes any failure to gap closure.
  </action>
  <what-built>
    Post-push live behavioral smoke of the security-critical mitigations that could only be tested against
    real tables (deferred from Waves 1-3 per the human-gate note).
  </what-built>
  <how-to-verify>
    Against the live DB / running app, confirm each:
    1. As a normal authenticated (artist/buyer) session, a direct PostgREST read of funun_staff and
       staff_audit_log returns no rows / permission denied (service-role-only holds).
    2. As a buyer/authenticated session, buyer_orgs reads do NOT expose ae_user_id (column not in the GRANT list).
    3. Create a second staff account (AE) via /admin/staff; assign it to one buyer org (leadership) and
       confirm a PATCH to a DIFFERENT org from that AE returns 404 (assignment scope holds).
    4. Perform one buyer-org edit and one AE assignment; confirm exactly one staff_audit_log row per action.
    5. Confirm the AE's /admin/my-companies shows only its assigned company; leadership sees all.
    6. Confirm an AE/BD navigating directly to a leadership-only admin page (e.g. /admin/verification) is redirected.
  </how-to-verify>
  <resume-signal>Type "verified" once all six live checks pass, or list any that failed for gap closure.</resume-signal>
  <verify><human-check>All six live behavioral checks pass</human-check></verify>
  <done>Service-role-only tables, private ae_user_id, assignment-scope 404, one-audit-row-per-action, scoped queue, and leadership-page redirect all confirmed live.</done>
</task>

<task type="auto">
  <name>Task 3: Register Phase 25 requirement IDs (TEAM-01..07) in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (mirror the existing per-phase section format — heading, ID table/list, status column)
    - 25-01..25-06 SUMMARYs (map each TEAM-ID to the behavior actually delivered)
  </read_first>
  <action>
    Add a Phase 25 section to .planning/REQUIREMENTS.md registering the seven provisional IDs against the
    behaviors this phase actually shipped, matching the file's existing section format exactly:
    TEAM-01 staff role gate (getStaffRole/requireStaff, verifyAdmin alias); TEAM-02 staff schema
    (funun_staff + staff_audit_log zero-RLS, buyer_orgs.ae_user_id private); TEAM-03 staff provisioning
    (createStaffAccount atomic role-set + leadership-only routes); TEAM-04 assignment-scoped buyer editing
    + AE assignment (field allowlist + isAssignedToOrg); TEAM-05 staff audit trail (logStaffAction);
    TEAM-06 lead/work routing (AE-assigned notification + My Companies queue); TEAM-07 staff admin surface
    (widened /admin gate + role-aware sidebar). Do NOT overstate delivery (e.g. buyer-signup lead-routing
    call site lands in Phase 23; note it as such). Mark each ID's status per the file's convention. Run the
    full suite as the closeout gate.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md has a Phase 25 section listing TEAM-01..TEAM-08 in the file's existing format
    - Requirement text matches shipped behavior (Phase 23 hooks noted, not claimed as delivered)
    - Full jest suite green; `npx tsc --noEmit` and lint clean
  </acceptance_criteria>
  <done>TEAM-01..07 registered against delivered behavior; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored migration → live DB | crossing this boundary is human-gated (owner runs `supabase db push` via Codex); Claude only confirms parity |
| owner seed → leadership principal | the first leadership account is set out-of-band (service role), the same trust root as today's is_admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03 | Repudiation | staff_audit_log (live) | high | mitigate | Live check 1 + 4 confirm service-role-only reachability and one-row-per-action logging |
| T-25-04 | Information Disclosure | ae_user_id (live) | medium | mitigate | Live check 2 confirms the column is not exposed to buyer/authenticated clients |
| T-25-08 | Elevation of Privilege | AE cross-org edit (live) | high | mitigate | Live check 3 confirms an unassigned-org PATCH returns 404 |
| T-25-02 | Elevation of Privilege | is_admin fallback (live) | medium | mitigate | A1 decision recorded; fallback grants no new privilege (owner is already full-admin) and is tracked for retirement |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `supabase migration list` shows LOCAL=REMOTE through 086 (human-confirmed)
- Six live behavioral checks pass (human-confirmed)
- `npm test` + `npx tsc --noEmit` + lint green
- REQUIREMENTS.md Phase 25 section present
</verification>

<success_criteria>
- 085/086 live; leadership account reachable; A1 decision recorded
- All deferred live-DB mitigations verified
- TEAM-01..07 registered; full suite green
</success_criteria>

## Artifacts this phase produces (Plan 25-07)

| Artifact | File | Kind |
|----------|------|------|
| Phase 25 requirement registration | .planning/REQUIREMENTS.md | doc (TEAM-01..07) |
| Push + live-smoke record | 25-07-SUMMARY.md | closeout record |

<output>
Create `.planning/phases/25-funun-team-accounts-ae/25-07-SUMMARY.md` when done. Record push confirmation,
the A1 fallback decision, live-smoke results, and requirement registration.
</output>
