---
phase: 25-funun-team-accounts-ae
plan: 10
type: execute
wave: 4
depends_on: [25-03, 25-06]
files_modified:
  - app/(admin)/admin/directory/page.tsx
  - components/admin/TeamDirectory.tsx
autonomous: true
requirements: [TEAM-09]
must_haves:
  truths:
    - "/admin/directory is accessible to EVERY staff role (gate admits getStaffRole(user) !== null — leadership, AE, BD all see it; NOT leadership-only) — the all-roles Team Member Directory"
    - "it lists all Team Members as contact cards: an avatar photo (avatar_url when set, else an initials circle fallback), display_name, role type (Leadership/AE/BD), title, email, and phone when present"
    - "contact actions are real: an 'Email' action is a mailto: link to the member's email (opens the viewer's mail client); a 'Call' tel: link renders only when phone is set"
    - "the Directory offers TWO view modes via a client-side toggle — CARD view (the contact-card grid) and LIST view (a compact table: avatar · name · role · title · email · phone) — BOTH show the avatar (photo/initials fallback) and carry the same mailto:/tel: actions; the viewer's choice may persist (localStorage) but need not survive server round-trips"
    - "the page reads funun_staff (service client, column-explicit: user_id, staff_role, display_name, title, phone) and attaches each member's email from auth.users (mirror the members page email-join)"
    - "the Directory is READ-ONLY — it never creates, edits, role-changes, or deactivates a Team Member (that stays the leadership-only /admin/team-members from 25-06); no writes, no logStaffAction"
    - "the cards render correctly in both light and dark via the console tokens (25-08) — no hardcoded dark-only colors"
  artifacts:
    - "app/(admin)/admin/directory/page.tsx (any-staff gate + funun_staff read + email join)"
    - "components/admin/TeamDirectory.tsx (contact-card grid + mailto/tel actions)"
  key_links:
    - "reads the SAME funun_staff the leadership page manages, but read-only and for ALL roles — the Directory is the phone book, /admin/team-members is HR management"
    - "the /admin/directory nav link is added for every role by 25-06's role-aware sidebar; this plan builds the page it points to"
    - "email join mirrors app/(admin)/admin/members/page.tsx (per-row email attach via the service client)"
  prohibitions:
    - "MUST NOT allow any management action (create/edit/role-change/deactivate) from the Directory — read-only"
    - "MUST NOT expose fields beyond contact info (name, role, title, email, phone) — no app_metadata, no audit, no assignment internals"
    - "MUST NOT gate the Directory to leadership — it is explicitly for every Team Member"
    - "MUST NOT list non-staff (only funun_staff rows)"
---

<objective>
Give EVERY Team Member a **Team Member Directory** (owner 2026-08-05): a list of all team members with a
**contact card** each — name, role, title, email, phone — and real **contact actions** (email a teammate via
mailto:, call via tel:). This is the internal phone book, distinct from the leadership-only Team Members
*management* page (25-06): the Directory is **all-roles and read-only**.

Requirement: TEAM-09 (Team Member Directory). Uses the funun_staff contact fields (title, phone) added to
migration 089 in 25-03, and the `/admin/directory` nav link added for all roles by 25-06.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-funun-team-accounts-ae/25-CONTEXT.md
@.planning/phases/25-funun-team-accounts-ae/25-03-SUMMARY.md
@.planning/phases/25-funun-team-accounts-ae/25-06-SUMMARY.md
@.planning/phases/25-funun-team-accounts-ae/25-08-SUMMARY.md

# Verified analogs
@app/(admin)/admin/members/page.tsx
@components/admin/console-theme.ts
@lib/admin/gate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: /admin/directory page — any-staff gate + funun_staff read + email join</name>
  <files>app/(admin)/admin/directory/page.tsx</files>
  <read_first>
    - app/(admin)/admin/members/page.tsx (server-component shape: force-dynamic, gate, service-client read, per-row email attach)
    - lib/admin/gate.ts (getStaffRole — the gate admits ANY non-null role here, NOT leadership-only)
    - supabase/migrations/089_funun_staff_and_audit.sql (the funun_staff columns incl. title, phone from 25-03)
  </read_first>
  <action>
    Create app/(admin)/admin/directory/page.tsx as a server component with export const dynamic = 'force-dynamic'
    and a gate that admits ANY staff role: fetch user, if getStaffRole(user) === null redirect('/') (do NOT
    require leadership). Read funun_staff via the service client, column-explicit (user_id, staff_role,
    display_name, title, phone, avatar_url), ordered by display_name; attach each row's email from auth.users using the
    admin API per-user lookup exactly like the members page. Map to a plain view-model array and render
    components/admin/TeamDirectory.tsx. No writes, no logStaffAction — this is a read-only surface.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Gate admits leadership, AE, and BD (any non-null role); redirects non-staff
    - Reads funun_staff column-explicit + attaches email; no write calls in the file
    - tsc clean
  </acceptance_criteria>
  <done>The all-roles Directory page reads staff contact data server-side and renders the grid.</done>
</task>

<task type="auto">
  <name>Task 2: TeamDirectory component — contact-card grid + contact actions</name>
  <files>components/admin/TeamDirectory.tsx</files>
  <read_first>
    - components/admin/console-theme.ts (the console tokens to style through, for light + dark)
    - components/admin/StaffAdmin.tsx (existing admin component style/classes to stay consistent with)
  </read_first>
  <action>
    Create components/admin/TeamDirectory.tsx ('use client' — it holds view-mode + search state) rendering the
    directory in TWO switchable views behind a small segmented control (Cards / List; default Cards, persisted to
    localStorage). CARD view: a responsive grid of contact cards — initials/avatar, display_name, role-type pill
    (Leadership/AE/BD), title (when present), and contact actions — "Email" as `<a href={`mailto:${email}`}>` and,
    when phone is present, "Call" as `<a href={`tel:${phone}`}>`. Render the avatar as an `<img>` of avatar_url when
    set, else an initials circle (the avatar-with-initials-fallback pattern used across the app). LIST view: a compact
    table with a small avatar + the same fields (avatar · name · role · title · email · phone) and the same
    mailto:/tel: actions per row. Setting a member's photo reuses components/profile/AvatarBannerUpload.tsx
    (upload → avatar_url), populated at create (25-04) or a self-edit fast-follow — this plan DISPLAYS it. Add a simple client-side
    search box filtering by name/title/role, shared across both views. Style everything through the console tokens
    (var(--c-…)) so it reads in both light and dark; follow project style (no semicolons, named exports, `@/`
    imports). Contact actions are plain links — no backend, no new endpoint.

    NOTE (future, do NOT build now): a third "By team" grouping/filter view is planned once internal team/department
    structure exists — leave the view toggle easy to extend, but ship only Cards + List in this plan.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Each card renders name, role pill, title (if set), a mailto: Email link, and a tel: Call link only when phone is set
    - Colors come from console tokens (grep finds no hardcoded dark-only hex); tsc + build clean
  </acceptance_criteria>
  <done>The contact-card directory renders with working email/call links, themed for light + dark.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any Team Member → Directory | every staff role may read the directory; the gate admits any non-null staff role and no one else |
| Directory read → funun_staff + auth email | server-side service-client read exposes ONLY contact fields (name, role, title, email, phone) to staff |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-18 | Information Disclosure | non-staff reaching the directory | medium | mitigate | Page gate redirects any user whose getStaffRole is null before any read; only staff see staff contacts |
| T-25-19 | Information Disclosure | over-exposing fields (app_metadata, assignments, audit) | low | mitigate | The page selects only contact columns + email; no app_metadata, assignment, or audit data is passed to the client |
| T-25-20 | Elevation of Privilege | a management action smuggled into the Directory | low | mitigate | Directory is strictly read-only — no write path, no route mutations; management stays on the leadership-only page |
| T-25-SC | Tampering | npm/pip installs | low | accept | No new dependencies — mailto:/tel: are native links |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds
- Live (25-07 checkpoint or a dev run): an AE and a BD both reach /admin/directory (not just leadership), see every teammate's card, and the Email link opens a mail draft to that teammate; phone shows a Call link only when set
- grep confirms no hardcoded dark-only hex in TeamDirectory
</verification>

<success_criteria>
- Every Team Member (any role) can open the Directory, see all teammates as contact cards, and email/call them
- Read-only — no management actions; contact fields only
- Themed for light + dark; no new dependencies or endpoints
</success_criteria>

## Artifacts this phase produces (Plan 25-10)

| Route / Component | File | Kind |
|-------------------|------|------|
| `/admin/directory` (all-roles) | app/(admin)/admin/directory/page.tsx | page |
| `TeamDirectory` | components/admin/TeamDirectory.tsx | component (contact cards) |

<output>
Create `.planning/phases/25-funun-team-accounts-ae/25-10-SUMMARY.md` when done.
</output>
