---
phase: 25-funun-team-accounts-ae
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/admin/gate.ts
  - lib/admin/gate.test.ts
  - lib/staff/scope.ts
  - lib/staff/scope.test.ts
autonomous: true
requirements: [TEAM-01]
must_haves:
  truths:
    - "getStaffRole(user) returns 'leadership' | 'ae' | 'bd' for the matching app_metadata.staff_role, and null for any other value"
    - "getStaffRole treats app_metadata.is_admin === true as 'leadership' (backward-compat fallback for the owner bootstrap account, per D-02/A1)"
    - "requireStaff(allowed) returns { error:'Unauthorized', status:401 } for no session, { error:'Forbidden', status:403 } for a session whose role is not in allowed, and { user, staffRole } on success"
    - "verifyAdmin() still returns the exact same discriminated union it does today and now resolves to requireStaff(['leadership']) so all ~15 existing /api/admin/* callers keep working unchanged"
    - "isAssignedToOrg(org, staffUserId) is a pure, fail-closed predicate that returns true only when org.ae_user_id === staffUserId, false for null/undefined/mismatch, and never throws"
  artifacts:
    - "lib/admin/gate.ts exports StaffRole type, getStaffRole(), requireStaff(), verifyAdmin() (preserved)"
    - "lib/staff/scope.ts exports isAssignedToOrg() pure predicate"
    - "lib/admin/gate.test.ts and lib/staff/scope.test.ts green"
  key_links:
    - "requireStaff() is the single authority every staff route calls before createServiceClient() — no parallel auth path (D-01)"
    - "verifyAdmin alias binding — breaking it silently 403s every existing admin route"
  prohibitions:
    - "MUST NOT introduce a per-capability boolean matrix (can_create_buyer etc.) — role implies capability set (D-03)"
    - "MUST NOT touch EDITABLE_FIELDS / SECTION_VALUES / ACTION_TYPE_VALUES / KEY_REGEX in gate.ts — they belong to the unrelated checklist feature (D-01)"
    - "MUST NOT create an RLS policy keyed to auth.jwt()->'app_metadata' — that is the forbidden parallel auth path (D-01)"
---

<objective>
Generalize the existing binary admin gate into the phase's single authorization authority and add the
assignment-scope predicate. This is the foundation every Wave 2 route depends on.

Decision IDs used below map to CONTEXT.md "Locked defaults": D-01 = generalize is_admin → staff role
(extend gate.ts, keep verifyAdmin alias, no parallel auth path); D-02 = bootstrap seeded like is_admin;
D-03 = role-level + assignment-scoped; D-04 = audit staff writes.

Provisional requirement TEAM-01 (Staff role gate). No Phase 25 IDs are registered in REQUIREMENTS.md yet —
register TEAM-01..TEAM-07 via `/gsd-docs-update` (see 25-07). Do NOT invent a REQUIREMENTS.md entry inline.

Purpose: Widen an existing primitive rather than invent a new one (RESEARCH primary finding). Everything
else in this phase gates on requireStaff() and isAssignedToOrg().
Output: getStaffRole/requireStaff/StaffRole in lib/admin/gate.ts, isAssignedToOrg in lib/staff/scope.ts,
plus their Nyquist unit-test scaffolds (this is the Wave-0 test scaffolding the phase requires).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-funun-team-accounts-ae/25-CONTEXT.md
@.planning/phases/25-funun-team-accounts-ae/25-PATTERNS.md

# Authoritative analogs (verified in 25-PATTERNS.md — extend the REAL files, not RESEARCH.md's imagined gate)
@lib/admin/gate.ts
@lib/buyers/permissions.ts
@lib/buyers/permissions.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize lib/admin/gate.ts into getStaffRole / requireStaff (keep verifyAdmin alias)</name>
  <files>lib/admin/gate.ts, lib/admin/gate.test.ts</files>
  <read_first>
    - lib/admin/gate.ts (current 56-line file — extend in place; lines 30-56 are the unrelated checklist
      constants, leave byte-identical)
    - 25-PATTERNS.md section "lib/admin/gate.ts (MODIFY IN PLACE)" — the return-type-discipline note and the
      warning NOT to paste RESEARCH.md's fabricated code block
    - lib/buyers/permissions.test.ts (test structure to mirror: fixtures at top, explicit fail-closed block)
  </read_first>
  <behavior>
    - getStaffRole({ app_metadata: { staff_role: 'leadership' } }) returns 'leadership'
    - getStaffRole returns 'ae' and 'bd' for those exact staff_role values
    - getStaffRole({ app_metadata: { is_admin: true } }) returns 'leadership' (fallback, D-02/A1)
    - getStaffRole({ app_metadata: { staff_role: 'superadmin' } }) returns null (unrecognized string)
    - getStaffRole({}) and getStaffRole({ app_metadata: undefined }) return null, never throw
    - staff_role wins over is_admin when both are present (explicit role is authoritative)
    - requireStaff denial paths return the { error, status } union (401 no user, 403 role-not-allowed);
      success returns { user, staffRole }
    - verifyAdmin() success still returns { user } (unchanged public shape for existing callers)
  </behavior>
  <action>
    Add to lib/admin/gate.ts, alongside the existing verifyAdmin(): a StaffRole type union
    ('leadership'|'ae'|'bd'); a module-level ALL_STAFF_ROLES array; a pure getStaffRole(user) that reads
    app_metadata.staff_role, returns it when it matches the closed union, else falls back to 'leadership'
    when app_metadata.is_admin === true (D-02/A1 — the owner bootstrap account is not locked out on deploy),
    else returns null. Add an async requireStaff(allowed = ALL_STAFF_ROLES) that mirrors verifyAdmin's
    createApiClient()+getUser() flow and returns the same discriminated-union shape extended with staffRole:
    { error:'Unauthorized', status:401 } when no user, { error:'Forbidden', status:403 } when
    getStaffRole is null or not in allowed, else { user, staffRole }. Re-point verifyAdmin() to return
    requireStaff(['leadership']) narrowed to its existing { user } | { error, status } shape so the ~15
    current callers need zero changes (per D-01, single authority, no parallel path). Follow project style:
    named exports, no semicolons, 2-space indent, `@/` imports. Do NOT modify EDITABLE_FIELDS or the other
    checklist constants below it. Add lib/admin/gate.test.ts covering the behavior bullets, mirroring
    permissions.test.ts structure (typed fixtures, one explicit fail-closed/never-throws block).
  </action>
  <verify>
    <automated>npx jest lib/admin/gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/admin/gate.test.ts passes; every behavior bullet has a corresponding assertion
    - `npx tsc --noEmit` clean (verifyAdmin's public return type unchanged)
    - grep confirms EDITABLE_FIELDS / SECTION_VALUES / ACTION_TYPE_VALUES / KEY_REGEX are still present and unchanged
  </acceptance_criteria>
  <done>getStaffRole/requireStaff/StaffRole exported; verifyAdmin preserved as leadership alias; tests green; checklist constants untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add isAssignedToOrg pure scope predicate in lib/staff/scope.ts</name>
  <files>lib/staff/scope.ts, lib/staff/scope.test.ts</files>
  <read_first>
    - lib/buyers/permissions.ts (pure, fail-closed, zero-I/O predicate discipline to copy exactly)
    - lib/buyers/permissions.test.ts (test shape)
    - 25-PATTERNS.md section "lib/staff/scope.ts (NEW)" — note it may expose BOTH a pure row-predicate and
      an optional I/O variant; this task ships the pure row-predicate (the I/O route-layer check is written
      inline in 25-05's route where the org row is fetched)
  </read_first>
  <behavior>
    - isAssignedToOrg({ ae_user_id: 'u1' }, 'u1') returns true
    - isAssignedToOrg({ ae_user_id: 'u2' }, 'u1') returns false
    - isAssignedToOrg({ ae_user_id: null }, 'u1') returns false
    - isAssignedToOrg(null, 'u1') and isAssignedToOrg(undefined, 'u1') return false, never throw
    - isAssignedToOrg({ ae_user_id: 'u1' }, '') returns false (empty caller id never matches)
  </behavior>
  <action>
    Create lib/staff/scope.ts exporting a pure isAssignedToOrg(org, staffUserId) predicate that takes an
    already-fetched org row shaped as Pick<{ ae_user_id: string | null }, ...> | null | undefined and a
    caller user id string, returning org?.ae_user_id === staffUserId with a guard that a falsy/empty
    staffUserId never matches. No Supabase client, no I/O, no throw — mirror lib/buyers/permissions.ts's
    fail-closed convention exactly (D-03 assignment-scoping). Define the org row type locally
    (ae_user_id: string | null) rather than importing a heavy type. Add lib/staff/scope.test.ts covering
    the behavior bullets, including the explicit never-throws block from permissions.test.ts.
  </action>
  <verify>
    <automated>npx jest lib/staff/scope.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/staff/scope.test.ts passes with a never-throws fail-closed assertion for null/undefined
    - `npx tsc --noEmit` clean
    - Function performs no I/O (no import of a Supabase client)
  </acceptance_criteria>
  <done>isAssignedToOrg exported, pure and fail-closed; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session JWT → server gate | app_metadata.staff_role/is_admin is readable in the session JWT but not user-editable; the server must re-derive the role, never trust a client-asserted role |
| caller identity → assignment scope | staffUserId supplied to isAssignedToOrg must be the server-verified user id, never a request-body field |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Elevation of Privilege | requireStaff role check | high | mitigate | Closed StaffRole enum; role derived server-side from app_metadata via getStaffRole; denial returns 403; requireStaff is the single authority every route calls before any service-role client (D-01) |
| T-25-02 | Elevation of Privilege | is_admin → leadership fallback | medium | mitigate | Fallback only maps an already-full-admin owner-seeded is_admin===true to leadership — grants no NEW privilege; tracked as a Pending Todo to retire once the leadership account carries an explicit staff_role (A1) |
| T-25-13 | Spoofing | client-asserted role | low | mitigate | getStaffRole reads only server-side app_metadata; role is used to decide what to request, never trusted as sensitive-data render authority |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx jest lib/admin/gate.test.ts lib/staff/scope.test.ts` green
- `npx tsc --noEmit` clean
- Manual grep: every existing `/api/admin/*` route still imports and calls verifyAdmin() with no signature change
</verification>

<success_criteria>
- requireStaff/getStaffRole/StaffRole and isAssignedToOrg exist, exported, unit-tested
- verifyAdmin behavior byte-compatible for existing callers
- No RLS policy, no per-capability matrix, no parallel auth path introduced
</success_criteria>

## Artifacts this phase produces (Plan 25-01)

| Symbol | File | Kind |
|--------|------|------|
| `StaffRole` | lib/admin/gate.ts | type ('leadership'\|'ae'\|'bd') |
| `getStaffRole(user)` | lib/admin/gate.ts | pure function |
| `requireStaff(allowed?)` | lib/admin/gate.ts | async gate |
| `verifyAdmin()` | lib/admin/gate.ts | preserved leadership alias |
| `isAssignedToOrg(org, staffUserId)` | lib/staff/scope.ts | pure predicate |

<output>
Create `.planning/phases/25-funun-team-accounts-ae/25-01-SUMMARY.md` when done
</output>
