---
phase: 25-funun-team-accounts-ae
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/085_funun_staff_and_audit.sql
  - supabase/migrations/086_buyer_orgs_ae_assignment.sql
  - __tests__/migration-085-086.test.ts
autonomous: true
requirements: [TEAM-02]
must_haves:
  truths:
    - "085 creates funun_staff (id, user_id FK auth.users, staff_role TEXT CHECK IN ('leadership','ae','bd'), display_name, created_at) as a zero-RLS-policy, REVOKE-ALL, service-role-only table"
    - "085 creates staff_audit_log (id, actor_id FK auth.users, action, target_type, target_id, changes JSONB, created_at) with the same zero-RLS/REVOKE-ALL posture as verification_audit_log (migration 058)"
    - "086 adds buyer_orgs.ae_user_id UUID REFERENCES auth.users ON DELETE SET NULL, nullable, with an index"
    - "086 deliberately does NOT extend migration 080's authenticated GRANT SELECT allowlist — ae_user_id stays private/staff-only (Pitfall 2), documented in the column comment"
  artifacts:
    - "supabase/migrations/085_funun_staff_and_audit.sql"
    - "supabase/migrations/086_buyer_orgs_ae_assignment.sql"
    - "__tests__/migration-085-086.test.ts (static migration-text assertions, green pre-push)"
  key_links:
    - "funun_staff.staff_role is a DISPLAY copy; app_metadata.staff_role remains authoritative for the gate (Pitfall 1) — any role change must write both"
    - "ae_user_id absence from the authenticated GRANT list is the intended lockdown, not an oversight"
  prohibitions:
    - "MUST NOT run `supabase db push` — migrations are HUMAN-GATED in this project; the push is the blocking checkpoint in 25-07"
    - "MUST NOT add ae_user_id to buyer_orgs' GRANT SELECT (id, name, is_personal, verified, created_at) list"
    - "MUST NOT create any RLS policy on funun_staff / staff_audit_log (zero-policy + REVOKE-ALL is the design)"
---

<objective>
Author the two additive migrations this phase needs — the staff/audit tables and the AE-assignment column —
using the codebase's proven zero-RLS-policy and column-privilege patterns. Files only: the push is
human-gated and happens in 25-07.

Decision IDs: D-04 = audit staff writes (reuse verification_audit_log's exact shape); D-03 = assignment
storage (single nullable ae_user_id column, not a join table — A2); D-01 = staff_role stored on app_metadata
with funun_staff as a display/profile companion (A4). Provisional requirement TEAM-02 (staff schema).

Purpose: Schema for staff identity, the audit trail, and AE↔company assignment — all service-role-only,
no new client-reachable surface, no new RLS on the buyer tables (RESEARCH primary finding).
Output: migrations 085 + 086 + a static migration-text test that gives an automated pre-push verify.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-funun-team-accounts-ae/25-PATTERNS.md

# Verified analogs (read the exact line ranges 25-PATTERNS.md cites)
@supabase/migrations/058_trust_safety_schema.sql
@supabase/migrations/080_buyer_orgs_members.sql
@supabase/migrations/081_license_requests_deals.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration 085 — funun_staff + staff_audit_log (zero-RLS, service-role-only)</name>
  <files>supabase/migrations/085_funun_staff_and_audit.sql</files>
  <read_first>
    - supabase/migrations/058_trust_safety_schema.sql (verification_audit_log block: CREATE TABLE, index,
      ENABLE ROW LEVEL SECURITY, the zero-policy reasoning comment, REVOKE from authenticated/anon — reapply verbatim shape)
    - 25-PATTERNS.md section "085_funun_staff_and_audit.sql" for the exact column lists
  </read_first>
  <action>
    Create supabase/migrations/085_funun_staff_and_audit.sql defining two tables, each following migration
    058's verification_audit_log shape exactly: (1) funun_staff — id UUID PK, user_id UUID REFERENCES
    auth.users ON DELETE CASCADE, staff_role TEXT NOT NULL with a CHECK constraint restricting it to the
    three staff roles, display_name TEXT, created_at TIMESTAMPTZ default now(); (2) staff_audit_log — id
    UUID PK, actor_id UUID REFERENCES auth.users ON DELETE SET NULL, action TEXT NOT NULL, target_type TEXT
    NOT NULL, target_id UUID, changes JSONB, created_at TIMESTAMPTZ default now(). For BOTH tables: add the
    appropriate indexes (funun_staff on user_id; staff_audit_log on (actor_id, created_at DESC) and
    (target_type, target_id, created_at DESC)), ENABLE ROW LEVEL SECURITY, create ZERO policies, and REVOKE
    SELECT/INSERT/UPDATE/DELETE from authenticated and anon — reachable only via the service role (D-04 /
    A4). Add a COMMENT on each table stating the service-role-only intent and that funun_staff.staff_role is
    a display copy of the authoritative app_metadata.staff_role (Pitfall 1). End with NOTIFY pgrst, 'reload
    schema'. Do NOT run the migration.
  </action>
  <verify>
    <automated>npx jest __tests__/migration-085-086.test.ts -t "085"</automated>
  </verify>
  <acceptance_criteria>
    - File contains both CREATE TABLE statements with the CHECK-constrained staff_role
    - Both tables have ENABLE ROW LEVEL SECURITY, zero CREATE POLICY statements, and a REVOKE from authenticated, anon
    - A table COMMENT documents funun_staff.staff_role as a display copy (app_metadata authoritative)
  </acceptance_criteria>
  <done>Migration 085 authored with the verified zero-RLS/REVOKE-ALL pattern; not pushed.</done>
</task>

<task type="auto">
  <name>Task 2: migration 086 — buyer_orgs.ae_user_id private assignment column</name>
  <files>supabase/migrations/086_buyer_orgs_ae_assignment.sql</files>
  <read_first>
    - supabase/migrations/081_license_requests_deals.sql (owner_id single nullable FK column precedent)
    - supabase/migrations/080_buyer_orgs_members.sql (the REVOKE/GRANT column-privilege posture to PRESERVE, not extend)
    - 25-PATTERNS.md section "086_buyer_orgs_ae_assignment.sql" (Pitfall 2 explanation)
  </read_first>
  <action>
    Create supabase/migrations/086_buyer_orgs_ae_assignment.sql that ALTER TABLE public.buyer_orgs ADD
    COLUMN ae_user_id UUID REFERENCES auth.users ON DELETE SET NULL (nullable, additive, no backfill —
    every existing org starts unassigned). Add CREATE INDEX idx_buyer_orgs_ae_user_id on (ae_user_id). Add
    a COMMENT ON COLUMN explaining this is the assigned AE (one per company, nullable until leadership sets
    it) and that it is DELIBERATELY kept out of migration 080's authenticated GRANT SELECT allowlist — a
    private/staff-only column mirroring how verified_at/created_by stay private (Pitfall 2, D-03). Do NOT
    add ae_user_id to any GRANT SELECT statement. End with NOTIFY pgrst, 'reload schema'. Do NOT run the migration.
  </action>
  <verify>
    <automated>npx jest __tests__/migration-085-086.test.ts -t "086"</automated>
  </verify>
  <acceptance_criteria>
    - File adds a nullable ae_user_id FK column with ON DELETE SET NULL and an index
    - The column comment documents the deliberate omission from the authenticated GRANT list
    - No GRANT SELECT statement in this file names the new column
  </acceptance_criteria>
  <done>Migration 086 authored, ae_user_id private-by-default; not pushed.</done>
</task>

<task type="auto">
  <name>Task 3: static migration-text assertions (__tests__/migration-085-086.test.ts)</name>
  <files>__tests__/migration-085-086.test.ts</files>
  <read_first>
    - An existing migration-content test in __tests__/ (e.g. a migration-0NN.test.ts) for the file-read +
      string-assertion convention this repo uses (Jest cannot execute PL/pgSQL, so these assert against the
      literal migration text)
  </read_first>
  <action>
    Create __tests__/migration-085-086.test.ts that reads each migration file from disk and asserts against
    its text. For 085: both tables present, staff_role CHECK restricts to the three roles, both tables
    ENABLE ROW LEVEL SECURITY, neither table has a CREATE POLICY, and both REVOKE from authenticated and
    anon. For 086: an ADD COLUMN ae_user_id with ON DELETE SET NULL is present; and — region-scoped to the
    lines that begin with GRANT SELECT — assert none of those GRANT lines mention the new column (extract
    the GRANT SELECT lines first, then assert the column name is absent from that extracted subset, so the
    check does not false-fail on the legitimate ADD COLUMN / index / comment occurrences). Group with
    describe blocks named "085" and "086" so the `-t` filter in each authoring task works.
  </action>
  <verify>
    <automated>npx jest __tests__/migration-085-086.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test green against the authored 085 + 086 files
    - The 086 GRANT-line assertion is region-scoped to GRANT SELECT lines (not a file-wide search)
    - describe blocks "085" and "086" exist for `-t` filtering
  </acceptance_criteria>
  <done>Migration-text test green; provides an automated pre-push gate for both migrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated/anon client → funun_staff & staff_audit_log | zero policies + REVOKE-ALL make these tables unreachable by any client; service role only |
| authenticated/anon client → buyer_orgs.ae_user_id | column omitted from the GRANT SELECT allowlist → private/staff-only despite row-level buyer_orgs SELECT being granted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03 | Repudiation | staff_audit_log tampering/bypass | high | mitigate | RLS-enabled + zero policies + REVOKE-ALL — client writes/reads structurally impossible (mirrors migration 058) |
| T-25-04 | Information Disclosure | ae_user_id leak to buyers | medium | mitigate | Column deliberately excluded from migration 080's authenticated GRANT SELECT list (Pitfall 2); documented in the column comment |
| T-25-16 | Elevation of Privilege | funun_staff self-write | high | mitigate | Zero-policy + REVOKE-ALL — a client cannot INSERT/UPDATE its own funun_staff row to grant itself a role; role changes go only through service-role routes (25-04) |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx jest __tests__/migration-085-086.test.ts` green (static text assertions)
- Migration NOT pushed — live verification is gated behind 25-07's human push checkpoint
</verification>

<success_criteria>
- 085 + 086 authored with the verified patterns; migration-text test green
- ae_user_id private-by-default; funun_staff + staff_audit_log service-role-only
- No `supabase db push` executed by the agent
</success_criteria>

## Artifacts this phase produces (Plan 25-03)

| Object | File | Kind |
|--------|------|------|
| `funun_staff` table | 085_funun_staff_and_audit.sql | migration DDL (zero-RLS) |
| `staff_audit_log` table | 085_funun_staff_and_audit.sql | migration DDL (zero-RLS) |
| `buyer_orgs.ae_user_id` column | 086_buyer_orgs_ae_assignment.sql | migration DDL (private column) |

<output>
Create `.planning/phases/25-funun-team-accounts-ae/25-03-SUMMARY.md` when done. Record in the SUMMARY that
migrations 085/086 are authored-but-unpushed and blocked on the 25-07 human push checkpoint.
</output>
