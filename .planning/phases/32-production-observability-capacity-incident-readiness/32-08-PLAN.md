---
phase: 32-production-observability-capacity-incident-readiness
plan: 08
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - docs/observability/THRESHOLDS-AND-SEVERITY.md
autonomous: true
requirements: [R8]

must_haves:
  truths:
    - "A thresholds table shows proposed vs. baseline-adjusted values for Vercel 5xx, function throttles, dynamic-route p95, Supabase CPU, DB connections, disk, Auth/API 5xx, and external-uptime consecutive failures, sourced from lib/observability/config.ts THRESHOLDS (R8)."
    - "No-baseline thresholds are explicitly marked provisional (baseline-adjusted column pending Plan 09's k6 baseline); a no-data metric is 'unknown', never silently 'healthy' (R8 empty edge)."
    - "The SEV-1..SEV-4 table has all six fields per severity: notification channel, primary owner, backup owner, acknowledgement expectation, escalation rule, resolution criteria (R8)."
    - "Threshold bands are non-overlapping and a value exactly at a threshold (and one step either side) resolves to one documented severity, matching classifyThreshold's rule; p50/p95/p99 + rate thresholds use a documented window + rounding (R8 boundary/adjacency/precision edges)."
  artifacts:
    - "docs/observability/THRESHOLDS-AND-SEVERITY.md"
  key_links:
    - "The doc's numeric thresholds are the human-readable projection of lib/observability/config.ts THRESHOLDS + classifyThreshold (Plan 01) — single source of truth, no parallel numbers."
    - "Primary owner = Pete (D-13); backup owner field present but marked TBD (no dedicated backup yet, added later via the config layer)."
  prohibitions:
    - statement: "MUST NOT let threshold bands overlap or leave a boundary value's severity ambiguous."
      status: resolved
      verification: "test/judgment — bands mirror config.ts (warning < critical asserted in Plan 01); the doc states the exact-boundary resolution rule."
---

<objective>
Author the R8 alert-thresholds table and the SEV-1..4 severity model documentation. The thresholds table shows proposed vs. baseline-adjusted values for every monitored signal (seeded from the SPEC's proposed values, mirroring `lib/observability/config.ts` `THRESHOLDS`), with the baseline-adjusted column marked provisional until Plan 09's k6 baseline lands. The SEV-1..4 table gives all six fields per severity (channel, primary owner, backup owner, ack expectation, escalation, resolution), naming Pete as primary owner (D-13) with the backup field present-but-TBD.

Purpose: the human-readable severity policy the incident runbook (Plan 10) and the operating rhythm consume. Depends on Plan 01 so the doc's numbers project the config layer's single source of truth rather than inventing parallel values. Thresholds are provisional-until-baseline (SPEC empty edge) so this plan does not block on Plan 09.
Output: `docs/observability/THRESHOLDS-AND-SEVERITY.md`.
</objective>

## Artifacts this phase produces (Plan 08)

- `docs/observability/THRESHOLDS-AND-SEVERITY.md` — proposed-vs-baseline thresholds table + SEV-1..4 model (six fields per severity) + the exact-boundary resolution rule.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-SPEC.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-CONTEXT.md
@lib/observability/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thresholds + SEV-1..4 model documentation</name>
  <files>docs/observability/THRESHOLDS-AND-SEVERITY.md</files>
  <read_first>
    - lib/observability/config.ts (THRESHOLDS, SEVERITY_LABELS/VALUES, classifyThreshold, budget constants — the numeric source of truth)
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-SPEC.md (R8 target/acceptance; Edge Coverage R8 boundary/adjacency/empty/precision rows)
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-CONTEXT.md (D-13 owner=Pete, no backup yet; D-08 destinations extensible; D-14/D-15 budgets)
  </read_first>
  <action>Write docs/observability/THRESHOLDS-AND-SEVERITY.md with two sections. Section 1 — a thresholds table with columns: signal (Vercel 5xx rate, function throttles/FUNCTION_THROTTLED, dynamic-route p95, Supabase CPU, DB connections, disk utilization, Auth/API 5xx, external-uptime consecutive failures, monthly spend), proposed warning, proposed critical, baseline-adjusted (marked "provisional — pending Plan 09 k6 baseline"), measurement window + rounding rule (for rate/percentile metrics), and the read-location. State that these numbers are the human projection of lib/observability/config.ts THRESHOLDS (do not invent parallel values) and that classifyThreshold resolves a value exactly at warning to warning, a between-value to the lower (warning) band, a value at/above critical to critical, and a no-data metric to "unknown" (never silently healthy). Section 2 — the SEV-1..SEV-4 table with all six fields per severity: notification channel (email to the config recipient list now, Slack later per D-08), primary owner (Pete, pete@funun.studio, D-13), backup owner (field present, marked TBD — no dedicated backup yet, added later via the config layer per D-13), acknowledgement expectation, escalation rule, and resolution criteria. Map each SEV level to representative threshold breaches (e.g. SEV-1 = production down / FUNCTION_THROTTLED sustained; SEV-2 = critical-band breach; SEV-3 = warning-band breach; SEV-4 = cosmetic). Include the D-15 $100 infra-review trigger and the D-14 ~$50/mo Supabase compute auto-upgrade ceiling as documented, measurable decision points.</action>
  <acceptance_criteria>
    - Source assertion: the thresholds table has a proposed column AND a baseline-adjusted column marked provisional for every signal; the "unknown-on-no-data" and exact-boundary rules are stated.
    - Source assertion: the SEV table has SEV-1..SEV-4 rows each populated with all six fields (channel, primary owner, backup owner, ack, escalation, resolution).
    - Source assertion: the doc cites lib/observability/config.ts as the numeric source of truth and includes the $100 review trigger + $50 compute ceiling.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/observability/THRESHOLDS-AND-SEVERITY.md && grep -qi "provisional" docs/observability/THRESHOLDS-AND-SEVERITY.md && grep -q "SEV-1" docs/observability/THRESHOLDS-AND-SEVERITY.md && grep -q "SEV-4" docs/observability/THRESHOLDS-AND-SEVERITY.md</automated>
  </verify>
  <done>The thresholds table (proposed vs. provisional baseline) and the six-field SEV-1..4 model exist, project config.ts's single source of truth, and state the non-overlapping-band + no-data-unknown rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented policy → operators | The severity model is a policy document consumed by humans; no runtime execution path. Its integrity risk is drift from config.ts, not an attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-13 | Integrity (policy drift) | thresholds doc vs config.ts | low | mitigate | The doc explicitly projects lib/observability/config.ts THRESHOLDS as the single source of truth and marks baseline-adjusted values provisional until Plan 09 — no parallel authoritative numbers. |
</threat_model>

<verification>
- `test -f` + grep gates pass (provisional, SEV-1, SEV-4 present).
- Manual review confirms all six SEV fields populated and thresholds mirror config.ts.
</verification>

<success_criteria>
A thresholds table (proposed vs. provisional baseline) and a complete SEV-1..4 model exist with owners named, non-overlapping bands, and a documented boundary-resolution rule — ready for the runbook and operating rhythm to consume.
</success_criteria>

<output>
Create `.planning/phases/32-production-observability-capacity-incident-readiness/32-08-SUMMARY.md` when done.
</output>
