---
phase: 32-production-observability-capacity-incident-readiness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/observability/VERCEL-ALERTS-RESPONSE.md
  - docs/observability/SUPABASE-HEALTH-REVIEW.md
autonomous: false
requirements: [R1, R2]
user_setup:
  - service: vercel
    why: "R1 usage/spend notifications + FUNCTION_THROTTLED response are configured entirely in the Vercel dashboard (no app code). Plan tier (Hobby vs Pro) is load-bearing and must be confirmed before configuration."
    dashboard_config:
      - task: "Confirm the live plan tier (Hobby vs Pro) in Billing/Usage"
        location: "Vercel Dashboard -> Settings -> Billing / Usage"
      - task: "Enable Usage notifications (50/75/100%); enable Spend Management with a $100 threshold; leave 'Pause production deployment' OFF (D-07); set notification destination to pete@funun.studio; send a forced test notification"
        location: "Vercel Dashboard -> Settings -> Notifications / Spend Management"
  - service: supabase
    why: "R2 is a review-only procedure over Supabase Reports/Advisor (no native alert-config surface assumed); owner walks each metric to its read-location."
    dashboard_config:
      - task: "Confirm whether direct-Postgres (Supavisor pooler) traffic exists vs HTTP/Data-API only"
        location: "Supabase Dashboard -> Database -> Connection pooling / Reports"

must_haves:
  truths:
    - "A written table maps each Vercel warning (5xx, latency, invocations, bandwidth, throttles, usage %, spend) to an owner and a documented operating response; FUNCTION_THROTTLED is documented as an URGENT event with a defined action (R1/R8)."
    - "A forced Vercel test notification is verified received on pete@funun.studio (R1 acceptance)."
    - "A written Supabase review checklist names each metric (DB CPU, memory, connections, disk, IOPS, Auth/API errors+latency, slow/outlier queries, Query Performance Advisor findings), where to read it, and its warning/critical trigger (R2)."
    - "Pooler-utilization items are included ONLY if direct-Postgres traffic is confirmed; otherwise explicitly marked N/A with the reason (R2 acceptance)."
    - "Spend Management's 'Pause production deployment' toggle is left OFF (alerts-only, D-07)."
  artifacts:
    - "docs/observability/VERCEL-ALERTS-RESPONSE.md"
    - "docs/observability/SUPABASE-HEALTH-REVIEW.md"
  key_links:
    - "The Vercel response table's thresholds cite lib/observability/config.ts THRESHOLDS + SEV model (Plan 01/08) as the single source of truth."
    - "The Supabase checklist's compute-upgrade decision points cite SUPABASE_COMPUTE_AUTO_UPGRADE_CEILING_USD (Plan 01, D-14)."
  prohibitions:
    - statement: "MUST NOT automatically pause production, or automatically change Supabase compute, without an explicit owner decision."
      status: resolved
      verification: "judgment — Spend Management pause toggle left OFF (D-07); Supabase compute changes documented as owner-decision points only, never automated."
---

<objective>
Configure and document the two platform-native monitoring surfaces that live entirely in vendor dashboards: Vercel usage/spend/throttle alerting with an owned response (R1), and a Supabase health/capacity review procedure over Reports/Advisor (R2). No Funūn code is written here — these are owner click-through configuration steps verified by a checkpoint plus two written operating documents a dashboard alone cannot produce.

Vercel Observability Plus is skipped initially (D-06) — built-in usage/spend alerts plus Sentry cover R1; revisit only if anomaly detection is wanted.

Purpose: A1 is load-bearing — STATE.md records the Vercel plan as Hobby (2026-07-06) but Spend Management requires Pro. The first task gates all R1 configuration on confirming the live tier so the plan does not assume an unreachable feature. R2 is written as a documented human review procedure (not a code/alert-config task) because Supabase's Reports/Advisor is read-only observability, not an alert-rule engine (RESEARCH Pitfall 5).
Output: `docs/observability/VERCEL-ALERTS-RESPONSE.md` and `docs/observability/SUPABASE-HEALTH-REVIEW.md`, plus verified dashboard configuration.
</objective>

## Artifacts this phase produces (Plan 04)

- `docs/observability/VERCEL-ALERTS-RESPONSE.md` — Vercel signal → owner → response table with FUNCTION_THROTTLED marked urgent.
- `docs/observability/SUPABASE-HEALTH-REVIEW.md` — Supabase metric → read-location → warning/critical trigger checklist, with pooler items N/A-with-reason unless confirmed.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-SPEC.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-CONTEXT.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Checkpoint — confirm Vercel plan tier (A1)</name>
  <action>Pause for human verification. The owner performs the how-to-verify steps below; do not proceed until they reply with the resume-signal.</action>
  <what-built>Nothing yet — this is the load-bearing A1 pre-check. STATE.md records Hobby (2026-07-06); Spend Management + Observability Plus require Pro. Every R1 configuration step below depends on the answer.</what-built>
  <how-to-verify>
    1. Open Vercel Dashboard -> Settings -> Billing (and Usage). Confirm the current plan tier: Hobby or Pro.
    2. Check whether "Spend Management" appears (Pro-only) or is absent/greyed out (Hobby).
    3. Report back: the tier, and whether Spend Management + Usage notifications are available on it.
  </how-to-verify>
  <resume-signal>Type the plan tier ("Hobby" or "Pro") and whether Spend Management is available. If Hobby: the executor will document the $100 heads-up as a manual owner habit / Funūn-built fallback (RESEARCH Open Q#2) rather than assuming a native Spend Management notification.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — configure Vercel usage/spend notifications</name>
  <action>Pause for human verification. The owner performs the how-to-verify steps below; do not proceed until they reply with the resume-signal.</action>
  <what-built>Owner-configured Vercel notifications (contingent on the tier confirmed above).</what-built>
  <how-to-verify>
    1. Enable Usage notifications at 50/75/100% (My Notifications).
    2. If Pro: enable Spend Management, set the threshold to $100 (D-09), and LEAVE "Pause production deployment" OFF (D-07). If Hobby: skip Spend Management and note the $100 heads-up stays a manual owner check.
    3. Set the notification destination to pete@funun.studio (D-08).
    4. Send a forced test notification and confirm it arrives at pete@funun.studio.
  </how-to-verify>
  <resume-signal>Confirm "test notification received" (and Hobby/Pro path taken), or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Vercel response table + Supabase review checklist docs</name>
  <files>docs/observability/VERCEL-ALERTS-RESPONSE.md, docs/observability/SUPABASE-HEALTH-REVIEW.md</files>
  <read_first>
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-SPEC.md (R1 + R2 targets/acceptance; Prohibition row 7)
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-RESEARCH.md (Work Split table rows R1/R2; Pitfall 2 tier gating; Pitfall 5 Supabase alerting gap; Common Pitfall #5 pooler N/A rule)
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-CONTEXT.md (D-07 no auto-pause; D-09 $100; D-13 owner = Pete; D-14 compute ceiling)
    - lib/observability/config.ts (cite THRESHOLDS + SEVERITY as the numeric source of truth once Plan 01 lands)
  </read_first>
  <action>Write docs/observability/VERCEL-ALERTS-RESPONSE.md as a table mapping every Vercel signal (5xx rate, route latency/p95, function invocations, function duration, FUNCTION_THROTTLED, edge requests, bandwidth, usage 50/75/100%, monthly spend) to a column set of: owner (Pete, pete@funun.studio, per D-13), warning/critical trigger (referencing lib/observability/config.ts THRESHOLDS by metric name), and the documented operating response. Mark FUNCTION_THROTTLED as URGENT with a concrete defined action (investigate concurrency ceiling immediately; do not restate Vercel's ~30k Function figure as a simultaneous-user capacity). Record the confirmed plan tier and, if Hobby, the documented $100-heads-up fallback. State explicitly that Spend Management's pause toggle is OFF (D-07). Write docs/observability/SUPABASE-HEALTH-REVIEW.md as a checklist naming each metric (DB CPU, memory, active connections, service connection distribution across PostgREST/Auth/Storage, disk utilization + growth, IOPS, API errors/latency, Auth failures/anomalies, slow/outlier queries, lock contention, long-running queries, Query Performance Advisor findings) with three columns: where to read it (exact Supabase dashboard path), warning/critical trigger, and the compute-upgrade decision point (citing the D-14 ~$50/mo auto-upgrade ceiling). Include pooler-utilization rows ONLY if the A1/pooler checkpoint confirmed direct-Postgres traffic; otherwise mark each pooler item N/A with the reason "HTTP/Data-API-only; no direct-Postgres traffic confirmed." Note that Supabase Reports/Advisor is review-only (no native email-alert config assumed) and instruct the executor/owner to re-check live for a native alert-config surface.</action>
  <acceptance_criteria>
    - Source assertion: VERCEL-ALERTS-RESPONSE.md contains a row for FUNCTION_THROTTLED marked urgent with a defined action, and names an owner + trigger for every listed Vercel signal.
    - Source assertion: SUPABASE-HEALTH-REVIEW.md names a read-location + trigger for every listed metric and either includes pooler rows (if confirmed) or marks them N/A with reason.
    - Source assertion: both docs state the no-auto-pause / no-auto-compute-change posture (D-07) and cite config.ts thresholds/budget constants rather than inventing parallel numbers.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/observability/VERCEL-ALERTS-RESPONSE.md && test -f docs/observability/SUPABASE-HEALTH-REVIEW.md && grep -qi "FUNCTION_THROTTLED" docs/observability/VERCEL-ALERTS-RESPONSE.md && grep -qi "N/A\|pooler" docs/observability/SUPABASE-HEALTH-REVIEW.md</automated>
  </verify>
  <done>Both operating documents exist; every Vercel signal has an owner+response with FUNCTION_THROTTLED urgent; every Supabase metric has a read-location+trigger with pooler items correctly scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner → Vercel/Supabase dashboards | All configuration + review happens in vendor consoles; no Funūn runtime code path exists (SPEC Architectural Responsibility Map). |
| spend event → production availability | An automatic spend-pause could turn a legitimate traffic spike into a self-inflicted outage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-08 | Availability / Cost (self-inflicted outage) | Vercel Spend Management pause toggle | medium | mitigate | Pause toggle left OFF (D-07); notify-only. Documented in VERCEL-ALERTS-RESPONSE.md; no auto-compute-change on the Supabase side either. |
| T-32-09 | Info gap → undetected failure | Supabase pooler blind spot | low | accept | Pooler items marked N/A-with-reason unless direct-Postgres confirmed; SPEC's own design contains the risk. Re-check on confirmation. |
</threat_model>

<verification>
- Owner checkpoints resolved (plan tier confirmed; forced test notification received).
- `test -f` for both docs passes; grep gates pass.
</verification>

<success_criteria>
Vercel usage/spend/throttle alerting is configured with a verified test delivery and a written owned-response table (FUNCTION_THROTTLED urgent); Supabase health review is a documented per-metric procedure with pooler items correctly scoped; no auto-pause/auto-compute posture is preserved.
</success_criteria>

<output>
Create `.planning/phases/32-production-observability-capacity-incident-readiness/32-04-SUMMARY.md` when done.
</output>
