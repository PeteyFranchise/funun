---
phase: 32-production-observability-capacity-incident-readiness
plan: 07
type: execute
wave: 2
depends_on: [32-03]
files_modified:
  - docs/observability/UPTIME-MONITORING.md
autonomous: false
requirements: [R3]
user_setup:
  - service: better-stack
    why: "R3 external uptime monitoring is fully hosted in Better Stack (no Funūn code). An independent monitor is required because a monitor running ON Vercel cannot report a Vercel-wide outage."
    dashboard_config:
      - task: "Create monitors for the 4 routes at the free 3-min interval (D-05); alert after 2-3 consecutive failures; enable the public status page (e.g. status.funun.studio)"
        location: "Better Stack -> Uptime -> Monitors + Status Pages"
      - task: "Configure the /api/health monitor as a status-code check treating 503 (degraded) as down (matches the Plan 03 contract)"
        location: "Better Stack -> Monitor -> Expected status codes"

must_haves:
  truths:
    - "An independent monitor checks https://www.funun.studio/, /signin, /sync/catalog, and /api/health every 1-3 minutes (D-05 documents the deliberate 3-min free-tier relaxation from the SPEC's 1-2 min target, with a pre-launch upgrade note) (R3)."
    - "Alerts require 2-3 consecutive failures; a deliberately failing check produces a delivered alert AFTER the configured consecutive-failure count and NOT on the (N-1)th; a lone failure→success does not alert (R3 adjacency edge)."
    - "An unreachable target (DNS/timeout/zero response) counts as a failure, never 'unknown/skip' (R3 empty edge)."
    - "The doc states WHY external (not Vercel-internal) monitoring is required and records the public status-page URL (R3)."
    - "The /api/health monitor is configured so the 503 degraded contract registers as down (status-code check aligned with Plan 03)."
  artifacts:
    - "docs/observability/UPTIME-MONITORING.md"
  key_links:
    - "Better Stack polls /api/health (Plan 03) as its 4th route; the 503 degraded contract is the down signal."
    - "Consecutive-failure + destination config reference the SEV model + recipient list (Plan 01/08)."
  prohibitions:
    - statement: "MUST NOT rely solely on Vercel-internal monitoring, which cannot report its own outage."
      status: resolved
      verification: "judgment — an independent hosted monitor (Better Stack) is used; the doc states the rationale."
---

<objective>
Stand up the independent external uptime monitor (R3): Better Stack (D-05) polling the four production routes (`/`, `/signin`, `/sync/catalog`, `/api/health`) on the free 3-minute tier with a 2-3 consecutive-failure alert threshold and a public status page, plus a short written note on why external (not Vercel-internal) monitoring is required. All monitoring logic is hosted — no Funūn code — so this is an owner configuration plan verified by a forced-failure test, plus one documentation deliverable.

Purpose: a monitor running on Vercel cannot detect a Vercel-wide outage; R3 exists specifically to observe production from outside Funūn's infrastructure. Depends on Plan 03 so the 4th route (`/api/health`) exists with its documented 503 degraded contract.
Output: verified Better Stack monitors + status page, and `docs/observability/UPTIME-MONITORING.md`.
</objective>

## Artifacts this phase produces (Plan 07)

- `docs/observability/UPTIME-MONITORING.md` — the "why external" rationale, the 4 monitored routes + interval + consecutive-failure config, the /api/health 503-as-down decision, the public status-page URL, and the pre-launch 1-min-tier upgrade trigger (D-05).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-SPEC.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-CONTEXT.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Checkpoint — create Better Stack monitors + status page</name>
  <action>Pause for human verification. The owner performs the how-to-verify steps below; do not proceed until they reply with the resume-signal.</action>
  <what-built>Owner-created Better Stack monitors + public status page (fully hosted; no code).</what-built>
  <how-to-verify>
    1. Create a Better Stack account and monitors for https://www.funun.studio/, /signin, /sync/catalog, and /api/health at the free 3-min interval (D-05).
    2. Set alerting to require 2-3 consecutive failures; destination = pete@funun.studio.
    3. Configure the /api/health monitor as a status-code check treating 200 as up and 503 (degraded) as down (Plan 03 contract).
    4. Enable the public status page (e.g. status.funun.studio) and record its URL.
    5. Run a deliberately failing check (e.g. point a scratch monitor at a guaranteed-404 path) and confirm an alert is delivered ONLY after the Nth consecutive failure (not the N-1th), and that a lone failure→recovery does NOT alert.
  </how-to-verify>
  <resume-signal>Confirm "monitors live on 4 routes; forced-failure alert fired on the Nth failure; status page URL = ___" or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Uptime monitoring documentation</name>
  <files>docs/observability/UPTIME-MONITORING.md</files>
  <read_first>
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-SPEC.md (R3 target/acceptance; Edge Coverage R3 adjacency + empty rows)
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-CONTEXT.md (D-05 Better Stack free 3-min → paid 1-min pre-launch; public status page)
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-RESEARCH.md ("Don't Hand-Roll" external-reachability row — why a Vercel cron cannot detect a Vercel outage)
    - app/api/health/route.ts (the 503 degraded status contract the monitor keys on)
  </read_first>
  <action>Write docs/observability/UPTIME-MONITORING.md documenting: the four monitored routes and their check interval (free 3-min tier, with the explicit note that this is a deliberate relaxation of the SPEC's 1-2 min target and the pre-launch trigger to upgrade to the ~$25/mo 1-min tier before a major launch/invite batch, D-05); the 2-3 consecutive-failure alert rule and that a single failure→recovery does not alert (R3 adjacency); that an unreachable target counts as a failure, never unknown/skip (R3 empty); the decision that /api/health's 503 degraded response is treated as down via a status-code check (cross-referencing the Plan 03 contract); the recorded public status-page URL; and a short "why external, not Vercel-internal" rationale (a monitor hosted on Vercel cannot report a Vercel-wide outage — SPEC requires stating this). Reference the recipient/SEV config (Plan 01) rather than restating a destination address.</action>
  <acceptance_criteria>
    - Source assertion: the doc lists all four routes, the interval + consecutive-failure rule, the 503-as-down decision, the status-page URL, and the "why external" rationale.
    - `test -f docs/observability/UPTIME-MONITORING.md` and it contains "why external" (or equivalent rationale) and "/api/health".
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/observability/UPTIME-MONITORING.md && grep -qi "external" docs/observability/UPTIME-MONITORING.md && grep -q "/api/health" docs/observability/UPTIME-MONITORING.md</automated>
  </verify>
  <done>The external monitor is documented with its routes, interval, consecutive-failure rule, 503-as-down decision, status-page URL, and the external-monitoring rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Better Stack (external) → production routes | Unauthenticated polling from outside Funūn's infra; the same public surface any visitor reaches. No new Funūn attack surface introduced (observed-from-outside). |
| status page → public | The public status page exposes up/down state to buyers; must not surface internal error detail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-11 | Information Disclosure | public status page | low | mitigate | Status page shows up/down + response-time only; /api/health body is already minimal + secret-safe (Plan 03), so a status probe reveals nothing sensitive. |
| T-32-12 | Detection gap (single-provider blind spot) | monitoring topology | medium | mitigate | An independent hosted monitor (not Vercel-internal) so a Vercel-wide outage is still detectable; rationale documented (R3). |
</threat_model>

<verification>
- Owner checkpoint resolved (4 monitors live, forced-failure alert fired on the Nth failure, status page URL recorded).
- `test -f` + grep gates on the doc pass.
</verification>

<success_criteria>
An independent uptime monitor watches the four production routes with a verified consecutive-failure alert and a public status page, and the external-monitoring rationale + 503-as-down decision are documented.
</success_criteria>

<output>
Create `.planning/phases/32-production-observability-capacity-incident-readiness/32-07-SUMMARY.md` when done.
</output>
