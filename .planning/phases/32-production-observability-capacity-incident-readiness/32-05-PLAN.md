---
phase: 32-production-observability-capacity-incident-readiness
plan: 05
type: execute
wave: 2
depends_on: [32-01, 32-03]
files_modified:
  - lib/observability/alerts.ts
  - lib/observability/alerts.test.ts
  - app/api/cron/daily-observability-check/route.ts
  - app/api/cron/daily-observability-check/route.test.ts
  - vercel.json
autonomous: true
requirements: [R1, R8, R10]

must_haves:
  truths:
    - "fanOutAlert reads the growable recipient list from lib/observability/config.ts and sends to EVERY recipient via lib/email/sendEmail — never a hardcoded single sink (D-08)."
    - "One failed recipient send never aborts the batch; fanOutAlert returns { sent, failed } counts and never throws (curator-reach resilience pattern)."
    - "GET /api/cron/daily-observability-check fails closed when CRON_SECRET is unset or the Bearer header mismatches (401), then runs the daily digest (uptime/error/spend/throttle summary) and fans it out (R10 daily automated)."
    - "The digest re-checks /api/health and classifies observed metrics via classifyThreshold, surfacing any warning/critical/unknown states (R8/R10)."
  artifacts:
    - "lib/observability/alerts.ts"
    - "lib/observability/alerts.test.ts"
    - "app/api/cron/daily-observability-check/route.ts"
    - "app/api/cron/daily-observability-check/route.test.ts"
    - "vercel.json (new daily cron entry, existing weekly curator-reach entry preserved)"
  key_links:
    - "alerts.ts -> getAlertRecipients() (Plan 01) -> lib/email/sendEmail — the extensible fan-out chain (D-08)."
    - "vercel.json daily cron -> /api/cron/daily-observability-check -> fanOutAlert."
  prohibitions:
    - statement: "MUST NOT hardcode a single alert sink; alert destinations must be extensible (fan-out to a growable recipient list)."
      status: resolved
      verification: "test — alerts.test.ts asserts fanOutAlert sends once per recipient from config, not a literal address."
    - statement: "MUST NOT automatically pause production or auto-change Supabase compute without an owner decision."
      status: resolved
      verification: "judgment — the cron only reads status + emails a digest; it takes no infrastructure control action."
---

<objective>
Build the extensible alert fan-out helper (D-08) and the R10 daily automated observability check. `lib/observability/alerts.ts` reads the growable recipient list from the Plan-01 config layer and loops `lib/email/sendEmail` once per recipient — the concrete realization of "never a hardcoded single sink; add company people as the team grows." `app/api/cron/daily-observability-check/route.ts` is a Vercel cron (reusing the existing `vercel.json` mechanism + `CRON_SECRET` fail-closed guard) that runs the daily uptime/error/spend/throttle digest and fans it out.

Purpose: closes the loop between the config layer (recipients/thresholds) and actual owner-received alerts, and delivers R10's daily automated cadence. Depends on Plan 01 (config/getAlertRecipients + classifyThreshold) and Plan 03 (/api/health to re-check).
Output: the fan-out helper + cron route + tests + a new `vercel.json` daily cron entry.
</objective>

## Artifacts this phase produces (Plan 05)

- `lib/observability/alerts.ts` exporting `fanOutAlert(subject, html)` returning `{ sent, failed }`.
- `lib/observability/alerts.test.ts`
- `app/api/cron/daily-observability-check/route.ts` exporting `GET`.
- `app/api/cron/daily-observability-check/route.test.ts`
- `vercel.json` — adds `{ "path": "/api/cron/daily-observability-check", "schedule": "0 6 * * *" }` (daily; Hobby cron ceiling), preserving the existing weekly curator-reach entry.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-SPEC.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-PATTERNS.md
@app/api/cron/curator-reach/route.ts
@lib/email/index.ts
@vercel.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extensible alert fan-out helper</name>
  <files>lib/observability/alerts.ts, lib/observability/alerts.test.ts</files>
  <read_first>
    - lib/email/index.ts (sendEmail signature + no-op-when-unconfigured philosophy)
    - app/api/cron/curator-reach/route.ts (per-row loop, never let one failure abort the batch)
    - lib/observability/config.ts (getAlertRecipients — the growable recipient source, Plan 01)
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-CONTEXT.md (D-08 extensible sinks)
  </read_first>
  <behavior>
    - fanOutAlert calls getAlertRecipients() and sends one email per returned recipient (2 recipients -> sendEmail called twice with each recipient's email).
    - A sendEmail returning { ok:false } for one recipient increments failed but does not throw or stop the loop; other recipients still receive.
    - With the config falling back to the single Pete default, fanOutAlert sends exactly once to pete@funun.studio.
    - fanOutAlert returns { sent, failed } counts matching the mocked outcomes.
  </behavior>
  <action>Create lib/observability/alerts.ts exporting fanOutAlert(subject: string, html: string): Promise<{ sent: number; failed: number }>. It awaits getAlertRecipients() from lib/observability/config.ts, iterates the recipients, and calls sendEmail({ to: recipient.email, subject, html }) once per recipient, incrementing sent on { ok:true } and failed otherwise, never letting one failure abort the loop (mirror curator-reach's resilience). Return the counts. Do NOT reference any literal recipient address in this file — the address list comes only from config. Write alerts.test.ts mocking both @/lib/observability/config (getAlertRecipients) and @/lib/email (sendEmail), covering the multi-recipient fan-out, the one-failure-doesn't-abort case, and the single-default-recipient case.</action>
  <acceptance_criteria>
    - `npx jest lib/observability/alerts.test.ts` passes all cases.
    - Source assertion: alerts.ts contains no literal email address (recipients come only from config); `grep -c '@funun' lib/observability/alerts.ts` returns 0.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx jest lib/observability/alerts.test.ts</automated>
  </verify>
  <done>fanOutAlert delivers to the full growable recipient list from config, tolerates per-recipient failure, and hardcodes no sink.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Daily observability cron (R10 automated) + vercel.json entry</name>
  <files>app/api/cron/daily-observability-check/route.ts, app/api/cron/daily-observability-check/route.test.ts, vercel.json</files>
  <read_first>
    - app/api/cron/curator-reach/route.ts (fail-closed CRON_SECRET Bearer check — copy verbatim including the !process.env.CRON_SECRET guard)
    - vercel.json (existing weekly cron; add the daily entry alongside, do not replace)
    - lib/observability/config.ts (classifyThreshold for surfacing warning/critical/unknown)
    - lib/observability/alerts.ts (fanOutAlert)
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-RESEARCH.md (Common Pitfall #6 Hobby daily cron ceiling)
  </read_first>
  <behavior>
    - GET with a missing/incorrect Authorization header returns 401 and does NOT run the digest or send any alert.
    - GET with the correct Bearer CRON_SECRET runs the digest: re-checks /api/health status, classifies any provided metrics via classifyThreshold, and calls fanOutAlert once when there is something to report; returns { ok:true }.
    - When CRON_SECRET is unset, the route returns 401 (fail closed), never proceeding on `Bearer undefined`.
  </behavior>
  <action>Create app/api/cron/daily-observability-check/route.ts exporting an async GET(request) that first performs the fail-closed CRON_SECRET Bearer check copied from curator-reach (reject when CRON_SECRET is unset OR the header mismatches). Then build the daily digest: re-check /api/health (call the health route's logic or fetch its status), run classifyThreshold over any available metric values to flag warning/critical/unknown states, compose a concise digest subject + html summarizing uptime/error/spend/throttle status, and call fanOutAlert(subject, html) from lib/observability/alerts.ts. Return NextResponse.json({ ok: true }). Keep the loop/never-throw discipline. Add the new cron entry to vercel.json: { "path": "/api/cron/daily-observability-check", "schedule": "0 6 * * *" } (daily at 06:00, within Hobby's once-per-day ceiling), keeping the existing weekly curator-reach entry. Write route.test.ts mocking fanOutAlert + config, covering the 401-unauthorized, 401-when-secret-unset, and authorized-digest-fans-out cases.</action>
  <acceptance_criteria>
    - `npx jest app/api/cron/daily-observability-check/route.test.ts` passes (401 unauth, 401 when secret unset, authorized fan-out).
    - Source assertion: the route contains the `!process.env.CRON_SECRET ||` fail-closed guard.
    - Source assertion: vercel.json contains BOTH the curator-reach weekly entry and the new daily-observability-check entry; `node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"` parses without error.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx jest app/api/cron/daily-observability-check/route.test.ts && node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))"</automated>
  </verify>
  <done>The daily cron is auth-gated fail-closed, builds a threshold-classified digest, fans it out to the recipient list, and is registered in vercel.json without disturbing the existing cron.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /api/cron/daily-observability-check | Vercel attaches Bearer CRON_SECRET; any other caller must be rejected before any work (quota-burn DoS vector). |
| cron → email recipients | Digest content leaves the process to Resend; must carry only summary status, never raw sensitive records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-10 | Denial of Service | daily cron endpoint | medium | mitigate | Fail-closed CRON_SECRET Bearer check copied verbatim from curator-reach (rejects unset-secret bypass) before any digest work. |
| T-32-06 | Information Disclosure | digest email content | medium | mitigate | Digest emits only summary status/threshold classifications, never raw user records; recipients sourced from service-role-only config. |
</threat_model>

<verification>
- `npx jest lib/observability/alerts.test.ts app/api/cron/daily-observability-check/route.test.ts` green.
- `vercel.json` parses; both cron entries present.
- `npx tsc --noEmit` and `npm run lint` clean.
</verification>

<success_criteria>
Alerts fan out to a growable recipient list (no hardcoded sink), and a daily auth-gated cron produces a threshold-classified digest — delivering R10's daily automated cadence.
</success_criteria>

<output>
Create `.planning/phases/32-production-observability-capacity-incident-readiness/32-05-SUMMARY.md` when done.
</output>
