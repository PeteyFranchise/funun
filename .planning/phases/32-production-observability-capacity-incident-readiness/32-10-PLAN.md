---
phase: 32-production-observability-capacity-incident-readiness
plan: 10
type: execute
wave: 3
depends_on: [32-05, 32-08, 32-09]
files_modified:
  - docs/observability/RUNBOOK.md
  - docs/observability/OPERATING-RHYTHM.md
  - docs/observability/monthly-capacity-report-sample.md
autonomous: false
requirements: [R9, R10]

must_haves:
  truths:
    - "The incident runbook determines whether an incident originates in Vercel/Supabase/app/DNS/Auth/Storage/external, where to check first, and how to correlate a user report → request/correlation ID → deployment (R9)."
    - "The runbook documents how to roll back a Vercel deployment safely AND when NOT to roll back because deployed DB schema is ahead of app code (R9 schema-ahead caveat / idempotency edge — rollback + post-incident steps are safe to repeat)."
    - "The runbook references (not duplicates) docs/BREAK-GLASS.md and contains NO destructive-recovery / DB-reset / migration-repair instructions (R9)."
    - "Weekly (~10-min Vercel + Supabase review), pre-launch, and monthly capacity-report checklists exist and NAME an owner (Pete, D-13); a measurable capacity-upgrade trigger is written (R10)."
    - "One monthly capacity report is produced (using load-test/baseline data) demonstrating the format; the capacity-upgrade trigger is a documented measurable threshold where crossing it (and one step either side) yields a deterministic recommend/hold (R10 boundary edge)."
    - "The runbook passes a tabletop exercise (a simulated incident walked end-to-end) (R9)."
  artifacts:
    - "docs/observability/RUNBOOK.md"
    - "docs/observability/OPERATING-RHYTHM.md"
    - "docs/observability/monthly-capacity-report-sample.md"
  key_links:
    - "The runbook's correlation step uses the Plan 02 correlation ID (log line + Sentry event share one value)."
    - "The operating rhythm's daily automated item is the Plan 05 cron; its severity routing uses Plan 08's SEV model; its capacity figures use Plan 09's measured baseline."
    - "The capacity-upgrade trigger cites lib/observability/config.ts budget constants (D-14/D-15)."
  prohibitions:
    - statement: "MUST NOT include destructive-recovery / DB-reset / migration-repair instructions in the runbook."
      status: resolved
      verification: "judgment — the runbook references docs/BREAK-GLASS.md for break-glass and contains no reset/repair steps; asserted by review + a grep for forbidden verbs."
    - statement: "MUST NOT claim a simultaneous-user capacity from Vercel's Function limit alone; capacity claims cite measured evidence."
      status: resolved
      verification: "judgment — the monthly report + upgrade recommendation cite Plan 09's measured baseline, not the 30k Function figure."
---

<objective>
Author the R9 incident-response runbook and the R10 operating-rhythm documentation — the operating process that keeps the whole system in routine founder-scale use. The runbook triages incident origin (Vercel/Supabase/app/DNS/Auth/Storage/external), correlates a user report → correlation ID → deployment, documents safe Vercel rollback AND the schema-ahead "when NOT to roll back" caveat, references (not duplicates) `docs/BREAK-GLASS.md`, and carries no destructive-recovery steps. The operating rhythm names owners for daily/weekly/pre-launch/monthly cadences, writes a measurable capacity-upgrade trigger, and produces one sample monthly capacity report from Plan 09's measured baseline. A tabletop exercise validates the runbook.

Purpose: this is the "system nobody uses is a failure condition" guardrail — the founder-scale process wrapper. Depends on Plan 05 (daily cron), Plan 08 (SEV model), and Plan 09 (measured baseline).
Output: `docs/observability/RUNBOOK.md`, `docs/observability/OPERATING-RHYTHM.md`, `docs/observability/monthly-capacity-report-sample.md`, plus a tabletop sign-off.
</objective>

## Artifacts this phase produces (Plan 10)

- `docs/observability/RUNBOOK.md` — incident triage/correlation/rollback runbook (references BREAK-GLASS.md; no destructive steps; schema-ahead caveat; post-incident review template).
- `docs/observability/OPERATING-RHYTHM.md` — daily/weekly/pre-launch/monthly checklists with named owners + the measurable capacity-upgrade trigger.
- `docs/observability/monthly-capacity-report-sample.md` — one demonstration monthly report using measured baseline data.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-SPEC.md
@.planning/phases/32-production-observability-capacity-incident-readiness/32-CONTEXT.md
@docs/BREAK-GLASS.md
@vercel.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Incident response runbook (references BREAK-GLASS, no destructive steps)</name>
  <files>docs/observability/RUNBOOK.md</files>
  <read_first>
    - docs/BREAK-GLASS.md (the existing DB break-glass procedure — REFERENCE it, do not duplicate; note its three layers)
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-SPEC.md (R9 target/acceptance; Edge Coverage R9 idempotency row; Prohibition on destructive steps)
    - docs/observability/THRESHOLDS-AND-SEVERITY.md (SEV model the runbook routes on — Plan 08)
    - lib/logging/correlation.ts (the correlation ID used in the user-report → request-ID → deployment correlation step — Plan 02)
  </read_first>
  <action>Write docs/observability/RUNBOOK.md as a concise incident runbook covering: how to determine whether an incident originates in Vercel, Supabase, application code, DNS, Auth, Storage, or an external provider, and where to check first for each; how to correlate a user report → correlation/request ID (Plan 02) → the deployment that introduced it; how to roll back a Vercel deployment safely; the explicit "when NOT to roll back because the deployed DB schema is ahead of the app code" caveat (rolling the app back below a live migration can break it — this is the R9 schema-ahead / idempotency safeguard); how existing break-glass constraints apply by REFERENCING docs/BREAK-GLASS.md (do not copy its steps); how to communicate degraded service (status page from Plan 07); how to record an incident timeline; and a post-incident review template capturing root cause, user impact, detection method, detection gap, resolution, preventive action, owner, and due date. Contain NO destructive-recovery, DB-reset, or migration-repair instructions — those live behind break-glass only. State that rollback + post-incident steps are safe to repeat (idempotent).</action>
  <acceptance_criteria>
    - Source assertion: RUNBOOK.md references docs/BREAK-GLASS.md (by path) and does NOT contain destructive verbs (no "db reset", "drop database", "migration repair", "supabase db reset").
    - Source assertion: the schema-ahead "when NOT to roll back" caveat is present; the post-incident template has all eight fields.
    - `grep -qi "BREAK-GLASS" docs/observability/RUNBOOK.md` and a grep for forbidden destructive phrases returns none.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/observability/RUNBOOK.md && grep -qi "BREAK-GLASS" docs/observability/RUNBOOK.md && ! grep -Eqi "db reset|drop database|migration repair|supabase db reset" docs/observability/RUNBOOK.md</automated>
  </verify>
  <done>The runbook triages origin, correlates report→ID→deploy, documents safe rollback + the schema-ahead caveat, references (not duplicates) break-glass, has an eight-field post-incident template, and carries no destructive steps.</done>
</task>

<task type="auto">
  <name>Task 2: Operating rhythm checklists + sample monthly capacity report</name>
  <files>docs/observability/OPERATING-RHYTHM.md, docs/observability/monthly-capacity-report-sample.md</files>
  <read_first>
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-SPEC.md (R10 target/acceptance; Edge Coverage R10 boundary row)
    - .planning/phases/32-production-observability-capacity-incident-readiness/32-CONTEXT.md (D-13 owner=Pete; D-14/D-15 budgets)
    - docs/observability/CAPACITY-REPORT.md (Plan 09 measured baseline for the sample monthly report)
    - vercel.json (the daily cron is the "daily automated" item — Plan 05)
    - lib/observability/config.ts (budget constants for the capacity-upgrade trigger)
  </read_first>
  <action>Write docs/observability/OPERATING-RHYTHM.md with four cadences, each naming an owner (Pete, pete@funun.studio, D-13): (1) daily automated — the Plan 05 cron's uptime/error/spend/throttle digest; (2) weekly ~10-minute Vercel + Supabase review with a trend-recording step; (3) a pre-launch checklist (run the load-test profile, review slow queries, confirm capacity headroom, verify alert delivery, name the release + rollback owner); (4) a monthly capacity report cadence. Write the measurable capacity-upgrade trigger as a documented threshold (citing the D-15 $100 infra-review trigger and D-14 ~$50/mo Supabase compute ceiling from lib/observability/config.ts) where crossing it — and one step either side — yields a deterministic recommend/hold. State that after every incident the review is completed and the detection gap closed or tracked. Then write docs/observability/monthly-capacity-report-sample.md: one demonstration monthly report using Plan 09's measured baseline data, with traffic, peak measured concurrency, p95, error rate, DB utilization, disk growth, cost + projection, and throttling, producing a 90-day capacity recommendation — citing measured evidence, never the Vercel 30k figure as a simultaneous-user capacity.</action>
  <acceptance_criteria>
    - Source assertion: OPERATING-RHYTHM.md has daily/weekly/pre-launch/monthly sections each NAMING an owner, and a measurable capacity-upgrade trigger with the recommend/hold rule.
    - Source assertion: monthly-capacity-report-sample.md is a populated sample report citing measured baseline data with a 90-day recommendation, not the 30k figure.
    - `grep -qi "owner" docs/observability/OPERATING-RHYTHM.md` and both files exist.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/observability/OPERATING-RHYTHM.md && test -f docs/observability/monthly-capacity-report-sample.md && grep -qi "weekly" docs/observability/OPERATING-RHYTHM.md && grep -qi "monthly" docs/observability/OPERATING-RHYTHM.md</automated>
  </verify>
  <done>Daily/weekly/pre-launch/monthly checklists name an owner, a measurable capacity-upgrade trigger is written, and one sample monthly report demonstrates the format from measured baseline data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — tabletop the runbook + confirm owner cadence</name>
  <action>Pause for human verification. The owner performs the how-to-verify steps below; do not proceed until they reply with the resume-signal.</action>
  <what-built>The runbook + operating rhythm are drafted. R9 requires a tabletop exercise and R10 requires the owner to confirm the named cadence.</what-built>
  <how-to-verify>
    1. Tabletop the runbook: pick a simulated incident (e.g. "buyers report /sync/catalog 500s after a deploy") and walk it end-to-end — origin triage, report→correlation-ID→deployment, rollback decision (including a schema-ahead case where you must NOT roll back), degraded-service comms, timeline, post-incident review.
    2. Confirm the runbook was sufficient (or note gaps to fix), and that it referenced break-glass without duplicating it and had no destructive steps.
    3. Confirm the weekly + monthly checklists name you as owner and the capacity-upgrade trigger is measurable.
  </how-to-verify>
  <resume-signal>Type "tabletop passed" (with any gaps found) or "needs revision" with specifics.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook reader → production recovery actions | The runbook guides human recovery; an unsafe instruction (destructive recovery / rolling app behind live schema) could cause a worse outage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-16 | Availability (unsafe recovery) | runbook rollback guidance | medium | mitigate | The schema-ahead "when NOT to roll back" caveat + reference-only to BREAK-GLASS.md + no destructive-recovery steps; validated by a tabletop exercise. |
| T-32-15 | Accuracy/integrity | monthly capacity claims | low | mitigate | The sample report cites Plan 09 measured baseline; never restates the Vercel 30k Function figure as simultaneous-user capacity. |
</threat_model>

<verification>
- `test -f` + grep gates on RUNBOOK.md / OPERATING-RHYTHM.md / monthly-capacity-report-sample.md pass; no destructive verbs in the runbook.
- Tabletop exercise passed (checkpoint); owner-named cadence confirmed.
</verification>

<success_criteria>
A tabletop-tested incident runbook (references break-glass, no destructive steps, schema-ahead caveat) and a named-owner operating rhythm (daily/weekly/pre-launch/monthly) with a measurable capacity-upgrade trigger and one sample monthly report exist — completing the founder-scale operating process.
</success_criteria>

<output>
Create `.planning/phases/32-production-observability-capacity-incident-readiness/32-10-SUMMARY.md` when done.
</output>
