---
phase: 38.0.3
artifact: ORCHESTRATOR-NOTES
date: 2026-09-08
---

# 38.0.3 — carry-forwards from plan-check

Plan-check verdict: **01 BLOCK · 02 BLOCK · 03 PASS (2 flags) · 04 PASS ·
05 PASS (1 flag) · 06 PASS**. The blocker is fixed; the four warnings are
carried here so they are not lost in a SUMMARY.

## BLOCKER — FIXED 2026-09-08

Plans 01 and 02 told the owner to apply migrations 207/208 **before** plan 03's
Part A pre-run, while plan 03 requires that pre-run as a gate. Followed
literally, the owner would have run
`DROP FUNCTION public.workspace_roster_relationship_is_live` with no
`pg_proc.prosrc` body-caller check, no event-trigger audit and no `pg_policies`
diff — the migration-198 shape: clean apply, silent `42883` on first live call.
It would also have destroyed the pre-run disclosure evidence irreversibly.

Both `<human-check>` blocks rewritten to gate on the pre-run first, mirroring
plans 05/06 which already had it right. No task, migration or test changed.

## W1 — `workspace_member_role` is never behaviourally proven. UAT REQUIRED.

Part B rows B5/B6 cannot prove the bind on `workspace_member_role`, because the
workspace tables are empty while D-56 is off: B6's cross-user NULL has no
positive control and B5 is INFO only. Part A covers it structurally, but this is
the **37-reference** helper feeding `workspace_audit_log`,
`workspace_roster_relationships` and `workspaces` reads, and it is the one
Tier-2 bind whose "returns false to everybody" failure mode the harness cannot
detect.

**ACTION: re-run Part B rows B5/B6 once D-56 is switched on, BEFORE beta
traffic.** Add to the D-56 cutover checklist, not just to this phase's close.

## W2 — three mutation checks are inspection-only, not proofs

Plan 03 items 5 and 6, and plan 06 items 4-7, say "confirm by inspection, since
the query cannot be run." That is an unavoidable consequence of the human-gating
rule and is honestly labelled — but an inspection is not a proof. **Record these
as a residual in the SUMMARY; do not report them as mutation-verified.**

## W3 — plan 05 touches a function D2 declared out of scope

Migration 209 must retarget the `no_block` call inside
`discover_profile_id_by_email`. Its body is a string literal, so PostgreSQL
records no dependency and the drop would break it silently. This is
mechanically forced, not scope creep, and is guarded by drift assertion 11.

**ACTION: plan 05's SUMMARY must state explicitly that D2's "no action" now
means "one call target retargeted, body otherwise byte-identical"** — otherwise
a future reader sees a flat contradiction between the SCOPE and migration 209.

## W4 — `public.zz_verify_b_results` — LOGGED

A public table with no RLS, left by this phase's own predecessors. Now tracked
at `.planning/todos/pending/2026-09-08-drop-zz-verify-b-results-public-table.md`
so it does not close with the phase. 38.0.3's harnesses use `TEMP` tables.

## What plan-check independently reproduced

Worth recording, because it means these are evidenced rather than asserted: the
Tier-1 four appear in **zero** `CREATE/ALTER POLICY` statements across all 200
migrations (comment-stripped, dollar-region-masked scan); parameter names are
non-uniform and plan 02's table is character-correct on all 13; every "copy the
body from" row is latest-definition-wins correct; there are exactly two
application `.rpc()` call sites and both are service-client; `no_block` has
**11** live policy sites, not the 15 D4 quoted loosely; `checkViewerBlock` is
fail-closed today and `loadBlockedIds` does discard its error.
