---
phase: 38.0.3
artifact: PART B RESULTS
date: 2026-09-09
run_by: owner, Supabase SQL editor, production
rows: 16
tier1: PROVEN
tier2: STRUCTURALLY VERIFIED, BEHAVIOURALLY UNPROVEN
---

# Part B -- 2026-09-09, after migrations 208 and 209

## Tier 1 is PROVEN behaviourally

| Row | Check | Result |
|---|---|---|
| B8 | `workspace_access_enabled()` as authenticated | `42501 permission denied` - **PASS** |
| B9 | `green_room_post_matches_custom_audience` as authenticated | `42501` - **PASS** |
| B9b | `workspace_roster_relationship_is_live` as authenticated | `42883 does not exist` - **PASS**, the drop is real |
| B10 | definer-body callers survive the revoke | `workspace_project_permission returned false without raising` - **PASS** |
| B13 | session hygiene | `current_user=postgres request.jwt.claims=(unset)` - **PASS**, harness wrote nothing |

**B10 is the important one.** It proves the TRAP exemption's safety argument
with live evidence: a `SECURITY DEFINER` body runs as the function OWNER and
needs no EXECUTE grant from the caller, so revoking the three Tier-1 helpers did
not break any of the fourteen definer bodies that call them internally.

## Tier 2 is NOT proven behaviourally, and this is stated plainly

**Every cross-user Tier-2 assertion was SKIPPED for want of a subject.**

| Row | Check | Why it could not run |
|---|---|---|
| B1/B2 | `green_room_can_view_post` self vs other | no visible, undeleted `green_room_posts` row |
| B3/B4 | `is_split_sheet_party` self vs other | no `split_sheet_parties` row with a non-null `user_id` |
| B5/B6 | `workspace_member_role` self vs other | `workspaces=0 workspace_members=0` (W1) |
| B7 | service_role disjunct when `auth.uid()` is NULL | needs the STRICT Green Room subject |
| B11/B11b | RLS reads still work | no author subject |

The thirteen binds are **structurally verified** -- Part A rows 201-213 confirm
each function's text, its identity parameter, the `service_role` disjunct and the
absence of `pg_trigger_depth`. But **no live call has exercised one.**

That is precisely the gap migration 198's `42702` fell through: a green
text-lock suite while the first live call was broken. It is not called verified
here.

## CARRY-FORWARD W1, WIDENED

W1 was scoped to `workspace_member_role`. **It is actually all thirteen.**

**ACTION: re-run Part B once real data exists** -- when the first beta users
create Green Room posts and split sheets, rows B1-B7 gain subjects and can
finally test the binds. Add this to the D-56 cutover checklist, before beta
traffic.

Until then the binds are correct-by-construction and unverified-by-behaviour.
**This does not hold up the phase**: the exposure is closed regardless, because
Part A proved the grants and the drop structurally and B8/B9/B9b proved them
behaviourally.

## Baseline captured for plan 06

**B12**: `SQLSTATE 42501: permission denied for function no_block` as anon.
This confirms the pre-existing finding that `rc_select_public` carries no `TO`
clause, so `anon` evaluates it, and `anon` has never held EXECUTE on `no_block`
-- anonymous reads of `release_comments` already fail today, before any
relocation. Plan 06 must reproduce this exact baseline, not "fix" it.

## Status

**Phase 38.0.3 Tiers 1 and 2: APPLIED and VERIFIED** (with the Tier-2
behavioural caveat above). Tier 3 (`no_block` relocation, migration 210, plans
04-06) remains, sequenced after this by owner decision D4.
