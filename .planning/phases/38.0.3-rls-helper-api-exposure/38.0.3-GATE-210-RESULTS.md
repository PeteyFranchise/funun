# Pre-apply gate for migration 210 — run 2026-09-09

Owner-run, read-only, Supabase SQL editor. Seven rows.

| # | Check | Detail | Verdict |
|---|-------|--------|---------|
| G1 | public body callers of `no_block` | `discover_profile_id_by_email, green_room_can_view_post` | **PASS** — exactly the two section 3 retargets |
| G2 | body callers outside `public` | (none) | **PASS** |
| G3 | `public.no_block(uuid, uuid)` pre-state | ABSENT | **FALSE STOP — see below** |
| G4 | policies referencing `no_block` | 11 found | **PASS** |
| G5 | policies NOT covered by section 4 | (none) | **PASS** |
| G6 | schema `private` | absent | **PASS** — new schema, nothing inherited |
| G7 | PostgREST exposed schemas | `private` not present | **PASS** |

## G3 was my assertion, not the database

The gate located the function with

```sql
AND pg_get_function_identity_arguments(p.oid) = 'uuid, uuid'
```

`pg_get_function_identity_arguments` **keeps parameter names**. Production
returns `a uuid, b uuid` (length 14), so the equality failed on a function that
is present and healthy. A follow-up diagnostic over every schema returned one
row, and every attribute matches migration 035:

| attribute | live value |
|-----------|-----------|
| schema.name | `public.no_block` |
| identity args | `a uuid, b uuid` |
| `prosecdef` | true |
| `provolatile` | `s` |
| `proconfig` | `search_path=""` |
| `anon` EXECUTE | false |
| `authenticated` EXECUTE | **true** |

That last row is the live confirmation of the exposure Tier 3 closes: any
logged-in user can execute the symmetric block oracle today.

**This is the third instance of the same failure shape in this phase** — the
twelve false `search_path` FAILs in 38.0.2 Part A, the `%public.%` pattern that
matched `graphql_public.`, and now this. Every one was a hand-written predicate
compared against a rendering the author assumed rather than checked. The lesson
is the same each time: **resolve objects by identity, not by string equality on
a rendered form.**

The plan-05 agent's own gate, quoted in `38.0.3-05-SUMMARY.md`, does not have
this defect — it uses `to_regprocedure('public.no_block(uuid,uuid)')`, which
resolves by type signature and is immune to how names and spacing render. That
is the form to reuse.

## Verdict

**Gate is GREEN.** G1 is the load-bearing row and it PASSES: exactly the two
body callers migration 210 section 3 retargets, and no third. Migration 210 is
cleared to apply.

---

# Migration 210 APPLIED 2026-09-09 — post-apply verification

Applied by the owner in the Supabase SQL editor as one paste.
`Success. No rows returned.`

**The drop is itself evidence.** `DROP FUNCTION public.no_block(uuid, uuid)` at
line 604 carries no `CASCADE`. PostgreSQL's restrictive default means it would
have refused had any policy still depended on the function. It did not refuse,
so nothing was left pointing at it.

## Post-apply results — 12 rows

| # | Check | Detail | Verdict |
|---|-------|--------|---------|
| P1 | `private.no_block` exists | `private.no_block(uuid,uuid)` | PASS |
| P2 | `public.no_block` is gone | null | PASS — no HTTP route can exist |
| P3 | schema USAGE on `private` | authenticated=true anon=false service_role=false | PASS |
| P4 | EXECUTE on `private.no_block` | authenticated=true anon=false service_role=false | PASS |
| P5 | relocated attributes | secdef=true volatile=s cfg=`search_path=""` | PASS |
| D1 | policies naming `private.no_block` | 11 of 11 | PASS |
| D2 | any policy still on `public.no_block` | (none) | PASS |
| D3 | `rc_select_public` roles | `{public}` | PASS — pre-existing shape preserved |
| D4 | the other ten | 10 of 10 `{authenticated}` | PASS |
| B1 | Tier 2 survived on `green_room_can_view_post` | binding present | PASS — bound AND retargeted |
| B2 | `discover_profile_id_by_email` retargeted | `private.no_block` | PASS |
| X1 | exposed public definers | see below | PASS on re-run |

## X1 needed a correct ruler — the fourth predicate error of this phase

X1 first read **63**. The count cannot rise from this migration, so the
predicate was suspect rather than the database. It was: my ad-hoc version
omitted two filters the canonical sweep in `38.0.3-VERIFY-A-STRUCTURAL.sql`
carries, so it counted trigger functions and extension-owned functions that
were never in the 53 baseline:

```sql
AND p.prorettype <> 'trigger'::regtype
AND NOT EXISTS (SELECT 1 FROM pg_depend d WHERE d.objid = p.oid AND d.deptype = 'e')
```

Re-run with the canonical predicate copied character-for-character:

| metric | value |
|--------|-------|
| `exposed_count` | **48** |
| `all_public_definers` | **107** |

Both close arithmetically and independently:

```
53  baseline 2026-09-08
-4  Tier 1 (208): three revokes + one drop   -> 49
-1  Tier 3 (210): no_block relocated         -> 48   MATCHES
109 definers at inventory
 -2 workspace_roster_relationship_is_live, no_block -> 107   MATCHES
```

**Four predicate errors in this phase, all one shape:** the twelve false
`search_path` FAILs, the `%public.%` pattern that matched `graphql_public.`,
G3's `= 'uuid, uuid'` against a rendering that keeps parameter names, and now
X1's missing filters. Each time the data was right and the hand-written
assertion was wrong. **Reuse the canonical predicate; do not retype it.**

## Verdict

**Tier 3 is COMPLETE and VERIFIED structurally.** The symmetric block oracle
(threat T-08-03) is no longer reachable over HTTP by any role: the function has
no route, because PostgREST introspects only exposed schemas and `private` is
not one.

**Still behaviourally unproven, and not claimed:** no live Green Room read has
exercised a retargeted policy. Same standing caveat as Tier 2 — re-run the
behavioural harness once beta users create posts and comments.
