---
phase: 38.0.3
artifact: PART A PRE-RUN RESULTS
date: 2026-09-09
run_by: owner, Supabase SQL editor, production
rows: 127
---

# Part A pre-run — 2026-09-09

Run BEFORE migrations 208/209 were applied. **The FAIL rows below are the
evidence that the disclosure existed** and cannot be reconstructed afterwards.

## All four gates CLEAR

| Gate | Row | Result |
|---|---|---|
| **A5** new exposures since the 2026-09-08 sweep | 141 | `(none)` — **PASS** |
| **D3** migration corpus vs production policies | 470 | `repo 44 / production 44 / matched 44` — **PASS** |
| **D4 / D4b** identity arguments | 481-540 | `48 of 48 pass auth.uid()` — **PASS** |
| **E2** body callers of the dropped helper | 590 | `(none)` — **PASS, the drop is safe** |
| **E3** anti-vacuity for E2 | 591 | 14 pairs found, so E2's zero is measured — **PASS** |
| **C1** event-trigger handlers containing GRANT | 300 | **FAIL, then RESOLVED — see below** |

### C1 resolved by reading the handler bodies

C1 is a conservative text match for `GRANT` in an event-trigger handler. It
named `grant_pg_cron_access`. A per-handler scan of all six then showed:

| handler | grants on fn | touches `public` | anon | authenticated |
|---|---|---|---|---|
| `grant_pg_cron_access` | yes | no | no | no |
| `grant_pg_graphql_access` | yes | **yes** | **yes** | **yes** |
| `grant_pg_net_access` | yes | no | yes | yes |
| `pgrst_ddl_watch` | no | no | no | no |
| `pgrst_drop_watch` | no | no | no | no |
| `set_graphql_placeholder` | no | yes | no | no |

`grant_pg_graphql_access` looked alarming on all four columns, so its body was
read in full. Two independent reasons it cannot affect this phase:

1. Lines 7-14 are an **early return**: it exits unless the DDL that fired it was
   on the `pg_graphql` extension. A `REVOKE` on a `public` function never
   reaches a grant statement.
2. Lines 38-41 target **`graphql` and `graphql_public` only** — never `public`.

**The `touches_public_schema = true` flag was a false positive in the probe's
own pattern**: `%public.%` matches `graphql_public.`. Recorded because the same
sloppy pattern would mislead the next reader too.

`grant_pg_net_access` is the same Supabase-authored shape scoped to `net`, and
`grant_pg_cron_access` is scoped to `cron` and names neither of our roles.

**Assumption A1 HOLDS. Migration 208's revokes will be durable.**

## Expected pre-run FAILs (the disclosure evidence)

| Row | Check | Detail |
|---|---|---|
| 101-103 | A1 Tier-1 revoked | three helpers `anon=false authed=true` — still exposed |
| 110 | A2 Tier-1 dropped | `workspace_roster_relationship_is_live` still present |
| 150 | A6 empty search_path | `definers=109 offenders=1 apply_to_opportunity_atomic [search_path=public]` — migration 200 authored, not applied |
| 201-213 | B1 Tier-2 bind deployed | all thirteen `binds=false service_role_disjunct=false` |
| 140 | A4 exposed definer count | `actual=53 baseline=53` — matches the sweep exactly |

## Passing already

- **A3** (121-133) Tier-2 grant posture as expected.
- **B2** (221-226) migration 174's six functions still carry `pg_trigger_depth` —
  nothing this phase did disturbed them.
- **C3** (340) six event triggers visible, so C1 read real rows.

## Two corrections to earlier claims

**`public.zz_verify_b_results` HAS RLS enabled.** Row 601 reads
`EXISTS rls_enabled=true rows=-1 grants: anon:INSERT, anon:SELECT, anon:UPDATE...`.
Earlier notes said it had no RLS. `anon` does hold table grants, but with RLS on
and no policies, access is denied. Still worth dropping; not the exposure it was
described as.

**Population is thinner than assumed** (rows 611-619): `blocks` 0,
`green_room_comments` 0, `green_room_posts` 0, `release_comments` 0,
`split_sheet_parties` 0, `split_sheets` **8**, `user_profiles` **5**,
`workspaces` 0, `workspace_members` 0. Part B's positive controls will be
meaningful only for split sheets and profiles; the rest record INFO.

**F4** (640): `enabled = false` — D-56 off, as required.

## VERDICT

**GO for applying migrations 208 and 209.**
