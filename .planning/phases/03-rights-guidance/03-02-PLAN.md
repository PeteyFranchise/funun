---
phase: 03-rights-guidance
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - components/vault/RightsStatusPatch.tsx
  - components/vault/SongtrustGuideCard.tsx
  - components/vault/CopyrightFiling.tsx
  - app/(artist)/vault/[projectId]/rights/page.tsx
  - app/(artist)/vault/[projectId]/page.tsx
autonomous: true
requirements:
  - RIGHTS-01
  - RIGHTS-02
  - RIGHTS-03
  - RIGHTS-04
  - SONGTRUST-01
must_haves:
  truths:
    - "A page exists at /vault/[projectId]/rights as a server component"
    - "The rights page shows a copyright guide card with deep-link to https://eco.copyright.gov and a 3-state status indicator (not filed / filed / registered)"
    - "The rights page shows a PRO guide card with ASCAP, BMI, SESAC, SOCAN options and deep-links for ASCAP/BMI/SESAC"
    - "The rights page shows a SoundExchange guide card; status auto-shows 'Ready to register' when all project tracks have RDR-N core profile"
    - "The rights page shows a Songtrust guide card linking to /vault/[projectId]/metadata/cwr"
    - "The project page readiness panel contains a 'Rights & Registrations' link to /vault/[projectId]/rights"
    - "RightsStatusPatch component calls PATCH /api/vault/[projectId]/rights and refreshes the page on success"
  artifacts:
    - app/(artist)/vault/[projectId]/rights/page.tsx
    - components/vault/RightsStatusPatch.tsx
    - components/vault/SongtrustGuideCard.tsx
  key_links:
    - "rights/page.tsx imports assessRdrReadiness from lib/metadata/rdr to derive SoundExchange readiness"
    - "rights/page.tsx reads copyright_status, pro_registration_status, soundexchange_registered from vault_projects row"
    - "SongtrustGuideCard links to /vault/[projectId]/metadata/cwr (existing CWR page)"
    - "project page.tsx readiness panel Link href points to /vault/[projectId]/rights"
---

<objective>
Build the per-project Rights & Registrations page with all four checklist guide cards (copyright, PRO, SoundExchange, Songtrust), add the status-update client component, and wire the "Rights & Registrations" link from the project readiness panel.

Purpose: Delivers RIGHTS-01 through RIGHTS-04 and SONGTRUST-01 to artists — a single page where they can see registration status, follow guided steps, and act.
Output: rights/page.tsx server component, RightsStatusPatch.tsx client component, SongtrustGuideCard.tsx, and an extended CopyrightFiling.tsx. Plus a nav link from the project home page.
</objective>

<execution_context>
@/Users/peterzora/Desktop/funun/.claude/gsd-core/workflows/execute-plan.md
@/Users/peterzora/Desktop/funun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/peterzora/Desktop/funun/.planning/PROJECT.md
@/Users/peterzora/Desktop/funun/.planning/ROADMAP.md
@/Users/peterzora/Desktop/funun/.planning/STATE.md
@/Users/peterzora/Desktop/funun/.planning/phases/03-rights-guidance/03-01-SUMMARY.md
@/Users/peterzora/Desktop/funun/app/(artist)/vault/[projectId]/readiness/page.tsx
@/Users/peterzora/Desktop/funun/app/(artist)/vault/[projectId]/page.tsx
@/Users/peterzora/Desktop/funun/components/vault/CopyrightFiling.tsx
@/Users/peterzora/Desktop/funun/app/(artist)/vault/[projectId]/metadata/registrations/page.tsx
@/Users/peterzora/Desktop/funun/lib/metadata/rdr.ts
@/Users/peterzora/Desktop/funun/lib/metadata/registration.ts
@/Users/peterzora/Desktop/funun/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RightsStatusPatch client component + SongtrustGuideCard</name>
  <files>
    components/vault/RightsStatusPatch.tsx,
    components/vault/SongtrustGuideCard.tsx
  </files>
  <action>
    Create two new client/static components.

    ── RightsStatusPatch.tsx ──

    Mark with `'use client'` at the top.

    Props interface:
    - `projectId: string`
    - `field: 'copyright_status' | 'pro_registration_status' | 'soundexchange_registered'`
    - `value: string | boolean`
    - `label: string`
    - `disabled?: boolean`

    Behavior:
    - Use `useState(false)` for `saving` state and `useRouter` from `next/navigation` for page refresh.
    - On button click: set `saving = true`, call `fetch(`/api/vault/${projectId}/rights`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ [field]: value }) })`, then set `saving = false` and call `router.refresh()`.
    - Render a `<button>` styled with Tailwind using existing design tokens (lav/indigo palette, consistent with CopyrightFiling.tsx button style). Button is disabled when `saving || disabled`.
    - Show `saving ? 'Saving…' : label` as button text.

    This is a generic component — the rights page composes it with specific field/value pairs.

    ── SongtrustGuideCard.tsx ──

    This is a pure display component (no `'use client'` needed). Props: `cwrHref: string`.

    Render a card section (matching the card visual style used in the registrations page — white/5 background, rounded-xl border border-white/10) explaining what Songtrust does (publishing admin, global collection, CWR submission) and containing:
    - A brief paragraph: Songtrust is a publishing administrator that collects royalties globally on the artist's behalf by submitting a CWR file to PROs worldwide.
    - A link `<a href={cwrHref}>` with text "Download CWR file →" that navigates to the existing CWR page. The `cwrHref` prop will be `/vault/[projectId]/metadata/cwr`. Do NOT hardcode the projectId — always use the prop.
    - An external link `<a href="https://www.songtrust.com/" target="_blank" rel="noopener noreferrer">` with text "Learn about Songtrust →". Note: the Songtrust URL is marked [ASSUMED] in research — a checkpoint in Plan 03 will verify it.

    Style to match the existing guide-card aesthetic: consistent heading size with other sections, muted description text (text-white/60), indigo accent for the action links. No new color tokens — use existing Tailwind config classes only.
  </action>
  <verify>
    <automated>cd /Users/peterzora/Desktop/funun && npm run build 2>&1 | grep -E "error TS|RightsStatusPatch|SongtrustGuideCard" | head -10 || echo "Build check complete"</automated>
  </verify>
  <done>Both component files exist. TypeScript build passes. RightsStatusPatch renders a button that calls the PATCH route and refreshes. SongtrustGuideCard renders with CWR link and Songtrust external link.</done>
</task>

<task type="auto">
  <name>Task 2: Extend CopyrightFiling.tsx with 3-state status prop + rights page server component</name>
  <files>
    components/vault/CopyrightFiling.tsx,
    app/(artist)/vault/[projectId]/rights/page.tsx
  </files>
  <action>
    ── CopyrightFiling.tsx — extend for 3-state status ──

    Read the existing CopyrightFiling.tsx to understand its current props and behavior before editing.

    Add a new optional prop `copyrightStatus?: 'not_filed' | 'filed' | 'registered'` to the component's props interface.

    Update the component's rendering logic:
    - When `copyrightStatus === 'registered'`: show a green "Registered" badge (emerald color, consistent with signed document badge style). Do not show the "Mark as filed" button.
    - When `copyrightStatus === 'filed'` (or when a copyright_registration document is already present, as the existing check does): show an amber "Filed — awaiting certificate" badge, AND add a `RightsStatusPatch` button below it: `<RightsStatusPatch projectId={projectId} field="copyright_status" value="registered" label="Mark as registered" />`. Import RightsStatusPatch from `@/components/vault/RightsStatusPatch`. This gives the artist a path to transition from filed to registered.
    - When `copyrightStatus` is `undefined` or `'not_filed'` and no document exists: existing behavior (show eCO link + "Mark as filed" button) is unchanged.

    The existing "Mark as filed" button behavior (writing a vault document via POST /api/vault/[projectId]/documents) is NOT changed — do not alter that path. The only addition is the "Mark as registered" button that appears after filing.

    If `projectId` is not already a prop on CopyrightFiling, add it as a required `string` prop. The component needs it to construct the RightsStatusPatch call.

    ── app/(artist)/vault/[projectId]/rights/page.tsx — server component ──

    Create this file following the exact server component pattern from `app/(artist)/vault/[projectId]/readiness/page.tsx`.

    At the top of the file:
    - `export const dynamic = 'force-dynamic'`
    - `const DEMO = process.env.NEXT_PUBLIC_VAULT_DEMO === 'true'`

    Function signature:
    ```
    export default async function RightsPage({
      params,
    }: {
      params: Promise<{ projectId: string }>
    })
    ```
    Await params: `const { projectId } = await params`

    Data fetching:
    1. Guard DEMO mode: if DEMO, redirect to `/vault` with `notFound()` or show a minimal placeholder — follow how readiness/page.tsx handles demo mode.
    2. `createServerClient()` from `@/lib/supabase/server`.
    3. `supabase.auth.getUser()` — if no user, redirect to `/signin`.
    4. SELECT from `vault_projects` for the current user:
       - Columns: `id, title, type, copyright_status, pro_registration_status, soundexchange_registered, p_line, label, publisher`
       - Filter: `.eq('id', projectId).eq('user_id', user.id).single()`
       - If null or error: call `notFound()`.
    5. SELECT tracks for this project:
       - Columns: `id, title, isrc, iswc, metadata, duration_seconds`
       - Filter: `.eq('project_id', projectId).order('track_number')`
    6. SELECT artist profile: `id, artist_name, pro, ipi, soundexchange_id` from `artist_profiles` where `id = user.id`.
    7. Derive RDR readiness by calling `assessRdrReadiness()` from `@/lib/metadata/rdr`. The function expects `RdrTrackInput[]` — build the input array from the tracks rows using `readPerformers` from `@/lib/metadata/schema` to parse performer metadata. Map each track to: `{ id, title, isrc, mainArtist: profile.artist_name, rightsOwner: project.p_line ?? project.label ?? profile.artist_name, performers: readPerformers(track.metadata) }`.
    8. Derive SoundExchange display status:
       - `const seReady = rdr.coreCount === rdr.tracks.length && rdr.tracks.length > 0`
       - `const seStatus = project.soundexchange_registered ? 'registered' : seReady ? 'ready' : 'not_ready'`

    Page layout (JSX):
    - Page heading: "Rights & Registrations" with the project title as subtitle. Match the heading style of readiness/page.tsx.
    - Four sections in order: Copyright, PRO Registration, SoundExchange, Songtrust. Each section is a `<section>` with a heading and guide card.

    Copyright section (RIGHTS-01):
    - Heading: "Copyright Registration"
    - Brief instruction: file with the US Copyright Office eCO system to protect your composition and sound recording.
    - Display `copyright_status` as a badge: `not_filed` → gray "Not filed", `filed` → amber "Filed", `registered` → green "Registered". Null maps to "Not filed" display.
    - Render `<CopyrightFiling projectId={projectId} copyrightStatus={project.copyright_status ?? 'not_filed'} />` — this reuses the existing component including its eCO deep-link.

    PRO section (RIGHTS-02):
    - Heading: "PRO Registration"
    - Show `pro_registration_status` badge: `not_registered` → gray "Not registered", `registered` → green "Registered".
    - Display artist's current PRO from `profile.pro` if present: "Your PRO: [name]".
    - Show ISWC proxy note: list how many tracks in this project have ISWC codes (count from tracks).
    - PRO option links:
      - ASCAP: `href="https://www.ascap.com/music-creators"` (existing URL from codebase)
      - BMI: `href="https://www.bmi.com/songwriters"` (existing URL from codebase)
      - SESAC: `href="https://www.sesac.com"` (existing URL from codebase)
      - SOCAN: `href="https://www.socan.com/music-creators/"` — mark with a `// TODO: verify SOCAN URL` comment in the JSX (a checkpoint in Plan 03 will confirm this). Render the link visually the same as others. The URL `https://www.socan.com/music-creators/` is from research [ASSUMED] — the Plan 03 checkpoint will verify.
    - Add `<RightsStatusPatch projectId={projectId} field="pro_registration_status" value="registered" label="Mark as registered" disabled={project.pro_registration_status === 'registered'} />`.
    - All PRO links open in `target="_blank" rel="noopener noreferrer"`.

    SoundExchange section (RIGHTS-03):
    - Heading: "SoundExchange"
    - Brief instruction: SoundExchange collects digital performance royalties for sound recordings.
    - Show derived `seStatus` badge: `not_ready` → gray "Data incomplete", `ready` → amber "Ready to register", `registered` → green "Registered".
    - When `seStatus === 'not_ready'`, show a brief "missing data" note: list how many tracks lack ISRC codes and/or performer credits (from `rdr.tracks` array — check which tracks have `core` profile false).
    - SoundExchange link: `href="https://www.soundexchange.com"` (verified in existing codebase).
    - Add `<RightsStatusPatch projectId={projectId} field="soundexchange_registered" value={true} label="Mark as registered" disabled={!!project.soundexchange_registered} />`.

    Songtrust section (SONGTRUST-01):
    - Render `<SongtrustGuideCard cwrHref={`/vault/${projectId}/metadata/cwr`} />`.

    Import all lib functions from their existing paths. Use existing Tailwind classes for layout and color — match the card grid pattern from `app/(artist)/vault/[projectId]/metadata/registrations/page.tsx` for visual consistency. Use `text-white/60` for description text, `text-white` for headings, and the existing border-white/10 card style.

    Do NOT import from `@/lib/metadata/registration` or `@/lib/metadata/bundle` — this page does not need the full registration package builder. The RDR assessment and direct column reads are sufficient.
  </action>
  <verify>
    <automated>cd /Users/peterzora/Desktop/funun && npm run build 2>&1 | grep -E "error TS|rights/page|CopyrightFiling" | head -15 || echo "Build check complete"</automated>
  </verify>
  <done>
    - rights/page.tsx exists and the TypeScript build passes
    - CopyrightFiling.tsx accepts copyrightStatus prop and renders the "Mark as registered" RightsStatusPatch button when status is 'filed'
    - The rights page fetches project + tracks + artist_profile, derives RDR readiness, and renders all four guide sections
    - All four requirement sections are present in the JSX: copyright, PRO, SoundExchange, Songtrust
  </done>
</task>

<task type="auto">
  <name>Task 3: Add "Rights &amp; Registrations" link to project readiness panel</name>
  <files>app/(artist)/vault/[projectId]/page.tsx</files>
  <action>
    Add a "Rights & Registrations" navigation link to the readiness panel in `app/(artist)/vault/[projectId]/page.tsx`.

    Read the file to find the `readinessPanel` section (around line 219–279 based on existing content). There are two existing links in the panel: "Complete the documentation" (href /vault/[projectId]/documents) and "Prepare release metadata" (href /vault/[projectId]/metadata).

    Add a third link immediately after the metadata link (before the readiness progress bar div):

    The link:
    - `href={`/vault/${project.id}/rights`}`
    - Label text: "Rights & Registrations"
    - Arrow: `→`
    - Styling: identical to the existing two links — `mt-2 flex items-center justify-between rounded-lg border border-indigo-400/30 bg-indigo-400/10 px-3 py-2 text-sm text-indigo-200 transition hover:bg-indigo-400/15`

    This is the only change to this file. Do NOT alter any other part of the page.
  </action>
  <verify>
    <automated>grep -c 'rights' /Users/peterzora/Desktop/funun/app/\(artist\)/vault/\[projectId\]/page.tsx</automated>
  </verify>
  <done>The project page readiness panel contains a third Link with href pointing to /vault/[projectId]/rights. Existing links are unchanged. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → rights/page.tsx | Server component; only renders for authenticated user (supabase.auth.getUser() guard) |
| rights/page.tsx → vault_projects | SELECT filtered by .eq('user_id', user.id) — artist can only see their own project |
| RightsStatusPatch → PATCH /api/vault/[projectId]/rights | Covered by Plan 01 threat model (T-03-01, T-03-02, T-03-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Information Disclosure | rights/page.tsx server SELECT | medium | mitigate | `.eq('user_id', user.id)` on vault_projects SELECT; notFound() if null prevents data leakage |
| T-03-07 | Elevation of Privilege | CopyrightFiling "Mark as registered" button | medium | mitigate | RightsStatusPatch calls PATCH route which enforces ownership (T-03-01 mitigates at route level) |
| T-03-08 | Tampering | External deep-links (SOCAN/Songtrust assumed URLs) | low | accept | Links open in new tab with rel="noopener noreferrer"; URL correctness verified via Plan 03 checkpoint |
| T-03-09 | Spoofing | SOCAN/Songtrust URLs are [ASSUMED] | low | accept | Plan 03 checkpoint:human-verify gate blocks shipping until URLs are confirmed |
</threat_model>

<verification>
1. `/vault/[projectId]/rights` resolves to a server component page with no runtime errors
2. Project page readiness panel has the "Rights & Registrations" Link
3. Copyright section shows correct badge for each status value
4. PRO section shows all four PRO options with links
5. SoundExchange section shows seStatus derived from assessRdrReadiness()
6. Songtrust card renders with CWR link pointing to /vault/[projectId]/metadata/cwr
7. `npm run build && npm run lint` pass clean
</verification>

<success_criteria>
- All five ROADMAP Phase 3 success criteria are representable from the rights page:
  1. Copyright guide + status indicator (RIGHTS-01)
  2. PRO guide + four PRO options with links (RIGHTS-02)
  3. SoundExchange guide + auto-derived readiness status (RIGHTS-03)
  4. All checklists on one page with visual badges (RIGHTS-04)
  5. Songtrust card with CWR export link (SONGTRUST-01)
- TypeScript build passes with zero new errors
- ESLint passes clean
</success_criteria>

<output>
Create `.planning/phases/03-rights-guidance/03-02-SUMMARY.md` when done.
</output>
