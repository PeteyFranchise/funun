---
phase: 03-rights-guidance
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified: []
autonomous: false
requirements:
  - RIGHTS-02
  - SONGTRUST-01
must_haves:
  truths:
    - "SOCAN deep-link URL is confirmed as correct and navigates to the SOCAN membership/join page"
    - "Songtrust deep-link URL is confirmed as the correct Songtrust website"
    - "All four PRO links (ASCAP, BMI, SESAC, SOCAN) open in a new tab and land on the correct destination"
    - "Songtrust card CWR link navigates to the existing /vault/[projectId]/metadata/cwr page"
    - "Rights page renders end-to-end on a real vault project without JavaScript errors"
  artifacts: []
  key_links:
    - "SOCAN URL in rights/page.tsx PRO section confirmed or corrected"
    - "Songtrust URL in SongtrustGuideCard.tsx confirmed or corrected"
---

<objective>
Human verification checkpoint: confirm SOCAN and Songtrust deep-link URLs before Phase 3 ships, and smoke-test the full rights page end-to-end on a real project.

Purpose: Two URLs in the Phase 3 UI are marked [ASSUMED] in research — SOCAN (https://www.socan.com/music-creators/) and Songtrust (https://www.songtrust.com/). These must be validated by a human before artists follow them from the app. A broken deep-link in a rights-guidance context directly harms the artist's ability to register their work.
Output: Confirmed or corrected URLs in the two component files, plus sign-off that the rights page renders correctly for a real project.
</objective>

<execution_context>
@/Users/peterzora/Desktop/funun/.claude/gsd-core/workflows/execute-plan.md
@/Users/peterzora/Desktop/funun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/peterzora/Desktop/funun/.planning/phases/03-rights-guidance/03-02-SUMMARY.md
@/Users/peterzora/Desktop/funun/components/vault/SongtrustGuideCard.tsx
@/Users/peterzora/Desktop/funun/app/(artist)/vault/[projectId]/rights/page.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 02 built:
    - /vault/[projectId]/rights — per-project Rights & Registrations page with copyright, PRO, SoundExchange, and Songtrust guide cards
    - PRO section links to ASCAP, BMI, SESAC (verified from existing codebase), and SOCAN (URL assumed from training knowledge: https://www.socan.com/music-creators/)
    - Songtrust card links externally to https://www.songtrust.com/ (assumed from training knowledge) and internally to /vault/[projectId]/metadata/cwr
    - "Rights & Registrations" link added to each project's readiness panel
  </what-built>
  <how-to-verify>
    Step 1 — Verify SOCAN URL:
    Open a browser and navigate to: https://www.socan.com/music-creators/
    - If it loads the SOCAN membership or music creators page: URL is correct, no change needed.
    - If it 404s or redirects to the homepage: find the correct SOCAN membership URL (e.g., https://www.socan.com/join or https://www.socan.com/become-a-member/).
    - Update the SOCAN href in `app/(artist)/vault/[projectId]/rights/page.tsx` if the URL needs correction.

    Step 2 — Verify Songtrust URL:
    Open a browser and navigate to: https://www.songtrust.com/
    - If it loads the Songtrust website: URL is correct, no change needed.
    - If it 404s or redirects elsewhere: find the correct Songtrust URL and update it in `components/vault/SongtrustGuideCard.tsx`.

    Step 3 — Smoke test the rights page:
    Start the dev server (npm run dev) and open a vault project that has:
    - At least one track with an ISRC code and performer credits
    - A project type of single, EP, or album (not snippet/unreleased)

    Navigate to: http://localhost:3000/vault/[your-project-id]/rights

    Check:
    a. Page loads without a JavaScript console error
    b. "Copyright Registration" section is visible with a badge and the eCO link
    c. "PRO Registration" section shows ASCAP, BMI, SESAC, SOCAN options
    d. "SoundExchange" section shows "Ready to register" status (amber badge) for the project with ISRC + performers
    e. "Songtrust" section shows the guide card with "Download CWR file →" link and "Learn about Songtrust →" link
    f. Click "Download CWR file →" — confirm it navigates to /vault/[projectId]/metadata/cwr (the existing CWR page)
    g. On the project's main page (/vault/[projectId]), confirm the readiness panel contains a "Rights & Registrations" link

    Step 4 — Test status update (optional but recommended):
    On the rights page, click "Mark as registered" under PRO Registration.
    - Confirm the page refreshes and the badge updates to "Registered".
  </how-to-verify>
  <resume-signal>
    Type "approved" if all URLs are correct and the page renders as described.
    If URLs needed correction, type "approved — updated SOCAN to [new URL]" or "approved — updated Songtrust to [new URL]" so the correction is recorded.
    If the page has rendering issues, describe the specific problem.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artist browser → SOCAN external site | External link; new tab, rel="noopener noreferrer" prevents opener access |
| Artist browser → Songtrust external site | External link; same protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Tampering | SOCAN/Songtrust external URLs | low | mitigate | Human verification in this checkpoint confirms correct destinations before Phase 3 ships |
| T-03-11 | Repudiation | No record of which URL was confirmed | low | accept | Resume signal captures confirmed URL; SUMMARY.md records it |
</threat_model>

<verification>
Human confirms:
1. SOCAN URL loads the correct SOCAN membership page (or URL has been corrected)
2. Songtrust URL loads the correct Songtrust website (or URL has been corrected)
3. Rights page renders end-to-end without errors for a real project
4. All four sections (copyright, PRO, SoundExchange, Songtrust) are visible
5. CWR link navigates correctly to /vault/[projectId]/metadata/cwr
6. "Rights & Registrations" nav link is present on the project home page
</verification>

<success_criteria>
- SOCAN and Songtrust URLs verified as correct (or corrected and committed)
- Rights page smoke test passes for a real project with ISRC + performer data
- Phase 3 is ready to close
</success_criteria>

<output>
Create `.planning/phases/03-rights-guidance/03-03-SUMMARY.md` when done.
</output>
