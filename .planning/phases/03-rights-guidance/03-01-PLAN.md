---
phase: 03-rights-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/024_vault_project_rights_status.sql
  - types/index.ts
  - app/api/vault/[projectId]/rights/route.ts
autonomous: true
requirements:
  - RIGHTS-01
  - RIGHTS-02
  - RIGHTS-03
must_haves:
  truths:
    - "vault_projects has copyright_status TEXT DEFAULT 'not_filed' with CHECK constraint"
    - "vault_projects has pro_registration_status TEXT DEFAULT 'not_registered' with CHECK constraint"
    - "vault_projects has soundexchange_registered BOOLEAN DEFAULT false"
    - "VaultProject type in types/index.ts includes the three new fields with correct TypeScript types"
    - "PATCH /api/vault/[projectId]/rights returns 200 with only copyright_status, pro_registration_status, or soundexchange_registered in request body"
    - "PATCH rejects requests from users who do not own the project (401 or 403)"
  artifacts:
    - supabase/migrations/024_vault_project_rights_status.sql
    - app/api/vault/[projectId]/rights/route.ts
  key_links:
    - "types/index.ts VaultProject.copyright_status feeds the rights page server component"
    - "PATCH route is the single write path for all three rights status fields"
---

<objective>
Add the three per-project rights registration status columns to vault_projects, extend the VaultProject TypeScript type, and create the PATCH API route that the rights page uses to update status.

Purpose: Establishes the data layer for Phase 3. The UI in Plans 02 cannot persist status without this foundation.
Output: Migration 024 SQL file, updated VaultProject type, and PATCH route at /api/vault/[projectId]/rights.
</objective>

<execution_context>
@/Users/peterzora/Desktop/funun/.claude/gsd-core/workflows/execute-plan.md
@/Users/peterzora/Desktop/funun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/peterzora/Desktop/funun/.planning/PROJECT.md
@/Users/peterzora/Desktop/funun/.planning/ROADMAP.md
@/Users/peterzora/Desktop/funun/.planning/STATE.md
@/Users/peterzora/Desktop/funun/types/index.ts
@/Users/peterzora/Desktop/funun/app/api/vault/[projectId]/route.ts
@/Users/peterzora/Desktop/funun/supabase/migrations/023_vault_documents_file_url.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 024 — three rights status columns on vault_projects</name>
  <files>supabase/migrations/024_vault_project_rights_status.sql</files>
  <action>
    Create `supabase/migrations/024_vault_project_rights_status.sql` adding three columns to `vault_projects`.

    The file must use `ADD COLUMN IF NOT EXISTS` for each column:

    1. `copyright_status TEXT DEFAULT 'not_filed'` with CHECK constraint allowing exactly three values: not_filed, filed, registered. This is a 3-state enum: the artist tracks whether they have submitted an application (filed) or received the registration certificate (registered).

    2. `pro_registration_status TEXT DEFAULT 'not_registered'` with CHECK constraint allowing exactly two values: not_registered, registered. Binary for MVP — the artist either belongs to a PRO or does not.

    3. `soundexchange_registered BOOLEAN DEFAULT false`. This is an artist override: the rights page auto-derives "ready to register" from RDR-N data, but the artist can mark themselves as already registered via this boolean.

    Add a SQL comment block at the top explaining the purpose and that these columns do NOT feed into calculate_vault_readiness() — they track registration actions, not data completeness.

    Do NOT add new RLS policies (vault_projects already has USING (auth.uid() = user_id) covering all writes).
    Do NOT add triggers.
    Do NOT modify calculate_vault_readiness().

    Follow the header comment style of migration 023: brief description, date, migration number.
  </action>
  <verify>
    <automated>grep -c 'copyright_status' /Users/peterzora/Desktop/funun/supabase/migrations/024_vault_project_rights_status.sql && grep -c 'pro_registration_status' /Users/peterzora/Desktop/funun/supabase/migrations/024_vault_project_rights_status.sql && grep -c 'soundexchange_registered' /Users/peterzora/Desktop/funun/supabase/migrations/024_vault_project_rights_status.sql</automated>
  </verify>
  <done>Migration file exists with all three columns, correct defaults, and CHECK constraints on the text enum columns.</done>
</task>

<task type="auto">
  <name>Task 2: Extend VaultProject type with three new fields</name>
  <files>types/index.ts</files>
  <action>
    Add three new optional fields to the `VaultProject` type in `types/index.ts`, after the existing `distributor` field and before the `tracks` relations block.

    The three fields:
    - `copyright_status: 'not_filed' | 'filed' | 'registered' | null`
    - `pro_registration_status: 'not_registered' | 'registered' | null`
    - `soundexchange_registered: boolean | null`

    All three are nullable because existing project rows will have these columns as NULL until the migration sets defaults (Supabase applies defaults on INSERT, not retroactively on existing rows). The rights page server component must handle null as the unset/default state.

    Add a comment above the three fields: `// Rights registration status (migration 024)` to match the comment style used for other migration groups (e.g., the `// Metadata Studio` and `// Distribution` groups already in the type).

    Do NOT change any other type in the file. Do NOT add any new exported constants or types.
  </action>
  <verify>
    <automated>cd /Users/peterzora/Desktop/funun && npm run build 2>&1 | grep -E "error TS|copyright_status|pro_registration_status|soundexchange_registered" | head -10 || echo "Build check complete"</automated>
  </verify>
  <done>TypeScript build passes with zero new errors. VaultProject.copyright_status, VaultProject.pro_registration_status, and VaultProject.soundexchange_registered are present in the type.</done>
</task>

<task type="auto">
  <name>Task 3: PATCH /api/vault/[projectId]/rights route</name>
  <files>app/api/vault/[projectId]/rights/route.ts</files>
  <action>
    Create `app/api/vault/[projectId]/rights/route.ts` as a Next.js 15 API route with a single `PATCH` handler.

    Requirements:
    - Export `async function PATCH(request: Request, { params }: { params: Promise<{ projectId: string }> })` — params is a Promise per Next.js 15 convention; await it.
    - Use `createApiClient()` from `@/lib/supabase/server` (not createServerClient — the API client binds to the request auth cookies).
    - Call `supabase.auth.getUser()` and return `NextResponse.json({ error: 'Unauthorized' }, { status: 401 })` if no user.
    - Parse the request body with `await request.json()`.
    - Define `ALLOWED_FIELDS` constant: `['copyright_status', 'pro_registration_status', 'soundexchange_registered']`.
    - Build an `update` object by iterating ALLOWED_FIELDS — only copy keys that exist in the body. This prevents the caller from injecting arbitrary columns.
    - Validate enum values before writing: if `body.copyright_status` is present, verify it is one of `not_filed`, `filed`, `registered`; if `body.pro_registration_status` is present, verify it is one of `not_registered`, `registered`. Return `{ error: 'Invalid status value' }` with status 400 if validation fails.
    - Execute `.update(update).eq('id', projectId).eq('user_id', user.id)` — the `.eq('user_id', user.id)` enforces ownership and prevents IDOR.
    - If Supabase returns an error, return `NextResponse.json({ error: error.message }, { status: 500 })`.
    - On success return `NextResponse.json({ ok: true })`.
    - No GET handler in this file — this route is write-only.

    Follow the no-semicolons, 2-space indent code style. Use named export (not default export).

    Import `NextResponse` from `'next/server'` and `createApiClient` from `'@/lib/supabase/server'`.
  </action>
  <verify>
    <automated>cd /Users/peterzora/Desktop/funun && npm run build 2>&1 | grep -E "error TS|rights/route" | head -10 || echo "Build check complete"</automated>
  </verify>
  <done>Route file exists. TypeScript build passes. The route rejects requests with no user (401), bad enum values (400), and mismatched user_id (rows affected = 0 — Supabase silently returns success but updates nothing, which is the correct IDOR defense for this pattern).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PATCH /api/vault/[projectId]/rights | Artist submits status updates; request body is untrusted |
| Supabase auth cookie → API handler | Session cookie parsed by createApiClient(); invalid/expired sessions return no user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | PATCH /rights route | high | mitigate | `.eq('user_id', user.id)` on every UPDATE — artist A cannot update artist B's project status |
| T-03-02 | Tampering | copyright_status PATCH body | medium | mitigate | Allowlist validation in route handler before write; Supabase CHECK constraint as defense in depth |
| T-03-03 | Tampering | pro_registration_status PATCH body | medium | mitigate | Same allowlist validation pattern as T-03-02 |
| T-03-04 | Spoofing | Supabase auth cookie | low | accept | Existing middleware enforces session validity on all artist routes; createApiClient() re-validates on each request |
| T-03-05 | Information Disclosure | Migration 024 schema | low | accept | Schema changes are server-side only; CHECK constraints are implementation detail not exposed to clients |
</threat_model>

<verification>
1. `supabase/migrations/024_vault_project_rights_status.sql` exists and contains all three columns with correct constraints
2. `types/index.ts` VaultProject type includes copyright_status, pro_registration_status, soundexchange_registered with correct union types and nullable
3. `app/api/vault/[projectId]/rights/route.ts` exists with PATCH handler
4. `npm run build` passes with zero TypeScript errors
5. `npm run lint` passes with zero new ESLint errors
</verification>

<success_criteria>
- Migration 024 SQL file exists and is syntactically correct
- VaultProject type updated — no TypeScript errors on build
- PATCH route exists, uses createApiClient, validates enum values, enforces ownership with .eq('user_id', user.id)
- `npm run build && npm run lint` pass clean
</success_criteria>

<output>
Create `.planning/phases/03-rights-guidance/03-01-SUMMARY.md` when done.
</output>
