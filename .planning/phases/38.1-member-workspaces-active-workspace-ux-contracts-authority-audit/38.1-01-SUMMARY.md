---
phase: 38.1-member-workspaces-active-workspace-ux-contracts-authority-audit
plan: 01
subsystem: member-workspaces
status: complete
requirements: [WS-13, WS-14, WS-16]
completed: 2026-09-12
---

# Plan 38.1-01 Summary — Active Workspace Context and Persistent Chrome

## What changed

- Added a server-resolved active workspace context carried exclusively by `/w/[workspaceId]`.
- Added a Personal/workspace switcher that changes URLs without signing out or writing browser storage.
- Added persistent workspace navigation and identity chrome showing workspace name, type, and current role.
- Added a protected workspace landing route and extended middleware authentication coverage to `/w`.
- Kept Personal/Funūn Team Member identity switching separate and unchanged.
- Split client-safe navigation types and helpers from server-only Supabase and authorization loaders so the workspace switcher cannot pull server dependencies into the client bundle.

## Validation

- `npm test -- --runInBand lib/workspaces/active-context.test.ts lib/workspaces/access.test.ts lib/workspaces/access-kill-switch.test.ts lib/workspaces/cohort.test.ts __tests__/workspace-structural-exclusions.test.ts` — 5 suites, 248 tests passed.
- Focused ESLint across every changed application, component, workspace library, test, and middleware file — passed with zero warnings.
- Static boundary check for session-identity imports, browser storage, Supabase imports, and server workspace authorization imports in the client switcher path — passed.
- `npm run typecheck` — passed from a cold compiler cache.
- `git diff --check` — passed.

## Deferred human verification

- Sign in as a pilot Member with multiple active workspace seats.
- Switch between Personal and each workspace without reauthentication.
- Confirm persistent workspace identity at desktop and mobile widths.
- Confirm a stale, expired, inaccessible, or malformed workspace URL leaks no workspace details.

## Remaining work

- Plan 38.1-02 adds Roster and Activity as sibling workspace views using one consistent read model.
