---
phase: 17-split-sheet-esign
plan: 05
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - app/(artist)/contracts/page.tsx
  - components/contracts/ContractLocker.tsx
  - lib/split-sheets/distribution.ts
  - lib/split-sheets/distribution.test.ts
  - app/api/split-sheets/[id]/attach/route.ts
  - app/api/split-sheets/[id]/reconcile/route.ts
  - components/split-sheets/ReconcileDiff.tsx
  - __tests__/contracts-standalone-docs.test.ts
autonomous: true
requirements: [ESIGN-10, ESIGN-11, ESIGN-12]

must_haves:
  truths:
    - "Contract Locker surfaces a standalone (project_id IS NULL) split-sheet document via a second direct vault_documents query merged into the same rows, since the existing project-nested query can never reach a projectless row (RESEARCH Pitfall 2, gap fix 2, P17-05)."
    - "The cross-account fan-out row builder produces one vault_documents row per Funūn-account party pointing at the SAME storage path, each scoped to that party's user_id, satisfying the evidence-guard (file_url + document_data.esign.completedAt) (P17-06, RESEARCH Pattern 3)."
    - "A standalone executed sheet can be attached to a vault project only by a user who is BOTH a party on the sheet AND the owner of the destination project — the document follows the song (P17-05/P17-05a, RESEARCH Pattern 4)."
    - "Write-back into tracks.metadata.composers[] is OFFERED via a confirmable diff, never silent, and an unresolved mismatch renders a visible warning (P17-07)."
  artifacts:
    - lib/split-sheets/distribution.ts
    - app/api/split-sheets/[id]/attach/route.ts
    - app/api/split-sheets/[id]/reconcile/route.ts
    - components/split-sheets/ReconcileDiff.tsx
  key_links:
    - "buildFanoutRows() → consumed by the webhook (17-07) to insert per-party locker rows."
    - "Contract Locker direct query (project_id IS NULL) → renders the standalone sheet the webhook filed."
    - "reconcile route → reconcileSplits diff (17-01) → confirmed composers[] write."
---

<objective>
Close the two distribution gaps and deliver the offered write-back: make Contract Locker able to render a projectless standalone sheet (RESEARCH Pitfall 2 / gap fix 2, P17-05), provide the cross-account fan-out row builder (P17-06) and the attach-later route (P17-05/P17-05a), and build the never-silent reconciliation diff route + UI (P17-07). All credential-free — the webhook that actually fans out and files documents lands in 17-07 and reuses this plan's helper.

Purpose: P17-05's "standalone sheet lands unattached in the initiator's locker" promise depends on a query path that does not exist today (Contract Locker only reaches vault_documents by joining FROM vault_projects). P17-06 needs a fan-out primitive. P17-07 needs an offered, confirmable diff that never mutates registration-feeding composers[] silently. This plan builds all three so 17-07 only wires I/O.
Output: The Contract Locker standalone query fix, the fan-out builder + attach route with tests, and the reconcile route + ReconcileDiff UI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-split-sheet-esign/17-CONTEXT.md
@.planning/phases/17-split-sheet-esign/17-RESEARCH.md
@.planning/phases/17-split-sheet-esign/17-01-SUMMARY.md
@.planning/phases/17-split-sheet-esign/17-02-SUMMARY.md

# The locker surface + the ownership-check precedent + the evidence guard
@app/(artist)/contracts/page.tsx
@components/contracts/ContractLocker.tsx
@app/api/split-sheets/[id]/send-for-approval/route.ts
@lib/split-sheets/reconciliation.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract Locker standalone (projectless) document query</name>
  <files>app/(artist)/contracts/page.tsx, components/contracts/ContractLocker.tsx, __tests__/contracts-standalone-docs.test.ts</files>
  <behavior>
    The Contract Locker page assembles rows from TWO sources: the existing project-nested vault_documents query AND a second direct query of vault_documents WHERE user_id = user.id AND project_id IS NULL.
    - A standalone split-sheet row (project_id NULL) appears in the rows list with an "unattached" affordance.
    - Rows are merged/de-duplicated so a document never appears twice.
    - When DEMO mode is on, behavior is unchanged.
  </behavior>
  <action>
    Fix app/(artist)/contracts/page.tsx per RESEARCH Pitfall 2: the current query starts at vault_projects and nests vault_documents, so a project_id IS NULL row is unreachable no matter how correctly it was inserted (gap fix 2, P17-05). Add a SECOND direct query — vault_documents filtered by user_id = user.id AND project_id IS NULL — and merge its rows into the same ContractRow[] the locker already renders. Give standalone rows an "unattached" label/affordance (they have no project title) and a hook into the attach flow (Task 2's route) by href/handler. Keep the project-nested query and all existing sorting/verification logic intact; only ADD the standalone source and merge.

    Update components/contracts/ContractLocker.tsx as needed to render an unattached row (no projectTitle) and expose an "Attach to a project" affordance for standalone split sheets.

    Write __tests__/contracts-standalone-docs.test.ts as a query-shape/merge test (mock the Supabase client per this repo's existing route-test style): assert that a projectless vault_documents row is included in the assembled rows and that a document appearing in both sources is not duplicated. RED first, then implement.
  </action>
  <verify>
    <automated>npx jest __tests__/contracts-standalone-docs.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Contract Locker renders a projectless standalone split-sheet document via the added direct query, with no duplication and an unattached affordance; DEMO mode is unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-account fan-out row builder + attach-later route</name>
  <files>lib/split-sheets/distribution.ts, lib/split-sheets/distribution.test.ts, app/api/split-sheets/[id]/attach/route.ts</files>
  <behavior>
    buildFanoutRows({ parties, sheet, executedFileUrl, auditTrailUrl, completedAt, requestId }):
    - Returns one vault_documents insert shape per party whose user_id is non-null (account holders only); zero for parties without an account.
    - Every row: user_id = party.user_id, project_id = sheet.vault_project_id (NULL for standalone), type 'split_sheet', status 'signed', signed_at = completedAt, file_url = executedFileUrl, and document_data.esign populated so the evidence guard is satisfied (file_url + document_data.esign.completedAt).
    - All rows point at the SAME storage path (no file duplication — only DB rows).
    Attach route authorization:
    - 401 without a session; 403 unless the caller is a party on the sheet (initiator or account-holder signer) AND owns the destination project (double check).
    - On success: sets split_sheets.vault_project_id and updates the caller's own vault_documents row's project_id so the destination project's readiness recomputes (existing trigger).
  </behavior>
  <action>
    Create lib/split-sheets/distribution.ts exporting buildFanoutRows per RESEARCH Pattern 3 — one duplicate vault_documents ROW per Funūn-account party (split_sheet_parties.user_id IS NOT NULL), each scoped to that party's user_id, all pointing at the same storage path (no file duplication). Populate document_data.esign in the shape lib/esign/provider.ts already defines (provider 'docuseal', requestId, signers, signedFileUrl, auditTrailUrl, completedAt) so vault_documents_status_requires_evidence_chk passes automatically (RESEARCH Pattern 3). This is a pure builder; 17-07's webhook applies the rows via the service client.

    Create app/api/split-sheets/[id]/attach/route.ts per RESEARCH Pattern 4, mirroring send-for-approval's ownership-check-with-session-client-then-service-write structure: verify the caller is a party on the sheet (initiator OR an account-holder signer) with the session client, AND verify they own the target vault_project, THEN switch to the service client to set split_sheets.vault_project_id and update the caller's own split-sheet vault_documents row's project_id (the document follows the song — P17-05/P17-05a). Both ownership checks are mandatory (V4 double check); reject with 403 if either fails. Respect the Phase 13 block doctrine — no "who blocked me"-style leak on this cross-user surface; the response reveals nothing about other parties beyond what the sheet already exposes to a party.

    Write lib/split-sheets/distribution.test.ts RED first covering the account-holder-only fan-out, the same-path invariant, and the evidence-guard shape; also assert the attach authorization matrix via a mocked-client route test. Implement to green.
  </action>
  <verify>
    <automated>npx jest lib/split-sheets/distribution.test.ts && npx tsc --noEmit && npm run lint && grep -Eq "vault_project_id" "app/api/split-sheets/[id]/attach/route.ts"</automated>
  </verify>
  <done>buildFanoutRows emits account-holder-only rows sharing one storage path and satisfying the evidence guard; the attach route enforces the party-AND-owner double check and moves the document + readiness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Offered (never-silent) reconciliation diff route + UI</name>
  <files>app/api/split-sheets/[id]/reconcile/route.ts, components/split-sheets/ReconcileDiff.tsx</files>
  <action>
    Create app/api/split-sheets/[id]/reconcile/route.ts: gated by the session client to the sheet's initiator/party and the target project's owner, it computes the diff via reconcileSplits from lib/split-sheets/reconciliation.ts (17-01) comparing the executed split_sheet_parties against the project's tracks.metadata.composers[], and returns it for display. A SEPARATE confirm action (a distinct request shape) applies ONLY the artist-confirmed changes to composers[] via the service client — never a silent or automatic write (P17-07). The route must let the artist re-map an unmatched/ambiguous party to a specific composer row rather than guessing, since matching is by normalized name only and no id link exists (RESEARCH Pitfall 4).

    Create components/split-sheets/ReconcileDiff.tsx: a client component that renders the diff (matched pairs with equal/mismatch, unmatched parties, extra composers), lets the artist confirm or dismiss and manually re-map ambiguous rows, and only POSTs the confirmed set. Surface the visible mismatch warning until resolved, extending Contract Locker's existing ≠100% cross-check to also compare against the executed sheet (P17-07). Do not mutate composers[] anywhere in the UI — the write happens only through the confirm action server-side.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -Eq "reconcileSplits" "app/api/split-sheets/[id]/reconcile/route.ts"</automated>
    <human-check>Open the reconcile diff for an executed sheet whose composers[] differs; confirm the mismatch warning shows, a dismiss leaves composers[] untouched, and only an explicit confirm writes the change.</human-check>
  </verify>
  <done>The reconcile route computes the name-matched diff and applies only artist-confirmed changes; the UI shows matched/unmatched/mismatch, supports manual re-map, and never mutates composers[] without confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| party session → attach action | A cross-user action moving a shared legal document onto a project; requires party-AND-owner authorization. |
| initiator session → composers[] write | The confirm action writes registration-feeding data; must apply only explicitly confirmed changes. |
| standalone doc → locker read | A projectless document must be reachable only by its own user_id-scoped rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-12 | Elevation of Privilege | attaching a sheet to a project the caller does not own | high | mitigate | Double ownership check (party-of-sheet AND owner-of-project) with the session client before any service-role write (RESEARCH Pattern 4 / V4). |
| T-17-13 | Tampering | silent write-back overwriting composers[] | high | mitigate | Diff is display-only; composers[] changes apply solely through an explicit confirm action, unit/flow-asserted never to auto-write (P17-07). |
| T-17-14 | Information Disclosure | cross-user block leak on the shared-document surface | medium | mitigate | Attach/reconcile responses expose nothing about other parties beyond existing sheet visibility; Phase 13 block doctrine respected. |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan. |
</threat_model>

<verification>
- `npx jest __tests__/contracts-standalone-docs.test.ts lib/split-sheets/distribution.test.ts` green.
- `npx tsc --noEmit` and `npm run lint` clean.
- Full-suite regression gate: `npm test` green (baseline 455+ tests).
- Manual (human-check): reconcile diff never writes composers[] without explicit confirm.
</verification>

<success_criteria>
A standalone executed split sheet is visible and attachable in Contract Locker, every account-holding party can be filed a copy via the fan-out builder, and the artist is offered a confirmable splits diff that never silently mutates composers[].
</success_criteria>

<output>
Create `.planning/phases/17-split-sheet-esign/17-05-SUMMARY.md` when done.
</output>
