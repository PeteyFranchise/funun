---
phase: 17-split-sheet-esign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/vault/pdf/split-sheet.tsx
  - lib/vault/pdf/split-sheet.test.ts
autonomous: true
requirements: [ESIGN-02]

must_haves:
  truths:
    - "A split sheet renders to a real PDF buffer server-side from split_sheets/split_sheet_parties/collaborator data, cloning the metadata-sheet/credits-sheet precedent (ESIGN-02)."
    - "Every party row shows name, role, PRO, IPI, and split % — the data already captured in migration 018 — matching the credits-sheet column layout."
    - "Each party gets a signature field rendered as a DocuSeal text-tag literal at its signature line, so the ONLY path to a DocuSeal template is Funūn's own renderer (AM-2 template-only by construction)."
    - "The renderer runs in the Node runtime (renderToBuffer), never Edge (RESEARCH Pitfall 6)."
  artifacts:
    - lib/vault/pdf/split-sheet.tsx
  key_links:
    - "SplitSheetDocument + partyRoleTag() → consumed by the mint route (17-06) which submits the buffer to DocuSeal /templates/pdf and maps submitters to the same role tags."
---

<objective>
Build the missing split-sheet PDF renderer (ESIGN-02) alongside the existing lib/vault/pdf/metadata-sheet.tsx and credits-sheet.tsx — a server-side @react-pdf/renderer document with a per-party row (name, role, PRO, IPI, split %) and a DocuSeal signature text-tag per party, plus a stable role-tag helper the mint route reuses.

Purpose: This is the artifact the whole two-step flow hangs on: unanimous approval (or fast-lane send) renders this PDF, embeds text-tag signature fields per party, and posts it to DocuSeal's PDF-template API. Because the artist never supplies a PDF and Funūn's renderer is the only path to the template API, AM-2's "Funūn split-sheet template only" guardrail holds by construction. No credentials are needed to build or test this.
Output: lib/vault/pdf/split-sheet.tsx (renderer + role-tag helper) with a test asserting a valid PDF buffer and the per-party fields/tags.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-split-sheet-esign/17-CONTEXT.md
@.planning/phases/17-split-sheet-esign/17-RESEARCH.md

# The two PDF renderers to clone + the party/schema shapes
@lib/vault/pdf/credits-sheet.tsx
@lib/metadata/schema.ts
@supabase/migrations/018_collaborators_split_sheets.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split-sheet PDF document + stable per-party role-tag helper</name>
  <files>lib/vault/pdf/split-sheet.tsx, lib/vault/pdf/split-sheet.test.ts</files>
  <behavior>
    partyRoleTag(index): returns a deterministic, DocuSeal-safe role identifier per party position (e.g. Party1, Party2 …) so the PDF text tag and the mint submitters[] entry agree.
    SplitSheetDocument({ songName, projectTitle, initiatorName, parties }):
    - Renders a header (song/project/initiator) mirroring credits-sheet's header block.
    - Renders one table row per party with name, role label, PRO label, IPI (or em dash), and split %.
    - Renders a signature line per party carrying the DocuSeal signature text tag bound to that party's role tag.
    - Renders a total-splits line and the Funūn confidential footer, matching credits-sheet styling.
    renderSplitSheet(input): returns a PDF Buffer via renderToBuffer.
  </behavior>
  <action>
    Create lib/vault/pdf/split-sheet.tsx by cloning the structure, StyleSheet, and footer of lib/vault/pdf/credits-sheet.tsx (consistent branding across all three generated PDFs — RESEARCH Don't Hand-Roll; do not introduce a second PDF library). Populate rows from the split_sheet_parties shape (name, role, pro, ipi, split_percentage from migration 018), reusing PRO_LABELS and the role labels from lib/metadata/schema.ts for display; include PRO/IPI per party per CONTEXT (the data is already captured). Export a renderSplitSheet(input) that returns a Buffer via renderToBuffer, and export SplitSheetDocument for testability, exactly as credits-sheet does.

    Render each party's signature line as a literal DocuSeal signature text tag bound to partyRoleTag(index) at the signature position — this is what makes Funūn's renderer the sole path into DocuSeal's template API and satisfies AM-2's template-only guardrail by construction (RESEARCH Pattern 2). Export partyRoleTag so 17-06's mint route maps each submitters[] entry to the identical role tag. Add a header comment that this module must run under the default Node runtime — never add an Edge runtime export to any route that calls it, because renderToBuffer depends on Node built-ins (RESEARCH Pitfall 6).

    Write split-sheet.test.ts RED first: assert renderSplitSheet returns a non-empty Buffer whose PDF header bytes are present, that partyRoleTag is stable and unique per index, and (via SplitSheetDocument prop inspection or a rendered-text assertion) that each party contributes a row and a role-tagged signature field. Then implement to green.
  </action>
  <verify>
    <automated>npx jest lib/vault/pdf/split-sheet.test.ts && npx tsc --noEmit && npm run lint && test -f lib/vault/pdf/split-sheet.tsx</automated>
  </verify>
  <done>renderSplitSheet produces a valid PDF buffer with per-party name/role/PRO/IPI/split rows and one role-tagged DocuSeal signature field per party; partyRoleTag is exported, deterministic, and unique; the module is documented Node-runtime-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server DB state → rendered PDF | The PDF is derived only from server-side split-sheet DB state; no client-supplied file bytes ever reach it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-04 | Tampering | arbitrary-PDF injection bypassing AM-2's template-only guardrail | high | mitigate | The renderer accepts only structured DB state (never a file), and it is the sole producer of the buffer the mint route submits to DocuSeal — there is no code path where client-supplied bytes become a template (RESEARCH Security Domain). |
| T-17-05 | Denial of Service | PDF render failing under the Edge runtime | medium | mitigate | Documented Node-runtime-only; the calling mint route (17-06) mirrors the export-pack route's default Node runtime (RESEARCH Pitfall 6). |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — @react-pdf/renderer is already a project dependency. |
</threat_model>

<verification>
- `npx jest lib/vault/pdf/split-sheet.test.ts` green.
- `npx tsc --noEmit` and `npm run lint` clean.
- Full-suite regression gate: `npm test` green (baseline 455+ tests).
</verification>

<success_criteria>
Funūn can render a branded, per-party split-sheet PDF with PRO/IPI and DocuSeal signature text-tags entirely from server-side data, with no new dependency and no credentials.
</success_criteria>

<output>
Create `.planning/phases/17-split-sheet-esign/17-03-SUMMARY.md` when done.
</output>
