---
phase: 17-split-sheet-esign
plan: 06
type: execute
wave: 5
depends_on: ["17-01", "17-02", "17-03", "17-04", "17-09", "17-10"]
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - lib/esign/docuseal.ts
  - lib/esign/docuseal.test.ts
  - app/api/split-sheets/[id]/mint-envelope/route.ts
  - app/api/split-sheets/[id]/void/route.ts
  - components/split-sheets/SplitSheetSigningEmbed.tsx
  - components/split-sheets/SplitApprovalView.tsx
autonomous: false
requirements: [ESIGN-01, ESIGN-04, ESIGN-05, ESIGN-06, ESIGN-13, ESIGN-18]

user_setup:
  - service: docuseal
    why: "First live e-sign integration (AM-5, D-18b). DocuSeal hosted API + embedded signing. No keyless sandbox — template/submission creation and the webhook secret require a real account."
    env_vars:
      - name: DOCUSEAL_API_KEY
        source: "DocuSeal dashboard -> API -> X-Auth-Token"
      - name: DOCUSEAL_WEBHOOK_SECRET
        source: "DocuSeal dashboard -> Webhooks (used by the 17-07 webhook route)"
      - name: ESIGN_FROM_EMAIL
        source: "Funūn's monitored e-sign mailbox (esign@funun.studio) — the sender and reply-to for Funūn's own signature invites (17-10, P17-10)"
    dashboard_config:
      - task: "Create the DocuSeal trial/Pro account and generate an API token (hosted ~$0.20/completed-doc tier; multi-party = one completion)"
        location: "docuseal.com -> account -> API"
      - task: "Run the provider-verification trial: inspect a real Certificate of Signature, confirm white-label scope/price, run a 3-signer async multi-party template test, check deliverability, and confirm whether VOIDED/archived submissions bill"
        location: "docuseal.com dashboard + a live 3-signer test submission"
      - task: "PURCHASE the DocuSeal Pro plan ($20/user/mo + $0.20/completion — matches AM-1's assumed economics). REQUIRED BEFORE ANY REAL ARTIST USE, not merely for white-label: the provider gate confirmed the free tier shows a 'Developer Sandbox — Upgrade to start using in Production' banner on the signing surface. Pro also unlocks white-labeling the embedded form itself (logo, custom domain), which is the paid half of P17-10. Pete performs this purchase — NO PLAN TASK MAY ATTEMPT IT."
        location: "docuseal.com -> billing -> Pro"

must_haves:
  truths:
    - "DocuSeal is implemented as a plain fetch adapter behind the existing EsignProvider interface — callers import no vendor server SDK (ESIGN-01, D-18b, 16-09 reuses the pattern)."
    - "Envelope mint renders the Funūn split-sheet PDF, posts it to /templates/pdf then /submissions with order 'random' (parallel async), and persists esign_envelopes + esign_envelope_signers rows (ESIGN-04, RESEARCH Pattern 2)."
    - "The submission is minted with send_email disabled and per-submitter reply_to set to Funūn's e-sign mailbox; Funūn sends its own branded invite per signer via sendSignatureInvite (17-10) — the collaborator never receives provider-branded mail (P17-10, ESIGN-18)."
    - "The mint route calls assertCounselReviewedForProduction (17-09) before any DocuSeal call, so unreviewed operative agreement language can never reach a real artist's signature in production (P17-09a)."
    - "The rendered PDF is the full legal-grade agreement (17-09): the mint route builds the complete SplitSheetAgreementInput — rights scope, agreement date, ISWC/ISRC from the linked track, sample disclosure, and per-party legal name / publisher / writer / publisher / master shares — not the reduced pre-063 input (ESIGN-16)."
    - "The mint route enforces the ~10/mo cap server-side before minting and the fast lane backfills approval from signature; the void route archives the DocuSeal submission and resets the sheet, marking the envelope voided (not deleted) for audit (P17-01/P17-02, AM-2, ESIGN-13)."
    - "Signing is embedded and mobile-first — a party signs inside Funūn via @docuseal/react at a 375px viewport, never redirected (D-18b, ESIGN-06, spike 006a)."
    - "DOCUSEAL_API_KEY is server-only; the client embed receives only a per-signer scoped src/token, never the API key (RESEARCH V13)."
  artifacts:
    - lib/esign/docuseal.ts
    - app/api/split-sheets/[id]/mint-envelope/route.ts
    - app/api/split-sheets/[id]/void/route.ts
    - components/split-sheets/SplitSheetSigningEmbed.tsx
  key_links:
    - "renderSplitSheet (17-03, rebuilt as the legal-grade agreement in 17-09) -> /templates/pdf -> /submissions -> esign_envelopes/esign_envelope_signers rows -> per-signer embed src."
    - "checkMonthlyCap + buildFastLaneBackfill + buildVoidReset (17-01) -> applied by the mint/void routes."
    - "assertCounselReviewedForProduction (17-09) -> called by the mint route before any DocuSeal call."
    - "sendSignatureInvite (17-10) -> called per signer after envelope/signer rows persist, replacing the provider's own invite email."
    - "SplitSheetSigningEmbed -> mounted into SplitApprovalView's 'sign' region (17-04)."
---

<cap_semantics_amended>
**AMENDED 2026-07-20 — AM-2c supersedes this plan's document-count cap.**

The cap enforced at the mint route is **NOT** "~10 documents per month." It is a limit on **distinct NEW recipients per month** — email addresses this initiator has never sent a split sheet to before. Rationale in `.planning/FINANCIALS.md` §5 and the deliberation doc's AM-2c.

- Regular collaborators stop counting after the first send; a stable writing team costs nothing ongoing.
- A 12-track album sent to the same 3 bandmates = **3 recipients**, not 12 documents. This is the case a document cap broke (P18-15 requires a sheet per track).
- **No binding document cap.** Do not also enforce a document ceiling — AM-3's $500/mo aggregate trigger is the cost monitor.
- **Uploads are exempt** — they mint no envelope, so they never touch the limit.
- Proposed number: **~25 new recipients/month**, NOT yet confirmed by Pete. Keep it a single named constant in `lib/split-sheets/envelopes.ts` so tuning is a one-line change; surface the current value in the admin telemetry from 17-07.
- `lib/split-sheets/envelopes.ts`'s `checkMonthlyCap()` (built in 17-01 with document-count semantics) must be reworked to count distinct new recipients. Update its tests accordingly.
- `VOIDED_ENVELOPES_COUNT_TOWARD_CAP` remains `false` (verified: DocuSeal bills per completion, so a void costs nothing).

</cap_semantics_amended>

<objective>
Wire the first live DocuSeal integration (AM-5): the fetch adapter behind lib/esign/provider.ts (ESIGN-01), the cap-enforced, fast-lane-aware envelope mint (ESIGN-04/ESIGN-13/P17-01), the any-party void that archives and re-mints (ESIGN-05/P17-02), and the embedded mobile-first signing surface (ESIGN-06/D-18b). This plan is gated: a blocking provider-verification checkpoint runs FIRST — no live API task executes until DOCUSEAL_API_KEY exists and the trial answers are recorded (including the void-billing answer that sets VOIDED_ENVELOPES_COUNT_TOWARD_CAP).

Purpose: Everything upstream (contract, schema, PDF, gating, UI shell) was built credential-free so this credential-gated surface stays small and auditable. This is also the pattern 16-09's SignWell adapter will reuse. Template-only (AM-2) holds by construction: the only bytes reaching /templates/pdf are the server-rendered PDF from 17-03 as rebuilt into the legal-grade agreement by 17-09.

## AMENDED 2026-07-20 (provider-verification review) — read before executing

This plan was drafted before the 2026-07-20 provider gate. Three amendments land here, all inside tasks this plan already owns:

1. **This plan OWNS the mint route's email behavior.** The submission is minted with `send_email` disabled and a per-submitter `reply_to`; Funūn sends its own branded invite. 17-10 owns ONLY the invite email module and the certificate renderer — it wires no routes. Implement the mint-side behavior here and only here (P17-10, ESIGN-18).
2. **The counsel gate is called here.** `assertCounselReviewedForProduction()` (17-09) runs before any DocuSeal call, so unreviewed operative agreement language cannot reach a real signature in production (P17-09a).
3. **The render input is the full legal-grade agreement input** from 17-09, not the reduced pre-063 shape.

Wave moved 4 → 5 and `depends_on` gained 17-09 and 17-10 as a consequence. 17-07 moved 5 → 6.

Output: The DocuSeal adapter with tests, the mint + void routes (cap-enforced, counsel-gated, Funūn-invited), and the embedded signing component mounted into the sign-phase region.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-split-sheet-esign/17-CONTEXT.md
@.planning/phases/17-split-sheet-esign/17-RESEARCH.md
@.planning/phases/17-split-sheet-esign/17-01-SUMMARY.md
@.planning/phases/17-split-sheet-esign/17-03-SUMMARY.md
@.planning/phases/17-split-sheet-esign/17-04-SUMMARY.md

# The contract, the ownership-check precedent, the PDF + helpers to wire
@lib/esign/provider.ts
@app/api/split-sheets/[id]/send-for-approval/route.ts
@lib/vault/pdf/split-sheet.tsx
@lib/split-sheets/envelopes.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Nothing yet — this runs FIRST. Execution of this phase must not start before the DocuSeal provider-verification pass, and no task below may run against a live account until DOCUSEAL_API_KEY exists (17-CONTEXT Provider Verification Gate; RESEARCH Environment Availability).</what-built>
  <how-to-verify>
    1. Verify @docuseal/react on npmjs.com/package/@docuseal/react (MIT, DocuSeal-maintained, no postinstall) before it is installed — RESEARCH Package Legitimacy Audit returned verdict OK; confirm the name once more here.
    2. Create the DocuSeal account, generate the API token, set DOCUSEAL_API_KEY, and add DOCUSEAL_API_KEY and DOCUSEAL_WEBHOOK_SECRET unset to .env.example.
    3. Run the trial and RECORD in the plan summary: (a) the Certificate of Signature fields/timestamps/IP granularity; (b) white-label scope + price for the embedded form; (c) the result of a 3-signer async multi-party template test (order 'random'); (d) a deliverability check; (e) the EXACT webhook signature header name + scheme (for 17-07); (f) whether VOIDED/archived submissions bill.
    4. Set VOIDED_ENVELOPES_COUNT_TOWARD_CAP in lib/split-sheets/envelopes.ts (17-01) from answer (f) — this resolves the voided-submission billing/cap question here, at the gate.
    4b. EXECUTION-CHECKLIST GUARD (checker Warning 2): before ANY live-API task in this plan runs, verify the flag is explicitly set to true or false with a comment citing the provider-trial answer — a still-default value means the checkpoint was skipped; STOP and resolve it.
    5. Confirm the per-submitter signing link's expiry behavior vs Funūn's 30-day approval token (RESEARCH Assumption A4); if signer links expire independently, plan to set the submission expire_at generously (30+ days).
  </how-to-verify>
  <resume-signal>Type "approved" and paste the confirmed webhook signature scheme, the void-billing answer (with VOIDED_ENVELOPES_COUNT_TOWARD_CAP set accordingly), and the multi-party test result — no live API task runs until these are known.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: DocuSeal adapter behind the EsignProvider interface</name>
  <files>package.json, package-lock.json, .env.example, lib/esign/docuseal.ts, lib/esign/docuseal.test.ts</files>
  <action>
    Install @docuseal/react (the MIT embed SDK — verdict OK per RESEARCH; only the client embed uses it). Implement lib/esign/docuseal.ts as a plain fetch-based DocuSealProvider implementing EsignProvider (createRequest, downloadSignedPdf, parseWebhook) — no vendor SERVER SDK, matching the provider file's own header philosophy and 16-09's SignWell approach. Authenticate server calls with the X-Auth-Token header from DOCUSEAL_API_KEY (server-only, never NEXT_PUBLIC_*, RESEARCH V13).

    createRequest posts the Funūn-rendered PDF buffer (from renderSplitSheet, 17-03 as rebuilt by 17-09) to /templates/pdf to create a one-off template, then posts /submissions against that template_id with order 'random' (parallel async — days-apart signing per P17-01/P17-02) and one submitters[] entry per party whose role matches partyRoleTag (17-03). Set the submission expire_at generously (30+ days) to outlast Funūn's approval token window (RESEARCH Assumption A4). Return the request id and per-signer embed sources. parseWebhook delegates verification + mapping to lib/esign/webhook.ts (17-01) — do not re-implement HMAC here.

    AMENDED (P17-10, ESIGN-18) — build this in from the start, do not mint with default email behavior and change it later: set the /submissions payload's send_email flag to false so the provider sends NO invite mail, and set each submitter's reply_to to ESIGN_FROM_EMAIL. Funūn sends its own branded invite in Task 2 via sendSignatureInvite (17-10). A default-on mint that ships even once puts provider-branded mail in a real collaborator's inbox, which is exactly what P17-10 removes; and reply_to still matters for the provider's own transactional mail (completion copies), which Pro branding does not fully cover — a collaborator's reply must reach a monitored Funūn mailbox.

    Write lib/esign/docuseal.test.ts mocking fetch (no live calls): createRequest posts to /templates/pdf then /submissions with order 'random' and the correct submitters; the /submissions payload disables the provider's invite email and carries a per-submitter reply_to; the API key is read from the server env and never appears in a client-facing return; parseWebhook rejects a tampered signature and maps a valid completion. RED first, then green.
  </action>
  <verify>
    <automated>npx jest lib/esign/docuseal.test.ts && npx tsc --noEmit && npm run lint && grep -Eq "EsignProvider" lib/esign/docuseal.ts</automated>
  </verify>
  <done>The adapter implements the interface with only the client embed SDK installed, authenticates server-side via X-Auth-Token, mints via /templates/pdf + /submissions with order 'random', and delegates webhook verification to the 17-01 pure module.</done>
</task>

<task type="auto">
  <name>Task 2: Cap-enforced, counsel-gated envelope mint route + Funūn invites + any-party void route</name>
  <files>app/api/split-sheets/[id]/mint-envelope/route.ts, app/api/split-sheets/[id]/void/route.ts</files>
  <action>
    Create app/api/split-sheets/[id]/mint-envelope/route.ts mirroring send-for-approval's ownership-check-with-session-client-then-service-write structure. Two entry paths: post-consensus (sheet is 'approved' — auto-mint) and the initiator FAST LANE from draft (P17-01) which first applies buildFastLaneBackfill (17-01) so the completed signature backfills approval state before minting. BEFORE any DocuSeal call, enforce the cap: count the initiator's calendar-month esign_envelopes via checkMonthlyCap (17-01, honoring VOIDED_ENVELOPES_COUNT_TOWARD_CAP set at the checkpoint) and reject the (cap+1)th mint (AM-2, ESIGN-13). Then render the PDF (renderSplitSheet — default Node runtime, never add an Edge export per RESEARCH Pitfall 6), call the adapter's createRequest, and persist one esign_envelopes row plus esign_envelope_signers rows from the response, flipping split_sheets.status to 'esign_pending' (readiness tier 10). All envelope creation is server-owned after the cap + ownership checks — never expose DOCUSEAL_API_KEY or let the client trigger a mint (RESEARCH Anti-Patterns).

    AMENDED (P17-09a) — also BEFORE any DocuSeal call, call assertCounselReviewedForProduction() from lib/split-sheets/agreement.ts (17-09). It is a no-op outside production and throws in production while the operative agreement language is unreviewed. Place it beside the cap check so both gates are visibly in the same pre-flight block; an unreviewed legal instrument reaching a real artist's signature is the failure P17-09a exists to prevent.

    AMENDED (ESIGN-16) — build the FULL SplitSheetAgreementInput for the render (17-09), not the reduced pre-063 shape: read rights_scope, agreement_date, sample disclosure, and the ISWC/ISRC (from the linked track when the sheet is attached to a project, snapshotting them onto the sheet at mint time so the executed record is stable), plus each party's legal_name, publisher_name, publisher_pro, publisher_ipi, writer_share, publisher_share, and master_share. Minting the reduced input would put the old ambiguous single-percentage table in front of real signers and silently defeat 17-09.

    AMENDED (P17-10, ESIGN-18) — after the envelope and signer rows persist, call sendSignatureInvite (17-10) once per signer with that signer's own approval token, and collect the per-signer results. The invites are Funūn's because the mint disabled the provider's (Task 1). Never let an invite failure abort or roll back the mint: the envelope is already spent by then, so surface partial delivery in the response and leave the resend affordance to the nudge path (17-04). Do NOT re-implement the email template or the send here — 17-10 owns that module; this route only calls it.

    Create app/api/split-sheets/[id]/void/route.ts for P17-02: any party (session-authorized as a party on the sheet, or the initiator) can object before all signatures land. Archive the DocuSeal submission via the adapter (DELETE /submissions/{id}), then apply buildVoidReset (17-01) — mark the esign_envelopes row 'voided' with voided_at (NOT deleted — audit history preserved) and reset split_sheets.status to 'countered'/'pending_approval'. Re-consensus mints a NEW envelope row on the next mint (preserving the prior attempt). Record the envelope's billed flag per the provider-gate answer.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -Eq "checkMonthlyCap" "app/api/split-sheets/[id]/mint-envelope/route.ts" && grep -Eq "createRequest" "app/api/split-sheets/[id]/mint-envelope/route.ts" && grep -Eq "assertCounselReviewedForProduction" "app/api/split-sheets/[id]/mint-envelope/route.ts" && grep -Eq "sendSignatureInvite" "app/api/split-sheets/[id]/mint-envelope/route.ts" && grep -Eq "voided" "app/api/split-sheets/[id]/void/route.ts"</automated>
  </verify>
  <done>Mint enforces the cap, the counsel gate, and ownership before any DocuSeal call, renders the full legal-grade agreement input, fast-lane backfills approval, persists envelope/signer rows, sends Funūn's own per-signer invites without letting a delivery failure abort the mint, and flips to esign_pending; void archives the submission and resets the sheet while preserving the voided envelope row.</done>
</task>

<task type="auto">
  <name>Task 3: Embedded mobile-first signing component + mount into the sign region</name>
  <files>components/split-sheets/SplitSheetSigningEmbed.tsx, components/split-sheets/SplitApprovalView.tsx</files>
  <action>
    Create components/split-sheets/SplitSheetSigningEmbed.tsx as a thin client wrapper around @docuseal/react's DocusealForm, taking only the per-signer scoped src/token for THIS party's esign_envelope_signers row (never the API key — RESEARCH V13). Signature capture, field wizard, and completion are the vendor's (RESEARCH Don't Hand-Roll — spike 006a validated draw/type/camera at 375px); do not build a custom canvas pad. Wire the onComplete callback to reflect local completion state.

    Mount SplitSheetSigningEmbed into SplitApprovalView's 'sign' region built in 17-04 (replacing the placeholder mount point). Keep the mobile-first layout — the embed and its container must be thumb-signable and non-overflowing at 375px (D-18b, the studio-with-only-a-phone canonical test). No redirect: the signer stays inside Funūn.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -Eq "DocusealForm" components/split-sheets/SplitSheetSigningEmbed.tsx</automated>
    <human-check>On a 375px viewport, an esign_pending party's /approve/[token] renders the embedded DocuSeal form with thumb-friendly signature capture and no redirect (D-18b, spike 006a).</human-check>
  </verify>
  <done>The embedded signing component renders the per-signer DocuSeal form inside Funūn at 375px with no redirect and no API-key exposure, mounted into the sign-phase region.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DocuSeal API → server | Server-only X-Auth-Token calls; the key must never reach the browser. |
| browser → embedded iframe | The signer interacts with a per-signer-scoped embed; only a scoped src crosses, never the API key. |
| client → mint trigger | A privileged, cost-bearing action that must run server-side after cap + ownership checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-15 | Information Disclosure | DOCUSEAL_API_KEY exposure | high | mitigate | Server-only env var; the client embed receives only a per-signer scoped src/token; the mint call is server-owned (RESEARCH V13). |
| T-17-16 | Elevation of Privilege (cost) | scripting mint calls to exceed the ~10/mo cap | high | mitigate | Server-side checkMonthlyCap inside the mint route before any DocuSeal call, counting the calendar month's esign_envelopes (AM-2). |
| T-17-17 | Tampering | arbitrary-PDF bypass of AM-2 template-only | high | mitigate | The mint route only ever posts 17-03's server-rendered buffer to /templates/pdf; no client file bytes reach the template API. |
| T-17-18 | Repudiation | voided envelope losing audit history | medium | mitigate | Void marks the row 'voided' with voided_at (never deletes); each re-mint is a new row (P17-02). |
| T-17-19 | Elevation of Privilege | signer-token confusion across parties | medium | mitigate | The embed src is minted per esign_envelope_signers row and is never derived from the Funūn approval token. |
| T-17-35 | Tampering | unreviewed operative agreement language reaching a production signature | high | mitigate | assertCounselReviewedForProduction (17-09) runs in the mint pre-flight block alongside the cap check, before any DocuSeal call (P17-09a). |
| T-17-36 | Spoofing | provider-branded invite mail reaching a collaborator | medium | mitigate | The /submissions payload disables the provider's invite email and sets a per-submitter reply_to; Funūn sends the only invite, from its own monitored mailbox, linking only to Funūn's approve page (P17-10, ESIGN-18). |
| T-17-SC | Tampering | npm install @docuseal/react | medium | mitigate | Package verified at the blocking checkpoint (npmjs.com, MIT, no postinstall, verdict OK per RESEARCH audit); only the MIT client embed SDK is added — no server SDK. |
</threat_model>

<verification>
- `npx jest lib/esign/docuseal.test.ts` green including tampered-signature rejection.
- `npx tsc --noEmit` and `npm run lint` clean.
- Full-suite regression gate: `npm test` green (baseline 455+ tests).
- Blocking checkpoint first: provider verification recorded; VOIDED_ENVELOPES_COUNT_TOWARD_CAP set.
- Manual (human-check): embedded signing works at 375px with no redirect.
</verification>

<success_criteria>
An initiator mints a Funūn split-sheet envelope (post-consensus or fast lane) within the cap, collaborators sign it embedded and mobile-first inside Funūn, and any party can void a minted envelope back to negotiation with the attempt preserved for audit.
</success_criteria>

<output>
Create `.planning/phases/17-split-sheet-esign/17-06-SUMMARY.md` when done.
</output>
