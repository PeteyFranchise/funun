---
phase: 17-split-sheet-esign
plan: 07
type: execute
wave: 5
depends_on: ["17-05", "17-06"]
files_modified:
  - app/api/webhooks/docuseal/route.ts
  - __tests__/docuseal-webhook.test.ts
  - lib/esign/telemetry.ts
  - lib/esign/telemetry.test.ts
  - app/api/admin/esign/usage/route.ts
  - app/(artist)/admin/esign-usage/page.tsx
autonomous: false
requirements: [ESIGN-07, ESIGN-09, ESIGN-10, ESIGN-14]

must_haves:
  truths:
    - "The webhook route verifies the HMAC over the RAW body FIRST (via the 17-01 pure fn) and does no work on a bad or stale signature — a forged submission.completed can never fabricate an executed sheet (ESIGN-07, RESEARCH V6)."
    - "On completion it downloads the executed PDF + Certificate (40-min-expiry URLs) and re-uploads to the release-documents bucket synchronously within the handler (RESEARCH Anti-Patterns)."
    - "It is idempotent on docuseal_submission_id: an already-completed envelope short-circuits with 200 and repeats no fan-out or notifications (RESEARCH Pitfall 7)."
    - "On completion it flips the sheet to 'executed', fans out one vault_documents row per account-holder party (buildFanoutRows, 17-05), fires the executed notification, and OFFERS write-back — never silent (P17-06, P17-07, ESIGN-09/ESIGN-10)."
    - "Admin can see completed-envelope count + estimated spend for the current month, feeding the AM-3 $500/mo trigger (ESIGN-14, AM-3)."
  artifacts:
    - app/api/webhooks/docuseal/route.ts
    - lib/esign/telemetry.ts
    - app/api/admin/esign/usage/route.ts
  key_links:
    - "verifyDocusealSignature (17-01) -> raw-body gate -> download+re-upload -> sheet 'executed' + fan-out rows (17-05) -> readiness trigger (17-02) fires."
    - "esign_envelopes completed rows -> telemetry COUNT + estimated spend -> admin usage surface (AM-3)."
    - "fully-executed -> split_sheet_executed notification (17-01) + reconcile offer (17-05)."
---

<objective>
Close the loop: the signature-verified DocuSeal webhook that turns a completed submission into a fully executed split sheet — stored PDF + Certificate, sheet 'executed', cross-account locker fan-out, executed notifications, and an OFFERED write-back — plus the AM-3 usage/spend telemetry the whole access model is metered against (ESIGN-07/ESIGN-09/ESIGN-10/ESIGN-14). Credential-gated: it needs DOCUSEAL_WEBHOOK_SECRET and closes with the end-to-end UAT checkpoint.

Purpose: This is Funūn's first live webhook integration, so it must be spoof-proof (raw-body HMAC first), idempotent (providers retry), and prompt about the 40-minute document URLs. On completion the existing readiness trigger (17-02) moves the project to tier 15 automatically once the fan-out rows land. Telemetry queries esign_envelopes directly — the authoritative ledger — rather than a drift-prone counter table.
Output: The verified idempotent webhook with tests, the telemetry helper + admin usage surface, and the end-to-end UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-split-sheet-esign/17-CONTEXT.md
@.planning/phases/17-split-sheet-esign/17-RESEARCH.md
@.planning/phases/17-split-sheet-esign/17-01-SUMMARY.md
@.planning/phases/17-split-sheet-esign/17-05-SUMMARY.md
@.planning/phases/17-split-sheet-esign/17-06-SUMMARY.md

# The verification fn, fan-out builder, adapter, and storage/notify helpers
@lib/esign/webhook.ts
@lib/split-sheets/distribution.ts
@lib/esign/docuseal.ts
@lib/notifications/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Signature-verified, idempotent completion webhook</name>
  <files>app/api/webhooks/docuseal/route.ts, __tests__/docuseal-webhook.test.ts</files>
  <behavior>
    POST /api/webhooks/docuseal:
    - Reads the raw request body FIRST, verifies the HMAC via verifyDocusealSignature (17-01); an invalid or >5min-stale signature returns non-2xx with zero side effects.
    - On a valid submission.completed: if the matching esign_envelopes row is already 'completed', returns 200 immediately with no repeated work (idempotent on docuseal_submission_id).
    - Otherwise: downloads the executed PDF + audit log via the adapter, re-uploads both to the release-documents bucket within the handler, sets the envelope 'completed'/completed_at + executed_file_path/audit_log_path, sets each signer 'completed'/signed_at, and flips split_sheets.status to 'executed'.
    - Runs the Node runtime (raw-body access for HMAC), never Edge.
  </behavior>
  <action>
    Create app/api/webhooks/docuseal/route.ts. Read the raw body as text FIRST and hand it to verifyDocusealSignature (17-01) with DOCUSEAL_WEBHOOK_SECRET before any parsing or side effect; a verification failure returns non-2xx with no partial processing (RESEARCH V6 — a forged completion must never fabricate an executed sheet). Force the Node runtime (raw-body access required) — do not add an Edge export (RESEARCH Pitfall 6, Architectural Responsibility Map).

    Make the handler idempotent on docuseal_submission_id: look up the esign_envelopes row; if already 'completed', return 200 immediately and do nothing else (providers retry — RESEARCH Pitfall 7). On the first valid completion, download the combined document URL and the audit_log URL via the adapter and re-upload BOTH to the release-documents bucket synchronously inside the handler — these URLs expire in ~40 minutes, so never queue them for later (RESEARCH Anti-Patterns). Update the envelope to 'completed' with completed_at + executed_file_path + audit_log_path, mark each esign_envelope_signers row 'completed'/signed_at, and flip split_sheets.status to 'executed'. (The readiness trigger from 17-02 then moves the project to tier 15 once Task 2's fan-out rows land.)

    Write __tests__/docuseal-webhook.test.ts (mock fetch + service client, no live account): a tampered/stale signature does no work and returns non-2xx; a valid completion transitions the envelope/sheet; a redelivered completion for an already-completed envelope returns 200 and repeats nothing. RED first, then green.
  </action>
  <verify>
    <automated>npx jest __tests__/docuseal-webhook.test.ts && npx tsc --noEmit && npm run lint && grep -Eq "verifyDocusealSignature" app/api/webhooks/docuseal/route.ts</automated>
  </verify>
  <done>The webhook verifies the raw-body HMAC first, is idempotent on the submission id, promptly re-hosts the executed PDF + Certificate, and marks envelope/signers/sheet executed — with zero side effects on a bad signature.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-account fan-out + executed notify + write-back offer + usage telemetry</name>
  <files>app/api/webhooks/docuseal/route.ts, lib/esign/telemetry.ts, lib/esign/telemetry.test.ts, app/api/admin/esign/usage/route.ts, app/(artist)/admin/esign-usage/page.tsx</files>
  <behavior>
    monthlyEsignUsage({ completedCount, perDocRate }):
    - Returns completed-envelope count for the current calendar month and estimated spend = count * perDocRate, for the AM-3 $500/mo trigger.
    Fan-out + notify (in the webhook completion path):
    - Inserts one vault_documents row per account-holder party via buildFanoutRows (17-05); zero rows for parties without an account.
    - Fires the split_sheet_executed notification (17-01) to the initiator (and account-holder parties), and surfaces the reconcile offer (17-05) — never a silent composers[] write.
  </behavior>
  <action>
    In the webhook completion path (same route file), after the envelope/sheet transition, apply buildFanoutRows (17-05) via the service client — one vault_documents row per account-holder party, all pointing at the re-hosted storage path, each satisfying the evidence guard (P17-06). Fire the split_sheet_executed notification via createNotification (lib/notifications/index.ts) using the 17-01 builder, and trigger the reconcile OFFER (17-05) rather than writing composers[] — write-back is offered, never silent (P17-07). Keep all of this inside the idempotency guard so a redelivery does not double-insert or double-notify (RESEARCH Pitfall 7).

    Create lib/esign/telemetry.ts exporting monthlyEsignUsage per the behavior block — a pure computation over the completed-envelope count and the known per-doc rate (RESEARCH Don't Hand-Roll: query esign_envelopes directly, no separate counter table). Create app/api/admin/esign/usage/route.ts (admin-gated via the existing admin gate) that counts completed esign_envelopes for the current month and returns the usage + estimated spend, and app/(artist)/admin/esign-usage/page.tsx rendering it for the admin (AM-3 trigger visibility). Keep this read-only and minimal.

    Write lib/esign/telemetry.test.ts RED first (count → spend math, month boundary), then implement to green.
  </action>
  <verify>
    <automated>npx jest lib/esign/telemetry.test.ts && npx tsc --noEmit && npm run lint && grep -Eq "buildFanoutRows" app/api/webhooks/docuseal/route.ts</automated>
  </verify>
  <done>Completion fans out account-holder locker rows once, notifies executed, offers (never forces) write-back, and admin can see the current-month completed count + estimated spend for the AM-3 trigger.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Phase 17 e-sign loop: adapter, mint/void, embedded signing, verified webhook, cross-account distribution, readiness move, offered reconciliation, and usage telemetry.</what-built>
  <how-to-verify>
    1. Configure the DocuSeal webhook to POST /api/webhooks/docuseal with DOCUSEAL_WEBHOOK_SECRET set.
    2. End-to-end on a real 3-signer split sheet: approve → mint → sign each party via the embedded form ON A 375px MOBILE VIEWPORT (D-18b, spike 006a) → confirm the webhook files the executed PDF + Certificate into every account-holder party's Contract Locker and the sheet reads 'executed'.
    3. Confirm the project's readiness moved to tier 15 (executed) via the trigger; a standalone (projectless) sheet lands unattached and is attachable, then moves that project's readiness.
    4. Confirm the executed notification fired and the write-back diff is OFFERED (not auto-applied); dismiss leaves composers[] untouched.
    5. Void a minted (unsigned) envelope from another party and confirm the sheet returns to negotiation with the voided attempt preserved; re-consensus mints a new envelope.
    6. Replay a captured webhook payload (tampered) and confirm rejection with no state change; replay a valid one and confirm idempotency (no duplicate documents/notifications).
    7. Confirm the admin usage surface shows completed count + estimated spend against the AM-3 $500/mo trigger.
  </how-to-verify>
  <resume-signal>Type "approved" once the end-to-end mobile signing loop files documents, readiness moves, void/idempotency/reconciliation behave, and telemetry reads — or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DocuSeal → webhook endpoint | Unauthenticated inbound HTTP asserting a sheet is executed; must be HMAC-verified before any effect. |
| webhook → vault_documents fan-out | A completion writes cross-account locker rows; must be idempotent and evidence-guard-valid. |
| admin → usage surface | Read-only spend visibility; must be admin-gated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-20 | Spoofing / Tampering | forged submission.completed fabricating an executed sheet | critical | mitigate | Raw-body-first HMAC verification via verifyDocusealSignature (17-01) with the 5-minute window; non-2xx and zero side effects on failure (RESEARCH V6). |
| T-17-21 | Tampering / Repudiation | replayed completion double-filing documents/notifications | high | mitigate | Idempotency keyed on docuseal_submission_id + envelope 'completed' status before any fan-out/notify (RESEARCH Pitfall 7). |
| T-17-22 | Information Disclosure | expired document URLs / leaked signed PDF | medium | mitigate | 40-min URLs fetched and re-hosted to release-documents synchronously; locker rows RLS-scoped per user_id (RESEARCH Anti-Patterns; migration 001 policy). |
| T-17-23 | Information Disclosure | non-admin reading spend telemetry | medium | mitigate | Admin gate on the usage route/page; read-only. |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan. |
</threat_model>

<verification>
- `npx jest __tests__/docuseal-webhook.test.ts lib/esign/telemetry.test.ts` green including tampered-signature and idempotency cases.
- `npx tsc --noEmit` and `npm run lint` clean.
- Full-suite regression gate: `npm test` green (baseline 455+ tests).
- Human checkpoint: end-to-end mobile signing files documents, readiness moves, void/idempotency/reconciliation/telemetry behave.
</verification>

<success_criteria>
A completed DocuSeal signature — verified, idempotent, and prompt — files the executed split sheet + Certificate into every account-holder's Contract Locker, moves readiness to executed, offers (never forces) the splits write-back, and surfaces the usage/spend the AM-3 trigger watches.
</success_criteria>

<output>
Create `.planning/phases/17-split-sheet-esign/17-07-SUMMARY.md` when done.
</output>
