# Phase 38.2-02 Summary — Beta Usage Metering and Workspace Usage Surface

## Completed

- Added migration 222 with an append-only, service-only, idempotent workspace usage
  ledger and a bounded aggregate function.
- Limited observations to storage ingested, AI requests/tokens, e-sign requests, and audio
  processing time; the schema has no free-form metadata field for sensitive payloads.
- Added a best-effort server recorder that cannot throw into or block the underlying creative work.
- Added current seat and accepted-roster gauges plus 30-day measured usage to the workspace Plan &
  usage page.
- Labeled all beta measurements “Tracked, not enforced” and kept them separate from Member credits.

## Verification

- Focused usage suite: 2 suites, 8 tests passed.
- TypeScript: `npx tsc --noEmit` passed.
- Migration 222 was applied, registered, and structurally verified in production on 2026-09-13.
- The matching application shipped through PR #68 / merge commit `cb0ec3a7`.
