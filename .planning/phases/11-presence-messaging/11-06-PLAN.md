---
phase: 11-presence-messaging
plan: 06
type: execute
wave: 4
depends_on: [11-03, 11-04]
files_modified:
  - components/profile/ProfileView.tsx
  - components/profile/DmWidget.tsx
  - app/u/[handle]/page.tsx
  - components/profile/ProfilePresenceDot.tsx
autonomous: true
requirements: [PRESENCE-01, CONNECT-03, CONNECT-05]
must_haves:
  truths:
    - "A profile avatar shows the emerald 'Online' presence pill only when that member is currently active on the platform, driven by the presence-global Realtime channel — never falsely shown (PRESENCE-01, D-22)"
    - "The profile Message button is a link to /messages?with={ownerId} and no longer mounts an in-place DmWidget (D-02)"
    - "app/u/[handle]/page.tsx no longer builds/loads DmState for an in-place widget; it passes the owner id for the Message link instead"
  artifacts:
    - components/profile/ProfilePresenceDot.tsx
  key_links:
    - "ProfilePresenceDot reads the presence-global channel (key: userId) that PresenceTracker (Plan 04) tracks"
    - "The Message link target /messages?with={ownerId} is resolved by MessagesPageClient's initialWith handling (Plan 05)"
  prohibitions:
    - statement: "The presence dot must never render 'Online' from a stale last_seen_at value on the profile — the live pill is driven by the Realtime presence channel, not the timestamp"
      status: resolved
      verification: "ProfilePresenceDot derives online from presenceState() of presence-global; the timestamp path only ever produces 'Active X ago' text, never the green Online pill"
    - statement: "app/u/[handle]/page.tsx must not select('*') on artist_profiles"
      status: resolved
      verification: "grep the page for select('*') on artist_profiles → must be zero (already explicit-column; keep it so)"
---

<objective>
Wire real presence into the profile and retire the in-place DM widget. Replace the honest-stub `PresenceDot` in `ProfileView.tsx` with a live `ProfilePresenceDot` that reads the presence-global Realtime channel, and change the profile Message button from a `DmWidget` mount to a `<Link href="/messages?with={ownerId}">` (D-02). Simplify `app/u/[handle]/page.tsx` to stop loading `DmState` for the removed widget.

Purpose: PRESENCE-01's profile-avatar "Online" pill is the most visible presence surface, and D-02 makes the profile Message button route to the new inbox rather than opening a profile-scoped widget. This plan closes the profile side of the phase.
Output: a new `ProfilePresenceDot` client component, a modified `ProfileView`, a simplified `DmWidget`/page, all keeping the Phase 9 avatar/live-pill slot intact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-presence-messaging/11-CONTEXT.md
@.planning/phases/11-presence-messaging/11-RESEARCH.md
@.planning/phases/11-presence-messaging/11-UI-SPEC.md
@.planning/phases/11-presence-messaging/11-PATTERNS.md
@components/profile/ProfileView.tsx
@components/profile/DmWidget.tsx
@app/u/[handle]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProfilePresenceDot — live 'Online' pill from the presence channel</name>
  <files>components/profile/ProfilePresenceDot.tsx</files>
  <read_first>
    - components/profile/ProfileView.tsx lines 89-100 (the current `PresenceDot({ online })` stub — its exact pill markup: absolute bottom-3 right-3, emerald dot + "Online" text, the `border-hairstrong bg-ink` pill — reuse this markup verbatim)
    - .planning/phases/11-presence-messaging/11-RESEARCH.md §Pattern 1 (presence-global channel, presenceState()/sync event — how to READ presence without a second track)
    - .planning/phases/11-presence-messaging/11-UI-SPEC.md §Presence Dot States + §Component Inventory PresenceDot (profile avatar variant: pill 8×8 emerald dot + "Online", absolute bottom-right of the 168px avatar)
    - components/nav/PresenceTracker.tsx (the channel name `presence-global` and key convention — read the SAME channel, do not open a competing tracker)
    - lib/supabase/client.ts (createClient)
  </read_first>
  <action>
    Create `components/profile/ProfilePresenceDot.tsx` ('use client') exporting `ProfilePresenceDot({ targetUserId }: { targetUserId: string })`. It subscribes (read-only) to the `presence-global` channel and derives whether `targetUserId` is present:
    - `const channel = supabase.channel('presence-global', { config: { presence: { key: '' } } })` — NOTE: for a read-only subscriber we still join the same channel name; do NOT call `track()` here (only PresenceTracker tracks, for the current user). On the `sync` event, read `channel.presenceState()` and set `online = Boolean(presenceState()[targetUserId]?.length)`. Also handle `join`/`leave` events to update. removeChannel on cleanup.
    - When `online` is false/unknown, render `null` (the Phase 9 honesty rule: never falsely show Online). When true, render the exact pill markup from the current `PresenceDot` stub (absolute `bottom-3 right-3`, `border-hairstrong bg-ink px-[9px] py-1 text-[12px] font-bold text-emerald-400`, 8×8 emerald dot + "Online").

    This component is presence-CHANNEL-driven (live), not last_seen_at-driven — the green "Online" pill means "currently connected," matching D-20's "app open AND tab visible." (The DM header's "Active X ago" bucket text, driven by last_seen_at, lives in Plan 05's ConversationView/DockedWidget, not here.)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c 'ProfilePresenceDot' | grep -q '^0$' && grep -q "presence-global" components/profile/ProfilePresenceDot.tsx && grep -q "removeChannel" components/profile/ProfilePresenceDot.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/profile/ProfilePresenceDot.tsx` exists, is a client component, subscribes to `presence-global`, and does NOT call `track()`
    - Derives online from `presenceState()[targetUserId]` on sync/join/leave; renders null when not online
    - When online, renders the emerald "Online" pill markup matching the Phase 9 stub (8×8 dot + text-emerald-400)
    - `removeChannel` in cleanup; no second presence channel tracked
    - `npx tsc --noEmit` clean for this file
  </acceptance_criteria>
  <done>A live, honesty-preserving presence pill renders "Online" only for currently-connected members, reading the shared presence channel.</done>
</task>

<task type="auto">
  <name>Task 2: ProfileView — mount live dot, Message button → /messages link; simplify DmWidget + page</name>
  <files>components/profile/ProfileView.tsx, components/profile/DmWidget.tsx, app/u/[handle]/page.tsx</files>
  <read_first>
    - components/profile/ProfileView.tsx (the PresenceDot stub lines 89-100, its mount at line 210, the DmWidget import line 10, and the Message-button block lines 288-294 — the exact spots to change)
    - components/profile/DmWidget.tsx (the current full component; after this plan it is reduced to a link or removed from the profile mount — decide per action)
    - app/u/[handle]/page.tsx (the DmState build lines ~263-280 and the `dm={dm}` prop pass ~299 — the load to remove; the `profile.id`/`profile.artist_name`/`profile.handle` already available for the Message link)
    - .planning/phases/11-presence-messaging/11-UI-SPEC.md §Modified components (DmWidget.tsx → link; ProfileView.tsx → wire PresenceDot + Message link)
  </read_first>
  <action>
    `components/profile/ProfileView.tsx`:
    - Import `ProfilePresenceDot` and replace the `<PresenceDot />` mount at line ~210 with `<ProfilePresenceDot targetUserId={data.id ?? ownerId} />` (use whichever prop carries the profile's user id — confirm from the ProfileView props; the presence dot needs the profile owner's auth user id). Remove the local `PresenceDot` stub function (lines 89-100) since it is superseded. If `data` does not already carry the owner user id, thread it through (it is available at the page as `profile.id`).
    - Replace the Message-button block (lines 288-294, currently either `<DmWidget dm={dm} />` or a disabled button) with a single `<Link href={/messages?with=${ownerId}} className="inline-flex items-center gap-[9px] rounded-[11px] border border-hairstrong bg-card px-[22px] py-[13px] text-[15px] font-bold text-white">Message</Link>` using the existing Message-button classes (D-02: Message goes to the inbox, not an in-place widget). Only render it in the visitor branch (not owner). Remove the `dm`/`DmState` prop from ProfileView's signature and the `DmWidget` import.

    `components/profile/DmWidget.tsx`: this component is no longer mounted on the profile. Either (a) delete the file if nothing else imports it, or (b) if any other importer exists, leave it but remove the profile's usage. Grep for other importers first (`import.*DmWidget`); if only ProfileView/page reference it, delete it and remove the `DmState` type export usage. Record the choice in the SUMMARY. The DmWidget PATTERNS/JSX already live on in DockedWidget/ConversationView (Plan 05), so deletion loses nothing.

    `app/u/[handle]/page.tsx`: remove the `DmState` build block (the `canMessage`/`loadConversation`/`findThread` calls and the `dm = {...}` object) and stop passing `dm={dm}` to `<ProfileView>`. Remove the now-unused imports (`loadConversation`, `findThread`, `DmState`) if they are unused elsewhere in the file. Pass the owner id needed for the Message link (ensure ProfileView receives `profile.id` as `ownerId` or via `data.id`). Keep every other prop (connect/follow/wall/endorsements/comments/activity) unchanged and keep the explicit-column artist_profiles selects.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E 'ProfileView|DmWidget|u/\[handle\]/page' | grep -c error | grep -q '^0$' && grep -q "ProfilePresenceDot" components/profile/ProfileView.tsx && grep -q "/messages?with=" components/profile/ProfileView.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileView.tsx` mounts `<ProfilePresenceDot targetUserId=... />` and the old inline `PresenceDot` stub function is removed
    - The visitor Message affordance is a `<Link href="/messages?with={ownerId}">` (grep: `/messages?with=` present; `<DmWidget` absent from ProfileView)
    - `app/u/[handle]/page.tsx` no longer builds `DmState` or passes `dm={dm}` (grep: no `loadConversation`/`findThread` remaining unless used elsewhere; no `dm={dm}`)
    - `DmWidget.tsx` is either deleted or no longer imported by ProfileView/page (grep confirms no remaining profile mount)
    - No `select('*')` on artist_profiles in the page
    - `npx tsc --noEmit` clean; `npm run build` succeeds
  </acceptance_criteria>
  <done>The profile shows a live Online pill and a Message link to the inbox; the in-place DmWidget path is retired without breaking the build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile visitor → presence signal | Presence is visible to all signed-in members (D-23); the profile dot only reflects the presence-global channel, and cannot be forged for another member. |
| page (server) → artist_profiles | Must keep explicit-column selects (migration 040 lockdown). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-18 | Spoofing | falsely showing a member as "Online" | low | mitigate | The pill renders only from live presence-global membership (D-20 app-open+visible); a stale last_seen_at never produces the green pill (it only yields "Active X ago" text elsewhere) |
| T-11-19 | Information Disclosure | select('*') leaking private profile columns on the page | medium | mitigate | Page keeps explicit-column artist_profiles selects; removing the DM load does not introduce any new query |
| T-11-SC | Tampering | npm installs | high | mitigate | No packages installed. If any install is attempted, halt and add a blocking-human legitimacy checkpoint |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run build` succeeds
- `npm test` full suite green (profile-load tests still pass after the DmState removal)
- Manual (end-of-phase): visiting a profile shows the Online pill only when that member is active; clicking Message opens /messages with that person's thread selected
</verification>

<success_criteria>
- Profile avatar shows a live, honesty-preserving Online pill (PRESENCE-01)
- Message button links to /messages?with={ownerId} (D-02); in-place DmWidget retired
- Page no longer loads DmState; build/tests green
</success_criteria>

<output>
Create `.planning/phases/11-presence-messaging/11-06-SUMMARY.md` when done
</output>
