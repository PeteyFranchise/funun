---
phase: 11-presence-messaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/presence.test.ts
  - __tests__/dm-request.test.ts
  - __tests__/dm-unread.test.ts
  - lib/social/presence.ts
  - lib/social/dm.ts
  - lib/social/notifications.ts
autonomous: true
requirements: [PRESENCE-01, PRESENCE-02, PRESENCE-03, CONNECT-03, CONNECT-04, CONNECT-05]
must_haves:
  truths:
    - "formatPresenceStatus() returns 'Active now' for a timestamp under 2 minutes old and null for one older than 7 days (PRESENCE-01/02, D-21)"
    - "countRecentRequests() counts only status='pending' dm_threads created by the requester within a rolling 7-day window (CONNECT-04, D-14)"
    - "isConnected() returns true only when an accepted connections row exists in either direction (CONNECT-05, D-13)"
    - "buildMessageRequestNotification()/buildNewDmNotification() return the Phase 10 NotificationPayload shape with type set to message_request/new_dm (CONNECT-03, NOTIF-01)"
    - "unreadThreadPredicate helpers compute unread from dm_thread_reads.last_read_at vs dm_messages.created_at, never a cached counter (PRESENCE-03, D-07)"
  artifacts:
    - lib/social/presence.ts
    - __tests__/presence.test.ts
    - __tests__/dm-request.test.ts
    - __tests__/dm-unread.test.ts
  key_links:
    - "NOTIFICATION_TYPES catalog in lib/social/notifications.ts must include message_request + new_dm before the send/accept routes (Plan 03) reference them"
    - "countRecentRequests()/isConnected() in lib/social/dm.ts are the exact functions Plan 03's /api/dm/send gate calls"
---

<objective>
Wave 0 foundation for Phase 11: create three RED Jest test scaffolds and the pure-function helpers they cover — the D-21 presence bucket formatter (`lib/social/presence.ts`), the rate-limit + connection-gate query helpers and unread-count predicate (`lib/social/dm.ts` additions), and the two new notification builders + catalog entries (`lib/social/notifications.ts`). No database, API, or UI work — this plan produces the tested primitives every later Phase 11 plan builds on.

Purpose: The rate limit (CONNECT-04), connection gate (CONNECT-05), presence buckets (PRESENCE-01/02), and unread computation (PRESENCE-03) are all pure logic that must be machine-verified before the API/UI layers wire them. Landing them first (RED then GREEN) makes the whole phase's core rules testable in <5s without a live DB.
Output: `lib/social/presence.ts` (net-new), extended `lib/social/dm.ts` + `lib/social/notifications.ts`, and three test files under `__tests__/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-presence-messaging/11-CONTEXT.md
@.planning/phases/11-presence-messaging/11-RESEARCH.md
@.planning/phases/11-presence-messaging/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED test scaffolds for presence buckets, rate-limit/connection gate, and unread count</name>
  <files>__tests__/presence.test.ts, __tests__/dm-request.test.ts, __tests__/dm-unread.test.ts</files>
  <read_first>
    - __tests__/connections.test.ts (the canonical Wave 0 RED-test style for this codebase — describe/it structure, module-resolution-fail comment header, migration-content assertion pattern)
    - jest.config.js (ts-jest preset, node env, @/* moduleNameMapper — confirms import style)
    - .planning/phases/11-presence-messaging/11-RESEARCH.md §Validation Architecture (the Req→Test map and Wave 0 Gaps list)
    - .planning/phases/11-presence-messaging/11-PATTERNS.md §lib/social/presence.ts and §lib/social/dm.ts (exact function signatures to assert against)
  </read_first>
  <action>
    Create three RED Jest test files. They import from modules extended in Tasks 2-4 of THIS plan, so on first run (before Tasks 2-4) they fail on assertion or module resolution — that is the intended RED state; Tasks 2-4 make them GREEN.

    `__tests__/presence.test.ts` — import `formatPresenceStatus` from `@/lib/social/presence`. Assert the D-21 buckets against fixed offsets from `Date.now()`: under 2 min → exactly 'Active now'; ~5 min → 'Active 5m ago'; ~90 min → 'Active 1h ago'; ~26h (same-week, over a day) → 'Active this week'; ~8 days → null; and null input → null. Use `jest.useFakeTimers()` with `setSystemTime`, or construct ISO strings via `new Date(Date.now() - offsetMs).toISOString()`. Cover PRESENCE-01 (dot-eligible 'Active now') and PRESENCE-02 ('Active X ago' + the >7-day null cutoff).

    `__tests__/dm-request.test.ts` — pure-unit test the two query helpers by passing a hand-rolled fake Supabase client object (a plain object exposing `.from().select().eq().gte()` etc. as chainable stubs that return canned `{ count }` / `{ data }`), NOT a real client. Cover: `countRecentRequests(fakeClient, requesterId)` returns the stubbed count and that its query chain filters `status='pending'`, `requester_id=<requesterId>`, and a `created_at >= (now - 7 days)` bound (CONNECT-04, D-14); `isConnected(fakeClient, a, b)` returns true when the stub yields a row and false when it yields null, and that its filter uses `status='accepted'` with an either-direction `.or(...)` (CONNECT-05, D-13). Also assert the rate-limit constants exported from `@/lib/social/dm`: `BASELINE_REQUEST_LIMIT === 10` and `VERIFIED_REQUEST_LIMIT === 30` (D-15), and `PENDING_STACK_CAP === 3` (D-18).

    `__tests__/dm-unread.test.ts` — cover PRESENCE-03/D-07 unread computation as a pure function: import `hasUnread(lastReadAt, latestMessageAt)` from `@/lib/social/dm` and assert it returns true when the latest message is newer than the read marker (or the marker is null), false when the read marker is newer-or-equal. This proves the badge is computed by timestamp comparison, never a stored counter.

    Do NOT put a fenced code block referencing the string 'Active now' inside any comment that a negative grep might target — these are positive assertions only, so no allowlist marker is needed. Keep each file's header comment noting it is RED until its GREEN task lands.
  </action>
  <verify>
    <automated>npx jest --listTests 2>/dev/null | grep -E 'presence.test.ts|dm-request.test.ts|dm-unread.test.ts' | wc -l | grep -q 3 && echo "3 scaffolds present"</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/presence.test.ts`, `__tests__/dm-request.test.ts`, `__tests__/dm-unread.test.ts` all exist and are discovered by `npx jest --listTests`
    - Each file imports from `@/lib/social/presence` or `@/lib/social/dm` (grep confirms the import lines)
    - `presence.test.ts` contains an assertion that an ~8-day-old timestamp maps to `null` (the D-21 cutoff)
    - `dm-request.test.ts` asserts `BASELINE_REQUEST_LIMIT === 10`, `VERIFIED_REQUEST_LIMIT === 30`, and `PENDING_STACK_CAP === 3`
    - Before Tasks 2-4 run, `npm test -- --testPathPattern='presence|dm-request|dm-unread'` FAILS (RED state confirmed)
  </acceptance_criteria>
  <done>Three RED test files exist, discovered by Jest, and fail because the modules under test lack the new exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — presence bucket formatter (lib/social/presence.ts)</name>
  <files>lib/social/presence.ts</files>
  <behavior>
    - Under 2 min old → 'Active now'
    - 2–59 min → 'Active {N}m ago' (floored minutes)
    - 1–23 h → 'Active {N}h ago' (floored hours)
    - Same day but ≥ under-a-day boundary already covered by hours; ≥1 day and <7 days → 'Active this week'
    - ≥ ~7 days OR null input → null (D-21 cutoff: a stale member advertises no status)
  </behavior>
  <read_first>
    - .planning/phases/11-presence-messaging/11-PATTERNS.md §lib/social/presence.ts (the exact module shape and bucket thresholds)
    - lib/social/dm.ts (the pure-function module convention: no Supabase client at module top level, named exports, types first)
    - __tests__/presence.test.ts (the binding contract created in Task 1 — implement to satisfy it)
  </read_first>
  <action>
    Create `lib/social/presence.ts` as a pure-function module (no Supabase import). Export `formatPresenceStatus(lastSeenAt: string | null): string | null` implementing the D-21 bucket ladder from RESEARCH Pattern 5 / PATTERNS §presence: null-in → null; diff < 2 min → 'Active now'; < 60 min → `Active ${Math.floor(diffMin)}m ago`; < 24 h → `Active ${Math.floor(diffHr)}h ago`; < 7 days → 'Active this week'; else null. Use `Date.now() - new Date(lastSeenAt).getTime()` for the diff. Match the exact string outputs the Task 1 tests assert. Do not add `date-fns` — the inline arithmetic is sufficient (RESEARCH Environment Availability: inline helper preferred).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=presence 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `lib/social/presence.ts` exists and exports `formatPresenceStatus`
    - `npm test -- --testPathPattern=presence` passes (GREEN)
    - No `import` of `@supabase` or any I/O in the module (grep: file has no `supabase` reference)
    - No `date-fns` import added (grep confirms)
  </acceptance_criteria>
  <done>`formatPresenceStatus` passes all presence-bucket assertions including the >7-day null cutoff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — dm.ts gate/rate-limit helpers + unread predicate + constants</name>
  <files>lib/social/dm.ts</files>
  <behavior>
    - countRecentRequests filters status='pending' + requester_id + created_at within 7 days, returns count ?? 0
    - isConnected filters status='accepted' with an either-direction .or(), returns boolean
    - hasUnread(lastReadAt, latestMessageAt) → true if marker null or message strictly newer; false otherwise
    - BASELINE_REQUEST_LIMIT=10, VERIFIED_REQUEST_LIMIT=30, PENDING_STACK_CAP=3 exported
  </behavior>
  <read_first>
    - lib/social/dm.ts (extend in place — preserve canonicalPair/findThread/ensureThread/loadConversation exactly)
    - .planning/phases/11-presence-messaging/11-PATTERNS.md §lib/social/dm.ts (exact new-helper signatures and query chains)
    - app/api/connections/route.ts (the connections either-direction `.or()` filter shape to mirror in isConnected — lines 69-77 and the requester/addressee column names)
    - __tests__/dm-request.test.ts and __tests__/dm-unread.test.ts (binding contracts from Task 1)
  </read_first>
  <action>
    Extend `lib/social/dm.ts` (keep all four existing exports byte-for-byte). Add these named exports:

    Constants (D-15/D-18): `export const BASELINE_REQUEST_LIMIT = 10`, `export const VERIFIED_REQUEST_LIMIT = 30`, `export const PENDING_STACK_CAP = 3`. These are the tunable rate-limit constants; this file is their home (Claude's-discretion resolution).

    `countRecentRequests(supabase: SupabaseClient, requesterId: string): Promise<number>` — rolling 7-day window (D-14): compute `since = new Date(Date.now() - 7*24*60*60*1000).toISOString()`, query `dm_threads` with `.select('id', { count: 'exact', head: true }).eq('requester_id', requesterId).eq('status', 'pending').gte('created_at', since)`, return `count ?? 0`.

    `isConnected(supabase: SupabaseClient, a: string, b: string): Promise<boolean>` — query `connections` with `.select('id').eq('status', 'accepted').or('and(requester_id.eq.'+a+',addressee_id.eq.'+b+'),and(requester_id.eq.'+b+',addressee_id.eq.'+a+')').maybeSingle()`, return `!!data`. This is the CONNECT-05 gate; it reads the connections table read-only.

    `countPendingMessagesFrom(supabase: SupabaseClient, threadId: string, senderId: string): Promise<number>` — the D-18 stacked-message cap query: `dm_messages` `.select('id', { count: 'exact', head: true }).eq('thread_id', threadId).eq('sender_id', senderId)`, return `count ?? 0`.

    `hasUnread(lastReadAt: string | null, latestMessageAt: string | null): boolean` — pure comparison (no client): return `false` if `latestMessageAt` is null; return `true` if `lastReadAt` is null; otherwise `new Date(latestMessageAt).getTime() > new Date(lastReadAt).getTime()`. This is the D-07 unread rule expressed as a pure timestamp comparison — no counter.

    Import `type { SupabaseClient } from '@supabase/supabase-js'` (already imported at top). All async helpers return data-or-primitive following the existing module convention.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='dm-request|dm-unread' 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `lib/social/dm.ts` still exports `canonicalPair`, `findThread`, `ensureThread`, `loadConversation` (grep confirms all four)
    - New exports present: `countRecentRequests`, `isConnected`, `countPendingMessagesFrom`, `hasUnread`, `BASELINE_REQUEST_LIMIT`, `VERIFIED_REQUEST_LIMIT`, `PENDING_STACK_CAP`
    - `npm test -- --testPathPattern='dm-request|dm-unread'` passes (GREEN)
    - `isConnected` filters on `status` = accepted and uses an either-direction `.or(` (grep confirms both)
  </acceptance_criteria>
  <done>All rate-limit/connection/unread helpers pass their Task 1 assertions; existing dm.ts exports unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Add message_request + new_dm to the notification catalog and builders</name>
  <files>lib/social/notifications.ts</files>
  <read_first>
    - lib/social/notifications.ts (extend in place — the NOTIFICATION_TYPES catalog lines 21-32, the NotificationPayload type lines 38-48, and buildConnectionRequestNotification as the exact builder shape to mirror)
    - .planning/phases/11-presence-messaging/11-PATTERNS.md §lib/social/notifications.ts (the two new builder signatures + catalog entries)
    - lib/notifications/index.ts (createNotification args — confirms actorId/actorName/actorAvatarUrl fields the builders must supply)
  </read_first>
  <action>
    Extend `lib/social/notifications.ts`. In the `NOTIFICATION_TYPES` object (before the `as const` close) add two entries so the existing catalog `NotificationType` union covers the new types the Phase 10 panel already renders generically: `message_request: { icon: 'message-circle', inlineAction: 'message_request_respond' }` and `new_dm: { icon: 'message-square', inlineAction: null }`.

    Append two builders following the `buildConnectionRequestNotification` shape exactly (same field set, returning `NotificationPayload`):

    `buildMessageRequestNotification(args: { recipientId, actorId, actorName, actorAvatarUrl, actorHandle, threadId, preview })` → `{ userId: recipientId, type: 'message_request', title: '${actorName} sent you a message request', body: preview, link: '/messages?thread=${threadId}', data: { threadId }, actorId, actorName, actorAvatarUrl }`. `preview` is the truncated (~60 char) first-message body — the caller truncates.

    `buildNewDmNotification(args: { recipientId, actorId, actorName, actorAvatarUrl, threadId })` → `{ userId: recipientId, type: 'new_dm', title: '${actorName} replied to your message', link: '/messages?thread=${threadId}', data: { threadId }, actorId, actorName, actorAvatarUrl }`.

    Keep the existing suppression-note comment header intact; these are additive. Do not touch buildMarkAllReadFilter or the cursor helpers.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c 'notifications.ts' | grep -q '^0$' && grep -q "buildMessageRequestNotification" lib/social/notifications.ts && grep -q "buildNewDmNotification" lib/social/notifications.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `NOTIFICATION_TYPES` contains `message_request` and `new_dm` keys (grep confirms)
    - `buildMessageRequestNotification` and `buildNewDmNotification` are exported and return the `NotificationPayload` shape
    - `buildMessageRequestNotification` sets `type: 'message_request'` and `link` to `/messages?thread=${threadId}`
    - `npx tsc --noEmit` reports no error originating in `lib/social/notifications.ts`
    - No pre-existing builder (new_follower, connection_request, etc.) is modified
  </acceptance_criteria>
  <done>The catalog and two builders exist, typecheck clean, and match the Phase 10 payload shape for the Plan 03 send/accept routes to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → lib helpers | These are pure/query helpers; they carry no auth themselves — the API routes (Plan 03) that call them own the auth boundary. Their correctness (correct filters) is a security prerequisite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | `hasUnread`/`countRecentRequests` as pure logic | low | mitigate | Unread + rate-limit are computed by timestamp/COUNT (D-07/D-14), never a mutable stored counter; RED tests assert the comparison semantics so a future refactor to a cached counter fails CI |
| T-11-02 | Elevation of Privilege | `isConnected` filter correctness | medium | mitigate | Test asserts the accepted-status + either-direction `.or()` filter so the CONNECT-05 gate (consumed by Plan 03) cannot be silently weakened to match pending/declined rows |
| T-11-SC | Tampering | npm installs | high | mitigate | This plan installs NO packages (date-fns explicitly avoided per RESEARCH); slopcheck N/A. If any install is attempted, halt and add a blocking-human legitimacy checkpoint. |
</threat_model>

<verification>
- `npm test -- --testPathPattern='presence|dm-request|dm-unread'` passes (all three GREEN after Tasks 2-4)
- `npm test` full suite still green (no regression in prior Phase 8-10 tests)
- `npx tsc --noEmit` clean
- No new npm package added (git diff of package.json is empty)
</verification>

<success_criteria>
- Three test files under `__tests__/` covering PRESENCE-01/02/03, CONNECT-03/04/05 pure logic, all GREEN
- `lib/social/presence.ts` created; `lib/social/dm.ts` and `lib/social/notifications.ts` extended without breaking existing exports
- Rate-limit constants (10/30/3) live in `lib/social/dm.ts`
</success_criteria>

<output>
Create `.planning/phases/11-presence-messaging/11-01-SUMMARY.md` when done
</output>
