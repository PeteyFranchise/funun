---
phase: 11-presence-messaging
plan: 05
type: execute
wave: 4
depends_on: [11-03, 11-04]
files_modified:
  - app/(artist)/messages/page.tsx
  - components/messages/MessagesPageClient.tsx
  - components/messages/ThreadList.tsx
  - components/messages/ConversationView.tsx
  - components/messages/RequestView.tsx
  - components/messages/DockedWidget.tsx
  - components/messages/Composer.tsx
autonomous: true
requirements: [CONNECT-03, CONNECT-04, CONNECT-05, PRESENCE-02, PRESENCE-03]
must_haves:
  truths:
    - "The /messages page renders a two-pane inbox on desktop (thread list left, active conversation right) and single-pane full-screen on mobile below lg (D-04/D-05)"
    - "The thread list shows a separate Requests section for status='pending' threads addressed to the viewer, with a pending count chip (D-10)"
    - "Opening a thread fires POST /api/dm/read/[threadId] and clears its unread state (D-06 auto-read)"
    - "Composing to a non-connection shows a remaining-requests hint (D-17) and, at zero budget, a RateLimitWall with a Send-Connect-Request escape hatch replacing the composer (CONNECT-04, D-17)"
    - "The RequestView (pending thread, viewer is recipient) shows the message(s) then Accept / Decline / Block, calling the Plan 03 request routes; Block uses an inline confirmation (D-10/D-11/D-12)"
    - "The conversation/widget header shows presence status via formatPresenceStatus(last_seen_at) plus an emerald dot when Active now (PRESENCE-02, D-22)"
    - "The DockedWidget mounts via the layout wrapper's context, persists across navigation, and offers pop-out/close (D-03)"
  artifacts:
    - app/(artist)/messages/page.tsx
    - components/messages/ThreadList.tsx
    - components/messages/ConversationView.tsx
    - components/messages/RequestView.tsx
    - components/messages/DockedWidget.tsx
  key_links:
    - "MessagesPageClient calls GET /api/dm/threads, POST /api/dm/read, and (via ConversationView) /api/dm/send (Plan 03)"
    - "RequestView calls /api/dm/request/{accept,decline,block}/[threadId] (Plan 03)"
    - "DockedWidget is rendered by ArtistLayoutClient (Plan 04); pop-out calls the MessagesDockContext openDock()"
    - "presence status uses formatPresenceStatus() (Plan 01) over last_seen_at; live dot uses the presence-global channel presenceState() (Plan 04)"
  prohibitions:
    - statement: "Message bodies and previews must never be rendered via dangerouslySetInnerHTML"
      status: resolved
      verification: "grep components/messages for dangerouslySetInnerHTML → must be zero; bodies render as React text children"
    - statement: "The send-path connection/rate-limit gate must not be re-implemented client-side as the source of truth — the UI reflects the server's 429/400, it does not authorize the send"
      status: resolved
      verification: "the composer surfaces server responses (budget hint from server count, wall on 429); it does not itself decide who may message whom"
---

<objective>
Build the `/messages` inbox: the server page + client shell (two-pane desktop / single-pane mobile), `ThreadList` (with a separate Requests section + presence dots), `ConversationView` (message thread + auto-read + composer), `RequestView` (recipient accept/decline/block), the layout-level `DockedWidget`, and the composer with the rate-limit budget hint + `RateLimitWall`. Everything reuses `DmWidget.tsx`'s proven Realtime/optimistic-send patterns and the locked UI-SPEC visual tokens.

Purpose: This is the primary messaging surface (D-01). It makes CONNECT-03/04/05 and PRESENCE-02/03 real for the user — the request quarantine, the rate-limit wall, direct messaging for connections, presence in the header, and the persistent docked widget.
Output: the page + six client components under `components/messages/` and `app/(artist)/messages/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-presence-messaging/11-CONTEXT.md
@.planning/phases/11-presence-messaging/11-RESEARCH.md
@.planning/phases/11-presence-messaging/11-UI-SPEC.md
@.planning/phases/11-presence-messaging/11-PATTERNS.md
@components/profile/DmWidget.tsx
@lib/social/presence.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composer, RateLimitWall, budget hint, and shared message-bubble rendering</name>
  <files>components/messages/Composer.tsx</files>
  <read_first>
    - components/profile/DmWidget.tsx (the optimistic-send with revert, Enter-to-send, maxLength={4000}, send-button SVG, and bubble className tokens `bg-grad`/`bg-card2`/`border-hair` to reuse)
    - .planning/phases/11-presence-messaging/11-UI-SPEC.md §Copywriting Contract (Composer, Rate-limit wall/hint copy) and §Interaction Contracts Message composer (placeholders per connection state, icon-only send button, disabled rules; pending-thread stacked hint + disable at 3)
    - lib/social/dm.ts (BASELINE_REQUEST_LIMIT/VERIFIED_REQUEST_LIMIT/PENDING_STACK_CAP for the copy's {N})
  </read_first>
  <action>
    Create `components/messages/Composer.tsx` ('use client') — the shared composer used by ConversationView and DockedWidget. Props: `{ otherId, isConnection, remainingRequests, pendingCount, onSent }` (plus disabled state). Behavior:
    - Input + icon-only send button (paper-plane SVG from DmWidget, `aria-label="Send"`), Enter-to-send, `maxLength={4000}`, disabled when empty or busy.
    - Placeholder text per state (UI-SPEC copy): connection → "Write a message…"; non-connection first message → "Send a message request…".
    - Non-connection with `remainingRequests > 0`: render the `RequestsBudgetHint` below the input — "{N} message request{s} left this week" (`12px text-lavdim`). Pluralize on N.
    - Non-connection with `remainingRequests <= 0`: replace the whole composer with the `RateLimitWall` — heading "You've used all {N} message requests this week", body "Your next slot opens {date}. Send a Connect request to message directly — no request needed.", and a gradient CTA button "Send Connect Request" that links to the recipient's profile (`/u/{handle}` or a connect action). {date} is the earliest expiry of a pending request (7 days after the oldest pending request's created_at) — pass it in as a prop computed by the parent from the thread data; if unknown, show "in up to a week".
    - Pending thread (sender side): when `pendingCount >= 1`, show the stacked hint "You can add up to 3 messages while your request is pending."; disable the composer when `pendingCount >= PENDING_STACK_CAP`.
    - Optimistic send: mirror DmWidget.send() — insert tmp row via the `onSent`/lift-state callback, POST `/api/dm/send { toUserId: otherId, body }`, on !ok revert; on 429 surface the rate-limit error (UI-SPEC "You've reached your weekly request limit."); on 403 (blocked) swallow silently and leave the sent state unchanged (UI-SPEC blocked copy). Keep bubble/message state in the parent so both ConversationView and DockedWidget can reuse.

    Export a small `MessageBubble` component (or shared render fn) here reusing the DmWidget bubble className exactly (`bg-grad` for mine, `bg-card2 border border-hair text-lav` for theirs, `rounded-[14px]`, timestamp `opacity-60`), so ConversationView, DockedWidget, and RequestView all render bubbles identically. Never use dangerouslySetInnerHTML — bodies are React text children.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c 'messages/Composer' | grep -q '^0$' && grep -q 'aria-label="Send"' components/messages/Composer.tsx && grep -Lq 'dangerouslySetInnerHTML' components/messages/Composer.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `components/messages/Composer.tsx` exists and exports the composer + a shared bubble renderer
    - Placeholder switches between "Write a message…" and "Send a message request…" by `isConnection`
    - Renders `RequestsBudgetHint` when non-connection and `remainingRequests > 0`; replaces composer with `RateLimitWall` (heading + Connect-request CTA) when `remainingRequests <= 0`
    - Disables input at `pendingCount >= PENDING_STACK_CAP` and shows the stacked hint at `pendingCount >= 1`
    - No `dangerouslySetInnerHTML` anywhere in the file
    - `npx tsc --noEmit` clean for this file
  </acceptance_criteria>
  <done>The shared composer enforces the UI-SPEC composer states (budget hint, rate-limit wall, stacked cap) and reuses the DmWidget send/bubble patterns.</done>
</task>

<task type="auto">
  <name>Task 2: ThreadList (with Requests section + presence dots) and RequestView</name>
  <files>components/messages/ThreadList.tsx, components/messages/RequestView.tsx</files>
  <read_first>
    - components/profile/DmWidget.tsx (initials() helper, avatar span pattern, bubble rendering for RequestView's read-only message display)
    - components/nav/NotificationBell.tsx (Realtime subscribe + refetch discipline for keeping the thread list live)
    - .planning/phases/11-presence-messaging/11-UI-SPEC.md §Component Inventory ThreadList + RequestView, §Presence Dot States (8×8px emerald dot placement), §Copywriting Contract (empty states, Requests label, RequestView labels/buttons, Block inline confirmation)
    - .planning/phases/11-presence-messaging/11-PATTERNS.md §ThreadList, §RequestView (analogs + presence-dot rendering guidance)
    - lib/social/presence.ts (formatPresenceStatus for offline buckets)
  </read_first>
  <action>
    `components/messages/ThreadList.tsx` ('use client'). Props: threads (from GET /api/dm/threads), the active thread id, an onSelect callback, and a presence map (userId→online boolean from the presence-global channel presenceState(), passed from the page shell). Render:
    - A people-only search input at top (D-08) filtering the list by the other person's name/handle client-side — placeholder "Search by name…".
    - Normal threads (status='direct', or pending threads the viewer sent) as rows: 40×40 avatar (initials fallback), name (15px bold), last-message preview (14px text-lavdim truncate), timestamp (12px text-lavdim), an 8×8 unread dot (`bg-brandfuchsia`) when `hasUnread`, and an 8×8 emerald presence dot overlaid on the avatar when the other user is online (presence map). Selected row: `bg-card2` + `border-l-[3px] border-brandindigo`.
    - A separate "Requests" section BELOW the normal list for threads where `status='pending' AND requester_id !== viewerId` (received requests), with a section label and a `chip.info` count badge (UI-SPEC Requests accent). Empty states per UI-SPEC copy ("No messages yet" / "No pending requests").
    - Keep the list live: subscribe to dm_messages INSERTs (channel `dm-threads-list-${viewerId}`) and refetch GET /api/dm/threads on insert; never maintain a client-side counter. removeChannel on cleanup.

    `components/messages/RequestView.tsx` ('use client') — shown in the right pane (or as the widget body) when the active thread is `status='pending'` and the viewer is the recipient. Props: threadId, the requester's name/avatar, the message(s). Render the label "{Name} sent you a message request", the message bubble(s) read-only (reuse the Composer MessageBubble), then an action row: Accept (`bg-grad`), Decline (`bg-card2 border border-hairstrong`), Block (`text-rose-500 border border-rose-500/30 bg-rose-500/10`). Actions POST to `/api/dm/request/accept|decline|block/[threadId]` following the DmWidget fetch→revert pattern; on accept success, surface the post-accept empty state ("You've accepted. Say hello!") and let the parent swap to ConversationView; on decline, show "Request declined." Block uses an INLINE confirmation (no modal): replace the button row with "Block {Name}? They won't be notified." + "Yes, block" (rose gradient) / "Cancel" (ghost); on confirm POST block, then the thread leaves the requests list. Surface accept/block failures with the UI-SPEC error copy.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E 'messages/ThreadList|messages/RequestView' | grep -c error | grep -q '^0$' && grep -q 'Requests' components/messages/ThreadList.tsx && grep -q 'request/accept' components/messages/RequestView.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ThreadList.tsx` renders a separate Requests section filtered to `status==='pending' && requester_id!==viewerId` with a count chip and honors the UI-SPEC empty states
    - Presence dot (8×8 emerald) renders on a thread avatar only when the presence map marks that user online; unread dot (8×8 brandfuchsia) renders when `hasUnread`
    - `ThreadList` subscribes to dm_messages and refetches (no client-side unread counter); removeChannel on cleanup
    - `RequestView.tsx` POSTs to the three `/api/dm/request/*` routes and uses an inline Block confirmation (no modal/dialog element)
    - No `dangerouslySetInnerHTML` in either file
    - `npx tsc --noEmit` clean for both files
  </acceptance_criteria>
  <done>The thread list quarantines requests with a count chip and live presence dots; RequestView drives recipient accept/decline/block with inline block confirmation.</done>
</task>

<task type="auto">
  <name>Task 3: ConversationView, DockedWidget, and the /messages page shell</name>
  <files>components/messages/ConversationView.tsx, components/messages/DockedWidget.tsx, components/messages/MessagesPageClient.tsx, app/(artist)/messages/page.tsx</files>
  <read_first>
    - components/profile/DmWidget.tsx (the FULL component to inherit: Realtime subscribe lifecycle, 20s reconcile poll, optimistic send, fixed-position `.pf-dm` chrome, header, bubble list, auto-scroll listRef)
    - app/(artist)/vault/page.tsx (the server-component fetch → client-component prop pattern for the /messages page shell)
    - components/nav/ArtistLayoutClient.tsx (the MessagesDockContext openDock/closeDock the page uses to pop out a conversation — created in Plan 04)
    - .planning/phases/11-presence-messaging/11-UI-SPEC.md §Component Inventory ConversationView/DockedWidget, §Focal Point, §Interaction Contracts (two-pane, docked widget, auto-read, realtime/polling, breakpoint lg)
    - .planning/phases/11-presence-messaging/11-PATTERNS.md §ConversationView, §DockedWidget, §app/(artist)/messages/page.tsx
    - lib/social/presence.ts (formatPresenceStatus for the header status line)
    - lib/supabase/server.ts (createServerClient for the page's initial thread-list fetch)
  </read_first>
  <action>
    `components/messages/ConversationView.tsx` ('use client') — the two-pane right column (full height, not fixed). Inherit DmWidget's Realtime subscribe (`dm-${threadId}`, INSERT on dm_messages filtered by thread_id, dedupe by id), the 20s reconcile poll against `/api/dm/messages?with=${otherId}`, optimistic send via the shared Composer, and auto-scroll on new message. Header: 36×36 avatar + name + presence status line (`formatPresenceStatus(last_seen_at)`; emerald dot + "Active now" when the presence map marks them online, else the bucket text in `text-lavdim`). Auto-read on open (D-06): `useEffect` firing `POST /api/dm/read/${threadId}` when threadId/viewerId set (best-effort, no await). If the thread is `status='pending'` and the viewer is the recipient, render `RequestView` instead of the composer. A pop-out affordance (↗) in the header calls the MessagesDockContext `openDock(threadId)` to dock the conversation.

    `components/messages/DockedWidget.tsx` ('use client') — the bottom-right floating widget rendered by ArtistLayoutClient. Inherit DmWidget's fixed-position chrome EXACTLY (`fixed bottom-0 right-8 z-50 w-[336px] ... rounded-t-[14px] border border-hairstrong bg-card shadow-[...]`). Props: `{ threadId, viewerId, onClose }`. It fetches its own thread context (the other participant + messages) from `/api/dm/messages` / `/api/dm/threads`, reuses the same Realtime + poll + optimistic-send + Composer as ConversationView, header shows presence status (replacing the old "Direct message" sub-label with `formatPresenceStatus`), header controls: pop-out-to-page (↗ → `/messages?thread=${threadId}`, aria-label "Open in Messages") and close (chevron-down, aria-label "Close" → onClose). Auto-read on open. Desktop-only is enforced by the layout wrapper's `hidden lg:block`.

    `components/messages/MessagesPageClient.tsx` ('use client') — the inbox shell. Props: initial threads + viewerId + an optional `?thread=`/`?with=` initial selection. Holds active-thread state, subscribes to the presence-global channel (read-only `presenceState()` — do NOT open a second presence channel; reuse the one PresenceTracker created by tracking the same channel name `presence-global` and reading state) to build the presence map for ThreadList/ConversationView. Layout: `lg:` two-pane — `<ThreadList>` (320px fixed) left, `<ConversationView>`/`<RequestView>` right; below `lg`, single-pane (thread list; tapping a thread swaps to a full-screen conversation with a back affordance). Selecting a thread fires auto-read and sets active. Resolve an initial `?with={userId}` by finding-or-preparing the thread with that user (so the profile Message button — Plan 06 — deep-links correctly).

    `app/(artist)/messages/page.tsx` — server component (mirror vault/page.tsx): `createServerClient`, get user (redirect to /signin if none), fetch the initial thread list server-side (explicit columns — never select('*') on artist_profiles), read `searchParams` for `thread`/`with`, and render `<MessagesPageClient initialThreads=... viewerId=user.id initialThreadId=... initialWith=... />`. Page title "Messages" / subtitle "Your direct conversations" per UI-SPEC copy.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E 'messages/ConversationView|messages/DockedWidget|MessagesPageClient|\(artist\)/messages/page' | grep -c error | grep -q '^0$' && grep -q "fixed bottom-0 right-8" components/messages/DockedWidget.tsx && grep -q "/api/dm/read/" components/messages/ConversationView.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/(artist)/messages/page.tsx` is a server component that fetches the initial thread list and renders `MessagesPageClient`; page title copy is "Messages"
    - `MessagesPageClient` renders two-pane at `lg` and single-pane below; resolves an initial `?with=`/`?thread=` selection
    - `ConversationView` fires `POST /api/dm/read/${threadId}` on open and shows a presence status line via `formatPresenceStatus`
    - `DockedWidget` uses the exact `fixed bottom-0 right-8 z-50 w-[336px]` chrome and has pop-out (aria-label "Open in Messages") + close (aria-label "Close") controls
    - No `select('*')` on artist_profiles in the page; no `dangerouslySetInnerHTML` in any component
    - `npx tsc --noEmit` clean; `npm run build` succeeds
  </acceptance_criteria>
  <done>The /messages inbox renders two-pane/single-pane, drives auto-read + presence status, and the navigation-persistent docked widget reuses the DmWidget chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (page) → artist_profiles | Initial thread-list render must use explicit column lists; select('*') 42501s under migration 040. |
| client → message body render | Untrusted member-authored message text crosses into the DOM; must render as React text, never HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-15 | Tampering | XSS via message body / preview / presence text | medium | mitigate | All message bodies, previews, and presence strings render as React text children; no dangerouslySetInnerHTML anywhere in components/messages (grep-verified) |
| T-11-16 | Information Disclosure | select('*') leaking private artist_profiles columns into the inbox | medium | mitigate | Page + client fetches use explicit column lists (artist_name, avatar_url, handle, last_seen_at) matching the migration-040 GRANT set |
| T-11-17 | Elevation of Privilege | UI implying a client-authorized send bypass | low | mitigate | The composer only reflects server decisions (server-computed budget, 429 wall, 403 silent-block); it never authorizes a send — the Plan 03 route is the gate |
| T-11-SC | Tampering | npm installs | high | mitigate | No packages installed (inline SVG icons, no date-fns/lucide). If any install is attempted, halt and add a blocking-human legitimacy checkpoint |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across page + six components
- `npm run build` succeeds
- `npm test` full suite green
- Manual (end-of-phase, human-check): two-pane desktop inbox; requests quarantined; rate-limit wall at zero budget; presence status in header; docked widget survives navigating away from /messages
</verification>

<success_criteria>
- /messages is the primary inbox (two-pane desktop, single-pane mobile) with a Requests section, auto-read, presence status, and rate-limit budget/wall
- RequestView drives accept/decline/block with inline block confirmation
- DockedWidget persists across navigation and reuses the .pf-dm chrome
</success_criteria>

<output>
Create `.planning/phases/11-presence-messaging/11-05-SUMMARY.md` when done
</output>
