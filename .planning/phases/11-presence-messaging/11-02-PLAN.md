---
phase: 11-presence-messaging
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - supabase/migrations/054_dm_request_status_presence.sql
  - __tests__/migration-054.test.ts
autonomous: false
requirements: [PRESENCE-01, PRESENCE-02, CONNECT-03, CONNECT-04]
user_setup:
  - service: supabase
    why: "Migration 054 must be pushed to the live database; the local operator runs the push (established Phases 8-10 convention) because supabase db push requires interactive confirmation"
    dashboard_config:
      - task: "Run `supabase db push` then `npx supabase migration list` and confirm 054 shows in both LOCAL and REMOTE columns"
        location: "local terminal (project linked to project-ref wgfjakfiyeewzfuxkgyo)"
must_haves:
  truths:
    - "dm_threads carries a status column ('direct'|'pending'|'declined') defaulting to 'direct', so all pre-existing threads are grandfathered as direct (CONNECT-03, D-DISCRETION grandfathering)"
    - "dm_threads carries a requester_id column recording who started the thread, enabling correct rate-limit counting and Requests-section direction (CONNECT-04, RESEARCH Pitfall 5)"
    - "artist_profiles carries a last_seen_at TIMESTAMPTZ column with column-level SELECT granted to authenticated/anon per the migration-040 doctrine (PRESENCE-01/02)"
    - "Migration 054 is pushed live and verified present in both LOCAL and REMOTE via supabase migration list"
  artifacts:
    - supabase/migrations/054_dm_request_status_presence.sql
    - __tests__/migration-054.test.ts
  key_links:
    - "dm_threads.status is the column Plan 03's send-gate sets to 'pending' and the accept/decline routes transition"
    - "dm_threads.requester_id is read by countRecentRequests() (Plan 01) and the Requests section (Plan 05)"
    - "artist_profiles.last_seen_at is written by Plan 03's heartbeat route and read by every presence-status render (Plans 05/06)"
prohibitions:
  - statement: "artist_profiles must not be exposed via a table-level GRANT SELECT (would defeat the migration-040 column lockdown)"
    status: resolved
    verification: "grep migration 054 for any bare 'GRANT SELECT ON artist_profiles TO' without a column list → must be zero; only the column-scoped GRANT SELECT (last_seen_at) is permitted"
---

<objective>
Ship migration 054: add `status` and `requester_id` to `dm_threads` (the message-request state machine and rate-limit direction), and `last_seen_at` to `artist_profiles` (offline presence buckets), each following the migration-040 column-privilege doctrine. Then the operator pushes it live and verifies it in both LOCAL and REMOTE.

Purpose: Every Phase 11 server surface — the connection gate, the request flow, rate limiting, and presence "Active X ago" — depends on these three columns existing with the right privileges. This is the schema root of the phase; the build/type checks pass without the push, so the push is a mandatory blocking step to avoid a false-positive verification.
Output: `supabase/migrations/054_dm_request_status_presence.sql`, a migration-content assertion test, and a live-pushed, list-verified schema.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-presence-messaging/11-CONTEXT.md
@.planning/phases/11-presence-messaging/11-RESEARCH.md
@supabase/migrations/012_social_layer.sql
@supabase/migrations/036_notifications_dm_reads.sql
@supabase/migrations/040_artist_profiles_column_privileges.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 054 + content-assertion test</name>
  <files>supabase/migrations/054_dm_request_status_presence.sql, __tests__/migration-054.test.ts</files>
  <read_first>
    - supabase/migrations/012_social_layer.sql (dm_threads/dm_messages base schema, the a_id<b_id canonical CHECK, RLS policies — the table 054 alters)
    - supabase/migrations/036_notifications_dm_reads.sql (the closest structural analog: additive ALTER + idempotent guards + RLS; the dm_thread_reads table is already live)
    - supabase/migrations/040_artist_profiles_column_privileges.sql (the EXACT REVOKE/GRANT column-privilege pattern to follow for last_seen_at — lines 83-96 GRANT SELECT column list)
    - supabase/migrations/038_block_enforcement_existing_tables.sql (confirms dmt_insert_participant already carries no_block(); this migration must NOT drop or weaken that policy)
    - __tests__/connections.test.ts (the migration-content-assertion test style — readFileSync + toContain assertions)
  </read_first>
  <action>
    Create `supabase/migrations/054_dm_request_status_presence.sql`, idempotent, header-commented in the house style (see migration 036 header). Three additive changes:

    1. `dm_threads.status` (CONNECT-03, RESEARCH Pattern 3): `ALTER TABLE dm_threads ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'direct' CHECK (status IN ('direct', 'pending', 'declined'));`. The `DEFAULT 'direct'` grandfathers every existing thread as direct (Claude's-discretion grandfathering: the connection gate applies to NEW conversation starts only). Add a partial index for the Requests-section / rate-limit queries: `CREATE INDEX IF NOT EXISTS dm_threads_pending_idx ON dm_threads (requester_id, created_at) WHERE status = 'pending';`.

    2. `dm_threads.requester_id` (CONNECT-04, RESEARCH Pitfall 5): `ADD COLUMN IF NOT EXISTS requester_id UUID REFERENCES auth.users ON DELETE SET NULL;`. Records who started the thread so rate-limit COUNT and the Requests-section "received vs sent" split are correct. Nullable so grandfathered threads (no known initiator) are valid. Comment that existing direct threads legitimately have NULL requester_id.

    3. `artist_profiles.last_seen_at` (PRESENCE-01/02): `ADD COLUMN IF NOT EXISTS last_seen_at TIMESTAMPTZ;`. Then apply the migration-040 column-privilege doctrine — because migration 040 did `REVOKE SELECT ON artist_profiles FROM authenticated, anon` and re-granted an explicit column list, a NEW column has NO grant and would return 42501. Add exactly: `GRANT SELECT (last_seen_at) ON artist_profiles TO authenticated, anon;`. Do NOT grant UPDATE on last_seen_at to authenticated — it is written only by the service-role heartbeat route (Plan 03), so no authenticated UPDATE grant is needed (mirror migration 040's sound_identity rationale). Do NOT issue any table-level (columnless) GRANT SELECT on artist_profiles.

    Do NOT touch dm_threads RLS policies (leave dmt_insert_participant + its no_block() clause from migration 038 intact) — the connection gate lives in the API layer (Plan 03), not RLS.

    Also create `__tests__/migration-054.test.ts` (migration-content assertion, mirroring connections.test.ts's migration block): readFileSync of 054, assert it contains `ADD COLUMN IF NOT EXISTS status`, `CHECK (status IN ('direct', 'pending', 'declined'))`, `DEFAULT 'direct'`, `ADD COLUMN IF NOT EXISTS requester_id`, `ADD COLUMN IF NOT EXISTS last_seen_at`, and `GRANT SELECT (last_seen_at) ON artist_profiles`. Add a NEGATIVE assertion that the file does NOT contain a bare `GRANT SELECT ON artist_profiles TO` (columnless) — proving the column lockdown is preserved.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=migration-054 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/054_dm_request_status_presence.sql` exists and is idempotent (ADD COLUMN IF NOT EXISTS / CREATE INDEX IF NOT EXISTS throughout)
    - Contains the status CHECK constraint with exactly `('direct', 'pending', 'declined')` and `DEFAULT 'direct'`
    - Contains `requester_id UUID REFERENCES auth.users` and `last_seen_at TIMESTAMPTZ`
    - Contains `GRANT SELECT (last_seen_at) ON artist_profiles TO authenticated, anon` and NO columnless `GRANT SELECT ON artist_profiles TO`
    - Does NOT DROP or recreate `dmt_insert_participant` (grep confirms the policy name is absent from 054)
    - `npm test -- --testPathPattern=migration-054` passes
  </acceptance_criteria>
  <done>Migration 054 authored, idempotent, column-privilege-correct, and its content-assertion test is GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Push migration 054 and verify LOCAL=REMOTE</name>
  <files>supabase/migrations/054_dm_request_status_presence.sql</files>
  <action>
    [BLOCKING] The human operator runs the schema push and confirms it live, per this project's established convention (as in Phases 8-10; CI cannot run this unattended). Run `supabase db push` (non-TTY: `SUPABASE_ACCESS_TOKEN=... supabase db push`) to apply migration 054, then `npx supabase migration list` and confirm `054` is populated in BOTH the LOCAL and REMOTE columns (matching 001-053). Then run the two DB-level column spot-checks in <how-to-verify> against the live database. Do NOT proceed to Wave 3 until the push is confirmed live — build/type checks pass without it (types come from config, not the live DB), which would be a false-positive verification state.
  </action>
  <verify>
    <human-check>Operator confirms `npx supabase migration list` shows 054 in LOCAL and REMOTE, and the two column spot-checks return the expected rows / `t`.</human-check>
  </verify>
  <what-built>Migration 054 (dm_threads.status + requester_id, artist_profiles.last_seen_at + column GRANT) is authored and content-verified but not yet applied to the live Supabase database. Build and Jest pass without the push because types come from config, not the live DB — so this checkpoint is mandatory to reach a true verification state.</what-built>
  <how-to-verify>
    1. Ensure the project is linked: `npx supabase link --project-ref wgfjakfiyeewzfuxkgyo` (if not already linked).
    2. Run `supabase db push` and confirm it applies `054_dm_request_status_presence.sql` with no error.
    3. Run `npx supabase migration list` and confirm `054` appears in BOTH the LOCAL and REMOTE columns (matching 001-053).
    4. Spot-check the columns landed: in the Supabase SQL editor (or psql), run
       `SELECT column_name FROM information_schema.columns WHERE table_name='dm_threads' AND column_name IN ('status','requester_id');`
       (expect 2 rows) and
       `SELECT has_column_privilege('authenticated','artist_profiles','last_seen_at','SELECT');`
       (expect `t`).
  </how-to-verify>
  <resume-signal>Type "approved" once `migration list` shows 054 in LOCAL and REMOTE and the two column spot-checks pass, or describe the error.</resume-signal>
  <done>Migration 054 is applied live and confirmed present in LOCAL and REMOTE; dm_threads.status/requester_id and artist_profiles.last_seen_at exist with the correct SELECT privilege.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated role → artist_profiles columns | Migration 040 locked artist_profiles to a column-scoped GRANT; any new column defaults to no-grant. last_seen_at must be added to the grant deliberately and only for SELECT. |
| authenticated role → dm_threads | New status/requester_id columns are readable by thread participants via existing RLS; no new write path is opened by the migration (transitions happen through the API in Plan 03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Information Disclosure | artist_profiles.last_seen_at exposure | medium | mitigate | Column-scoped `GRANT SELECT (last_seen_at)` only — no table-level grant; presence is deliberately visible to all signed-in members this phase (D-23), and the >7-day null bucket (Plan 01) stops a stale value advertising indefinitely |
| T-11-04 | Tampering | authenticated overwriting last_seen_at | high | mitigate | NO UPDATE grant on last_seen_at for authenticated; only the service-role heartbeat route (Plan 03) writes it, so a member cannot forge their own "Active now" via direct PostgREST |
| T-11-05 | Elevation of Privilege | bypassing the connection gate by INSERTing dm_threads directly | high | mitigate | dm_threads keeps its migration-038 no_block() INSERT policy untouched; the connection gate itself is enforced in the API (Plan 03) — this migration does not open a new write path, and requester_id/status are set server-side |
| T-11-SC | Tampering | npm/supabase-cli installs | high | mitigate | No new packages; supabase CLI already in stack. Push is a human-gated blocking checkpoint (never auto-approved). |
</threat_model>

<verification>
- `npm test -- --testPathPattern=migration-054` passes
- `npm test` full suite green
- Operator confirms `npx supabase migration list` shows 054 in LOCAL and REMOTE
- Column spot-checks return the expected rows / `t`
</verification>

<success_criteria>
- dm_threads has status (default 'direct') + requester_id; artist_profiles has last_seen_at with column-scoped SELECT grant
- Migration is idempotent and preserves the migration-038 no_block() dm_threads policy
- Schema is live and list-verified (the phase cannot pass verification otherwise)
</success_criteria>

<output>
Create `.planning/phases/11-presence-messaging/11-02-SUMMARY.md` when done
</output>
