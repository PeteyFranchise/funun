---
phase: 16-gtm-beta-buyer-portal
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - lib/buyers/createBuyerAccount.ts
  - lib/email/buyerInvite.ts
  - app/api/admin/buyer-orgs/route.ts
  - app/api/admin/buyer-orgs/[id]/members/route.ts
  - app/api/buyer/members/route.ts
  - app/(admin)/admin/buyer-orgs/page.tsx
  - components/admin/BuyerOrgsAdmin.tsx
  - app/(buyer-portal)/layout.tsx
  - components/buyer/BuyerPortalNav.tsx
  - app/buyers/access/page.tsx
autonomous: true
requirements: [BUYER-03, BUYER-04, BUYER-06]
user_setup: []

must_haves:
  truths:
    - "A platform admin can create a buyer company and invite its first org admin from the admin panel (D-12)."
    - "An org admin can invite additional employees with a scoped requester or approver tier (D-13) without being a platform admin."
    - "A buyer account's app_metadata.role is set atomically inside admin.createUser() — never a post-insert UPDATE."
    - "Every route under the buyer portal is gated by the layout's own getUser() + role check, not by middleware."
    - "An unauthenticated visitor to a buyer route lands on a buyer-specific access page, never /signin."
  artifacts:
    - lib/buyers/createBuyerAccount.ts
    - app/api/admin/buyer-orgs/route.ts
    - app/api/buyer/members/route.ts
    - app/(buyer-portal)/layout.tsx
    - app/buyers/access/page.tsx
  key_links:
    - "createBuyerAccount() → admin.createUser({app_metadata:{role:buyer}}) → generateLink(magiclink) → Resend email (createIndustryMember precedent)."
    - "Account creation → buyer_members row (org_id, buyer_role, is_org_admin) written by the same service-role route."
    - "(buyer-portal)/layout.tsx → getUser() + app_metadata role check → redirect to /buyers/access (curator-portal precedent)."
---

<objective>
Build the account/org machinery on top of plan 16-01's schema: platform admins create buyer companies and their first org admin (D-12 — no self-serve org signup in beta), org admins invite employees with scoped tiers (D-13), and the buyer portal gets its own authenticated shell and access landing (D-11 separate login).

Purpose: Beta buyer orgs are founder-created and born verified (D-14). This plan wires the proven curator/industry account primitives to buyers so account creation is atomic and cannot produce a phantom artist_profiles row, and establishes the portal gate every buyer surface in Wave 3 renders inside.
Output: Buyer account-creation helper + invite email, admin buyer-orgs API and UI, org-admin member-invite API, buyer portal layout/nav, and the buyer access landing page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-gtm-beta-buyer-portal/16-CONTEXT.md
@.planning/phases/16-gtm-beta-buyer-portal/16-RESEARCH.md
@.planning/phases/16-gtm-beta-buyer-portal/16-01-SUMMARY.md

# Precedents to mirror exactly
@lib/industry/createIndustryMember.ts
@app/api/admin/members/route.ts
@app/(curator-portal)/layout.tsx
@lib/admin/gate.ts
@lib/buyers/permissions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buyer account creation helper + invite email</name>
  <files>lib/buyers/createBuyerAccount.ts, lib/email/buyerInvite.ts</files>
  <action>
    Write createBuyerAccount as a standalone reusable helper modelled line-for-line on lib/industry/createIndustryMember.ts, with one critical divergence: buyers follow the CURATOR early-return precedent, so this helper must NOT expect or create an artist_profiles row (D-11 fully separate — migration 062's handle_new_user buyer branch returns early).

    Set app_metadata role to the buyer value INSIDE the same service.auth.admin.createUser() call (never a post-insert UPDATE) so the trigger branch fires in the same transaction with no phantom-row race — this is the bug class the repo already fixed twice (RESEARCH Pitfall 1 / Pattern 2). Pass display_name plus the org id and buyer tier through user_metadata for downstream display. Then call generateLink with the magiclink type and send a custom Resend invite email — never Supabase's built-in invite template.

    After the auth user exists, insert the buyer_members row (org_id, user_id, buyer_role, is_org_admin, invited_by) using the service-role client, since migration 062 revoked client writes. Return the created user id and the email-delivery boolean so callers can warn the admin when Resend is unconfigured or delivery failed (the WR-04 pattern in createIndustryMember).

    Mirror createIndustryMember's error discipline: export a dedicated DuplicateBuyerAccountError for the genuine email-exists case (createUser error code email_exists or status 422) and throw a descriptive Error for every other failure, so a transient outage is never reported to the admin as "already invited".

    lib/email/buyerInvite.ts returns a subject and html for the invite, following the shape of the existing industry invite email module.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -Eq "app_metadata" lib/buyers/createBuyerAccount.ts && grep -Eq "generateLink" lib/buyers/createBuyerAccount.ts && grep -Eq "buyer_members" lib/buyers/createBuyerAccount.ts</automated>
  </verify>
  <done>createBuyerAccount sets the role atomically at creation, sends a custom magic-link invite, writes the membership row via the service role, distinguishes duplicates from transient failures, and never touches artist_profiles.</done>
</task>

<task type="auto">
  <name>Task 2: Admin buyer-org API + admin UI, and org-admin member invites</name>
  <files>app/api/admin/buyer-orgs/route.ts, app/api/admin/buyer-orgs/[id]/members/route.ts, app/api/buyer/members/route.ts, app/(admin)/admin/buyer-orgs/page.tsx, components/admin/BuyerOrgsAdmin.tsx</files>
  <action>
    Admin routes: gate every handler with verifyAdmin() from lib/admin/gate.ts before creating a service client, returning the gate's error and status verbatim (the app/api/admin/members precedent). GET lists buyer orgs with member counts using an explicit column list (never select star). POST creates a buyer org (D-12: admin creates the company record) and, in the same request, invites its first org admin via createBuyerAccount with the approver tier and the org-admin flag set — org admins are approvers with member management (D-13). Validate strictly against an explicit allowlist: org name, admin email, admin display name only. Never spread the request body into an insert. POST on the nested members route lets an admin add a member to an existing org.

    Buyer-facing route (app/api/buyer/members/route.ts): the same invite primitive, but gated by ORG MEMBERSHIP rather than platform admin — resolve the caller's buyer_members row via the session client, then require canManageMembers (lib/buyers/permissions.ts) to be true before calling createBuyerAccount for the new employee, forcing the new member's org_id to the caller's own org id rather than reading it from the body (an org admin must not be able to inject members into a different company). Accept only email, display name, and the requested tier, and coerce the tier through normalizeBuyerRole so an unknown value fails closed to requester.

    Admin UI: app/(admin)/admin/buyer-orgs/page.tsx is a server component fetching the org list; components/admin/BuyerOrgsAdmin.tsx is the client component with the create-org form and per-org member list, following the existing ReportsAdmin/VerificationAdmin component conventions established in 13-04/13-05. Add the Buyer orgs link to the admin sidebar in app/(admin)/layout.tsx only if that file is not otherwise being modified in this wave; if a nav slot already exists, extend it. Surface the email-delivery warning when the invite email could not be sent.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -Eq "verifyAdmin" app/api/admin/buyer-orgs/route.ts && grep -Eq "canManageMembers" app/api/buyer/members/route.ts</automated>
  </verify>
  <done>Admins create orgs plus a first org admin; org admins invite employees scoped to their own org with a validated tier; both paths reuse createBuyerAccount; no raw body assignment anywhere.</done>
</task>

<task type="auto">
  <name>Task 3: Buyer portal shell, nav, and access landing page</name>
  <files>app/(buyer-portal)/layout.tsx, components/buyer/BuyerPortalNav.tsx, app/buyers/access/page.tsx</files>
  <action>
    Write app/(buyer-portal)/layout.tsx as an exact structural mirror of app/(curator-portal)/layout.tsx: await createServerClient, call getUser, redirect an unauthenticated visitor to the buyer access landing (never /signin — buyers authenticate by magic link), then check app_metadata role equals the buyer value and redirect any non-buyer to the site root. Include the same explanatory header comment the curator layout carries, stating that this route group's path prefix is deliberately NOT added to middleware.ts's isProtected array and that this layout's own check is the sole gate (RESEARCH Pitfall 4). Do not modify middleware.ts in this plan.

    Additionally resolve the caller's buyer_members row in the layout and redirect to the access page if no membership exists, so an auth user carrying the buyer role but no org cannot reach portal surfaces.

    components/buyer/BuyerPortalNav.tsx renders the portal shell nav with static hrefs for all three Wave 3 surfaces — catalog, shortlists, and requests — so the page plans in Wave 3 add routes without editing this shared file. Show the buyer's company name in the shell (D-13a: company is always visible) and the member's own tier.

    app/buyers/access/page.tsx is the dedicated buyer sign-in landing, mirroring the curator claim-page pattern: a minimal branded page explaining that buyer access is invite-only during beta (D-12) with a magic-link email form. It must not offer self-serve org signup — that is an explicitly deferred idea.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -Eq "getUser" "app/(buyer-portal)/layout.tsx" && grep -Eq "app_metadata" "app/(buyer-portal)/layout.tsx" && grep -Eq "buyers/access" "app/(buyer-portal)/layout.tsx" && test -f app/buyers/access/page.tsx</automated>
  </verify>
  <done>Buyer routes are gated by the layout's own role + membership check; unauthenticated visitors reach the buyer access page; the portal nav exposes the three Wave 3 surfaces; middleware.ts is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated visitor → buyer portal routes | Layout gate is the sole authority. |
| org admin → member invitation | A buyer-tier user invoking an account-creation primitive. |
| platform admin → org creation | verifyAdmin-gated service-role writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-09 | Elevation of Privilege | POST /api/buyer/members org_id | high | mitigate | org_id is forced to the caller's own membership row, never read from the request body; canManageMembers must be true. |
| T-16-10 | Elevation of Privilege | buyer account role assignment | high | mitigate | app_metadata role set atomically inside admin.createUser(); no post-insert UPDATE window (RESEARCH Pattern 2). |
| T-16-11 | Spoofing | unauthenticated access to portal routes | high | mitigate | Layout-level getUser + role + membership check; route group deliberately excluded from middleware isProtected to avoid an inconsistent double gate (Pitfall 4). |
| T-16-12 | Elevation of Privilege | tier escalation via invite payload | medium | mitigate | Requested tier coerced through normalizeBuyerRole, failing closed to requester; is_org_admin is never client-settable on the buyer-facing route. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (zero new dependencies this phase). |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run lint` clean.
- Buyer portal layout mirrors the curator gate; middleware.ts unmodified (confirm with git diff).
- Admin org creation and org-admin employee invite both flow through createBuyerAccount.
</verification>

<success_criteria>
A founder can create a buyer company and its first org admin from the admin panel; that org admin can add employees at a chosen tier; invited buyers land in a gated portal shell via magic link with no artist_profiles row anywhere.
</success_criteria>

<output>
Create `.planning/phases/16-gtm-beta-buyer-portal/16-03-SUMMARY.md` when done.
</output>
