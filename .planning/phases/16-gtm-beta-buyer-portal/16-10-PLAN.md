---
phase: 16-gtm-beta-buyer-portal
plan: 10
type: execute
wave: 5
depends_on: ["16-07", "16-08", "16-09"]
files_modified:
  - lib/deals/delivery.ts
  - lib/deals/delivery.test.ts
  - app/api/buyer/deals/[id]/export/route.ts
  - components/buyer/DeliveryPanel.tsx
  - lib/deals/metrics.ts
  - lib/deals/metrics.test.ts
  - app/api/admin/deals/metrics/route.ts
  - app/(admin)/admin/deals/metrics/page.tsx
  - components/admin/GtmMetricsDashboard.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DELIVERY-01, METRICS-01, METRICS-02]

must_haves:
  truths:
    - "Once the contract is signed and the deal is paid, the buyer's dashboard unlocks the export-pack download for the licensed project (D-19)."
    - "Delivery reuses the Phase 14 export-pack pipeline — no parallel export system is built."
    - "A buyer cannot download a pack for a deal that is not both signed and paid, or for another org's deal."
    - "The founder sees every D-10 GTM metric computed from real deal data: closed deals, request-to-quote time, quote-to-close rate, average sync fee, artist readiness pass rate, and repeat/referral signal."
    - "Phase 16 requirement IDs are registered in .planning/REQUIREMENTS.md with phase traceability."
  artifacts:
    - lib/deals/delivery.ts
    - app/api/buyer/deals/[id]/export/route.ts
    - lib/deals/metrics.ts
    - app/(admin)/admin/deals/metrics/page.tsx
  key_links:
    - "isDeliveryUnlocked(deal) -> buyer export route -> existing lib/vault/export-pack pipeline -> signed URL."
    - "license_requests aggregate -> metrics module -> admin GTM dashboard (D-10 gates)."
---

<objective>
Close the loop: deliver the licensed assets through the portal by reusing Phase 14's export pack (D-19), and instrument the founder-led motion with the real beta metrics that gate the AE hire and any paid acquisition (D-10/D-06). Finally, register this phase's requirement IDs so traceability is complete.

Purpose: D-19 says the deal ends inside the product with no manual file sending, and D-10 says the GTM model must run on real closed-deal evidence rather than spreadsheet confidence — the adversarial review's P2 finding. The org request dashboard built in plan 16-06 is the substrate both of these read.
Output: The delivery gate with tests, the buyer-scoped export route, the metrics module and admin dashboard, and the REQUIREMENTS.md registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-gtm-beta-buyer-portal/16-CONTEXT.md
@.planning/phases/16-gtm-beta-buyer-portal/16-VALIDATION.md
@.planning/phases/16-gtm-beta-buyer-portal/16-08-SUMMARY.md
@.planning/phases/16-gtm-beta-buyer-portal/16-09-SUMMARY.md

# The pipeline to reuse, unchanged
@lib/vault/export-pack.ts
@app/api/vault/[projectId]/export/route.ts
@lib/deals/commission.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delivery unlock gate + buyer-scoped export route</name>
  <files>lib/deals/delivery.ts, lib/deals/delivery.test.ts, app/api/buyer/deals/[id]/export/route.ts, components/buyer/DeliveryPanel.tsx</files>
  <behavior>
    isDeliveryUnlocked(deal):
    - deal has a linked signed contract document AND a completed payment state -> true.
    - contract signed but payment not completed -> false.
    - paid but no signed contract linked -> false.
    - neither -> false.
    - a deal in any stage before contract -> false regardless of other fields (fail closed).
  </behavior>
  <action>
    Implement isDeliveryUnlocked in lib/deals/delivery.ts as a pure predicate over an already-fetched deal shape. Both conditions are required: D-19 unlocks delivery once the contract is signed, and D-17 makes payment the other half of a completed deal — shipping masters before the money has moved is the failure mode this gate exists to prevent. Write delivery.test.ts to the behavior block first (RED), then implement.

    The buyer export route resolves the caller's buyer membership, loads the deal, and returns 404 for a deal outside the caller's org (never 403 — no existence leak, consistent with plan 16-06). Then it enforces isDeliveryUnlocked, returning a descriptive error naming what is still outstanding when locked.

    Delivery must REUSE the existing export pipeline rather than building a second one. Wrap the established assemble-then-upload-then-sign approach from app/api/vault/[projectId]/export/route.ts: the route must never return archive bytes as the response body, because this project runs on Vercel Hobby with a hard 10s function ceiling — the pack is uploaded to Storage and the client receives a signed URL so the byte transfer happens client-to-Supabase outside the function budget. Preserve the existing Node runtime and maxDuration declarations, and reuse buildExportManifest and the PDF renderers unchanged. Scope the manifest to the licensed project and, where the pipeline supports it, the specific licensed tracks recorded in license_request_tracks.

    Extract the shared assembly into a reusable helper only if it can be done without changing the artist route's behavior; otherwise prefer a thin buyer-scoped wrapper over refactoring a working delivery path late in the phase.

    DeliveryPanel renders in the buyer request detail: a locked state explaining what remains (signature, payment, or both), and once unlocked, the download action plus what the pack contains.
  </action>
  <verify>
    <automated>npx jest lib/deals/delivery.test.ts && npx tsc --noEmit && npm run lint && grep -Eq "isDeliveryUnlocked" "app/api/buyer/deals/[id]/export/route.ts" && grep -Eq "buildExportManifest" "app/api/buyer/deals/[id]/export/route.ts"</automated>
  </verify>
  <done>Delivery unlocks only on signed-and-paid; cross-org deals 404; the export reuses the Phase 14 pipeline and never streams bytes through the function response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GTM beta metrics module + admin dashboard</name>
  <files>lib/deals/metrics.ts, lib/deals/metrics.test.ts, app/api/admin/deals/metrics/route.ts, app/(admin)/admin/deals/metrics/page.tsx, components/admin/GtmMetricsDashboard.tsx</files>
  <behavior>
    computeGtmMetrics(deals):
    - closedDeals counts only deals in the won terminal stage (GTM-01, gate is 3-5 before an AE hire).
    - requestToQuoteHours averages the elapsed time from submission to the first quote across deals that have been quoted; deals never quoted are excluded, not counted as zero.
    - quoteToCloseRate is won deals divided by quoted deals; with zero quoted deals it returns null, never a division-by-zero or a misleading zero (GTM-03).
    - averageSyncFeeCents averages the gross fee across won deals only (GTM-04).
    - repeatBuyerOrgs counts orgs with more than one submitted request (GTM-05).
    - adminCreatedShare reports the fraction of requests created through the manual-intake fallback, as the founder-touch/support-burden signal (GTM-07).
    - an empty deal list returns zeroed counts with null rates rather than throwing.
  </behavior>
  <action>
    Implement computeGtmMetrics in lib/deals/metrics.ts as a pure aggregation over already-fetched deal rows — no Supabase client — so every metric definition is unit-testable and unambiguous. Null-versus-zero discipline matters here: a rate with no denominator must be null so the dashboard shows "not enough data" instead of a number the founder might act on. Write metrics.test.ts to the behavior block first (RED), then implement.

    Add the artist readiness pass rate (GTM-06) as a separate exported function over requested projects: the share of requested projects that were rights-ready at request time, which is the signal for whether supply is blocking demand.

    The metrics API route is verifyAdmin-gated and reads license_requests with the service role (these aggregates span every org and include commission economics, so they are admin-only). The page is a server component; GtmMetricsDashboard renders each metric with its D-10 decision gate stated alongside it — most importantly that the AE hire is gated behind three to five closed deals and a written playbook, and that paid acquisition waits on quote-to-close evidence. Show sample sizes next to every rate so a 100% close rate on one deal cannot be misread as a trend.
  </action>
  <verify>
    <automated>npx jest lib/deals/metrics.test.ts && npx tsc --noEmit && npm run lint && grep -Eq "verifyAdmin" app/api/admin/deals/metrics/route.ts</automated>
  </verify>
  <done>All six D-10 metric families compute correctly with null-safe rates and empty-input handling; the dashboard is admin-gated and states the decision gate and sample size beside each metric.</done>
</task>

<task type="auto">
  <name>Task 3: Register Phase 16 requirements in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Append a Phase 16 requirements section to .planning/REQUIREMENTS.md documenting the IDs this phase introduced, each traced to the CONTEXT decision it implements, and extend the traceability table with a row per ID mapped to Phase 16 and its status. Do not disturb the existing v1.2 Green Room requirement sections or their coverage counts — this is an additive edit to a document that is the source of truth for another milestone's traceability. Use a scoped edit, never a whole-file rewrite.

    Register these IDs with one-line descriptions derived from the decisions:
    BUYER-01 separate buyer account type with no artist_profiles row (D-04/D-11); BUYER-02 buyer org and member schema with two permission tiers (D-11/D-13); BUYER-03 admin-created org plus first org-admin invite (D-12); BUYER-04 org-admin employee invites with scoped tiers (D-13); BUYER-05 org-level verification inherited by members (D-14); BUYER-06 dedicated buyer access landing and portal gate (D-11); BUYER-07 dual-level individual and company attribution (D-13a).
    DEAL-01 first-class license-request schema (D-07); DEAL-02 multi-track requests via join table; DEAL-03 deal-stage pipeline (D-16a); DEAL-04 per-project pre-cleared terms, the Marmoset five (D-15); DEAL-05 pre-cleared matching with admin-negotiation routing (D-15a); DEAL-06 commission economics on every deal (D-20); DEAL-07 server-owned writes and column-privilege doctrine on all deal tables.
    PORTAL-01 filtered rights-ready catalog browse with no free-text search (D-16); PORTAL-02 the single tunable rights-ready helper; PORTAL-03 org-shared shortlists (D-14c); PORTAL-04 structured request composer (D-02/D-07); PORTAL-05 org request dashboard with stages (D-16a).
    ARTIST-01 artist Deals room across all projects (D-15b); ARTIST-02 per-project pre-cleared terms settings (D-15).
    ADMIN-01 admin negotiation queue (D-15a/D-06); ADMIN-02 server-owned stage, owner, and commission transitions; ADMIN-03 manual intake writing to the same tables (D-03).
    MONEY-01 Stripe Connect Express onboarding, transfers-only (D-17a); MONEY-02 buyer Checkout payment with destination split (D-17/D-20); MONEY-03 signature-verified Stripe webhook.
    PAPER-01 SignWell adapter behind the e-sign interface (D-18a); PAPER-02 admin-drafted sync license sent for embedded signature (D-18); PAPER-03 signed contract filed into Contract Locker (D-08); PAPER-04 e-sign integration doc updated to SignWell (D-18a).
    DELIVERY-01 portal export-pack delivery unlock after signature and payment (D-19).
    METRICS-01 GTM beta metrics dashboard over real deal data (D-10); METRICS-02 founder-led sales instrumentation including support burden (D-10/D-06).

    Note on ID naming: use the METRICS- prefix for these two requirement IDs. The GTM-01 through GTM-07 identifiers are already taken — they are the GTM validation metric IDs defined in 16-VALIDATION.md's GTM Validation table — and reusing that prefix for requirements would make traceability ambiguous. Where this plan's metric behaviors reference GTM-01 through GTM-07, those are citations of the validation doc's metric definitions, not requirement IDs.

    Also update the Phase 16 Requirements line in .planning/ROADMAP.md, replacing the TBD placeholder with the registered ID families, using a scoped edit that leaves every other phase entry untouched.
  </action>
  <verify>
    <automated>grep -Eq "BUYER-01" .planning/REQUIREMENTS.md && grep -Eq "DELIVERY-01" .planning/REQUIREMENTS.md && grep -Eq "METRICS-02" .planning/REQUIREMENTS.md && grep -Eq "PROFILE-01" .planning/REQUIREMENTS.md && grep -Eq "FEED-18" .planning/REQUIREMENTS.md && echo REQUIREMENTS_INTACT</automated>
  </verify>
  <done>All 34 Phase 16 requirement IDs are registered with decision traceability and mapped in the traceability table; pre-existing v1.2 requirements remain intact; the ROADMAP TBD placeholder is replaced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buyer session -> licensed asset delivery | Master audio and stems leave the platform here; the gate is the last control. |
| admin session -> aggregate deal economics | Cross-org commercial data including commission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-44 | Elevation of Privilege | export download before signature or payment | critical | mitigate | isDeliveryUnlocked requires both a linked signed contract and completed payment; enforced server-side in the route, not by UI state. |
| T-16-45 | Information Disclosure | cross-org export access | critical | mitigate | Deal scoped to the caller's org with a 404 response for anything else. |
| T-16-46 | Information Disclosure | aggregate commission economics reaching non-admins | high | mitigate | Metrics route is verifyAdmin-gated and service-role-read; no buyer or artist surface consumes it. |
| T-16-47 | Denial of Service | export assembly exceeding the Vercel Hobby ceiling | medium | mitigate | Reuse the existing assemble-then-upload-then-sign pattern with its size cap; never stream archive bytes through the response body. |
| T-16-48 | Repudiation | signed URL shared beyond the licensee | medium | accept | Short-TTL signed URLs match the existing artist export behaviour; contractual terms govern redistribution at beta scale. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan. |
</threat_model>

<verification>
- `npx jest lib/deals/delivery.test.ts lib/deals/metrics.test.ts` green.
- `npx tsc --noEmit` and `npm run lint` clean.
- A locked deal's export attempt returns a descriptive error; an unlocked one returns a signed URL.
- REQUIREMENTS.md contains all Phase 16 IDs with the v1.2 sections intact.
</verification>

<success_criteria>
A paid, signed deal delivers its export pack to the buyer inside the product with no manual file sending, the founder can read every D-10 gate metric from real data with honest sample sizes, and Phase 16 requirements are traceable.
</success_criteria>

<output>
Create `.planning/phases/16-gtm-beta-buyer-portal/16-10-SUMMARY.md` when done.
</output>
