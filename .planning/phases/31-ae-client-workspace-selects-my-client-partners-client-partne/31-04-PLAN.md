---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 04
type: execute
wave: 2
depends_on: ["31-02", "31-03"]
files_modified:
  - lib/selects/persistence.ts
  - app/api/admin/selects/route.ts
  - app/api/admin/selects/[id]/route.ts
  - app/api/admin/selects/[id]/tracks/route.ts
  - app/api/admin/selects/[id]/send/route.ts
autonomous: true
requirements: [R11, R5, D-02]
user_setup: []
must_haves:
  truths:
    - "An AE can create a Selects for one of THEIR assigned clients, add/remove/reorder Crate tracks with per-track notes, set a cover note, and Send it; leadership can do so for any client (own-book scoping via requireStaff + isAssignedToOrg on every route)."
    - "Adding a track already present in the Selects is idempotent (no duplicate selects_tracks row) — re-add returns the existing row (R11 AC)."
    - "An empty Selects cannot be sent — the send route rejects a Selects with zero non-removed tracks (R11 AC)."
    - "A track's rights-ready status is evaluated via lib/deals/catalog.ts (isRightsReady/isAdmittedToSyncLibrary), never re-derived inline, and surfaced on the track payload so the builder can flag a not-rights-ready track before send."
    - "Send mints/keeps the share_token, sets status via isLegalSelectsTransition(draft|changes_requested → sent), stamps sent_at, and applies the AE's download_enabled / download_max_seconds settings (D-02)."
    - "Remove is a SOFT remove (sets removed_at/removed_by; the row stays for the Removed tray), never a hard delete (co-edit cannot wipe curation)."
  artifacts:
    - "lib/selects/persistence.ts — service-role CRUD guards (create, add/remove/reorder track, send) reused by routes"
    - "app/api/admin/selects/route.ts, [id]/route.ts, [id]/tracks/route.ts, [id]/send/route.ts"
  key_links:
    - "Every route calls requireStaff() then isAssignedToOrg(org, user.id) (leadership bypasses) BEFORE any service-role write — the own-book gate."
    - "The send route is the single place share_token becomes live and status→sent — it is the boundary the public player (31-13) reads across."
  prohibitions:
    - statement: "MUST NOT show/allow an AE to write any client/Selects data outside their own assigned book (R5/R6)."
      status: resolved
      verification: "test — an assignment-scope unit/route test asserts a non-leadership AE creating/reading a Selects for an unassigned org gets 404 (not 403 — no existence leak)."
    - statement: "MUST NOT re-derive rights-readiness inside the Selects builder (single source: lib/deals/catalog.ts)."
      status: resolved
      verification: "judgment — code review confirms isRightsReady/isAdmittedToSyncLibrary are imported, not re-implemented."
---

<objective>
Build the AE-facing Selects builder persistence + core API: create/list/patch/delete a Selects, add/remove(soft)/reorder/annotate its Crate tracks, and Send it (mint token, status→sent, apply the download gate). Everything is server-owned, own-book-scoped (requireStaff + isAssignedToOrg), and reuses the shipped rights-ready authority and the 31-03 status state machine.

Purpose: this is the write-side of R11 — the motion that ends in "a client receives a Selects." The builder UI (31-10) and the AI-draft/saved-search API (31-05) sit on top of these routes.
Output: lib/selects/persistence.ts + four API routes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/crate-lead-engine-BUILD-SPEC.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-SPEC.md
@lib/selects/stage-machine.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Selects CRUD routes + own-book scope guards + assignment-scope test</name>
  <read_first>
    - lib/admin/gate.ts — requireStaff(allowed) contract (returns {error,status} | {user,staffRole})
    - lib/staff/scope.ts — isAssignedToOrg(org, staffUserId) (pure, fail-closed)
    - app/(admin)/admin/my-client-partners/page.tsx — the verbatim own-book read pattern (role !== 'leadership' → .eq('ae_user_id', user.id))
    - app/(admin)/admin/client-partners/[orgId]/page.tsx — the notFound()-not-403 scope-denial pattern
    - lib/supabase/server.ts — createApiClient / createServiceClient factories
    - lib/selects/types.ts, lib/selects/stage-machine.ts — SelectsStatus + isLegalSelectsTransition
    - an existing /api/admin/* route (e.g. app/api/admin/buyer-orgs equivalent) for the NextResponse + zod-validation + EDITABLE_FIELDS allowlist convention
  </read_first>
  <action>
    Create lib/selects/persistence.ts with pure-ish service-role helpers (each takes an already-created service client): createSelects({orgId, aeUserId, name, coverNote, briefId?}), patchSelects (allowlisted fields only: name, cover_note, download_enabled, download_max_seconds), softDeleteSelects. Create app/api/admin/selects/route.ts: POST creates a Selects (zod-validate body; requireStaff(); resolve the target buyer_org; if staffRole !== 'leadership' require isAssignedToOrg(org, user.id) else notFound()); GET lists the caller's Selects (own-book scoped: non-leadership filtered to orgs where ae_user_id = user.id). Create app/api/admin/selects/[id]/route.ts: PATCH (allowlist via a SELECTS_EDITABLE_FIELDS const — includes download_enabled/download_max_seconds for D-02) and DELETE (soft), both re-checking own-book scope on the Selects' org before writing. Return 404 (notFound-style JSON) for a Selects whose org the caller does not cover — never 403 (no existence leak). Add an assignment-scope test (lib/selects/persistence.test.ts) asserting: createSelects payload for an unassigned org is rejected for a non-leadership AE; leadership is allowed; the allowlist rejects a non-listed field.
  </action>
  <acceptance_criteria>
    - `npx jest lib/selects/persistence.test.ts` passes, including the non-leadership-AE-unassigned-org rejection and the field-allowlist rejection.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
    - Every route calls requireStaff() and (for non-leadership) isAssignedToOrg before any service-role write; a not-covered org returns a 404-shaped response, not 403.
    - PATCH accepts download_enabled + download_max_seconds (D-02) and rejects unlisted fields.
  </acceptance_criteria>
  <verify>
    <automated>npx jest lib/selects/persistence.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Selects create/list/patch/delete are live, own-book-scoped, allowlisted, and 404-on-scope-denial; the assignment-scope test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Track add (idempotent) / remove (soft) / reorder / note routes with rights-ready</name>
  <read_first>
    - lib/deals/catalog.ts — isRightsReady(project, stage3), isAdmittedToSyncLibrary(project) (the single rights-ready authority; import, do not re-derive)
    - lib/deals/shortlists.ts — the stale-entry "degrade loudly at read time" pattern for catalogue rows
    - supabase/migrations/111_selects.sql — selects_tracks columns (track_id, position, note, source, removed_at, removed_by, added_by)
    - supabase/migrations/096_sync_library.sql — how a track's admission/rights state is read
  </read_first>
  <action>
    Create app/api/admin/selects/[id]/tracks/route.ts. POST adds a track: zod-validate {trackId, note?, source? default 'crate'}; own-book re-check on the Selects' org; IDEMPOTENT — if a non-removed selects_tracks row already exists for (selects_id, track_id) return it unchanged (no duplicate); if a soft-removed row exists, un-remove it (clear removed_at/removed_by) rather than inserting a second; set added_by = user.id, position = next. DELETE soft-removes (set removed_at/removed_by; do NOT hard delete). PATCH updates a track note or reorders (position). On every GET/return, resolve each track's rights-ready via lib/deals/catalog.ts and include a rights_ready boolean + a display badge field on the payload (read-time evaluation, so a track that lost readiness degrades loudly). All writes own-book-scoped.
  </action>
  <acceptance_criteria>
    - Re-adding an existing (non-removed) track returns the same row — no second selects_tracks row is created (idempotent, R11 AC).
    - DELETE sets removed_at (soft) — the row is not physically deleted.
    - Each returned track carries a rights_ready flag sourced from lib/deals/catalog.ts (grep confirms the import; no inline threshold re-implementation).
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "isRightsReady" app/api/admin/selects/\[id\]/tracks/route.ts</automated>
  </verify>
  <done>Track add is idempotent, remove is soft, notes/reorder work, and rights-ready is sourced from the single authority — all own-book-scoped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Send route — mint token, status→sent, empty-guard, download gate</name>
  <read_first>
    - lib/selects/stage-machine.ts — isLegalSelectsTransition (gate the draft|changes_requested → sent move)
    - supabase/migrations/111_selects.sql — share_token default, status CHECK, download_enabled/download_max_seconds, sent_at
    - .planning/phases/31-.../31-SPEC.md — R11 AC ("an empty Selects cannot be sent"; "Send produces a shareable link") + D-02 (download cap/disable at send)
  </read_first>
  <action>
    Create app/api/admin/selects/[id]/send/route.ts (POST). Own-book re-check. Reject with a 4xx + a clear message if the Selects has zero non-removed tracks (empty Selects cannot be sent — R11 AC). Validate the status move with isLegalSelectsTransition (current → 'sent') and reject an illegal move. On success: ensure share_token is present (it defaults at insert), set status = 'sent', stamp sent_at = now(), and persist the send-time download settings (download_enabled, download_max_seconds from the request or existing values — D-02). Return the shareable link path (/selects/{share_token}). Add a small unit test (lib/selects/persistence.test.ts extension or a send-guard test) asserting the empty-Selects guard and the illegal-transition rejection.
  </action>
  <acceptance_criteria>
    - Sending a zero-track Selects is rejected with a clear error (R11 AC); a ≥1-track Selects sends and returns a /selects/{token} path.
    - The status move is gated by isLegalSelectsTransition (grep confirms import + call); a Selects already 'approved' cannot be re-sent.
    - sent_at is stamped and the D-02 download settings persist.
    - `npx jest` for the send-guard test passes; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "isLegalSelectsTransition" app/api/admin/selects/\[id\]/send/route.ts</automated>
  </verify>
  <done>Send mints the live share link, enforces the empty-guard + legal status move, and applies the AE's download gate — the "a client receives a Selects" boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AE (staff) → another AE's book | Cross-AE create/read/write must be blocked server-side |
| AE request → service-role client | requireStaff must pass before any service-role write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-07 | Information Disclosure | own-book scoping on all selects routes | high | mitigate | requireStaff() + isAssignedToOrg() (leadership bypass) before every service-role read/write; scope-denial returns 404, not 403 (no existence leak). |
| T-31-08 | Elevation of Privilege | PATCH mass-assignment | medium | mitigate | SELECTS_EDITABLE_FIELDS allowlist (mirrors EDITABLE_FIELDS convention); no direct body assignment. |
| T-31-09 | Tampering | send status move | medium | mitigate | isLegalSelectsTransition gates the write; empty-Selects guard prevents an unusable share link. |
| T-31-SC | Tampering | package installs | high | accept | No new packages (existing Supabase/zod/Jest). |
</threat_model>

## Artifacts this phase produces (31-04)
- **lib/selects/persistence.ts** — service-role Selects CRUD guards (net-new)
- **API routes:** `app/api/admin/selects/route.ts`, `.../[id]/route.ts`, `.../[id]/tracks/route.ts`, `.../[id]/send/route.ts` (all net-new)
- **Const:** `SELECTS_EDITABLE_FIELDS` allowlist

<verification>
- `npx jest lib/selects/persistence.test.ts` green; `npx tsc --noEmit` clean; `npm run build` succeeds.
- Own-book scope test proves cross-AE access returns 404; idempotent add + soft remove + empty-send guard verified.
</verification>

<success_criteria>
An AE can curate and Send a Selects for their own client end-to-end via the API, with rights-ready from the single authority, an idempotent add, a soft remove, and the D-02 download gate applied at send.
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-04-SUMMARY.md` when done.
</output>
