---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 09
type: execute
wave: 3
depends_on: ["31-06", "31-04"]
files_modified:
  - components/admin/ClientWorkspace.tsx
  - components/admin/ContactsPanel.tsx
  - app/(admin)/admin/client-partners/[orgId]/page.tsx
  - app/(admin)/admin/clients/[personId]/page.tsx
autonomous: true
requirements: [R1, R5, D-05, D-08, D-09]
user_setup: []
must_haves:
  truths:
    - "The company workspace shows the four jobs (Contacts · Activity · Curation/Selects · Notes+status) plus the company website; the person workspace mirrors the same four tabs, person-scoped (R1)."
    - "The Contacts job lists multiple contacts with the primary flagged, opens a rich D-09 record, and supports add/edit/set-primary via the 31-06 API; the Notes+status job reads/appends the relationship log."
    - "A person appearing under a company (Companies-tab contacts) and in the Clients tab opens the SAME contact record — one record, two entry points (R1 adjacency edge)."
    - "Both workspaces are own-book-scoped: a non-leadership AE opening an uncovered org/person gets notFound() (not 403); leadership opens any (R5)."
    - "The Curation/Selects job lists this client's Selects (31-04 GET) with a Build Selects entry; the Activity job shows their briefs/license-requests read-only."
  artifacts:
    - "components/admin/ClientWorkspace.tsx — the four-job workspace shell (company + person)"
    - "components/admin/ContactsPanel.tsx — the Contacts CRM UI (list + rich record + set-primary)"
    - "app/(admin)/admin/client-partners/[orgId]/page.tsx — rebuilt company workspace (replaces ClientPartnerDetail member list)"
    - "app/(admin)/admin/clients/[personId]/page.tsx — new person workspace"
  key_links:
    - "The workspace's Notes tab renders the client_relationship_log via the 31-06 route — the same log 31.1's assignment entries append to."
    - "The person and company workspaces share ClientWorkspace; the person view is the same shell scoped to one contact (Game Plan panel is added in 31.1, not here)."
  prohibitions:
    - statement: "MUST NOT let an AE open a company/person workspace outside their own assigned book (R5)."
      status: resolved
      verification: "judgment/test — both pages call notFound() when a non-leadership AE is not isAssignedToOrg for the org (mirrors the shipped [orgId] pattern)."
    - statement: "MUST NOT surface any free-text cross-party field or another AE's notes in the workspace (staff-only, own-book)."
      status: resolved
      verification: "judgment — the workspace reads only the covered org's staff-only tables via requireStaff-gated routes."
---

<objective>
Build the four-job company/person workspace (R1): Contacts CRM · Activity · Curation/Selects · Notes+status, plus the company website. The person workspace mirrors the same shell scoped to one contact. Both are own-book-scoped; a person is one record reachable from both the Companies contacts and the Clients tab (adjacency edge).

Purpose: this is where the AE "works the account" — the drill-in destination from 31-08's list and the home of the CRM-lite people layer (D-05/D-08/D-09).
Output: ClientWorkspace + ContactsPanel + rebuilt company page + new person page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-UI-SPEC.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-SPEC.md
@.planning/design/phase-31-my-client-partners-mockup.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactsPanel — multi-contact list + rich record + set-primary</name>
  <read_first>
    - app/api/admin/client-partners/[orgId]/contacts/route.ts (31-06) — the CRUD + set-primary contract
    - .planning/design/phase-31-my-client-partners-mockup.html — the Contacts job visual (list, primary flag, record fields) — build to match
    - .planning/phases/31-.../31-UI-SPEC.md — the workspace tab treatment (secondary underline tabs) + Color/Typography contract
    - .planning/phases/31-.../31-CONTEXT.md — D-08 (one primary) + D-09 (rich record + custom fields)
  </read_first>
  <action>
    Create components/admin/ContactsPanel.tsx ('use client'): lists a company's contacts with the primary visibly flagged; opens a rich record (name, title, email(s), phone(s), linkedin_url, timezone, tags, address, notes, custom_fields) editable via the 31-06 route; supports add, edit, and set-primary (calls the set-primary endpoint; the one-primary invariant is server-enforced). One-tap email/call affordances on the primary. Export a person-scoped variant (single record) so the person workspace reuses it. Match the mockup; medium-weight typography (advisory per UI-SPEC).
  </action>
  <acceptance_criteria>
    - The panel lists multiple contacts with the primary flagged; set-primary re-renders exactly one primary.
    - The rich D-09 fields render and save through the 31-06 route.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test -f components/admin/ContactsPanel.tsx</automated>
  </verify>
  <done>ContactsPanel renders the multi-contact CRM record with a single primary and the full D-09 shape.</done>
</task>

<task type="auto">
  <name>Task 2: ClientWorkspace shell — four jobs + relationship log + Selects list</name>
  <read_first>
    - app/api/admin/client-partners/[orgId]/relationship-log/route.ts (31-06) — read/append notes
    - app/api/admin/selects/route.ts (31-04 GET) — the client's Selects list + Build Selects entry
    - components/admin/ContactsPanel.tsx (Task 1)
    - the existing briefs/license-request read source (buyer_briefs / license_requests) for the Activity job
    - .planning/design/phase-31-my-client-partners-mockup.html + .planning/phases/31-.../31-UI-SPEC.md — the four-job layout (secondary underline tabs), the company website slot, the empty-Curation "Build Selects" CTA
  </read_first>
  <action>
    Create components/admin/ClientWorkspace.tsx ('use client'): a header (name + status + company website for the company view) and a secondary underline-tab row for the four jobs — Contacts (ContactsPanel) · Activity (read-only briefs + license requests) · Curation/Selects (this client's Selects with per-Selects status + a Build Selects CTA that hands off to 31-10's builder) · Notes+status (the relationship log via the 31-06 route: read newest-first + append a note/conversation; company status shown). Accept a mode (company|person). Leave a clearly-marked slot where the person view's Game Plan panel mounts in 31.1 (do NOT build Game Plan here — R14 is deferred). Render the UI-SPEC empty states (No Selects yet → Build Selects).
  </action>
  <acceptance_criteria>
    - All four job tabs render; Contacts and Notes are interactive (via 31-06), Activity is read-only, Curation lists Selects with a Build Selects entry.
    - The company website renders in the company view; a Game-Plan mount slot is present but empty (31.1 marker).
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test -f components/admin/ClientWorkspace.tsx</automated>
  </verify>
  <done>The four-job workspace shell renders Contacts/Activity/Curation/Notes with the relationship log and a Selects list, company + person modes.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild company page + new person page (own-book, one-record adjacency)</name>
  <read_first>
    - app/(admin)/admin/client-partners/[orgId]/page.tsx — the current scope-denial pattern (notFound if !isAssignedToOrg for non-leadership) to preserve; replace ClientPartnerDetail with ClientWorkspace
    - lib/admin/gate.ts (getStaffRole) + lib/staff/scope.ts (isAssignedToOrg)
    - components/admin/ClientWorkspace.tsx (Task 2)
    - .planning/phases/31-.../31-SPEC.md — R1 adjacency edge (a person appears in both tabs and opens the SAME record)
  </read_first>
  <action>
    Rebuild app/(admin)/admin/client-partners/[orgId]/page.tsx to server-fetch the org + its contacts + Selects + relationship log (own-book: notFound() for a non-leadership AE that is not isAssignedToOrg) and render ClientWorkspace mode="company". Create app/(admin)/admin/clients/[personId]/page.tsx: resolve the person → their buyer_org, apply the SAME own-book notFound() gate on that org, and render ClientWorkspace mode="person" scoped to the one contact — so the Clients-tab entry and the Companies-tab contact entry resolve to one shared record (adjacency edge). Both pages use the shipped px-9 py-[30px] page-wrapper convention.
  </action>
  <acceptance_criteria>
    - A non-leadership AE opening an uncovered org OR an uncovered person's page gets notFound() (not 403); leadership opens any.
    - The person page and the company's contact entry open the same underlying contact record (one record, two entry points).
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Both workspaces are live, own-book-scoped, and a person is a single record reachable from both tabs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AE session → org/person workspace | Cross-AE workspace access must be blocked server-side (notFound) |
| staff-only tables → workspace render | Contacts/log are staff-only; the workspace must only read covered-org data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-21 | Information Disclosure | workspace own-book scope | high | mitigate | Both pages apply the notFound()-not-403 own-book gate before rendering; leadership bypass; person page gates on the person's org. |
| T-31-22 | Information Disclosure | contact/log render | medium | mitigate | Reads only via requireStaff-gated 31-06 routes against covered-org staff-only tables. |
| T-31-SC | Tampering | package installs | high | accept | No new packages. |
</threat_model>

## Artifacts this phase produces (31-09)
- **components/admin/ClientWorkspace.tsx** — four-job workspace shell (net-new)
- **components/admin/ContactsPanel.tsx** — Contacts CRM UI (net-new)
- **Rebuilt:** `app/(admin)/admin/client-partners/[orgId]/page.tsx` (replaces ClientPartnerDetail)
- **New:** `app/(admin)/admin/clients/[personId]/page.tsx`

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Own-book notFound gate on both pages; one-record adjacency; four jobs render.
</verification>

<success_criteria>
The AE works the account from a four-job workspace — CRM contacts, activity, Selects curation, and an append-only relationship log — on any client they cover, with a person resolving to one shared record.
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-09-SUMMARY.md` when done.
</output>
