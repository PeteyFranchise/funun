---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 11
type: execute
wave: 3
depends_on: ["31-07", "31-10"]
files_modified:
  - components/admin/CrateRequestsFeed.tsx
  - app/(admin)/admin/crate-requests/page.tsx
  - app/(admin)/admin/lead-engine/page.tsx
autonomous: true
requirements: [R10, R5]
user_setup: []
must_haves:
  truths:
    - "Crate Requests renders the intent-ranked feed (31-07) as rows — client name/avatar, activity-type icon, intent chip (Hot/Warm/New lead), and a single dominant one-click action per row (Build Selects / Reply / Follow up / See lead) (R10)."
    - "A guest/anonymous signal renders as a visible 'new lead' row (muted avatar placeholder + New-lead chip), not hidden or de-emphasized (R10 AC)."
    - "The room is own-book-scoped (feed comes from the own-book 31-07 route); the old read-only Lead Engine is retired/redirected to this room, not left as a parallel surface (R10 absorbs it)."
    - "The Build Selects action hands off to the 31-10 builder for that client; intent chips (Hot/Warm/New-lead thresholds) are the UI's Claude's-discretion labeling over the 31-07 intent weight."
  artifacts:
    - "components/admin/CrateRequestsFeed.tsx — the ranked demand-inbox feed component"
    - "app/(admin)/admin/crate-requests/page.tsx — the Crate Requests room"
    - "app/(admin)/admin/lead-engine/page.tsx — redirected/retired to Crate Requests"
  key_links:
    - "The feed reads the own-book 31-07 route (rankCrateRequests); Build Selects links to the 31-10 create-Selects path pre-targeting the row's client."
  prohibitions:
    - statement: "MUST NOT show an AE demand from clients outside their own book (R5)."
      status: resolved
      verification: "judgment — the page reads the own-book-scoped 31-07 feed; no unscoped client read here."
    - statement: "MUST NOT hide or drop a guest signal — it appears as a 'new lead' row (R10)."
      status: resolved
      verification: "judgment — guest rows render with the New-lead chip + muted avatar; sourced from the 31-07 new_lead items."
---

<objective>
Build the Crate Requests demand-inbox room (R10): render the own-book, intent-ranked feed (31-07) as scannable rows with intent chips and one dominant one-click action each, guests visible as new leads. Retire the read-only Lead Engine into this room.

Purpose: the human-facing half of R10 — the AE's demand inbox and the on-ramp to the Selects motion ("Build Selects" from a hot brief).
Output: CrateRequestsFeed + the room page + a Lead Engine redirect.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-UI-SPEC.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-SPEC.md
@.planning/design/phase-31-my-client-partners-mockup.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: CrateRequestsFeed component — ranked rows, intent chips, one-click actions, guest leads</name>
  <read_first>
    - app/api/admin/crate-requests/route.ts (31-07) — the ranked feed payload (intent weight, client tag, dominant action, guest new_lead rows)
    - .planning/design/phase-31-my-client-partners-mockup.html — the Crate Requests visual (rows, intent badges, single action button) — build to match
    - .planning/phases/31-.../31-UI-SPEC.md — "Crate Requests — demand inbox (R10)": Hot = fuchsia-tinted (NOT health-rose), Warm = amber, New lead = outlined neutral + guest glyph; a single compact action per row (not four buttons); empty-state copy
    - app/(admin)/admin/selects/[id]/page.tsx (31-10) — the Build Selects handoff target
  </read_first>
  <action>
    Create components/admin/CrateRequestsFeed.tsx ('use client'): render each ranked item as a row (client name/avatar, activity-type icon, intent chip, one dominant action button). Map the 31-07 intent weight to Hot/Warm/New-lead chips using the UI-SPEC color guidance (Hot = fuchsia-tinted chip, distinct from the health-rose; Warm = amber; New-lead = outlined neutral + guest glyph) — these Hot/Warm cutoffs are the UI's Claude's-discretion labeling. The dominant per-row action is a single compact button (Build Selects for a brief → hands off to the 31-10 builder pre-targeting the row's client; Reply/Follow up for a search/re-open; See lead for a guest — a minimal panel of what they did, consent-first, no fingerprinting). Guest rows render with a muted avatar placeholder + New-lead chip (visible, not de-emphasized). Render the UI-SPEC empty state ("No new demand yet").
  </action>
  <acceptance_criteria>
    - Each row shows the client tag, an intent chip, and exactly one dominant action button (not four).
    - Guest signals render as New-lead rows with a muted avatar; Build Selects links to the 31-10 builder for the row's client.
    - Hot chip uses the fuchsia-tinted treatment (not the health-rose).
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test -f components/admin/CrateRequestsFeed.tsx</automated>
  </verify>
  <done>The feed renders ranked rows with intent chips, a single one-click action each, and visible guest leads.</done>
</task>

<task type="auto">
  <name>Task 2: Crate Requests room page + retire Lead Engine</name>
  <read_first>
    - app/(admin)/admin/lead-engine/page.tsx — the read-only feed being absorbed (retire/redirect, do not keep parallel)
    - app/api/admin/crate-requests/route.ts (31-07) — the own-book feed source
    - app/(admin)/admin/my-client-partners/page.tsx — the staff-check + px-9 py-[30px] page convention
    - components/admin/CrateRequestsFeed.tsx (Task 1)
  </read_first>
  <action>
    Create app/(admin)/admin/crate-requests/page.tsx: staff-gated, fetch the own-book feed via the 31-07 route (leadership unscoped), render CrateRequestsFeed. Replace app/(admin)/admin/lead-engine/page.tsx body with a redirect() to /admin/crate-requests (retire the read-only surface into this room, per R10 — do not leave two feeds). The nav link for Crate Requests was added by 31-08 (layout.tsx) — do not touch layout.tsx here.
  </action>
  <acceptance_criteria>
    - /admin/crate-requests renders the ranked own-book feed; /admin/lead-engine redirects to it.
    - No parallel read-only Lead Engine feed remains.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "redirect" app/(admin)/admin/lead-engine/page.tsx && npm run build</automated>
  </verify>
  <done>The Crate Requests room is live and the Lead Engine is retired into it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AE session → demand feed | The feed must be own-book-scoped (via 31-07); guest attribution stays link/session-level |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-25 | Information Disclosure | feed scope in the room | high | mitigate | The room reads only the own-book 31-07 feed; no unscoped client read is added in the page. |
| T-31-26 | Spoofing | guest "See lead" panel | medium | mitigate | Consent-first: shows what the guest did + reach paths, never fingerprinting/bought data, never a guessed named contact. |
| T-31-SC | Tampering | package installs | high | accept | No new packages. |
</threat_model>

## Artifacts this phase produces (31-11)
- **components/admin/CrateRequestsFeed.tsx** — ranked demand-inbox feed (net-new)
- **Page:** `app/(admin)/admin/crate-requests/page.tsx` (net-new)
- **Retired:** `app/(admin)/admin/lead-engine/page.tsx` → redirect to Crate Requests

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Own-book feed, guest new-lead rows, single per-row action, Lead Engine retired.
</verification>

<success_criteria>
The AE's demand inbox is a ranked, own-book, guest-aware feed with one-click actions that flow into the Selects motion; the Lead Engine is absorbed.
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-11-SUMMARY.md` when done.
</output>
