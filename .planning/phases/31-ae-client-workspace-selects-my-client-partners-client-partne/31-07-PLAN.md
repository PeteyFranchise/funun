---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 07
type: execute
wave: 2
depends_on: ["31-02"]
files_modified:
  - lib/crate-requests/ranking.ts
  - lib/crate-requests/ranking.test.ts
  - app/api/admin/crate-requests/route.ts
autonomous: true
requirements: [R10, R5]
user_setup: []
must_haves:
  truths:
    - "The Crate Requests feed ranks buyer activity by intent in the locked order briefs > repeat searches > Selects re-opens > tag browsing, each item tagged to the originating client and carrying a one-click action (R10)."
    - "A guest/anonymous Crate signal with no account attached surfaces as a distinct 'new lead' row, not hidden or dropped (R10 AC)."
    - "The feed is own-book-scoped — an AE sees only their assigned clients' activity; leadership sees all (R5)."
    - "Intent ranking is a pure, deterministic function with a stable, documented tiebreak — the same input set always yields the same order (R10 backstop: held-out ranking-stability test)."
  artifacts:
    - "lib/crate-requests/ranking.ts — the pure intent-ranking + de-dup function"
    - "lib/crate-requests/ranking.test.ts — ranking + stability + guest-lead suite"
    - "app/api/admin/crate-requests/route.ts — the own-book-scoped feed route"
  key_links:
    - "This route absorbs/replaces the read-only Lead Engine (app/(admin)/admin/lead-engine) — the same buyer_briefs source, now a ranked multi-source actionable feed."
    - "Each feed row's 'Build Selects' action links to the 31-04 create-Selects path for that client."
  prohibitions:
    - statement: "MUST NOT show an AE Crate demand from clients outside their own assigned book (R5)."
      status: resolved
      verification: "test — the feed route returns only covered-org activity for a non-leadership AE."
    - statement: "MUST NOT drop or silently hide a guest/anonymous demand signal — it must appear as a 'new lead' row (R10)."
      status: resolved
      verification: "test — a guest signal with null buyer_org_id/created_by is present in the ranked output as a new-lead item."
---

<objective>
Build the Crate Requests demand inbox API (R10): an intent-ranked, own-book-scoped feed of buyer briefs + repeat searches + Selects re-opens + tag browsing, each tagged to a client with a one-click action, guests surfaced as new leads. This absorbs the read-only Lead Engine into a ranked, actionable feed.

Purpose: the read-side of R10; the room UI (31-11) renders it, and "Build Selects" hands off to the 31-04 Selects flow.
Output: lib/crate-requests/ranking.ts (+ test) + the feed route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/crate-lead-engine-BUILD-SPEC.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN — pure intent-ranking with stability + guest-lead + de-dup</name>
  <read_first>
    - .planning/design/crate-lead-engine-BUILD-SPEC.md §4 (Lead Engine rank: briefs > searches > browsing; deadline/budget boost; unactioned-first; newer-first) + §2 (buyer_briefs, buyer_search_activity, selects columns)
    - .planning/phases/31-.../31-SPEC.md — R10 (intent order + guest new-lead) + its Edge Coverage row (concurrency backstop: ranking stable + de-duped under concurrent inserts)
    - lib/deals/stage-machine.ts — the pure-function convention to mirror (no I/O)
    - app/(admin)/admin/lead-engine/page.tsx — the current read-only feed being absorbed
  </read_first>
  <action>
    Create lib/crate-requests/ranking.ts exporting a pure rankCrateRequests(items) that takes an already-fetched, normalized activity list (each item: kind in brief/repeat_search/selects_reopen/tag_browse, clientOrgId|null, isGuest, createdAt, actionedAt|null, plus optional deadline/budget) and returns a deterministically ordered, de-duped list: primary sort by intent weight (brief > repeat_search > selects_reopen > tag_browse), then unactioned-first, then newer-first, with a documented STABLE final tiebreak (item id) so equal keys never reorder run-to-run. De-dup collapses repeat signals of the same kind+client within a window into one ranked row (carrying a count). A guest item (isGuest / null client) is tagged 'new_lead' and kept in the output (never dropped). No I/O. Create lib/crate-requests/ranking.test.ts: assert the intent ordering; assert a held-out stability case (shuffle an equal-key input, expect identical output order — the R10 backstop); assert a guest item appears as a new_lead row; assert de-dup collapses duplicates deterministically. RED first, then implement to GREEN.
  </action>
  <acceptance_criteria>
    - `npx jest lib/crate-requests/ranking.test.ts` passes, including the shuffle-invariant stability case and the guest-new-lead case.
    - rankCrateRequests is a pure named export (no @/lib/supabase import).
    - De-dup is deterministic (same input → same collapsed output).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx jest lib/crate-requests/ranking.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Intent ranking is pure, deterministic, stable under shuffle, de-duped, and keeps guests as new leads — the R10 backstop is covered by a held-out test.</done>
</task>

<task type="auto">
  <name>Task 2: Own-book-scoped Crate Requests feed route</name>
  <read_first>
    - lib/crate-requests/ranking.ts (Task 1) — the ranker to feed
    - lib/admin/gate.ts (requireStaff) + lib/staff/scope.ts (isAssignedToOrg) + app/(admin)/admin/my-client-partners/page.tsx (own-book read pattern)
    - supabase/migrations/106_buyer_briefs.sql — buyer_briefs + (if present) buyer_search_activity source columns
    - supabase/migrations/111_selects.sql — selects (for re-open signals via sent/status + a viewed timestamp if available)
  </read_first>
  <action>
    Create app/api/admin/crate-requests/route.ts (GET). requireStaff(); collect the raw activity for covered orgs only (non-leadership → orgs where ae_user_id = user.id; leadership → all), normalize briefs / search activity / Selects re-opens / tag browsing into the ranker's item shape, include guest signals (null buyer_org_id) as new-lead items, then return rankCrateRequests(items) with each row carrying its client tag and its dominant one-click action (Build Selects for a brief, Reply/Follow up for a search/re-open, See lead for a guest). Do not compute Hot/Warm/New-lead thresholds in a second place — expose the intent weight and let the UI (31-11) label chips; the exact Hot/Warm cutoffs are Claude's-discretion per CONTEXT and belong in the UI.
  </action>
  <acceptance_criteria>
    - A non-leadership AE's feed contains only covered-org activity; leadership's is unscoped.
    - Guest signals appear as new-lead rows in the response.
    - The route calls rankCrateRequests (grep confirms) rather than re-implementing ordering.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "rankCrateRequests" app/api/admin/crate-requests/route.ts</automated>
  </verify>
  <done>The feed route returns own-book-scoped, intent-ranked activity with per-row actions and guests as new leads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AE (staff) → other AE's client demand | Cross-AE feed reads must be blocked server-side |
| guest activity → feed | Anonymous signals surface as leads without attributing them to a named contact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-16 | Information Disclosure | feed own-book scope | high | mitigate | requireStaff + covered-org filter before the service-role read; leadership unscoped. |
| T-31-17 | Spoofing | guest-lead attribution | medium | mitigate | A guest signal is a 'new_lead' row tied to the link/session, never attributed to a named contact it cannot verify. |
| T-31-18 | Tampering | ranking determinism | low | mitigate | Pure ranker with a stable id tiebreak + deterministic de-dup (held-out shuffle test). |
| T-31-SC | Tampering | package installs | high | accept | No new packages. |
</threat_model>

## Artifacts this phase produces (31-07)
- **lib/crate-requests/ranking.ts** — `rankCrateRequests` pure intent-ranker (net-new)
- **lib/crate-requests/ranking.test.ts** — ranking/stability/guest suite (net-new)
- **API route:** `app/api/admin/crate-requests/route.ts` (net-new; absorbs the Lead Engine read)

<verification>
- `npx jest lib/crate-requests/ranking.test.ts` green; `npx tsc --noEmit` clean; `npm run build` succeeds.
- Feed own-book-scoped; guests present as leads; ordering stable under shuffle.
</verification>

<success_criteria>
The Crate Requests demand inbox reads as an intent-ranked, own-book-scoped, guest-aware feed with per-row actions — the Lead Engine absorbed into an actionable surface.
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-07-SUMMARY.md` when done.
</output>
