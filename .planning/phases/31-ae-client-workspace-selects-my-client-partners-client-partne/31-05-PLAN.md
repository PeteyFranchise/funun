---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 05
type: execute
wave: 2
depends_on: ["31-02"]
files_modified:
  - lib/selects/ai-draft.ts
  - app/api/admin/selects/[id]/ai-draft/route.ts
  - app/api/admin/selects/saved-searches/route.ts
autonomous: true
requirements: [R11, R5, D-11, D-12]
user_setup: []
must_haves:
  truths:
    - "AI-draft produces a rights-ready-FIRST, ~10-track reviewable starter (tracklist + cover note + per-track 'why it fits') off a brief; the AE curates/edits before send — not a hard rights-ready-only filter (D-11: 'AI drafts, AE curates')."
    - "The AI-draft call mirrors lib/buyer/brief-ai.ts exactly (new Anthropic({apiKey}), model claude-sonnet-4-6, filter TextBlocks, single-shot structured JSON) — no new prompt scaffolding."
    - "Any AE can save a Crate search privately and flip one to team-shared; all AEs can recall team-shared searches with no leadership gate (D-12)."
    - "AI-draft and saved-search routes are staff-gated (requireStaff) and, for AI-draft, own-book-scoped to the target Selects' org."
  artifacts:
    - "lib/selects/ai-draft.ts — the Anthropic structured-draft helper (mirrors brief-ai.ts)"
    - "app/api/admin/selects/[id]/ai-draft/route.ts"
    - "app/api/admin/selects/saved-searches/route.ts"
  key_links:
    - "ai-draft reuses lib/deals/catalog.ts rights-ready to order the candidate pool rights-ready-first before the model call (D-11)."
    - "saved-searches.is_team_shared is the only cross-AE visibility this plan grants — read-only to peers, no leadership approval."
  prohibitions:
    - statement: "MUST NOT let an AE AI-draft into, or read/write, a Selects outside their own assigned book (R5)."
      status: resolved
      verification: "test — the ai-draft route returns 404 for a Selects whose org a non-leadership AE does not cover."
    - statement: "MUST NOT hard-filter the AI draft to rights-ready only (D-11 — badges shown, rights-ready prioritized, AE curates)."
      status: resolved
      verification: "judgment — the candidate pool is ordered rights-ready-first but may include near-rights-ready; the route flags rather than drops."
---

<objective>
Add the two "AI drafts, AE curates" affordances to the Selects motion: a brief→starter AI draft (D-11) that populates a ~10-track reviewable tracklist + cover note + per-track reasons into a draft Selects, and saved/team-shared Crate searches (D-12). Both reuse shipped patterns — the Anthropic structured-draft shape from lib/buyer/brief-ai.ts and the staff gate.

Purpose: completes R11's "AI-draft populates a starter" and "save a Crate search and re-apply / share with the team" acceptance criteria without inventing new AI or persistence scaffolding.
Output: lib/selects/ai-draft.ts + the ai-draft route + the saved-searches route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-SPEC.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI-draft helper + route (rights-ready-first starter)</name>
  <read_first>
    - lib/buyer/brief-ai.ts — draftBriefFromProse / rerankCandidates: the EXACT Anthropic SDK pattern (new Anthropic({apiKey}), model claude-sonnet-4-6, response.content.filter(TextBlock).map(text).join(''), parse structured JSON) to mirror
    - lib/buyer/brief.ts — the Brief schema (buyer_briefs.brief jsonb) the draft reads from
    - lib/deals/catalog.ts — isRightsReady/isAdmittedToSyncLibrary + buildCatalogFilter to assemble + order the candidate pool rights-ready-first
    - lib/admin/gate.ts + lib/staff/scope.ts — requireStaff + isAssignedToOrg
    - app/api/admin/selects/[id]/tracks/route.ts (31-04) — how a track is added (so the draft can persist starter tracks the same idempotent way)
    - .planning/phases/31-.../31-CONTEXT.md — D-11 (rights-ready-first, ~10 tracks, per-track 'why it fits', AE curates)
  </read_first>
  <action>
    Create lib/selects/ai-draft.ts exporting draftSelectsFromBrief(brief, candidatePool) that mirrors lib/buyer/brief-ai.ts: build a single-shot structured-output prompt asking for ~10 tracks (best-first) each with a short per-track "why it fits" reason plus a cover note; return a typed { ok: true, draft } | { ok: false, error } result (never throw). Order the candidatePool rights-ready-first using lib/deals/catalog.ts before handing it to the model (D-11 — prioritize, do not hard-filter). Create app/api/admin/selects/[id]/ai-draft/route.ts (POST): requireStaff(); own-book re-check on the target Selects' org (404 if not covered); resolve the linked brief (brief_id) and the covered candidate pool; call draftSelectsFromBrief; persist the returned starter tracks into selects_tracks (idempotent add, source 'crate', per-track note = the reason) and set the Selects cover_note if empty. Return the drafted set for the builder to review. Length-cap and best-effort: an AI failure returns a clear error, never a 500 that loses the draft Selects.
  </action>
  <acceptance_criteria>
    - The route imports the Anthropic pattern via lib/selects/ai-draft.ts (grep confirms `new Anthropic` originates there, mirroring brief-ai.ts) — no second prompt-scaffolding copy.
    - Candidate ordering uses lib/deals/catalog.ts (grep confirms isRightsReady import) and does NOT hard-drop near-rights-ready tracks (D-11).
    - A non-leadership AE calling ai-draft on an uncovered Selects gets 404.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "isRightsReady" lib/selects/ai-draft.ts</automated>
  </verify>
  <done>The AI-draft route populates a rights-ready-first ~10-track starter with per-track reasons + a cover note into a draft Selects, own-book-scoped, reusing the shipped Anthropic pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Saved / team-shared Crate searches route (D-12)</name>
  <read_first>
    - supabase/migrations/111_selects.sql — selects_saved_searches (created_by, name, filters jsonb, is_team_shared)
    - lib/admin/gate.ts — requireStaff
    - lib/buyer/brief.ts / lib/deals/catalog.ts — the Crate filter shape stored in filters jsonb
    - an existing simple /api/admin/* GET+POST route for the NextResponse + zod convention
  </read_first>
  <action>
    Create app/api/admin/selects/saved-searches/route.ts. GET: requireStaff(); return the caller's own saved searches PLUS all is_team_shared searches (any AE can recall a team search — no leadership gate, D-12). POST: zod-validate {name, filters}; create a private saved search owned by created_by = user.id. PATCH (same file, or a nested handler): flip is_team_shared on a search the caller OWNS (only the owner can share/unshare their own search). Validate filters as a bounded jsonb object (no arbitrary depth). All staff-gated.
  </action>
  <acceptance_criteria>
    - GET returns the caller's own searches + every is_team_shared search (verified by a test-shaped assertion or a route test); a private search of another AE is NOT returned.
    - POST creates a private (is_team_shared=false) search owned by the caller; PATCH can flip only the caller's own search to team-shared.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test -f app/api/admin/selects/saved-searches/route.ts</automated>
  </verify>
  <done>Saved searches persist per-AE, team-shared ones are recallable by all AEs, and only an owner can share/unshare their own — D-12 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AE request → Anthropic API | Brief prose + catalogue data leave to the model; server-side only (API key never client-exposed) |
| AE → team-shared search visibility | A team-shared search is the one cross-AE read this plan grants — bounded to read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-10 | Information Disclosure | ai-draft own-book scope | high | mitigate | requireStaff + isAssignedToOrg on the target Selects' org before any read/write; 404 on scope denial. |
| T-31-11 | Elevation of Privilege | saved-search share flip | low | mitigate | Only the owner (created_by) can flip is_team_shared; peers get read-only via the team-shared arm. |
| T-31-12 | Denial of Service | Anthropic single-shot draft | low | mitigate | Length-capped prompt + best-effort result ({ok:false} on failure), never a hang/500 that loses the draft. |
| T-31-SC | Tampering | package installs | high | accept | No new packages (existing @anthropic-ai/sdk 0.52.0, zod, Supabase). |
</threat_model>

## Artifacts this phase produces (31-05)
- **lib/selects/ai-draft.ts** — `draftSelectsFromBrief` Anthropic structured-draft helper (net-new)
- **API routes:** `app/api/admin/selects/[id]/ai-draft/route.ts`, `app/api/admin/selects/saved-searches/route.ts` (net-new)

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- ai-draft ordered rights-ready-first (grep isRightsReady) and own-book-scoped; saved-searches team-share visible to all AEs, owner-only flip.
</verification>

<success_criteria>
The AE can one-click an AI starter Selects off a brief and recall a saved (or team-shared) Crate search — both reusing shipped patterns, both fail-closed on scope.
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-05-SUMMARY.md` when done.
</output>
