---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 12
type: execute
wave: 4
depends_on: ["31-01", "31-02"]
files_modified:
  - lib/watermark/stream-preview.ts
  - lib/watermark/signed-url.ts
  - lib/watermark/signed-url.test.ts
autonomous: true
requirements: [R12, D-01, D-03]
user_setup: []
must_haves:
  truths:
    - "A per-track watermarked stream-preview is produced by an async/pre-computed step (rendered once per track, stored in a PRIVATE previews bucket, served by signed URL thereafter) — never rendered inline in a request handler (Vercel Hobby 10s cap)."
    - "The preview carries the D-01 audible tag (soft sub-audible tonal pulse per the owner-confirmed 31-01 decision) so a 30s+ evaluative listen stays meaningful."
    - "The signed-URL accessor returns a URL to the WATERMARKED-preview storage path ONLY — it is structurally incapable of returning a signed URL to the original master bucket path (the R12 'never a clean master' guarantee enforced at the code boundary, not by convention)."
    - "A preview that is not yet rendered resolves to a 'processing' status the player can show — the render never blocks the player's play/react/approve flow (A2: the forensic per-share DOWNLOAD render is the fast-follow, tracked separately)."
  artifacts:
    - "lib/watermark/stream-preview.ts — implements WatermarkProvider.renderStreamPreview (per-track, pre-computed, private bucket)"
    - "lib/watermark/signed-url.ts — getPreviewSignedUrl(trackId) → watermarked path only + status"
    - "lib/watermark/signed-url.test.ts — asserts the accessor never resolves a master-bucket path"
  key_links:
    - "The player (31-13) obtains audio ONLY through getPreviewSignedUrl — never through the master signed-URL helper used by the Sound Vault."
    - "This plan implements the 31-01 WatermarkProvider interface; the forensic renderForensicDownload (per-share, D-03) is stubbed with a 'processing/unavailable' status and sequenced as the A2 fast-follow."
  prohibitions:
    - statement: "MUST NOT serve clean master audio through the player, by stream OR download — watermarked previews only (R12)."
      status: resolved
      verification: "test — signed-url.test asserts getPreviewSignedUrl resolves a path in the PREVIEWS bucket and never the master bucket; the accessor takes a trackId, not a master path."
    - statement: "MUST NOT render a per-share forensic file synchronously inside a request handler (Vercel Hobby 10s — Pitfall 5)."
      status: resolved
      verification: "judgment — renders are pre-computed/async with a 'processing' interim status; no request handler awaits a full transcode."
---

<objective>
Implement the stream-preview half of the watermarking pipeline against the 31-01 WatermarkProvider interface: a per-track, pre-computed, private-bucket watermarked preview + a signed-URL accessor that is structurally incapable of handing back a clean master. This is the content-protection layer the player streams through.

Purpose: R12/D-01 — the player must stream ONLY watermarked previews. Per A2, this ships the stream-preview render now; the clean-sounding per-share forensic DOWNLOAD render (D-03) is stubbed as 'processing' and sequenced as the fast-follow so the player's play/react/approve flow is never blocked on it.
Output: lib/watermark/stream-preview.ts + lib/watermark/signed-url.ts (+ test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-RESEARCH.md
@lib/watermark/provider.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement renderStreamPreview (per-track, pre-computed, private bucket)</name>
  <read_first>
    - lib/watermark/provider.ts + lib/watermark/README.md (31-01) — the interface + the owner-confirmed approach/tag character
    - app/(artist)/vault/[projectId]/play/page.tsx — the real signed-URL streaming precedent (createSignedUrls, master storage paths) — the render READS the master, never re-serves it
    - lib/storage/index.ts — bucket/signed-URL helpers + how buckets are named/created in this repo
    - .planning/phases/31-.../31-RESEARCH.md — Common Pitfalls #5 (async/pre-computed render, Vercel Hobby 10s), Architectural Responsibility Map (watermarking = API/Backend dedicated render + a private previews bucket)
  </read_first>
  <action>
    Create lib/watermark/stream-preview.ts implementing WatermarkProvider.renderStreamPreview: given a track_id, resolve the master storage path (service role), apply the approved audible-tag preview render (the 31-01-approved tool/approach) to produce a watermarked preview, and write it to a PRIVATE previews bucket at a deterministic path keyed by track_id (ensure the private bucket exists; it must NOT be public). Expose renderPreviewIfAbsent(trackId) that renders only when the preview is missing (idempotent, cacheable per-track since the stream tag is not per-recipient) and can be invoked pre-computed (best-effort at Send time and/or lazily on first player open). Do NOT await a full transcode inside a request handler — structure the render so a not-yet-ready track returns a 'processing' status. Stub renderForensicDownload to return an 'unavailable/processing' status with a comment marking it the A2 fast-follow (per-share, D-03) — do not implement the per-share forensic encode here.
  </action>
  <acceptance_criteria>
    - renderStreamPreview writes to a private (non-public) previews bucket path keyed by track_id; the master bucket is read, never re-served.
    - renderPreviewIfAbsent is idempotent (a second call for a ready track does not re-render).
    - renderForensicDownload is present but returns a 'processing/unavailable' status (A2 fast-follow), not a clean master.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "renderStreamPreview" lib/watermark/stream-preview.ts</automated>
  </verify>
  <done>Per-track watermarked previews render into a private bucket via a pre-computed, idempotent step; the forensic download is stubbed as the A2 fast-follow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getPreviewSignedUrl — watermarked-path-only accessor (+ never-master test)</name>
  <read_first>
    - lib/watermark/stream-preview.ts (Task 1) — the previews-bucket path scheme
    - lib/storage/index.ts / app/(artist)/vault/[projectId]/play/page.tsx — createSignedUrl(s) usage + the master bucket name (so the test can assert it is NEVER used here)
    - .planning/phases/31-.../31-RESEARCH.md — Security Domain (clean master via player = Information Disclosure; signed URLs must point ONLY at watermarked-render paths)
  </read_first>
  <action>
    Create lib/watermark/signed-url.ts exporting getPreviewSignedUrl(trackId) that returns { status: 'ready', url } (a short-TTL signed URL to the watermarked preview path) or { status: 'processing' } (triggering renderPreviewIfAbsent), and NOTHING that resolves a master-bucket path. The function takes a track_id, resolves the previews-bucket path internally, and signs only that — it must be impossible to pass a master path through it. Create lib/watermark/signed-url.test.ts asserting: the resolved/signed path is in the previews bucket; the master bucket name never appears in the accessor's output; a missing preview yields 'processing' (not a master fallback). Since audio bytes cannot be asserted in CI, the test asserts the PATH/BUCKET guarantee and the status contract (the audible-tag correctness is a human UAT check).
  </action>
  <acceptance_criteria>
    - `npx jest lib/watermark/signed-url.test.ts` passes: the signed path is the previews bucket; the master bucket is never resolved; a missing preview → 'processing'.
    - getPreviewSignedUrl accepts a trackId (not a storage path) — no caller can inject a master path.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx jest lib/watermark/signed-url.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The player's only audio accessor resolves watermarked-preview paths exclusively — a clean master cannot be signed through it, proven by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| master bucket → previews bucket | The render reads the master and writes a new watermarked artifact; the master path never crosses to the player |
| previews bucket → public token viewer | The player (unauthenticated) obtains only watermarked-preview signed URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-27 | Information Disclosure (IP leak) | getPreviewSignedUrl | high | mitigate | Accessor takes a trackId and resolves ONLY the previews-bucket path; asserted by test that the master bucket is never signed — the "never a clean master (stream)" guarantee at the code boundary. |
| T-31-28 | Denial of Service | inline transcode | medium | mitigate | Pre-computed/idempotent render + 'processing' status; no request handler awaits a full transcode (Vercel Hobby 10s). |
| T-31-29 | Information Disclosure | previews bucket exposure | high | mitigate | Previews bucket is PRIVATE; access only via short-TTL signed URLs minted server-side. |
| T-31-SC | Tampering | watermarking package | high | accept | Any package was already legitimacy-gated + human-approved in 31-01; no new un-vetted install here. |
</threat_model>

## Artifacts this phase produces (31-12)
- **lib/watermark/stream-preview.ts** — `renderStreamPreview` / `renderPreviewIfAbsent` (net-new; implements the 31-01 provider)
- **lib/watermark/signed-url.ts** — `getPreviewSignedUrl` watermarked-only accessor (net-new)
- **lib/watermark/signed-url.test.ts** — never-master guarantee (net-new)
- **Storage:** a PRIVATE previews bucket (ensured by the render pipeline)

<verification>
- `npx jest lib/watermark/signed-url.test.ts` green; `npx tsc --noEmit` clean.
- Render is per-track, pre-computed, private-bucket; the accessor cannot sign a master path.
</verification>

<success_criteria>
The player can obtain watermarked previews via signed URLs with a structural guarantee that no clean master is ever served through the stream path; the forensic download is cleanly sequenced as the A2 fast-follow.
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-12-SUMMARY.md` when done.
</output>
