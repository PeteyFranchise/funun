---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 13
type: execute
wave: 4
depends_on: ["31-12", "31-04"]
files_modified:
  - app/selects/[token]/page.tsx
  - components/selects-player/SelectsPlayer.tsx
  - components/selects-player/theme.ts
  - app/api/selects/[token]/react/route.ts
  - app/api/selects/[token]/respond/route.ts
  - app/api/selects/[token]/download/route.ts
autonomous: true
requirements: [R12, R5, D-01, D-02, D-03, D-13]
user_setup: []
must_haves:
  truths:
    - "Opening a valid /selects/[token] link (no login) plays WATERMARKED previews only (via the 31-12 accessor), records per-track reactions (love/pass/more-like-this), and offers Approve / Request changes — the Apple-Music-native player built to the locked reference phase-31-shareable-music-player.html (D-13)."
    - "An invalid/expired token renders the safe 'This link isn't live.' state — a dark full-bleed page with NO org name, client name, AE name, track titles, or artwork (R12: leaks nothing)."
    - "A Selects is reachable ONLY via its unguessable share_token (service-role read by token); there is no id-enumerable path (R11/R12 prohibition)."
    - "Clean master audio is never served — stream OR download; the player streams only 31-12 watermarked-preview signed URLs, and download yields a watermarked file only (R12)."
    - "Play / keep / pass / Approve / Request changes are open to everyone with no login; only download (and licensing/checkout) requires a free Client Partner account (the gate modal). AE-configured disable / length-cap (D-02) is respected by the served file."
    - "A guest reaction is attributed to the link/session (viewer_key), never to a specific named contact it cannot verify (R13 prohibition applies to reactions)."
  artifacts:
    - "app/selects/[token]/page.tsx — the SSR player route (outside (admin)) with OpenGraph unfurl + safe invalid-token state"
    - "components/selects-player/SelectsPlayer.tsx — the Family B client player (playback, reactions, approve/request-changes, Suggested Songs, mini-player, Glow Up toggle)"
    - "components/selects-player/theme.ts — the standalone always-dark player theme (from fnbl dark hex)"
    - "app/api/selects/[token]/react/route.ts, /respond/route.ts, /download/route.ts"
  key_links:
    - "The page resolves the Selects by share_token via a service-role read (RLS bypassed deliberately — the token IS the authorization, BUILD-SPEC §4)."
    - "All audio comes from lib/watermark/signed-url.ts getPreviewSignedUrl — never the Sound Vault master accessor."
  prohibitions:
    - statement: "MUST NOT make a Selects reachable without its unguessable share token (no id enumeration)."
      status: resolved
      verification: "test — a request to the route/APIs without a valid share_token is rejected and returns no org/track data (invalid-token integration test)."
    - statement: "MUST NOT serve clean master audio through the player, by stream OR download."
      status: resolved
      verification: "test/judgment — the page/routes source audio only via getPreviewSignedUrl (31-12); the download route returns a watermarked file only; a data-flow review confirms no master path is signed."
    - statement: "MUST NOT leak any org/client/AE/track data on the invalid/expired-token state."
      status: resolved
      verification: "test — the invalid-token render contains none of the Selects' org/client/AE/track fields (assert absence in the response)."
    - statement: "MUST NOT attribute an anonymous/forwarded reaction to a specific named contact it cannot verify (R13)."
      status: resolved
      verification: "judgment — a guest reaction is keyed to viewer_key (link/session); a named contact is used only for an authenticated known org member."
---

<objective>
Build the one brand-critical, client-facing surface: the public `/selects/[token]` SSR player (Family B, D-13), built to the locked reference `phase-31-shareable-music-player.html`. A valid token (no login) plays watermarked previews, records reactions, and offers Approve / Request changes; an invalid token shows the safe leaks-nothing state; download is account-gated and never serves a clean master.

Purpose: R12 — the moment "a client actually receives and acts on a Selects." This is the outbound Selects motion's payoff.
Output: the SSR route + the player components + the react/respond/download token routes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-UI-SPEC.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-SPEC.md
@.planning/design/phase-31-shareable-music-player.html
@.planning/design/crate-lead-engine-BUILD-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSR token route — service-role read, OpenGraph unfurl, safe invalid-token state, react/respond APIs</name>
  <read_first>
    - .planning/design/crate-lead-engine-BUILD-SPEC.md §3–§4 — the /selects/{token} SSR + service-role read contract (token is the authorization; RLS bypassed deliberately) + react/respond routes
    - supabase/migrations/111_selects.sql — selects/selects_tracks/selects_reactions columns (share_token, status, download_enabled/max_seconds, reacted_by/viewer_key)
    - lib/selects/stage-machine.ts (31-03) — isLegalSelectsTransition for the respond route (sent → approved | changes_requested)
    - lib/supabase/server.ts — createServiceClient
    - .planning/phases/31-.../31-UI-SPEC.md — "Invalid / expired token state" (dark, no data), "OpenGraph / rich unfurl" (og:title/description/image), Family B copy contract
  </read_first>
  <action>
    Create app/selects/[token]/page.tsx as an SSR server component OUTSIDE the (admin) group (a public route, no login). Resolve the Selects by share_token via createServiceClient (deliberate RLS bypass — the token is the authorization); there is NO id-addressable variant. If the token does not resolve (or the Selects is expired/not sent), render the safe state: a dark full-bleed page with the UI-SPEC copy ("This link isn't live." / body), a single muted broken-link glyph, and NONE of the Selects' org/client/AE/track fields — assert leaks-nothing. On a valid token, generate OpenGraph metadata (generateMetadata: og:title "{name} · A Selects from Funūn", og:description = cover note truncated / fallback, og:image = a branded gradient card) so a forwarded link unfurls on-brand. Pass the resolved Selects + tracks (with per-track watermarked-preview status via 31-12) to SelectsPlayer. Create app/api/selects/[token]/react/route.ts (POST): validate the token, upsert a reaction (love/pass/more_like_this) attributed to reacted_by when the caller is an authenticated known org member, otherwise to viewer_key (link/session) — never a guessed named contact (R13). Create app/api/selects/[token]/respond/route.ts (POST): validate the token, move status via isLegalSelectsTransition (sent → approved | changes_requested), append the reason on changes; reject an illegal move. Both routes reject a missing/invalid token with a safe error that leaks no data.
  </action>
  <acceptance_criteria>
    - A valid token SSR-renders the player; an invalid/expired token renders the safe state containing none of the org/client/AE/track fields (assert absence).
    - There is no id-addressable Selects route — only the token path resolves (grep confirms the lookup is by share_token via service role).
    - react attributes to viewer_key for guests, reacted_by for authenticated org members; respond gates the status move via isLegalSelectsTransition.
    - OpenGraph metadata is emitted via generateMetadata.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "share_token" app/selects/\[token\]/page.tsx && grep -q "isLegalSelectsTransition" app/api/selects/\[token\]/respond/route.ts</automated>
  </verify>
  <done>The token route resolves only by share_token, unfurls on-brand, fails safe with no leak, and records reactions + approve/request-changes.</done>
</task>

<task type="auto">
  <name>Task 2: SelectsPlayer — watermarked playback, reactions, approve/request-changes, Suggested Songs, mini-player, Glow Up toggle</name>
  <read_first>
    - .planning/design/phase-31-shareable-music-player.html — THE locked build reference (Apple-Music-native dense list, docked mini-player pill, ••• sheet, Look 2 default + Glow Up toggle, three-circle app bar, full-width rows) — build to match, do not re-derive
    - .planning/phases/31-.../31-UI-SPEC.md Family B — the full surface spec (hero, CURATED TRACKS, Preview pill, reactions, Suggested Songs shown to all, mini-player, typography incl. the one 900 hero title, copy contract)
    - lib/watermark/signed-url.ts (31-12) — getPreviewSignedUrl (the ONLY audio source; a 'processing' status → show a processing affordance)
    - components/buyer/fnbl-theme.ts + components/buyer/CatalogBrowserLight.tsx — the fnbl dark hex to derive the standalone theme + the (fixture-only) player base to inform layout (NOT a real-audio source — Pitfall 2)
    - lib/deals/catalog.ts — a simple rights-ready sample for Suggested Songs (a lightweight seed, not the AI Vibe Match — out of scope)
  </read_first>
  <action>
    Create components/selects-player/theme.ts (a standalone, always-dark player theme built from the fnbl dark hex — the player is neither in .fncon nor the logged-in buyer shell). Create components/selects-player/SelectsPlayer.tsx ('use client') built to the locked reference: app bar (three-circle: account/cart/share, brand FUNŪN), the Glow Up View toggle (default Look 2 flat-dark, keep backdrop-filter OFF in Look 2), hero (currently-playing cover that swaps with playback, Preview pill), the Approve these (gradient primary) / Request changes (ghost) deal actions kept above the list, the CURATED TRACKS dense tap-to-play full-width list (each row: artwork · title/artist · marquee "why it fits" · ••• / love / pass / download cluster; ••• opens the Apple-Music sheet with the AE per-track note + License), previewable Suggested Songs (shown to everyone; "+") sourced from a simple rights-ready catalogue sample, per-track attribution (who added — AE vs client, read from added_by/source), and the docked mini-player pill. Playback streams ONLY via getPreviewSignedUrl (31-12); a 'processing' preview shows an interim state, never a master. Reactions POST to the react route; Approve/Request changes POST to the respond route. Do NOT build the per-track qualified-listen telemetry (R13 → 31.1) — but leave the play events cleanly separable. Do NOT build client-side co-edit add/remove beyond the Suggested "+" add for an authenticated client (guests cannot add) — full co-edit + Removed-tray recovery is a marked 31.1 extension (schema is ready).
  </action>
  <acceptance_criteria>
    - The player matches the locked reference (Look 2 default, Glow Up toggle, three-circle app bar, dense full-width tap-to-play list, docked mini-player, ••• sheet).
    - Audio plays only via getPreviewSignedUrl; a not-ready preview shows a processing state (never a master).
    - Reactions and Approve/Request changes work via the token routes; Suggested Songs are previewable and shown to all.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "getPreviewSignedUrl" components/selects-player/SelectsPlayer.tsx && npm run build</automated>
  </verify>
  <done>The Family B player is built to the locked reference, streams watermarked-only, and records reactions + deal actions — open to everyone with no login.</done>
</task>

<task type="auto">
  <name>Task 3: Download gate — account-gated, watermarked-only, D-02 disable/cap respected</name>
  <read_first>
    - .planning/phases/31-.../31-UI-SPEC.md — "Download gate (R12, D-01/D-02/D-03)": cloud-download icon always (never a lock); guest → gate modal (Create Free Client Partner Account / Log in / Maybe later); signed-in Client Partner → watermarked file; disable/length-cap respected; fine-print copy
    - supabase/migrations/111_selects.sql — selects.download_enabled / download_max_seconds (D-02)
    - lib/watermark/signed-url.ts + lib/watermark/stream-preview.ts (31-12) — the watermarked artifact; renderForensicDownload is the A2 fast-follow (returns 'processing/unavailable')
    - .planning/phases/31-.../31-RESEARCH.md — Security Domain (clean master via download = Information Disclosure) + A2 (forensic download fast-follow)
  </read_first>
  <action>
    Create app/api/selects/[token]/download/route.ts (GET/POST): validate the token. If the requester is not an authenticated Client Partner, return a gate signal (the player shows the gate modal — Create Free Client Partner Account / Log in / Maybe later); do NOT serve a file to a guest. If download_enabled is false (D-02), the affordance stays visible-but-disabled and the route refuses. For an authenticated Client Partner, serve a WATERMARKED file ONLY: prefer the clean-sounding forensic per-share render when ready (31-12 renderForensicDownload) but, per A2 (forensic is the fast-follow), if it is not yet available, either serve the audible-tagged watermarked stream-preview render as the interim watermarked file OR return a 'download preparing' status — NEVER a clean master under any path. Respect download_max_seconds (length cap) when serving. Wire the player's download affordance (row / hero / ••• cloud-download) to this route with the gate modal for guests. Add a marked comment that the clean-sounding per-share forensic file (D-03) is the A2 fast-follow.
  </action>
  <acceptance_criteria>
    - A guest download attempt yields the gate modal, never a file; download_enabled=false disables the affordance and the route refuses (D-02).
    - An authenticated Client Partner receives a watermarked file only — the route never resolves or serves a master path (data-flow review + it sources only 31-12 artifacts).
    - The length-cap (download_max_seconds) is applied when set.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test -f app/api/selects/\[token\]/download/route.ts && npm run build</automated>
  </verify>
  <done>Download is account-gated, D-02-configurable, and watermarked-only under every path; the clean-sounding forensic file is the flagged A2 fast-follow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /selects/[token] | Unauthenticated; the unguessable token is the sole authorization (service-role read) |
| player → audio storage | Audio only via 31-12 watermarked-preview signed URLs; never the master accessor |
| guest → download | Download requires a free Client Partner account; guests are gated, never served a file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-30 | Information Disclosure | token resolution | high | mitigate | Resolve ONLY by share_token via service role; no id-addressable route; invalid token → safe state leaking no org/client/AE/track data (asserted). |
| T-31-31 | Information Disclosure (IP leak) | stream + download audio | high | mitigate | All audio via getPreviewSignedUrl (watermarked previews); download serves a watermarked file only, never a master, under every branch (incl. the A2 interim). |
| T-31-32 | Spoofing | reaction attribution | medium | mitigate | Guest reactions keyed to viewer_key (link/session); named-contact attribution only for an authenticated known org member (R13). |
| T-31-33 | Elevation of Privilege | respond status move | medium | mitigate | isLegalSelectsTransition gates sent → approved/changes_requested; illegal moves rejected. |
| T-31-34 | Information Disclosure | OpenGraph unfurl | low | accept | og fields use only the Selects name + cover note + a branded gradient (owner-authored, share-intended); no private routing internals. |
| T-31-SC | Tampering | package installs | high | accept | No new packages (watermarking dependency already gated in 31-01). |
</threat_model>

## Artifacts this phase produces (31-13)
- **app/selects/[token]/page.tsx** — public SSR player route + OpenGraph + safe invalid-token state (net-new)
- **components/selects-player/SelectsPlayer.tsx** + **theme.ts** — Family B client player (net-new)
- **API routes:** `app/api/selects/[token]/react/route.ts`, `.../respond/route.ts`, `.../download/route.ts` (net-new, token-gated public)

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Valid token plays watermarked previews + records reactions/approve/request-changes; invalid token leaks nothing; download is account-gated + watermarked-only; no id-enumerable path.
- Human UAT: open a sent Selects link (fresh + expired), verify playback, the Preview tag audibility, reactions, approve/request-changes, the guest download gate, and the leaks-nothing dead-link page.
</verification>

<success_criteria>
A client opens a forwarded link and plays, reacts to, and approves/requests-changes on a Selects — watermarked previews only, no login for the core flow, download account-gated and never a clean master, an expired link leaking nothing. The outbound Selects motion is complete.
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-13-SUMMARY.md` when done.
</output>
