---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 06
type: execute
wave: 2
depends_on: ["31-02"]
files_modified:
  - lib/client-partners/contacts.ts
  - app/api/admin/client-partners/[orgId]/contacts/route.ts
  - app/api/admin/client-partners/[orgId]/relationship-log/route.ts
autonomous: true
requirements: [R1, R5, D-05, D-08, D-09]
user_setup: []
must_haves:
  truths:
    - "An AE (own book) or leadership (any org) can list/create/edit/delete the contacts under a buyer_org — multiple contacts, with at most one flagged primary (D-08); setting a new primary atomically clears the previous one."
    - "A contact record carries the full D-09 shape (title, email, phone, linkedin_url, timezone, tags, address, notes, custom_fields) and is export-friendly."
    - "The relationship log can be read and appended (note/conversation entries) per org, scoped own-book; entries are immutable append-only (no edit/delete of a logged record)."
    - "Every route is requireStaff-gated and own-book-scoped (isAssignedToOrg; leadership bypass); a not-covered org returns 404, never 403."
  artifacts:
    - "lib/client-partners/contacts.ts — contact + relationship-log service-role helpers"
    - "app/api/admin/client-partners/[orgId]/contacts/route.ts"
    - "app/api/admin/client-partners/[orgId]/relationship-log/route.ts"
  key_links:
    - "The relationship-log append path is the same table (client_relationship_log) that 31.1's assignment write (R7) will append to — Slice 1 owns the note/conversation kinds."
  prohibitions:
    - statement: "MUST NOT show an AE any contact/relationship-log data outside their own assigned book (R5)."
      status: resolved
      verification: "test — a contacts/log route test asserts a non-leadership AE hitting an uncovered org's contacts gets 404."
    - statement: "MUST NOT expose staff-private routing internals (ae_user_id) or the raw relationship log to a buyer/authenticated non-staff caller."
      status: resolved
      verification: "judgment — routes are under /api/admin/* behind requireStaff; the tables are staff-only (REVOKE, mig 112)."
---

<objective>
Build the CRM-lite people layer's API (D-05/D-08/D-09): contacts CRUD under a buyer_org (multiple, one primary, rich record) and the org's relationship log (append-only notes/conversations). Own-book-scoped, staff-only, export-friendly.

Purpose: the workspace's Contacts + Notes jobs (R1) read/write through these; the relationship log is the auditable spine Slice 2 (assignment) later appends to.
Output: lib/client-partners/contacts.ts + the contacts route + the relationship-log route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-SPEC.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contacts CRUD + one-primary invariant + own-book scope test</name>
  <read_first>
    - supabase/migrations/112_client_partners_crm.sql — buyer_org_contacts columns + the WHERE is_primary partial unique index
    - lib/admin/gate.ts (requireStaff) + lib/staff/scope.ts (isAssignedToOrg)
    - app/(admin)/admin/client-partners/[orgId]/page.tsx — the existing [orgId] route param + notFound-not-403 scope pattern
    - app/api/profile/route.ts — the EDITABLE_FIELDS allowlist + sanitize pattern for a rich record with custom fields
    - .planning/phases/31-.../31-CONTEXT.md — D-08 (multiple, one primary) + D-09 (rich record, export-friendly)
  </read_first>
  <action>
    Create lib/client-partners/contacts.ts with service-role helpers: listContacts(orgId), createContact(orgId, payload), patchContact(contactId, payload via a CONTACT_EDITABLE_FIELDS allowlist), deleteContact(contactId), and setPrimaryContact(orgId, contactId) which, in one transaction/RPC-ish sequence, clears is_primary on the org's other contacts then sets it on the target (upholding the one-primary invariant even though the partial unique index also guards it). Create app/api/admin/client-partners/[orgId]/contacts/route.ts (GET list, POST create, PATCH edit incl. set-primary, DELETE) — requireStaff(); resolve the org; non-leadership requires isAssignedToOrg(org, user.id) else 404. Validate the full D-09 shape with zod (tags string[], address/custom_fields bounded jsonb). Add lib/client-partners/contacts.test.ts asserting: setPrimaryContact leaves exactly one primary; the allowlist rejects an unlisted field; a non-leadership AE on an uncovered org is rejected (404-shaped).
  </action>
  <acceptance_criteria>
    - `npx jest lib/client-partners/contacts.test.ts` passes, including the exactly-one-primary assertion and the uncovered-org rejection.
    - The route validates the D-09 fields via zod and rejects unlisted fields (allowlist).
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx jest lib/client-partners/contacts.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Contacts CRUD is live with the one-primary invariant, the rich D-09 record, an allowlist, and own-book scoping proven by test.</done>
</task>

<task type="auto">
  <name>Task 2: Relationship-log append + read route</name>
  <read_first>
    - supabase/migrations/112_client_partners_crm.sql — client_relationship_log columns (kind, body, meta, author_user_id, contact_id)
    - lib/admin/gate.ts (requireStaff) + lib/staff/scope.ts (isAssignedToOrg)
    - lib/staff/audit.ts — logStaffAction (the staff audit convention; a note append is distinct from the audit log but follows the append-only posture)
  </read_first>
  <action>
    Create app/api/admin/client-partners/[orgId]/relationship-log/route.ts. GET: requireStaff() + own-book scope; return the org's relationship-log entries newest-first (optionally filtered by contact_id). POST: append an entry with kind in note/conversation (Slice 1 kinds only — status_change/assignment are Slice 2), body (zod-validated, length-capped), author_user_id = user.id, optional contact_id. Entries are append-only — do NOT expose an edit or delete handler (a logged record is immutable). Add the append helper to lib/client-partners/contacts.ts (or a sibling lib/client-partners/relationship-log.ts) as a pure-ish service-role function appendRelationshipLog(orgId, {kind, body, contactId, authorUserId}).
  </action>
  <acceptance_criteria>
    - GET returns own-org entries newest-first; a non-leadership AE on an uncovered org gets 404.
    - POST appends a note/conversation entry stamped with the caller as author; no edit/delete handler exists (append-only).
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test -f app/api/admin/client-partners/\[orgId\]/relationship-log/route.ts</automated>
  </verify>
  <done>The relationship log reads and appends notes/conversations, own-book-scoped and append-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AE (staff) → another AE's org contacts/log | Cross-AE reads must be blocked server-side |
| authenticated buyer → contacts/log tables | Staff-only tables; no buyer read path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-13 | Information Disclosure | contacts/log own-book scope | high | mitigate | requireStaff + isAssignedToOrg on every route; 404 on scope denial; tables staff-only (REVOKE). |
| T-31-14 | Repudiation | relationship-log integrity | medium | mitigate | Append-only (no edit/delete handler); every entry stamps author_user_id = the authenticated caller. |
| T-31-15 | Elevation of Privilege | contact mass-assignment | medium | mitigate | CONTACT_EDITABLE_FIELDS allowlist + zod on the rich D-09 payload. |
| T-31-SC | Tampering | package installs | high | accept | No new packages. |
</threat_model>

## Artifacts this phase produces (31-06)
- **lib/client-partners/contacts.ts** — contact + relationship-log service-role helpers (net-new)
- **API routes:** `app/api/admin/client-partners/[orgId]/contacts/route.ts`, `.../relationship-log/route.ts` (net-new)
- **Const:** `CONTACT_EDITABLE_FIELDS` allowlist

<verification>
- `npx jest lib/client-partners/contacts.test.ts` green; `npx tsc --noEmit` clean; `npm run build` succeeds.
- One-primary invariant + own-book 404 + append-only log verified.
</verification>

<success_criteria>
The CRM-lite people layer's API is live: multi-contact-one-primary rich records and an append-only relationship log, staff-only and own-book-scoped.
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-06-SUMMARY.md` when done.
</output>
