---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 08
type: execute
wave: 3
depends_on: ["31-04", "31-06", "31-07"]
files_modified:
  - components/admin/ClientPartnersList.tsx
  - lib/client-partners/columns.ts
  - lib/client-partners/columns.test.ts
  - app/(admin)/admin/my-client-partners/page.tsx
  - app/(admin)/layout.tsx
autonomous: true
requirements: [R1, R2, R5, A1]
user_setup: []
must_haves:
  truths:
    - "My Client Partners lands on a Clients / Companies pill-tab list; a row click drills into the matching person/company workspace; the list opens sorted by Next action, overdue-first (deterministic default) (R1)."
    - "Insight columns can be hidden/shown, drag-reordered (@dnd-kit), and click-sorted (one active key); the arrangement persists per-AE via the console cookie mechanism and restores on reload; the identity column is pinned (unhideable, always first) (R2)."
    - "Equal sort keys resolve to a documented stable tiebreak (identity) — the same rows never reorder run-to-run (R2 backstop: held-out sort-stability test)."
    - "An AE sees only their own assigned book; the 'Client Partners' nav link and the leadership tower are hidden for the AE role; an unassigned account never appears in an AE's list; an AE with zero clients sees the defined empty state (R5)."
    - "A1: the 'Days in stage' column uses the seeded-default pipeline stages (constants); the leadership-editable stage config is D-10 → deferred to Phase 31.1 — flagged in-code."
  artifacts:
    - "components/admin/ClientPartnersList.tsx — the shared list+tabs+insight-columns component (R1/R2, reused by the 31.1 leadership tower)"
    - "lib/client-partners/columns.ts — column defs (Companies 12 / Clients 10), the pinned-identity rule, and the pure sort-with-stable-tiebreak"
    - "lib/client-partners/columns.test.ts — column-visibility/order persistence shape + sort-stability suite"
    - "app/(admin)/admin/my-client-partners/page.tsx — rebuilt own-book list page (replaces the MyCompanies card list)"
    - "app/(admin)/layout.tsx — R5 nav gating + Slice-1 room nav entries"
  key_links:
    - "ClientPartnersList is the single component the 31.1 leadership tower reuses unfiltered — build it list-source-agnostic (rows + column config in, drill-in href out)."
    - "Per-AE column state uses the same cookie mechanism as console-theme (constraint from CONTEXT); the identity column is structurally exempt from hide/reorder."
  prohibitions:
    - statement: "MUST NOT show an AE any client/company data outside their own assigned book, and MUST NOT surface an unassigned account in an AE's list (R5)."
      status: resolved
      verification: "test/judgment — the page query is own-book scoped server-side (role !== 'leadership' → .eq('ae_user_id', user.id)); the 'Client Partners' link renders only for leadership."
    - statement: "MUST NOT allow the identity column to be hidden or the table to reach zero columns (R2)."
      status: resolved
      verification: "test — columns.test asserts the identity column has no hide/reorder affordance and is always first."
---

<objective>
Rebuild My Client Partners as the list+tabs+insight-columns surface (R1/R2) and gate the console nav by role (R5). The list component is built once, list-source-agnostic, so the 31.1 leadership tower reuses it unfiltered. Insight columns are show/hide + drag-reorder + click-sort, persisted per-AE, identity pinned; sort resolves equal keys to a stable identity tiebreak (R2 backstop).

Purpose: this is the AE's front door — the scannable book that drills into the workspace. It replaces the thin MyCompanies card list.
Output: ClientPartnersList + column model (+ test) + rebuilt page + nav gating.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-UI-SPEC.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-SPEC.md
@.planning/design/phase-31-my-client-partners-mockup.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Column model — defs, pinned identity, pure stable sort (+ test)</name>
  <read_first>
    - .planning/phases/31-.../31-UI-SPEC.md — "Insight columns — show/hide, reorder, sort (R2)" (Companies 12 cols, Clients 10 cols, pinned identity, one active sort key) + the Color/Typography contract
    - .planning/phases/31-.../31-SPEC.md — R2 + its Edge Coverage backstop row (equal keys → stable identity tiebreak) + R1 default sort (Next action, overdue-first)
    - components/admin/ChecklistAdmin.tsx — the existing @dnd-kit grip/drag-handle idiom to reuse
    - components/admin/console-theme.ts — the per-member cookie persistence mechanism to reuse
  </read_first>
  <action>
    Create lib/client-partners/columns.ts: export COMPANIES_COLUMNS (12: Company · Next action · Status · Health · Days in stage · Open briefs · Active Selects · Open deal · Lifetime value · Last brief · Last touch · Contacts) and CLIENTS_COLUMNS (10: Client · Next action · Company · Role · Status · Last touch · Last brief · Briefs · Selects seen · Deals) as ordered column-def objects (key, label, sortable, isIdentity). Mark the identity column (Company/Client) isIdentity=true (unhideable, un-reorderable, pinned first). Export a pure sortRows(rows, sortKey, dir) that sorts by the active key then falls back to a STABLE identity tiebreak so equal keys never reorder. Export DEFAULT_SORT = Next action / overdue-first (R1). Add a comment on the Days-in-stage column recording A1 (seeded-default stages here; leadership-editable config is D-10 → 31.1). NOTE the Health column here is a placeholder render slot in Slice 1 (computed health is R3 → 31.1); render it as the neutral 'unknown' dashed chip per the UI-SPEC Color section until 31.1 wires computation — do NOT fake a green. Create lib/client-partners/columns.test.ts: assert the identity column is pinned/unhideable; assert sortRows is stable under an equal-key shuffle (R2 backstop); assert DEFAULT_SORT is Next-action overdue-first.
  </action>
  <acceptance_criteria>
    - `npx jest lib/client-partners/columns.test.ts` passes, including the shuffle-invariant stable-sort case and the pinned-identity assertion.
    - COMPANIES_COLUMNS has 12 entries, CLIENTS_COLUMNS has 10, identity first and isIdentity.
    - The Days-in-stage column carries the A1 comment; the Health column renders 'unknown' (not green) in Slice 1.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx jest lib/client-partners/columns.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The column model is defined (12/10, pinned identity), the sort is pure + stable, and the A1/health-placeholder carries are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: ClientPartnersList component — tabs, drag-reorder columns, click-sort, per-AE persistence, empty states</name>
  <read_first>
    - .planning/design/phase-31-my-client-partners-mockup.html — the visual source of truth (list, tabs, insight rows, Columns popover) — build to match (do not re-derive)
    - .planning/phases/31-.../31-UI-SPEC.md — the pill-tab idiom (.tabs/.tab), the Columns control popover, the empty-state copy table, the 12px insight-row density, medium-weight typography (advisory)
    - lib/client-partners/columns.ts (Task 1) — column defs + sortRows
    - components/admin/ChecklistAdmin.tsx / CuratorAdmin.tsx — @dnd-kit sortable usage to mirror
    - components/admin/console-theme.ts — cookie persistence helper
  </read_first>
  <action>
    Create components/admin/ClientPartnersList.tsx ('use client'): props are the row data + a mode (clients|companies) + a drill-in href builder — list-source-agnostic so the 31.1 tower reuses it unfiltered. Render the Clients/Companies pill-tab switcher, the insight table using the Task-1 column model, a "Columns" popover (checkbox show/hide + @dnd-kit drag-handle reorder, identity column exempt), click-to-sort headers (one active key, chevron), and the defined empty states from the UI-SPEC copy table (zero clients / zero companies). Persist column visibility/order + active sort key per-AE via the console cookie mechanism; restore on mount. A row click navigates (full-panel) to the drill-in href. Match the mockup look (medium weight, off-grid spacing sanctioned per UI-SPEC).
  </action>
  <acceptance_criteria>
    - Columns can be hidden/shown and drag-reordered; the identity column has no hide/drag affordance and stays first.
    - Column state + sort key persist across reload (per-AE cookie); the zero-state renders the UI-SPEC copy, not a blank table.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "dnd-kit" components/admin/ClientPartnersList.tsx</automated>
  </verify>
  <done>ClientPartnersList renders tabs + persisted, reorderable, sortable insight columns with pinned identity and defined empty states, matching the mockup.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild the page (own-book) + R5 nav gating</name>
  <read_first>
    - app/(admin)/admin/my-client-partners/page.tsx — the current own-book read pattern (role !== 'leadership' → .eq('ae_user_id', user.id)) to keep, swapping MyCompanies for ClientPartnersList
    - app/(admin)/layout.tsx — the inline sidebar nav (isLeadership already computed; the "Client Partners" → /admin/buyer-orgs link currently shown to ALL staff)
    - app/api/admin/crate-requests/route.ts, app/api/admin/selects/route.ts (31-07/31-04) — the rooms the new nav entries point at
    - .planning/phases/31-.../31-SPEC.md — R5 (AE sees own book only; Client Partners + leadership tower hidden for AE; unassigned never in AE list; zero-client empty state)
  </read_first>
  <action>
    Rebuild app/(admin)/admin/my-client-partners/page.tsx to fetch own-book-scoped org + person rows (reuse the existing role !== 'leadership' → .eq('ae_user_id', user.id) filter; leadership's own view is the hold-queue variant — same component, filtered — but only the AE own-book path ships here; the leadership hold-queue is 31.1) and render ClientPartnersList with the enriched columns (Open briefs / Active Selects / Contacts counts from 31-04/31-06/31-07 reads). Wire the AE zero-client empty state. In app/(admin)/layout.tsx, gate the "Client Partners" (/admin/buyer-orgs) link behind isLeadership (R5 — hidden for AE), and add the Slice-1 room nav entries (Crate Requests → /admin/crate-requests, Selects → /admin/selects) in the funnel order (Crate Requests → Selects), every-staff-role visible. Do NOT add a leadership tower link for AE. Keep the nav edits confined to this file (sibling UI plans do not touch layout.tsx).
  </action>
  <acceptance_criteria>
    - For a non-leadership AE the page query is `.eq('ae_user_id', user.id)` (own-book) and the "Client Partners" nav link does NOT render; for leadership both render.
    - The AE zero-client state shows the UI-SPEC "No Client Partners yet" copy.
    - Crate Requests + Selects nav entries exist in funnel order.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>My Client Partners is the own-book list+tabs surface, nav is role-gated (R5), and the Slice-1 rooms are reachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AE session → own-book list | The list must render only assigned rows; nav must hide leadership-only rooms |
| client cookie → column state | Per-AE persisted UI state is non-sensitive but must not carry another AE's data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-19 | Information Disclosure | own-book list scope + R5 nav | high | mitigate | Server-side own-book query (.eq ae_user_id); "Client Partners"/tower links render only for isLeadership; unassigned rows excluded from the AE query. |
| T-31-20 | Tampering | client-set column/sort state | low | accept | Column visibility/order is presentation-only, per-AE cookie; no data authority derives from it. |
| T-31-SC | Tampering | package installs | high | accept | No new packages (@dnd-kit already installed). |
</threat_model>

## Artifacts this phase produces (31-08)
- **components/admin/ClientPartnersList.tsx** — shared list+tabs+insight-columns component (net-new; reused by 31.1 tower)
- **lib/client-partners/columns.ts** + **columns.test.ts** — column defs + pure stable sort (net-new)
- **Rebuilt:** `app/(admin)/admin/my-client-partners/page.tsx` (replaces MyCompanies card list)
- **Edited:** `app/(admin)/layout.tsx` — R5 nav gating + Crate Requests / Selects nav entries

<verification>
- `npx jest lib/client-partners/columns.test.ts` green; `npx tsc --noEmit` clean; `npm run build` succeeds.
- Own-book scoping + R5 nav gating + pinned-identity + stable-sort + empty state verified.
</verification>

<success_criteria>
The AE opens a scannable, per-AE-tuned Clients/Companies list that drills into the workspace, sees only their book, and the console nav is role-correct.
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-08-SUMMARY.md` when done.
</output>
