---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 10
type: execute
wave: 3
depends_on: ["31-04", "31-05"]
files_modified:
  - components/admin/SelectsBuilder.tsx
  - app/(admin)/admin/selects/page.tsx
  - app/(admin)/admin/selects/[id]/page.tsx
autonomous: true
requirements: [R11, R5, D-11, D-12]
user_setup: []
must_haves:
  truths:
    - "An AE builds a Selects by pulling Crate tracks (add/remove, per-track note, cover note), targeting a client + optional brief, via three methods: from scratch, off a brief, or AI-drafted (D-11); rights-readiness badges show per track."
    - "The builder auto-saves the draft continuously as the AE edits (debounced PATCH via 31-04), with a manual Save also available (R11)."
    - "The Crate search supports saving a search and re-applying it in one click; a saved search can be flipped team-shared and recalled by any AE (D-12, via 31-05)."
    - "Send is disabled until ≥1 track (empty Selects cannot be sent — inline 'Add at least one track before sending'); Send mints the shareable link (R11)."
    - "Adding a track already present is idempotent (no duplicate row); a not-rights-ready track is badged/flagged before send, not silently dropped."
  artifacts:
    - "components/admin/SelectsBuilder.tsx — the curate-and-send console"
    - "app/(admin)/admin/selects/page.tsx — the AE's Selects list (status-filtered)"
    - "app/(admin)/admin/selects/[id]/page.tsx — the builder detail page"
  key_links:
    - "The builder writes exclusively through the 31-04/31-05 routes (own-book enforced server-side) — no direct client DB writes."
    - "Send returns the /selects/{token} path that the public player (31-13) serves."
  prohibitions:
    - statement: "MUST NOT let the builder operate on a Selects outside the AE's own book (R5)."
      status: resolved
      verification: "judgment — all writes go through the own-book-scoped 31-04/31-05 routes; the list/detail pages are staff-gated + scoped."
    - statement: "MUST NOT allow sending an empty Selects (R11)."
      status: resolved
      verification: "judgment/test — Send is disabled with the inline copy until ≥1 non-removed track; the server also rejects (31-04)."
---

<objective>
Build the Selects builder UI (R11): the AE's curate-and-send console. Pull Crate tracks with per-track + cover notes, three build methods (scratch / off a brief / AI-drafted, D-11), rights-ready badges, auto-save + manual save, saved/team-shared Crate searches (D-12), and Send (disabled until ≥1 track) that mints the shareable link.

Purpose: the human-facing half of R11 — the surface where the AE actually assembles and sends the Selects that the client receives.
Output: SelectsBuilder + the Selects list page + the builder detail page.

Scope note (carry): Slice 1 ships the Selects LIST (status-filtered) + the builder. The richer cross-client pipeline ROOM (List/Board funnel toggle, event-derived stages, next-best-action coaching questions, "+ Game Plan") described in the UI-SPEC leans on Slice-2 concepts (health, Game Plan, event-derived stages) and is deferred to Phase 31.1 — flag it in-code, do not build it here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-UI-SPEC.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-SPEC.md
@.planning/design/phase-31-my-client-partners-mockup.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: SelectsBuilder component — curate, notes, badges, auto-save, saved searches, AI-draft</name>
  <read_first>
    - app/api/admin/selects/[id]/route.ts, [id]/tracks/route.ts (31-04) — PATCH (name/cover/download settings), track add(idempotent)/remove(soft)/reorder/note, plus the rights_ready flag on each track payload
    - app/api/admin/selects/[id]/ai-draft/route.ts + saved-searches/route.ts (31-05) — AI-draft + saved/team-shared searches
    - lib/deals/catalog.ts (buildCatalogFilter) — the Crate search filter shape stored in saved searches
    - components/admin/ChecklistAdmin.tsx — @dnd-kit sortable (track reorder), grip idiom
    - .planning/design/phase-31-my-client-partners-mockup.html + .planning/phases/31-.../31-UI-SPEC.md — the Selects builder visual, the Send-disabled-until-≥1-track inline copy, the "Build Selects" gradient CTA (one accent per screen), the remove-with-undo toast
  </read_first>
  <action>
    Create components/admin/SelectsBuilder.tsx ('use client'): a Crate-search pane (with a saved-search recall dropdown incl. team-shared, and a "save this search" affordance → 31-05) on one side and the working tracklist on the other. Add/remove tracks (add is idempotent; remove is a soft-remove with an inline Undo toast per the UI-SPEC), per-track note + a cover-note field, and a rights-ready badge per track from the payload's rights_ready flag (flag, never drop, a not-rights-ready track). Provide the three build methods: from scratch, off a brief (prefill from a linked brief), and an AI-draft button (calls 31-05 ai-draft, then lets the AE curate). Auto-save the draft on edit via a debounced PATCH (31-04), with a manual Save button also present and a visible saved/saving state. The Send button is disabled (opacity-40, cursor-not-allowed, never hidden) with the inline "Add at least one track before sending" until ≥1 non-removed track; Send POSTs to 31-04's send route and surfaces the returned /selects/{token} link. Match the mockup; reserve the one gradient-accent slot for Send.
  </action>
  <acceptance_criteria>
    - Add is idempotent (re-adding shows no duplicate); remove is soft with an Undo toast; per-track + cover notes persist via 31-04.
    - Auto-save fires (debounced) on edit and a manual Save exists; a saved-search can be recalled and a search saved/team-shared via 31-05.
    - The AI-draft button populates a starter tracklist the AE can then edit.
    - Send is disabled with the inline copy until ≥1 track, then mints + shows the share link.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "dnd-kit" components/admin/SelectsBuilder.tsx</automated>
  </verify>
  <done>The builder curates Crate tracks with notes + rights-ready badges, auto-saves, recalls saved searches, AI-drafts, and Sends only a non-empty Selects.</done>
</task>

<task type="auto">
  <name>Task 2: Selects list page + builder detail page</name>
  <read_first>
    - app/api/admin/selects/route.ts (31-04 GET) — the own-book Selects list
    - components/admin/SelectsBuilder.tsx (Task 1)
    - app/(admin)/admin/my-client-partners/page.tsx — the px-9 py-[30px] page-wrapper + staff-check convention
    - .planning/phases/31-.../31-UI-SPEC.md — the Selects room framing (status funnel strip as a simple status filter in Slice 1) + the deferred List/Board note
  </read_first>
  <action>
    Create app/(admin)/admin/selects/page.tsx: staff-gated, own-book-scoped list of the AE's Selects (leadership sees all) rendered as a simple status-filterable list (a lightweight funnel strip that click-filters by status draft/sent/approved/changes_requested — NOT the full Board; leave a marked slot + comment noting the Board/coaching pipeline room is 31.1). Each row links to the builder detail. Create app/(admin)/admin/selects/[id]/page.tsx: staff-gated + own-book (notFound for an uncovered Selects' org), fetch the Selects + its tracks, render SelectsBuilder. New Selects creation entry (from the list and from the workspace Curation tab / Crate Requests "Build Selects") targets a client + optional brief.
  </action>
  <acceptance_criteria>
    - The list is own-book-scoped and status-filterable; a row opens the builder.
    - The detail page notFound()s an uncovered Selects for a non-leadership AE.
    - A marked 31.1 slot/comment records the deferred List/Board pipeline room.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>The Selects list + builder detail pages are live, own-book-scoped, with the Board/coaching room explicitly deferred to 31.1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AE builder (client) → admin selects routes | All writes cross into own-book-scoped server routes; no direct DB write |
| AE → another AE's Selects | The list/detail must be own-book-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-23 | Information Disclosure | Selects list/detail scope | high | mitigate | List own-book-scoped; detail notFound()s an uncovered Selects' org; all writes via the 31-04/31-05 own-book routes. |
| T-31-24 | Tampering | send from the UI | medium | mitigate | Send disabled until ≥1 track client-side AND server-rejected empty (31-04) — defense in depth. |
| T-31-SC | Tampering | package installs | high | accept | No new packages (@dnd-kit installed). |
</threat_model>

## Artifacts this phase produces (31-10)
- **components/admin/SelectsBuilder.tsx** — curate-and-send console (net-new)
- **Pages:** `app/(admin)/admin/selects/page.tsx`, `app/(admin)/admin/selects/[id]/page.tsx` (net-new)

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Idempotent add, soft remove+undo, auto-save+manual, saved-search recall, AI-draft, empty-send guard, Send mints link — all verified through the UI against the 31-04/31-05 routes.
</verification>

<success_criteria>
The AE can assemble a Selects from The Crate with notes, badges, AI assistance, and saved searches, and Send it — producing the shareable link the client opens.
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-10-SUMMARY.md` when done.
</output>
