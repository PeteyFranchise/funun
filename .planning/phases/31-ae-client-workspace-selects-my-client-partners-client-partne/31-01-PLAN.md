---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/watermark/provider.ts
  - lib/watermark/README.md
autonomous: false
requirements: [R12, D-01, D-03]
user_setup: []
must_haves:
  truths:
    - "A single named module (lib/watermark/provider.ts) declares the WatermarkProvider interface both the render pipeline (31-12) and the player (31-13) depend on — a stream-preview render and a forensic-download render, both async/pre-computed (never inline in a request handler)."
    - "The chosen watermark approach (in-house ffmpeg-class tone injection vs. a third-party forensic service) and the audible-tag character are recorded as an explicit owner-confirmed decision, not an implicit default (A2: the UI-SPEC 'soft sub-audible tonal pulse' default is flagged for confirmation)."
    - "No watermarking dependency is installed until the Package Legitimacy Gate + a blocking-human checkpoint clears it (A2 — the package is [ASSUMED] until verified)."
  artifacts:
    - "lib/watermark/provider.ts — the WatermarkProvider interface + async-render contract"
    - "lib/watermark/README.md — the approach decision, audible-tag character, and Vercel-Hobby async-render rationale"
  key_links:
    - "lib/watermark/provider.ts is the ONLY contract 31-12 (render) and 31-13 (player) import — neither hard-codes a transcode call."
  prohibitions:
    - statement: "MUST NOT install any watermarking/forensic package before the Package Legitimacy Gate + a blocking-human checkpoint approves it."
      status: resolved
      verification: "checkpoint:human-verify (blocking-human) — owner verifies any [ASSUMED] package on npmjs.com/authoritative source before install; no install task runs in this plan."
    - statement: "MUST NOT design a per-share forensic render that runs synchronously inside a request handler (Vercel Hobby 10s cap — Pitfall 5)."
      status: resolved
      verification: "judgment — the interface's render methods are documented async/pre-computed (render once per (track, share_token), store to a private bucket, serve via signed URL thereafter)."
---

<objective>
Resolve the phase's one genuinely net-new, LOW-confidence dependency BEFORE the player is built: the audio content-protection / watermarking pipeline (D-01 layered watermark, D-03 per-share forensic encoding). This plan does NOT install anything and does NOT render audio — it produces the WatermarkProvider interface + an owner-confirmed approach decision so the render pipeline (31-12) and player (31-13) build against a stable contract, and it gates any proposed dependency behind a blocking-human Package Legitimacy checkpoint.

Purpose: de-risk the phase's only spike-class unknown up front; keep the rest of Slice 1 unblocked (the stream-preview tag + play/react/approve ship on the stream render; the forensic download is sequenced as a fast-follow if the spike runs long — A2).
Output: lib/watermark/provider.ts (interface + async contract) and lib/watermark/README.md (decision record); a cleared package gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-RESEARCH.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-CONTEXT.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the WatermarkProvider interface + approach decision</name>
  <read_first>
    - .planning/phases/31-.../31-RESEARCH.md — Common Pitfalls #5 (Vercel Hobby 10s cap; async/pre-computed render), Open Questions #1 (in-house ffmpeg tone-injection for the stream tag + spread-spectrum/LSB forensic encode for downloads vs. a third-party service), Package Legitimacy Audit
    - .planning/phases/31-.../31-CONTEXT.md — D-01 (layered watermark: audible preview tag + clean-sounding forensic download tag), D-02 (download defaults full-length, AE can cap length / disable), D-03 (forensic payload encodes BOTH the Selects and the recipient/share-token)
    - .planning/phases/31-.../31-UI-SPEC.md — "Watermark character (Claude's discretion)" + "Preview affordance" + "Download gate" sections
    - app/(artist)/vault/[projectId]/play/page.tsx — the real signed-URL streaming precedent the render pipeline (31-12) will adapt
    - lib/storage/index.ts — existing Supabase Storage/signed-URL helpers (the render output must be served this way)
  </read_first>
  <action>
    Create lib/watermark/provider.ts declaring a WatermarkProvider TypeScript interface (named exports, no semicolons, @/ imports, 2-space) with two async methods: renderStreamPreview(input) returning a stored preview object (bucket path + content type), and renderForensicDownload(input) returning a stored forensic-render object; input carries the source master storage path plus selectsId and shareToken (D-03: the forensic payload is keyed to the Selects AND the share-token grantee). Type both as async/Promise and document in the doc-comment that renders are PRE-COMPUTED (rendered once per (track, share_token) at Send time or lazily on first open, stored in a PRIVATE bucket, served by signed URL thereafter) and MUST NOT run inline in a request handler (Vercel Hobby 10s cap). Add a WatermarkRenderStatus union (pending / ready / failed) for the interim "processing" state. Do NOT implement any method body (that is 31-12); provide a single un-exported NotImplemented placeholder or leave method signatures on the interface only. Do NOT reference or import any not-yet-installed package. Record the approach + audible-tag decision in lib/watermark/README.md: the recommended default is in-house ffmpeg-class soft sub-audible tonal-pulse tag for the stream, a forensic (inaudible) encode for the download, both async — flagged as A2 (owner confirms the exact tag character and the in-house-vs-service call at the Task 2 checkpoint before 31-12 implements). Keep the README a decision record, not code.
  </action>
  <acceptance_criteria>
    - `test -f lib/watermark/provider.ts && test -f lib/watermark/README.md` both pass.
    - `grep -q "renderStreamPreview" lib/watermark/provider.ts && grep -q "renderForensicDownload" lib/watermark/provider.ts` — both methods declared.
    - `grep -q "shareToken" lib/watermark/provider.ts` — the forensic input carries the share-token (D-03).
    - `npx tsc --noEmit` passes (the interface type-checks with no missing-import errors — nothing uninstalled is referenced).
    - lib/watermark/README.md states the async/pre-computed render decision and flags the audible-tag character + in-house-vs-service choice as A2 (owner-confirm).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test -f lib/watermark/provider.ts && grep -q "renderForensicDownload" lib/watermark/provider.ts && grep -q "shareToken" lib/watermark/provider.ts</automated>
  </verify>
  <done>The WatermarkProvider interface exists, type-checks, declares both async renders with a share-token-keyed forensic input, and the approach + audible-tag decision are recorded and flagged A2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approach lock + Package Legitimacy checkpoint</name>
  <action>Pause for the owner to lock the watermark approach + audible-tag character and to clear any [ASSUMED] watermarking package through the Package Legitimacy Gate BEFORE any install. No agent install occurs in this plan; this is a human decision gate.</action>
  <what-built>lib/watermark/provider.ts (the async render contract) + lib/watermark/README.md (approach + audible-tag decision, flagged A2). No package was installed.</what-built>
  <how-to-verify>
    1. Read lib/watermark/README.md. Confirm the audible-tag character (default: soft sub-audible tonal pulse) and the in-house-ffmpeg vs. third-party-forensic-service choice — this is your D-01/D-03 decision to lock.
    2. If any specific watermarking dependency is proposed for 31-12, verify it against an authoritative source (npmjs.com/package/<name> — publish date, download counts, source repo) per the Package Legitimacy Gate. Any [ASSUMED]/[SUS] package must be confirmed legitimate here; a [SLOP] package is forbidden. Nothing installs until you approve.
    3. Confirm the async/pre-computed render architecture is acceptable given the current Vercel tier (STATE.md assumes Hobby 10s — re-verify the live tier; if upgraded to Pro/Fluid Compute, note it so 31-12 can simplify).
    4. Confirm you accept the A2 sequencing: the stream-preview tag + the play/react/approve player flow ship first; the forensic-download feature is a fast-follow if the spike runs long.
  </how-to-verify>
  <resume-signal>Type "approved" with the locked approach + tag character (and any approved package name), or describe changes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | A watermarking dependency, if any, crosses an untrusted supply-chain boundary |
| render output → public player | Watermarked renders (31-12) will cross to unauthenticated token viewers (31-13) — the interface must make "master path never served" expressible |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Information Disclosure | WatermarkProvider input surface | high | mitigate | Interface takes the master path only as a render INPUT and returns a NEW private-bucket path; documents that the returned path (never the master path) is what the player may sign — makes the R12 "never a clean master" guarantee expressible at the type boundary. |
| T-31-SC | Tampering | watermarking npm/ffmpeg dependency | high | mitigate | No install in this plan; any [ASSUMED]/[SUS] package is gated by the blocking-human Package Legitimacy checkpoint (Task 2); [SLOP] forbidden. |
</threat_model>

## Artifacts this phase produces (31-01)
- **lib/watermark/provider.ts** — `WatermarkProvider` interface, `WatermarkRenderStatus` union, async `renderStreamPreview` / `renderForensicDownload` contract (net-new file/module)
- **lib/watermark/README.md** — approach + audible-tag decision record (net-new)

<verification>
- `npx tsc --noEmit` clean.
- The interface declares both async renders; the forensic input is share-token-keyed (D-03).
- The blocking-human checkpoint recorded an approach decision and cleared (or deferred) any package before install.
</verification>

<success_criteria>
The watermarking contract + owner-confirmed approach exist and the player build (31-12/31-13) can proceed against a stable interface, with the forensic-download feature explicitly sequenced as a fast-follow (A2).
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-01-SUMMARY.md` when done.
</output>
