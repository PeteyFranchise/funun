---
phase: 31-ae-client-workspace-selects-my-client-partners-client-partne
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/selects/types.ts
  - lib/selects/stage-machine.ts
  - lib/selects/stage-machine.test.ts
autonomous: true
requirements: [R11]
user_setup: []
must_haves:
  truths:
    - "The Selects status pipeline (draft → sent → approved | changes_requested; changes_requested → sent) is a pure, I/O-free validator (isLegalSelectsTransition) mirroring lib/deals/stage-machine.ts — no backward step, no skip, no same-stage self-transition."
    - "A drafted Selects can only reach 'sent' (never straight to approved/changes_requested); a client response can only move a 'sent' Selects to approved or changes_requested; a changes_requested Selects can be re-sent."
  artifacts:
    - "lib/selects/types.ts — the SelectsStatus union + shared Selects/SelectsTrack row types"
    - "lib/selects/stage-machine.ts — isLegalSelectsTransition (pure)"
    - "lib/selects/stage-machine.test.ts — the RED-first legality suite"
  key_links:
    - "31-04's send route and 31-13's respond route both call isLegalSelectsTransition — the single legality authority, never an inline status check."
  prohibitions: []
---

<objective>
Ship the Selects status pipeline as a pure, unit-tested state machine BEFORE any route consumes it (R11). This is the Wave-0 gap RESEARCH's Validation Architecture lists (`lib/selects/stage-machine.test.ts`). It mirrors the shipped `lib/deals/stage-machine.ts` shape exactly.

Purpose: make status legality a single I/O-free authority every writer (31-04 send, 31-13 respond) shares, so an illegal transition (e.g. draft→approved, or a backward move) is impossible by construction and cheaply testable.
Output: lib/selects/types.ts + lib/selects/stage-machine.ts (green) + its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-SPEC.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — Selects status legality suite</name>
  <read_first>
    - lib/deals/stage-machine.ts — the pure forward-pipeline validator shape to mirror (isLegalTransition; one step ahead only, never backward/skip/self)
    - lib/deals/stage-machine.test.ts (if present) — the existing test style
    - .planning/phases/31-.../31-SPEC.md — R11 (status draft → sent → approved/changes_requested) + Edge Coverage row for R11
  </read_first>
  <behavior>
    - draft → sent is legal; draft → approved and draft → changes_requested are illegal.
    - sent → approved and sent → changes_requested are legal; sent → draft is illegal.
    - changes_requested → sent is legal (re-send after a change); changes_requested → approved is illegal (must re-send first).
    - approved is terminal for this pipeline (no legal outbound transition in Slice 1).
    - a same-stage self-transition (x → x) is always illegal.
  </behavior>
  <action>
    Create lib/selects/types.ts exporting a SelectsStatus union (draft/sent/approved/changes_requested) and the shared Selects + SelectsTrack row types the API layer will reuse (match the migration-111 columns). Create lib/selects/stage-machine.test.ts encoding the behavior cases above against a not-yet-implemented isLegalSelectsTransition(from, to) imported from lib/selects/stage-machine. Run it and confirm it FAILS (RED). Commit: test(31-03): add failing Selects status legality suite.
  </action>
  <acceptance_criteria>
    - `npx jest lib/selects/stage-machine.test.ts` runs and FAILS (the module/function does not yet exist or throws) — a genuine RED.
    - lib/selects/types.ts exports SelectsStatus with exactly the four values.
  </acceptance_criteria>
  <verify>
    <automated>npx jest lib/selects/stage-machine.test.ts || true</automated>
  </verify>
  <done>The legality suite exists, imports the not-yet-written validator, and fails RED.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement isLegalSelectsTransition</name>
  <read_first>
    - lib/selects/stage-machine.test.ts (from Task 1) — the binding contract
    - lib/deals/stage-machine.ts — the pure-function convention (named export, no I/O, no throw for a boolean predicate)
  </read_first>
  <action>
    Create lib/selects/stage-machine.ts exporting a pure isLegalSelectsTransition(from: SelectsStatus, to: SelectsStatus): boolean that returns true only for the legal edges (draft→sent, sent→approved, sent→changes_requested, changes_requested→sent) and false for everything else including self-transitions. No Supabase, no I/O, no throw. Run the suite to GREEN. Commit: feat(31-03): implement Selects status state machine.
  </action>
  <acceptance_criteria>
    - `npx jest lib/selects/stage-machine.test.ts` passes (GREEN).
    - `npx tsc --noEmit` clean.
    - isLegalSelectsTransition is a named export, pure (no imports from @/lib/supabase).
  </acceptance_criteria>
  <verify>
    <automated>npx jest lib/selects/stage-machine.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The validator is green and type-clean; the four legal edges pass and all illegal/self transitions are rejected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure logic — no I/O, no user input crosses a boundary here; consumers (31-04/31-13) own their own auth boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-06 | Tampering | Selects status transitions | medium | mitigate | A single pure legality authority prevents a route from writing an illegal status (e.g. skipping the client-approval step) — consumers must gate on it. |
| T-31-SC | Tampering | package installs | high | accept | No new packages (pure TS + existing Jest). |
</threat_model>

## Artifacts this phase produces (31-03)
- **lib/selects/types.ts** — `SelectsStatus` union + `Selects`/`SelectsTrack` row types (net-new)
- **lib/selects/stage-machine.ts** — `isLegalSelectsTransition` (net-new, pure)
- **lib/selects/stage-machine.test.ts** — legality suite (net-new)

<verification>
- `npx jest lib/selects/stage-machine.test.ts` green; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
Selects status legality is a shared, pure, tested authority ready for 31-04 (send) and 31-13 (respond) to consume.
</success_criteria>

<output>
Create `.planning/phases/31-ae-client-workspace-selects-my-client-partners-client-partne/31-03-SUMMARY.md` when done.
</output>
