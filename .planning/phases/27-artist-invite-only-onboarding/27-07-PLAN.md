---
phase: 27-artist-invite-only-onboarding
plan: 07
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - app/api/waitlist/route.ts
  - app/api/waitlist/route.test.ts
  - app/api/waitlist/resubscribe/route.ts
  - app/api/waitlist/resubscribe/route.test.ts
autonomous: true
requirements: [INVITE-07, INVITE-12]
must_haves:
  truths:
    - "A public POST /api/waitlist captures {email, name, note} behind rate-limit + Turnstile captcha, upserting artist_waitlist (D-11/D-12, INVITE-07)."
    - "Re-submitting the waitlist form auto-resubscribes: the upsert clears unsubscribed_at on conflict (D-19, INVITE-12)."
    - "A public POST /api/waitlist/resubscribe clears unsubscribed_at for a row identified by its unsubscribe_token (D-19)."
    - "Captcha verification failure rejects BEFORE any DB write (fail-closed)."
  artifacts:
    - app/api/waitlist/route.ts
    - app/api/waitlist/resubscribe/route.ts
  key_links:
    - "The upsert conflict target is the case-insensitive email index (idx_artist_waitlist_email_lower from 27-01); ON CONFLICT sets unsubscribed_at = NULL."
    - "resubscribe identifies the row by the random unsubscribe_token (not the PK) — IDOR-safe (27-01)."
---

<objective>
Build the public waitlist capture and resubscribe routes. The waitlist submit is D-11's inline denial capture and D-12's protected public endpoint; resubscribe is one of D-19's two re-opt-in paths (the other being auto-resubscribe on rejoin, handled by the same upsert).

Purpose: The signup page (27-09) posts denied visitors to /api/waitlist; the unsubscribe page (27-09) posts to /api/waitlist/resubscribe.
Output: `waitlist/route.ts` + `waitlist/resubscribe/route.ts` (+ tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-artist-invite-only-onboarding/27-CONTEXT.md
@.planning/phases/27-artist-invite-only-onboarding/27-RESEARCH.md
@.planning/phases/27-artist-invite-only-onboarding/27-PATTERNS.md
@app/api/sync/register/route.ts
@.planning/phases/27-artist-invite-only-onboarding/27-01-SUMMARY.md
@.planning/phases/27-artist-invite-only-onboarding/27-02-SUMMARY.md
</context>

<artifacts>
This plan produces:
- `app/api/waitlist/route.ts` — public POST `{ email, name, note, turnstileToken }` → upsert artist_waitlist
- `app/api/waitlist/resubscribe/route.ts` — public POST `{ token }` → clear unsubscribed_at
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/waitlist — captcha + rate-limited upsert with auto-resubscribe</name>
  <files>app/api/waitlist/route.ts, app/api/waitlist/route.test.ts</files>
  <read_first>
    - app/api/sync/register/route.ts (public-write shape, rate-limit, generic errors)
    - lib/security/turnstile.ts (verifyTurnstileToken, fail-closed — from 27-02)
    - lib/security/rate-limit.ts (shared limiter — from 27-02)
    - lib/invites/schema.ts (sanitizeWaitlistEntry field allowlist — from 27-01)
    - .planning/phases/27-artist-invite-only-onboarding/27-RESEARCH.md (the ON CONFLICT auto-resubscribe upsert)
  </read_first>
  <behavior>
    - POST with a valid captcha token + sanitized {email,name,note} under the limit → upsert artist_waitlist, 201/200 success.
    - Captcha verification fails → 400/403 BEFORE any DB write (fail-closed).
    - Over the ip OR email rate limit → 429 generic.
    - Existing email (ON CONFLICT on lower(email)) → updates name/note and clears unsubscribed_at (auto-resubscribe, D-19).
    - Invalid email (sanitizeWaitlistEntry rejects) → 400.
    - Response never reveals whether the email was already on the list (neutral success).
  </behavior>
  <action>
    Create `app/api/waitlist/route.ts` — public POST, no session gate. Rate-limit by `ip:` (shared limiter from `@/lib/security/rate-limit`). Parse the body, run `sanitizeWaitlistEntry` from `@/lib/invites/schema`; 400 on reject. Rate-limit by `email:` too. Verify the Turnstile token via `verifyTurnstileToken(token, ip)` from `@/lib/security/turnstile` — if false, return a generic 400/403 and do NOT touch the DB (fail-closed). Then `createServiceClient()` and upsert into `artist_waitlist` using ON CONFLICT on the case-insensitive email index, setting name/note and `unsubscribed_at = NULL` on conflict (D-19 auto-resubscribe) — mirror RESEARCH's upsert. Return a neutral success (never leak already-present state). Write `app/api/waitlist/route.test.ts` covering: valid submit → success + upsert called; captcha-fail → no DB write; invalid email → 400; over-limit → 429; conflict path clears unsubscribed_at. Mock service client + turnstile + rate limiter.
  </action>
  <verify>
    <automated>npm test -- app/api/waitlist/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- app/api/waitlist/route.test.ts` passes.
    - A failing captcha short-circuits before any DB call (assert upsert not invoked).
    - The upsert clears unsubscribed_at on conflict (auto-resubscribe).
    - Field allowlist (sanitizeWaitlistEntry) is enforced; extra keys never reach the DB.
  </acceptance_criteria>
  <done>The waitlist route captures behind captcha+rate-limit, upserts with auto-resubscribe, and is tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/waitlist/resubscribe — token-scoped re-opt-in</name>
  <files>app/api/waitlist/resubscribe/route.ts, app/api/waitlist/resubscribe/route.test.ts</files>
  <read_first>
    - app/api/sync/register/route.ts (public-write shape)
    - lib/security/rate-limit.ts (shared limiter)
    - .planning/phases/27-artist-invite-only-onboarding/27-01-SUMMARY.md (artist_waitlist.unsubscribe_token — the random, non-PK identifier)
  </read_first>
  <behavior>
    - POST { token } matching an artist_waitlist.unsubscribe_token → set unsubscribed_at = NULL, success.
    - Unknown/missing token → generic 404/400 (no enumeration of valid tokens beyond existence).
    - Rate-limited by ip.
    - Never accepts a row PK or email as the identifier — only the random unsubscribe_token (IDOR-safe).
  </behavior>
  <action>
    Create `app/api/waitlist/resubscribe/route.ts` — public POST, rate-limited by ip via the shared limiter. Parse `{ token }`; `createServiceClient()`; UPDATE `artist_waitlist SET unsubscribed_at = NULL, updated_at = NOW() WHERE unsubscribe_token = $token` returning the affected row. If zero rows, return a generic 404. Never accept the row PK or email as the resubscribe key — only the random `unsubscribe_token` (RESEARCH IDOR mitigation, T-27-05). Return neutral success. Write `app/api/waitlist/resubscribe/route.test.ts` covering: valid token clears unsubscribed_at; unknown token → 404; over-limit → 429.
  </action>
  <verify>
    <automated>npm test -- app/api/waitlist/resubscribe/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- app/api/waitlist/resubscribe/route.test.ts` passes.
    - The route keys off unsubscribe_token only (never PK/email).
    - A valid token clears unsubscribed_at; unknown token → generic 404.
  </acceptance_criteria>
  <done>The resubscribe route re-opts-in via the random token and is tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → waitlist submit | First-class public write; spam/scraping/injection surface |
| unauthenticated client → resubscribe | Public state toggle keyed by a token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-04 | Denial of Service | public waitlist form spam/scraping | high | mitigate | Rate-limit (ip+email) + Turnstile captcha, fail-closed before DB write (D-12) |
| T-27-05 | Tampering | unsubscribe/resubscribe link tampering (IDOR) | medium | mitigate | Resubscribe keyed by random unsubscribe_token distinct from PK; never accepts PK/email |
| T-27-13 | Elevation of Privilege | direct PostgREST write to artist_waitlist | high | mitigate | Table is zero-RLS + REVOKE (27-01); only these service-role routes write it, behind field allowlist |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed (Turnstile is script + fetch) |
</threat_model>

<verification>
- `npm test -- app/api/waitlist/route.test.ts app/api/waitlist/resubscribe/route.test.ts` green.
- `npm run build` / `tsc --noEmit` clean.
- Live captcha requires the Turnstile keys provisioned in 27-11's checkpoint before the form works end-to-end.
</verification>

<success_criteria>
The public waitlist capture (captcha + rate-limit + auto-resubscribe upsert) and the token-scoped resubscribe route exist, enforce field allowlists, and are tested.
</success_criteria>

<output>
Create `.planning/phases/27-artist-invite-only-onboarding/27-07-SUMMARY.md` when done.
</output>
