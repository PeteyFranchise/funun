---
phase: 27-artist-invite-only-onboarding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/collaborators/CollaboratorInvitePrompt.tsx
  - components/collaborators/CollaboratorRoster.tsx
autonomous: true
requirements: [INVITE-04]
must_haves:
  truths:
    - "After saving a new collaborator that has an email, a default-on prompt offers to invite them to Funūn (D-08a); it does not block or gate the save."
    - "The prompt is a new entry point into the EXISTING /api/collaborators/[id]/invite send action — not a new send mechanism (UI-SPEC surface 4)."
    - "Declining ('Not now') dismisses the prompt without sending; sending calls the existing endpoint and reflects success/error inline."
  artifacts:
    - components/collaborators/CollaboratorInvitePrompt.tsx
  key_links:
    - "CollaboratorRoster.handleSaved detects a newly-created collaborator with a non-empty email and mounts the prompt, wiring its Send action to the existing handleInvite(id)."
---

<objective>
Add the collaborator-side, default-on "Invite {name} to Funūn?" prompt that appears right after saving a new collaborator with an email (D-08a). This is the third of D-08's three invite pathways (the prompt); the explicit "Invite" button on CollaboratorCard already exists, and the auto-send mode is deferred behind a future explicit toggle (INVITE-04).

Purpose: An artist who names a collaborator by email should be nudged — deliberately, opt-in, not silently — to invite them; that collaborator's email is already auto-allowed by the gate (D-04), so the invite deep-link lands them straight into self-serve signup.
Output: `CollaboratorInvitePrompt.tsx` + the CollaboratorRoster wiring to mount it post-save.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-artist-invite-only-onboarding/27-CONTEXT.md
@.planning/phases/27-artist-invite-only-onboarding/27-UI-SPEC.md
@.planning/phases/27-artist-invite-only-onboarding/27-PATTERNS.md
@components/collaborators/CollaboratorRoster.tsx
@components/collaborators/CollaboratorForm.tsx
@components/collaborators/CollaboratorCard.tsx
</context>

<artifacts>
This plan produces:
- Component: `components/collaborators/CollaboratorInvitePrompt.tsx` (default-checked inline prompt)
- Wiring: `CollaboratorRoster.handleSaved` mounts the prompt for a newly-created collaborator with an email
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: CollaboratorInvitePrompt.tsx — default-on inline invite prompt</name>
  <files>components/collaborators/CollaboratorInvitePrompt.tsx</files>
  <read_first>
    - components/collaborators/CollaboratorForm.tsx (the inline-swap panel convention + Cancel text-button styling to mirror for "Not now")
    - components/collaborators/CollaboratorCard.tsx (the existing bg-grad shadow-cta "Invite" button treatment to reuse for "Send invite")
    - .planning/phases/27-artist-invite-only-onboarding/27-UI-SPEC.md (surface 4 + copywriting row "Collaborator-side invite prompt")
  </read_first>
  <action>
    Create `components/collaborators/CollaboratorInvitePrompt.tsx` as a `'use client'` component rendered inline (NOT a modal), matching the codebase's inline-swap preference. Props: `{ collaboratorName: string; onSend: () => Promise<{ ok: boolean; error?: string }>; onDismiss: () => void }`. Render a small transient panel using the authenticated brand tokens (`bg-card`, `border-hair`, `text-lav`/`text-lavdim`) with copy per UI-SPEC: heading "Invite {first name} to Funūn?" plus a pre-checked checkbox/toggle (default-on per D-08). Two actions: primary "Send invite" using the existing `bg-grad ... shadow-cta` gradient button treatment (the reserved accent per UI-SPEC Color); secondary "Not now" as a text-only button matching CollaboratorForm's Cancel. On Send: call `onSend()`, show a present-participle "Sending…" disabled state, then on `{ok:true}` swap to a brief "Invite sent" confirmation and auto-dismiss; on `{ok:false}` show the returned error in the existing rose error-block style. First name is derived from collaboratorName (split on whitespace, fall back to full name). No new send mechanism — Send delegates entirely to the caller's onSend.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Component compiles; default state has the invite toggle checked (D-08 default-on).
    - "Send invite" uses the reserved gradient (bg-grad/shadow-cta); "Not now" is text-only.
    - Send delegates to the injected onSend prop (no direct fetch inside the prompt).
    - Error path renders the existing rose error block; success auto-dismisses.
  </acceptance_criteria>
  <done>The prompt component exists, is default-on, and drives all sending through its onSend prop.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the prompt into CollaboratorRoster's post-save flow</name>
  <files>components/collaborators/CollaboratorRoster.tsx</files>
  <read_first>
    - components/collaborators/CollaboratorRoster.tsx (handleSaved line 50, handleInvite line 107 — the existing endpoint call to reuse)
  </read_first>
  <action>
    In `CollaboratorRoster.tsx`, extend `handleSaved(saved)` so that when the saved collaborator is NEWLY created (not an edit of an existing row) AND has a non-empty email, it sets a small piece of state (e.g. `invitePromptFor: CollaboratorProfile | null`) that mounts `CollaboratorInvitePrompt` inline near the just-saved row (or at the top of the roster list). Wire the prompt's `onSend` to the existing `handleInvite(saved.id)` (unchanged — it already POSTs to `/api/collaborators/[id]/invite`) and `onDismiss` to clear `invitePromptFor`. The prompt must NOT block or delay the save — the row is already added to `list` by the existing handleSaved logic; the prompt is a post-save affordance only (D-08a: "does not block the save"). Distinguish new-vs-edit using the existing `editingId`/`creating` state or by checking whether the saved id was already present in `list` before the save.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Saving a NEW collaborator with an email mounts the prompt; saving an edit, or a new collaborator with no email, does not.
    - The collaborator row is added to the roster regardless of the prompt (save is never gated).
    - onSend reuses the existing handleInvite endpoint call; no new send path is introduced.
    - `npm run build` clean.
  </acceptance_criteria>
  <done>The post-save prompt appears only for new email-bearing collaborators and reuses the existing invite endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated artist → existing invite endpoint | Prompt is a new UI entry point into an already-authorized, owned-collaborator action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-09 | Spoofing | Collaborator invite send | low | mitigate | Reuses the existing `/api/collaborators/[id]/invite` route unchanged — its ownership check (collaborator must belong to the requesting user) and 60s cooldown already apply; this plan adds no new send authority |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed |
</threat_model>

<verification>
- `npm run build` and `tsc --noEmit` clean.
- Manual/visual: after creating a new collaborator with an email, the default-on prompt renders; "Not now" dismisses; "Send invite" reflects success/error (covered by 27-11 live checkpoint if desired).
</verification>

<success_criteria>
The default-on collaborator invite prompt (D-08a, INVITE-04) exists and reuses the existing send action without blocking the save.
</success_criteria>

<output>
Create `.planning/phases/27-artist-invite-only-onboarding/27-03-SUMMARY.md` when done.
</output>
