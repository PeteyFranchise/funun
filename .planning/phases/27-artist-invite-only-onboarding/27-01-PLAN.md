---
phase: 27-artist-invite-only-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/097_artist_invites_and_waitlist.sql
  - lib/invites/schema.ts
  - lib/invites/schema.test.ts
autonomous: true
requirements: [INVITE-02]
must_haves:
  truths:
    - "artist_invites and artist_waitlist exist as service-role-only tables (zero RLS policies + REVOKE from anon/authenticated) — a browser cannot read or write them directly (D-04, RESEARCH Pitfall 4)."
    - "An email can be looked up for an existing account without a full-table scan, via a service-role-only SECURITY DEFINER helper (RESEARCH Don't Hand-Roll)."
    - "The invite/waitlist field vocabulary (sources, statuses) has a single TypeScript source of truth with input sanitizers."
  artifacts:
    - supabase/migrations/097_artist_invites_and_waitlist.sql
    - lib/invites/schema.ts
    - lib/invites/schema.test.ts
  key_links:
    - "artist_invites.email + artist_waitlist.email carry a case-insensitive index (LOWER(email)) so the gate predicate and check-invite route match emails consistently (D-04 claim_collaborators parity)."
    - "artist_waitlist.unsubscribe_token is a distinct random token, NOT the row PK (RESEARCH Security Domain — IDOR mitigation for D-19 unsubscribe links)."
---

<objective>
Create the data foundation for invite-only onboarding: the two new tables the gate and the public routes read/write, plus the TypeScript schema module (source/status vocabularies + input sanitizers) every consumer imports.

Purpose: Every other plan in this phase reads from or writes to `artist_invites`/`artist_waitlist`, or the `lib/invites/schema.ts` vocabulary. Building this first unblocks Waves 2–4 in parallel.
Output: Migration 097 (schema-only, isolated from the higher-risk gate migration 098), `lib/invites/schema.ts` + its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-artist-invite-only-onboarding/27-CONTEXT.md
@.planning/phases/27-artist-invite-only-onboarding/27-RESEARCH.md
@.planning/phases/27-artist-invite-only-onboarding/27-PATTERNS.md
</context>

<artifacts>
This plan produces:
- Tables: `public.artist_invites`, `public.artist_waitlist`
- Function: `public.email_has_account(p_email text) RETURNS boolean` (SECURITY DEFINER, service_role only)
- Indexes: `idx_artist_invites_email_lower`, `idx_artist_waitlist_email_lower` (unique), plus any status index
- Migration file: `supabase/migrations/097_artist_invites_and_waitlist.sql`
- Module: `lib/invites/schema.ts` exporting `ARTIST_INVITE_SOURCE_VALUES`, `ARTIST_INVITE_STATUS_VALUES`, types `ArtistInviteSource`/`ArtistInviteStatus`, and `sanitizeWaitlistEntry()`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Migration 097 — artist_invites + artist_waitlist tables + email_has_account() helper</name>
  <files>supabase/migrations/097_artist_invites_and_waitlist.sql</files>
  <read_first>
    - supabase/migrations/089_funun_staff_and_audit.sql (zero-RLS-policy + REVOKE + service-role-only shape — the exact analog per 27-PATTERNS)
    - supabase/migrations/018_collaborators_split_sheets.sql (tokened invite table column shape)
    - supabase/migrations/075_phase19_privilege_hardening.sql (the "why not reuse X" header-comment convention + claim_collaborators() EXECUTE lockdown to mirror for email_has_account)
    - supabase/migrations/001_initial_schema.sql (the dead legacy `waitlist` table — confirm its shape so 097's header documents the deliberate divergence, RESEARCH Pitfall 5)
  </read_first>
  <action>
    Create migration 097 as a SCHEMA-ONLY migration (no trigger changes — the gate lives in a separate, higher-risk 098 per RESEARCH's isolation recommendation). Follow the `funun_staff` shape exactly (per D-04 / RESEARCH Pitfall 4): each table gets `ENABLE ROW LEVEL SECURITY` with ZERO row-level policy-creation statements plus `REVOKE SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON <table> FROM authenticated, anon`. All reads/writes go through service-role API routes in later plans.

    Table `public.artist_invites`: `id` uuid PK default gen_random_uuid(); `email` text NOT NULL; `status` text NOT NULL default 'pending' with CHECK in the ARTIST_INVITE_STATUS_VALUES set (pending/accepted/expired); `invite_token` text (nullable — owner_seed rows may carry none); `token_expires_at` timestamptz; `invited_by_user_id` uuid NULL REFERENCES auth.users ON DELETE SET NULL (nullable — waitlist conversions and owner seed may have no single named inviter); `source` text NOT NULL CHECK in ARTIST_INVITE_SOURCE_VALUES (collaborator/staff/waitlist_conversion/owner_seed); `accepted_user_id` uuid NULL REFERENCES auth.users ON DELETE SET NULL; `accepted_at` timestamptz; `created_at`/`updated_at` timestamptz NOT NULL default now(). Add `CREATE INDEX idx_artist_invites_email_lower ON public.artist_invites (LOWER(email))` and an index on `status` for the gate's pending lookup.

    Table `public.artist_waitlist`: `id` uuid PK default gen_random_uuid(); `email` text NOT NULL; `name` text; `note` text; `unsubscribed_at` timestamptz NULL (D-19 broadcast-scoped opt-out); `unsubscribe_token` text NOT NULL default encode(gen_random_bytes(32),'hex') (distinct random token, NOT the PK — RESEARCH IDOR mitigation); `notified_reopen_at` timestamptz NULL (RESEARCH Pitfall 6 idempotency guard); `converted_to_invite_at` timestamptz NULL; `created_at`/`updated_at` timestamptz NOT NULL default now(). Enforce case-insensitive uniqueness with `CREATE UNIQUE INDEX idx_artist_waitlist_email_lower ON public.artist_waitlist (LOWER(email))` (the `email_lower` conflict target RESEARCH's upsert example relies on).

    Function `public.email_has_account(p_email text) RETURNS boolean` LANGUAGE sql SECURITY DEFINER: `SELECT EXISTS(SELECT 1 FROM auth.users WHERE LOWER(email) = LOWER(p_email))`. Immediately `REVOKE EXECUTE ON FUNCTION public.email_has_account(text) FROM PUBLIC, anon, authenticated` and `GRANT EXECUTE ... TO service_role`, mirroring migration 075's claim_collaborators lockdown (RESEARCH Don't Hand-Roll — Supabase JS admin SDK has no getUserByEmail).

    Header comment (mirror migration 075's "why not reuse X" style): document (a) this is a deliberate NEW `artist_waitlist`, not the dead migration-001 `waitlist` table (schema mismatch: no name/note/unsubscribed_at; its permissive INSERT policy predates this repo's privilege hardening — RESEARCH Pitfall 5); (b) HUMAN-GATED push — never `supabase db push` from an agent (project convention; the push is 27-11's blocking task).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/097_artist_invites_and_waitlist.sql | grep -c 'CREATE POLICY' | grep -qx 0 && grep -q 'REVOKE' supabase/migrations/097_artist_invites_and_waitlist.sql && grep -q 'artist_invites' supabase/migrations/097_artist_invites_and_waitlist.sql && grep -q 'artist_waitlist' supabase/migrations/097_artist_invites_and_waitlist.sql && grep -q 'email_has_account' supabase/migrations/097_artist_invites_and_waitlist.sql && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both tables have `ENABLE ROW LEVEL SECURITY` and ZERO `CREATE POLICY` statements (service-role-only shape).
    - Both tables carry `REVOKE ... FROM authenticated, anon`.
    - `artist_waitlist` has a distinct `unsubscribe_token` column that is not the primary key, and a case-insensitive UNIQUE index on email.
    - `email_has_account(text)` is SECURITY DEFINER with EXECUTE revoked from PUBLIC/anon/authenticated and granted to service_role only.
    - Header comment documents the deliberate divergence from the dead migration-001 `waitlist` table and the human-gated push.
  </acceptance_criteria>
  <done>Migration 097 file exists, is schema-only (no handle_new_user change), and satisfies the service-role-only + IDOR-safe token + case-insensitive-email criteria above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/invites/schema.ts — source/status vocabularies + waitlist sanitizer</name>
  <files>lib/invites/schema.ts, lib/invites/schema.test.ts</files>
  <read_first>
    - lib/metadata/schema.ts (the `_VALUES`/`_LABELS` export convention this repo uses — 27-PATTERNS role-match)
    - lib/collaborators/index.ts (the sanitizer/read-loosely-with-type-guards pattern to mirror)
    - app/api/profile/route.ts (explicit editable-field allowlist / mass-assignment defense convention)
  </read_first>
  <behavior>
    - sanitizeWaitlistEntry({email, name, note}) trims strings, lowercases email, and returns only the allowlisted fields (email/name/note) — extra keys are dropped (mass-assignment defense).
    - sanitizeWaitlistEntry rejects (returns an error result, not a throw) when email is empty or not a valid-looking address; name/note are optional and may be empty.
    - note is length-capped (choose a sane cap, e.g. 1000 chars) and coerced to a string.
    - ARTIST_INVITE_SOURCE_VALUES = ['collaborator','staff','waitlist_conversion','owner_seed']; ARTIST_INVITE_STATUS_VALUES = ['pending','accepted','expired']; both exported as `as const` with derived union types.
  </behavior>
  <action>
    Create `lib/invites/schema.ts` with named exports only, no semicolons, 2-space indent, `@/` imports. Export `ARTIST_INVITE_SOURCE_VALUES`/`ARTIST_INVITE_STATUS_VALUES` (`as const`) and the derived `ArtistInviteSource`/`ArtistInviteStatus` types. Export `sanitizeWaitlistEntry(input: unknown)` returning a discriminated result (`{ ok: true, value: { email, name, note } } | { ok: false, error: string }`) that mirrors the split-sheets/profile sanitizer convention: read loosely with `typeof` guards, trim, lowercase the email, drop non-allowlisted keys, cap note length, and validate email shape with a simple regex. Keep it pure (no I/O) so both the waitlist route (27-07) and tests import it. Write `lib/invites/schema.test.ts` covering the behavior cases above (valid entry, missing email, extra-key stripping, note cap, email lowercasing).
  </action>
  <verify>
    <automated>npm test -- lib/invites/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/invites/schema.test.ts` passes.
    - sanitizeWaitlistEntry drops any key not in the {email,name,note} allowlist.
    - Email is lowercased and shape-validated; invalid email returns `{ ok: false }`.
    - Source/status value arrays are exported `as const` with union types.
  </acceptance_criteria>
  <done>Schema module + tests exist and pass; the vocabulary and sanitizer are importable by later plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PostgREST | Untrusted client could attempt direct reads/writes of the new tables |
| API route → auth.users | email-existence lookup crosses into the protected auth schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Elevation of Privilege | artist_invites / artist_waitlist direct PostgREST access | high | mitigate | Zero-RLS-policy + REVOKE from anon/authenticated (funun_staff shape); every access via service-role route in later plans |
| T-27-05 | Tampering | artist_waitlist.unsubscribe_token | medium | mitigate | Random 32-byte token distinct from the row PK, so waitlist IDs leaked elsewhere can't toggle a stranger's subscription (D-19) |
| T-27-02 | Information Disclosure | email_has_account() over auth.users | medium | mitigate | SECURITY DEFINER function EXECUTE-revoked from anon/authenticated, granted to service_role only; never exposed to PostgREST |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no new packages (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npm test -- lib/invites/schema.test.ts` green.
- Migration 097 grep-gate passes (zero policies, REVOKE present, both tables + helper present).
- `npm run build` / `tsc --noEmit` clean (schema module compiles; migration is not type-checked).
</verification>

<success_criteria>
Both tables + the email_has_account helper are defined in migration 097 with the service-role-only posture; the schema module + sanitizer are tested and importable.
</success_criteria>

<output>
Create `.planning/phases/27-artist-invite-only-onboarding/27-01-SUMMARY.md` when done.
</output>
