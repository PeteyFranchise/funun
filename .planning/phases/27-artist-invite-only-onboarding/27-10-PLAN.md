---
phase: 27-artist-invite-only-onboarding
plan: 10
type: execute
wave: 4
depends_on: ["27-08"]
files_modified:
  - app/(admin)/admin/artist-invites/page.tsx
  - components/admin/ArtistInvitesAdmin.tsx
  - app/(admin)/layout.tsx
autonomous: true
requirements: [INVITE-08, INVITE-09]
must_haves:
  truths:
    - "The Team Console has an Artist Invites page at /admin/artist-invites, reachable from the all-staff nav section (D-06/D-14, INVITE-09)."
    - "Any staff sees the waitlist with a 'Convert to invite' per-row action; only Leadership sees the 'Reopen & broadcast' page-level action (D-15) — gated by a server-resolved isLeadership prop, not CSS."
    - "Opted-out rows show an 'Unsubscribed' chip but keep 'Convert to invite' enabled (a personal invite still sends, D-19)."
    - "A search filters the waitlist by name+email live; a search-specific empty state shows when nothing matches."
  artifacts:
    - app/(admin)/admin/artist-invites/page.tsx
    - components/admin/ArtistInvitesAdmin.tsx
    - modified app/(admin)/layout.tsx (all-staff nav link)
  key_links:
    - "The page runs the getStaffRole gate (any staff), fetches waitlist+invites via the service client, and passes isLeadership to the component (mirrors team-members page)."
    - "The component calls the 27-08 routes: convert (any staff), broadcast (leadership, two-step confirm)."
---

<objective>
Build the Team Console Artist Invites surface (D-14): a page + client component that lists the waitlist, converts entries to invites (any staff), and — for Leadership only — reopens with a broadcast, plus the nav link. This is the staff management home for the invite/waitlist system.

Purpose: Gives staff the UI over the 27-08 routes; enforces the D-06/D-15 privilege split visually and server-side.
Output: `admin/artist-invites/page.tsx`, `ArtistInvitesAdmin.tsx`, and the nav link in `app/(admin)/layout.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-artist-invite-only-onboarding/27-CONTEXT.md
@.planning/phases/27-artist-invite-only-onboarding/27-UI-SPEC.md
@.planning/phases/27-artist-invite-only-onboarding/27-PATTERNS.md
@app/(admin)/admin/team-members/page.tsx
@app/(admin)/layout.tsx
@.planning/phases/27-artist-invite-only-onboarding/27-08-SUMMARY.md
</context>

<artifacts>
This plan produces:
- `app/(admin)/admin/artist-invites/page.tsx` — server component (any-staff gate, list fetch, isLeadership prop)
- `components/admin/ArtistInvitesAdmin.tsx` — client list + search + convert + unsubscribed chip + leadership-only broadcast
- Modified `app/(admin)/layout.tsx` — "Artist Invites" link in the all-staff nav section
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: /admin/artist-invites page + all-staff nav link</name>
  <files>app/(admin)/admin/artist-invites/page.tsx, app/(admin)/layout.tsx</files>
  <read_first>
    - app/(admin)/admin/team-members/page.tsx (page shell + per-page role gate + service-client list fetch — the exact analog)
    - app/(admin)/layout.tsx (the all-staff nav section, lines 96-102 — "My Client Partners"/"Directory"; add the link here, NOT the isLeadership block)
    - lib/admin/gate.ts (getStaffRole)
  </read_first>
  <action>
    Create `app/(admin)/admin/artist-invites/page.tsx` with `export const dynamic = 'force-dynamic'`, mirroring the team-members page shell: `createServerClient()`, `getUser()` (redirect /signin if none), `getStaffRole(user)` — but admit ANY staff role (D-06: redirect '/' only if no staff role at all, unlike team-members' leadership-only gate). Use `createServiceClient()` to fetch the waitlist (column-explicit select: id, email, name, note, unsubscribed_at, converted_to_invite_at, created_at) ordered by created_at desc, and the artist_invites list (column-explicit). Render `<div className="flex-1 px-9 py-[30px]">` + `<h1 className="text-2xl font-bold text-[color:var(--ink)]">Artist Invites</h1>` + `<ArtistInvitesAdmin initialWaitlist={...} initialInvites={...} isLeadership={role === 'leadership'} />`. Then edit `app/(admin)/layout.tsx` to add a `<Link href="/admin/artist-invites" className={NAV_LINK_CLASS}>Artist Invites</Link>` inside the ALL-STAFF section (next to "My Client Partners"/"Directory"), NOT inside the `isLeadership` block (D-06 — any Team Member manages individual invites).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q '/admin/artist-invites' "app/(admin)/layout.tsx" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - The page admits any staff role (not leadership-only) and redirects non-staff.
    - It fetches waitlist + invites via the service client (column-explicit) and passes isLeadership.
    - The nav link lives in the all-staff section (visible to AE/BD, not gated to leadership).
    - `npm run build` clean.
  </acceptance_criteria>
  <done>The page + all-staff nav link exist; any staff can reach it, isLeadership is passed through.</done>
</task>

<task type="auto">
  <name>Task 2: ArtistInvitesAdmin.tsx — list, search, convert, unsubscribed chip, leadership broadcast</name>
  <files>components/admin/ArtistInvitesAdmin.tsx</files>
  <read_first>
    - components/admin/StaffAdmin.tsx (the themed .fncon list/row/action markup + var(--panel)/var(--border) treatment — the exact analog)
    - components/collaborators/CollaboratorForm.tsx (the two-step delete-confirm pattern to mirror for "Reopen & broadcast")
    - components/collaborators/CollaboratorCard.tsx (timeAgo() "Invited Xh ago" relative-timestamp pattern)
    - .planning/phases/27-artist-invite-only-onboarding/27-UI-SPEC.md (surface 5 — search, rows, convert action, unsubscribed chip, leadership-only broadcast; copywriting rows)
  </read_first>
  <action>
    Create `components/admin/ArtistInvitesAdmin.tsx` (`'use client'`). Props: `{ initialWaitlist, initialInvites, isLeadership }`. Render the themed `.fncon` list on `var(--panel)`/`var(--border)` (mirror StaffAdmin rows). Above the list: a search input (themed `var(--input)` bg, leading magnifier glyph, placeholder "Search the waiting list by name or email…") that live-filters rows case-insensitively over name+email (no submit button), with a live count ("N matches" while searching / "Showing N of M" at rest) and a search-specific empty state ("No one on the waiting list matches that search.") distinct from the never-populated empty state ("No one on the waiting list yet"). Each waitlist row: email/name/note + a per-row "Convert to invite" action as a small `.fncon-cta` gradient button (visible to ANY staff); on success flip the row's status chip to "Invited" with a relative timestamp (timeAgo). Rows whose `unsubscribed_at` is set show a small rose "Unsubscribed" pill (`border-rose-500/30 text-rose-200`), but the "Convert to invite" action STAYS ENABLED (D-19 — a personal invite still sends). Page-level "Reopen & notify waitlist" button rendered ONLY when `isLeadership` (never hidden via CSS alone) → two-step inline confirm mirroring CollaboratorForm's delete-confirm ("Send this to {N} people on the waiting list? This can't be undone." + solid `bg-rose-500/90 hover:bg-rose-500` "Yes, send" + text-only "Cancel"). Wire actions to the 27-08 routes: convert → POST `/api/admin/artist-invites/[id]/convert`; broadcast → POST `/api/admin/artist-invites/broadcast`. Reflect loading/success/error inline.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Convert action is visible to any staff and calls the convert route; success flips the row chip to "Invited" with a relative time.
    - The "Reopen & broadcast" button renders only when isLeadership (server-resolved prop), with a two-step confirm.
    - Unsubscribed rows show the rose chip but keep Convert enabled (D-19).
    - Search filters name+email live with the correct count + search-empty state.
    - `npm run build` clean.
  </acceptance_criteria>
  <done>The admin component delivers surface 5: search, any-staff convert, leadership-only broadcast, unsubscribed chip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff session → admin page | Any-staff visibility; leadership-only broadcast affordance |
| client component → 27-08 routes | The routes re-enforce authz; UI gating is defense-in-depth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-06 | Elevation of Privilege | leadership-only broadcast affordance | high | mitigate | Button rendered only on the server-resolved isLeadership prop (never CSS-hidden); the broadcast route independently enforces requireStaff(['leadership']) (27-08) |
| T-27-16 | Elevation of Privilege | non-staff reaching the admin page | high | mitigate | Per-page getStaffRole gate + the layout gate (defense-in-depth, team-members precedent) |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed |
</threat_model>

<verification>
- `npm run build` / `tsc --noEmit` clean.
- Nav link present in the all-staff section (grep gate).
- Visual/functional convert + broadcast confirm exercised in the 27-11 live checkpoint if desired.
</verification>

<success_criteria>
The Team Console Artist Invites surface exists with the D-06/D-15 privilege split (any-staff convert, leadership-only broadcast), the unsubscribed chip (D-19), search, and the all-staff nav link.
</success_criteria>

<output>
Create `.planning/phases/27-artist-invite-only-onboarding/27-10-SUMMARY.md` when done.
</output>
