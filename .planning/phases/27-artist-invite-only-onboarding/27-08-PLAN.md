---
phase: 27-artist-invite-only-onboarding
plan: 08
type: execute
wave: 3
depends_on: ["27-01", "27-05"]
files_modified:
  - app/api/admin/artist-invites/route.ts
  - app/api/admin/artist-invites/route.test.ts
  - app/api/admin/artist-invites/[id]/convert/route.ts
  - app/api/admin/artist-invites/[id]/convert/route.test.ts
  - app/api/admin/artist-invites/broadcast/route.ts
  - app/api/admin/artist-invites/broadcast/route.test.ts
autonomous: true
requirements: [INVITE-03, INVITE-08]
must_haves:
  truths:
    - "Any Team Member can list the waitlist/invites and issue an individual invite (GET + POST /api/admin/artist-invites), gated by requireStaff() with no role restriction (D-06, INVITE-03)."
    - "Invites are unlimited for now — no per-inviter cap or quota is enforced (D-07, quotas deferred per CONTEXT Deferred Ideas)."
    - "Any Team Member can convert a waitlist entry to an invite; a 'spot opened' email sends (D-13a, INVITE-08)."
    - "Only Leadership can trigger the reopen broadcast; it is idempotent (skips already-notified rows) and excludes opted-out recipients while personal invites still send (D-15/D-19, INVITE-08)."
    - "Every invite/convert/broadcast action writes through logStaffAction (staff audit precedent)."
  artifacts:
    - app/api/admin/artist-invites/route.ts
    - app/api/admin/artist-invites/[id]/convert/route.ts
    - app/api/admin/artist-invites/broadcast/route.ts
  key_links:
    - "convert + POST-invite use generateApprovalToken (lib/split-sheets/approval) for invite_token, INSERT artist_invites, and send the artist* emails from 27-05."
    - "broadcast query filters artist_waitlist WHERE unsubscribed_at IS NULL AND notified_reopen_at IS NULL, then stamps notified_reopen_at (RESEARCH Pitfall 6 idempotency)."
---

<objective>
Build the Team Console backend: list + issue individual invites (any staff), convert a waitlist entry to an invite with a "spot opened" email (any staff), and the Leadership-only reopen broadcast (idempotent, opt-out-aware). These are D-06/D-13/D-15's staff surfaces.

Purpose: The admin UI (27-10) calls these; each is a staff mutation that must carry the requireStaff gate + logStaffAction audit.
Output: three route files (+ tests) under `app/api/admin/artist-invites/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-artist-invite-only-onboarding/27-CONTEXT.md
@.planning/phases/27-artist-invite-only-onboarding/27-RESEARCH.md
@.planning/phases/27-artist-invite-only-onboarding/27-PATTERNS.md
@lib/admin/gate.ts
@lib/staff/audit.ts
@app/api/collaborators/[id]/invite/route.ts
@.planning/phases/27-artist-invite-only-onboarding/27-05-SUMMARY.md
</context>

<artifacts>
This plan produces:
- `app/api/admin/artist-invites/route.ts` — GET (list waitlist + invites, any staff) + POST (issue direct invite by email, any staff, source='staff', sends artistInvite email A)
- `app/api/admin/artist-invites/[id]/convert/route.ts` — POST (waitlist row → invite, any staff, source='waitlist_conversion', sends artistSpotOpened email B)
- `app/api/admin/artist-invites/broadcast/route.ts` — POST (Leadership-only reopen broadcast, sends artistReopened email C, idempotent)
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET+POST /api/admin/artist-invites — list + issue individual invite (any staff)</name>
  <files>app/api/admin/artist-invites/route.ts, app/api/admin/artist-invites/route.test.ts</files>
  <read_first>
    - lib/admin/gate.ts (requireStaff() — default ALL_STAFF_ROLES; the { error, status } vs { user, staffRole } contract)
    - lib/staff/audit.ts (logStaffAction signature — actorId/action/targetType/targetId/changes)
    - lib/split-sheets/approval.ts (generateApprovalToken / APPROVAL_TOKEN_EXPIRY_DAYS)
    - lib/email/artistInvite.ts (template A from 27-05) + lib/email/index.ts (sendEmail)
  </read_first>
  <behavior>
    - GET (any staff) → { waitlist: [...], invites: [...] } from the two tables via service client; non-staff → 403; unauthenticated → 401.
    - POST { email } (any staff) → sanitize/validate email; INSERT artist_invites (source='staff', status='pending', invite_token=generateApprovalToken(), token_expires_at=+APPROVAL_TOKEN_EXPIRY_DAYS, invited_by_user_id=actor); send artistInvite email A with a /signup?invite=token deep-link; logStaffAction('artist_invite.create'). Duplicate pending invite for the same email → idempotent (no duplicate row, still ok).
    - All DB access via createServiceClient() AFTER the requireStaff gate.
  </behavior>
  <action>
    Create `app/api/admin/artist-invites/route.ts`. GET: call `requireStaff()` (default any staff role); on error return its {error,status}; else `createServiceClient()` and select the column-explicit waitlist + invites lists (never select-star), return `{ waitlist, invites }`. POST: `requireStaff()` (any staff); validate the email (reuse a simple email check or sanitizeWaitlistEntry's email validation); guard against an existing pending invite for that email (idempotent); INSERT `artist_invites` with source='staff', a fresh `generateApprovalToken()`, expiry = now + APPROVAL_TOKEN_EXPIRY_DAYS, invited_by_user_id = the actor; build the deep-link `${NEXT_PUBLIC_APP_URL}/signup?invite=${token}`; send `artistInviteEmail({ inviterName, actionLink })` via sendEmail (best-effort — a send failure returns ok with emailSent:false, matching the collaborators invite route); call `logStaffAction(service, { actorId, action:'artist_invite.create', targetType:'artist_invite', targetId, changes:{ email } })`. Write `route.test.ts` covering: GET as staff returns both lists; GET as non-staff → 403; POST creates an invite + sends email + audits; POST duplicate → no second row.
  </action>
  <verify>
    <automated>npm test -- app/api/admin/artist-invites/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- app/api/admin/artist-invites/route.test.ts` passes.
    - Both GET and POST call requireStaff() before createServiceClient(); non-staff → 403.
    - POST issues a tokened invite (generateApprovalToken), sends template A, and logs a staff action.
    - Duplicate pending invite for the same email is idempotent.
  </acceptance_criteria>
  <done>The list + direct-invite route is gated to any staff, tokened, emailed, and audited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/admin/artist-invites/[id]/convert — waitlist → invite (any staff)</name>
  <files>app/api/admin/artist-invites/[id]/convert/route.ts, app/api/admin/artist-invites/[id]/convert/route.test.ts</files>
  <read_first>
    - lib/admin/gate.ts (requireStaff any staff)
    - lib/staff/audit.ts (logStaffAction)
    - lib/email/artistSpotOpened.ts (template B from 27-05)
    - lib/split-sheets/approval.ts (token generation)
  </read_first>
  <behavior>
    - POST [id] (any staff) → load the artist_waitlist row by id; INSERT artist_invites (source='waitlist_conversion', pending, tokened, invited_by=actor) for that email; set artist_waitlist.converted_to_invite_at; send artistSpotOpened email B with the deep-link; logStaffAction('artist_invite.convert').
    - Convert stays enabled even for an unsubscribed row — a personal invite still sends (D-19).
    - Unknown waitlist id → 404. Already-converted → idempotent (no duplicate invite).
  </behavior>
  <action>
    Create `app/api/admin/artist-invites/[id]/convert/route.ts` — POST resolving the `[id]` param. `requireStaff()` (any staff); `createServiceClient()`; load the `artist_waitlist` row (404 if missing); INSERT an `artist_invites` row (source='waitlist_conversion', status='pending', invite_token=generateApprovalToken(), expiry, invited_by_user_id=actor, email from the waitlist row); stamp `artist_waitlist.converted_to_invite_at = NOW()`; send `artistSpotOpenedEmail({ actionLink })` (best-effort) — send it EVEN IF the row is unsubscribed (D-19: personal invites always reach); `logStaffAction(action:'artist_invite.convert', targetType:'artist_waitlist', targetId:id)`. Guard idempotency (an already-converted row does not create a second invite). Write `convert/route.test.ts` covering: convert creates invite + stamps converted_to_invite_at + sends B + audits; unsubscribed row still sends; unknown id → 404; non-staff → 403.
  </action>
  <verify>
    <automated>npm test -- 'app/api/admin/artist-invites/[id]/convert/route.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- 'app/api/admin/artist-invites/[id]/convert/route.test.ts'` passes.
    - Convert works for any staff role and sends template B even when the row is unsubscribed (D-19).
    - converted_to_invite_at is stamped; a re-convert does not duplicate the invite.
    - logStaffAction is called.
  </acceptance_criteria>
  <done>The convert route issues a personal invite (any staff, unsubscribe-agnostic), sends B, and audits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: POST /api/admin/artist-invites/broadcast — Leadership-only idempotent reopen</name>
  <files>app/api/admin/artist-invites/broadcast/route.ts, app/api/admin/artist-invites/broadcast/route.test.ts</files>
  <read_first>
    - lib/admin/gate.ts (requireStaff(['leadership']) — the leadership-only variant / verifyAdmin)
    - lib/email/artistReopened.ts (template C + its unsubscribe link from 27-05)
    - app/api/sync/register/route.ts (routeLead best-effort side-effect pattern, scaled to a loop)
    - .planning/phases/27-artist-invite-only-onboarding/27-RESEARCH.md (Pitfall 6 idempotency)
  </read_first>
  <behavior>
    - POST (Leadership ONLY) → select artist_waitlist WHERE unsubscribed_at IS NULL AND notified_reopen_at IS NULL; for each, send artistReopened email C (with that row's unsubscribe link built from unsubscribe_token) and stamp notified_reopen_at; return { sent: N }.
    - Non-leadership staff (AE/BD) → 403; unauthenticated → 401.
    - Idempotent: a second call sends 0 (all rows already stamped) — no double-send (Pitfall 6).
    - Opted-out rows are excluded from the broadcast (D-19) — but their personal-invite path is untouched.
    - logStaffAction('artist_invite.broadcast', changes:{ count }).
  </behavior>
  <action>
    Create `app/api/admin/artist-invites/broadcast/route.ts` — POST calling `requireStaff(['leadership'])` (D-15 — reopen broadcast is Leadership-only, unlike convert/create); non-leadership → 403. `createServiceClient()`; select the eligible rows (`unsubscribed_at IS NULL AND notified_reopen_at IS NULL`). Loop, building each recipient's unsubscribe link `${NEXT_PUBLIC_APP_URL}/unsubscribe?token=${row.unsubscribe_token}`, sending `artistReopenedEmail({ actionLink, unsubscribeLink })` best-effort (a single send failure must not abort the batch), and stamping `notified_reopen_at = NOW()` for that row so a retry/double-click never re-sends (Pitfall 6 — accept at-most-once). Return `{ sent }`. `logStaffAction(action:'artist_invite.broadcast', targetType:'artist_waitlist', changes:{ count: sent })`. Write `broadcast/route.test.ts` covering: leadership triggers send to eligible rows only; opted-out + already-notified rows excluded; second call sends 0 (idempotent); AE/BD → 403.
  </action>
  <verify>
    <automated>npm test -- app/api/admin/artist-invites/broadcast/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- app/api/admin/artist-invites/broadcast/route.test.ts` passes.
    - Only requireStaff(['leadership']) passes; AE/BD get 403 (D-15).
    - The query excludes unsubscribed_at IS NOT NULL and notified_reopen_at IS NOT NULL rows.
    - A second broadcast sends 0 (idempotency guard); logStaffAction records the count.
  </acceptance_criteria>
  <done>The reopen broadcast is Leadership-only, opt-out-aware, idempotent, and audited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff session → admin invite routes | Privilege split: any-staff vs Leadership-only |
| admin route → artist_invites/artist_waitlist | Service-role writes to zero-RLS tables |
| bulk send → many recipients | One-shot mass email, high blast radius |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-06 | Elevation of Privilege | reopen broadcast (any-staff vs leadership) | high | mitigate | broadcast uses requireStaff(['leadership']); create/convert use requireStaff() (any staff) per D-06/D-15; all after-gate before service client |
| T-27-14 | Denial of Service / Reputation | reopen broadcast double-send | high | mitigate | notified_reopen_at idempotency guard; per-row stamp; at-most-once semantics (Pitfall 6) |
| T-27-15 | Repudiation | untracked staff invite/broadcast actions | medium | mitigate | logStaffAction on every create/convert/broadcast (staff audit precedent) |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed |
</threat_model>

<verification>
- `npm test -- app/api/admin/artist-invites/route.test.ts 'app/api/admin/artist-invites/[id]/convert/route.test.ts' app/api/admin/artist-invites/broadcast/route.test.ts` green.
- `npm run build` / `tsc --noEmit` clean.
- Live broadcast is additionally gated by the BD/counsel + owner-copy launch checkpoints in 27-11.
</verification>

<success_criteria>
The three Team Console routes exist with the correct any-staff vs Leadership-only privilege split, tokened invites, the three branded emails wired, idempotent broadcast, and full staff auditing.
</success_criteria>

<output>
Create `.planning/phases/27-artist-invite-only-onboarding/27-08-SUMMARY.md` when done.
</output>
