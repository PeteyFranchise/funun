---
phase: 27-artist-invite-only-onboarding
plan: 05
type: execute
wave: 2
depends_on: ["27-02"]
files_modified:
  - lib/email/artistInvite.ts
  - lib/email/artistSpotOpened.ts
  - lib/email/artistReopened.ts
  - lib/email/artist-emails.test.ts
autonomous: true
requirements: [INVITE-10]
must_haves:
  truths:
    - "Three distinct Funūn-branded transactional email templates exist (invite / spot-opened / reopened), each returning both an HTML body and a plain-text fallback (D-17, UI-SPEC surface 6)."
    - "Every interpolated user-supplied value (inviter name, waitlist name) is HTML-escaped via the shared esc() (no re-derived escaper)."
    - "Each template's CTA uses the brand gradient background, on a light email background (deliverability)."
  artifacts:
    - lib/email/artistInvite.ts
    - lib/email/artistSpotOpened.ts
    - lib/email/artistReopened.ts
  key_links:
    - "All three import esc from @/lib/email/esc (27-02) — the single escape helper."
    - "Templates return the { subject, html, text } shape sendEmail() consumes (lib/email/index.ts)."
---

<objective>
Build the three branded transactional email templates D-17 requires: the founding-member invite (A), the waitlist spot-opened notice (B), and the reopen broadcast (C). All three raise the bar above the minimal `industryInvite`/`staffInvite` style — HTML + plain-text, gradient CTA, founding-member positioning.

Purpose: The Team Console routes (27-08) send A/B/C; final copy needs owner sign-off (a launch gate tracked in 27-11), so this plan delivers the structural templates.
Output: `artistInvite.ts`, `artistSpotOpened.ts`, `artistReopened.ts` + a test asserting subject contracts, dual-body presence, and esc usage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-artist-invite-only-onboarding/27-CONTEXT.md
@.planning/phases/27-artist-invite-only-onboarding/27-UI-SPEC.md
@.planning/phases/27-artist-invite-only-onboarding/27-PATTERNS.md
@lib/email/industryInvite.ts
@lib/email/index.ts
@app/api/collaborators/[id]/invite/route.ts
</context>

<artifacts>
This plan produces:
- `lib/email/artistInvite.ts` — `artistInviteEmail({ inviterName?, actionLink })` → `{ subject, html, text }` (template A, D-17)
- `lib/email/artistSpotOpened.ts` — `artistSpotOpenedEmail({ actionLink })` → `{ subject, html, text }` (template B)
- `lib/email/artistReopened.ts` — `artistReopenedEmail({ actionLink, unsubscribeLink })` → `{ subject, html, text }` (template C)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: artistInvite.ts — founding-member invite (template A) + spot-opened (template B)</name>
  <files>lib/email/artistInvite.ts, lib/email/artistSpotOpened.ts</files>
  <read_first>
    - lib/email/industryInvite.ts (the { subject, html } export shape to extend with a `text` field)
    - app/api/collaborators/[id]/invite/route.ts (the HTML+text dual-body sendEmail call shape to copy, lines 83-127)
    - .planning/phases/27-artist-invite-only-onboarding/27-UI-SPEC.md (copywriting rows: "Branded email — Invite" and "Branded email — Spot opened"; surface 6 structural contract)
    - lib/email/esc.ts (the shared escape helper from 27-02)
  </read_first>
  <action>
    Create `lib/email/artistInvite.ts` exporting `artistInviteEmail(args: { inviterName?: string | null; actionLink: string }): { subject: string; html: string; text: string }`. Subject per UI-SPEC contract: founding-member framing ("You're invited to Funūn"), NOT the minimal industryInvite tone. HTML body: light background (near-white), small Funūn wordmark header in dark ink (matching AuthLayout wordmark weight), founding-cohort story leading before the CTA, "who invited you" line when inviterName is present, and a CTA button whose background is the brand gradient `linear-gradient(105deg,#818CF8 0%,#D946EF 100%)` linking to actionLink. Plain-text fallback covers the same content. Escape inviterName and actionLink via `esc` from `@/lib/email/esc`. Create `lib/email/artistSpotOpened.ts` exporting `artistSpotOpenedEmail(args: { actionLink: string }): { subject, html, text }` — subject "Your spot on Funūn just opened", must reference that they were on the waiting list (D-13a per-person conversion), same light-bg + gradient-CTA + dual-body structure. Named exports, no semicolons.
  </action>
  <verify>
    <automated>npm test -- lib/email/artist-emails.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both functions return an object with non-empty `subject`, `html`, and `text`.
    - artistInviteEmail includes the inviter name when provided and omits that line cleanly when absent.
    - Both use `esc` from `@/lib/email/esc` (no local escaper).
    - CTA background uses the two-stop brand gradient; email background is light.
  </acceptance_criteria>
  <done>Templates A and B exist with dual bodies, gradient CTA, and shared escaping.</done>
</task>

<task type="auto">
  <name>Task 2: artistReopened.ts — reopen broadcast (template C) + tests</name>
  <files>lib/email/artistReopened.ts, lib/email/artist-emails.test.ts</files>
  <read_first>
    - lib/email/staffInvite.ts (template shape analog)
    - .planning/phases/27-artist-invite-only-onboarding/27-UI-SPEC.md (copywriting row "Branded email — We've reopened" + D-19 unsubscribe scope)
    - lib/email/esc.ts (shared escape helper)
  </read_first>
  <action>
    Create `lib/email/artistReopened.ts` exporting `artistReopenedEmail(args: { actionLink: string; unsubscribeLink: string }): { subject, html, text }` — subject "Funūn has reopened invites"; broadcast tone (must not read as personally singled-out, D-13b) while keeping founding-member warmth. Because this is the COMMERCIAL broadcast (D-19/RESEARCH A4), it MUST carry a visible unsubscribe link (both HTML and text) pointing to unsubscribeLink; the personal templates (A/B) do NOT (they are transactional). Same light-bg + gradient-CTA + dual-body structure, esc-escaped interpolation. Then write `lib/email/artist-emails.test.ts` covering all three templates: subject-contract strings present; `text` field non-empty for each; artistReopenedEmail contains the unsubscribeLink in BOTH html and text while artistInviteEmail/artistSpotOpenedEmail do NOT contain an unsubscribe link; an inviterName containing HTML is escaped in artistInviteEmail's output.
  </action>
  <verify>
    <automated>npm test -- lib/email/artist-emails.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/email/artist-emails.test.ts` passes for all three templates.
    - artistReopenedEmail includes the unsubscribe link in html AND text; the two personal templates do not.
    - HTML-injecting interpolated values are escaped.
  </acceptance_criteria>
  <done>Template C exists with a mandatory unsubscribe link; all three templates are tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied value → email HTML | inviter name / waitlist name interpolated into rendered email |
| commercial broadcast → recipient | CAN-SPAM-applicable bulk mail must carry unsubscribe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-08 | Tampering | HTML injection via interpolated email values | medium | mitigate | Shared esc() escapes all interpolated values in every template |
| T-27-12 | Repudiation | Commercial broadcast without unsubscribe (CAN-SPAM) | medium | mitigate | Reopen template C carries a mandatory unsubscribe link (html+text); framing flagged for BD/counsel in 27-11 (RESEARCH A4) |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed |
</threat_model>

<verification>
- `npm test -- lib/email/artist-emails.test.ts` green.
- `npm run build` / `tsc --noEmit` clean.
- Final copy remains subject to owner sign-off (launch gate in 27-11) — structural contract only here.
</verification>

<success_criteria>
Three branded, dual-body email templates exist, share the escaper, and correctly split transactional (no unsubscribe) vs commercial (unsubscribe required).
</success_criteria>

<output>
Create `.planning/phases/27-artist-invite-only-onboarding/27-05-SUMMARY.md` when done.
</output>
