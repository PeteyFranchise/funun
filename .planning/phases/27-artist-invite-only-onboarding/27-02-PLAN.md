---
phase: 27-artist-invite-only-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/security/rate-limit.ts
  - lib/security/rate-limit.test.ts
  - lib/security/turnstile.ts
  - lib/security/turnstile.test.ts
  - lib/email/esc.ts
  - lib/email/esc.test.ts
  - app/api/sync/register/route.ts
  - lib/email/industryInvite.ts
  - lib/email/staffInvite.ts
autonomous: true
requirements: [INVITE-07, INVITE-10]
user_setup:
  - service: cloudflare-turnstile
    why: "D-12 waitlist captcha — verifyTurnstileToken() reads the secret at runtime"
    env_vars:
      - name: TURNSTILE_SECRET_KEY
        source: "Cloudflare Dashboard -> Turnstile -> your widget -> Secret key (server-only; provisioned in 27-11 checkpoint)"
      - name: NEXT_PUBLIC_TURNSTILE_SITE_KEY
        source: "Cloudflare Dashboard -> Turnstile -> your widget -> Site key (public; rendered in the widget)"
must_haves:
  truths:
    - "A single shared rate limiter backs every public route this phase adds — there is no 4th/5th copy-pasted limiter (RESEARCH Don't Hand-Roll)."
    - "Turnstile tokens are verified server-side and fail closed on any verification error (RESEARCH Pitfall 7)."
    - "One shared HTML-escape helper serves all invite emails; the two pre-existing templates import it instead of duplicating esc()."
  artifacts:
    - lib/security/rate-limit.ts
    - lib/security/turnstile.ts
    - lib/email/esc.ts
  key_links:
    - "app/api/sync/register/route.ts imports the extracted limiter so behavior/constants stay identical across all public routes (no drift)."
    - "TURNSTILE_SECRET_KEY is read only inside lib/security/turnstile.ts, matching how RESEND_API_KEY is read only inside lib/email/index.ts."
---

<objective>
Extract and build the three shared primitives every later plan reuses: an in-memory rate limiter, a Turnstile captcha verifier, and an HTML-escape helper. This kills the "copy-paste drift" risk RESEARCH flags as this phase's single biggest reuse hazard.

Purpose: check-invite, waitlist, and resubscribe routes (Wave 3) all need the limiter; the waitlist route needs Turnstile; all three branded emails (27-05) need esc().
Output: `lib/security/rate-limit.ts`, `lib/security/turnstile.ts`, `lib/email/esc.ts` (+ tests), with the existing sync/register route and the two existing email templates refactored to import the shared versions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-artist-invite-only-onboarding/27-RESEARCH.md
@.planning/phases/27-artist-invite-only-onboarding/27-PATTERNS.md
</context>

<artifacts>
This plan produces:
- `lib/security/rate-limit.ts` — `createRateLimiter({ windowMs, maxAttempts })` returning `{ isRateLimited }`, plus `getClientIp(request)`
- `lib/security/turnstile.ts` — `verifyTurnstileToken(token, remoteIp?)`
- `lib/email/esc.ts` — `esc(s)` HTML-escape helper
- Refactors (behavior-preserving): `app/api/sync/register/route.ts`, `lib/email/industryInvite.ts`, `lib/email/staffInvite.ts` now import the shared helpers
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared rate limiter into lib/security/rate-limit.ts</name>
  <files>lib/security/rate-limit.ts, lib/security/rate-limit.test.ts, app/api/sync/register/route.ts</files>
  <read_first>
    - app/api/sync/register/route.ts (lines 28-54 — the in-route limiter + getClientIp to lift verbatim; 27-PATTERNS exact analog)
  </read_first>
  <behavior>
    - createRateLimiter({ windowMs, maxAttempts }) returns an object whose isRateLimited(key) is false for the first `maxAttempts` calls in a window and true on the (maxAttempts+1)th within windowMs.
    - Each limiter instance owns its own Map (so check-invite/waitlist/resubscribe get independent buckets).
    - getClientIp(request) returns the first x-forwarded-for entry, then x-real-ip, then 'unknown'.
    - Default export/config preserves sync/register's existing constants (15-minute window, 5 attempts) so its behavior is byte-for-byte unchanged after refactor.
  </behavior>
  <action>
    Create `lib/security/rate-limit.ts` exporting a `createRateLimiter({ windowMs, maxAttempts })` factory (each call owns a fresh `Map<string, number[]>`) with the sliding-window `isRateLimited(key)` logic lifted verbatim from sync/register (filter timestamps within window, cap at maxAttempts, push+store). Also export `getClientIp(request: Request)` identical to the existing implementation, and export the shared constants `RATE_LIMIT_WINDOW_MS` (15*60*1000) and `RATE_LIMIT_MAX_ATTEMPTS` (5) as the defaults. Then refactor `app/api/sync/register/route.ts` to delete its local limiter/getClientIp and import from `@/lib/security/rate-limit`, instantiating one limiter with the same constants — the route's observable behavior must not change. Write `lib/security/rate-limit.test.ts` proving the threshold boundary (allows N, blocks N+1) and getClientIp header precedence.
  </action>
  <verify>
    <automated>npm test -- lib/security/rate-limit.test.ts && ! grep -q 'rateLimitStore' app/api/sync/register/route.ts && grep -q "@/lib/security/rate-limit" app/api/sync/register/route.ts && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/security/rate-limit.test.ts` passes with a boundary test (allows maxAttempts, blocks the next).
    - sync/register no longer declares its own limiter Map; it imports from `@/lib/security/rate-limit`.
    - `npm run build` clean — sync/register behavior unchanged (same window/max constants).
  </acceptance_criteria>
  <done>Shared limiter exists, is tested, and sync/register consumes it with identical behavior.</done>
</task>

<task type="auto">
  <name>Task 2: lib/security/turnstile.ts — fail-closed server-side captcha verification</name>
  <files>lib/security/turnstile.ts, lib/security/turnstile.test.ts</files>
  <read_first>
    - lib/email/index.ts (env-var discipline — how RESEND_API_KEY is read only inside the module, the pattern to mirror for the secret key)
    - .planning/phases/27-artist-invite-only-onboarding/27-PATTERNS.md (the verified Cloudflare siteverify pattern, lines ~200-217)
  </read_first>
  <action>
    Create `lib/security/turnstile.ts` exporting `verifyTurnstileToken(token: string, remoteIp?: string): Promise<boolean>` using the RESEARCH-verified Cloudflare pattern: read `process.env.TURNSTILE_SECRET_KEY` INSIDE the function (never at module top-level, never exposed to the client); return false immediately if the secret or token is missing; POST `secret`+`response`(+`remoteip`) as `application/x-www-form-urlencoded` to `https://challenges.cloudflare.com/turnstile/v0/siteverify`; return `data.success === true`; wrap in try/catch and return false on any error (fail closed — a Cloudflare outage must not open the waitlist to abuse). The secret key must remain server-only and must never be given the `NEXT_PUBLIC_` prefix (only the site key is public). Write `lib/security/turnstile.test.ts` mocking global fetch to assert: success token → true; missing secret → false; fetch rejection → false (fail-closed).
  </action>
  <verify>
    <automated>npm test -- lib/security/turnstile.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/security/turnstile.test.ts` passes covering success, missing-secret, and fetch-error (fail-closed) cases.
    - The secret is read only inside the function body; no `NEXT_PUBLIC_`-prefixed secret variable is introduced anywhere.
    - Verification failure defaults to `false`, never `true`.
  </acceptance_criteria>
  <done>Turnstile verifier exists, fails closed, and is tested with a mocked fetch.</done>
</task>

<task type="auto">
  <name>Task 3: Extract lib/email/esc.ts and repoint the two existing templates</name>
  <files>lib/email/esc.ts, lib/email/esc.test.ts, lib/email/industryInvite.ts, lib/email/staffInvite.ts</files>
  <read_first>
    - lib/email/industryInvite.ts (the duplicated esc(), lines 12-18)
    - lib/email/staffInvite.ts (the second copy of esc() to remove)
  </read_first>
  <action>
    Create `lib/email/esc.ts` exporting `esc(s: string): string` with the exact escape sequence already used (&, <, >, "). Refactor `lib/email/industryInvite.ts` and `lib/email/staffInvite.ts` to delete their local `esc` and import `{ esc }` from `@/lib/email/esc` — their rendered output must be byte-for-byte identical (RESEARCH: do not let a 5th copy drift; the three new templates in 27-05 will import this same module). Write `lib/email/esc.test.ts` asserting each of the four characters is escaped and that already-safe input is unchanged.
  </action>
  <verify>
    <automated>npm test -- lib/email/esc.test.ts && ! grep -rq 'function esc' lib/email/industryInvite.ts lib/email/staffInvite.ts && grep -q "@/lib/email/esc" lib/email/industryInvite.ts && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/email/esc.test.ts` passes.
    - Neither industryInvite.ts nor staffInvite.ts declares a local `esc` function anymore; both import from `@/lib/email/esc`.
    - `npm run build` clean; existing email output unchanged.
  </acceptance_criteria>
  <done>Shared esc() exists, is tested, and both pre-existing templates consume it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → public route | Rate limiter is the first line against abuse |
| server → Cloudflare siteverify | Captcha verification round-trip; must fail closed |
| user-supplied string → email HTML | Injection surface for interpolated values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-04 | Denial of Service | shared rate limiter backing public routes | high | mitigate | Sliding-window per-key limiter (ip + email dimensions) reused by every public route; blocks spam/scraping |
| T-27-07 | Information Disclosure | TURNSTILE_SECRET_KEY exposure | high | mitigate | Secret read only server-side inside turnstile.ts; only the site key is public (no NEXT_PUBLIC secret) |
| T-27-08 | Tampering | HTML injection via interpolated email values | medium | mitigate | Single shared esc() escapes &/</>/" before interpolation in every template |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed (Turnstile is script-tag + fetch); RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `npm test -- lib/security/rate-limit.test.ts lib/security/turnstile.test.ts lib/email/esc.test.ts` green.
- `npm run build` and `tsc --noEmit` clean.
- sync/register + both existing email templates still compile and behave identically.
</verification>

<success_criteria>
Three shared primitives exist and are tested; the one pre-existing rate limiter and the two pre-existing esc() copies now route through the shared modules (no proliferation).
</success_criteria>

<output>
Create `.planning/phases/27-artist-invite-only-onboarding/27-02-SUMMARY.md` when done.
</output>
