---
phase: 27-artist-invite-only-onboarding
plan: 11
type: execute
wave: 5
depends_on: ["27-04", "27-06", "27-07", "27-09", "27-10"]
files_modified: []
autonomous: false
requirements: [INVITE-07, INVITE-10, INVITE-11]
user_setup:
  - service: cloudflare-turnstile
    why: "D-12 waitlist captcha — the widget cannot function until keys exist"
    env_vars:
      - name: NEXT_PUBLIC_TURNSTILE_SITE_KEY
        source: "Cloudflare Dashboard -> Turnstile -> Add widget -> Site key"
      - name: TURNSTILE_SECRET
        source: "Cloudflare Dashboard -> Turnstile -> Add widget -> Secret key (server-only)"
    dashboard_config:
      - task: "Create a Turnstile widget for the Funūn domain(s)"
        location: "Cloudflare Dashboard -> Turnstile"
  - service: supabase
    why: "Apply migrations 097 + 098 to the live database (the gate is a DB trigger)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (for non-TTY push)"
must_haves:
  truths:
    - "The owner is NOT locked out: an owner artist account (or an owner_seed artist_invites row) exists BEFORE migration 098 is applied (D-18, RESEARCH Pitfall 2, INVITE-11)."
    - "Migrations 097 + 098 are applied to the live DB and confirmed via migration list (schema-push convention)."
    - "A live smoke proves: an uninvited email is rejected, an invited/collaborator email is admitted, and existing accounts are unaffected (INVITE-01)."
    - "Turnstile keys are provisioned so the waitlist captcha works end-to-end (INVITE-07)."
    - "Owner sign-off on the three email templates' final copy is recorded before broadcast/invite emails ship (D-17 launch gate, INVITE-10); the transactional-vs-commercial framing is flagged for BD/counsel (RESEARCH A4)."
  artifacts:
    - "Live migrations 097 + 098 applied (no new files — this plan operates on already-written migrations)"
  key_links:
    - "The bootstrap checkpoint MUST complete before the db push (self-lockout prevention); the push MUST complete before /gsd-verify-work (avoids false-positive verification since build passes without the push)."
---

<objective>
Close the phase safely: provision the Turnstile keys, confirm the owner cannot be locked out, apply migrations 097 + 098 to the live DB (the [BLOCKING] push), run a live gate smoke, and record the D-17 owner-copy sign-off + BD/counsel email-framing flag. This is the launch-gate plan — no source files, all checkpoints + the blocking push.

Purpose: The gate is a live DB trigger; build/type checks pass WITHOUT the push (types come from config), so this plan is mandatory to avoid false-positive verification and owner self-lockout.
Output: live migrations applied, smoke-tested, and launch sign-offs recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-artist-invite-only-onboarding/27-CONTEXT.md
@.planning/phases/27-artist-invite-only-onboarding/27-RESEARCH.md
@.planning/phases/27-artist-invite-only-onboarding/27-VALIDATION.md
@.planning/phases/27-artist-invite-only-onboarding/27-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Bootstrap — confirm the owner is not self-locked-out (before the gate push)</name>
  <what-built>The gate migration 098 (from 27-04) will reject any signup whose email is neither a collaborator nor a pending artist_invite. If the owner's artist account does not already exist, applying 098 could lock the owner out of their own product (RESEARCH Pitfall 2 / D-18).</what-built>
  <action>Confirm the owner's artist account exists (or seed an owner_seed artist_invites row) BEFORE the gate migration is pushed. This checkpoint gates Task 3 (the push) — do not proceed until the owner is provably safe (D-18, RESEARCH Pitfall 2).</action>
  <how-to-verify>
    1. Confirm whether the owner already has an artist `user_profiles` row (owner: check via the Supabase dashboard / an existing signed-in session, or a service-role query on user_profiles for the owner's email).
    2. If YES → the owner is safe; proceed (existing accounts are never gated, D-03).
    3. If NO → seed one `artist_invites` row for the owner's email BEFORE pushing 098: either add an `INSERT INTO public.artist_invites (email, status, source)` for the owner's email with source='owner_seed' to migration 097/098, or insert it via a one-off service-role statement, so the owner's first signup self-admits.
    4. Confirm the founding-cohort seeding plan (owner invites the founding artists via the real invite mechanism after the gate is live, D-18 — no open soft-launch window).
  </how-to-verify>
  <resume-signal>Type "owner-safe" (account exists) or "seeded" (owner_seed row added), then approve to proceed to the push.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Provision Cloudflare Turnstile keys (operator)</name>
  <what-built>The waitlist form (27-09) renders the Turnstile widget and the /api/waitlist route (27-07) verifies the token server-side. Both need keys that do not yet exist in this project (RESEARCH Environment Availability — hard blocker for the captcha).</what-built>
  <action>Provision a Cloudflare Turnstile widget and set NEXT_PUBLIC_TURNSTILE_SITE_KEY (public) + TURNSTILE_SECRET (server-only) in the deployment env, then confirm the widget renders and a real waitlist submit verifies server-side (RESEARCH Pitfall 7 — only the site key is public).</action>
  <how-to-verify>
    1. In the Cloudflare dashboard, create a Turnstile widget for the Funūn domain(s).
    2. Set NEXT_PUBLIC_TURNSTILE_SITE_KEY (public) and TURNSTILE_SECRET (server-only) in the deployment environment (and .env.local for local testing).
    3. Confirm the widget renders on /signup's denied→waitlist state and that a real submission passes server-side siteverify (a genuine waitlist submit succeeds; a missing/blank captcha is rejected).
    4. Confirm no secret variable carries the NEXT_PUBLIC_ prefix (only the site key is public — RESEARCH Pitfall 7).
  </how-to-verify>
  <resume-signal>Type "turnstile-live" once the widget renders and a real waitlist submit verifies server-side.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Apply migrations 097 + 098 (supabase db push) + live gate smoke</name>
  <what-built>Migrations 097 (tables + email_has_account) and 098 (handle_new_user gate) are written but not applied. The gate is a live DB trigger — build/type checks pass without the push, so this must be applied and smoke-tested before verification (project schema-push convention; every prior schema phase gated the push to a human).</what-built>
  <action>After Task 1 (bootstrap) is confirmed, apply migrations 097 + 098 via `supabase db push` (non-TTY: set SUPABASE_ACCESS_TOKEN), confirm LOCAL=REMOTE through 098 via `supabase migration list`, and run the live gate smoke (uninvited rejected, invited admitted, existing accounts + other lanes unaffected). This is the [BLOCKING] push — human-run, never from an agent.</action>
  <how-to-verify>
    1. Ensure Task 1 (bootstrap) is complete — the owner is safe BEFORE this push.
    2. Apply the migrations to the live DB via `supabase db push` (non-TTY: set SUPABASE_ACCESS_TOKEN). Per project convention this is human-run, never from an agent.
    3. Confirm LOCAL=REMOTE for 001–098 via `supabase migration list` (or `npx supabase migration list`).
    4. Reload the PostgREST schema cache if needed; confirm a service-role read of artist_invites/artist_waitlist returns 200.
    5. Live gate smoke (INVITE-01): (a) an UNINVITED test email at /signup is rejected server-side (no user_profiles row created); (b) an INVITED email (a collaborator email or an issued artist_invites row) is admitted and completes self-serve signup; (c) an existing account is unaffected (can still sign in); (d) a curator/industry/buyer provisioning path still works (regression — the gate is artist-branch-only, RESEARCH Pitfall 1).
  </how-to-verify>
  <resume-signal>Type "pushed-and-smoked" with the migration-list parity result + the four smoke outcomes.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Owner email-copy sign-off + BD/counsel framing flag (D-17 launch gate)</name>
  <what-built>The three branded templates (27-05) carry founding-member copy that D-17 requires the owner to sign off before ship, and the reopen broadcast's transactional-vs-commercial framing (D-19) is flagged for BD/counsel (RESEARCH A4 — not counsel-reviewed).</what-built>
  <action>Have the owner review + approve (or edit) the final copy for the three branded emails (D-17 launch gate), confirm C carries an unsubscribe link and A/B do not (D-19), and record the BD/counsel status for the CAN-SPAM framing before the reopen broadcast is enabled in production.</action>
  <how-to-verify>
    1. Owner reviews the final subject + body copy for artistInvite (A), artistSpotOpened (B), artistReopened (C) and approves or edits.
    2. Confirm template C (broadcast) carries a working unsubscribe link and A/B (personal) do not (D-19).
    3. Record the BD/counsel review status for the CAN-SPAM transactional-vs-commercial characterization (do NOT enable the reopen broadcast in production until this is acknowledged — 27-VALIDATION manual gate).
  </how-to-verify>
  <resume-signal>Type "copy-approved" (with any edits applied) and note the BD/counsel status.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB | Applying the gate trigger to production |
| owner account → gate | Self-lockout risk on flip |
| commercial broadcast → recipients | CAN-SPAM compliance gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-17 | Denial of Service | owner self-lockout on gate flip | high | mitigate | Blocking bootstrap checkpoint confirms an owner account / owner_seed row exists BEFORE the push (RESEARCH Pitfall 2, D-18) |
| T-27-04 | Denial of Service | waitlist unprotected without captcha keys | high | mitigate | Turnstile keys provisioned + verified before the waitlist goes live (blocking checkpoint) |
| T-27-12 | Repudiation | commercial broadcast without counsel review | medium | mitigate | BD/counsel framing flag recorded; broadcast not enabled in prod until acknowledged (RESEARCH A4) |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Full suite `npm test` green (all prior waves) before this plan's checkpoints.
- `supabase migration list` shows LOCAL=REMOTE through 098.
- Live gate smoke: uninvited rejected, invited admitted, existing accounts + other lanes unaffected.
- Turnstile widget verifies server-side; owner copy sign-off + BD/counsel flag recorded.
</verification>

<success_criteria>
Migrations 097 + 098 are live and smoke-verified with no owner self-lockout; Turnstile keys work; the D-17 owner-copy sign-off and BD/counsel framing flag are recorded. The phase is safe to mark shipped.
</success_criteria>

<output>
Create `.planning/phases/27-artist-invite-only-onboarding/27-11-SUMMARY.md` when done.
</output>
