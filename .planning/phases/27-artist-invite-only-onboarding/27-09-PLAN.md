---
phase: 27-artist-invite-only-onboarding
plan: 09
type: execute
wave: 4
depends_on: ["27-06", "27-07"]
files_modified:
  - app/(auth)/signup/page.tsx
  - app/unsubscribe/page.tsx
autonomous: true
requirements: [INVITE-06, INVITE-12]
must_haves:
  truths:
    - "The signup page LEADS with the invite-only gate ('Have an invite? Enter your email') before the credential form (D-10, INVITE-06)."
    - "The client runs gate → checking → (allowed | existing-account | denied→waitlist) states in place, no redirect (D-11)."
    - "A deep-link (?invite=token) pre-fills the email + shows 'invited by [name]'; editing the email silently falls back to the generic gate (D-09)."
    - "The unsubscribe page states the broadcast-only scope + reassurance box and offers Resubscribe + rejoin-the-waitlist (D-19, INVITE-12)."
    - "The credential-creation step is the UNCHANGED self-serve signUp() — the artist sets their own password (D-01)."
  artifacts:
    - app/(auth)/signup/page.tsx
    - app/unsubscribe/page.tsx
  key_links:
    - "The page calls POST /api/signup/check-invite (27-06) to branch states and GET /api/signup/invite/[token] (27-06) for deep-link pre-fill; the denial state posts to POST /api/waitlist (27-07)."
    - "The waitlist form renders the Turnstile widget (NEXT_PUBLIC_TURNSTILE_SITE_KEY) in a fixed-height slot above submit; the unsubscribe page posts to /api/waitlist/resubscribe (27-07)."
---

<objective>
Build the public-facing UX: the invite-gated signup state machine (surfaces 1–3) and the unsubscribe/resubscribe landing page (surface 7). This is the founding-member front door (D-10/D-11/D-16) and the D-19 opt-out surface. The real gate stays server-side (27-04); this is the friendly layer.

Purpose: Turns the routes from 27-06/27-07 into the artist onboarding experience.
Output: modified `app/(auth)/signup/page.tsx` (state machine + waitlist + captcha + deep-link) and new `app/unsubscribe/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-artist-invite-only-onboarding/27-CONTEXT.md
@.planning/phases/27-artist-invite-only-onboarding/27-UI-SPEC.md
@.planning/phases/27-artist-invite-only-onboarding/27-PATTERNS.md
@app/(auth)/signup/page.tsx
@.planning/phases/27-artist-invite-only-onboarding/27-06-SUMMARY.md
@.planning/phases/27-artist-invite-only-onboarding/27-07-SUMMARY.md
</context>

<artifacts>
This plan produces:
- Modified `app/(auth)/signup/page.tsx` — invite-gate state machine + inline denial/waitlist + captcha + deep-link pre-fill
- New `app/unsubscribe/page.tsx` — broadcast-only-scope unsubscribe/resubscribe landing
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Signup page — invite-gate state machine + inline waitlist + deep-link</name>
  <files>app/(auth)/signup/page.tsx</files>
  <read_first>
    - app/(auth)/signup/page.tsx (the baseline to extend — inputClass line 7, card shell line 58, error block lines 96-98, primary button lines 101-107, existing signUp form lines 62-93; the signUp call lines 24-28 stays unchanged)
    - .planning/phases/27-artist-invite-only-onboarding/27-UI-SPEC.md (surfaces 1/2/3 + the full copywriting contract; Color: pre-auth stays white/black, NO gradient)
    - components/collaborators/CollaboratorForm.tsx (the resize-none textarea treatment for the waitlist note)
  </read_first>
  <action>
    Extend `app/(auth)/signup/page.tsx` into a client state machine: `gate → checking → (allowed | existing-account | denied)`. Default `gate` state: founding-member heading ("Funūn is invite-only — for now.") + single email input + "Check my invite" button (reuse inputClass, the `rounded-xl border border-white/10 bg-white/[0.03] p-6` card, and the white `bg-white text-black` button — NO gradient on this pre-auth surface per UI-SPEC Color). On submit, POST to `/api/signup/check-invite` (27-06); button label swaps to "Checking…" (disabled, no layout shift). Branch on the response: `allowed` → render the EXISTING unmodified credential form (email prefilled, password) with a "You're invited ✓" line above the "Create your account" heading; the existing `supabase.auth.signUp()` call stays UNCHANGED (D-01/D-02 — server is the real gate); `existingAccount` true → show "You already have an account — sign in instead" with a `/signin?email=…` link; `denied` → replace the card contents IN PLACE (no redirect, D-11) with the generic respectful denial copy (identical for mistyped vs never-invited — enumeration mitigation) + the waitlist form. Waitlist form: email (prefilled from the denied attempt), name, optional note (resize-none textarea), a Turnstile widget in a fixed-height slot above the submit (render the script + `data-sitekey` from `process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY`), and a "Join the waiting list" white button; POST {email,name,note,turnstileToken} to `/api/waitlist` (27-07); on success replace the form in place with "You're on the list — we'll email you the moment a spot opens." Deep-link (surface 2): on mount, read `?invite=token` from the URL; if present, GET `/api/signup/invite/[token]`, pre-fill the email (editable), render the "[Inviter name] invited you to Funūn" header + "This invite was sent to {email}." helper, and proceed as if the email was checked (still round-trips through check-invite — never a client-side skip of the server gate); if the visitor edits the email, silently fall back to the generic gate (no error). Expired token → render the re-request copy. Keep copy aligned to UI-SPEC (owner owns final wording).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The page leads with the invite gate before any credential form (D-10).
    - Allowed → the unmodified self-serve signUp() form (artist sets own password, D-01); existing-account → sign-in link; denied → inline waitlist (no redirect, D-11).
    - Denial copy is generic regardless of reason (enumeration mitigation).
    - Deep-link pre-fills + frames "invited by [name]"; editing the email falls back to the gate; still round-trips through check-invite (no client-side admission).
    - The waitlist form renders the Turnstile widget from the public site key and posts the token.
    - `npm run build` clean.
  </acceptance_criteria>
  <done>The signup page runs the full invite-gate state machine with deep-link + captcha-protected inline waitlist, self-serve credential creation unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Unsubscribe/resubscribe landing page (surface 7)</name>
  <files>app/unsubscribe/page.tsx</files>
  <read_first>
    - app/(auth)/signup/page.tsx (the dark card shell + inputClass + the `sent`-boolean state-swap pattern to reuse)
    - .planning/phases/27-artist-invite-only-onboarding/27-UI-SPEC.md (surface 7 + copywriting rows "Unsubscribe confirmation" / "Re-subscribe confirmation")
  </read_first>
  <action>
    Create `app/unsubscribe/page.tsx` — a dark auth-style centered card (reuse the `rounded-xl border border-white/10 bg-white/[0.03] p-6` treatment, NOT the light email). Read `?token=` from the URL. Landing state: heading "You've unsubscribed", body "You won't get Funūn's waiting-list broadcasts anymore.", plus a reassurance box making the broadcast-only scope explicit (a personal invite from a collaborator or the Funūn team still reaches them — D-19). Primary action "Resubscribe" (white `bg-white text-black` public-surface button) → POST `{ token }` to `/api/waitlist/resubscribe` (27-07); on success swap in place (the `sent`-style pattern) to "You're resubscribed — you're back on the list." Secondary link: "rejoin the waiting list" → `/signup` (auto-resubscribe path). Handle a missing/invalid token gracefully with the generic error block. Keep copy aligned to UI-SPEC.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The page states the broadcast-only scope + the personal-invite reassurance (D-19).
    - Resubscribe posts the token to /api/waitlist/resubscribe and swaps to the success card in place.
    - A "rejoin the waiting list" link is present; invalid token shows the generic error.
    - `npm run build` clean.
  </acceptance_criteria>
  <done>The unsubscribe/resubscribe page exists with correct broadcast-only framing and the resubscribe action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public visitor → signup page | Client UX only; the real gate is server-side (D-02) |
| public visitor → unsubscribe link | Token-scoped state toggle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Elevation of Privilege | client-side pre-check | high | mitigate | The page never admits — the unmodified signUp() still passes through the server gate (27-04); client states are UX only (D-02) |
| T-27-02 | Information Disclosure | denial copy revealing invite status | medium | mitigate | Generic identical denial regardless of reason (owner-accepted residual, D-10) |
| T-27-07 | Information Disclosure | Turnstile secret leakage via client | high | mitigate | Only the public site key is rendered client-side; the secret is verified server-side in 27-07 |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages (Turnstile via script tag) |
</threat_model>

<verification>
- `npm run build` / `tsc --noEmit` clean.
- Live end-to-end (invited admitted / uninvited → waitlist / deep-link pre-fill / unsubscribe→resubscribe) is exercised at the 27-11 live smoke after keys are provisioned + migrations pushed.
</verification>

<success_criteria>
The signup page runs the full invite-gate + inline waitlist + deep-link flow with self-serve credential creation unchanged, and the unsubscribe page delivers D-19's broadcast-only opt-out/resubscribe.
</success_criteria>

<output>
Create `.planning/phases/27-artist-invite-only-onboarding/27-09-SUMMARY.md` when done.
</output>
