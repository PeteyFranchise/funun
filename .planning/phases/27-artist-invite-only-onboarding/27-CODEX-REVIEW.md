# Phase 27 — Independent Security Review (Codex) + Dispositions

**Date:** 2026-08-09
**Reviewer:** Codex (independent, read-only). Blockers 1–3 + M1 re-verified in-code by the orchestrator.
**Verdict:** DO-NOT-SHIP — 3 blockers, 2 high, 8 medium, 3 low. Build/`tsc`/1,864 tests all passed, yet three core flows are broken (exactly why the review ran before cutover).
**Status (2026-08-09):** All 3 blockers, both highs, and mediums/lows M5/M6/L3 (plus the earlier DB-gate mediums/lows M1–M4/L1 via migration 099) are fixed at the code level. `npm test` (166 suites/1946 tests), `tsc`, `next lint`, and `next build` all clean. Migrations 097/098/099/100 are drafted but **NOT pushed** — the owner still reviews and pushes them together at this phase's blocking cutover checkpoint. L2 remains an accepted residual risk for beta.

## Blockers (all confirmed; all fixed before cutover)

- **B1 — staff provisioning rejected by the artist gate.** Migration 098 gates every account without `role`=curator/buyer/industry as an artist, but `lib/staff/createStaffAccount.ts:31` sets `app_metadata.staff_role` (not `role`) → new staff hit the gated artist branch and are rejected; also breaks break-glass Layer 2. **Fix (Fix-1):** migration 099 — `handle_new_user` staff branch/early-return (on `staff_role`) BEFORE the artist gate, replicating Phase-25 staff provisioning; update break-glass Layer 2 docs; add a staff-provisioning test.
- **B2 — unsubscribe does not unsubscribe.** `app/unsubscribe/page.tsx` renders "You've unsubscribed" on token load; the only mutation is resubscribe → `unsubscribed_at` is never set → false opt-out + the broadcast's opt-out filter has nothing to exclude. **Fix (Fix-2):** token-authenticated `POST /api/waitlist/unsubscribe` that sets `unsubscribed_at`; page calls it on load, shows confirmation only on success. **FIXED 2026-08-09** (commit c7a2e88): added `app/api/waitlist/unsubscribe/route.ts`, symmetrical with `resubscribe/route.ts` (unsubscribe_token-only filter, rate-limited, generic 404, idempotent); `app/unsubscribe/page.tsx` now calls it on load via a `checking` state before showing the confirmation. 163 suites/1912 tests, tsc, lint, build all clean. No migration involved.
- **B3 — reopen broadcast doesn't authorize recipients.** `broadcast/route.ts:40` sends a bare `/signup` link without minting an invite → gated recipients bounce. **Decision (owner):** Option A now — mint/activate a pending `artist_invites` row per recipient before send (gate stays invite-only). Option B (a DB signup-open toggle honored by trigger+precheck) is DEFERRED for the future "open signup season". **Fix (Fix-3). FIXED 2026-08-09** (commit `def0c0f`): `broadcast/route.ts` now calls the new shared `mintOrRotateInvite()` (`lib/invites/mintInvite.ts`, commit `fd526b8`) for each eligible recipient BEFORE sending, so the deep-link they receive carries a token the invite gate actually admits. Relies on migration 099's partial unique index on `LOWER(email) WHERE status='pending'` as the atomic claim.

## High

- **H1 — expired invites cannot be re-issued.** `admin/artist-invites/route.ts:78` treats any pending invite as a duplicate even past `token_expires_at`; `.../[id]/convert/route.ts:42` won't re-convert. **Fix (Fix-3):** detect expired pending, rotate token/expiry, resend/replace on both paths. **FIXED 2026-08-09** (commits `fd526b8`, `def0c0f`): both routes now go through the shared `mintOrRotateInvite()` helper, which rotates token+expiry in place for a past-expiry pending row (instead of reporting a stale duplicate) and resends; a still-active pending row remains a true no-resend duplicate. Convert can now re-issue for a previously-converted row whose invite has since expired. Covered by `lib/invites/mintInvite.test.ts` + updated route tests.
- **H2 — waitlist can report false success.** `app/api/waitlist/route.ts` ignores select/update/retry/most insert errors before returning `{ok:true}`. **Fix (Fix-3):** check every error, atomic normalized-email upsert, neutral failure when persistence fails. **FIXED 2026-08-09** (commit `d5eed0d`): new migration 100 adds `upsert_artist_waitlist(text, text, text)`, a service-role-only RPC doing a single atomic `INSERT ... ON CONFLICT (LOWER(email)) DO UPDATE` against migration 097's functional unique index (Postgres itself has no PostgREST-client-only conflict-target limitation). The route now calls that RPC once and checks its single error/result; `{ok:true}` is never returned without a persisted row. Migration not pushed (human-gated, matches 097/098/099).

## Medium

- **M1 — precheck/gate parity break.** `lib/invites/allowlist.ts:27` uses `.ilike(email, input)` (`%`/`_` are wildcards) vs the gate's exact `LOWER()=LOWER()`. **Fix (Fix-1):** exact normalized equality / one shared predicate.
- **M2 — parity test is substring-based** (`__tests__/migration-098-gate.test.ts:86`), so drift/wildcards pass. **Fix (Fix-1):** executable/behavioral parity incl. the `%`/`_` case + structural branch comparison.
- **M3 — gate accepts ALL pending invites for the email** without expiry/which-invite (098). **Fix (Fix-1):** accept only the specific active invitation used to admit.
- **M4 — non-unique `artist_invites` email index + select-before-write** (097) → concurrent duplicate invites/emails. **Fix (Fix-1):** partial unique index on `lower(email)` for active invites + transactional claim.
- **M5 — broadcast ignores send/update results but stamps `sent`** (broadcast:44). **Fix (Fix-3, with B3):** record delivered/failed separately; stamp only on successful delivery. **FIXED 2026-08-09** (commit `def0c0f`): `broadcast/route.ts` now tracks `delivered`/`failed` separately (response shape changed from `{sent}` to `{delivered, failed}`); `notified_reopen_at` is stamped ONLY after a successful send. A mint failure or a send failure both leave the row unstamped so it stays eligible/retryable on the next broadcast run.
- **M6 — collaborator invite uses a legacy inline email with unescaped `collaborator.name`** (`app/api/collaborators/[id]/invite/route.ts:87`) → HTML/markup injection. **Fix (Fix-3):** route through the escaped branded template. **FIXED 2026-08-09** (commit `6348363`): `collaborator.name`, `joinUrl`, and `inviteUrl` are now escaped via `lib/email/esc.ts` (same helper the branded templates use) before interpolation into the HTML body. New `route.test.ts` (none existed previously) locks the escaping behavior plus the route's existing wiring.

## Low

- **L1 — `email_has_account()` uses `SET search_path=public`** not `''` (097). **Fix (Fix-1):** `search_path=''` + qualify `pg_catalog`.
- **L2 — check route returns exact booleans + per-instance limiter.** **Disposition: ACCEPT for beta** (research-accepted residual enumeration risk; revisit with a durable shared limiter for GA).
- **L3 — waitlist email/name lack length/non-empty enforcement** (`lib/invites/schema.ts:47`). **Fix (Fix-3):** validate email syntax, require a trimmed name, cap fields server-side. **FIXED 2026-08-09** (commit `d5eed0d`): `sanitizeWaitlistEntry()` now requires a trimmed, non-empty `name` (parity with the signup page's already-`required` client-side field) and caps `email` at 254 chars / `name` at 200 chars server-side; email syntax was already validated by `EMAIL_REGEX`.

## Deferred

- **Signup-open toggle** (Option B for "reopen") — for the future public open-signup season.

## Follow-up review (2026-08-09) — concurrency/atomicity of the B3/H1/M5 fix commits

**Scope:** an independent re-audit of the B3/H1/M5 fix itself (`fd526b8`, `def0c0f`), not the original 097-100 findings. Found the shared `mintOrRotateInvite()` claim helper those commits introduced was not actually atomic, plus two related write-result-ignored/non-atomic issues in the routes that call it.

- **HIGH — mintInvite rotation race (read-then-unconditional-update).** `rotateRow()` (`lib/invites/mintInvite.ts`, pre-fix ~:46) issued a plain `SELECT` to find the pending row, then an unconditional `UPDATE ... SET invite_token = <new>` keyed only on `id`+`status` — no compare-and-set against the row's prior token/expiry. Two concurrent callers reading the SAME expired row (broadcast fan-out, or a convert racing an issue) could each mint their own token and each `UPDATE` would succeed — a torn read/write: whichever email went out first silently stopped working the instant the other caller's `UPDATE` landed. **FIXED** (commit `e3d2d54`): migration 101's `mint_or_rotate_artist_invite()` — a SECURITY DEFINER, `search_path=''`, service-role-only RPC — collapses the entire mint/rotate/reuse decision into ONE row-locked `INSERT ... ON CONFLICT (LOWER(email)) WHERE status='pending' DO UPDATE` statement against migration 099's partial unique index. Two concurrent callers now serialize on Postgres's own row lock instead of racing in application code; the second caller re-evaluates its `CASE` against the just-rotated (no-longer-expired) row and correctly resolves to `'reused'`. `lib/invites/mintInvite.ts` now only generates a candidate token/expiry and calls the RPC — no JS-side select-then-branch, no 23505 fallback handling (the DB collapses that into the same statement).
- **HIGH — reintroduced ILIKE wildcard.** `mintInvite.ts` used `.ilike('email', email)` (pre-fix ~:68/:105) with no escaping — reintroducing M1's original bug (an email containing `%` or `_` would match multiple/other rows) despite `lib/invites/allowlist.ts` already establishing the correct escaping pattern (`exactCaseInsensitiveEmailPattern()`) elsewhere in this same phase. **FIXED** (commit `e3d2d54`), as a direct consequence of the atomicity fix above: the RPC matches via the same exact `LOWER(email)` equality the partial unique index and `handle_new_user()`'s gate already use — there is no client-side `SELECT` left to express with `.ilike()` at all. Covered by `lib/invites/mintInvite.test.ts`'s new test asserting a `%`/`_`-containing email reaches the RPC as an untouched parameterized string.
- **HIGH — broadcast recipient claim was select-then-stamp, not atomic.** `broadcast/route.ts` (pre-fix ~:35) selected eligible rows, then stamped `notified_reopen_at` only AFTER a successful send — not a claim. Two overlapping broadcast runs (a retry racing a still-in-flight leadership click, or two leadership members triggering it near-simultaneously) could both select the SAME unnotified row and both mint+email it before either stamp landed. **FIXED** (commit `b963ca2`): each row is now atomically claimed via a single conditional `UPDATE ... WHERE notified_reopen_at IS NULL`, checked via `.select('id').maybeSingle()` BEFORE any mint/send work — a lost claim race (another concurrent run already owns the row) is distinguished from a genuine DB error and is not double-counted. A mint or send failure resets the claim (`notified_reopen_at = null`) so the row stays retryable instead of silently marked delivered or stuck.
- **MEDIUM — broadcast `notified_reopen_at` update result ignored; `delivered` incremented unconditionally.** Superseded by the HIGH fix directly above — the same atomic-claim rewrite checks every update's result and only increments `delivered` after a confirmed send.
- **MEDIUM — convert `converted_to_invite_at` update ignored before send.** `convert/route.ts` (pre-fix ~:71) stamped `converted_to_invite_at` with a fire-and-forget `UPDATE` (result never checked) AFTER deciding, from a stale read taken at the top of the handler, whether to send. Two concurrent first-time convert calls for the SAME row could both read `converted_to_invite_at` as `null`, both pass `mintOrRotateInvite` (one `'created'`, one `'reused'` once atomic), and the `'reused'` caller's duplicate check (`row.converted_to_invite_at && state === 'reused'`) was still false against its OWN stale read — double email. **FIXED** (commit `16cb802`): a first-time conversion now requires winning an atomic `UPDATE ... WHERE converted_to_invite_at IS NULL` claim BEFORE any mint/send work; losing that race returns `duplicate: true` with no email sent. An already-converted row skips the claim entirely and keeps H1's re-issue-on-expiry path (decided by `mintOrRotateInvite`'s own atomic state), but its restamp write is now checked via `.select('id').maybeSingle()` instead of ignored.

**Disposition of the original B3/H1/M5 entries above:** B3, H1, M5 were **PARTIAL** at the code-review level (the right shape, the right idea) but not atomic under concurrency — now **RESOLVED** by the three fixes in this section. M1 (mintInvite ILIKE reintroduction, not the original `allowlist.ts` M1) is also **RESOLVED**.

**Status:** `npm test` (166 suites / 1951 tests), `tsc --noEmit`, `next lint`, and `next build` all clean after this section's fixes. Migration 101 is drafted TEXT/test-only — **NOT pushed** — applies after 100, reviewed and pushed by the owner alongside 097-100 at the phase's blocking cutover checkpoint.
