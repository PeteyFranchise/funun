---
phase: 27-artist-invite-only-onboarding
plan: 04
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - lib/invites/allowlist.ts
  - lib/invites/allowlist.test.ts
  - lib/invites/invite-fixtures.ts
  - supabase/migrations/098_artist_signup_gate.sql
  - __tests__/migration-098-gate.test.ts
autonomous: true
requirements: [INVITE-01, INVITE-02]
must_haves:
  truths:
    - "An uninvited email cannot create an artist account: the provisioning trigger raises and rolls back the whole transaction, leaving no auth.users row and no user_profiles row (D-01/D-02, INVITE-01)."
    - "A collaborator email OR a pending non-expired artist_invites email IS admitted (D-04/D-05, INVITE-02)."
    - "The gate applies to the ARTIST branch ONLY — curator/buyer/industry provisioning is byte-for-byte unchanged (D-03, RESEARCH Pitfall 1)."
    - "The TypeScript allowlist predicate (isArtistEmailAllowed) and the SQL EXISTS predicate agree on a shared fixture table (RESEARCH Pitfall 3 twin-parity)."
  artifacts:
    - lib/invites/allowlist.ts
    - lib/invites/invite-fixtures.ts
    - supabase/migrations/098_artist_signup_gate.sql
    - __tests__/migration-098-gate.test.ts
  key_links:
    - "The SQL EXISTS(collaborators) OR EXISTS(artist_invites pending, unexpired) in handle_new_user() is the twin of isArtistEmailAllowed() — the shared invite-fixtures.ts scenario table anchors both."
    - "On admission the trigger marks the matching artist_invites row accepted before claim_collaborators() runs, so claim only fires for admitted signups (RESEARCH sequencing)."
---

<objective>
Build the server-authoritative gate — the real, unbypassable boundary (D-02) — as an early check inside `handle_new_user()`'s artist branch, and its TypeScript twin (`isArtistEmailAllowed`) that the pre-check UX route (27-06) reuses. A shared fixture table plus a migration-content assertion guards the two sides from drifting (RESEARCH's highest-priority pitfall).

Purpose: This is the phase's core — everything else is UX around this one check.
Output: `lib/invites/allowlist.ts` + `invite-fixtures.ts` (+ test), migration 098 (the gate), and the migration-content twin-parity test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-artist-invite-only-onboarding/27-CONTEXT.md
@.planning/phases/27-artist-invite-only-onboarding/27-RESEARCH.md
@.planning/phases/27-artist-invite-only-onboarding/27-PATTERNS.md
@supabase/migrations/086_restore_buyer_branch_handle_new_user.sql
@.planning/phases/27-artist-invite-only-onboarding/27-01-SUMMARY.md
</context>

<artifacts>
This plan produces:
- Function: `public.handle_new_user()` re-defined with the artist-branch invite gate + accept-marking (migration 098)
- Migration file: `supabase/migrations/098_artist_signup_gate.sql`
- Module: `lib/invites/allowlist.ts` exporting `isArtistEmailAllowed(service, email)` and `emailHasExistingAccount(service, email)`
- Fixture: `lib/invites/invite-fixtures.ts` exporting the shared scenario table `INVITE_ALLOWLIST_SCENARIOS`
- Test: `__tests__/migration-098-gate.test.ts` (migration-content assertion + twin-parity)
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/invites/allowlist.ts (TS predicate) + shared fixture table</name>
  <files>lib/invites/allowlist.ts, lib/invites/invite-fixtures.ts, lib/invites/allowlist.test.ts</files>
  <read_first>
    - app/api/claim-collaborators/route.ts (the service-client email-match convention this predicate mirrors)
    - lib/invites/schema.ts (the status vocabulary — pending/accepted/expired — from 27-01)
    - .planning/phases/27-artist-invite-only-onboarding/27-RESEARCH.md (Pattern 2 + Pitfall 3 twin-parity)
  </read_first>
  <behavior>
    - isArtistEmailAllowed(service, email) → true when a collaborators row exists with LOWER(email) match; OR an artist_invites row exists with LOWER(email) match, status pending, and (token_expires_at null OR in the future). False otherwise.
    - Matching is case-insensitive and trims the input email.
    - emailHasExistingAccount(service, email) → calls the email_has_account RPC and returns boolean.
    - invite-fixtures scenarios cover: collaborator-only match (allowed); pending-invite match (allowed); expired-invite only (denied); accepted-invite only (denied); no rows (denied); mixed-case email still matches (allowed).
  </behavior>
  <action>
    Create `lib/invites/invite-fixtures.ts` exporting `INVITE_ALLOWLIST_SCENARIOS` — an array of `{ name, email, collaboratorEmails, inviteRows: { email, status, expired }[], expected: boolean }` covering the behavior cases above. This is the single twin-parity anchor (RESEARCH Pitfall 3), consumed by BOTH this TS test and the migration-content test in Task 3. Create `lib/invites/allowlist.ts` exporting `isArtistEmailAllowed(service: SupabaseClient, email: string): Promise<boolean>` that runs the two service-role reads mirroring the trigger SQL exactly (collaborators LOWER(email) match; artist_invites LOWER(email) match with status pending and token_expires_at null-or-future), and `emailHasExistingAccount(service, email)` calling the `email_has_account` RPC from 27-01. Named exports, no semicolons, `@/` imports. Write `lib/invites/allowlist.test.ts` driving `isArtistEmailAllowed` against a mocked service client for every fixture scenario, asserting `expected`.
  </action>
  <verify>
    <automated>npm test -- lib/invites/allowlist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/invites/allowlist.test.ts` passes for every scenario in invite-fixtures.ts.
    - The predicate matches case-insensitively and treats expired/accepted invites as NOT allowed.
    - emailHasExistingAccount routes through the email_has_account RPC (service-role), not a client query.
  </acceptance_criteria>
  <done>The TS allowlist twin + shared fixture table exist and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 098 — handle_new_user() artist-branch invite gate</name>
  <files>supabase/migrations/098_artist_signup_gate.sql</files>
  <read_first>
    - supabase/migrations/086_restore_buyer_branch_handle_new_user.sql (THE current live handle_new_user body — reproduce it; do NOT stop at 076; verify no later migration re-defines it before editing)
    - .planning/phases/27-artist-invite-only-onboarding/27-RESEARCH.md (Pattern 1 code block, the exact recommended gate body)
    - supabase/migrations/075_phase19_privilege_hardening.sql (header "why" convention + HUMAN-GATED-push note style)
  </read_first>
  <action>
    First confirm the current live `handle_new_user()` body by checking that no migration after 086 re-defines it (grep the migrations dir; 086 is the latest as of 27-RESEARCH). Create migration 098 as `CREATE OR REPLACE FUNCTION public.handle_new_user()` reproducing 086's body VERBATIM (curator RETURN NEW then buyer RETURN NEW then industry branch with capability_grants insert RETURN NEW then default/artist branch), then insert the gate as the FIRST statements inside the default (artist) branch ONLY — after all three role-specific `IF ... RETURN NEW` blocks and BEFORE the existing `INSERT INTO public.user_profiles`. The gate: declare `v_is_invited boolean` in the function's DECLARE section; `SELECT EXISTS(SELECT 1 FROM public.collaborators WHERE LOWER(email)=LOWER(NEW.email)) OR EXISTS(SELECT 1 FROM public.artist_invites WHERE LOWER(email)=LOWER(NEW.email) AND status='pending' AND (token_expires_at IS NULL OR token_expires_at > NOW())) INTO v_is_invited;` then raise on the negative path with `RAISE EXCEPTION 'not_invited' USING ERRCODE='P0001';` inside `IF NOT v_is_invited THEN ... END IF;` (the raise rolls back the whole transaction incl. the auth.users row — no phantom account; D-01/D-02/D-03). After the gate and before the inserts, add an exception-isolated `UPDATE public.artist_invites SET status='accepted', accepted_user_id=NEW.id, accepted_at=NOW() WHERE LOWER(email)=LOWER(NEW.email) AND status='pending'` wrapped in `BEGIN ... EXCEPTION WHEN OTHERS THEN NULL; END;` so a mark-accepted failure can never roll back a legitimate signup. Leave the existing user_profiles/subscriptions inserts and the claim_collaborators() exception block UNCHANGED — claim now runs only for admitted signups by construction. Header comment: state the gate is artist-branch-only (RESEARCH Pitfall 1), is the real boundary per D-02, and is HUMAN-GATED push (never `supabase db push` from an agent; the push is 27-11's blocking task).
  </action>
  <verify>
    <automated>grep -q "'not_invited'" supabase/migrations/098_artist_signup_gate.sql && grep -q 'CREATE OR REPLACE FUNCTION public.handle_new_user' supabase/migrations/098_artist_signup_gate.sql && grep -q "= 'buyer'" supabase/migrations/098_artist_signup_gate.sql && grep -q "= 'industry'" supabase/migrations/098_artist_signup_gate.sql && grep -q 'artist_invites' supabase/migrations/098_artist_signup_gate.sql && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - The gate raises on the uninvited path and only inside the default/artist branch (after the curator/buyer/industry RETURN NEW blocks).
    - The curator, buyer, and industry branches are reproduced unchanged from 086.
    - The accept-marking UPDATE is exception-isolated and runs before the existing inserts; claim_collaborators() is unchanged.
    - Header documents artist-branch-only scope and the human-gated push.
  </acceptance_criteria>
  <done>Migration 098 defines the gated handle_new_user() with the artist-only invite check and accept-marking.</done>
</task>

<task type="auto">
  <name>Task 3: migration-098-gate.test.ts — placement assertion + twin-parity guard</name>
  <files>__tests__/migration-098-gate.test.ts</files>
  <read_first>
    - .planning/phases/27-artist-invite-only-onboarding/27-RESEARCH.md (Pitfall 1 + Pitfall 3 — the migration-content assertion strategy this repo uses for un-executable PL/pgSQL)
    - __tests__/claim-collaborators-rpc.test.ts (existing migration-content assertion test pattern to mirror, if present)
    - lib/invites/invite-fixtures.ts (the shared scenario table from Task 1)
  </read_first>
  <action>
    Create `__tests__/migration-098-gate.test.ts` reading the migration 098 file text and asserting: (1) PLACEMENT — the `RAISE EXCEPTION 'not_invited'` substring appears AFTER the last `= 'buyer'`/`= 'industry'` branch's `RETURN NEW;` and BEFORE the artist branch `INSERT INTO public.user_profiles` (RESEARCH Pitfall 1 — the gate is artist-branch-only). Compute string indices and assert ordering. (2) SCOPE — the curator/buyer/industry early-return blocks are present and unchanged in shape (the three `RETURN NEW` role branches exist before the gate). (3) TWIN-PARITY — for each `INVITE_ALLOWLIST_SCENARIOS` case, assert the SQL predicate would produce `expected`, using a structural description: verify the migration text contains both EXISTS clauses (collaborators LOWER(email) match; artist_invites status='pending' AND token_expires_at null-or-future) so the SQL side structurally covers the same three allowlist sources the TS twin checks. Since Jest cannot execute PL/pgSQL, this is a text-structural assertion (matching Phase 17/19 precedent).
  </action>
  <verify>
    <automated>npm test -- __tests__/migration-098-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- __tests__/migration-098-gate.test.ts` passes.
    - The placement assertion fails if the gate is moved before the role branches (proving Pitfall 1 is guarded).
    - The parity assertion references the shared invite-fixtures.ts table (single source of truth for both twins).
  </acceptance_criteria>
  <done>The migration-content test enforces artist-branch-only placement and SQL↔TS structural parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon client → Supabase /auth/v1/signup | An uninvited caller can hit signup directly, bypassing any page-only check |
| trigger SQL predicate ↔ TS pre-check | Divergence produces a UI that says "invited" but a server that rejects (or vice versa) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Elevation of Privilege | handle_new_user() artist branch | high | mitigate | Server-authoritative gate raises + rolls back the whole transaction on the uninvited path (D-02) — unbypassable via direct PostgREST/anon signup |
| T-27-10 | Tampering | SQL↔TS predicate drift | high | mitigate | Shared invite-fixtures.ts scenario table + migration-content twin-parity test asserting both sides cover the same three allowlist sources (RESEARCH Pitfall 3) |
| T-27-11 | Denial of Service | Gate over-scoped, locking out staff/industry/buyer | high | mitigate | Placement assertion enforces the gate lives inside the artist branch only, after the role RETURN NEW blocks (RESEARCH Pitfall 1) |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed |
</threat_model>

<verification>
- `npm test -- lib/invites/allowlist.test.ts __tests__/migration-098-gate.test.ts` green.
- Migration 098 grep-gate passes (raise present; role branches present; artist_invites referenced).
- `npm run build` / `tsc --noEmit` clean (allowlist compiles; migration not type-checked).
- NOTE: live enforcement is proven by the 27-11 blocking `supabase db push` + live smoke — this plan's tests are structural/unit only (no live DB).
</verification>

<success_criteria>
The server-authoritative gate exists in migration 098 (artist-branch-only), its TS twin is tested against the shared fixtures, and the migration-content test guards both placement and SQL↔TS parity.
</success_criteria>

<output>
Create `.planning/phases/27-artist-invite-only-onboarding/27-04-SUMMARY.md` when done.
</output>
