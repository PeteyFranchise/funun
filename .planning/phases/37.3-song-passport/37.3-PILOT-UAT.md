# Phase 37.3 — Song Passport Pilot UAT

## Preconditions

- Migrations 150–156 appear applied in order.
- `SONG_PASSPORT_KILL_SWITCH=false`.
- Use `SONG_PASSPORT_PILOT_ENABLED=true` plus server-created cohort rows, or a temporary controlled `SONG_PASSPORT_ENABLED=true`.
- Test with backed-up, non-irreplaceable pilot audio.

## Required stories

1. Solo work: review legacy facts twice; no duplicate values/issues.
2. Multi-writer work: collaborative fields are shared, own private identity is visible, and legal fields require grants.
3. Conflict: two sessions save one fact; one receives a conflict rather than silently replacing the other.
4. Trust: self-confirm identity, approve composition, change it and retain the approval plus successor-review task.
5. Versions: use three recordings, link a final mix from Release Report, choose and replace a master, preserving both designations.
6. Graduation: create Release Report without approved-fact re-entry or overwrite; repeat request is idempotent.
7. Artifacts: generate JSON/sidecar; tagged MP3 only from MP3; WAV/FLAC gives sidecar guidance; source hash stays unchanged.
8. Custody: create scoped package and test transfer; former/new controller and prior receipts remain, plus access-review task.
9. Rollback: emergency stop removes panel/APIs while rows remain.

## Stop conditions

Stop the pilot and enable the kill switch for any unauthorized read/write, private-data leak, source mutation, silent overwrite, orphaned evidence, or language treating generated/prepared as transmitted/accepted.
