---
phase: 05-launchpad-checklist
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - middleware.ts
  - package.json
  - package-lock.json
  - app/(artist)/launchpad/[projectId]/page.tsx
  - app/(admin)/layout.tsx
  - app/(admin)/checklist/page.tsx
  - app/(admin)/tips/page.tsx
  - app/(admin)/page.tsx
autonomous: true
requirements: [LAUNCH-01, LAUNCH-05]
must_haves:
  truths:
    - "Unauthenticated requests to /launchpad/* and /admin/* are redirected to /signin by middleware"
    - "Non-admin authenticated users hitting any /admin/* route are redirected to / by the (admin) layout is_admin gate"
    - "@dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities are installed and importable"
    - "Bare server pages exist at /launchpad/[projectId], /admin/checklist, /admin/tips and /admin redirects to /admin/checklist"
  artifacts:
    - "app/(admin)/layout.tsx (is_admin gate + admin nav)"
    - "app/(artist)/launchpad/[projectId]/page.tsx (scaffold)"
    - "app/(admin)/checklist/page.tsx, app/(admin)/tips/page.tsx, app/(admin)/page.tsx"
    - "@dnd-kit packages in package.json dependencies"
  key_links:
    - "middleware.ts isProtected includes pathname.startsWith('/launchpad') and pathname.startsWith('/admin')"
    - "(admin)/layout.tsx redirects when (user.app_metadata as { is_admin?: boolean })?.is_admin !== true"
---

<objective>
Establish the routing and dependency foundation: add `/launchpad` and `/admin` to the middleware protected-route guard, install the three @dnd-kit packages, create the shared `(admin)` route group with its `is_admin`-gated layout and nav, and scaffold all bare-bones server pages that Plans 04 (artist room) and 06 (admin UI) will fill in.

Purpose: De-risk the route architecture and the new admin auth boundary before any feature components are built. Plans 04 and 06 attach client components to these page shells.
Output: middleware guard update, @dnd-kit install, `(admin)` layout + 3 admin pages + redirect, artist `[projectId]` page scaffold.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-launchpad-checklist/05-CONTEXT.md
@.planning/phases/05-launchpad-checklist/05-RESEARCH.md
@.planning/phases/05-launchpad-checklist/05-PATTERNS.md
@.planning/phases/05-launchpad-checklist/05-UI-SPEC.md
</context>

## Artifacts this phase produces

New symbols created in this plan (exclude from drift verification):
- New route group: `app/(admin)/`
- Server components: `AdminLayout` (app/(admin)/layout.tsx), admin index redirect page, `/admin/checklist` scaffold page, `/admin/tips` scaffold page, `LaunchpadProjectPage` scaffold (app/(artist)/launchpad/[projectId]/page.tsx)
- Dependencies: `@dnd-kit/core` 6.3.1, `@dnd-kit/sortable` 10.0.0, `@dnd-kit/utilities` 3.2.2
- The `is_admin` gate pattern: cast `user.app_metadata` to `{ is_admin?: boolean }` and compare the `is_admin` member strictly to `true`

<tasks>

<task type="auto">
  <name>Task 1: Add /launchpad and /admin to middleware protected routes + install @dnd-kit</name>
  <files>middleware.ts, package.json, package-lock.json</files>
  <read_first>
    - middleware.ts (current isProtected block, lines 17-22; the (artist) route-group note about real path prefixes)
    - .planning/phases/05-launchpad-checklist/05-RESEARCH.md § Package Legitimacy Audit (all three @dnd-kit packages already returned OK verdict — no further checkpoint needed)
    - .planning/phases/05-launchpad-checklist/05-PATTERNS.md § middleware.ts (modification)
  </read_first>
  <action>
    In `middleware.ts`, extend the `isProtected` boolean to also match the two new path prefixes: add `pathname.startsWith('/launchpad')` and `pathname.startsWith('/admin')` to the existing OR chain (which already covers /vault, /dashboard, /settings, /collaborators, /split-sheets). Do not change the Phase 4 claim-collaborators fire-and-forget block or the matcher config — only add the two prefixes.

    Then install the three drag-and-drop packages (already audited OK in RESEARCH.md — versions: @dnd-kit/core 6.3.1, @dnd-kit/sortable 10.0.0, @dnd-kit/utilities 3.2.2): run `npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`. Confirm all three land in package.json `dependencies`.
  </action>
  <verify>
    <automated>grep -q "startsWith('/launchpad')" middleware.ts && grep -q "startsWith('/admin')" middleware.ts && node -e "const p=require('./package.json').dependencies; process.exit(p['@dnd-kit/core']&&p['@dnd-kit/sortable']&&p['@dnd-kit/utilities']?0:1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - middleware.ts isProtected chain contains `pathname.startsWith('/launchpad')` and `pathname.startsWith('/admin')`
    - The Phase 4 claim block and `export const config` matcher are unchanged
    - package.json dependencies contains @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities
    - `node -e` exit-0 check above passes
  </acceptance_criteria>
  <done>Both new path prefixes are guarded by middleware and all three @dnd-kit packages are installed in dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create the (admin) route group — is_admin-gated layout, nav, and three pages</name>
  <files>app/(admin)/layout.tsx, app/(admin)/page.tsx, app/(admin)/checklist/page.tsx, app/(admin)/tips/page.tsx</files>
  <read_first>
    - app/(artist)/layout.tsx (the layout shell wrapper pattern to adapt)
    - .planning/phases/05-launchpad-checklist/05-PATTERNS.md § app/(admin)/layout.tsx (the is_admin gate adaptation + minimal admin sidebar)
    - .planning/phases/05-launchpad-checklist/05-RESEARCH.md § is_admin Check Implementation (the app_metadata cast + redirect pattern)
    - .planning/phases/05-launchpad-checklist/05-UI-SPEC.md § Color and Typography (admin shell uses bg-ink text-white)
  </read_first>
  <action>
    Create `app/(admin)/layout.tsx` as an async server component. Call `createServerClient()` from `@/lib/supabase/server`, get the user via `supabase.auth.getUser()`, `redirect('/signin')` if no user, then compute the admin flag by casting `user.app_metadata` to `{ is_admin?: boolean }` and checking the `is_admin` member strictly equals `true`; `redirect('/')` when it does not. Render an outer `div className="flex min-h-screen bg-ink text-white"` with a minimal admin sidebar (a simple vertical nav using `next/link` linking to `/admin/checklist` labeled "Checklist Items" and `/admin/tips` labeled "Tips") and a `<div className="flex min-h-screen flex-1 flex-col">{children}</div>` content column. Keep the sidebar lightweight — do not reuse ArtistNav.

    Create `app/(admin)/page.tsx` as a server component that immediately `redirect('/admin/checklist')` (matches CONTEXT.md admin route map: `/admin` → redirect to `/admin/checklist`).

    Create `app/(admin)/checklist/page.tsx` as a minimal async server component scaffold: a heading "Checklist Items" and a placeholder paragraph. Plan 06 replaces the body with the `ChecklistAdmin` client component. Use page-level wrapper `div className="flex-1 px-9 py-[30px]"` to match existing page padding.

    Create `app/(admin)/tips/page.tsx` as a minimal async server component scaffold: a heading "Tips" and a placeholder paragraph. Plan 06 replaces the body with `TipsAdmin`. Same wrapper padding.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/(admin)/layout.tsx contains `createServerClient` and a redirect to `/` guarded by the is_admin check
    - app/(admin)/layout.tsx casts `user.app_metadata` and compares `is_admin` to `true`
    - app/(admin)/page.tsx contains `redirect('/admin/checklist')`
    - app/(admin)/checklist/page.tsx and app/(admin)/tips/page.tsx exist and render headings "Checklist Items" and "Tips"
    - `npm run build` completes with zero TypeScript errors
  </acceptance_criteria>
  <done>The (admin) route group exists with a single is_admin gate at the layout, /admin redirects to /admin/checklist, and both admin pages render scaffold headings; build is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the per-project Launchpad room page</name>
  <files>app/(artist)/launchpad/[projectId]/page.tsx</files>
  <read_first>
    - app/(artist)/vault/[projectId]/rights/page.tsx (the exact server-component auth + ownership + notFound pattern to copy)
    - .planning/phases/05-launchpad-checklist/05-PATTERNS.md § app/(artist)/launchpad/[projectId]/page.tsx (auth + ownership + multi-query patterns)
    - app/(artist)/launchpad/page.tsx (Topbar usage and page wrapper classes for visual consistency)
  </read_first>
  <action>
    Create `app/(artist)/launchpad/[projectId]/page.tsx` as an async server component with signature `params: Promise<{ projectId: string }>`. `await params` to get `projectId`. Call `createServerClient()`, `supabase.auth.getUser()`, `redirect('/signin')` if no user. Query `vault_projects` selecting `id, title, release_date` with `.eq('id', projectId).eq('user_id', user.id).single()`; call `notFound()` if no project. For now render a minimal shell: a `<Topbar>` (title `{project.title} — Launchpad`, subtitle "Your post-release action plan — week by week" per UI-SPEC § Copywriting) and a placeholder `div className="flex-1 px-9 py-[30px]"` with text "Checklist loading…". Plan 04 will add the two parallel queries (launchpad_checklist_items + launchpad_progress), the merge, and the `<LaunchpadRoom>` client component. Add `export const dynamic = 'force-dynamic'` to match the global launchpad page.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/(artist)/launchpad/[projectId]/page.tsx uses `params: Promise<{ projectId: string }>` and `await params`
    - The page selects `release_date` from vault_projects (needed by Plan 04 collapse logic)
    - The page calls `notFound()` when the project does not belong to the user
    - Page title uses the `{project.title} — Launchpad` copy from UI-SPEC
    - `npm run build` completes with zero TypeScript errors
  </acceptance_criteria>
  <done>The per-project Launchpad page exists with Next.js 15 async params, owner-scoped project fetch, notFound on miss, and a Topbar; build is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated request → protected pages | Middleware must block /launchpad and /admin without a session |
| non-admin user → /admin/* | The (admin) layout must redirect non-admins before any admin content renders |
| supply chain → npm install | Three new @dnd-kit packages introduced into the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Elevation of Privilege | /admin/* routes | high | mitigate | (admin) layout casts `user.app_metadata` to `{ is_admin?: boolean }` and redirects to `/` unless `is_admin === true`; middleware adds defense-in-depth by gating unauthenticated access. Admin API routes re-verify independently in Plan 05 |
| T-05-04 | Information Disclosure | unauthenticated /launchpad access | medium | mitigate | middleware.ts isProtected now matches `/launchpad` and `/admin`, redirecting sessionless requests to /signin |
| T-05-SC | Tampering | @dnd-kit npm installs | high | accept | All three packages returned OK in RESEARCH.md § Package Legitimacy Audit (18M+ weekly downloads, same github.com/clauderic/dnd-kit source, MIT). No [ASSUMED]/[SUS]/[SLOP] packages — no blocking checkpoint required |
</threat_model>

<verification>
- `npm run build` passes with zero TypeScript errors
- Visiting `/admin/checklist` while signed in as a non-admin redirects to `/`
- Visiting `/launchpad/anything` while signed out redirects to `/signin`
- `import { DndContext } from '@dnd-kit/core'` resolves at build time
</verification>

<success_criteria>
- Middleware guards both new prefixes
- @dnd-kit packages installed
- (admin) route group renders with the is_admin gate working
- Per-project launchpad page scaffold builds and fetches an owner-scoped project
</success_criteria>

<output>
Create `.planning/phases/05-launchpad-checklist/05-02-SUMMARY.md` when done
</output>
