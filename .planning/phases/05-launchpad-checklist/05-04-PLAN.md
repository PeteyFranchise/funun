---
phase: 05-launchpad-checklist
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - components/launchpad/TipPanel.tsx
  - components/launchpad/ChecklistItem.tsx
  - components/launchpad/ChecklistSection.tsx
  - components/launchpad/LaunchpadRoom.tsx
  - app/(artist)/launchpad/[projectId]/page.tsx
  - app/(artist)/launchpad/page.tsx
autonomous: true
requirements: [LAUNCH-01, LAUNCH-02, LAUNCH-03, LAUNCH-04]
must_haves:
  truths:
    - "Artist opens /launchpad/[projectId] and sees the checklist grouped into 4 sections (Before release, Week 1, Week 2, Weeks 3-4) ordered by suggested_week then sort_order"
    - "Clicking an item row opens the TipPanel; clicking the checkbox toggles completion WITHOUT opening the panel"
    - "Toggling a checkbox persists via PATCH and survives a hard page reload"
    - "The Before release section auto-collapses to a confirmation block when the project release_date is in the past"
    - "Approved tips render in the TipPanel; when no approved tip exists only steps + CTA show; the global /launchpad page shows project cards above the existing playbook"
  artifacts:
    - "components/launchpad/TipPanel.tsx, ChecklistItem.tsx, ChecklistSection.tsx, LaunchpadRoom.tsx"
    - "app/(artist)/launchpad/[projectId]/page.tsx (wired to LaunchpadRoom)"
    - "app/(artist)/launchpad/page.tsx (project cards added above playbook)"
  key_links:
    - "ChecklistItem checkbox onClick calls stopPropagation so row-click (open panel) and checkbox-click (toggle) stay independent"
    - "LaunchpadRoom optimistic toggle → PATCH /api/launchpad/[projectId]/progress with rollback on error"
    - "[projectId]/page.tsx passes release_date to LaunchpadRoom for before-release collapse"
---

<objective>
Build the artist-facing Launchpad room: the `TipPanel` side drawer (verbatim shell copy of ToolSidePanel), the `ChecklistItem` row (independent checkbox vs row-click), the `ChecklistSection` (week grouping + before-release collapse), the `LaunchpadRoom` container (panel state + optimistic progress toggle), wire them into the `[projectId]` page (replacing the Plan 02 scaffold), and add project cards above the existing global `/launchpad` playbook.

Purpose: This is the user-visible deliverable for LAUNCH-01 through LAUNCH-04 — the screen an artist actually works after release day.
Output: 4 launchpad components + wired per-project page + project cards on the global page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-launchpad-checklist/05-CONTEXT.md
@.planning/phases/05-launchpad-checklist/05-PATTERNS.md
@.planning/phases/05-launchpad-checklist/05-UI-SPEC.md
@components/vault/ToolSidePanel.tsx
@app/(artist)/launchpad/page.tsx
</context>

## Artifacts this phase produces

New symbols created in this plan (exclude from drift verification):
- Client components: `TipPanel`, `ChecklistItem`, `ChecklistSection`, `LaunchpadRoom`
- Section ordering constant covering `before_release`, `week_1`, `week_2`, `weeks_3_4` with their UI-SPEC headers and sub-labels
- Project-card grid block inside the existing `app/(artist)/launchpad/page.tsx`
- Type already produced in Plan 01: `MergedChecklistItem`

<tasks>

<task type="auto">
  <name>Task 1: TipPanel + ChecklistItem components</name>
  <files>components/launchpad/TipPanel.tsx, components/launchpad/ChecklistItem.tsx</files>
  <read_first>
    - components/vault/ToolSidePanel.tsx (copy the shell verbatim: container, backdrop, aside, header, body, footer classes; Escape key useEffect; reset-on-item-change useEffect)
    - .planning/phases/05-launchpad-checklist/05-PATTERNS.md § components/launchpad/TipPanel.tsx and § components/launchpad/ChecklistItem.tsx (full reference structures)
    - .planning/phases/05-launchpad-checklist/05-UI-SPEC.md § Component Interaction Contracts (TipPanel + ChecklistItem row), § Copywriting Contract, § Color, § Typography
  </read_first>
  <action>
    Create `components/launchpad/TipPanel.tsx` (`'use client'`). Props: `item: MergedChecklistItem | null`, `onClose: () => void`. Replicate the ToolSidePanel shell exactly — container `fixed inset-0 z-50 flex justify-end`, backdrop button `absolute inset-0 bg-black/60 backdrop-blur-sm` (click closes, aria-label "Close tip panel"), aside `relative flex h-full w-full max-w-md flex-col border-l border-white/10 bg-[#0a0a0f] shadow-2xl`, header `flex items-start justify-between gap-3 border-b border-white/10 p-5` (item.label as `text-lg font-semibold text-white` plus a close button), body `flex-1 overflow-y-auto p-5`, footer `border-t border-white/10 p-5`. Add the Escape-key `useEffect` (active only when item is non-null) and return null when item is null. Body: render `item.tip_body` paragraph (`text-[14px] text-white/70 leading-[1.5]`) only when it is truthy; when no tip, show the copy "Steps for this item are coming soon." per UI-SPEC. Footer CTA: render the action link ONLY when `item.action_href` is non-null — an `<a>` with `bg-white text-black font-bold` styling, label `item.action_label`; for `action_type === 'external_url'` set `target="_blank"` and `rel="noopener noreferrer"`; for `internal_tool` omit target (in-app navigation). Hide the CTA entirely when action_href is null (RESEARCH Open Question 3).

    Create `components/launchpad/ChecklistItem.tsx` (`'use client'`). Props: `item: MergedChecklistItem`, `onToggle: (key: string, completed: boolean) => void`, `onOpenPanel: (item: MergedChecklistItem) => void`. Render a row `div` with `flex items-center gap-4 rounded-[14px] border border-hair bg-card px-[18px] py-4 cursor-pointer hover:bg-white/5`, whose `onClick` calls `onOpenPanel(item)`. Inside, render a checkbox control (role="checkbox", aria-checked={item.completed}) wrapped in a `p-2.5` hit area (≥44×44 target per UI-SPEC accessibility); its `onClick` must call `e.stopPropagation()` so the row-click does not fire, and toggling calls `onToggle(item.key, !item.completed)`. Render the label `text-[14px] font-bold text-white`; when `item.completed`, add `line-through text-white/40` to the label and show the emerald-400 checked state. Use inline SVG for the checkmark (no icon library).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TipPanel.tsx contains `fixed inset-0 z-50 flex justify-end` and `max-w-md`
    - TipPanel returns null when item is null and closes on Escape
    - TipPanel hides the CTA when `action_href` is null and sets `target="_blank"` only for external_url
    - ChecklistItem checkbox handler calls `stopPropagation` so checkbox toggle does not open the panel
    - ChecklistItem applies `line-through text-white/40` to the label when completed
    - `npm run build` completes with zero TypeScript errors
  </acceptance_criteria>
  <done>TipPanel mirrors the ToolSidePanel shell with tip-gated body and conditional CTA; ChecklistItem has an independent checkbox (stopPropagation) and row-click-opens-panel behavior.</done>
</task>

<task type="auto">
  <name>Task 2: ChecklistSection + LaunchpadRoom container with optimistic persistence</name>
  <files>components/launchpad/ChecklistSection.tsx, components/launchpad/LaunchpadRoom.tsx</files>
  <read_first>
    - .planning/phases/05-launchpad-checklist/05-PATTERNS.md § components/launchpad/LaunchpadRoom.tsx (state + side panel wiring; section grouping) and § ChecklistSection
    - .planning/phases/05-launchpad-checklist/05-UI-SPEC.md § ChecklistSection collapse (Before release), § States and Visibility Rules, § Copywriting Contract (section headers, sub-labels, completion counter)
    - .planning/phases/05-launchpad-checklist/05-RESEARCH.md § Common Pitfalls (Pitfall 2 optimistic rollback; Pitfall 3 null release_date = always expanded)
    - app/(artist)/launchpad/page.tsx (section header classes: text-[12px] font-bold uppercase tracking-[.16em] text-lavdim; sub-label text-[13px] text-lavdim)
  </read_first>
  <action>
    Create `components/launchpad/ChecklistSection.tsx` (`'use client'`). Props: section metadata (key, header, subLabel), the section's items, the `onToggle`/`onOpenPanel` callbacks, and an `isReleased` boolean (only meaningful for the before_release section). Render a section header `text-[12px] font-bold uppercase tracking-[.16em] text-lavdim` using the UI-SPEC copy ("Before release", "Week 1 — Release week", "Week 2", "Weeks 3–4") and the sub-label `text-[13px] text-lavdim` ("Pre-release prep", "Algorithm window opens", "Keep the momentum", "Sustain and expand"). Render the item rows via `ChecklistItem`. For the `before_release` section only: when `isReleased` is true, render in collapsed mode by default — a header with a chevron toggle (`aria-expanded`, `aria-controls`) and a "Did you handle this before release?" confirmation block showing items as compact checkboxes; expandable via local `useState`. Non-before sections always render expanded. Collapse uses `transition-all duration-200` on the content area.

    Create `components/launchpad/LaunchpadRoom.tsx` (`'use client'`). Props: `project: { id: string; title: string; release_date: string | null }`, `items: MergedChecklistItem[]`. Hold `items` in `useState` (so optimistic toggles re-render) and `activeItem` in `useState<MergedChecklistItem | null>`. Compute `isReleased = project.release_date ? new Date(project.release_date) < new Date() : false` (null → false, always expanded, RESEARCH Pitfall 3). Group items by section in fixed order [before_release, week_1, week_2, weeks_3_4] and render one `ChecklistSection` per section, passing `isReleased` to the before section only. Render a completion counter "{N} of {total} steps complete" (UI-SPEC copy). Implement `onToggle(key, completed)`: optimistically update the local item, then `fetch('/api/launchpad/' + project.id + '/progress', { method: 'PATCH', body: JSON.stringify({ item_key: key, completed }) })`; on a non-ok response, roll the item state back to its prior value and surface the "Couldn't save your progress — please try again." message (UI-SPEC). Implement `onOpenPanel(item)` to set activeItem. Render `<TipPanel item={activeItem} onClose={() => setActiveItem(null)} />` at the end.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - LaunchpadRoom computes `isReleased` with a null-safe release_date check (null → false)
    - LaunchpadRoom groups items in the fixed order before_release, week_1, week_2, weeks_3_4
    - onToggle does an optimistic update then PATCHes /api/launchpad/[id]/progress and rolls back on error
    - ChecklistSection renders the before_release collapse with `aria-expanded` when isReleased is true
    - Completion counter renders "{N} of {total} steps complete"
    - `npm run build` completes with zero TypeScript errors
  </acceptance_criteria>
  <done>LaunchpadRoom renders 4 ordered sections with the side panel wired and optimistic-with-rollback persistence; ChecklistSection collapses the before-release block post-release.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the per-project page to LaunchpadRoom + add project cards to global /launchpad</name>
  <files>app/(artist)/launchpad/[projectId]/page.tsx, app/(artist)/launchpad/page.tsx</files>
  <read_first>
    - app/(artist)/launchpad/[projectId]/page.tsx (the Plan 02 scaffold — replace the placeholder body)
    - .planning/phases/05-launchpad-checklist/05-PATTERNS.md § app/(artist)/launchpad/[projectId]/page.tsx (multi-query merge; pass merged array to LaunchpadRoom)
    - app/(artist)/launchpad/page.tsx (the EXISTING global page — do NOT restructure the playbook render; only insert cards above it per scope fence)
    - .planning/phases/05-launchpad-checklist/05-UI-SPEC.md § ProjectCards (grid + card classes + CTA copy)
    - .planning/phases/05-launchpad-checklist/05-CONTEXT.md § Scope Fences (do not change the existing global playbook render structure)
  </read_first>
  <action>
    In `app/(artist)/launchpad/[projectId]/page.tsx`: replace the Plan 02 placeholder. After fetching the owner-scoped project (already includes `release_date`), add the two parallel Supabase queries from PATTERNS — `launchpad_checklist_items` select `*` ordered by `sort_order`, and `launchpad_progress` select `item_key, completed, completed_at` filtered by project_id + user_id. Merge into `MergedChecklistItem[]` (item spread + completed/completed_at from the progress map; gate tip_body to approved-only and drop tip_draft/tip_drafted_at/author, mirroring the API route). Render `<LaunchpadRoom project={project} items={merged} />` inside the existing page wrapper. Keep the Topbar from Plan 02.

    In `app/(artist)/launchpad/page.tsx` (the EXISTING global page): per the scope fence, do NOT modify the existing `LaunchpadPage` playbook render (the LAUNCH_PHASES/PLAYBOOK map, info banner, all classes stay intact). ONLY insert a project-cards section ABOVE the existing content. Fetch the user's `vault_projects` (id, title) server-side; for each, render a card using UI-SPEC ProjectCards classes — grid `grid grid-cols-2 gap-3` (single column below 640px), each card `rounded-[14px] border border-hair bg-card px-[18px] py-4 flex items-center justify-between` with the project title (`font-bold text-white`) and an "Open Launchpad →" link (`text-[13px] font-bold text-white bg-card2 rounded-[10px] px-4 py-[10px]`) to `/launchpad/{project.id}`. Separate the cards from the playbook with a `border-b border-hair mb-7` divider. If the user has no projects, render nothing in the cards slot (the playbook still shows). Convert the page to an async server component if it is not already (to fetch projects), preserving the existing static playbook render verbatim.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - [projectId]/page.tsx renders `<LaunchpadRoom` with a merged items array and the project (including release_date)
    - [projectId]/page.tsx gates tip_body to approved-only and does not pass tip_draft to the client
    - app/(artist)/launchpad/page.tsx still renders the existing LAUNCH_PHASES/PLAYBOOK map unchanged
    - app/(artist)/launchpad/page.tsx renders project cards linking to `/launchpad/{id}` above the playbook
    - `npm run build` completes with zero TypeScript errors
  </acceptance_criteria>
  <done>The per-project page renders the live LaunchpadRoom from merged data and the global page shows project cards above the untouched playbook render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client component → progress PATCH | Toggles must persist only to the calling user's rows |
| server component → client props | tip_draft/author must not be passed into the client component tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information Disclosure | server component → LaunchpadRoom props | high | mitigate | The server component merge gates tip_body to approved-only and drops tip_draft/tip_drafted_at/author before passing items to the client (mirrors the Plan 03 API) |
| T-05-01 | Elevation of Privilege | progress PATCH from client | high | mitigate | PATCH route (Plan 03) forces user_id to the session; client never sends user_id. RLS enforces ownership |
| T-05-SC | Tampering | npm installs | low | accept | No package installs in this plan (@dnd-kit installed in Plan 02; not used by artist components) |
</threat_model>

<verification>
- `npm run build` passes with zero TypeScript errors
- Visiting `/launchpad/[projectId]` shows 4 sections in order with items grouped by week
- Clicking a row opens the TipPanel; clicking the checkbox toggles without opening the panel
- Checking an item then hard-reloading preserves the checked state (LAUNCH-04)
- An item with `tip_approved=false` shows no tip body in the panel (LAUNCH-03)
- A project with a past release_date shows the Before release section collapsed
- The global `/launchpad` page shows project cards above the unchanged playbook
</verification>

<success_criteria>
- LAUNCH-01: per-project room with week-grouped checklist visible
- LAUNCH-02: items grouped/ordered by week; action CTAs navigate (internal) or open new tab (external)
- LAUNCH-03: only approved tips render
- LAUNCH-04: completion persists across reloads
</success_criteria>

<output>
Create `.planning/phases/05-launchpad-checklist/05-04-SUMMARY.md` when done
</output>
