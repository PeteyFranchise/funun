---
phase: 05-launchpad-checklist
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-05"]
files_modified:
  - components/admin/ChecklistAdmin.tsx
  - components/admin/TipsAdmin.tsx
  - app/(admin)/checklist/page.tsx
  - app/(admin)/tips/page.tsx
autonomous: true
requirements: [LAUNCH-03, LAUNCH-05]
must_haves:
  truths:
    - "Admin can add, edit, and hard-delete checklist items from /admin/checklist with inline forms (no modals)"
    - "Admin can reorder checklist rows by drag-and-drop on desktop (dnd-kit) and via up/down arrow buttons on mobile; both persist sort_order"
    - "Admin can edit a tip draft, then approve (publishes to artists) or reject (discards draft) from /admin/tips"
    - "Both admin pages render server-side behind the is_admin layout gate and load their data via the Plan 05 admin APIs"
  artifacts:
    - "components/admin/ChecklistAdmin.tsx (dnd-kit sortable CRUD)"
    - "components/admin/TipsAdmin.tsx (approve/reject)"
    - "app/(admin)/checklist/page.tsx + app/(admin)/tips/page.tsx wired to the components"
  key_links:
    - "ChecklistAdmin handleDragEnd → arrayMove → PATCH /api/admin/checklist { order } (atomic reorder)"
    - "ChecklistAdmin mobile up/down arrows persist the same sort_order PATCH"
    - "TipsAdmin approve/reject → PATCH /api/admin/tips/[itemKey] { action }"
---

<objective>
Build the admin UI: `ChecklistAdmin` (item list with inline add/edit/delete-confirm + dnd-kit drag reorder on desktop and up/down arrows on mobile) and `TipsAdmin` (draft list with editable textarea + approve/reject), then wire both into the `(admin)` pages scaffolded in Plan 02. This completes LAUNCH-05 (admin CRUD/reorder/delete UI) and the admin-facing half of LAUNCH-03 (tip approval UI).

Purpose: This is the in-app admin surface that replaces touching Supabase Studio directly (the core LAUNCH-05 promise).
Output: 2 admin components + both admin pages wired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-launchpad-checklist/05-CONTEXT.md
@.planning/phases/05-launchpad-checklist/05-RESEARCH.md
@.planning/phases/05-launchpad-checklist/05-PATTERNS.md
@.planning/phases/05-launchpad-checklist/05-UI-SPEC.md
</context>

## Artifacts this phase produces

New symbols created in this plan (exclude from drift verification):
- Client components: `ChecklistAdmin`, `TipsAdmin`, and an internal `SortableRow` for dnd-kit
- dnd-kit usage: `DndContext`, `SortableContext`, `useSortable`, `arrayMove`, `CSS.Transform.toString` (from @dnd-kit installed in Plan 02)
- Inline add/edit form and inline delete-confirm patterns
- Admin pages now rendering the client components instead of scaffolds

<tasks>

<task type="auto">
  <name>Task 1: ChecklistAdmin — dnd-kit sortable list with inline add/edit/delete and mobile arrows</name>
  <files>components/admin/ChecklistAdmin.tsx</files>
  <read_first>
    - .planning/phases/05-launchpad-checklist/05-RESEARCH.md § Pattern 2 (dnd-kit Sortable Admin Rows — DndContext/SortableContext/useSortable/arrayMove/CSS.Transform) and § Open Questions Q2 (verify @dnd-kit/sortable v10 API before coding)
    - .planning/phases/05-launchpad-checklist/05-UI-SPEC.md § Admin ChecklistAdmin (drag handle, dragging/drop styles, mobile arrows, inline delete confirm, inline add/edit form), § Copywriting Contract (admin copy), § Accessibility Contracts (drag handle + arrow aria-labels)
    - .planning/phases/05-launchpad-checklist/05-PATTERNS.md § No Analog Found (use RESEARCH dnd-kit pattern), § Tailwind Mobile Breakpoint (hidden md:flex drag handle; flex md:hidden arrows)
    - app/api/admin/checklist/route.ts (the GET/POST/PATCH-reorder contract from Plan 05) and [itemKey]/route.ts (PATCH edit, DELETE)
  </read_first>
  <action>
    Create `components/admin/ChecklistAdmin.tsx` (`'use client'`). Props: `initialItems: ChecklistItem[]`. Hold items in `useState`. Import dnd-kit from `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` (installed Plan 02). VERIFY the @dnd-kit/sortable v10 API against its installed types before coding (RESEARCH Open Q2) — use the names `DndContext`, `closestCenter`, `SortableContext`, `verticalListSortingStrategy`, `arrayMove`, `useSortable`, and `CSS.Transform.toString`.

    Render a `DndContext` (collisionDetection closestCenter, onDragEnd handler) wrapping a `SortableContext` (items = item keys, verticalListSortingStrategy) wrapping one `SortableRow` per item. `SortableRow` uses `useSortable({ id: item.key })`, applies `transform`/`transition` via `CSS.Transform.toString`, shows the dragging style `opacity-60 scale-[1.02] shadow-2xl` when `isDragging`, and renders a 6-dot grip drag handle with `{...attributes} {...listeners}` and `aria-label="Drag to reorder {item.label}"`. The grip is `hidden md:flex` (desktop only); on mobile (`flex md:hidden`) render up/down arrow buttons with `aria-label="Move {item.label} up"` / `"...down"` that call a `move(key, direction)` handler.

    `handleDragEnd(event)`: if `over` exists and differs from `active`, `arrayMove` the local list, then persist by POSTing the FULL reordered list as `{ order: reordered.map((it, idx) => ({ key: it.key, sort_order: idx })) }` to `PATCH /api/admin/checklist` (atomic reorder, Plan 05). `move(key, dir)` does the same swap+persist for mobile.

    Inline add/edit form (NOT a modal — inline expand below the list per UI-SPEC): an "Add checklist item" CTA (`bg-grad` gradient) toggles an inline form with fields Label (required), Section (dropdown of the four section literals), Action type (internal_tool / external_url), Action href, Action label, Sort order. Save POSTs to `/api/admin/checklist` (add) or PATCHes `/api/admin/checklist/[itemKey]` (edit) and refreshes local state. Inline delete-confirm (NOT a modal): each row has a Delete control that expands an inline `role="alert"` block with the copy "Delete this item? All artist progress on it will be permanently removed. This cannot be undone." plus [Cancel] / [Delete item]; confirming DELETEs `/api/admin/checklist/[itemKey]` and removes the row locally. Use UI-SPEC copy strings exactly. On any mutation error show "Couldn't save — please try again." Empty state: "No checklist items yet. Add the first one above."
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ChecklistAdmin imports DndContext/SortableContext/useSortable from @dnd-kit and arrayMove for reorder
    - handleDragEnd persists the FULL reordered list as `{ order: [...] }` to PATCH /api/admin/checklist
    - Mobile up/down arrow buttons exist (`flex md:hidden`) with move aria-labels and persist the same way
    - Add/edit uses an inline expand form (no modal) and posts to the Plan 05 endpoints
    - Delete uses an inline `role="alert"` confirm with the exact UI-SPEC copy and DELETEs by key
    - `npm run build` completes with zero TypeScript errors
  </acceptance_criteria>
  <done>ChecklistAdmin lists items with dnd-kit drag reorder (desktop) + arrow reorder (mobile), inline add/edit/delete, and persists every change through the Plan 05 admin APIs.</done>
</task>

<task type="auto">
  <name>Task 2: TipsAdmin — editable draft list with approve/reject</name>
  <files>components/admin/TipsAdmin.tsx</files>
  <read_first>
    - .planning/phases/05-launchpad-checklist/05-UI-SPEC.md § Admin TipsAdmin (card layout, editable textarea, approve/reject pill styles, approved/rejected states, author + date metadata), § Copywriting Contract (Approve/Reject copy, empty tips state), § Accessibility Contracts (approve/reject aria-labels)
    - app/api/admin/tips/route.ts and [itemKey]/route.ts (the GET drafts + approve/reject PATCH contract from Plan 05)
    - .planning/phases/05-launchpad-checklist/05-PATTERNS.md § No Analog Found (approve/reject two-action pattern)
  </read_first>
  <action>
    Create `components/admin/TipsAdmin.tsx` (`'use client'`). Props: `initialDrafts` — array of `{ key, label, tip_draft, tip_body, tip_approved, author, tip_drafted_at }`. Hold drafts in `useState`. Render each as a card `bg-card border-hair rounded-[14px] p-4` containing the item label, an editable `textarea` (resize-none, auto-grows with content) pre-filled with `tip_draft`, the author (`ai`/`admin`) and drafted date as `text-[11px] text-lavdim` metadata, and two pill buttons: Approve (`emerald-400/10 border-emerald-400/30 text-emerald-400`, aria-label "Approve tip for {key}") and Reject (`rose-400/10 border-rose-400/30 text-rose-400`, aria-label "Reject tip for {key}"). Approve PATCHes `/api/admin/tips/[key]` with `{ action: 'approve', tip_text: <current textarea value> }` so the admin's edits become the published tip; on success mark the card approved (`bg-emerald-400/[0.04] border-emerald-400/20`, show "Approved" badge, hide buttons). Reject PATCHes `{ action: 'reject' }`; mark the card rejected and filter it out of the default list (show under a "Show rejected" toggle per UI-SPEC States table). Empty state copy: "No tip drafts pending approval." On error: "Couldn't save — please try again."
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TipsAdmin renders an editable textarea per draft pre-filled with tip_draft
    - Approve PATCHes `/api/admin/tips/[key]` with action=approve and the edited tip_text
    - Reject PATCHes action=reject and removes the card from the default list
    - Approved cards show the "Approved" badge and hide the buttons
    - Approve/Reject buttons carry the UI-SPEC aria-labels
    - `npm run build` completes with zero TypeScript errors
  </acceptance_criteria>
  <done>TipsAdmin lets the admin edit and approve a draft (publishing it to artists) or reject it (discarding the draft) via the Plan 05 tip endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ChecklistAdmin + TipsAdmin into the (admin) pages</name>
  <files>app/(admin)/checklist/page.tsx, app/(admin)/tips/page.tsx</files>
  <read_first>
    - app/(admin)/checklist/page.tsx and app/(admin)/tips/page.tsx (the Plan 02 scaffolds — replace the placeholder body)
    - app/api/admin/checklist/route.ts and app/api/admin/tips/route.ts (data shape returned to seed the components)
    - .planning/phases/05-launchpad-checklist/05-PATTERNS.md § app/(admin)/checklist/page.tsx and § tips/page.tsx (role-match server component fetch pattern)
    - .planning/phases/05-launchpad-checklist/05-RESEARCH.md § Anti-Patterns (server component should query Supabase directly, not fetch its own API route)
  </read_first>
  <action>
    In `app/(admin)/checklist/page.tsx` (async server component, already behind the layout is_admin gate): fetch all `launchpad_checklist_items` directly via `createServiceClient()` ordered by `sort_order` (query Supabase directly in the server component — do NOT `fetch('/api/admin/...')`, per RESEARCH anti-pattern), then render `<ChecklistAdmin initialItems={items} />` inside the existing `flex-1 px-9 py-[30px]` wrapper with the "Checklist Items" heading. Keep the layout gate as the auth boundary (the page itself runs inside it); the underlying mutations still go through the Plan 05 admin APIs which re-verify is_admin.

    In `app/(admin)/tips/page.tsx`: fetch the pending drafts directly via `createServiceClient()` — `launchpad_checklist_items` rows where `tip_draft IS NOT NULL` selecting `key, label, tip_draft, tip_body, tip_approved, author, tip_drafted_at` ordered by `tip_drafted_at` — and render `<TipsAdmin initialDrafts={drafts} />` under the "Tips" heading in the same wrapper.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - checklist/page.tsx fetches items via createServiceClient() directly (not via fetch of its own API) and renders `<ChecklistAdmin`
    - tips/page.tsx fetches tip_draft IS NOT NULL rows and renders `<TipsAdmin`
    - Both pages keep their headings and run inside the (admin) layout gate
    - `npm run build` completes with zero TypeScript errors
  </acceptance_criteria>
  <done>Both admin pages load their data server-side and render the live admin components, completing the in-app admin surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin page render → service-role read | The admin pages read with the service client; access is gated by the (admin) layout |
| admin client component → admin APIs | All mutations route through Plan 05 endpoints that re-verify is_admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Elevation of Privilege | admin pages + client mutations | critical | mitigate | Pages render only inside the (admin) layout is_admin gate (Plan 02); every mutation the client components fire is independently re-verified by the Plan 05 admin API routes (defense in depth — layout gate is not the sole control) |
| T-05-09 | Tampering | client-driven reorder/delete | high | mitigate | Reorder, edit, and delete go through the Plan 05 service-role routes which validate the admin gate, the field allowlist, and the itemKey regex; the client cannot bypass them |
| T-05-SC | Tampering | @dnd-kit usage | low | accept | @dnd-kit packages were installed and audited OK in Plan 02; this plan only imports them — no new installs |
</threat_model>

<verification>
- `npm run build` passes with zero TypeScript errors
- As an admin, /admin/checklist lists seeded items; add/edit/delete and drag-reorder all persist and survive reload
- Reordering rows updates the artist-facing order on /launchpad/[projectId]
- /admin/tips shows pending drafts; editing + Approve makes the tip appear in the artist TipPanel; Reject discards it
- As a non-admin, both pages redirect (layout gate) and the underlying APIs 403
</verification>

<success_criteria>
- LAUNCH-05: admin can add/edit/reorder/delete items in-app without Supabase Studio
- LAUNCH-03 (admin UI): admin approves/rejects tip drafts; approved tips reach artists
- Desktop drag + mobile arrow reorder both persist; delete cascades to progress
</success_criteria>

<output>
Create `.planning/phases/05-launchpad-checklist/05-06-SUMMARY.md` when done
</output>
