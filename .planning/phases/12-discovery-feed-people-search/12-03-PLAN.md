---
phase: 12-discovery-feed-people-search
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01-PLAN.md
  - 12-02-PLAN.md
files_modified:
  - app/api/green-room/feed/route.ts
  - lib/green-room/feed-query.ts
  - __tests__/green-room-feed-api.test.ts
autonomous: true
requirements:
  - FEED-01
  - FEED-02
  - FEED-03
  - FEED-12
  - FEED-15
must_haves:
  truths:
    - "The feed API returns typed cards, not raw database rows."
    - "Every tab applies server-side visibility, block, and audience checks."
    - "Curated placements are explicitly labeled."
---

# Plan 12-03: Feed Read API

<objective>
Build `/api/green-room/feed` with tab modes, cursor pagination, transparent ranking labels, and curated placement insertion.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add feed query service</name>
  <acceptance_criteria>
    - Supports `for_you`, `following`, `discover`, and `opportunities` modes.
    - Applies viewer identity, relationships, block state, and visibility filters before returning cards.
    - Cursor pagination is deterministic and stable.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add API route</name>
  <acceptance_criteria>
    - Requires authenticated session.
    - Validates tab and cursor inputs.
    - Returns `{ cards, nextCursor }` with typed card fields only.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Insert featured/sponsored placements safely</name>
  <acceptance_criteria>
    - Placements are labeled as featured, sponsored, partner, program, or opportunity.
    - Placements obey visibility and active-window checks.
    - No targeting, billing, or self-serve ad surfaces are introduced.
  </acceptance_criteria>
</task>

</tasks>

<verify>
  <automated>npm test -- --runInBand __tests__/green-room-feed-api.test.ts</automated>
</verify>

