---
phase: 12-discovery-feed-people-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/green-room/feed.ts
  - __tests__/green-room-feed.test.ts
autonomous: true
requirements:
  - FEED-01
  - FEED-02
  - FEED-03
  - FEED-05
  - FEED-06
must_haves:
  truths:
    - "Visibility is an explicit domain value, not inferred from UI state."
    - "Draft posts are only visible to the author."
    - "Custom audience rules are validated and bounded before persistence exists."
    - "Ranking labels explain why a card appears."
  artifacts:
    - lib/green-room/feed.ts
    - __tests__/green-room-feed.test.ts
---

# Plan 12-01: Feed Domain Contracts

<objective>
Create the pure TypeScript contract layer for The Green Room feed before any schema, API, or UI work. This plan defines post types, visibility modes, reaction values, linked-object types, custom audience normalization, visibility checks, repost eligibility, and transparent ranking labels.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-discovery-feed-people-search/12-CONTEXT.md
@.planning/phases/12-discovery-feed-people-search/12-RESEARCH.md
@.planning/phases/12-discovery-feed-people-search/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add feed domain constants and type guards</name>
  <files>lib/green-room/feed.ts</files>
  <acceptance_criteria>
    - Post type, visibility, reaction, linked-object, and tab values are exported as readonly arrays plus union types.
    - Unknown values fail type guards.
    - Values match the Phase 12 planning scope.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add custom audience normalization</name>
  <files>lib/green-room/feed.ts</files>
  <acceptance_criteria>
    - Relationship, role, genre, location, and specific-person targeting are supported.
    - Empty and over-broad custom audiences are rejected.
    - Audience complexity is capped before it reaches database/API work.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add pure visibility and repost checks</name>
  <files>lib/green-room/feed.ts</files>
  <acceptance_criteria>
    - Blocked users never pass visibility checks.
    - Drafts are owner-only.
    - Followers, connections, public, and custom visibility are evaluated server-side-ready.
    - Reposts fail when resharing is disabled, the original is unavailable, visibility is draft/custom, or the viewer cannot see the original.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add ranking score and explanation labels</name>
  <files>lib/green-room/feed.ts</files>
  <acceptance_criteria>
    - Ranking uses transparent relationship, relevance, recency, engagement, and placement inputs.
    - Explanation labels are stable strings suitable for cards.
    - Sponsored/featured labels remain explicit and do not imply self-serve ads.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Add RED-to-green unit tests</name>
  <files>__tests__/green-room-feed.test.ts</files>
  <acceptance_criteria>
    - Tests cover value guards, audience normalization, draft privacy, block exclusion, custom audience matching, repost safety, and ranking labels.
    - Tests run without Supabase or network access.
  </acceptance_criteria>
</task>

</tasks>

<verify>
  <automated>npm test -- --runInBand __tests__/green-room-feed.test.ts</automated>
</verify>

