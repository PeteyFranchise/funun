---
phase: 23-buyer-onboarding-login-register
plan: 08
type: execute
wave: 4
depends_on: ["23-01", "23-02", "23-03", "23-04", "23-05", "23-06", "23-07"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [SYNC-01, SYNC-02, SYNC-03, SYNC-04, SYNC-05, SYNC-06, SYNC-07, SYNC-08, SYNC-09, SYNC-10]
must_haves:
  truths:
    - "Migration 092 is pushed to production by the owner (LOCAL=REMOTE) — never by an executor"
    - "The full onboarding loop is smoke-verified live: register → pending_onboarding account → admin queue → AE assign → status active → login → gated browse"
    - "Phase 23 requirement IDs (SYNC-01..SYNC-10) are registered in REQUIREMENTS.md"
  artifacts:
    - .planning/REQUIREMENTS.md
  key_links:
    - "human migration push ↔ every downstream buyer_orgs.status/qualifying-field read (nothing works live until 092 lands)"
---

<objective>
The **blocking human checkpoint** (locked directive 7): the owner pushes migration 092 via Codex, runs the live smoke of the full onboarding loop, and the phase's requirement IDs are registered. Mirrors Phase 25's 25-07 human-gate.

Purpose: Migrations are human-gated — authored, text-tested, and committed by executors, but NEVER pushed by an agent. Everything built in 23-01..23-07 references `buyer_orgs.status`/qualifying fields that do not exist in prod until the owner pushes.
Output: Migration 092 live (LOCAL=REMOTE), a green live smoke, and SYNC-01..SYNC-10 registered.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-buyer-onboarding-login-register/23-RESEARCH.md
@supabase/migrations/092_buyer_org_lead_fields.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gate (full suite + build)</name>
  <files>.planning/phases/23-buyer-onboarding-login-register/23-08-SUMMARY.md</files>
  <action>
Before the human checkpoint, run the full automated gate and record results in the SUMMARY: `npm test` (full suite green, including migration-092, register, catalog-query, postSignInPath, and buyer-orgs route tests) and `npm run build` (implicit `tsc --noEmit` clean). Confirm `supabase migration list` shows 092 as LOCAL-only (not yet pushed) so the owner knows exactly what will apply. Do NOT run `supabase db push`.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Full suite + build green; migration 092 confirmed LOCAL-only and ready for the owner to push.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Owner pushes migration 092 + live onboarding-loop smoke</name>
  <action>
Owner-only step (executors never run `supabase db push`): review + push migration 092 via Codex, then run the live onboarding-loop smoke below and report results. This is a blocking human gate — the phase does not complete until the owner confirms 092 is live (LOCAL=REMOTE) and the smoke passes.
  </action>
  <what-built>
Migration 092 (buyer_orgs status + lead-qualifying fields + column grants) is drafted, text-tested, and committed. The full buyer onboarding surface is built: public /sync landing + browse, the Login/Register modal, the public register endpoint with real lead routing, buyer password auth, and the AE onboarding-completion detail page. Nothing is live until the migration is pushed.
  </what-built>
  <how-to-verify>
1. Review `supabase/migrations/092_buyer_org_lead_fields.sql` and push it via Codex (owner's normal migration-push path). Confirm `supabase migration list` shows LOCAL=REMOTE through 092.
2. Live smoke the loop:
   a. Logged out, visit `/sync` → landing renders; `/sync/catalog` → browse + play a simulated preview; click License → the Login/Register modal opens.
   b. In the modal, Register a test company (company/contact/work email/phone/role/use-case) → confirmation state; verify a `buyer_orgs` row exists with `status='pending_onboarding'` and a first `approver` + `is_org_admin` member.
   c. As leadership (`/admin/buyer-orgs?unassigned=1`), the new lead appears in the unassigned queue; a `lead_routed` notification fired; assign an AE.
   d. Click the lead_routed / AE notification → `/admin/client-partners/[orgId]` opens (no 404) showing the qualifying fields → "Mark onboarding complete" → status flips to `active`.
   e. Complete the buyer's password setup (recovery link) → set a password → land on `/sync/catalog` (NOT /vault). Log out, reopen the modal, sign in with email/password → land on `/sync/catalog`.
   f. Confirm the assigned AE sees the org in `/admin/my-client-partners` and cross-company request visibility works for a second member.
3. Note any email steps that no-op (Resend not configured in prod) — the in-app notification + admin queue are the source of truth; email delivery is a separate owner setup.
  </how-to-verify>
  <resume-signal>Type "approved" once 092 is live (LOCAL=REMOTE) and the smoke passes, or describe issues.</resume-signal>
  <verify><human-check>Owner confirms LOCAL=REMOTE through 092 and the register→onboarding-complete→login→gated-browse loop passes live</human-check></verify>
  <done>Migration 092 live; the full Model A onboarding loop is smoke-verified end-to-end; any email no-ops (Resend unconfigured) noted.</done>
</task>

<task type="auto">
  <name>Task 2: Register Phase 23 requirement IDs</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
After the checkpoint is approved, add a `## v1.4 — Phase 23: Buyer Onboarding (Model A) & /sync Unification Requirements` section to REQUIREMENTS.md registering SYNC-01..SYNC-10 with the behavior descriptions used across this phase's plans, and add the corresponding rows to the Traceability table (mirror the Phase 25 TEAM-01..09 registration format at 25-07). Mark each as Complete only after the smoke verified it (SYNC-10 cross-company visibility = satisfied by existing migration 081 RLS; note the deferred approver-only oversight UI). Use `gsd-tools query requirements` helpers if available; otherwise edit REQUIREMENTS.md with scoped Edits (never a whole-file Write).
  </action>
  <verify>
    <automated>grep -q "SYNC-01" .planning/REQUIREMENTS.md && grep -q "SYNC-10" .planning/REQUIREMENTS.md && echo REGISTERED</automated>
  </verify>
  <done>SYNC-01..SYNC-10 registered in REQUIREMENTS.md with traceability rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → production DB | An executor must never `supabase db push`; only the owner applies migration 092 |
| pre-live built code → prod schema | Code assuming status/qualifying columns must not run against prod until 092 lands |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-26 | Tampering | agent pushing migration 092 out-of-band | high | mitigate | Blocking-human checkpoint; executors run tests/build only, never db push |
| T-23-27 | Denial of Service | code live before schema (missing columns) | high | mitigate | Wave-4 gating: this checkpoint is the last wave; the owner pushes 092 before real traffic |
| T-23-SC | Tampering | supply-chain (package installs) | n/a | accept | No npm/pip/cargo installs in this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `supabase migration list` shows LOCAL=REMOTE through 092 (owner-run)
- Live smoke loop passes (register → onboarding-complete → login → gated browse)
- `grep "SYNC-0" .planning/REQUIREMENTS.md` shows the registered IDs
</verification>

<success_criteria>
Migration 092 is live, the full Model A onboarding loop is verified end-to-end, and Phase 23's requirements are registered.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- Migration 092 applied to production (owner-pushed)
- `.planning/REQUIREMENTS.md` — SYNC-01..SYNC-10 registered
- `23-08-SUMMARY.md` — smoke results + Resend/email dependency note
</artifacts>

<output>
Create `.planning/phases/23-buyer-onboarding-login-register/23-08-SUMMARY.md` when done
</output>
