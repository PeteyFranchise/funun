---
phase: 23-buyer-onboarding-login-register
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/095_buyer_org_lead_fields.sql
  - __tests__/migration-095.test.ts
  - lib/buyers/schema.ts
autonomous: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "buyer_orgs carries a status lifecycle (pending_onboarding default → active) plus lead-qualifying columns"
    - "buyer's own session client can read status + use_case; raw CRM contact fields stay staff-only"
    - "migration file carries the HUMAN-GATED banner and is never pushed by an executor"
  artifacts:
    - supabase/migrations/095_buyer_org_lead_fields.sql
    - __tests__/migration-095.test.ts
    - lib/buyers/schema.ts
  key_links:
    - "BuyerOrg TS type ↔ migration 095 column-grant allowlist (must stay in lockstep — column-privilege doctrine)"
    - "status CHECK constraint ↔ STAFF_EDITABLE validation in 23-06"
---

<objective>
Add the "created-but-not-onboarded" lifecycle and lead-qualifying data to `buyer_orgs` via migration **095**, and extend the shared `BuyerOrg` type to match the buyer-readable column-grant allowlist.

Purpose: Every downstream plan (register endpoint, AE onboarding surface, buyer portal status display) depends on `buyer_orgs.status` and the qualifying fields existing with a deliberate per-column privilege decision (RESEARCH Pitfall 6 — this codebase's #1 recurring migration mistake).
Output: Drafted + text-tested migration 095 (NOT pushed — human-gated, per locked directive 7), plus the `BuyerOrg` type extension.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-buyer-onboarding-login-register/23-RESEARCH.md
@supabase/migrations/090_buyer_orgs_ae_assignment.sql
@supabase/migrations/080_buyer_orgs_members.sql
@__tests__/migration-089-090.test.ts
@lib/buyers/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 095 — buyer_orgs status + lead-qualifying fields + per-column grants</name>
  <files>supabase/migrations/095_buyer_org_lead_fields.sql</files>
  <action>
Create `supabase/migrations/095_buyer_org_lead_fields.sql`. Open with the standing HUMAN-GATED banner block that every migration since 058 carries (copy the exact banner shape from `090_buyer_orgs_ae_assignment.sql` — a comment header stating the migration is drafted + text-tested only and must never be pushed by an agent; the owner reviews and pushes via Codex at the 23-08 checkpoint; do not edit migrations 080–094).

`ALTER TABLE public.buyer_orgs` adding, all nullable/additive except status:
- `status text NOT NULL DEFAULT 'pending_onboarding' CHECK (status IN ('pending_onboarding','active'))` — the account-created-but-not-onboarded lifecycle (RESEARCH Answer 2; every existing row backfills to `pending_onboarding` via the default, which is acceptable — beta has no real buyers yet per STATE.md).
- `use_case text` — company-level qualifying answer (agency / film-tv / brand / other), free-form to match this codebase's phone/free-text precedent (RESEARCH: no validation library).
- `contact_name text`, `contact_email text`, `contact_phone text`, `contact_role text` — the registrant's CRM/lead-qualifying contact info the AE needs on the onboarding-detail surface without an `auth.admin.getUserById` round-trip.
- `source text NOT NULL DEFAULT 'register' CHECK (source IN ('register','sales_rep'))` — the two-doors discriminant (RESEARCH Answer 4: same pipeline, admin-copy distinction only).

Column-privilege decision (Pitfall 6 — decide per-field explicitly, never "grant everything" or "revoke everything"):
- Extend migration 080's authenticated SELECT allowlist so the buyer's own session client can read `status` and `use_case` (the portal shows "your account is being set up by your AE" during `pending_onboarding`). Emit `GRANT SELECT (status, use_case) ON public.buyer_orgs TO authenticated;` (additive to the existing `id, name, is_personal, verified, created_at` grant — Postgres column grants accumulate).
- Deliberately DO NOT grant `contact_name`, `contact_email`, `contact_phone`, `contact_role`, `source` to `authenticated` — these are staff-only lead/CRM fields (mirrors how `ae_user_id`/`verified_at`/`created_by` are kept private). Add a `COMMENT ON COLUMN` on `status` documenting the lifecycle and on the contact_* group documenting the staff-only intent.

End with `NOTIFY pgrst, 'reload schema';`.
  </action>
  <verify>
    <automated>test -f supabase/migrations/095_buyer_org_lead_fields.sql && grep -q "pending_onboarding" supabase/migrations/095_buyer_org_lead_fields.sql && echo OK</automated>
  </verify>
  <done>Migration 095 exists with the status CHECK, all six qualifying columns, the explicit two-tier column grant (status+use_case to authenticated; contact_*/source private), the HUMAN-GATED banner, and the pgrst reload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structural text-test for migration 095</name>
  <files>__tests__/migration-095.test.ts</files>
  <behavior>
    - Asserts the file adds `status ... CHECK (status IN ('pending_onboarding','active'))` with the `pending_onboarding` default
    - Asserts each qualifying column is added: use_case, contact_name, contact_email, contact_phone, contact_role, source
    - Asserts `GRANT SELECT (status, use_case) ON public.buyer_orgs TO authenticated`
    - Asserts the file does NOT grant any contact_* column to authenticated (staff-only privacy assertion)
    - Asserts the HUMAN-GATED banner phrase and the `NOTIFY pgrst` reload are present
  </behavior>
  <action>
Create `__tests__/migration-095.test.ts` mirroring `__tests__/migration-089-090.test.ts` exactly: `readFileSync` the migration, then a `describe('095')` block of `toContain`/`toMatch` structural assertions per the behavior list. For the staff-only privacy check, assert the granted-column list does not include a contact field (e.g. slice the `GRANT SELECT (...)` clause and assert it excludes `contact_phone`). This is a pure text assertion (ts-jest transpile-only) — no DB connection, mirroring the immutable-migration-text convention.
  </action>
  <verify>
    <automated>npm test -- __tests__/migration-095.test.ts</automated>
  </verify>
  <done>`npm test -- __tests__/migration-095.test.ts` passes; the test fails if any column, the CHECK, or the grant split is edited away.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the BuyerOrg type with the buyer-readable lifecycle fields</name>
  <files>lib/buyers/schema.ts</files>
  <action>
Extend the `BuyerOrg` type in `lib/buyers/schema.ts` with the two columns the authenticated session client can now read: `status: 'pending_onboarding' | 'active'` and `use_case: string | null`. Add a `BuyerOrgStatus` exported union (`'pending_onboarding' | 'active'`) and a `BUYER_ORG_STATUS_VALUES` const tuple so the 23-06 staff-edit route validates against a single source of truth (mirrors `BUYER_ROLE_VALUES`). Do NOT add contact_*/source to `BuyerOrg` — those are staff-only and not in the authenticated grant; keep the shared type honest to the column-privilege allowlist (the existing comment in this file already documents that discipline). Update the type's leading comment to note status/use_case were added by migration 095's grant extension.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "lib/buyers/schema" | grep -q '^0$' && echo TYPECLEAN</automated>
  </verify>
  <done>`BuyerOrg` includes `status` + `use_case`; `BuyerOrgStatus` + `BUYER_ORG_STATUS_VALUES` exported; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buyer session client → buyer_orgs columns | A buyer's own PostgREST reads must see status/use_case but never staff-only CRM fields |
| executor → production DB | An executor must never apply this migration (human-gated push) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Information Disclosure | new buyer_orgs contact_* columns | high | mitigate | Explicit column-grant allowlist — only status+use_case granted to authenticated; contact_*/source stay private (Pitfall 6) |
| T-23-02 | Tampering | migration applied out-of-band by an agent | high | mitigate | HUMAN-GATED banner; push deferred to the 23-08 blocking-human checkpoint; migration 080–094 left untouched |
| T-23-03 | Tampering | status column accepts arbitrary values | medium | mitigate | CHECK (status IN ('pending_onboarding','active')) at the DB layer + TS union mirror |
</threat_model>

<verification>
- `npm test -- __tests__/migration-095.test.ts` green
- `npx tsc --noEmit` clean for lib/buyers/schema.ts
- Manual: `grep "GRANT SELECT" supabase/migrations/095_buyer_org_lead_fields.sql` shows status+use_case only
</verification>

<success_criteria>
Migration 095 drafted + text-tested (not pushed), the buyer-readable columns are typed, and the staff-only fields are provably excluded from the authenticated grant.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `supabase/migrations/095_buyer_org_lead_fields.sql` — status lifecycle + qualifying fields + per-column grants (human-gated)
- `__tests__/migration-095.test.ts` — structural text-test
- `lib/buyers/schema.ts` — `BuyerOrg` + `BuyerOrgStatus` + `BUYER_ORG_STATUS_VALUES`
</artifacts>

<output>
Create `.planning/phases/23-buyer-onboarding-login-register/23-01-SUMMARY.md` when done
</output>
