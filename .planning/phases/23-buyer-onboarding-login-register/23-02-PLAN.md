---
phase: 23-buyer-onboarding-login-register
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/sync/layout.tsx
  - app/sync/page.tsx
  - app/sync/catalog/page.tsx
  - app/sync/shortlists/page.tsx
  - app/sync/requests/page.tsx
  - app/sync/requests/new/page.tsx
  - app/sync/requests/[id]/page.tsx
  - app/sync/access/page.tsx
  - components/buyer/BuyerTopNav.tsx
  - components/buyer/CatalogBrowserLight.tsx
  - lib/deals/request-target.ts
  - lib/deals/shortlists.ts
  - lib/deals/catalog-query.ts
  - lib/buyers/org.ts
autonomous: true
requirements: [SYNC-09]
must_haves:
  truths:
    - "The entire buyer world is reachable under /sync/* — landing at /sync, browse at /sync/catalog, portal routes moved"
    - "A public /sync landing page exists (hero, value prop, featured teaser, Browse + Log in CTAs)"
    - "The /sync route group's layout does NOT force-redirect logged-out visitors (public browse is architecturally possible)"
    - "No /buyers/* page route literal remains in code; internal names (buyer_orgs, /api/buyer/*, components/buyer) are unchanged"
  artifacts:
    - app/sync/layout.tsx
    - app/sync/page.tsx
    - app/sync/catalog/page.tsx
    - app/sync/access/page.tsx
  key_links:
    - "/sync layout (non-gating) ↔ public catalog browse in 23-03 (browse cannot live under the auth-gating (buyer-portal) layout)"
    - "route literals ↔ every redirect()/href across buyer code (mechanical find/replace, then a manual verify pass)"
---

<objective>
Execute the ROADMAP-locked **/sync unification**: rename the buyer namespace `/buyers/* → /sync/*` and add a public **/sync landing page**. Mechanical + reversible (locked directive 2).

Purpose: Two reasons this comes first, not last: (1) a public catalogue browse (23-03) CANNOT live under the `(buyer-portal)` layout, which force-redirects every unauthenticated visitor — the `/sync` group needs its own NON-gating layout; (2) building the modal/landing/browse directly on final `/sync` paths avoids a later re-sweep. Internal names stay unchanged (`buyer_orgs`, `buyer_members`, `/api/buyer/*`, `components/buyer/*`) — this is a route/label rename only (RESEARCH "Critical Update" §2, "Runtime State Inventory").
Output: `/sync/*` route tree, a public `/sync` landing page, a non-gating `/sync` layout, and zero `/buyers/*` page-route literals.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-buyer-onboarding-login-register/23-RESEARCH.md
@$HOME/.claude/gsd-core/references/ui-brand.md
@app/(buyer-portal)/layout.tsx
@app/(buyer-portal)/buyers/catalog/page.tsx
@app/buyers/access/page.tsx
@components/buyer/BuyerTopNav.tsx
@components/buyer/fnbl-theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the buyer route tree to /sync/* with a non-gating layout</name>
  <files>app/sync/layout.tsx, app/sync/catalog/page.tsx, app/sync/shortlists/page.tsx, app/sync/requests/page.tsx, app/sync/requests/new/page.tsx, app/sync/requests/[id]/page.tsx, app/sync/access/page.tsx</files>
  <action>
Move the buyer-portal pages from `app/(buyer-portal)/buyers/*` and `app/buyers/access/*` to a real `/sync` path segment (NOT a parenthesised route group — the URL must be `/sync/...`):
- `app/(buyer-portal)/buyers/catalog/page.tsx` → `app/sync/catalog/page.tsx`
- `app/(buyer-portal)/buyers/shortlists/page.tsx` → `app/sync/shortlists/page.tsx`
- `app/(buyer-portal)/buyers/requests/{page,new/page,[id]/page}.tsx` → `app/sync/requests/...`
- `app/buyers/access/page.tsx` → `app/sync/access/page.tsx`
- Delete the now-empty `app/(buyer-portal)/` group and `app/buyers/`.

Create `app/sync/layout.tsx` by adapting `app/(buyer-portal)/layout.tsx`, with ONE critical behavioral change: the layout must NOT redirect logged-out visitors — the `/sync` surface is public-browse-with-gated-engagement. Keep the `.fnbl` shell, `FNBL_CSS` single-injection, and the server-side theme cookie read. Render `BuyerTopNav` only when a buyer session exists (an anon visitor sees the catalogue's own public header — 23-03 handles the anon nav). Per-authenticated-route gating (requests/shortlists still require a buyer session) moves INTO those pages: each keeps its own `getUser()` + `buyer_members` membership check and redirects to `/sync/access` when absent — do not rely on the layout for it anymore. Update every in-file `redirect('/buyers/access')` to `redirect('/sync/access')`.

Update `app/sync/access/page.tsx`: keep the magic-link form but revise the stale "invite-only during beta" copy (RESEARCH "State of the Art" — it contradicts the new public register flow); frame it as "Already have an account? Sign in" since public register now exists via the modal.
  </action>
  <verify>
    <automated>test -f app/sync/catalog/page.tsx && test -f app/sync/layout.tsx && test -f app/sync/access/page.tsx && test ! -d "app/(buyer-portal)" && echo MOVED</automated>
  </verify>
  <done>All buyer pages live under `app/sync/*`; the `(buyer-portal)` group and `app/buyers/` are gone; the `/sync` layout does not force-redirect anon users; authenticated sub-routes self-gate to `/sync/access`.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep every /buyers/* route literal to /sync/*</name>
  <files>components/buyer/BuyerTopNav.tsx, components/buyer/CatalogBrowserLight.tsx, lib/deals/request-target.ts, lib/deals/shortlists.ts, lib/deals/catalog-query.ts, lib/buyers/org.ts</files>
  <action>
Update every `/buyers/` PAGE-route string literal to `/sync/` — `href=`, `redirect()`, and any constructed nav/link path. Grep `/buyers/` across `app components lib` first; the known page-literal call sites are `components/buyer/BuyerTopNav.tsx` (Browse/Shortlists/Requests hrefs), `components/buyer/CatalogBrowserLight.tsx`, `lib/deals/request-target.ts`, `lib/deals/shortlists.ts`, `lib/deals/catalog-query.ts`, `lib/buyers/org.ts`, plus the admin components that link into the buyer world. Do a CAREFUL manual pass, NOT a blind sed: DO NOT touch API routes (`/api/buyer/*` internal names stay), DO NOT rename the `components/buyer/` directory, DO NOT touch `buyer_orgs`/`buyer_members` identifiers, and DO NOT edit immutable migration text or migration-content tests that legitimately reference historical paths. After the sweep, re-grep for `"/buyers/` string literals and confirm only non-page references (internal API paths, DB identifiers, doc comments) remain.
  </action>
  <verify>
    <automated>! grep -rn '"/buyers/\|href="/buyers\|redirect(./buyers/' app components lib 2>/dev/null | grep -v '/api/buyer' | grep -q . && echo SWEPT</automated>
  </verify>
  <done>No `/buyers/` page-route literal remains (API `/api/buyer/*`, DB identifiers, and doc comments untouched); nav links resolve under `/sync/*`.</done>
</task>

<task type="auto">
  <name>Task 3: Public /sync landing page</name>
  <files>app/sync/page.tsx</files>
  <action>
Create the public `/sync` marketing landing page (server component, `.fnbl` light theme, Funūn wordmark per ui-brand.md — adopt one of the 5 wordmark explorations noted in CONTEXT `canonical_refs`). Sections: a hero ("Funūn Sync" — license independent music), a value-prop row, a featured-catalogue teaser (reuse `loadCatalogPage`/`SAMPLE_CATALOG_ROWS` for a small non-interactive strip; do not require auth), and two CTAs — **Browse** (link to `/sync/catalog`) and **Log in / Request access**. For Wave 1, render the "Log in / Request access" CTA as a plain link to `/sync/access`; 23-07 upgrades it to open the Login/Register modal in place. Keep the page fully renderable with no session. Mount within the non-gating `/sync` layout.
  </action>
  <verify>
    <automated>test -f app/sync/page.tsx && grep -q "sync/catalog" app/sync/page.tsx && echo LANDING</automated>
  </verify>
  <done>`/sync` renders logged-out with a hero, value prop, featured teaser, and Browse + Log in/Request-access CTAs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logged-out visitor → /sync/* | Public landing + browse are reachable with no session; authenticated sub-routes self-gate |
| route literal rename → auth gating | A missed gate on requests/shortlists could expose buyer data to anon users |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-04 | Elevation of Privilege | requests/shortlists losing the layout gate | high | mitigate | Each authenticated sub-page keeps its own getUser()+buyer_members check redirecting to /sync/access; verified by a manual pass |
| T-23-05 | Spoofing | broken nav literal 404/dead-link after rename | low | mitigate | Manual verify pass + re-grep for residual /buyers/ page literals |
| T-23-06 | Information Disclosure | accidental rename of /api/buyer/* internal paths | medium | accept→mitigate | Explicit "do not touch API/DB identifiers" rule + grep filter excluding /api/buyer |
</threat_model>

<verification>
- `npm run build` compiles the new `/sync` tree (route type-check passes)
- `grep -rn '/buyers/' app components lib | grep -v '/api/buyer'` returns only DB identifiers / doc comments
- Manual: each of requests / shortlists / requests/new self-redirects to `/sync/access` when logged out
</verification>

<success_criteria>
Buyer world lives under `/sync/*` with a public landing page and a non-gating layout; internal names untouched; no orphaned `/buyers/*` page routes.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `app/sync/layout.tsx` — non-gating buyer shell
- `app/sync/page.tsx` — public Funūn Sync landing page
- `app/sync/catalog|shortlists|requests|access/*` — moved portal routes
- Route-literal sweep across buyer nav + lib
</artifacts>

<output>
Create `.planning/phases/23-buyer-onboarding-login-register/23-02-SUMMARY.md` when done
</output>
