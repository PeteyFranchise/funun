---
phase: 23-buyer-onboarding-login-register
plan: 06
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - app/api/admin/buyer-orgs/[id]/route.ts
  - app/api/admin/buyer-orgs/route.ts
  - app/(admin)/admin/client-partners/[orgId]/page.tsx
  - components/admin/ClientPartnerDetail.tsx
  - app/api/admin/buyer-orgs/[id]/route.test.ts
autonomous: true
requirements: [SYNC-06, SYNC-10]
must_haves:
  truths:
    - "An AE opens /admin/client-partners/[orgId], sees the qualifying fields captured at register, and can advance status pending_onboarding → active"
    - "The lead_routed / ae_assigned notification link (/admin/client-partners/[orgId]) resolves to a real page, not a 404"
    - "Leadership sees an unassigned-lead queue (buyer_orgs WHERE ae_user_id IS NULL); AE/BD stay scoped to their assigned companies"
    - "Cross-company purchase visibility already works via migration 081 RLS — every org member sees every member's requests"
  artifacts:
    - app/(admin)/admin/client-partners/[orgId]/page.tsx
    - components/admin/ClientPartnerDetail.tsx
    - app/api/admin/buyer-orgs/[id]/route.ts
  key_links:
    - "notifications link '/admin/client-partners/${orgId}' ↔ the new detail page (Pitfall 4)"
    - "status transition ↔ STAFF_EDITABLE_BUYER_ORG_FIELDS + BUYER_ORG_STATUS_VALUES CHECK"
---

<objective>
Give AEs the surface to **complete onboarding**: a Client Partner detail page at `/admin/client-partners/[orgId]` (the target every Phase 25 lead/AE notification already links to — currently a 404, RESEARCH Pitfall 4), the `pending_onboarding → active` status transition, and leadership's unassigned-lead queue. Confirm cross-company purchase visibility is already delivered by existing RLS.

Purpose: Locked directives 1, 5 — wire AE + lead routing FOR REAL against the shipped Phase 25 infra. The AE literally needs a company-detail surface to see the qualifying fields and flip status, and the notification links point here.
Output: The detail page + status-transition edit + unassigned queue, plus a verification that migration 081's cross-company RLS satisfies the spend-approver visibility decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-buyer-onboarding-login-register/23-RESEARCH.md
@app/api/admin/buyer-orgs/[id]/route.ts
@app/api/admin/buyer-orgs/route.ts
@app/(admin)/admin/my-client-partners/page.tsx
@lib/staff/notifications.ts
@lib/admin/gate.ts
@lib/staff/scope.ts
@lib/buyers/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Status transition via the existing staff-edit allowlist</name>
  <files>app/api/admin/buyer-orgs/[id]/route.ts, app/api/admin/buyer-orgs/[id]/route.test.ts</files>
  <behavior>
    - A staff PATCH with `{ status: 'active' }` on an assigned org updates status and is audited
    - A PATCH with an invalid status (not in pending_onboarding|active) is rejected 400 (no write)
    - Scope denial (AE not assigned to the org) still returns 404, not 403 (unchanged)
    - `name` editing still works (existing behavior preserved)
  </behavior>
  <action>
Extend `STAFF_EDITABLE_BUYER_ORG_FIELDS` in `app/api/admin/buyer-orgs/[id]/route.ts` from `['name']` to include `status` and `use_case` (the file's own comment already anticipates this: "Phase 23 is expected to add company-profile columns... extend this list then"). Add explicit validation for `status`: only accept a value in `BUYER_ORG_STATUS_VALUES` (imported from `lib/buyers/schema.ts` — the single source of truth from 23-01), rejecting anything else with a 400 BEFORE the DB write, mirroring the existing string-trim validation loop. Keep `ae_user_id` OUT of the allowlist (assignment stays the separate leadership-only `.../ae` route). Keep the `isAssignedToOrg` 404-on-scope-denial and the unconditional `logStaffAction` audit. Add a route test file (mirror existing admin-route test conventions) covering the four behaviors.
  </action>
  <verify>
    <automated>npm test -- app/api/admin/buyer-orgs/[id]/route.test.ts</automated>
  </verify>
  <done>Staff can flip status to active (assigned + audited); invalid status is 400; name-edit + 404-scope-denial preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Client Partner detail page (the notification-link target)</name>
  <files>app/(admin)/admin/client-partners/[orgId]/page.tsx, components/admin/ClientPartnerDetail.tsx</files>
  <action>
Create `app/(admin)/admin/client-partners/[orgId]/page.tsx` (server component) — the surface `lib/staff/notifications.ts` links to for `lead_routed`/`ae_assigned`/`ae_unassigned` (Pitfall 4: this page does not exist yet, so every such notification currently 404s). Follow `app/(admin)/admin/my-client-partners/page.tsx`'s gate pattern exactly: explicit `getStaffRole` check; leadership sees any org, AE/BD only an org assigned to them (`ae_user_id === user.id`) else redirect/`notFound()` (scope denial parity with the API's 404 discipline). Read the org via the service client with an EXPLICIT column list including the staff-only qualifying fields (`status, use_case, contact_name, contact_email, contact_phone, contact_role, source, ae_user_id`) plus its members. Render a `components/admin/ClientPartnerDetail.tsx` client component showing: company header, status badge, the captured qualifying/lead fields, member list, and a "Mark onboarding complete" action that PATCHes `{ status: 'active' }` to `/api/admin/buyer-orgs/[id]` (Task 1) — disabled/hidden once already active. Keep it in the Team Console light/dark idiom used by the other admin pages.
  </action>
  <verify>
    <automated>test -f "app/(admin)/admin/client-partners/[orgId]/page.tsx" && grep -q "status" components/admin/ClientPartnerDetail.tsx && echo DETAIL</automated>
  </verify>
  <done>`/admin/client-partners/[orgId]` renders the qualifying fields + status, offers "Mark onboarding complete", and resolves the previously-404 notification links; scope-gated like my-client-partners.</done>
</task>

<task type="auto">
  <name>Task 3: Leadership unassigned-lead queue + confirm cross-company visibility</name>
  <files>app/api/admin/buyer-orgs/route.ts</files>
  <action>
Add an unassigned-queue view for leadership on `GET /api/admin/buyer-orgs`: accept an optional `?unassigned=1` query param that, for a leadership caller, adds `.is('ae_user_id', null)` to the existing query (the admin queue is just `buyer_orgs WHERE ae_user_id IS NULL` — RESEARCH anti-pattern: do NOT build a parallel queue table). Leave the existing AE/BD scoping (`.eq('ae_user_id', user.id)`) untouched — non-leadership callers never see the unassigned pool. Include `status` in the returned `ORG_COLUMNS` so the queue can show `pending_onboarding`.

Also VERIFY (no new code needed) that cross-company purchase visibility (SYNC-10 / CONTEXT spend-approver decision) is already delivered: migration 081's `license_requests_select_buyer_org_or_project_owner` RLS returns every org member's requests to every org member, and `app/(buyer-portal)/buyers/requests/page.tsx` (now `/sync/requests`) renders it. Record in the SUMMARY that this decision is satisfied by existing RLS + the OrgRequestDashboard; a dedicated approver-only spend-oversight UI is a deferred UI-only enhancement (no new RLS).
  </action>
  <verify>
    <automated>grep -q "unassigned" app/api/admin/buyer-orgs/route.ts && grep -q "ae_user_id', null" app/api/admin/buyer-orgs/route.ts && echo QUEUE</automated>
  </verify>
  <done>Leadership can list the unassigned lead pool; AE/BD scoping unchanged; cross-company visibility confirmed satisfied by migration 081 (recorded in SUMMARY).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff caller → Client Partner detail/edit | Only leadership (any org) or the assigned AE/BD (their orgs) may view/edit |
| status transition → buyer_orgs | Only a valid status in the CHECK set may be written |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-18 | Elevation of Privilege | AE editing an org not assigned to them | high | mitigate | isAssignedToOrg 404-on-denial on both the API and the detail page; leadership-only unassigned queue |
| T-23-19 | Tampering | arbitrary status value written | medium | mitigate | Validate against BUYER_ORG_STATUS_VALUES before write + DB CHECK |
| T-23-20 | Information Disclosure | qualifying/CRM fields leaking to non-staff | high | mitigate | Detail page reads via service client behind requireStaff/getStaffRole; contact_* stay out of the authenticated grant (23-01) |
| T-23-21 | Repudiation | untracked status/edit actions | low | mitigate | Unconditional logStaffAction audit on every edit |
</threat_model>

<verification>
- `npm test -- app/api/admin/buyer-orgs/[id]/route.test.ts` green
- `npm run build` clean
- Manual (at 23-08 smoke): click a lead_routed notification → detail page renders (no 404) → Mark onboarding complete → status flips to active
</verification>

<success_criteria>
AE-assisted onboarding is real: a working detail surface, a status transition, a leadership unassigned queue, and confirmed cross-company visibility — all against shipped Phase 25 infra with no stub.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `app/(admin)/admin/client-partners/[orgId]/page.tsx` + `components/admin/ClientPartnerDetail.tsx` — AE onboarding-completion surface (resolves Pitfall 4)
- `app/api/admin/buyer-orgs/[id]/route.ts` (+ test) — status transition via extended allowlist
- `app/api/admin/buyer-orgs/route.ts` — leadership unassigned-lead queue
</artifacts>

<output>
Create `.planning/phases/23-buyer-onboarding-login-register/23-06-SUMMARY.md` when done
</output>
