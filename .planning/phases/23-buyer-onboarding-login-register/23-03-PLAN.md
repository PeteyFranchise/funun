---
phase: 23-buyer-onboarding-login-register
plan: 03
type: execute
wave: 2
depends_on: ["23-02"]
files_modified:
  - lib/deals/catalog-query.ts
  - lib/deals/catalog-query.test.ts
  - app/sync/catalog/page.tsx
autonomous: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "A logged-out visitor can browse the catalogue and play simulated previews at /sync/catalog with no auth wall"
    - "loadCatalogPage never throws for an anonymous caller (no empty-string uuid into .eq/.or)"
    - "An authenticated buyer still gets the identical block-excluded, RLS-scoped catalogue"
  artifacts:
    - lib/deals/catalog-query.ts
    - lib/deals/catalog-query.test.ts
    - app/sync/catalog/page.tsx
  key_links:
    - "loadCatalogPage(buyerUserId: string | null) ↔ loadBlockedIds (skip when null — blocks are meaningless for an anon visitor)"
    - "public catalog page ↔ CatalogBrowserLight isPublic (engagement gating wired in 23-07)"
---

<objective>
Open the catalogue to **public (logged-out) browsing** at `/sync/catalog`, fixing RESEARCH Pitfall 3 (the anonymous-visitor bug) so the SSR page degrades gracefully instead of throwing a Postgres `invalid input syntax for type uuid`.

Purpose: Locked directive 4 — logged-out visitors browse + play simulated previews; any engagement (shortlist/License) pops the modal (wired in 23-07). RESEARCH confirms preview audio is entirely simulated (a `setInterval` playhead, no real asset) so exposing it publicly is a pure UI-state question (Answer 6).
Output: A null-safe `loadCatalogPage`, its first test file, and a `/sync/catalog` page that branches on session presence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-buyer-onboarding-login-register/23-RESEARCH.md
@lib/deals/catalog-query.ts
@lib/green-room/discover.ts
@app/sync/catalog/page.tsx
@lib/deals/request-payload.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Null-safe loadCatalogPage for anonymous callers</name>
  <files>lib/deals/catalog-query.ts, lib/deals/catalog-query.test.ts</files>
  <behavior>
    - Given `buyerUserId = null`, loadCatalogPage resolves WITHOUT calling loadBlockedIds (no `.or` against a uuid column) and returns the rights-ready public cards
    - Given a real `buyerUserId`, block exclusion still runs and blocked owners are filtered out (unchanged authenticated behavior)
    - The empty-projects short-circuit and all existing filter behavior are preserved
  </behavior>
  <action>
Widen `loadCatalogPage`'s signature to `buyerUserId: string | null`. When `buyerUserId` is null (anonymous visitor), SKIP the `loadBlockedIds(service, buyerUserId)` call entirely and treat `blockedIds` as an empty set — a visitor with no account can neither block nor be blocked, and `blocks` rows only ever reference real `auth.users` ids (RESEARCH Pitfall 3, fix option (a)). Keep the single-implementation doctrine documented in the file's header — do not fork a parallel public function; both the SSR page and any future public API path share this one fix.

Create `lib/deals/catalog-query.test.ts` (no existing test file for this module — RESEARCH Wave-0 gap). Mock the service client (mirror the existing route/lib mocking convention; `loadBlockedIds` is imported from `lib/green-room/discover` — mock it and assert it is NOT called when `buyerUserId` is null, and IS called with a real id otherwise). Cover the anonymous path (no throw, returns cards) and the authenticated path (block exclusion still applied).
  </action>
  <verify>
    <automated>npm test -- lib/deals/catalog-query.test.ts</automated>
  </verify>
  <done>`loadCatalogPage(service, null, ...)` does not call loadBlockedIds and does not throw; authenticated block exclusion unchanged; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Open /sync/catalog to logged-out visitors</name>
  <files>app/sync/catalog/page.tsx</files>
  <action>
Branch the `/sync/catalog` server component on session presence (the page now lives under the non-gating `/sync` layout from 23-02, so it is reachable with no session):
- Resolve `user` via `createServerClient().auth.getUser()`.
- If `user` is null: load `loadCatalogPage(service, null, filter, 1)` and render `<CatalogBrowserLight rows={...} isPublic />` (NOT embedded — the public visitor gets CatalogBrowserLight's own self-contained header with its Login button; the buyer-portal `BuyerTopNav` is only for authenticated members).
- If `user` exists but has no `buyer_members` row: keep the existing redirect to `/sync/access` (an authenticated non-buyer has no catalogue home).
- If `user` is a buyer: render the existing authenticated path (`loadCatalogPage(service, user.id, ...)`, `embedded`) exactly as today.
- Where there are no live rights-ready rows, keep the existing `SAMPLE_CATALOG_ROWS` fallback so the design shows in full for public visitors.
  </action>
  <verify>
    <automated>grep -q "isPublic" app/sync/catalog/page.tsx && grep -q "loadCatalogPage(service, null" app/sync/catalog/page.tsx && echo PUBLICBROWSE</automated>
  </verify>
  <done>Logged-out visitors reach `/sync/catalog`, browse, and play simulated previews; buyers keep the authenticated experience; non-buyer sessions redirect to `/sync/access`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor → catalogue query | An unauthenticated SSR read must not throw and must not expose non-public/blocked content |
| service-role read → public render | Only rights-ready, is_public, visible projects reach an anon visitor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-07 | Denial of Service | loadCatalogPage anon path | high | mitigate | Null-safe branch skips the uuid `.or` filter — the crash-for-anon bug is removed (Pitfall 3) |
| T-23-08 | Information Disclosure | public catalogue leaking non-public projects | high | mitigate | Existing is_public + isProfileVisibleTo + isRightsReady gates run unchanged for anon (only block-exclusion is skipped, which is inert for a no-account viewer) |
| T-23-09 | Information Disclosure | simulated preview audio exposure | low | accept | No real audio asset exists (setInterval playhead only) — nothing to expose (Answer 6) |
</threat_model>

<verification>
- `npm test -- lib/deals/catalog-query.test.ts` green
- `npm run build` clean
- Manual: visit `/sync/catalog` in a logged-out browser → catalogue renders, previews play, no 500
</verification>

<success_criteria>
Public catalogue browse works logged-out without throwing; authenticated buyer behavior and privacy gates are unchanged.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `lib/deals/catalog-query.ts` — `buyerUserId: string | null` anon-safe branch
- `lib/deals/catalog-query.test.ts` — first coverage for the module (anon + authed paths)
- `app/sync/catalog/page.tsx` — session-branching public/authenticated render
</artifacts>

<output>
Create `.planning/phases/23-buyer-onboarding-login-register/23-03-SUMMARY.md` when done
</output>
