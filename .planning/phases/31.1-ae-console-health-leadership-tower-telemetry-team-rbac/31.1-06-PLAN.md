---
phase: 31.1-ae-console-health-leadership-tower-telemetry-team-rbac
plan: 06
type: execute
wave: 3
depends_on: ["31.1-01", "31.1-04"]
files_modified:
  - app/api/admin/buyer-orgs/[id]/ae/route.ts
  - app/api/admin/buyer-orgs/[id]/ae/route.test.ts
  - lib/client-partners/contacts.ts
  - lib/client-partners/onboarding.ts
  - lib/client-partners/onboarding.test.ts
  - components/admin/AssignAePanel.tsx
  - components/admin/OnboardingTasksPanel.tsx
  - components/admin/ClientPartnersRoom.tsx
autonomous: true
requirements: [R6, R7, R8]

must_haves:
  truths:
    - "Assignment requires a handoff note — the assign route rejects an empty/whitespace note with 400, and the Assign button is disabled until the note is filled (D-31.1-05)"
    - "Leadership can self-assign a client to themselves — the target-role check accepts leadership in addition to ae/bd; the client lands in the assigner's My tab (D-31.1-05)"
    - "Assigning fires the D-07 structural handoff: a relationship-log entry (kind='assignment'), an auto-created onboarding_tasks row in the AE's queue (seeded default checklist + the handoff note), an in-app notification, and a best-effort intro email (D-07)"
    - "Email/notification delivery failure NEVER blocks the assignment — the ae_user_id authority write commits first, then all side effects run best-effort (D-07/R8)"
    - "The assign panel shows AE search + each AE's load + their book's health mix (D-31.1-05)"
    - "Open onboarding tasks for the signed-in AE surface on their My view (D-07/RESEARCH Open Q2)"
  artifacts:
    - "extended app/api/admin/buyer-orgs/[id]/ae/route.ts (+ route.test.ts)"
    - "widened lib/client-partners/contacts.ts appendRelationshipLog (kind now includes 'assignment', optional meta)"
    - "lib/client-partners/onboarding.ts (SEEDED_ONBOARDING_CHECKLIST, insertOnboardingTask, listOpenOnboardingTasks) + test"
    - "components/admin/AssignAePanel.tsx + components/admin/OnboardingTasksPanel.tsx (mounted in ClientPartnersRoom)"
  key_links:
    - "The ae route writes onboarding_tasks (assignee_id = the AE) — OnboardingTasksPanel reads them on the My view"
    - "appendRelationshipLog(kind:'assignment') is the audit trail every reassignment writes (SPEC R7 prohibition)"
  prohibitions:
    - "The intro email / notification / onboarding-task side effects MUST NOT block or fail the assignment — the authority write commits first, side effects are best-effort (.catch) (D-07)"
    - "An AE MUST NOT be able to self-assign — the route stays leadership-only; only leadership self-assign is permitted (SPEC prohibition R7)"
    - "Assignment MUST NOT proceed without a handoff note (server 400 + disabled button)"
    - "AssignAePanel / OnboardingTasksPanel ('use client') MUST NOT receive function props from the RSC page — data + string action paths only (Pitfall 1)"
  user_setup:
    - service: resend
      why: "The D-07 intro email uses Resend; the prod sender is unverified (project memory) — email is best-effort by design and must not block the assignment"
---

<objective>
Turn the existing leadership-only AE-assign route into the full D-07 structural handoff and build the richer assign panel (D-31.1-05): required handoff note, leadership self-assign, an auto-created onboarding task in the AE's queue, an in-app notification, and a best-effort intro email — none of which may block the assignment. Surface the AE's open onboarding tasks on their My view.

Purpose: R6/R7/R8 — leadership routes the book and every handoff carries context and lands as real work in the AE's queue.
Output: the extended ae route + handoff wiring, the onboarding-task helpers, the assign panel, and the My-view onboarding surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-CONTEXT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-RESEARCH.md
@.planning/design/phase-31.1-leadership-tower-mockup.html
@app/api/admin/buyer-orgs/[id]/ae/route.ts
@lib/client-partners/contacts.ts
@lib/staff/notifications.ts
@lib/notifications/index.ts
@components/admin/ClientPartnersRoom.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-07 handoff — extend the ae route + onboarding helpers + widen appendRelationshipLog</name>
  <files>app/api/admin/buyer-orgs/[id]/ae/route.ts, app/api/admin/buyer-orgs/[id]/ae/route.test.ts, lib/client-partners/contacts.ts, lib/client-partners/onboarding.ts, lib/client-partners/onboarding.test.ts</files>
  <read_first>
    - app/api/admin/buyer-orgs/[id]/ae/route.ts — the existing route in full: requireStaff(['leadership']), the ae/bd target-role check to widen, prior-AE read, best-effort dual notification pattern
    - lib/client-partners/contacts.ts — appendRelationshipLog (currently kind 'note'|'conversation'; widen to include 'assignment' + optional meta) and the RelationshipLogKind type
    - lib/staff/notifications.ts — buildAeAssignedNotification / buildAeUnassignedNotification
    - lib/notifications/index.ts — createNotification args (email, sendEmailCopy) for the best-effort intro email
    - supabase/migrations/128_ae_console_health.sql (plan 01) — onboarding_tasks columns
    - .planning/phases/31.1-.../31.1-RESEARCH.md § "Assignment + D-07 Handoff Wiring"
  </read_first>
  <behavior>
    - PATCH with a null/empty/whitespace note → 400, and ae_user_id is NOT changed
    - PATCH assigning to a valid AE with a note → ae_user_id updated, one onboarding_tasks row created (assignee_id=AE, seeded checklist, handoff_note=note), one kind='assignment' relationship-log entry, notification attempted
    - PATCH self-assigning to a leadership user succeeds (target-role check accepts leadership)
    - PATCH to a non-staff / artist / buyer target → 400
    - a thrown intro-email / notification error does NOT fail the response (200 still returned, ae_user_id committed)
    - a non-leadership caller → 403 (unchanged)
  </behavior>
  <action>
    Widen `appendRelationshipLog` in lib/client-partners/contacts.ts to accept kind 'assignment' (in addition to 'note'|'conversation') and an optional meta object, writing meta when provided. Create `lib/client-partners/onboarding.ts` exporting a SEEDED_ONBOARDING_CHECKLIST constant (the 31.1 seeded default SOP items as plain strings/objects), `insertOnboardingTask(service, { orgId, assigneeId, createdBy, title, handoffNote })`, and `listOpenOnboardingTasks(service, assigneeId)`. Extend the ae route PATCH: (1) require a non-empty trimmed `note` in the body → 400 if missing; (2) widen the target-role guard to accept 'leadership' as well as 'ae'/'bd' so a leader can self-assign; (3) after the ae_user_id update commits (the authority action), run in this order as best-effort side effects (each wrapped so it cannot fail the response): appendRelationshipLog(kind:'assignment', body:note), insertOnboardingTask(seeded checklist + note), createNotification(buildAeAssignedNotification, with sendEmailCopy + the AE email for the intro email), and the existing prior-AE unassigned notification. Keep logStaffAction. Route file exports only handlers + config. Write route.test.ts + onboarding.test.ts first (RED) covering <behavior> (mock the service client + createNotification; simulate an email throw), then implement.
  </action>
  <verify>
    <automated>npm test -- app/api/admin/buyer-orgs lib/client-partners/onboarding.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- app/api/admin/buyer-orgs lib/client-partners/onboarding.test.ts` passes
    - A test asserts an empty note → 400 with no ae_user_id write
    - A test asserts a thrown email/notification error still returns 200 with ae_user_id committed and the onboarding task created
    - A test asserts leadership self-assign succeeds and a non-staff target → 400
  </acceptance_criteria>
  <done>The assign route is the full D-07 structural handoff: required note, self-assign, seeded onboarding task, audit log, and best-effort email/notification that never block the assignment.</done>
</task>

<task type="auto">
  <name>Task 2: Assign panel + onboarding-tasks surface (mounted in ClientPartnersRoom)</name>
  <files>components/admin/AssignAePanel.tsx, components/admin/OnboardingTasksPanel.tsx, components/admin/ClientPartnersRoom.tsx</files>
  <read_first>
    - .planning/design/phase-31.1-leadership-tower-mockup.html — the assign panel (AE search + per-AE load + book health mix + the required note; Assign disabled until filled) and the My/All layout
    - components/admin/ClientPartnersRoom.tsx (plan 04) — the client wrapper + its data/string props + the row-action slot to wire the Assign trigger into
    - lib/client-partners/coverage.ts (plan 03) — AeCoverage (load + health mix) shape for the panel's per-AE display
    - lib/client-partners/onboarding.ts (Task 1) — the open-task shape OnboardingTasksPanel renders
  </read_first>
  <action>
    Create `components/admin/AssignAePanel.tsx` ('use client'): an AE search/select fed by a plain AE list prop (each with name + load + health mix from AeCoverage), a REQUIRED handoff-note textarea, and an Assign button disabled until the note is non-empty; on submit it PATCHes the string action path /api/admin/buyer-orgs/{id}/ae with { ae_user_id, note } and refreshes via router.refresh(). Create `components/admin/OnboardingTasksPanel.tsx` ('use client') rendering the signed-in AE's open onboarding tasks (title, handoff note, seeded checklist) from a plain prop. Wire both into ClientPartnersRoom: the Assign panel opens from the tower row action / company header (leadership only), and OnboardingTasksPanel renders on the My tab. Pass the AE list, coverage, and open-tasks data down from the RSC page (plan 04) as plain serializable props — no function props cross the boundary; build the PATCH inside the client component.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean
    - AssignAePanel disables Assign until the note textarea is non-empty (client-side guard mirroring the server 400)
    - AssignAePanel + OnboardingTasksPanel are 'use client' and receive only data + the string action path (no function props from the RSC page)
    - OnboardingTasksPanel renders on the My tab; the Assign panel is reachable only in the leadership tower
  </acceptance_criteria>
  <done>Leadership assigns via the richer panel (AE load + health mix + required note), and the AE sees their handoff tasks on their My view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leadership client → ae assign route | Only leadership may (re)assign; an AE must never self-assign |
| assign write → email/notification/task side effects | Delivery failures must not roll back the authority write |
| RSC page → AssignAePanel/OnboardingTasksPanel | Serialization boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-assign-elev | Elevation (V4) | ae assign route | high | mitigate | Route stays requireStaff(['leadership']); target-role widened only to admit leadership self-assign (still ae/bd/leadership) — an AE cannot self-assign |
| T-31.1-email-dos | DoS (availability) | intro email / notification / task side effects | high | mitigate | ae_user_id write commits first; all side effects run best-effort (.catch), so a Resend/notification failure cannot 500 the handoff (D-07) |
| T-31.1-mass-assign | Tampering (V5) | ae route body / relationship-log meta | medium | mitigate | Explicit note/ae_user_id validation (UUID + non-empty note); appendRelationshipLog author_user_id is server-set, never client-suppliable |
| T-31.1-rsc-func-prop | DoS/availability (V1) | assign / onboarding panels props | high | mitigate | Data + string action paths only; PATCH built client-side; refresh via router.refresh() (Pitfall 1) |
| T-31.1-SC | Tampering (supply chain) | npm installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `npm test -- app/api/admin/buyer-orgs lib/client-partners/onboarding.test.ts` green; `npx tsc --noEmit` clean.
- Manual (owner, post-push): assign a client with a note; confirm the assignment + onboarding task + notification succeed even if the intro email does not send (Resend prod sender unverified).
</verification>

<success_criteria>
- Leadership routes the book via a context-carrying handoff that lands as real work in the AE's queue, with email strictly best-effort.
</success_criteria>

<output>
Create `.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-06-SUMMARY.md` when done.
</output>
