---
phase: 31.1-ae-console-health-leadership-tower-telemetry-team-rbac
plan: 07
type: execute
wave: 3
depends_on: ["31.1-01", "31.1-04"]
files_modified:
  - app/api/admin/client-partners/[orgId]/game-plan/route.ts
  - app/api/admin/client-partners/[orgId]/game-plan/route.test.ts
  - lib/client-partners/game-plan.ts
  - lib/client-partners/game-plan.test.ts
  - components/admin/GamePlanPanel.tsx
  - components/admin/ClientWorkspace.tsx
  - app/(admin)/admin/clients/[personId]/page.tsx
autonomous: true
requirements: [R14]

must_haves:
  truths:
    - "The Game Plan is a saved per-account prep doc: topics (themes) each carrying open-ended questions, persisted per buyer_org and reloaded on the next visit (R14/D-31.1-06)"
    - "Suggested topics in 31.1 are seeded defaults + custom + Selects context (dynamic Playbook/Plays sourcing is explicitly deferred to 31.2) (D-31.1-06)"
    - "On/after the call the AE checks off covered topics; 'Log conversation' writes a kind='conversation' relationship-log entry recording 'X of N covered' + notes (R14/D-31.1-06)"
    - "Logging a call with 0 topics checked is allowed and records '0 of N covered' — never a silent blank (SPEC R14 boundary edge)"
    - "Access is own-book scoped: an AE not assigned to the org gets 404 (not 403), reusing canAccessOrgContacts (SPEC prohibition R5/R6)"
  artifacts:
    - "app/api/admin/client-partners/[orgId]/game-plan/route.ts (GET load, PUT save, POST log-conversation) + test"
    - "lib/client-partners/game-plan.ts (SEEDED_GAME_PLAN_TOPICS, coveredSummary, GamePlanTopic type) + test"
    - "components/admin/GamePlanPanel.tsx mounted in ClientWorkspace (person view)"
  key_links:
    - "The game-plan route reads/writes the game_plans row (one per org, migration 128) and appends to client_relationship_log via appendRelationshipLog(kind:'conversation')"
    - "GamePlanPanel mounts in ClientWorkspace mode='person' (the individual client view per D-31.1-06)"
  prohibitions:
    - "Do NOT source topics dynamically from the Playbook/Plays — 31.1 ships seeded defaults + custom + Selects context only (dynamic sourcing is 31.2)"
    - "'Log conversation' MUST NOT write a silent blank — a 0-covered call records '0 of N covered'"
    - "An out-of-book AE MUST NOT read or write another AE's client Game Plan — 404 via canAccessOrgContacts"
    - "GamePlanPanel ('use client') MUST NOT receive function props from its RSC parent — data + string action paths only (Pitfall 1)"
---

<objective>
Build the per-account call Game Plan (R14/D-31.1-06): a saved prep doc of topics carrying open-ended questions, checked off on the call, then logged to the relationship log as "X of N covered" + notes. Topics come from seeded defaults + custom + Selects context (dynamic Playbook sourcing is 31.2).

Purpose: give the AE pre-call planning and a structured conversation log in the individual client view.
Output: the game-plan route + pure helpers, and the GamePlanPanel mounted in the person workspace.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-CONTEXT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-RESEARCH.md
@.planning/design/phase-31-game-plan.html
@app/(admin)/admin/clients/[personId]/page.tsx
@components/admin/ClientWorkspace.tsx
@lib/client-partners/contacts.ts
@app/api/admin/client-partners/[orgId]/relationship-log/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Game Plan route (load/save/log) + seeded topics + coveredSummary</name>
  <files>app/api/admin/client-partners/[orgId]/game-plan/route.ts, app/api/admin/client-partners/[orgId]/game-plan/route.test.ts, lib/client-partners/game-plan.ts, lib/client-partners/game-plan.test.ts</files>
  <read_first>
    - app/api/admin/client-partners/[orgId]/relationship-log/route.ts — the requireStaff gate + canAccessOrgContacts 404 pattern + appendRelationshipLog usage to mirror
    - lib/client-partners/contacts.ts — canAccessOrgContacts (own-book 404), appendRelationshipLog(kind:'conversation')
    - supabase/migrations/128_ae_console_health.sql (plan 01) — game_plans (topics jsonb, one-per-org unique index)
    - .planning/design/phase-31-game-plan.html — the topic/question structure and the "Log conversation" action
    - .planning/phases/31.1-.../31.1-CONTEXT.md D-31.1-06 (seeded + custom + Selects context; dynamic sourcing is 31.2)
  </read_first>
  <behavior>
    - GET returns the org's game_plans row (topics), or a default seeded set when none exists yet
    - PUT persists the edited topics (upsert one-per-org) for an in-book caller
    - POST log-conversation with 3 of 5 topics marked done → appends a kind='conversation' relationship-log entry whose body records "3 of 5 covered" + notes, and (per D-31.1-06) clears/retires the logged plan
    - POST with 0 covered → records "0 of N covered" (never a blank)
    - an out-of-book AE (not assigned) → 404 on GET/PUT/POST
  </behavior>
  <action>
    Create `lib/client-partners/game-plan.ts` exporting a `GamePlanTopic` type (title, questions[], done), a SEEDED_GAME_PLAN_TOPICS constant (the 31.1 seeded default themes + open-ended questions), and a pure `coveredSummary(topics): { covered, total, text }` returning e.g. "X of N covered". Create `app/api/admin/client-partners/[orgId]/game-plan/route.ts` with GET (load or seed), PUT (save topics; zod-validated topic shape, upsert on the one-per-org index), and POST (log-conversation: build coveredSummary, appendRelationshipLog(kind:'conversation', body = summary text + notes), then retire the plan). Every handler runs requireStaff() then loads the org and applies canAccessOrgContacts → 404 when out of book (mirror the relationship-log route). Route file exports only handlers + config. Write game-plan.test.ts (coveredSummary boundaries incl. 0-of-N) + route.test.ts (in-book vs 404, log appends the summary) first (RED), then implement.
  </action>
  <verify>
    <automated>npm test -- app/api/admin/client-partners lib/client-partners/game-plan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- app/api/admin/client-partners lib/client-partners/game-plan.test.ts` passes
    - coveredSummary(0 done of 5) returns text "0 of 5 covered"
    - A test asserts an out-of-book AE gets 404 on GET/PUT/POST
    - A test asserts POST log-conversation appends a kind='conversation' entry whose body contains the "X of N covered" summary
  </acceptance_criteria>
  <done>The Game Plan persists per account, seeds default topics, and logs conversations as "X of N covered" — own-book scoped.</done>
</task>

<task type="auto">
  <name>Task 2: GamePlanPanel UI mounted in the person workspace</name>
  <files>components/admin/GamePlanPanel.tsx, components/admin/ClientWorkspace.tsx, app/(admin)/admin/clients/[personId]/page.tsx</files>
  <read_first>
    - .planning/design/phase-31-game-plan.html — the target layout: topic list with questions, check-off on the call, notes, Log conversation
    - components/admin/ClientWorkspace.tsx — the person-mode workspace to add the Game Plan section into (mode='person'), and its existing data+string-props convention
    - lib/client-partners/game-plan.ts (Task 1) — GamePlanTopic + coveredSummary the panel renders
    - app/(admin)/admin/clients/[personId]/page.tsx — how ClientWorkspace is fed props (add the initial game-plan topics prop here)
  </read_first>
  <action>
    Create `components/admin/GamePlanPanel.tsx` ('use client'): renders the topic list (each topic's open-ended questions), lets the AE add/remove custom topics (guided toward a small set) and pull Selects-context suggestions, check topics as covered, add notes, then Save (PUT string path) and Log conversation (POST string path) — refreshing via router.refresh(). Mount it in ClientWorkspace's person mode (D-31.1-06 — individual client view), fed initial topics + the string action base /api/admin/client-partners/{orgId}/game-plan from the RSC page. Data + string props only — no function props cross the boundary. Include the seeded topic suggestions and a note that dynamic Playbook/Plays sourcing is deferred to 31.2.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean
    - GamePlanPanel is 'use client' and receives only data + the string action base (no function props)
    - The panel renders in ClientWorkspace mode='person'; Save PUTs and Log conversation POSTs to the string paths, then router.refresh()
  </acceptance_criteria>
  <done>The AE plans and logs calls against a saved topic list in the individual client view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff client → game-plan route | An AE may only read/write the Game Plan of a client in their own book |
| RSC page → GamePlanPanel | Serialization boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-ownbook | Information Disclosure / Access Control (V4) | game-plan route | high | mitigate | requireStaff() + canAccessOrgContacts → 404 (not 403) for an out-of-book AE, mirroring the relationship-log route |
| T-31.1-mass-assign | Tampering (V5) | game-plan PUT topics | medium | mitigate | zod-validated topic shape; author_user_id server-set; body never spread |
| T-31.1-rsc-func-prop | DoS/availability (V1) | GamePlanPanel props | high | mitigate | Data + string action paths only; PUT/POST built client-side; refresh via router.refresh() (Pitfall 1) |
| T-31.1-SC | Tampering (supply chain) | npm installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `npm test -- app/api/admin/client-partners lib/client-partners/game-plan.test.ts` green; `npx tsc --noEmit` clean.
- Manual (owner, post-push): open a client (person view), build a Game Plan, check some topics, Log conversation, and confirm the relationship log shows "X of N covered" + notes.
</verification>

<success_criteria>
- A saved per-account Game Plan with seeded topics logs conversations as "X of N covered", own-book scoped.
</success_criteria>

<output>
Create `.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-07-SUMMARY.md` when done.
</output>
