---
phase: 31.1-ae-console-health-leadership-tower-telemetry-team-rbac
plan: 04
type: execute
wave: 2
depends_on: ["31.1-01", "31.1-02", "31.1-03"]
files_modified:
  - lib/client-partners/signals.ts
  - lib/client-partners/signals.test.ts
  - lib/deals/executed.ts
  - app/api/admin/deals/[id]/executed/route.ts
  - app/api/admin/deals/[id]/executed/route.test.ts
  - app/(admin)/admin/buyer-orgs/[id]/route.ts
  - app/(admin)/admin/client-partners/page.tsx
  - components/admin/ClientPartnersRoom.tsx
  - components/admin/ClientPartnersList.tsx
  - components/nav/AdminNav.tsx
  - lib/admin/gate.test.ts
autonomous: true
requirements: [R3, R6]

must_haves:
  truths:
    - "One consolidated Client Partners room at /admin/client-partners renders a My tab (own book) and, for leadership only, an All tab (whole-book tower + coverage + By-AE + Needs-an-AE queue) (D-31.1-01/04)"
    - "A non-leadership AE/BD never receives the All-tab data: the server does NOT fetch coverage/whole-book/By-AE for them (hide, not filter) — enforced server-side, backed by a test (D-31.1-01)"
    - "Each row's health color is computed on read from the executed-license source via computeHealth(); a never-licensed org shows the prospect marker (D-06/D-31.1-02/09)"
    - "license_requests.executed_at is stamped by an explicit executed/signed action (POST /api/admin/deals/[id]/executed), NOT by the closed_won stage transition (D-31.1-09)"
    - "Last contact is surfaced as a column and on the client card as 'last contacted X ago' but never drives the color (D-31.1-02)"
    - "The single nav item 'Client Partners' replaces the separate my-client-partners + buyer-orgs entries; the AE role never sees the All tab or the tower (D-31.1-01/07)"
  artifacts:
    - "lib/client-partners/signals.ts (loadBook, loadWholeBookWithCoverage, lastExecutedLicenseAt, org signal readers) + test"
    - "lib/deals/executed.ts (stampLicenseExecuted helper) + app/api/admin/deals/[id]/executed/route.ts (+ test)"
    - "app/(admin)/admin/client-partners/page.tsx (RSC, leadership-gated) + components/admin/ClientPartnersRoom.tsx (client wrapper)"
    - "extended components/admin/ClientPartnersList.tsx (Assigned-AE column render + row-action slot as data/string props)"
    - "consolidated components/nav/AdminNav.tsx + extended lib/admin/gate.test.ts"
  key_links:
    - "signals.ts.lastExecutedLicenseAt reads MAX(license_requests.executed_at) per org → the health color clock"
    - "The room's All branch runs only when staffRole === 'leadership' (requireStaffPage); allData is null for everyone else"
    - "ClientPartnersRoom is the client wrapper plans 06 (assign panel + onboarding surface) mount into"
  prohibitions:
    - "The RSC page MUST NOT pass any function prop to ClientPartnersRoom/ClientPartnersList — data + string paths only (Pitfall 1, commit 80443bb; production 500 otherwise)"
    - "The server MUST NOT fetch All-tab/coverage/By-AE data for a non-leadership caller (hide, not filter)"
    - "executed_at MUST NOT be stamped on the closed_won transition; only the explicit executed/signed action stamps it (D-31.1-09)"
    - "The AE role MUST NOT be able to reach the All tab via any client flag — the tab is not rendered AND its data is not sent"
---

<objective>
Consolidate the two Client Partners pages into ONE tabbed room (My / All) with server-side leadership gating (hide, not filter), wire the relationship-health color to the real executed-license source (computed on read), add the explicit executed/signed stamping action (D-31.1-09), and collapse the nav to a single Client Partners item (D-31.1-01/07).

Purpose: this is where R3 health and R6 leadership tower become real screens on top of the pure engines (plans 02/03) and the migration (plan 01).
Output: the consolidated room, its data-source layer, the executed-license stamping seam, the nav consolidation, and the non-leadership access test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-CONTEXT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-RESEARCH.md
@.planning/design/phase-31.1-leadership-tower-mockup.html
@.planning/design/phase-31-my-client-partners-mockup.html
@app/(admin)/admin/my-client-partners/page.tsx
@app/(admin)/admin/buyer-orgs/page.tsx
@components/admin/ClientPartnersList.tsx
@components/nav/AdminNav.tsx
@lib/admin/gate.ts
@app/api/admin/deals/[id]/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Health & stage data source — signals loaders + executed-license stamping seam</name>
  <files>lib/client-partners/signals.ts, lib/client-partners/signals.test.ts, lib/deals/executed.ts, app/api/admin/deals/[id]/executed/route.ts, app/api/admin/deals/[id]/executed/route.test.ts, app/(admin)/admin/buyer-orgs/[id]/route.ts</files>
  <read_first>
    - app/(admin)/admin/my-client-partners/page.tsx — the existing Promise.all signal-loading pattern (openBriefsByOrg, countByOrg, own-book scope) to extend
    - app/api/admin/deals/[id]/route.ts — the deal PATCH route + verifyAdmin gate + the DEAL_STAGE_VALUES / stage-machine convention (to mirror gating for the new executed route; do NOT stamp executed_at here)
    - lib/client-partners/health.ts (plan 02) — HealthSignals shape the loaders must produce
    - lib/client-partners/contacts.ts — canAccessOrgContacts (own-book predicate) + listRelationshipLog (last-contact source)
    - supabase/migrations/081_license_requests_deals.sql — license_requests columns (buyer_org_id, stage, gross_fee_cents, budget_cents, executed_at from plan 01)
  </read_first>
  <behavior>
    - lastExecutedLicenseAt(orgId) returns the max executed_at over the org's license_requests, or null when none is executed
    - stampLicenseExecuted sets executed_at to the provided timestamp only when currently null (idempotent — a second call does not move the date)
    - a non-leadership caller of the executed route is 403; leadership/admin succeeds
    - buyer-orgs edit route accepts pipeline_stage_id in its allowlist and stamps stage_entered_at=now() only when the stage actually changes
  </behavior>
  <action>
    Create `lib/client-partners/signals.ts` with service-role reader functions that assemble, per org: lastExecutedLicenseAt (MAX executed_at), hasOpenDeal (any non-terminal license_requests stage), openDealValue (sum gross_fee_cents/budget_cents over non-terminal), lifetimeValue (sum gross_fee_cents over executed), lastSelectsSentAt (max selects.sent_at), lastContactAt (max client_relationship_log.created_at), open-briefs count, stage + stage_entered_at. Export `loadBook(service, { aeUserId })` (own-book rows, mirroring my-client-partners/page.tsx scope) and `loadWholeBookWithCoverage(service)` (all rows + AE identity for the Assigned-AE column) — both build ClientPartnerRow[] with health resolved via computeHealth(signals, rules) using the health_rules_config singleton, and lastTouchAt set for the 'last contacted X ago' display. Create `lib/deals/executed.ts` exporting `stampLicenseExecuted(service, licenseRequestId, at)` (idempotent — only sets when null) and add `app/api/admin/deals/[id]/executed/route.ts` (POST, verifyAdmin-gated, calls the helper, logs via logStaffAction) — this is the explicit executed/signed event (D-31.1-09), distinct from the closed_won stage transition which stays untouched. Extend `app/(admin)/admin/buyer-orgs/[id]/route.ts` PATCH allowlist to accept pipeline_stage_id (UUID-validated) and stamp stage_entered_at=now() when it changes. Write signals.test.ts (health assembly + lastExecutedLicenseAt null→prospect path) and the executed route.test.ts (gate + idempotency) first, RED, then implement. Route files export only handlers + config (no helpers).
  </action>
  <verify>
    <automated>npm test -- lib/client-partners/signals.test.ts app/api/admin/deals</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/client-partners/signals.test.ts app/api/admin/deals` passes
    - stampLicenseExecuted is idempotent (a second call leaves executed_at unchanged) — asserted in a test
    - POST /api/admin/deals/[id]/executed returns 403 for a non-admin and never stamps on closed_won
    - loadBook rows carry health from computeHealth and lastTouchAt for the last-contact display
  </acceptance_criteria>
  <done>The health color reads a real, idempotently-stamped executed-license source; the stage clock has a write path; own-book vs whole-book loaders exist.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidated Client Partners room (My/All) + ClientPartnersList extension + nav</name>
  <files>app/(admin)/admin/client-partners/page.tsx, components/admin/ClientPartnersRoom.tsx, components/admin/ClientPartnersList.tsx, components/nav/AdminNav.tsx</files>
  <read_first>
    - app/(admin)/admin/my-client-partners/page.tsx — the page to fold into the My tab (own-book scope, force-dynamic, 'it'/no-role redirect)
    - app/(admin)/admin/buyer-orgs/page.tsx — the leadership create-org surface to fold into the All tab
    - components/admin/ClientPartnersList.tsx — its props (companyRows/clientRows/companyHrefBase/clientHrefBase, RSC-boundary JSDoc); extend with a data/string-only row-action slot + Assigned-AE column, never a function prop
    - components/nav/AdminNav.tsx — the two entries to replace (line ~57 buyer-orgs 'Client Partners'; line ~67 my-client-partners 'My Client Partners') and the show/section convention
    - .planning/design/phase-31.1-leadership-tower-mockup.html + phase-31-my-client-partners-mockup.html — the target layout for both tabs
    - lib/client-partners/coverage.ts (plan 03) — buildCoverageSummary/groupByAe for the All tab
  </read_first>
  <action>
    Create the RSC page `app/(admin)/admin/client-partners/page.tsx` (force-dynamic): requireStaffPage() (fail-closed, excludes 'it'); isLeadership = staffRole === 'leadership'; always loadBook for the caller's own book; ONLY when isLeadership, call loadWholeBookWithCoverage + buildCoverageSummary + groupByAe + the Needs-an-AE (unassigned) queue — pass allData=null otherwise. Render `components/admin/ClientPartnersRoom.tsx` (a 'use client' wrapper) with plain serializable props: myRows, isLeadership, allData (rows + coverage summary + byAe + unassigned queue), and string href bases. ClientPartnersRoom renders the My/All top-level tab toggle (All tab omitted entirely when !isLeadership), the coverage strip + Needs-an-AE queue + By-AE view (leadership), and reuses ClientPartnersList for both books. Extend ClientPartnersList to render the Assigned-AE column (from plan 02's ColumnDef) and an optional per-row action affordance driven by data/string props only (e.g. a rowActionLabel + the row id; the actual assign handler is added by plan 06 in the wrapper). Update AdminNav: replace the two entries with ONE `{ href: '/admin/client-partners', label: 'Client Partners', ... show: notIt }` (leadership sees All inside; AE sees only My) and add the OPS `{ href: '/admin/health-rules', label: 'Health Rules', show: isLead }` entry per D-31.1-07 (its page ships in plan 05). Repoint the old my-client-partners and buyer-orgs pages to redirect to /admin/client-partners (keep the create-org action reachable inside the All tab). No fenced code in the room — data + string props only (Pitfall 1).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean
    - app/(admin)/admin/client-partners/page.tsx computes allData only inside the isLeadership branch and passes null otherwise
    - ClientPartnersRoom is 'use client' and receives no function props from the page (grep shows no arrow/function-valued JSX prop from the RSC page)
    - AdminNav has exactly one Client Partners entry (no separate my-client-partners/buyer-orgs nav items) plus the leadership-only Health Rules entry
  </acceptance_criteria>
  <done>One tabbed Client Partners room renders My for everyone and the leadership tower for leadership only, reusing ClientPartnersList, with the nav collapsed to a single item.</done>
</task>

<task type="auto">
  <name>Task 3: Non-leadership access gate — server hides All-tab data (test)</name>
  <files>lib/admin/gate.test.ts, app/(admin)/admin/client-partners/page.tsx</files>
  <read_first>
    - lib/admin/gate.test.ts — the existing gate test conventions to extend
    - lib/admin/gate.ts — getStaffRole/requireStaffPage/OPERATIONAL_STAFF_ROLES fail-closed default
    - .planning/phases/31.1-.../31.1-VALIDATION.md row D-31.1-01 (server hides, not filters) and 31-SPEC.md Prohibitions row R5/R6
  </read_first>
  <action>
    Extend `lib/admin/gate.test.ts` (or add a colocated page-loader test) proving the D-31.1-01 hide-not-filter contract: assert that the room's data assembly returns allData=null for an 'ae' and a 'bd' role and a populated allData only for 'leadership'; assert that 'it' and no-role are redirected before any load. Factor the leadership decision into a tiny exported predicate in the page module (or reuse getStaffRole) so it is unit-testable without rendering. Confirm the AE branch never invokes loadWholeBookWithCoverage (spy/asserts the function is not called for non-leadership).
  </action>
  <verify>
    <automated>npm test -- lib/admin/gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/admin/gate.test.ts` passes
    - A test asserts loadWholeBookWithCoverage is NOT called for 'ae'/'bd' and allData is null for them
    - A test asserts allData is populated only for 'leadership'
  </acceptance_criteria>
  <done>The hide-not-filter invariant (D-31.1-01) is machine-verified: non-leadership never receives whole-book/coverage data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AE/BD client → RSC room page | A non-leadership staff member must never receive the whole-book/coverage/By-AE data |
| admin client → deals executed route | Only leadership/admin may stamp a license executed |
| buyer/authenticated client → new columns | executed_at / pipeline_stage_id / stage_entered_at must stay staff-only |
| RSC page → client wrapper | Serialization boundary — function props crash production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-info-disclosure | Information Disclosure (V1/V4) | client-partners room All-tab data | high | mitigate | Server hides (does not fetch) All-tab data for non-leadership; requireStaffPage fail-closed excludes 'it'/no-role; asserted by the Task 3 test |
| T-31.1-rsc-func-prop | DoS/availability (V1) | ClientPartnersRoom / ClientPartnersList props | high | mitigate | Client components receive data + string paths only; no function props from the RSC page (Pitfall 1, commit 80443bb) |
| T-31.1-color-clock | Tampering | executed route + signals.lastExecutedLicenseAt | medium | mitigate | executed_at stamped only via the explicit verifyAdmin-gated executed route (idempotent), never on closed_won (D-31.1-09) |
| T-31.1-buyer-read | Information Disclosure (V13) | new buyer_orgs/license_requests columns | high | mitigate | Columns private by default (migration 081/128 grant posture); reads only via service-role staff loaders |
| T-31.1-mass-assign | Tampering (V5) | buyer-orgs edit route pipeline_stage_id | medium | mitigate | Explicit allowlist + UUID validation; body never spread into the update |
| T-31.1-SC | Tampering (supply chain) | npm installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `npm test -- lib/client-partners/signals.test.ts app/api/admin/deals lib/admin/gate.test.ts` green; `npx tsc --noEmit` clean.
- Manual (owner, post-push): load /admin/client-partners as leadership (All tab + coverage visible) and as an AE (only My tab); confirm colors + 'last contacted X ago' match the mockups.
</verification>

<success_criteria>
- One consolidated, leadership-gated Client Partners room renders live compute-on-read health from the executed-license source, with the nav collapsed to a single item and the hide-not-filter invariant machine-verified.
</success_criteria>

<output>
Create `.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-04-SUMMARY.md` when done.
</output>
