---
phase: 31.1-ae-console-health-leadership-tower-telemetry-team-rbac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/128_ae_console_health.sql
  - __tests__/migration-128.test.ts
autonomous: false
requirements: [R3, R4, R6, R7, R8, R14]
user_setup:
  - service: supabase
    why: "Migration 128 must be applied to prod (project wgfjakfiyeewzfuxkgyo) by the owner — agents never run supabase db push"
    dashboard_config:
      - task: "Run `supabase db push` and confirm migration 128 applied via `supabase migration list` (LOCAL=REMOTE)"
        location: "Owner's local terminal against prod project wgfjakfiyeewzfuxkgyo"

must_haves:
  truths:
    - "A per-deal executed/signed-license timestamp exists on license_requests (executed_at) so the health color clock reads the EXECUTED license moment, not closed_won and not payment (D-31.1-09)"
    - "health_rules_config exists as a seeded singleton row (id=1) holding threshold days + keeps-warm toggles + prospect_image_url (D-31.1-03/08)"
    - "pipeline_stages exists and is seeded with the D-10 defaults (New lead, Contacted, Active, Negotiating, Closed/Dormant); buyer_orgs gains pipeline_stage_id + stage_entered_at (D-10)"
    - "game_plans exists (one row per buyer_org, unique index) for the per-account Game Plan (R14/D-31.1-06)"
    - "onboarding_tasks exists (net-new; NOT the jobs worker queue) for the D-07 auto-created handoff task, indexed by (assignee_id, status) (R7/R8/D-07)"
    - "Every new table is RLS-enabled with ZERO policies AND REVOKE'd from authenticated+anon; the migration ends with NOTIFY pgrst reload (migration-112 doctrine)"
    - "Migration 128 is applied to prod by the owner at the blocking checkpoint before any DB-dependent verification runs"
  artifacts:
    - "supabase/migrations/128_ae_console_health.sql"
    - "__tests__/migration-128.test.ts"
  key_links:
    - "license_requests.executed_at is the single source computeHealth() (plan 02) reads via lastExecutedLicenseAt() (plan 04)"
    - "health_rules_config row id=1 is the config computeHealth() and the Health Rules screen (plan 05) both read/write"
    - "onboarding_tasks.assignee_id ties a D-07 handoff task to the AE who received it (plan 06 insert; plan 04 My-view surface)"
  prohibitions:
    - "executed_at (and every new buyer_orgs/license_requests column) MUST NOT appear in any GRANT SELECT ... TO authenticated/anon statement — private by default (Pitfall 4/6)"
    - "No new table may declare a CREATE POLICY — zero-policy + REVOKE is the only access posture (staff service-role routes only)"
    - "An executor agent MUST NOT run `supabase db push` — the push is the owner's blocking checkpoint"
    - "executed_at MUST NOT be stamped on the closed_won stage transition (that write lands in plan 04, gated on the executed/signed event per D-31.1-09)"
---

<objective>
Author migration 128 — the gating data-model dependency for the entire phase — then hand it to the owner for the human-gated `supabase db push`. Adds the executed/signed-license timestamp that drives relationship health (D-31.1-09), the leadership health-rules config singleton (D-31.1-03/08), the D-10 pipeline-stage model, the R14 Game Plan store, and the D-07 onboarding-task queue.

Purpose: every downstream plan (health render, Health Rules, assign handoff, Game Plan) reads tables/columns that do not exist yet. This migration creates them under the migration-112 zero-policy + REVOKE doctrine. Types come from config, not the live DB, so verification is a false-positive until the migration is actually applied — hence the blocking push checkpoint.
Output: `supabase/migrations/128_ae_console_health.sql` + `__tests__/migration-128.test.ts`, applied to prod.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-CONTEXT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-RESEARCH.md
@supabase/migrations/112_client_partners_crm.sql
@supabase/migrations/081_license_requests_deals.sql
@__tests__/migration-112.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 128 (executed-license timestamp, health-rules config, pipeline stages, game plans, onboarding tasks)</name>
  <files>supabase/migrations/128_ae_console_health.sql</files>
  <read_first>
    - supabase/migrations/112_client_partners_crm.sql — the zero-policy + REVOKE + NOTIFY doctrine to mirror exactly (sections e/f), and the HUMAN-GATED header wording
    - supabase/migrations/081_license_requests_deals.sql — license_requests shape + its column-level GRANT SELECT allowlist (executed_at must NOT be added to it) + the gen_random_uuid() default convention
    - .planning/phases/31.1-.../31.1-RESEARCH.md § "Data Model + Migrations" and § "The Last-License / Paid-Placement Date Source"
    - .planning/phases/31.1-.../31.1-CONTEXT.md D-31.1-09 (executed/signed license = the health clock, NOT closed_won)
  </read_first>
  <action>
    Create `supabase/migrations/128_ae_console_health.sql` (repo tip is 127; 113/114 are taken — 128 is next). Use gen_random_uuid() defaults (081 convention), and open with a HUMAN-GATED header stating an executor agent never runs `supabase db push` (mirror 112's header wording).

    (a) VERIFY-then-ADD the executed-license source (D-31.1-09). RESEARCH confirmed esign_envelopes ties to split_sheets (songwriter side), NOT the buyer/sync license — so no per-deal executed timestamp exists tied to a buyer_org. Add `ALTER TABLE public.license_requests ADD COLUMN executed_at TIMESTAMPTZ;` and a COMMENT stating it is the executed/signed-license moment (e-sign completion), forward-compatible with lib/esign/provider.ts, and deliberately distinct from stage='closed_won'. No backfill (prod buyer_orgs/license_requests are empty). Do NOT add executed_at to migration 081's authenticated GRANT SELECT allowlist — private by default.

    (b) Create `public.pipeline_stages` (id uuid pk, key text unique with a CHECK matching the KEY_REGEX shape lower-alnum-underscore, label text, sort_order int, is_terminal bool, created_at, updated_at). Seed the D-10 defaults with stable keys: new_lead / contacted / active / negotiating / closed_dormant (is_terminal true on closed_dormant). Add `ALTER TABLE public.buyer_orgs ADD COLUMN pipeline_stage_id UUID REFERENCES public.pipeline_stages ON DELETE SET NULL, ADD COLUMN stage_entered_at TIMESTAMPTZ;` (both private by default — do not grant).

    (c) Create `public.health_rules_config` as a singleton (id integer pk default 1 CHECK id=1) with: good_within_days (default 90), warning_after_days (120), at_risk_after_days (180), cold_after_days (365), keep_warm_open_brief (true), keep_warm_open_deal (true), keep_warm_recent_selects (true), recent_selects_days (21), keep_warm_recent_contact (false), recent_contact_days (30), prospect_image_url text null, updated_by uuid refs auth.users, updated_at. Seed the singleton with `INSERT ... (id) VALUES (1) ON CONFLICT DO NOTHING;`. Document that prospect_image_url null means the neutral placeholder marker renders (D-31.1-08).

    (d) Create `public.game_plans` (id uuid pk, buyer_org_id uuid not null refs buyer_orgs on delete cascade, topics jsonb not null default '[]', updated_by uuid refs auth.users, created_at, updated_at) + a UNIQUE index game_plans_one_per_org on (buyer_org_id) (R14/D-31.1-06).

    (e) Create `public.onboarding_tasks` (id uuid pk, buyer_org_id uuid not null refs buyer_orgs on delete cascade, assignee_id uuid not null refs auth.users, created_by uuid refs auth.users, title text not null, checklist jsonb not null default '[]', status text not null default 'open' CHECK status IN open/done/dismissed, handoff_note text, created_at, completed_at) + index idx_onboarding_tasks_assignee on (assignee_id, status). This is the D-07 auto-created onboarding task queue — net-new, NOT the jobs worker queue (migration 118).

    (f) For every NEW table (pipeline_stages, health_rules_config, game_plans, onboarding_tasks): ENABLE ROW LEVEL SECURITY, create NO policies, and REVOKE SELECT, INSERT, UPDATE, DELETE ... FROM authenticated, anon. End the file with `NOTIFY pgrst, 'reload schema';` as the last statement.

    Do not modify migrations 001-127.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/128_ae_console_health.sql','utf8'); const need=['ADD COLUMN executed_at','pipeline_stages','health_rules_config','game_plans','onboarding_tasks',\"NOTIFY pgrst, 'reload schema';\"]; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('MISSING',miss);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/128_ae_console_health.sql` exists and contains CREATE TABLE for pipeline_stages, health_rules_config, game_plans, onboarding_tasks and `ALTER TABLE public.license_requests ADD COLUMN executed_at`
    - Every new table has both `ENABLE ROW LEVEL SECURITY` and a `REVOKE SELECT, INSERT, UPDATE, DELETE ... FROM authenticated, anon` line; no `CREATE POLICY` statement appears for any new table
    - executed_at, pipeline_stage_id, stage_entered_at do NOT appear in any `GRANT SELECT ... TO authenticated` statement
    - health_rules_config seeds the id=1 singleton; pipeline_stages seeds the 5 D-10 default stage keys; file ends with the NOTIFY reload
  </acceptance_criteria>
  <done>Migration 128 SQL authored to the migration-112 doctrine; the executed-license timestamp and all four new tables exist in the file, all private/staff-only.</done>
</task>

<task type="auto">
  <name>Task 2: Text-lock migration 128 in __tests__/migration-128.test.ts</name>
  <files>__tests__/migration-128.test.ts</files>
  <read_first>
    - __tests__/migration-112.test.ts — the exact mirroring convention (readFileSync + toContain / not.toMatch assertions, HUMAN-GATED footer, NOTIFY end-of-file assertion)
    - supabase/migrations/128_ae_console_health.sql — the file authored in Task 1
  </read_first>
  <action>
    Create `__tests__/migration-128.test.ts` mirroring `__tests__/migration-112.test.ts`: readFileSync the 128 SQL and assert, per the VALIDATION map row "Migration 128": (1) each of the four new tables is created; (2) `ALTER TABLE public.license_requests ADD COLUMN executed_at` is present; (3) each new table is RLS-enabled AND REVOKE'd from authenticated, anon; (4) no `CREATE POLICY` targets a new table; (5) executed_at / pipeline_stage_id / stage_entered_at are never granted to authenticated (use a not.toMatch on a GRANT SELECT ... TO authenticated pattern for each); (6) the health_rules_config singleton seed and the five pipeline_stages default keys are inserted; (7) the HUMAN-GATED provenance line is present; (8) the file ends with the schema-cache reload notification. Reference the literal SQL tokens by concept where an assertion would otherwise embed a forbidden token; the negated-grep assertions live only in this test file, which is never itself grepped by an acceptance gate.
  </action>
  <verify>
    <automated>npm test -- __tests__/migration-128.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- __tests__/migration-128.test.ts` passes
    - The suite fails if executed_at is ever added to an authenticated GRANT, if any new table gains a policy, or if the NOTIFY reload is not last
  </acceptance_criteria>
  <done>The migration is text-locked to its doctrine; a regression in RLS/REVOKE/grant posture fails jest.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Owner applies migration 128 to prod (supabase db push)</name>
  <files>supabase/migrations/128_ae_console_health.sql</files>
  <action>
    STOP — owner action required; an executor agent MUST NOT run `supabase db push`. This checkpoint runs AFTER the migration SQL + test are authored and BEFORE any DB-dependent verification in plans 04/05/06/07. The owner reviews the migration, runs `supabase db push` against prod (project wgfjakfiyeewzfuxkgyo), then confirms `supabase migration list` shows LOCAL=REMOTE through 128 with no error on the executed_at ADD COLUMN or any new table create. Types come from config, not the live DB, so downstream verification is a false-positive until this is done.
  </action>
  <what-built>Migration 128 SQL (four staff-only tables + the executed-license timestamp) is authored and text-locked by jest. Nothing is applied to the database — this repo never runs `supabase db push` from an agent.</what-built>
  <how-to-verify>
    1. Review `supabase/migrations/128_ae_console_health.sql`.
    2. Run `supabase db push` against prod (project wgfjakfiyeewzfuxkgyo).
    3. Run `supabase migration list` and confirm LOCAL=REMOTE through 128.
    4. Confirm no errors on the executed_at ADD COLUMN or any new table create.
  </how-to-verify>
  <verify>Owner confirms `supabase migration list` shows migration 128 live (LOCAL=REMOTE).</verify>
  <done>Migration 128 is applied to prod; the executed-license timestamp and all four new tables exist live.</done>
  <resume-signal>Type "128 applied" once `supabase migration list` shows migration 128 live, or describe any push error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buyer/authenticated client → Postgres (PostgREST) | An authenticated non-staff caller could attempt to read the new staff-only tables/columns directly |
| owner → prod DB | The human-gated `supabase db push` is the only path that mutates the live schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-buyer-read | Information Disclosure (V13 API) | new tables + license_requests.executed_at / buyer_orgs.pipeline_stage_id/stage_entered_at | high | mitigate | RLS-enabled + zero-policy + REVOKE from authenticated/anon on every new table; new columns private by default (never added to the 081/080 GRANT allowlist). Text-locked by migration-128.test.ts |
| T-31.1-mass-assign | Tampering (V5) | health_rules_config / game_plans / onboarding_tasks writes | medium | mitigate | Writes are downstream (plans 05/06/07) via zod .strict() + service-role staff routes; the schema itself grants no client write path |
| T-31.1-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A — no new dependencies) |
</threat_model>

<verification>
- `npm test -- __tests__/migration-128.test.ts` green.
- `supabase migration list` shows LOCAL=REMOTE through 128 (owner-confirmed at the checkpoint).
</verification>

<success_criteria>
- The executed-license timestamp and all four new tables exist in migration 128 under the zero-policy + REVOKE doctrine, and the migration is applied to prod.
</success_criteria>

<output>
Create `.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-01-SUMMARY.md` when done.
</output>
