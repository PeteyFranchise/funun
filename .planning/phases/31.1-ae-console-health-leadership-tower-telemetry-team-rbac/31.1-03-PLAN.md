---
phase: 31.1-ae-console-health-leadership-tower-telemetry-team-rbac
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/client-partners/stages.ts
  - lib/client-partners/stages.test.ts
  - lib/client-partners/coverage.ts
  - lib/client-partners/coverage.test.ts
autonomous: true
requirements: [R6]

must_haves:
  truths:
    - "daysInStage() computes whole days between stage_entered_at and now, returning null (never 0 or a fabricated value) when stage_entered_at is absent (D-10)"
    - "Pipeline stage resolution reads the leadership-configurable pipeline_stages set (label/sort_order/is_terminal), not a hardcoded stage list (D-10)"
    - "buildCoverageSummary() aggregates the whole-book rows into the tower coverage strip totals (clients, unassigned, AEs, open pipeline $, at-risk count) as a pure transform (D-31.1-04)"
    - "groupByAe() produces the By-AE coverage view — each AE with their load (assigned count) and a health mix (count per HealthState) — as a pure transform (D-31.1-04)"
  artifacts:
    - "lib/client-partners/stages.ts (daysInStage, resolveStage) + stages.test.ts"
    - "lib/client-partners/coverage.ts (buildCoverageSummary, groupByAe, CoverageSummary, AeCoverage) + coverage.test.ts"
  key_links:
    - "daysInStage feeds the tower's Stage·days column (plan 04) from buyer_orgs.stage_entered_at"
    - "buildCoverageSummary/groupByAe feed the leadership-only coverage strip + By-AE view the room (plan 04) renders"
  prohibitions:
    - "These modules MUST NOT perform I/O — the room's server loader (plan 04) fetches rows and passes them in (D-06 compute-on-read)"
    - "daysInStage MUST NOT return 0 for a missing stage_entered_at — absent data resolves to null and renders the defined no-data state"
---

<objective>
Build the pure stage and coverage-aggregation helpers the leadership tower renders: days-in-stage against the configurable pipeline stages (D-10), the coverage-strip totals, and the By-AE grouping with per-AE load + health mix (D-31.1-04). Pure transforms, unit-tested in isolation like columns.ts.

Purpose: keep the tower's aggregation math out of the RSC page so it is deterministic and testable; the page (plan 04) becomes a thin fetch-then-transform.
Output: lib/client-partners/stages.ts and lib/client-partners/coverage.ts (+ tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-CONTEXT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-RESEARCH.md
@lib/client-partners/columns.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Days-in-stage + configurable stage resolution — lib/client-partners/stages.ts</name>
  <files>lib/client-partners/stages.ts, lib/client-partners/stages.test.ts</files>
  <read_first>
    - lib/client-partners/columns.ts — the toMs/pure-helper convention and the daysInStage column already declared in COMPANIES_COLUMNS
    - .planning/phases/31.1-.../31.1-CONTEXT.md D-10 (stages leadership-configurable: New lead → Contacted → Active → Negotiating → Closed/Dormant; days-in-stage measured against them)
    - .planning/phases/31.1-.../31.1-RESEARCH.md § Data Model (pipeline_stages shape, stage_entered_at)
  </read_first>
  <behavior>
    - stage_entered_at 5 days before now → daysInStage === 5
    - stage_entered_at null → daysInStage === null
    - stage_entered_at in the future (clock skew) → 0, never negative
    - resolveStage(stageId, stages) returns the matching {key,label,sort_order,is_terminal} or null for an unknown id
  </behavior>
  <action>
    Create `lib/client-partners/stages.ts` exporting a `PipelineStage` type (id, key, label, sortOrder, isTerminal), `daysInStage(stageEnteredAt: string | null, nowMs?: number): number | null` (whole days, null when absent, floored at 0), and `resolveStage(stageId: string | null, stages: PipelineStage[]): PipelineStage | null`. TS strict, 2-space, no semicolons, named exports. Write stages.test.ts first (RED) covering <behavior>, then implement.
  </action>
  <verify>
    <automated>npm test -- lib/client-partners/stages.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/client-partners/stages.test.ts` passes
    - A test asserts daysInStage(null) === null and daysInStage(futureIso) === 0
    - resolveStage returns null for an unknown id
  </acceptance_criteria>
  <done>Days-in-stage and configurable stage resolution are pure and jest-locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coverage strip + By-AE grouping — lib/client-partners/coverage.ts</name>
  <files>lib/client-partners/coverage.ts, lib/client-partners/coverage.test.ts</files>
  <read_first>
    - lib/client-partners/columns.ts — ClientPartnerRow shape (health, openDealValue, ae fields) the aggregation reads
    - lib/client-partners/health.ts (plan 02) — HealthState, so the health mix keys match
    - .planning/design/phase-31.1-leadership-tower-mockup.html — the coverage strip metrics (clients · unassigned · AEs · open pipeline · at-risk) and the By-AE coverage layout (per-AE load + health mix)
    - .planning/phases/31.1-.../31.1-CONTEXT.md D-31.1-04
  </read_first>
  <behavior>
    - buildCoverageSummary over N rows returns totalClients=N, unassigned = count with no ae, aeCount = distinct assigned AEs, openPipelineCents = sum of openDealValue, atRiskCount = count where health is at_risk (and, per mockup, cold if the strip groups them — assert the documented choice)
    - groupByAe buckets rows by assigned AE id, each AeCoverage carrying aeId, aeName, load (row count), and healthMix (a record of HealthState → count)
    - an unassigned row is excluded from groupByAe buckets but counted in the summary's unassigned total
  </behavior>
  <action>
    Create `lib/client-partners/coverage.ts` exporting `CoverageSummary` and `AeCoverage` types and pure `buildCoverageSummary(rows): CoverageSummary` and `groupByAe(rows): AeCoverage[]` (sorted deterministically by aeName then aeId). Read health/openDealValue/assigned-AE fields off ClientPartnerRow (add the optional ae id/name fields to ClientPartnerRow only if not already present from plan 02's Assigned-AE column — if plan 02 added assignedAeName, reuse it and add assignedAeId here). Write coverage.test.ts first (RED) covering <behavior>, including a mixed assigned/unassigned fixture, then implement.
  </action>
  <verify>
    <automated>npm test -- lib/client-partners/coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/client-partners/coverage.test.ts` passes with a mixed assigned/unassigned fixture
    - unassigned rows are counted in the summary but excluded from By-AE buckets
    - healthMix keys are exactly the HealthState values from lib/client-partners/health.ts
  </acceptance_criteria>
  <done>The coverage strip totals and By-AE grouping are pure, deterministic, and jest-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | Pure aggregation modules; rows are supplied by the leadership-gated server loader (plan 04). The confidentiality of the aggregated data is enforced at that loader, not here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-info-disclosure | Information Disclosure | coverage aggregation output | high | mitigate | These transforms run ONLY inside the leadership branch of the room loader (plan 04) — non-leadership never receives rows to aggregate (server hides, not filters). Enforced in plan 04, asserted here as an input contract |
| T-31.1-SC | Tampering (supply chain) | npm installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `npm test -- lib/client-partners/stages.test.ts` and `npm test -- lib/client-partners/coverage.test.ts` green.
</verification>

<success_criteria>
- Days-in-stage (configurable) and the tower coverage/By-AE aggregation exist as pure, jest-locked transforms.
</success_criteria>

<output>
Create `.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-03-SUMMARY.md` when done.
</output>
