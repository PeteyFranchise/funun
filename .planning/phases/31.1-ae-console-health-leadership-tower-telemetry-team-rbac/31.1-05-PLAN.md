---
phase: 31.1-ae-console-health-leadership-tower-telemetry-team-rbac
plan: 05
type: execute
wave: 2
depends_on: ["31.1-01", "31.1-02"]
files_modified:
  - app/api/admin/health-rules/route.ts
  - app/api/admin/health-rules/route.test.ts
  - app/api/admin/health-rules/prospect-image/route.ts
  - app/api/admin/health-rules/prospect-image/route.test.ts
  - app/api/admin/pipeline-stages/route.ts
  - app/api/admin/pipeline-stages/route.test.ts
  - app/(admin)/admin/health-rules/page.tsx
  - components/admin/HealthRulesForm.tsx
autonomous: true
requirements: [R4]

must_haves:
  truths:
    - "Health Rules is a leadership-only settings screen: thresholds (Good/Warning/At-risk/Cold days) + keeps-warm toggles + a live preview of the resulting state split (R4/D-31.1-03)"
    - "Saving the config validates threshold ordering (good_within < warning_after < at_risk_after < cold_after) and writes only allowlisted fields; the body is never spread into the update (D-31.1-03)"
    - "The prospect marker is a leadership-swappable image uploaded from inside Health Rules (image → vault-assets → health_rules_config.prospect_image_url), applying live with no code change; a neutral placeholder ships by default (D-31.1-08)"
    - "Pipeline stages are leadership-configurable via a CRUD route with a KEY_REGEX-constrained key and a field allowlist (D-10)"
    - "The live preview recomputes on the client using computeHealth() over the current book signals — nothing is stored or recomputed server-side on save (D-06)"
  artifacts:
    - "app/api/admin/health-rules/route.ts (GET + PATCH config, zod .strict()) + test"
    - "app/api/admin/health-rules/prospect-image/route.ts (image upload) + test"
    - "app/api/admin/pipeline-stages/route.ts (stage CRUD, KEY_REGEX + allowlist) + test"
    - "app/(admin)/admin/health-rules/page.tsx + components/admin/HealthRulesForm.tsx"
  key_links:
    - "health-rules route writes the health_rules_config singleton (id=1) that computeHealth() (plan 02) and the room (plan 04) read"
    - "prospect-image route mirrors app/api/admin/staff/[id]/avatar route shape (vault-assets bucket, mime allowlist, size cap, service-role write)"
  prohibitions:
    - "Non-leadership MUST NOT read or write the config, upload the prospect image, or edit stages — every route/page gates ['leadership']"
    - "The request body MUST NOT be spread into the DB update — only zod-validated, allowlisted fields are written (mass-assignment)"
    - "A config save with overlapping/misordered thresholds MUST be rejected (400), not silently coerced"
    - "The HealthRulesForm ('use client') MUST NOT receive function props from its RSC page — data + string action paths only (Pitfall 1)"
    - "Do NOT embed any trademarked logo — ship a neutral placeholder; the owner supplies the prospect image (D-31.1-08)"
---

<objective>
Build the leadership-only Health Rules settings surface (R4/D-31.1-03): tunable last-license thresholds + keeps-warm toggles with a live preview of the state split, plus the swappable prospect-image control (D-31.1-08) and the D-10 pipeline-stage config CRUD. Saving applies immediately (nothing to recompute — D-06).

Purpose: give leadership the single place to tune what "at-risk" means and to set the prospect marker image, mirroring the D-10 "one place to adjust as we grow" pattern.
Output: the config route, prospect-image upload route, stage CRUD route, and the Health Rules page + form.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-CONTEXT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-RESEARCH.md
@.planning/design/phase-31.1-health-rules-mockup.html
@app/api/admin/staff/[id]/avatar/route.ts
@lib/admin/gate.ts
@lib/client-partners/health.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Health-rules config route + pipeline-stages CRUD route</name>
  <files>app/api/admin/health-rules/route.ts, app/api/admin/health-rules/route.test.ts, app/api/admin/pipeline-stages/route.ts, app/api/admin/pipeline-stages/route.test.ts</files>
  <read_first>
    - lib/admin/gate.ts — requireStaff(['leadership']), EDITABLE_FIELDS allowlist convention, KEY_REGEX
    - lib/client-partners/contacts.ts — the zod .strict() + explicit-allowlist (pickContactFields) precedent to mirror for config writes
    - lib/client-partners/health.ts (plan 02) — HealthRulesConfig field names the config schema must match
    - supabase/migrations/128_ae_console_health.sql (plan 01) — health_rules_config columns + the singleton id=1; pipeline_stages shape
  </read_first>
  <behavior>
    - GET /api/admin/health-rules returns the singleton config (leadership only; 403 otherwise)
    - PATCH with good_within=90, warning_after=120, at_risk_after=180, cold_after=365 succeeds; with warning_after <= good_within → 400
    - PATCH with an unknown field ignores it (allowlist) and never writes it
    - pipeline-stages POST with key 'Follow Up' (uppercase/space) → 400 (KEY_REGEX); with 'follow_up' → ok
    - all four handlers return 403 for a non-leadership caller
  </behavior>
  <action>
    Create `app/api/admin/health-rules/route.ts` with GET (returns the health_rules_config singleton) and PATCH (leadership-only via requireStaff(['leadership'])), a zod `.strict()` schema over the numeric thresholds + boolean keeps-warm toggles + recent_*_days, an explicit allowlist filter (mirroring pickContactFields), a threshold-ordering check (good_within_days < warning_after_days < at_risk_after_days < cold_after_days) returning 400 on violation, a service-role update of the id=1 row setting updated_by/updated_at, and logStaffAction. Create `app/api/admin/pipeline-stages/route.ts` (GET list + POST create + PATCH reorder/rename + DELETE) leadership-only, with KEY_REGEX-validated keys, a field allowlist (label, sort_order, is_terminal, key), and audit. Route files export only handlers + config. Write both route.test.ts files first (RED) covering <behavior>, then implement.
  </action>
  <verify>
    <automated>npm test -- app/api/admin/health-rules app/api/admin/pipeline-stages</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- app/api/admin/health-rules app/api/admin/pipeline-stages` passes
    - A test asserts a misordered threshold PATCH returns 400 and does not write
    - A test asserts an unknown/extra body field is never persisted (allowlist)
    - A test asserts a non-leadership caller gets 403 on every handler and pipeline-stage key KEY_REGEX rejection
  </acceptance_criteria>
  <done>Leadership can read/tune thresholds + keeps-warm and configure pipeline stages, mass-assignment-safe and ordering-validated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prospect-image upload route (copy the avatar route shape)</name>
  <files>app/api/admin/health-rules/prospect-image/route.ts, app/api/admin/health-rules/prospect-image/route.test.ts</files>
  <read_first>
    - app/api/admin/staff/[id]/avatar/route.ts — the exact shape to copy: vault-assets bucket, EXT_BY_MIME allowlist, MAX_BYTES cap, service-role upload + getPublicUrl, then write the config value + logStaffAction
    - .planning/phases/31.1-.../31.1-CONTEXT.md D-31.1-08 (swappable image, neutral placeholder, no trademarked logo)
  </read_first>
  <behavior>
    - POST multipart with a valid PNG/JPG/WebP under the cap → 200 and health_rules_config.prospect_image_url set to the public URL
    - a non-image mime → 400; an oversized file → 400
    - a non-leadership caller → 403
  </behavior>
  <action>
    Create `app/api/admin/health-rules/prospect-image/route.ts` (POST, multipart field `file`) mirroring the staff avatar route exactly but leadership-only (requireStaff(['leadership'])): validate mime against a PNG/JPG/WebP allowlist and enforce a size cap, upload to the vault-assets bucket under a health-rules/prospect-<timestamp>.<ext> path via the service client, getPublicUrl, update health_rules_config.prospect_image_url on the id=1 row, and logStaffAction. Write the route.test.ts first (RED) covering <behavior>, then implement.
  </action>
  <verify>
    <automated>npm test -- app/api/admin/health-rules/prospect-image</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- app/api/admin/health-rules/prospect-image` passes
    - Tests assert mime allowlist rejection, size-cap rejection, and non-leadership 403
    - On success the id=1 config row's prospect_image_url is written to the uploaded public URL
  </acceptance_criteria>
  <done>Leadership can swap the prospect marker image from inside Health Rules; it applies live via the config value with no code change.</done>
</task>

<task type="auto">
  <name>Task 3: Health Rules settings page + form with live preview</name>
  <files>app/(admin)/admin/health-rules/page.tsx, components/admin/HealthRulesForm.tsx</files>
  <read_first>
    - .planning/design/phase-31.1-health-rules-mockup.html — the target layout: thresholds, keeps-warm toggles, live state-split preview, the prospect-image control
    - lib/admin/gate.ts — requireStaffPage(['leadership']) for the page
    - lib/client-partners/health.ts (plan 02) — computeHealth for the client-side live preview
    - components/admin/ClientPartnersRoom.tsx (plan 04) — the data+string-props RSC-boundary convention to follow
  </read_first>
  <action>
    Create the RSC page `app/(admin)/admin/health-rules/page.tsx` (force-dynamic, requireStaffPage(['leadership'])) that loads the current config singleton + a lightweight sample of the book's health signals, and renders `components/admin/HealthRulesForm.tsx` ('use client') with plain serializable props: the config object, the sample signals, the current prospect_image_url (or null → neutral placeholder), and the string action paths (/api/admin/health-rules, /api/admin/health-rules/prospect-image). The form edits thresholds + toggles and shows a LIVE preview of the state split by running computeHealth over the sample signals on the client as the user drags — no server round-trip for the preview (D-06). Submit PATCHes the config; the prospect-image control POSTs the multipart upload and refreshes via router.refresh(). Ship a neutral placeholder marker in the prospect slot; do NOT embed any trademarked logo. No function props cross from the page.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean
    - HealthRulesForm is 'use client' and receives only data + string action paths (no function props from the page)
    - The preview recomputes client-side via computeHealth (no fetch in the preview path); a neutral placeholder renders when prospect_image_url is null
  </acceptance_criteria>
  <done>Leadership has a working Health Rules screen with a live preview and a swappable prospect image; saves apply immediately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff client → config/stage/image routes | Only leadership may read/tune the config, edit stages, or upload the prospect image |
| uploaded file → vault-assets storage | Untrusted image bytes cross into storage |
| RSC page → HealthRulesForm | Serialization boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-config-authz | Elevation/Info Disclosure (V4) | health-rules + pipeline-stages routes + page | high | mitigate | requireStaff(['leadership']) / requireStaffPage(['leadership']) on every handler and the page |
| T-31.1-mass-assign | Tampering (V5) | config PATCH + stage CRUD | high | mitigate | zod .strict() + explicit allowlist + threshold-ordering + KEY_REGEX; body never spread |
| T-31.1-file-upload | Tampering (V12) | prospect-image route | medium | mitigate | Copy the avatar route exactly: mime allowlist + size cap + service-role write to vault-assets |
| T-31.1-rsc-func-prop | DoS/availability (V1) | HealthRulesForm props | high | mitigate | Data + string action paths only; refresh via router.refresh() (Pitfall 1) |
| T-31.1-SC | Tampering (supply chain) | npm installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `npm test -- app/api/admin/health-rules app/api/admin/pipeline-stages` green; `npx tsc --noEmit` clean.
- Manual (owner, post-push): tune a threshold and watch the live preview shift; upload a prospect image and confirm the tower marker updates for a never-licensed client.
</verification>

<success_criteria>
- Leadership can tune health thresholds + keeps-warm with a live preview, configure pipeline stages, and swap the prospect image — all leadership-gated and mass-assignment-safe.
</success_criteria>

<output>
Create `.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-05-SUMMARY.md` when done.
</output>
