---
phase: 31.1-ae-console-health-leadership-tower-telemetry-team-rbac
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/client-partners/health.ts
  - lib/client-partners/health.test.ts
  - lib/client-partners/columns.ts
  - lib/client-partners/columns.test.ts
autonomous: true
requirements: [R3]

must_haves:
  truths:
    - "computeHealth() is a pure function (no I/O) that returns exactly one of good | warning | at_risk | cold | prospect (D-06/D-31.1-02)"
    - "A client with no executed license (lastExecutedLicenseAt null) resolves to prospect — off the recency axis, never green (D-31.1-02/09)"
    - "The color is driven ONLY by executed-license recency against the configurable thresholds; last-contact never sets the color (D-31.1-02)"
    - "keeps-warm holds lift an at_risk/cold client up to warning when a hold signal is active AND its toggle is on (open brief / open deal / recent Selects / optional recent contact) (D-31.1-03)"
    - "Band boundaries follow the documented rule: good_within is an inclusive upper bound, each *_after is the exclusive lower bound of the next band — unit-tested at the exact day values (RESEARCH Open Q3)"
    - "columns.ts HealthValue is extended to include cold and prospect with deterministic HEALTH_RANK sort and a HEALTH_TONE map; prospect renders the configurable image marker, not a color"
    - "The All-tab Assigned-AE column def exists in columns.ts for the leadership tower"
  artifacts:
    - "lib/client-partners/health.ts (computeHealth, HealthState, HealthRulesConfig, daysBetween/within helpers)"
    - "lib/client-partners/health.test.ts"
    - "extended lib/client-partners/columns.ts (HealthValue, HEALTH_RANK, HEALTH_TONE, Assigned-AE ColumnDef)"
    - "extended lib/client-partners/columns.test.ts"
  key_links:
    - "computeHealth() consumes HealthRulesConfig (the health_rules_config row shape from migration 128) — the Health Rules screen (plan 05) writes it, the room (plan 04) reads it"
    - "HealthValue's cold/prospect additions are what ClientPartnersList's HealthChip (plan 04) renders in the tower + client card"
  prohibitions:
    - "computeHealth() MUST NOT perform any I/O (no Supabase client, no fetch) — signals are passed in as plain values (D-06 compute-on-read)"
    - "A client with no executed license MUST NOT resolve to good or warning via any code path — prospect only"
    - "last-contact recency MUST NOT change the resolved color band except through the explicit keep_warm_recent_contact hold when its toggle is enabled"
---

<objective>
Build the pure relationship-health engine and extend the shared column model to carry the 5-state health value. This is the compute-on-read spine of R3 (D-06): a unit-testable function mirroring lib/client-partners/columns.ts / lib/crate-requests/ranking.ts.

Purpose: separate the health rules (thresholds + keeps-warm) from any rendering or I/O so the exact boundary and keeps-warm semantics are machine-locked before any screen renders them.
Output: lib/client-partners/health.ts (+ test) and the extended columns.ts (+ test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-CONTEXT.md
@.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-RESEARCH.md
@lib/client-partners/columns.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure health engine — lib/client-partners/health.ts + tests</name>
  <files>lib/client-partners/health.ts, lib/client-partners/health.test.ts</files>
  <read_first>
    - lib/client-partners/columns.ts — the pure-module convention (types-first, no I/O, exported for isolated tests) and the existing resolveHealth 'unknown' guardrail
    - .planning/phases/31.1-.../31.1-RESEARCH.md § "Health Compute-on-Read Approach (D-06)" (the computeHealth signature sketch) and § Open Questions #3 (boundary semantics)
    - .planning/phases/31.1-.../31.1-CONTEXT.md D-31.1-02 (five states + prospect off-axis), D-31.1-03 (keeps-warm), D-31.1-09 (executed license = the clock)
  </read_first>
  <behavior>
    - lastExecutedLicenseAt null → 'prospect' (regardless of any other signal)
    - executed 30 days ago, thresholds default → 'good'
    - executed exactly good_within_days ago → 'good' (inclusive upper bound)
    - executed good_within_days+1 ago → 'warning'
    - executed exactly warning_after_days ago → 'warning'; warning_after_days+1 → 'at_risk'
    - executed exactly at_risk_after_days ago → 'at_risk'; at_risk_after_days+1 → 'at_risk' until cold_after_days, then 'cold'
    - executed past cold_after_days → 'cold'
    - an at_risk client with an open brief AND keep_warm_open_brief on → held at 'warning'
    - a cold client with a recent Selects within recent_selects_days AND keep_warm_recent_selects on → held at 'warning'
    - a hold signal whose toggle is OFF does not lift the band
    - keeps-warm never lifts above 'warning' and never downgrades a 'good'/'warning' client
  </behavior>
  <action>
    Create `lib/client-partners/health.ts` exporting: type `HealthState = 'good' | 'warning' | 'at_risk' | 'cold' | 'prospect'`; type `HealthRulesConfig` matching the migration-128 health_rules_config columns (good_within_days, warning_after_days, at_risk_after_days, cold_after_days, keep_warm_open_brief, keep_warm_open_deal, keep_warm_recent_selects, recent_selects_days, keep_warm_recent_contact, recent_contact_days); a `HealthSignals` input type (lastExecutedLicenseAt, hasOpenBrief, hasOpenDeal, lastSelectsSentAt, lastContactAt, optional now); and `computeHealth(signals, rules): HealthState`. Implement the band ladder with the documented inclusive-good / exclusive-*_after boundary rule, then apply the keeps-warm lift (only when a signal is active AND its toggle is on; only lifts at_risk/cold up to warning). Add small pure helpers daysBetween(iso, nowMs) and within(iso, days, nowMs). Follow TS strict, 2-space, no semicolons, named exports. Write `health.test.ts` first covering every case in <behavior> plus the exact boundary day values; run it RED, then implement to GREEN.
  </action>
  <verify>
    <automated>npm test -- lib/client-partners/health.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/client-partners/health.test.ts` passes with explicit assertions at each band boundary day value
    - A test asserts lastExecutedLicenseAt null → 'prospect' and that no signal combination changes that
    - A test asserts a toggle-off hold signal does NOT lift the band; a toggle-on hold lifts at_risk→warning only
    - computeHealth references no Supabase/fetch import (grep of the module shows no client import)
  </acceptance_criteria>
  <done>The 5-state health function is pure, boundary-exact, and keeps-warm-correct, locked by jest.</done>
</task>

<task type="auto">
  <name>Task 2: Extend columns.ts HealthValue (+ tone/rank) and add the Assigned-AE column</name>
  <files>lib/client-partners/columns.ts, lib/client-partners/columns.test.ts</files>
  <read_first>
    - lib/client-partners/columns.ts — current HealthValue union, HEALTH_RANK, resolveHealth, sortRows and its stable identity tiebreak
    - lib/client-partners/columns.test.ts — the existing shuffle-invariant sort test to keep passing
    - lib/client-partners/health.ts (from Task 1) — align HealthValue to HealthState so the render layer uses one vocabulary
  </read_first>
  <action>
    Extend `HealthValue` in columns.ts from good|warning|at_risk|unknown to also include 'cold' and 'prospect'. Extend HEALTH_RANK to keep a deterministic order (e.g. at_risk < cold < warning < prospect < unknown < good, or per the mockup) — preserve resolveHealth's default-to-unknown guardrail and sortRows' stable identity tiebreak. Add a HEALTH_TONE map documenting that 'prospect' is a symbol/image marker (not a color) and 'unknown' stays the dashed no-data slot. Add an Assigned-AE ColumnDef (key 'assignedAe', label 'Assigned AE', not identity) to COMPANIES_COLUMNS for the leadership All tower, plus its sortValueFor branch reading a new optional row field (e.g. row.assignedAeName). Extend columns.test.ts to assert the new ranks sort deterministically and that a prospect/cold row sorts to its documented rank.
  </action>
  <verify>
    <automated>npm test -- lib/client-partners/columns.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- lib/client-partners/columns.test.ts` passes, including the pre-existing shuffle-invariant sort test
    - HealthValue includes 'cold' and 'prospect'; HEALTH_RANK has an entry for all six values
    - The Assigned-AE column def exists in COMPANIES_COLUMNS and sorts by the new row field
  </acceptance_criteria>
  <done>The shared column model speaks the 5-state health vocabulary and carries the tower's Assigned-AE column, deterministically sortable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | Pure logic modules: no external input crosses a boundary here; inputs are supplied by already-gated server callers (plans 04/05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-color-clock | Tampering (integrity of the signal) | computeHealth band logic | medium | mitigate | Color is derived solely from executed-license recency + config; no-license→prospect invariant and boundary semantics are unit-locked (D-31.1-02/09) |
| T-31.1-SC | Tampering (supply chain) | npm installs | low | accept | No package installs this phase (RESEARCH: no new dependencies) |
</threat_model>

<verification>
- `npm test -- lib/client-partners/health.test.ts` and `npm test -- lib/client-partners/columns.test.ts` green.
</verification>

<success_criteria>
- A pure, boundary-exact 5-state health engine and an extended column model exist and are jest-locked; no rendering or I/O introduced.
</success_criteria>

<output>
Create `.planning/phases/31.1-ae-console-health-leadership-tower-telemetry-team-rbac/31.1-02-SUMMARY.md` when done.
</output>
