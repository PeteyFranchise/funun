---
phase: 09-rich-member-profile
plan: 05
type: execute
wave: 4
depends_on: [09-01b, 09-02, 09-04]
files_modified:
  - components/profile/ProfileView.tsx
  - app/u/[handle]/page.tsx
autonomous: true
requirements: [PROFILE-01, PROFILE-03, PROFILE-06, PROFILE-08, PROFILE-09]
must_haves:
  truths:
    - "The profile header renders a presence-dot slot that shows nothing until Phase 11 wires a real signal (no false 'Online')"
    - "The Stats card shows a Placements landed row (money-gradient) in the correct order with a 'Self-reported by artist' caption"
    - "Owner sees a working Share button, avatar/banner upload affordances, and a Featured picker; visitor sees Follow/Message + a one-item more-options menu"
    - "The visitor Share/more-options affordance is omitted server-side when the profile owner's allow_resharing is false"
  artifacts:
    - components/profile/ProfileView.tsx
    - app/u/[handle]/page.tsx
  key_links:
    - "ProfileView mounts ShareButton/ProfileMoreMenu/FeaturedPicker/AvatarBannerUpload from Plans 02+04"
    - "page.tsx reads allow_resharing + owner releases and passes them into ProfileView for the reshare gate + picker"
    - "presence dot renders nothing when online is undefined/false (Phase 11 owns the real signal)"
---

<objective>
Integrate every Phase 9 profile surface into the two owned files — `ProfileView.tsx` and `app/u/[handle]/page.tsx` — mounting the components built in Plans 02 and 04: presence dot slot (PROFILE-01), placements stat + self-reported caption (PROFILE-06), wired Share button + avatar/banner upload + Featured picker for owners, and the visitor more-options menu, all gated correctly (PROFILE-08, PROFILE-09).

Purpose: Plans 02/03/04 built standalone components and API/schema. This plan is the single integration point that wires them into the actual profile page — kept separate so ProfileView.tsx has exactly one editing plan (no merge conflict across parallel waves).
Output: The rich member profile is fully assembled and behaves owner-vs-visitor per the locked design.

PROFILE-03 (location + tenure "On Funūn since [year]") is already fully implemented and rendering in ProfileView.tsx (`location` field + `since` derived from `created_at` in `buildProfileData()`) per RESEARCH.md — this plan owns ProfileView.tsx and confirms these render unchanged; it does NOT rebuild them (deep_work_rules: do not re-build already-correct shipped UI). PROFILE-03 is listed here for traceability only, verified by the existing `data.location`/`data.since` render remaining intact after this plan's edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rich-member-profile/09-RESEARCH.md
@.planning/phases/09-rich-member-profile/09-PATTERNS.md
@.planning/phases/09-rich-member-profile/09-UI-SPEC.md
@components/profile/ProfileView.tsx
@app/u/[handle]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Presence dot slot + placements stat + self-reported caption in ProfileView</name>
  <files>components/profile/ProfileView.tsx</files>
  <read_first>
    - components/profile/ProfileView.tsx (full — avatar block lines ~164-169, Stats card lines ~307-313 with Stat rows, OPEN_TO_LABELS, band()/fmtNum helpers; ProfileData type already carries placementsCount after Plan 01)
    - .planning/phases/09-rich-member-profile/09-UI-SPEC.md (section 1 presence dot — .pf-avatar .live pill spec, RENDER NOTHING when online undefined/false, NO hardcoded Online; section 2 placements stat — Stat row, insert as 3rd row order Followers→Monthly listeners→Placements landed→Avg. readiness, mtext money-gradient, "Self-reported by artist" caption under Stats header)
    - .planning/phases/09-rich-member-profile/09-RESEARCH.md (Pitfall 2 — presence dot is inert markup this phase, do NOT subscribe to any Realtime channel; A1 assumption)
    - .planning/phases/09-rich-member-profile/09-CONTEXT.md (PROFILE-01 presence dot, PROFILE-06 placements)
  </read_first>
  <action>In `components/profile/ProfileView.tsx`: (a) Add a `<PresenceDot online?: boolean>` element (inline component or small helper) positioned absolute bottom-12px/right-12px on the 168px avatar circle, matching the `.pf-avatar .live` spec (bg `--bg`, 1px border-strong, emerald text + 7px dot, 12px/700 label, 999px radius). When `online` is `undefined` or `false`, RENDER NOTHING — omit the pill entirely (Pitfall 2 / UI-SPEC section 1: no hardcoded "Online", no "Offline" state). Do NOT subscribe to any Supabase Realtime Presence channel — that is Phase 11. Wire the avatar container to be `relative` so the dot can absolutely position. (b) In the Stats card, insert a "Placements landed" `Stat` row as the 3rd row so the order is Followers → Monthly listeners → Placements landed → Avg. readiness; its value is `fmtNum(data.placementsCount)` styled with the money-gradient class (`mtext`, amber), distinct from avg.-readiness's `gtext`. Add a single "Self-reported by artist" caption (11px, `lavdim`) once under the Stats card header (not per row) — closes the REQUIREMENTS.md Out-of-Scope labeling gap. Do NOT change the Followers/Monthly listeners/Avg. readiness rows' existing wiring.</action>
  <verify>
    <automated>grep -c "Placements landed" components/profile/ProfileView.tsx && grep -c "Self-reported by artist" components/profile/ProfileView.tsx && grep -Ec "\.channel\(|postgres_changes" components/profile/ProfileView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Placements row present in correct position: `grep -c "Placements landed" components/profile/ProfileView.tsx` >=1, and it renders `data.placementsCount` with an `mtext` class.
    - Self-reported caption present once: `grep -c "Self-reported by artist" components/profile/ProfileView.tsx` >=1.
    - Presence dot is honest inert markup: no hardcoded always-on "Online" label rendered in the avatar block (`grep -c ">Online<" components/profile/ProfileView.tsx` returns 0), and the dot element is guarded so it renders nothing when its `online` prop is undefined/false.
    - No Realtime subscription is added in this phase: `grep -Ec "\.channel\(|postgres_changes" components/profile/ProfileView.tsx` returns 0 (the `PresenceDot`/presence-dot identifiers are expected and are NOT gated by a negative grep — that word appears in the component name by design).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Presence-dot slot ships as honest inert markup, placements stat lands in the right order with the money gradient + self-reported caption.</done>
</task>

<task type="auto">
  <name>Task 2: Mount Share button, avatar/banner upload, Featured picker (owner) + more-options menu (visitor)</name>
  <files>components/profile/ProfileView.tsx</files>
  <read_first>
    - components/profile/ProfileView.tsx (full — Actions block lines ~207-243 owner vs visitor; banner div lines ~152-160; avatar div lines ~164-169; Featured section lines ~263-282; Roles owner block lines ~328-334)
    - components/profile/ShareButton.tsx (Plan 04 — props url/caption/className/label)
    - components/profile/ProfileMoreMenu.tsx (Plan 04 — props profileUrl/caption)
    - components/profile/FeaturedPicker.tsx (Plan 04 — props releases/currentFeaturedId)
    - components/profile/AvatarBannerUpload.tsx (Plan 02 — variant avatar/banner, currentUrl)
    - .planning/phases/09-rich-member-profile/09-UI-SPEC.md (section 3 upload affordance placement; section 4c Featured picker near section header; section 5 Share wiring + captions; section 9 visitor menu one item; Copywriting Contract profile-share caption "Check out {artistName} on Funūn → {url}")
    - .planning/phases/09-rich-member-profile/09-CONTEXT.md (D-04 two share entry points, D-07 visitor reshare gate)
  </read_first>
  <action>
    In `components/profile/ProfileView.tsx`, add the props this plan needs to the component signature: `profileUrl: string` (the absolute /u/[handle] URL), `allowResharing: boolean` (owner's setting), and `ownerReleases?: {id,title,typeLabel,year,coverUrl,isPublic}[]` (for the Featured picker; owner mode only). Then:
    (a) Owner actions (mode==='owner'): replace the dead `<button>Share</button>` stub (lines ~214-216) with `<ShareButton url={profileUrl} caption={\`Check out ${data.name} on Funūn → ${profileUrl}\`} className="[reuse the existing stub className verbatim]" label="Share" />`. Keep "Edit profile" and the "Analytics" stub as-is (Analytics is out of scope — leave the disabled stub).
    (b) Owner avatar/banner upload: mount `<AvatarBannerUpload variant="banner" currentUrl={data.bannerUrl} />` as the frosted "Edit cover" affordance inset top-right of the banner div, and `<AvatarBannerUpload variant="avatar" currentUrl={data.avatarUrl} />` as the hover overlay on the avatar circle — BOTH owner-only (only render when mode==='owner'). Make the banner and avatar containers `relative` if not already.
    (c) Owner Featured picker: render `<FeaturedPicker releases={ownerReleases ?? []} currentFeaturedId={/* from data or a new prop */} />` as an owner-only ghost button near the "Featured" section header (line ~265). Visitors do not see the picker.
    (d) Visitor more-options: in the visitor actions branch (mode==='public'), after Follow/Message, render `<ProfileMoreMenu profileUrl={profileUrl} caption={\`Check out ${data.name} on Funūn → ${profileUrl}\`} />` ONLY when `allowResharing` is true (D-07 — the visitor reshare/copy-link affordance is omitted server-side when the owner disallows resharing). The owner always sees their own Share button regardless of allowResharing.
    Reuse existing classNames verbatim per UI-SPEC ("wire onClick only, no restyle"). Do NOT add Report/Block (Phase 13).
  </action>
  <verify>
    <automated>grep -c "ShareButton\|AvatarBannerUpload\|FeaturedPicker\|ProfileMoreMenu" components/profile/ProfileView.tsx && grep -c "allowResharing" components/profile/ProfileView.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All four components mounted: `grep -c "ShareButton" components/profile/ProfileView.tsx` >=1, `AvatarBannerUpload` >=1 (both variants), `FeaturedPicker` >=1, `ProfileMoreMenu` >=1.
    - Dead Share stub removed: the plain `<button ...>Share</button>` with no onClick no longer exists (replaced by ShareButton).
    - Visitor more-options is gated on `allowResharing`: `grep -c "allowResharing" components/profile/ProfileView.tsx` >=1 and the ProfileMoreMenu render is inside an `allowResharing &&` conditional (server-side omission, not CSS).
    - Avatar/banner upload + Featured picker render only in owner mode (inside `mode === 'owner'` branches).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Owner gets Share + upload + Featured picker; visitor gets a reshare-gated one-item more-options menu; no Report/Block; classNames unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Feed allow_resharing, owner releases, and profileUrl from the page</name>
  <files>app/u/[handle]/page.tsx</files>
  <read_first>
    - app/u/[handle]/page.tsx (full — profile SELECT list line ~114 (add allow_resharing), projects query lines ~122-127, buildProfileData call + ProfileView render lines ~202-213; note this file also has the owner/self path — confirm how mode is chosen)
    - components/profile/ProfileView.tsx (Task 2's new required props: profileUrl, allowResharing, ownerReleases, currentFeaturedId)
    - .planning/phases/09-rich-member-profile/09-RESEARCH.md (allow_resharing read server-side; profile column list must stay in sync with migration 040/043 GRANT SELECT)
    - .planning/STATE.md (08-05 note: the public column SELECT list must match migration 040's GRANT SELECT list — 043 adds allow_resharing to that grant)
  </read_first>
  <action>In `app/u/[handle]/page.tsx`: (a) Add `allow_resharing` to the explicit `artist_profiles` SELECT column list (line ~114) — it is now in migration 043's public GRANT SELECT, so the list must include it to stay in sync (the 08-05 drift lesson). (b) Compute the absolute profile URL (`profileUrl`) SERVER-SIDE from the existing `NEXT_PUBLIC_APP_URL` env var (grep confirms it already exists and is consumed across the app — `lib/notifications/index.ts`, `app/api/pitches/route.ts`, `app/(auth)/forgot-password/page.tsx`, etc.). Chosen strategy (exactly one, no fallback ambiguity): build `` `${process.env.NEXT_PUBLIC_APP_URL}/u/${handle}` `` in the page component and pass the resulting ABSOLUTE string as the `profileUrl` prop. Assert `NEXT_PUBLIC_APP_URL` is configured — if it is missing/empty, throw a loud descriptive Error ("NEXT_PUBLIC_APP_URL is not configured — profile share links require an absolute base URL") rather than silently falling back to a relative path or guessing the host from `req` (App Router page components have no `req` object). This yields a fully-resolved absolute URL server-side so `ShareButton`/`ProfileMoreMenu` can call `navigator.share({ url: profileUrl })` synchronously in the click handler with NO intervening `await` (RESEARCH Pitfall 5 — Safari user-gesture window; the URL is already absolute, no client-side origin resolution needed). (c) Determine `allowResharing` from `profile.allow_resharing` (default false when null). For the OWNER view, `allowResharing` gating does not hide the owner's own Share button (Task 2 already handles that); pass the owner's own value through. (d) Build `ownerReleases` from the already-fetched `projects` (map to `{id,title,typeLabel,year,coverUrl,isPublic}`) and pass it plus `currentFeaturedId={profile.featured_project_id}` into `<ProfileView>` ONLY for the owner path (or always, since the picker is owner-gated inside ProfileView). Pass `profileUrl` and `allowResharing` into `<ProfileView>` on both owner and public renders. Ensure the owner/self render path (the `/profile` or self-view branch, if this page handles it) passes the same new props.</action>
  <verify>
    <automated>grep -c "allow_resharing" app/u/[handle]/page.tsx && grep -c "allowResharing\|profileUrl\|ownerReleases" app/u/[handle]/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `allow_resharing` added to the SELECT column list: `grep -c "allow_resharing" app/u/[handle]/page.tsx` >=1.
    - New props passed to ProfileView: `grep -c "allowResharing" app/u/[handle]/page.tsx` >=1 and `profileUrl` passed; `ownerReleases`/`currentFeaturedId` passed for the owner path.
    - `profileUrl` is built from `NEXT_PUBLIC_APP_URL` server-side and is absolute: `grep -c "NEXT_PUBLIC_APP_URL" app/u/[handle]/page.tsx` >=1; the value passed as `profileUrl` matches `/^https?:\/\//` (absolute origin + `/u/${handle}`), never a bare relative `/u/...` path. The missing-env case throws loudly (no silent relative fallback).
    - `npx tsc --noEmit` clean and `npm run build` succeeds (whole app type-checks with the new ProfileView signature).
    - The public column SELECT list still matches migration 043's GRANT SELECT (allow_resharing included, no private column added).
  </acceptance_criteria>
  <done>The page reads allow_resharing (in sync with the 043 grant), builds owner-release data and the share URL, and feeds all new props into ProfileView for both owner and visitor renders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public visitor → /u/[handle] SSR | Unauthenticated render; the is_public gate + public column allowlist decide what is projected |
| allow_resharing (owner setting) → visitor markup | Server read decides whether the visitor Share/more-options affordance is emitted (D-07) |
| featured_project_id → public profile | Only an owned public release can be pinned (Plan 01 pre-check + migration 034 trigger) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-16 | Information Disclosure | u/[handle] SELECT drifting to include a private column when adding allow_resharing | high | mitigate | allow_resharing is public (migration 043 GRANT SELECT); the SELECT list is kept identical to the 043 grant list — no private column (legal name, contact) added; the 08-05 drift lesson is explicitly followed |
| T-09-17 | Elevation of Privilege | Visitor re-enabling the more-options/Share affordance disabled by allow_resharing | low | mitigate | The visitor ProfileMoreMenu is omitted from server-rendered markup when allow_resharing is false (server-side conditional, not CSS); note sharing a public /u URL is not privileged, so this is a UX-preference boundary |
| T-09-18 | Spoofing | Presence dot falsely implying a member is online | medium | mitigate | Presence dot renders nothing until a real signal exists (Phase 11); no hardcoded "Online" — the honest default avoids misleading the viewer |
| T-09-19 | Information Disclosure | Featured picker or owner-release data leaking to a visitor render | medium | mitigate | ownerReleases/FeaturedPicker are gated to owner mode inside ProfileView; the visitor render path never mounts the picker |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Grep gates: ProfileView mounts ShareButton/AvatarBannerUpload/FeaturedPicker/ProfileMoreMenu; presence dot has no hardcoded Online and no Realtime subscription; placements row + self-reported caption present; page reads allow_resharing and passes new props.
- Full Jest suite green: `npx jest`.
- Manual (per 09-VALIDATION.md): as owner, upload avatar+banner and confirm persistence; toggle resharing off and confirm the visitor Share/more-options affordance disappears for a logged-out viewer; confirm the presence dot shows nothing (no false Online); confirm Placements landed row + caption render.
</verification>

<success_criteria>
- Presence-dot slot renders nothing until Phase 11 (no false Online, no Realtime subscription).
- Placements stat renders in the correct order with the money gradient + a single self-reported caption.
- Owner sees a working Share button, avatar/banner upload, and Featured picker; visitor sees Follow/Message + a one-item more-options menu (gated by allow_resharing).
- The whole app type-checks and builds with the assembled profile.
</success_criteria>

## Artifacts this phase produces

New symbols/files created by this plan (exclude from downstream drift "unexplained new symbol" flags):

- Symbol: `PresenceDot` (inline component/helper in `components/profile/ProfileView.tsx`; prop `online?: boolean`; renders nothing when undefined/false — Phase 11 wires the real signal)
- Modified: `components/profile/ProfileView.tsx` — new props `profileUrl: string`, `allowResharing: boolean`, `ownerReleases?`, `currentFeaturedId?`; "Placements landed" Stat row + "Self-reported by artist" caption; mounts `ShareButton`, `AvatarBannerUpload` (avatar+banner), `FeaturedPicker`, `ProfileMoreMenu`
- Modified: `app/u/[handle]/page.tsx` — reads `allow_resharing` (added to the public SELECT column list, in sync with migration 043's GRANT SELECT), builds `ownerReleases` + `profileUrl`, passes new props to `ProfileView`

<output>
Create `.planning/phases/09-rich-member-profile/09-05-SUMMARY.md` when done.
</output>
