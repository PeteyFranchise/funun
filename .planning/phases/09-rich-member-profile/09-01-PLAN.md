---
phase: 09-rich-member-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/041_artist_profiles_allow_resharing.sql
  - lib/metadata/schema.ts
  - types/index.ts
  - components/player/PublicPlayer.tsx
  - components/player/useShare.ts
  - components/ui/Toast.tsx
  - components/profile/ProfileShareButton.tsx
  - components/profile/ProfileView.tsx
  - app/r/[projectId]/page.tsx
  - app/api/profile/route.ts
  - components/profile/ProfileForm.tsx
  - app/(artist)/settings/page.tsx
  - lib/profile/load.ts
  - app/globals.css
autonomous: true
requirements: [PROFILE-08]
must_haves:
  truths:
    - "D-01: /r/[projectId] renders a stream-only public player, not the owner's PlaybackView — no master/stems toggle, no file-status, no ISRC/ISWC/BPM, no split percentages"
    - "D-02: the public player keeps the existing /r/[projectId] URL and the is_public gate; only the rendered component changes"
    - "D-04/D-05: Share uses the Web Share API with a clipboard-copy + toast fallback, on both the player (per-track) and the owner profile header"
    - "D-06: shared content carries a track-focused caption ending in the release link"
    - "D-07: visitors see the player Share affordance only when the artist's allow_resharing toggle is on; the owner always sees it"
    - "D-08..D-12: credits (names + roles only, no splits) and static lyrics are reachable from the overflow menu, reusing readComposers/readLyrics"
    - "D-13: TrackLyrics carries an optional time-synced `lines` shape (additive, no DDL); the player ships static text"
  artifacts:
    - supabase/migrations/041_artist_profiles_allow_resharing.sql
    - components/player/PublicPlayer.tsx
  key_links:
    - "migration 041 grants SELECT(allow_resharing) to authenticated/anon because migration 040 locked artist_profiles to column-scoped grants; the /r/ page reads the flag on the session/anon client"
    - "no UPDATE grant for allow_resharing — it is written via createServiceClient() in app/api/profile/route.ts after an ownership check, mirroring migration 040's service-written-column reasoning"
---

<objective>
Close the Phase 9 public-playback gap (PROFILE-08 Share): replace the owner's full-detail PlaybackView at the public `/r/[projectId]` route with a clean, stream-only "now playing" component that never exposes private rights data, and wire the Share action across the player and the owner profile.

Purpose: A public share link (already sent to curators via Wave 3 pitch emails) currently reuses `PlaybackView`, leaking split percentages, ISRC/ISWC/BPM, and master/stems file status to anyone. This plan builds the public-safe player per `artist-profile.html`, adds the Web Share + clipboard mechanism, the artist-controlled reshare toggle, and credits/lyrics panels — resolving decisions D-01 through D-14.

Output: migration 041, the `PublicPlayer` + share + toast components, the reshare toggle (schema + settings UI + API), and a forward-compatible timed-lyrics shape.
</objective>

<tasks>
1. Migration 041 — `artist_profiles.allow_resharing BOOLEAN NOT NULL DEFAULT FALSE` + `GRANT SELECT` to authenticated/anon (D-07). Extend `TrackLyrics` with an optional `lines` time-synced shape + `hasTimedLyrics()` (D-13, no DDL). Add `allow_resharing` to the `ArtistProfile` type and demo fixtures.
2. Build `PublicPlayer` (stream-only, credits + static lyrics sheets, overflow menu), `useShare` (Web Share + clipboard fallback), and a minimal `Toast`. Swap `/r/[projectId]` to render it, dropping the private fields and reading `allow_resharing` + owner identity.
3. Wire the owner profile Share button (`ProfileShareButton`), add `allow_resharing` to the profile PATCH allowlist, and add the settings toggle.
</tasks>
