---
phase: 09-rich-member-profile
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - components/vault/PublicPlaybackView.tsx
  - components/vault/LyricsPanel.tsx
  - app/r/[projectId]/page.tsx
autonomous: true
requirements: [PROFILE-07, PROFILE-08]
must_haves:
  truths:
    - "A signed-out visitor at /r/[projectId] for a public release sees a clean now-playing player and can play the share MP3"
    - "The public player shows NO Master/Stems toggle, NO ISRC/ISWC/BPM table, NO split percentages, NO file-management UI"
    - "The overflow menu offers View credits (names+roles only) and Copy link always, and View lyrics only when lyrics exist"
    - "Lyrics open in a slide-up panel while the audio keeps playing underneath; dismiss returns to the player"
  artifacts:
    - components/vault/PublicPlaybackView.tsx
    - components/vault/LyricsPanel.tsx
  key_links:
    - "app/r/[projectId]/page.tsx keeps the is_public gate + service-client signed-URL mint; only the rendered component changes (D-02)"
    - "toTrackViews() feeds names+roles credits (no split) + lyrics text into PublicPlaybackView"
    - "allow_resharing read server-side gates whether the visitor Share affordance is rendered (D-07)"
---

<objective>
Split the public player from the private one (D-01, D-02, D-03): build a brand-new stream-only `PublicPlaybackView` sibling component matching `artist-profile.html`, a `LyricsPanel` slide-up (D-08, D-09, D-13 static), and swap the component rendered at the UNCHANGED `/r/[projectId]` route. Includes the overflow (⋯) menu (D-10, D-11, D-14) and the visitor Share affordance gated by `allow_resharing` (D-07).

Purpose: `/r/[projectId]` currently reuses the full-detail owner `PlaybackView` (master/stems toggle, split %, ISRC/ISWC/BPM), over-exposing internal detail on a public share link. The URL stays identical (preserves Wave 3 pitch-email links); only the rendering changes.
Output: Two new components + a one-call render swap in the existing route. The owner's `PlaybackView.tsx` and `/vault/[projectId]/play` are UNTOUCHED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rich-member-profile/09-CONTEXT.md
@.planning/phases/09-rich-member-profile/09-RESEARCH.md
@.planning/phases/09-rich-member-profile/09-PATTERNS.md
@.planning/phases/09-rich-member-profile/09-UI-SPEC.md
@app/r/[projectId]/page.tsx
@components/vault/PlaybackView.tsx
@components/collaborators/CollaboratorPicker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PublicPlaybackView — stream-only now-playing player + overflow menu</name>
  <files>components/vault/PublicPlaybackView.tsx</files>
  <read_first>
    - components/vault/PlaybackView.tsx (full — REFERENCE ONLY for transport/scrub/audio-ref state shape; lines ~77-117 state+audio, ~262-302 transport SVGs, ~394-400 hidden audio element. Do NOT import or fork it — D-01.)
    - .planning/phases/09-rich-member-profile/09-PATTERNS.md (PublicPlaybackView section — exactly which patterns to copy and the explicit "do NOT copy" list: master/stems toggle, files/stems section, waveform, split %, ISRC/ISWC/BPM dl)
    - .planning/phases/09-rich-member-profile/09-UI-SPEC.md (section 6 full pixel spec — app bar, 560px hero art + scrim gradient, meta block, 6px scrub bar, 78px play circle, transport 42px gaps, "More from {Artist}" up-next; section 8 overflow menu items+order)
    - .planning/phases/09-rich-member-profile/09-CONTEXT.md (D-01, D-03, D-10, D-11, D-14 — scope of the public player and overflow menu)
    - components/collaborators/CollaboratorPicker.tsx (click-outside dropdown pattern lines ~45-56 — reuse the mousedown listener for the overflow menu)
  </read_first>
  <action>Create `components/vault/PublicPlaybackView.tsx` (`'use client'`) as a genuinely new sibling — do NOT import `PlaybackView`. Props: `releaseTitle`, `artist` (name), `coverUrl`, `tracks: TrackView[]` (share-MP3 signed URLs already resolved by the page), `projectId`, `allowResharing: boolean`, and the artist's `monthlyListeners`/avatar for the meta block if available. Reuse the audio-ref/play/position state pattern and the hidden `<audio onTimeUpdate onEnded>` element verbatim from PlaybackView (per PATTERNS). Render per UI-SPEC section 6: app bar (42px frosted back-chevron + "NOW PLAYING" centered + 42px overflow ⋯ button); 560px hero art with the exact scrim gradient; meta block (FUNŪN pill, eyebrow release-type, 58px/900 title, artist row); a simple 6px scrub bar (NOT a waveform) with knob + times row (tabular-nums); transport row (shuffle · prev · 78px gradient play circle with the specified shadow · next · repeat) copying the inline-SVG icon markup; "More from {Artist}" up-next list of the release's tracks (D-03 — the existing `tracks[]`, no new model), currently-playing row styled with gtext + equalizer glyph. Stream ONLY the resolved share-MP3 URL (`TrackView.audioUrl`); never reference `metadata.master`. Build the overflow (⋯) menu with the CollaboratorPicker click-outside pattern (mousedown listener), anchored below-right, items in exact order: View credits → View lyrics (omit entirely when the active track has no lyrics, D-09) → Copy link. "View credits" opens a lightweight names+roles list from the track's `credits` (names + role labels ONLY — NO split percentage anywhere, D-11). "Copy link" runs the clipboard-copy of the D-06 track caption+URL with the "Link copied!" inline confirmation (reuse the label-swap convention, no toast lib). The Share affordance (Web Share / clipboard) for the whole track is rendered ONLY when `allowResharing` is true (D-07) — the page passes `false` to omit it; do not CSS-hide. EXPLICITLY DO NOT render: Master/Stems toggle, waveform, Files/StemsUpload, ISRC/ISWC/BPM table, split percentages.</action>
  <verify>
    <automated>test -f components/vault/PublicPlaybackView.tsx && grep -Ec "import .*(from )?['\"](@/components/vault/PlaybackView|\./PlaybackView)['\"]" components/vault/PublicPlaybackView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Component exists, is `'use client'`. It does NOT import the private player: an import of the sibling `PlaybackView` module resolves to 0 matches via `grep -Ec "import .*['\"](@/components/vault/PlaybackView|\./PlaybackView)['\"]" components/vault/PublicPlaybackView.tsx` (the substring PublicPlaybackView in its own name is expected and does not count — scope the grep to the import line, not the whole file).
    - The excluded owner-only features (Master/Instrumental toggle, waveform, stems/files upload, the identifier-metadata table, split percentages) render nowhere: verify by reading the component's JSX — no toggle control, no `dl`/table of track identifiers, no `%`-suffixed split value. This is a read-based assertion, NOT a whole-file negative grep (the feature names appear in this plan's guidance and must not be turned into a self-invalidating `== 0` gate).
    - Overflow menu items present in order (grep confirms "View credits", "View lyrics", "Copy link" substrings exist).
    - Share affordance is conditional on an `allowResharing` prop (grep shows `allowResharing` guarding the JSX, not a CSS class).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A clean stream-only player renders per artist-profile.html with an overflow menu (credits names+roles only, lyrics-when-present, copy link) and a reshare-gated Share affordance; no owner-only detail leaks.</done>
</task>

<task type="auto">
  <name>Task 2: LyricsPanel slide-up (static display, D-13-forward-compatible)</name>
  <files>components/vault/LyricsPanel.tsx</files>
  <read_first>
    - .planning/phases/09-rich-member-profile/09-UI-SPEC.md (section 7 full pixel spec — bottom sheet lower 70-80%, #0E0D1E surface, 18px top radius, 36×4px drag-handle, 20px/800 title + 42px close button, 16px/500 body line-height 1.6, 280ms ease-out motion, prefers-reduced-motion cross-fade, dismiss via handle/close/scrim, player keeps running)
    - .planning/phases/09-rich-member-profile/09-CONTEXT.md (D-08 slide-up, D-09 hide when no lyrics, D-13 render lyrics.text static only, ignore lyrics.synced this phase)
    - .planning/phases/09-rich-member-profile/09-PATTERNS.md (LyricsPanel "No Analog Found" note — build fresh; dismiss-on-outside-click is the only reusable half)
    - lib/metadata/schema.ts (TrackLyrics type extended in Plan 01 — read lyrics.text; synced exists but is ignored this phase)
  </read_first>
  <action>Create `components/vault/LyricsPanel.tsx` (`'use client'`). Props: `open: boolean`, `onClose: () => void`, `trackTitle: string`, `lyricsText: string`. Render a bottom sheet overlay covering the lower ~70-80% of the player viewport with the `#0E0D1E` (`card`) surface, 18px top-corner radius, top border, centered 36×4px drag-handle 12px from top, a header (20px/800 track title + a 42px frosted round close button top-right), and a scrollable body (16px/500, line-height 1.6, `lav` text) rendering ONLY `lyricsText` (static — D-13; if `lyrics.synced` is present, ignore it, no line-highlight logic). The player keeps running underneath — this is an overlay, not a route change and not an audio-pausing modal. Motion: slide up/down 280ms ease-out (within app.css 160-480ms budget), no bounce; under `prefers-reduced-motion` use a cross-fade instead of slide. Dismiss on: the close button, the drag-handle tap, and a tap on the scrim outside the sheet. The parent (PublicPlaybackView) only mounts/opens this when the active track has non-empty lyrics (D-09), so the panel itself does not need an empty state.</action>
  <verify>
    <automated>test -f components/vault/LyricsPanel.tsx && grep -Ec "reduced-motion|prefers-reduced|motion-reduce" components/vault/LyricsPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Component exists, is `'use client'`, takes `open`/`onClose`/`trackTitle`/`lyricsText` props.
    - Renders static text only, no timestamped line-highlight logic: read the component and confirm the body maps over `lyricsText` as a single block (or split on newlines for layout) with no per-line active-index/highlight state driven by playback position (read-based assertion — a whole-file negative grep on the timestamped-shape key would self-invalidate against this plan's own guidance prose).
    - `prefers-reduced-motion` is respected: `grep -Ec "reduced-motion|prefers-reduced|motion-reduce" components/vault/LyricsPanel.tsx` returns >=1.
    - Dismiss handled by close button + scrim + handle (onClose wired to all three).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A static lyrics slide-up sheet that respects reduced-motion, keeps audio playing, and dismisses three ways — forward-compatible with D-13 sync (renders text only for now).</done>
</task>

<task type="auto">
  <name>Task 3: Swap the render at /r/[projectId] + strip split from credits + read allow_resharing + lyrics</name>
  <files>app/r/[projectId]/page.tsx</files>
  <read_first>
    - app/r/[projectId]/page.tsx (full — everything above the return is UNCHANGED: is_public gate line ~76, signed-URL mint lines ~92-100, toTrackViews lines ~25-50, PlaybackView render lines ~118-128)
    - .planning/phases/09-rich-member-profile/09-PATTERNS.md (app/r/[projectId]/page.tsx section — only the render call + toTrackViews credits strip changes; exact BEFORE/AFTER render props)
    - .planning/phases/09-rich-member-profile/09-CONTEXT.md (D-01, D-02 keep URL, D-11 credits no split, D-12 readLyrics reuse)
    - lib/metadata/schema.ts (readComposers, COMPOSER_ROLE_LABELS, readLyrics — reuse for credits names+roles + lyrics text)
    - components/vault/PublicPlaybackView.tsx (the new component's prop contract from Task 1)
  </read_first>
  <action>Modify ONLY the render path in `app/r/[projectId]/page.tsx`. Keep the `is_public` gate, the service-client signed-URL mint, and the artist-name lookup exactly as-is (D-02). In `toTrackViews()`: drop `splitTotal` and change `credits` to `composers.map(c => ({ name: c.name, role: COMPOSER_ROLE_LABELS[c.role] }))` — NO `split` field at all (D-11); add a `lyrics` field per track via `readLyrics(t.metadata)` returning the `.text` (or null when empty) so the panel can hide when absent (D-09). Replace the `<PlaybackView .../>` render with `<PublicPlaybackView releaseTitle={project.title} artist={artist} coverUrl={project.cover_art_url} tracks={tracks} projectId={projectId} allowResharing={allowResharing} />`. Add a read of the release owner's `allow_resharing`: extend the existing `artist_profiles` SELECT (the one already fetching `artist_name` by `user_id`) to also select `allow_resharing`, and pass its boolean value (default to `false` if null/absent so the visitor Share affordance is omitted unless explicitly allowed — D-07). Remove the now-unused `PlaybackView` import and the ISRC/ISWC/BPM/instrumental fields from the TrackView projection if the new component's TrackView shape no longer needs them (keep the shared `TrackView` type consistent — if PlaybackView still uses those fields, leave the type alone and simply stop passing the excluded UI). Do NOT touch `app/(artist)/vault/[projectId]/play/page.tsx` or `PlaybackView.tsx`.</action>
  <verify>
    <automated>grep -c "PublicPlaybackView" app/r/[projectId]/page.tsx && grep -c "allow_resharing" app/r/[projectId]/page.tsx && grep -Ec "<PlaybackView" app/r/[projectId]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Route renders `PublicPlaybackView`, not the private player: `grep -c "PublicPlaybackView" app/r/[projectId]/page.tsx` >=1 and `grep -Ec "<PlaybackView" app/r/[projectId]/page.tsx` returns 0.
    - `allow_resharing` is read and passed: `grep -c "allow_resharing" app/r/[projectId]/page.tsx` >=1.
    - Credits carry names + role labels only, no percentage field: read `toTrackViews()` and confirm each `credits` entry is `{ name, role }` with no split/percentage key and that `splitTotal` is removed from the projection (read-based assertion — not a whole-file `split` negative grep, which the guidance prose in this plan would otherwise self-invalidate).
    - The is_public gate and `createSignedUrls` mint are unchanged (grep confirms both still present).
    - `PlaybackView.tsx` and `app/(artist)/vault/[projectId]/play/page.tsx` are unmodified (`git diff --stat` shows no changes to them).
    - `npx tsc --noEmit` clean and `npm run build` succeeds.
  </acceptance_criteria>
  <done>The public URL renders the clean player with names+roles-only credits and reshare-gated Share; the owner's private room is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public visitor → /r/[projectId] | Unauthenticated request; the is_public column gate is the authorization for streaming |
| private track-audio bucket → visitor <audio> | Only server-minted short-lived signed URLs cross; raw storage paths never reach the client |
| artist_profiles.allow_resharing → rendered Share affordance | Server-side read decides whether the visitor Share element is emitted at all (D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08 | Information Disclosure | Public player leaking owner-only detail (split %, ISRC/ISWC/BPM, master file, stems) | high | mitigate | Brand-new sibling component renders none of those elements (D-01); credits stripped to names+roles in toTrackViews (D-11); only `audio_file_url` (share MP3) is signed, never `metadata.master` |
| T-09-09 | Information Disclosure | Draft/private release streaming on the open page | high | mitigate | The existing `is_public` gate in page.tsx is preserved unchanged (D-02); notFound() when not public |
| T-09-10 | Elevation of Privilege | Visitor re-enabling the Share affordance the artist turned off (allow_resharing=false) | low | mitigate | Share element is omitted server-side from rendered markup when allow_resharing is false, not CSS-hidden; note per research: sharing a public /r URL is not privileged (the URL is already public), so this is a UX-preference boundary, not an access-control one — no RLS change needed |
| T-09-11 | Tampering | Signed URL replay after expiry | low | accept | 2-hour signed-URL expiry is the existing, accepted posture (unchanged from current /r route); no new exposure introduced by this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Grep gates: PublicPlaybackView rendered, no PlaybackView import in the new component, no split/master/isrc in the public surface.
- `git diff --stat` shows NO changes to `components/vault/PlaybackView.tsx` or `app/(artist)/vault/[projectId]/play/page.tsx`.
- Manual (per 09-VALIDATION.md): as a signed-out visitor load `/r/[projectId]` for a public release — confirm stream-only playback, no master/stems toggle, no metadata table; open overflow → credits show names+roles no %; lyrics slide up while audio keeps playing.
</verification>

<success_criteria>
- Public visitors get a clean now-playing player at the unchanged /r/[projectId] URL streaming only the share MP3.
- No master/stems toggle, ISRC/ISWC/BPM, split %, or file management appears on the public surface.
- Overflow menu shows credits (names+roles) and copy-link always, lyrics only when present; lyrics slide-up keeps audio running.
- The owner's private PlaybackView and play route are byte-unchanged.
</success_criteria>

## Artifacts this phase produces

New symbols/files created by this plan (exclude from downstream drift "unexplained new symbol" flags):

- Component: `PublicPlaybackView` at `components/vault/PublicPlaybackView.tsx` (stream-only public now-playing player + overflow ⋯ menu; props include `releaseTitle`, `artist`, `coverUrl`, `tracks`, `projectId`, `allowResharing`)
- Component: `LyricsPanel` at `components/vault/LyricsPanel.tsx` (slide-up sheet; props `open`, `onClose`, `trackTitle`, `lyricsText`)
- Modified: `app/r/[projectId]/page.tsx` renders `PublicPlaybackView` (was `PlaybackView`); `toTrackViews()` credits reduced to `{ name, role }` (no split); per-track `lyrics` text added; `allow_resharing` read from `artist_profiles`
- `TrackView` per-track `lyrics` field feeding the panel (names+roles credits, no split)

<output>
Create `.planning/phases/09-rich-member-profile/09-03-SUMMARY.md` when done.
</output>
