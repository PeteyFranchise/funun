---
phase: 09-rich-member-profile
plan: 04
type: execute
wave: 3
depends_on: [09-01b]
files_modified:
  - components/profile/ShareButton.tsx
  - components/profile/ProfileMoreMenu.tsx
  - components/profile/FeaturedPicker.tsx
  - components/profile/ProfileForm.tsx
  - app/(artist)/settings/page.tsx
autonomous: true
requirements: [PROFILE-02, PROFILE-04, PROFILE-05, PROFILE-08]
must_haves:
  truths:
    - "Owner can edit their roles (preset multi-select + custom title, set-as-lead, remove) and open-to chips in settings; changes persist via PATCH /api/profile"
    - "Owner can toggle 'Allow others to share my music' in settings and it persists to allow_resharing"
    - "A ShareButton triggers Web Share where supported and falls back to clipboard with a 'Link copied!' label swap"
    - "A FeaturedPicker lists only the owner's PUBLIC releases and pins one via PATCH featured_project_id"
    - "The visitor more-options menu shows exactly one item (Copy profile link) and no Report/Block"
  artifacts:
    - components/profile/ShareButton.tsx
    - components/profile/ProfileMoreMenu.tsx
    - components/profile/FeaturedPicker.tsx
  key_links:
    - "ProfileForm roles/open_to/allow_resharing editors → PATCH /api/profile allowlist (Plan 01)"
    - "FeaturedPicker → PATCH featured_project_id (Plan 01 pre-check enforces owner+public)"
    - "ShareButton Web-Share-first, clipboard fallback — .share() called as the first synchronous statement (Pitfall 5)"
---

<objective>
Build the owner edit surfaces and the reusable share/menu/picker components — everything EXCEPT the ProfileView/page integration (Plan 05 mounts these): the settings roles/open-to editors + resharing toggle (PROFILE-02, PROFILE-04, D-07), the wired `ShareButton` (PROFILE-08, D-04–D-06), the visitor `ProfileMoreMenu` (PROFILE-08), and the `FeaturedPicker` (PROFILE-05).

Purpose: These are standalone, independently-testable components + settings form edits. Keeping them out of ProfileView.tsx lets this plan run in parallel with Plans 02/03 (no shared-file conflict) and leaves Plan 05 to do only the mounting.
Output: Three new client components + extended ProfileForm + settings page wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rich-member-profile/09-CONTEXT.md
@.planning/phases/09-rich-member-profile/09-RESEARCH.md
@.planning/phases/09-rich-member-profile/09-PATTERNS.md
@.planning/phases/09-rich-member-profile/09-UI-SPEC.md
@components/tools/PitchCard.tsx
@components/collaborators/CollaboratorPicker.tsx
@components/profile/ProfileForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShareButton (Web Share + clipboard fallback) and ProfileMoreMenu (visitor)</name>
  <files>components/profile/ShareButton.tsx, components/profile/ProfileMoreMenu.tsx</files>
  <read_first>
    - components/tools/PitchCard.tsx (CopyButton lines ~8-22 — clipboard + label-swap convention to reuse verbatim)
    - components/collaborators/CollaboratorPicker.tsx (click-outside dropdown lines ~45-56, trigger+panel shell ~96-120 — reuse the mousedown listener + panel classes, drop the search/grouped-list machinery)
    - .planning/phases/09-rich-member-profile/09-RESEARCH.md (Pattern 5 Web Share code block — call .share() as the FIRST synchronous statement, catch AbortError silently; Pitfall 5 — no await before .share())
    - .planning/phases/09-rich-member-profile/09-PATTERNS.md (ShareButton section — exact className to reuse from the dead stub; ProfileMoreMenu section — simplified static menu)
    - .planning/phases/09-rich-member-profile/09-UI-SPEC.md (section 5 Share behavior + captions; section 9 visitor menu — EXACTLY ONE item "Copy profile link", NO Report/Block, leave a Phase 13 insertion comment; Copywriting Contract captions)
  </read_first>
  <action>
    Create `components/profile/ShareButton.tsx` (`'use client'`). Props: `url: string`, `caption: string`, and an optional `className`/`label` so both the profile Share button and the per-track share can reuse it. Handler MUST call `navigator.share({ title: caption, url })` as the FIRST synchronous statement inside `onClick` — no leading `await` (Pitfall 5, Safari user-gesture window). Feature-detect `navigator.share`; catch `AbortError` silently (user cancelled); on any other failure or missing Web Share, fall through to `navigator.clipboard.writeText(\`${caption} → ${url}\`)` then swap the button label to "Link copied!" for 1500ms (reuse the PitchCard label-swap convention, no toast lib). Captions are passed in by the parent — do NOT build them here (parent uses the D-06 track caption or the profile caption from the Copywriting Contract). The button visual reuses the existing dead-stub className exactly (PATTERNS ShareButton section) — wire onClick only, no restyle.
    Create `components/profile/ProfileMoreMenu.tsx` (`'use client'`). Props: `profileUrl: string` (and `caption` for the profile-share caption). Use the CollaboratorPicker click-outside pattern (mousedown listener + ref). Trigger is the circular `⋯` `.pf-btn.ghost.icon` button. Panel: `card` bg, `hairstrong`/`border` 1px, 12px radius, ~200px min-width, shadow, 6px vertical padding — reuse CollaboratorPicker's panel positioning/shadow classes but DROP its search/grouped-sections/roster-fetch machinery. Contents in Phase 9: EXACTLY ONE item — "Copy profile link" — which runs the same Web-Share-then-clipboard fallback (reuse ShareButton's mechanism or a shared helper). Leave a code comment marking the Phase 13 (SAFETY-01/02 Report/Block) insertion point. Do NOT add Report/Block items or any visually-present-but-non-functional stub (UI-SPEC section 9 + CLAUDE.md "no silent no-ops").
  </action>
  <verify>
    <automated>test -f components/profile/ShareButton.tsx && test -f components/profile/ProfileMoreMenu.tsx && grep -c "Phase 13" components/profile/ProfileMoreMenu.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both component files exist and are `'use client'`.
    - ShareButton calls `navigator.share` before any `await` in the click handler (grep shows `navigator.share` and the handler is not `async`-gated by a prior await on the caption/url — they are props).
    - The `url` passed to `navigator.share()`/clipboard is absolute, not relative: ShareButton does NOT construct or prepend an origin itself — it consumes the already-absolute `url` prop verbatim (Plan 05 resolves `${NEXT_PUBLIC_APP_URL}/u/${handle}` server-side and passes it in). Assert the component neither strips nor rebuilds the prefix; the shared/copied string carries the caller-supplied absolute `url` (matching `/^https?:\/\//`). No `window.location.origin` prepend inside ShareButton (Plan 05 already resolved the absolute URL) — `grep -c "window.location.origin" components/profile/ShareButton.tsx` returns 0.
    - Clipboard fallback + "Link copied!" label present: `grep -c "Link copied" components/profile/ShareButton.tsx` >=1.
    - ProfileMoreMenu ships exactly one rendered menu item ("Copy profile link") — read the component and confirm there is a single interactive item in the panel and no second/third functional action. The deferred moderation actions may be referenced only inside the Phase 13 insertion-point comment; do NOT gate this with a whole-file negative grep on those words (the insertion-point comment legitimately names them and would self-invalidate such a gate).
    - Phase 13 insertion-point comment exists: `grep -c "Phase 13" components/profile/ProfileMoreMenu.tsx` >=1.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Reusable ShareButton (Web-Share-first, clipboard fallback) and a single-item visitor more-options menu, both ready for Plan 05 to mount.</done>
</task>

<task type="auto">
  <name>Task 2: FeaturedPicker (owner-only, public releases only)</name>
  <files>components/profile/FeaturedPicker.tsx</files>
  <read_first>
    - components/collaborators/CollaboratorPicker.tsx (dropdown/list shell + click-outside — reuse pattern)
    - components/profile/FollowButton.tsx (optimistic fetch → PATCH pattern; router.refresh on success)
    - .planning/phases/09-rich-member-profile/09-UI-SPEC.md (section 4c Featured picker — ghost button "Change featured release"/"Pin a release", list filtered to PUBLIC releases only, 40px cover thumb + title + type/year row; Copywriting Contract empty state + error)
    - .planning/phases/09-rich-member-profile/09-RESEARCH.md (Pitfall 4 — never show private drafts in the picker; API pre-check returns the friendly error but the list should already be pre-filtered client-side)
    - .planning/phases/09-rich-member-profile/09-CONTEXT.md (D-05 PROFILE-05 pin one release as Featured)
  </read_first>
  <action>Create `components/profile/FeaturedPicker.tsx` (`'use client'`). Props: `releases: {id,title,typeLabel,year,coverUrl,isPublic}[]` (the owner's releases, passed from Plan 05's page data), `currentFeaturedId: string | null`. Render an owner-only ghost button (`.pf-btn.ghost` visual — card bg, hairstrong border, 15px/700 white, 11px radius) labeled "Change featured release" (or "Pin a release" when none set). Opens a click-outside dropdown listing ONLY releases where `isPublic === true` (never private drafts — Pitfall 4). Each row: 40px cover thumb + title + type/year. Selecting a row PATCHes `/api/profile` with `{ featured_project_id: id }` (or `null` to unpin), then `router.refresh()`. On a non-OK response, surface the API's friendly error text ("Only public releases can be featured — publish it first.") inline in warn tone (never rose). Empty state (owner has no public releases): show the copywriting-contract line "Publish a release to pin it here as your Featured spotlight." instead of an empty list. Reuse the click-outside mousedown pattern.</action>
  <verify>
    <automated>test -f components/profile/FeaturedPicker.tsx && grep -c "featured_project_id" components/profile/FeaturedPicker.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Component exists, `'use client'`, takes `releases` + `currentFeaturedId` props.
    - PATCHes featured_project_id: `grep -c "featured_project_id" components/profile/FeaturedPicker.tsx` >=1 and target is `/api/profile`.
    - Filters to public only: `grep -Ec "isPublic|is_public" components/profile/FeaturedPicker.tsx` >=1 with a filter that excludes non-public rows from the rendered list.
    - Empty-state and error copy match the Copywriting Contract verbatim.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>An owner-only picker that lists only public releases, pins via the validated PATCH, and shows the friendly empty/error copy — private drafts never appear.</done>
</task>

<task type="auto">
  <name>Task 3: Settings — roles editor, open-to editor, resharing toggle</name>
  <files>components/profile/ProfileForm.tsx, app/(artist)/settings/page.tsx</files>
  <read_first>
    - components/profile/ProfileForm.tsx (full — existing settings form structure, how it PATCHes /api/profile, its field-state convention)
    - app/(artist)/settings/page.tsx (full — how ProfileForm is mounted, what profile props are passed line ~115)
    - .planning/phases/09-rich-member-profile/09-UI-SPEC.md (section 4a roles editor — .pf-rolechip visual, preset multi-select of 6, +Add role dashed chip → inline custom input max 40 chars, "Set as lead" moves to index 0, × remove, min 1; section 4b open-to editor — .pf-openchip emerald toggle over Sync/Co-writes/Features/Brand deals; Copywriting Contract resharing toggle label + helper text)
    - types/index.ts (PROFILE_ROLES line ~239, PROFILE_ROLE_LABELS, ProfileRole union line ~259, OpenTo union line ~264)
    - .planning/phases/09-rich-member-profile/09-RESEARCH.md (Anti-pattern: lead role = array index 0, NOT a new schema field; Pattern 1 optimistic toggle)
  </read_first>
  <action>
    Extend `components/profile/ProfileForm.tsx` with three new editors, following the form's existing state/PATCH convention (PATCH /api/profile with the changed fields — the allowlist from Plan 01 accepts `roles`, `open_to`, `allow_resharing`):
    (a) Roles editor — `.pf-rolechip` visual (10px radius, card2 bg, hairstrong border, 14px/600, indigo icon). Preset multi-select over PROFILE_ROLES (labels via PROFILE_ROLE_LABELS), max 6 roles total. A dashed "+ Add role" chip (`.pf-rolechip.add`) opens an inline text input (placeholder "e.g. Mixing engineer", max 40 chars) that appends a `{kind:'custom',label}` entry. Lead-role control: NO drag-drop — each NON-lead chip shows an 11px "Set as lead" action that moves that entry to array index 0; the index-0 chip renders with `.pf-role.lead` gradient tint and no action. Each non-lead chip has a "×" remove; block removing the last role (min 1). Persist the `roles: ProfileRole[]` array (order = lead-first) via PATCH.
    (b) Open-to editor — `.pf-openchip` emerald toggle multi-select over the four labels Sync licensing / Co-writes / Features / Brand deals mapping to the correct `OpenTo` slugs (`sync`,`collabs`,`features`, plus the appropriate slug for brand deals — use the existing OpenTo union; if "brand deals" has no slug, use the closest existing slug and note it). Selected = filled emerald pill; unselected = card2 outline. Persist `open_to: OpenTo[]`.
    (c) Resharing toggle — a plain on/off switch labeled "Allow others to share my music" with helper text "When on, visitors can share your public tracks and profile link. Turned off, only you can share." No confirmation dialog (fully reversible). Persist `allow_resharing: boolean`. Initialize from the profile's current value.
    In `app/(artist)/settings/page.tsx`: ensure the `roles`, `open_to`, and `allow_resharing` values are read from the profile and passed into ProfileForm so the editors initialize with current state (extend the profile SELECT/props if those fields are not already passed).
  </action>
  <verify>
    <automated>grep -Ec "roles|open_to|allow_resharing" components/profile/ProfileForm.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ProfileForm contains roles, open_to, and allow_resharing editors: `grep -c "allow_resharing" components/profile/ProfileForm.tsx` >=1, `grep -c "open_to" components/profile/ProfileForm.tsx` >=1, `grep -c "roles" components/profile/ProfileForm.tsx` >=1.
    - Lead role uses array index 0, not a new field: no `is_lead`/`lead_role` identifier introduced (`grep -Ec "is_lead|lead_role" components/profile/ProfileForm.tsx` returns 0).
    - Resharing toggle label + helper text match the Copywriting Contract verbatim.
    - Roles capped at 6 and custom label capped at 40 chars client-side (matches the server Zod cap from Plan 01).
    - settings page passes roles/open_to/allow_resharing into ProfileForm.
    - `npx tsc --noEmit` clean and `npm run build` succeeds.
  </acceptance_criteria>
  <done>Settings gains roles/open-to/resharing editors that persist through the validated PATCH allowlist; lead role stays an array-order convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings form → PATCH /api/profile | Owner-authored roles/open_to/allow_resharing cross into the validated allowlist (Plan 01) |
| FeaturedPicker → PATCH featured_project_id | Selection crosses into the owner+public pre-check + DB trigger |
| ShareButton/MoreMenu → browser Web Share/clipboard | Entirely client-side; the shared URL is already-public info |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-12 | Tampering | Roles/open_to editor submitting malformed values | high | mitigate | Client caps (6 roles, 40-char label) mirror the server-side Zod validation (Plan 01 T-09-02); the API is the authoritative gate, the client caps are UX — the API rejects anything the client misses |
| T-09-13 | Information Disclosure | FeaturedPicker exposing a private draft as pinnable | high | mitigate | List pre-filtered client-side to `isPublic` rows (Pitfall 4); API pre-check + migration 034 trigger are the authoritative backstops if a crafted request bypasses the UI |
| T-09-14 | Spoofing | Visitor more-options menu presenting non-functional Report/Block that appear to work | medium | mitigate | Menu ships exactly one functional item (Copy profile link); Report/Block are deferred to Phase 13 with an insertion-point comment — no silent no-op stubs (UI-SPEC section 9, CLAUDE.md convention) |
| T-09-15 | Tampering | allow_resharing toggled by a non-owner | low | mitigate | Toggle only lives in owner-authenticated settings; PATCH writes on `.eq('id', user.id)` from `auth.getUser()` — a visitor has no path to another user's row |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Grep gates: ShareButton Web-Share-first + clipboard fallback; MoreMenu one item no Report/Block; FeaturedPicker public-only + featured_project_id PATCH; ProfileForm roles/open_to/allow_resharing editors, no is_lead field.
- Manual (per 09-VALIDATION.md): as owner, edit roles (add custom, set-as-lead, remove), toggle open-to chips, flip the resharing toggle — confirm all persist on reload; pin a public release as Featured and confirm a private draft cannot be selected.
</verification>

<success_criteria>
- Owner can edit roles (preset + custom, set-as-lead, remove, min 1, max 6), open-to chips, and the resharing toggle in settings; all persist.
- ShareButton uses Web Share where supported and clipboard fallback with "Link copied!" otherwise, calling .share() synchronously.
- FeaturedPicker lists only public releases and pins via the validated PATCH.
- Visitor more-options menu ships exactly one item; no Report/Block stub.
</success_criteria>

## Artifacts this phase produces

New symbols/files created by this plan (exclude from downstream drift "unexplained new symbol" flags):

- Component: `ShareButton` at `components/profile/ShareButton.tsx` (props `url`, `caption`, optional `className`/`label`; Web Share + clipboard fallback)
- Component: `ProfileMoreMenu` at `components/profile/ProfileMoreMenu.tsx` (props `profileUrl`, `caption`; single "Copy profile link" item; Phase 13 insertion-point comment)
- Component: `FeaturedPicker` at `components/profile/FeaturedPicker.tsx` (props `releases`, `currentFeaturedId`; PATCHes `featured_project_id`)
- Modified: `components/profile/ProfileForm.tsx` gains roles editor, open-to editor, and the "Allow others to share my music" resharing toggle (persist `roles`, `open_to`, `allow_resharing`)
- Modified: `app/(artist)/settings/page.tsx` passes `roles`/`open_to`/`allow_resharing` into ProfileForm

<output>
Create `.planning/phases/09-rich-member-profile/09-04-SUMMARY.md` when done.
</output>
