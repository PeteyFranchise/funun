---
phase: 07-social-campaign-planner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/033_social_campaigns.sql
  - lib/launchpad/campaigns.ts
  - lib/launchpad/platform-nudges.ts
autonomous: false
requirements: [SOCIAL-01, SOCIAL-02, SOCIAL-04, SOCIAL-06]
user_setup:
  - service: supabase
    why: "Migration 033 must be applied to the live Supabase DB; the executor sandbox has no Supabase CLI credentials (established Phase 6 convention — migration 030 was applied by the user directly)"
    dashboard_config:
      - task: "Apply supabase/migrations/033_social_campaigns.sql via `supabase db push` (own authenticated CLI) or the Dashboard SQL Editor"
        location: "Supabase Dashboard -> SQL Editor, or local `supabase db push`"

must_haves:
  truths:
    - "social_campaigns table exists with columns: id, project_id, user_id, name, platforms TEXT[], is_active, posts JSONB, created_at, updated_at"
    - "RLS is enabled on social_campaigns and a single policy USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id) gates all access (SOCIAL-01)"
    - "A partial unique index (WHERE is_active) enforces at most one active campaign per project at the DB layer (D-04)"
    - "readPosts() returns only slot objects whose platform ∈ the 6 supported values, content_type ∈ the 5 supported values, and week ∈ {1,2,3,4}; invalid rows are dropped (SOCIAL-04)"
    - "sanitizeSlotEdit() returns only allowlisted fields (caption, posting_time, completed) and never returns a full posts array (SOCIAL-06)"
    - "getPlatformNudges() resolves a genre (slug or free text) to a ranked platform list via defensive lowercase/trim + alias fallback, and returns an empty list (not an error) on no match (SOCIAL-02, D-08 hardcoded TS map)"
  artifacts:
    - "supabase/migrations/033_social_campaigns.sql"
    - "lib/launchpad/campaigns.ts (Platform/ContentType/SocialPost/SocialCampaign types + PLATFORM_VALUES/CONTENT_TYPE_VALUES/*_LABELS + readPosts + sanitizeSlotEdit)"
    - "lib/launchpad/platform-nudges.ts (GENRE_PLATFORM_NUDGES map + getPlatformNudges resolver)"
  key_links:
    - "readPosts()/sanitizeSlotEdit() are the ONLY read/write path for social_campaigns.posts — every downstream route (07-03, 07-04) imports them, never touches raw JSONB"
    - "platform-nudges.ts genre resolver is imported by PlatformSelector (07-05) for the advisory badges"
  prohibitions:
    - "social_campaigns.posts must never be written from a raw client-supplied array — only via sanitizeSlotEdit() allowlist merge"
    - "no UNIQUE constraint on project_id alone (multiple campaigns per project are allowed, D-04) — the uniqueness is partial, WHERE is_active only"
---

<objective>
Build the data foundation for the social campaign planner: the `social_campaigns` table (migration 033) plus the two pure TypeScript helper modules every downstream route and component depends on — `lib/launchpad/campaigns.ts` (typed JSONB read/sanitize, mirroring `lib/metadata/schema.ts`) and `lib/launchpad/platform-nudges.ts` (genre→platform advisory lookup, mirroring `lib/benchmarks/engine.ts`'s `GENRE_FACTORS`).

Purpose: Establish the storage schema and the "never trust raw JSONB" read/sanitize convention up front so waves 2–5 build on a stable, RLS-secured, type-safe foundation. This plan is the root of the dependency graph — nothing else can start until the types and table shape exist.
Output: One migration file (applied by the user), two new lib files with the exported types/values/functions listed in `must_haves.artifacts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-social-campaign-planner/07-CONTEXT.md
@.planning/phases/07-social-campaign-planner/07-RESEARCH.md
@.planning/phases/07-social-campaign-planner/07-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (07-01 subset)

New symbols introduced by this plan (exclude from source-drift verification — they do not exist before this plan):
- **Table:** `social_campaigns` (columns: `id`, `project_id`, `user_id`, `name`, `platforms`, `is_active`, `posts`, `created_at`, `updated_at`)
- **Index:** `idx_social_campaigns_one_active_per_project`, `idx_social_campaigns_project`
- **Trigger:** `set_social_campaigns_updated_at`
- **File:** `lib/launchpad/campaigns.ts`
- **Types:** `Platform`, `ContentType`, `SocialPost`, `SocialCampaign`
- **Consts:** `PLATFORM_VALUES`, `PLATFORM_LABELS`, `CONTENT_TYPE_VALUES`, `CONTENT_TYPE_LABELS`
- **Functions:** `readPosts(posts: unknown): SocialPost[]`, `sanitizeSlotEdit(input: unknown): Partial<Pick<SocialPost, 'caption' | 'posting_time' | 'completed'>>`
- **File:** `lib/launchpad/platform-nudges.ts`
- **Types:** `PlatformNudge`
- **Consts:** `GENRE_PLATFORM_NUDGES`, `GENRE_ALIASES`
- **Functions:** `getPlatformNudges(genreSlugsOrFreeText): Platform[]`
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Author migration 033 and confirm DB application</name>
  <files>supabase/migrations/033_social_campaigns.sql</files>
  <what-built>
    supabase/migrations/033_social_campaigns.sql creating the social_campaigns table with RLS, the one-active-per-project partial unique index, a project index, and an updated_at trigger — following supabase/migrations/030_curators_pitch_history.sql's denormalized-owner RLS pattern and 032_curators_claim_token_unique.sql's partial-unique-index pattern verbatim.
  </what-built>
  <read_first>
    - supabase/migrations/030_curators_pitch_history.sql (denormalized user_id/artist_id RLS pattern + update_updated_at trigger usage — the exact structural analog)
    - supabase/migrations/032_curators_claim_token_unique.sql (partial unique index pattern — CREATE UNIQUE INDEX ... WHERE)
    - supabase/migrations/001_initial_schema.sql (confirm update_updated_at() function exists + uuid_generate_v4() availability + vault_projects/cover_art_url reference target)
    - .planning/phases/07-social-campaign-planner/07-RESEARCH.md (Pattern 3 SQL, Pitfall 5, JSONB Schema Design section)
  </read_first>
  <action>
    Create `supabase/migrations/033_social_campaigns.sql`. Define `CREATE TABLE social_campaigns` with columns: `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`, `project_id UUID NOT NULL REFERENCES vault_projects(id) ON DELETE CASCADE`, `user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`, `name TEXT NOT NULL DEFAULT 'Campaign'`, `platforms TEXT[] NOT NULL DEFAULT '{}'`, `is_active BOOLEAN NOT NULL DEFAULT true`, `posts JSONB NOT NULL DEFAULT '[]'::jsonb`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`. Immediately after CREATE TABLE, add `ALTER TABLE social_campaigns ENABLE ROW LEVEL SECURITY` (CVE-2025-48757 project convention). Add ONE policy named "Users manage own campaigns" `FOR ALL USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id)`. Add `CREATE UNIQUE INDEX idx_social_campaigns_one_active_per_project ON social_campaigns (project_id) WHERE is_active` (D-04 DB-level backstop, per Pitfall 5 — written into 033 from day one, NOT retrofitted later). Add `CREATE INDEX idx_social_campaigns_project ON social_campaigns (project_id)`. Add `CREATE TRIGGER set_social_campaigns_updated_at BEFORE UPDATE ON social_campaigns FOR EACH ROW EXECUTE FUNCTION update_updated_at()` reusing the existing function from migration 001. Do NOT add any UNIQUE constraint on `project_id` alone — multiple campaigns per project are intentional (D-04). Include a top-of-file comment header matching the sibling migrations' `-- ─── ... ───` style describing the table's purpose. After writing the file, this is a blocking checkpoint: the user applies the migration to the live Supabase DB (the executor sandbox has no Supabase CLI credentials — established Phase 6 convention, STATE.md decisions log for migration 030).
  </action>
  <how-to-verify>
    1. Confirm supabase/migrations/033_social_campaigns.sql exists and contains `ENABLE ROW LEVEL SECURITY`, the `WHERE is_active` partial unique index, and `EXECUTE FUNCTION update_updated_at()`.
    2. Apply the migration via your authenticated `supabase db push` or the Dashboard SQL Editor.
    3. In the Supabase Dashboard, confirm the `social_campaigns` table appears with all 9 columns, RLS enabled (shield icon), and both indexes present under the table's Indexes tab.
  </how-to-verify>
  <verify>
    <automated>grep -q "ENABLE ROW LEVEL SECURITY" supabase/migrations/033_social_campaigns.sql && grep -q "WHERE is_active" supabase/migrations/033_social_campaigns.sql && grep -q "EXECUTE FUNCTION update_updated_at" supabase/migrations/033_social_campaigns.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/033_social_campaigns.sql` exists.
    - Contains literal `ALTER TABLE social_campaigns ENABLE ROW LEVEL SECURITY`.
    - Contains a partial unique index with the exact predicate `WHERE is_active` on `(project_id)`.
    - Contains `CREATE TRIGGER set_social_campaigns_updated_at ... EXECUTE FUNCTION update_updated_at()`.
    - Contains NO `UNIQUE` constraint or index on `project_id` without a `WHERE is_active` predicate.
    - The `posts` column is `JSONB NOT NULL DEFAULT '[]'::jsonb`.
    - User has confirmed the migration is applied to the live DB (checkpoint resume).
  </acceptance_criteria>
  <resume-signal>Type "applied" once the migration is live in Supabase, or describe any error.</resume-signal>
  <done>Migration 033 file exists with RLS enabled, the WHERE is_active partial unique index, and the updated_at trigger; user has confirmed it is applied to the live Supabase DB.</done>
</task>

<task type="auto">
  <name>Task 2: Create lib/launchpad/campaigns.ts (types + readPosts + sanitizeSlotEdit)</name>
  <files>lib/launchpad/campaigns.ts</files>
  <read_first>
    - lib/metadata/schema.ts (readComposers/sanitizeComposers — lines ~187-234 — the exact read/sanitize pattern to mirror: *_VALUES enum validation, Number.isFinite guards, String(...).trim(), .filter() to drop invalid rows)
    - .planning/phases/07-social-campaign-planner/07-RESEARCH.md (JSONB Schema Design section — the full SocialPost/SocialCampaign type + readPosts/sanitizeSlotEdit reference implementation)
    - .planning/phases/07-social-campaign-planner/07-PATTERNS.md ("lib/launchpad/campaigns.ts" section)
  </read_first>
  <action>
    Create `lib/launchpad/campaigns.ts`. Export type `Platform = 'instagram' | 'tiktok' | 'x' | 'youtube_shorts' | 'facebook' | 'threads'` plus `PLATFORM_VALUES` (array of all 6 in that order) and `PLATFORM_LABELS` (Record mapping each to its display label: Instagram, TikTok, X, YouTube Shorts, Facebook, Threads). Export type `ContentType = 'short_form_video' | 'static_image' | 'lyric_graphic' | 'text' | 'stories'` plus `CONTENT_TYPE_VALUES` and `CONTENT_TYPE_LABELS` (Short-form video, Static image, Lyric graphic, Text, Stories). Export type `SocialPost` with fields: `id: string`, `platform: Platform`, `week: 1 | 2 | 3 | 4`, `content_type: ContentType`, `caption: string`, `posting_time: string` (ISO 8601), `completed: boolean`, `completed_at: string | null`, `source: 'ai' | 'manual'`. Export type `SocialCampaign` with fields: `id`, `project_id`, `user_id`, `name`, `platforms: Platform[]`, `is_active: boolean`, `posts: SocialPost[]`, `created_at`, `updated_at` (all string except `platforms`/`posts`/`is_active`). Export `readPosts(posts: unknown): SocialPost[]` mirroring `readComposers`: return `[]` if not an array; for each row validate `platform` against `PLATFORM_VALUES`, `content_type` against `CONTENT_TYPE_VALUES`, `week` against `[1,2,3,4]` (via `Number(o.week)`), drop the row (return null) if any of those three is invalid, coerce `caption`/`posting_time`/`completed_at` defensively (`String(...)`, fall back `posting_time` to `new Date().toISOString()` if not a string, `completed` is `o.completed === true`, `completed_at` is the string or null, `source` is `'manual'` only if exactly that else `'ai'`), then `.filter()` out nulls and rows with empty `id`. Export `sanitizeSlotEdit(input: unknown): Partial<Pick<SocialPost, 'caption' | 'posting_time' | 'completed'>>` mirroring `sanitizeComposers`'s allowlist posture: build an empty out object, copy `caption` only if `typeof === 'string'` (slice to 2200 chars — Instagram's cap), copy `posting_time` only if a string parseable by `Date.parse` (not NaN), copy `completed` only if `typeof === 'boolean'`; return only those allowlisted keys — NEVER a full posts array. Use named exports only, 2-space indent, no semicolons (project style per CLAUDE.md).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "campaigns.ts" || echo "OK — campaigns.ts type-checks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/launchpad/campaigns.ts` exports `Platform`, `ContentType`, `SocialPost`, `SocialCampaign`, `PLATFORM_VALUES`, `PLATFORM_LABELS`, `CONTENT_TYPE_VALUES`, `CONTENT_TYPE_LABELS`, `readPosts`, `sanitizeSlotEdit`.
    - `PLATFORM_VALUES` has exactly 6 entries; `CONTENT_TYPE_VALUES` has exactly 5.
    - `readPosts` returns `[]` for a non-array input and drops any row whose `platform`/`content_type`/`week` fails enum/range validation (behavior assertion — a row with `platform: 'linkedin'` or `week: 5` is not returned).
    - `sanitizeSlotEdit` returns an object containing at most the keys `caption`, `posting_time`, `completed` — never any other key, never a `posts` array (behavior assertion — passing `{ posts: [...], is_active: false }` returns `{}`).
    - `npx tsc --noEmit` reports no errors originating in `campaigns.ts`.
  </acceptance_criteria>
  <done>campaigns.ts compiles clean and provides the typed read/sanitize helpers that are the sole JSONB access path for social_campaigns.posts.</done>
</task>

<task type="auto">
  <name>Task 3: Create lib/launchpad/platform-nudges.ts (genre→platform advisory map + resolver)</name>
  <files>lib/launchpad/platform-nudges.ts</files>
  <read_first>
    - lib/benchmarks/engine.ts (GENRE_FACTORS lookup, lines ~81-139 — the `Record<string, ...>` map + `[(genre ?? '').trim().toLowerCase()] ?? {}` defensive-lookup + graceful `?? {}` fallback pattern to mirror)
    - lib/genres.ts (the canonical 20-slug GENRES array — the exact key set for the nudge map)
    - .planning/phases/07-social-campaign-planner/07-RESEARCH.md ("Genre → Platform Nudge Table Content" — the full recommended genre→ranked-platform map, the free-text-vs-slug pitfall, the fallback rule)
    - lib/launchpad/campaigns.ts (import the Platform type — this file depends on Task 2)
  </read_first>
  <action>
    Create `lib/launchpad/platform-nudges.ts` — the D-08 hardcoded genre→platform TS map (no DB table, no admin UI). Import `Platform` from `@/lib/launchpad/campaigns`. Export `type PlatformNudge = { platforms: Platform[]; rationale: string }`. Export `GENRE_PLATFORM_NUDGES: Record<string, PlatformNudge>` keyed by the 20 genre slugs from `lib/genres.ts` (`pop`, `hip_hop_rap`, `rnb_soul`, `rock`, `electronic_dance`, `country`, `latin`, `jazz`, `classical`, `folk_americana`, `reggae`, `gospel_christian`, `metal`, `alternative`, `indie`, `blues`, `funk`, `afrobeats`, `k_pop`, `world_global`), with the exact ranked platform lists + short rationale text from RESEARCH.md's "Genre → Platform Nudge Table Content" table (copy the platform rankings verbatim — e.g. `pop` → `['tiktok', 'instagram', 'youtube_shorts']`, `country` → `['facebook', 'instagram', 'tiktok']`, `k_pop` → `['tiktok', 'instagram', 'x']`; correct the one obvious typo in the source table where hip_hop_rap's first entry reads "TikTik" — it is `tiktok`). Export `GENRE_ALIASES: Record<string, string>` mapping common free-text forms to slugs (`'r&b'` → `'rnb_soul'`, `'rnb'` → `'rnb_soul'`, `'hip-hop'` → `'hip_hop_rap'`, `'hip hop'` → `'hip_hop_rap'`, `'rap'` → `'hip_hop_rap'`, `'edm'` → `'electronic_dance'`, `'electronic'` → `'electronic_dance'`, `'dance'` → `'electronic_dance'`, `'kpop'` → `'k_pop'`, `'k-pop'` → `'k_pop'`, plus a few more sensible ones). Export `getPlatformNudges(profileGenres: string[] | null | undefined, projectGenreFreeText: string | null | undefined): Platform[]`. Resolution order per RESEARCH.md Pitfall: (1) if `profileGenres` is a non-empty array, use its first slug that has a `GENRE_PLATFORM_NUDGES` entry; (2) else normalize `projectGenreFreeText` via `.trim().toLowerCase()`, look it up directly in `GENRE_PLATFORM_NUDGES`, then via `GENRE_ALIASES`; (3) if nothing resolves, return `[]` (NOT an error, NOT the world_global default — return empty so the UI shows no badges, per D-09 "degrade gracefully, never a broken UI"). Return the resolved entry's `platforms` array. Also export `getPlatformNudgeRationale(...)` with the same resolution returning the `rationale` string or `''`, so PlatformSelector can show the "Best fit for {Genre}" badge text. Named exports only, 2-space indent, no semicolons.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "platform-nudges.ts" || echo "OK — platform-nudges.ts type-checks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/launchpad/platform-nudges.ts` exports `PlatformNudge`, `GENRE_PLATFORM_NUDGES`, `GENRE_ALIASES`, `getPlatformNudges`, `getPlatformNudgeRationale`.
    - `GENRE_PLATFORM_NUDGES` has exactly 20 keys matching the `lib/genres.ts` slug set.
    - Every value's `platforms` array contains only members of `PLATFORM_VALUES` (no typo'd `tiktik`).
    - `getPlatformNudges(['hip_hop_rap'], null)` returns a non-empty ranked list (behavior assertion).
    - `getPlatformNudges(null, 'R&B')` resolves via alias to `rnb_soul`'s list (behavior assertion — free-text + alias path).
    - `getPlatformNudges(null, 'zzz-unknown-genre')` returns `[]` (behavior assertion — graceful no-match, not an error).
    - `npx tsc --noEmit` reports no errors originating in `platform-nudges.ts`.
  </acceptance_criteria>
  <done>platform-nudges.ts resolves a genre to a ranked platform list defensively and degrades to an empty list on no match, ready for PlatformSelector's advisory badges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → social_campaigns row | An authenticated artist's requests reach the table; RLS + a denormalized user_id column are the only owner gate |
| Postgres concurrency → is_active invariant | Two concurrent "set active" requests could both flip is_active on without a DB backstop |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | social_campaigns RLS | high | mitigate | RLS policy `USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id)` — denormalized owner column, mirrors pitch_history (migration 030); no cross-user read/write possible even if a route forgets its own `.eq('user_id', ...)` |
| T-07-02 | Tampering (data integrity) | is_active partial unique index | high | mitigate | Partial unique index `WHERE is_active` written into migration 033 from day one (Pitfall 5) — DB rejects a second active campaign for the same project even under concurrent requests |
| T-07-03 | Tampering | readPosts() enum validation | medium | mitigate | readPosts() validates platform/content_type/week against *_VALUES on EVERY read (including AI-generated output consumed by later plans), dropping any hallucinated/out-of-range slot before it reaches the UI or CSV |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages introduced this phase (RESEARCH.md Package Legitimacy Audit — zero installs); nothing to verify |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no errors in `lib/launchpad/campaigns.ts` or `lib/launchpad/platform-nudges.ts`.
- Migration file present and contains the RLS enable, the partial unique index, and the updated_at trigger.
- User has confirmed migration 033 is applied to the live Supabase DB.
- `readPosts` and `sanitizeSlotEdit` behave as the JSONB safety layer (drop invalid rows; allowlist-only writes).
</verification>

<success_criteria>
- social_campaigns table live with RLS, one-active-per-project partial unique index, updated_at trigger (SOCIAL-01 storage foundation, D-04 invariant backstop).
- campaigns.ts provides the typed read/sanitize helpers (SOCIAL-04, SOCIAL-06 data layer).
- platform-nudges.ts resolves genre→platform advisories with graceful no-match (SOCIAL-02 data layer).
</success_criteria>

<output>
Create `.planning/phases/07-social-campaign-planner/07-01-SUMMARY.md` when done.
</output>
