---
phase: 07-social-campaign-planner
plan: 06
type: execute
wave: 5
depends_on: [07-05]
files_modified:
  - components/launchpad/SlotGeneratePanel.tsx
  - components/launchpad/SaveToCalendarPicker.tsx
  - components/launchpad/CampaignHistoryList.tsx
  - app/(artist)/launchpad/[projectId]/page.tsx
autonomous: true
requirements: [SOCIAL-03, SOCIAL-04, SOCIAL-05, SOCIAL-06]

must_haves:
  truths:
    - "SlotGeneratePanel shows the current caption and an AI suggestion side-by-side and only writes to the slot on an explicit 'Use this' click — never silently overwrites (D-10, SOCIAL-05)"
    - "SaveToCalendarPicker lets a standalone DropReady/SoundBait output be saved into a chosen platform/week/slot via the same preview→accept semantics, and never touches tool_outputs history (D-11, SOCIAL-05)"
    - "CampaignHistoryList lists campaigns, shows an 'Active' badge on the active one, offers 'Set active' on inactive ones, and hard-deletes inactive campaigns through an inline confirm block — the active campaign has no Delete trigger (D-04, D-05)"
    - "The Launchpad project page fetches the active social_campaigns row server-side (RLS-scoped) in its existing Promise.all and renders the CampaignCalendar section as a third stacked block below the pitch composer (SOCIAL-03)"
    - "SlotGeneratePanel supports Escape + backdrop-click dismissal; SaveToCalendarPicker supports backdrop-click + Escape dismissal (UI-SPEC Interaction Contracts)"
  artifacts:
    - "components/launchpad/SlotGeneratePanel.tsx"
    - "components/launchpad/SaveToCalendarPicker.tsx"
    - "components/launchpad/CampaignHistoryList.tsx"
    - "app/(artist)/launchpad/[projectId]/page.tsx (modified — adds the campaign section + server fetch)"
  key_links:
    - "SlotGeneratePanel POSTs to 07-04's slots/[slotId]/generate route for the preview, then writes via 07-03's slot PATCH on 'Use this'"
    - "the page's server fetch of the active campaign feeds CampaignCalendar's initialCampaign prop (07-05); CampaignHistoryList switches which campaign is active via 07-03's PATCH"
  prohibitions:
    - "SlotGeneratePanel and SaveToCalendarPicker must show the AI output before any write — the write is a distinct 'Use this'/'Save to slot' action, never an automatic side effect of generation completing (D-10/D-11)"
    - "the active campaign row must never be deletable — do not render the Delete trigger on the active row (D-05)"
    - "hard-delete must route through the inline confirm block — never a bare click-to-delete, never window.confirm (ChecklistAdmin precedent)"
---

<objective>
Complete the feature: the two preview/save surfaces (`SlotGeneratePanel.tsx` reusing `TipPanel.tsx`'s slide-in shell for D-10; `SaveToCalendarPicker.tsx` as a lighter centered modal for D-11), the `CampaignHistoryList.tsx` switch-active/hard-delete list (D-04/D-05, reusing `ChecklistAdmin.tsx`'s inline-confirm), and the page wiring that fetches the active campaign server-side and stacks the `CampaignCalendar` section into `/launchpad/[projectId]` exactly as Phase 6 stacked its pitch block.

Purpose: This wave closes the preview-then-accept loop (the D-10/D-11 "never silently overwrite" requirement), gives the artist campaign history management, and mounts the whole feature into the existing Launchpad room. After this plan, all of SOCIAL-01..07 are reachable end-to-end from the project page.
Output: three new components + one modified server page.

**Post-launch admin data task (D-14 — NOT executable code in this phase, surfaced per scope discipline):** After Phase 7 ships, social-adjacent Phase 5 checklist items (e.g. "social teasers") should have their `action_href` deep-linked to the campaign calendar section of `/launchpad/[projectId]`. CONTEXT.md D-14 is explicit that this is an **admin data edit through the existing Phase 5 admin checklist CRUD (`/admin/checklist`)** — no schema change, no new code path, no component. It is intentionally excluded from this plan's tasks; note it in the phase SUMMARY as a follow-up admin action for the project owner to perform once the calendar section is live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-social-campaign-planner/07-CONTEXT.md
@.planning/phases/07-social-campaign-planner/07-RESEARCH.md
@.planning/phases/07-social-campaign-planner/07-PATTERNS.md
@.planning/phases/07-social-campaign-planner/07-UI-SPEC.md
@.planning/phases/07-social-campaign-planner/07-03-SUMMARY.md
@.planning/phases/07-social-campaign-planner/07-04-SUMMARY.md
@.planning/phases/07-social-campaign-planner/07-05-SUMMARY.md
@components/launchpad/TipPanel.tsx
@components/admin/ChecklistAdmin.tsx
@app/(artist)/launchpad/[projectId]/page.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (07-06 subset)

New symbols introduced by this plan (exclude from source-drift verification):
- **File:** `components/launchpad/SlotGeneratePanel.tsx` — export `SlotGeneratePanel`
- **File:** `components/launchpad/SaveToCalendarPicker.tsx` — export `SaveToCalendarPicker`
- **File:** `components/launchpad/CampaignHistoryList.tsx` — export `CampaignHistoryList`
- **Modified:** `app/(artist)/launchpad/[projectId]/page.tsx` — adds active-campaign server fetch + CampaignCalendar/CampaignHistoryList render block
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: SlotGeneratePanel.tsx (D-10 preview-then-accept) + SaveToCalendarPicker.tsx (D-11)</name>
  <files>components/launchpad/SlotGeneratePanel.tsx, components/launchpad/SaveToCalendarPicker.tsx</files>
  <read_first>
    - components/launchpad/TipPanel.tsx (the FULL slide-in shell to reuse verbatim for SlotGeneratePanel: `fixed inset-0 z-50 flex justify-end`, `bg-black/60 backdrop-blur-sm` backdrop click-closes, `aside` `w-full max-w-md border-l border-white/10 bg-[#0a0a0f]`, the Escape-key useEffect, the `border-t border-white/10 p-5` footer)
    - components/curators/PitchComposer.tsx (select-field styling `w-full rounded-lg border border-white/15 bg-white/[0.03] px-3 py-2 text-sm text-white` + label style for SaveToCalendarPicker's three selects; secondary/primary button styles)
    - lib/launchpad/campaigns.ts (SocialPost, Platform, PLATFORM_LABELS, PLATFORM_VALUES)
    - .planning/phases/07-social-campaign-planner/07-UI-SPEC.md (§"SlotGeneratePanel.tsx" and §"SaveToCalendarPicker.tsx" — every visual detail incl. the Current/Suggestion blocks, the accent-tinted suggestion card, the two-button footer, the modal-vs-panel distinction)
    - .planning/phases/07-social-campaign-planner/07-04-SUMMARY.md (the slots/[slotId]/generate route request/response shape)
    - .planning/phases/07-social-campaign-planner/07-03-SUMMARY.md (the slot PATCH route — the write path used on 'Use this'/'Save to slot')
  </read_first>
  <action>
    Create `components/launchpad/SlotGeneratePanel.tsx` (`'use client'`). Props: `{ projectId: string; campaignId: string; slot: SocialPost | null; onClose: () => void; onAccept: (slotId: string, caption: string) => void }`. Reuse TipPanel's shell VERBATIM (slide-in aside, backdrop click-close, Escape useEffect). Header `text-lg font-semibold text-white` reading "Generate hook" when slot content_type ∈ {`short_form_video`,`stories`} else "Generate caption", plus TipPanel's close-X. On open (slot becomes non-null), POST to `/api/launchpad/${projectId}/campaigns/${campaignId}/slots/${slot.id}/generate` and store the returned `caption` as the suggestion; show "Generating…" text in the Suggestion block while loading (no spinner — text-based per codebase convention); on error show `text-xs text-rose-300` "Couldn't generate a suggestion — please try again.". Body: a "Current" block (label `text-[12px] font-bold uppercase tracking-[.14em] text-lavdim`, existing caption in `text-[14px] text-white/60`) and a "Suggestion" block (same label reading "Suggestion", the preview in `text-[14px] text-white` inside `rounded-[14px] border border-brandindigo/30 bg-brandindigo/5 p-4`). Footer (`border-t border-white/10 p-5`, two buttons): "Discard" (`border border-white/15 ... text-white/70`) closes unchanged; "Use this" (`bg-grad ... shadow-cta text-white font-bold`) calls `onAccept(slot.id, suggestion)` then closes — the write happens only on this click (D-10, never silent overwrite). Disable "Use this" until a suggestion exists.

    Create `components/launchpad/SaveToCalendarPicker.tsx` (`'use client'`). Props: `{ open: boolean; toolOutput: string; posts: SocialPost[]; onClose: () => void; onSave: (slotId: string, caption: string) => void }`. Render a centered modal (NOT the slide-in): `fixed inset-0 z-50 flex items-center justify-center`, same `bg-black/60 backdrop-blur-sm` backdrop (click closes), card `w-full max-w-sm rounded-[18px] border border-hair bg-card p-5`; support Escape-close for parity. Title `text-[15px] font-bold text-white` = "Save to calendar". Three stacked `<select>` fields (Platform, Week, Slot) using PitchComposer's select styling + the uppercase label style; Platform options from the distinct platforms present in `posts`, Week 1-4, Slot options filtered to `posts` matching the chosen platform+week (show a disabled "No open slot for this platform/week yet" option when none exist). Footer: "Cancel" (secondary) + "Save to slot" (`bg-grad shadow-cta`) — enabled only when a real slot is chosen; on click calls `onSave(chosenSlotId, toolOutput)` (writes the standalone tool output into that slot's caption via the same slot-PATCH path the parent wires) then closes. This component NEVER touches tool_outputs history — it only produces a slot write (D-11). 2-space indent, no semicolons.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "SlotGeneratePanel.tsx|SaveToCalendarPicker.tsx" || echo "OK — panels type-check clean"</automated>
  </verify>
  <acceptance_criteria>
    - Both files are `'use client'` components exporting `SlotGeneratePanel` and `SaveToCalendarPicker` respectively.
    - SlotGeneratePanel POSTs to the slots/[slotId]/generate route and writes to the slot ONLY via `onAccept` on the "Use this" click — generation completing does not itself write (behavior assertion — D-10 never-silent-overwrite).
    - SlotGeneratePanel reuses TipPanel's slide-in shell (backdrop click-close + Escape) (source assertion — UI-SPEC "reuse TipPanel verbatim").
    - The Suggestion block uses the accent-tint card `border-brandindigo/30 bg-brandindigo/5` (source assertion — the one place accent frames content).
    - SaveToCalendarPicker is a centered modal (`flex items-center justify-center`, `max-w-sm` card), NOT the slide-in aside, and saves only via `onSave` on "Save to slot" (behavior assertion — D-11).
    - SaveToCalendarPicker contains no reference to `tool_outputs` (source assertion — D-11).
    - `npx tsc --noEmit` reports no errors in either file.
  </acceptance_criteria>
  <done>Both preview surfaces show AI output before any write and only mutate a slot on an explicit accept action.</done>
</task>

<task type="auto">
  <name>Task 2: CampaignHistoryList.tsx (D-04 switch-active / D-05 inline-confirm hard-delete)</name>
  <files>components/launchpad/CampaignHistoryList.tsx</files>
  <read_first>
    - components/admin/ChecklistAdmin.tsx (the inline delete-confirm pattern — `role="alert"`, `rounded-[10px] border border-rose-500/30 bg-rose-500/5 p-4`, confirm/cancel button handlers, the confirm-pending state machine — reuse verbatim, never window.confirm)
    - components/curators/PitchComposer.tsx (secondary button style for "Set active")
    - lib/launchpad/campaigns.ts (SocialCampaign)
    - .planning/phases/07-social-campaign-planner/07-UI-SPEC.md (§"CampaignHistoryList.tsx" + Copywriting Contract — the "Active" emerald badge, "Set active" button, the delete confirm copy "Delete this campaign? All {N} slots...", active-row-has-no-delete rule)
    - .planning/phases/07-social-campaign-planner/07-03-SUMMARY.md (the PATCH set-active + DELETE campaign route shapes)
  </read_first>
  <action>
    Create `components/launchpad/CampaignHistoryList.tsx` (`'use client'`). Props: `{ projectId: string; campaigns: SocialCampaign[]; onActiveChanged: (campaignId: string) => void; onDeleted: (campaignId: string) => void }`. Container `rounded-[14px] border border-hair bg-card divide-y divide-hair`. Each row `flex items-center justify-between gap-3 px-[18px] py-4`: left = campaign `name` (`text-[14px] font-bold text-white`) + created date (`text-[12.5px] text-lavdim`, from `created_at` via toLocaleDateString). Right side: if `is_active`, render the "Active" badge `inline-flex items-center rounded-full border border-emerald-400/30 bg-emerald-400/10 px-2 py-0.5 text-[10px] font-bold text-emerald-300` and NO delete trigger (D-05 — active cannot be deleted). If inactive: a "Set active" secondary button (`rounded-md border border-white/15 px-3 py-1.5 text-xs text-white/70 hover:border-white/30 hover:text-white`) that PATCHes `/api/launchpad/${projectId}/campaigns` with `{ campaignId }` and calls `onActiveChanged` on success, plus a plain-text "Delete" destructive trigger (`text-xs text-rose-300 hover:text-rose-200`). Clicking Delete opens the INLINE confirm block (ChecklistAdmin pattern, never window.confirm): `role="alert"`, `rounded-[10px] border border-rose-500/30 bg-rose-500/5 p-4`, copy "Delete this campaign? All {N} slots and their completion progress will be permanently removed. This cannot be undone." (N = that campaign's posts.length), a "Delete campaign" solid rose button (`bg-rose-500 hover:bg-rose-600`) that DELETEs `/api/launchpad/${projectId}/campaigns` with `{ campaignId }` and calls `onDeleted` on success, and a "Cancel" button. Track confirm-pending + saving state per row. 2-space indent, no semicolons.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "CampaignHistoryList.tsx" || echo "OK — CampaignHistoryList type-checks clean"</automated>
  </verify>
  <acceptance_criteria>
    - File is a `'use client'` component exporting `CampaignHistoryList`.
    - The active row shows the emerald "Active" badge and renders NO "Delete" trigger (behavior assertion — D-05).
    - Inactive rows show a "Set active" button (PATCH) and a "Delete" trigger that opens an inline `role="alert"` confirm block — there is no `window.confirm` in the file (source assertion — ChecklistAdmin precedent).
    - The confirm copy interpolates the campaign's slot count `{N}` from `posts.length` (behavior assertion — UI-SPEC copy contract).
    - "Delete campaign" issues a DELETE to the campaigns route and calls `onDeleted` on success (source assertion).
    - `npx tsc --noEmit` reports no errors in this file.
  </acceptance_criteria>
  <done>CampaignHistoryList switches the active campaign and hard-deletes inactive ones through an inline confirm, never exposing delete on the active row.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CampaignCalendar + CampaignHistoryList into the Launchpad project page</name>
  <files>app/(artist)/launchpad/[projectId]/page.tsx</files>
  <read_first>
    - app/(artist)/launchpad/[projectId]/page.tsx (the FULL current file — the `createServerClient()` + `Promise.all([...])` data-fetch block, the `LaunchpadRoom` + `<div className="mt-9 space-y-9">` PitchComposer stacking Phase 6 added; the project fetch that already selects genre/release_date/cover_art_url; the redirect/notFound guards)
    - components/launchpad/CampaignCalendar.tsx (07-05 — its props: projectId, initialCampaign, profileGenres, projectGenre)
    - components/launchpad/CampaignHistoryList.tsx (Task 2 — its props)
    - lib/launchpad/campaigns.ts (readPosts, SocialCampaign — to shape the fetched active campaign)
    - .planning/phases/07-social-campaign-planner/07-RESEARCH.md ("app/(artist)/launchpad/[projectId]/page.tsx" reusable-asset note — add the campaign fetch to the existing Promise.all, normal user-scoped RLS read, third stacked block)
  </read_first>
  <action>
    Modify `app/(artist)/launchpad/[projectId]/page.tsx`. In the existing server data-fetch (`createServerClient()` + `Promise.all`), add a read of the active campaign: `.from('social_campaigns').select('*').eq('project_id', projectId).eq('user_id', user.id).eq('is_active', true).maybeSingle()` (normal user-scoped RLS read — NOT createServiceClient), and a list read of all campaigns for the history list. Also ensure the artist's `artist_profiles.genres` and the project's `genre` are available (the project fetch already selects `genre`, `release_date`, `cover_art_url` — confirm/extend the select if `genres` isn't already loaded). Shape the active campaign via `readPosts()` on its `posts` into a `SocialCampaign`-typed object (or null). After the existing `<div className="mt-9 space-y-9">` PitchComposer block, add a THIRD stacked block `<div className="mt-9 space-y-9">` containing the `CampaignCalendar` (props: `projectId`, `initialCampaign`, `profileGenres={profile?.genres ?? null}`, `projectGenre={project.genre}`) and, below it, the `CampaignHistoryList` (props: `projectId`, the campaigns list) — matching the exact stacking convention Phase 6 used. Import both components. Do not disturb the existing checklist/pitch rendering, redirect, or notFound logic. 2-space indent, no semicolons.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - The page's server fetch reads the active `social_campaigns` row with `.eq('user_id', user.id).eq('is_active', true).maybeSingle()` via `createServerClient()` (source assertion — normal RLS read, not service client).
    - `CampaignCalendar` is rendered in a third `mt-9 space-y-9` block below the existing PitchComposer block, receiving `initialCampaign`, `profileGenres`, and `projectGenre` props (source assertion — SOCIAL-03 mount).
    - `CampaignHistoryList` is rendered below CampaignCalendar with the campaigns list (source assertion — D-04/D-05 surface).
    - The existing `LaunchpadRoom` and `PitchComposer` render blocks are unchanged (source assertion — no regression).
    - `npm run build` completes successfully (green) — the whole feature compiles end-to-end.
  </acceptance_criteria>
  <done>The Launchpad project page fetches the active campaign server-side and renders the full campaign planner as a third stacked section, making SOCIAL-01..07 reachable end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server page → social_campaigns read | The page reads the active campaign with the user-scoped server client; RLS gates it |
| history-list actions → campaigns API | Set-active/delete requests re-authorized server-side (07-03), the client list is untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-19 | Elevation of Privilege | page active-campaign fetch | high | mitigate | Page reads social_campaigns via `createServerClient()` (user-scoped, RLS-enforced) with an explicit `.eq('user_id', user.id)` — never `createServiceClient()`, so no cross-user campaign can be rendered |
| T-07-20 | Tampering | history-list delete/set-active | high | mitigate | Both actions hit 07-03's routes which re-derive ownership + enforce the active-cannot-be-deleted rule server-side; the client cannot bypass by manipulating the list |
| T-07-21 | Repudiation | silent overwrite via panels | medium | mitigate | SlotGeneratePanel/SaveToCalendarPicker only write on explicit "Use this"/"Save to slot" — generation itself never mutates, so no unattributed caption change can occur (D-10/D-11) |
| T-07-SC | Tampering | npm installs | low | accept | No new packages (RESEARCH.md audit) |
</threat_model>

<verification>
- `npm run build` green (full feature compiles).
- Manual smoke (full Phase Requirements → Test Map pass from RESEARCH.md): generate → work slots → inline generate preview-then-accept → standalone save-to-calendar → complete slots → switch/delete campaigns → export subset CSV.
- Checker Dimension 1-6 pass against UI-SPEC (copy, visuals, color, typography, spacing, registry safety).
</verification>

<success_criteria>
- Preview-then-accept loop closed for both inline and standalone generation (SOCIAL-05, D-10/D-11).
- Campaign history switch-active + inline-confirm hard-delete (D-04/D-05).
- Full feature mounted into /launchpad/[projectId] — SOCIAL-01..07 reachable end-to-end (SOCIAL-03).
</success_criteria>

<output>
Create `.planning/phases/07-social-campaign-planner/07-06-SUMMARY.md` when done.
</output>
