---
phase: 07-social-campaign-planner
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - lib/tools/registry.ts
  - lib/launchpad/campaigns.ts
autonomous: true
requirements: [SOCIAL-03, SOCIAL-05, SOCIAL-07]

must_haves:
  truths:
    - "buildCalendarPrompt() returns a prompt that isolates release + collaborator data in a <release_data> block and hard-codes the 6 platforms' character/content-type constraints in non-user-controllable prompt text (SOCIAL-03)"
    - "buildCalendarPrompt() instructs the model to return a JSON object whose posts array carries platform, week (1-4), content_type, and caption per slot, with AI-decided per-platform pacing (D-06) and AI-assigned content_type (D-07)"
    - "buildSlotCaptionPrompt() and buildSlotHookPrompt() each return a prompt for ONE slot (platform + week + content_type + existing caption context) whose required JSON output shape is exactly { caption: string } — not the multi-field standalone tool shape (D-12, SOCIAL-05)"
    - "computeDefaultPostingTime() returns an ISO-8601 timestamp landing on the platform's default weekday+time within the slot's week window [release_date + (week-1)*7, release_date + week*7 - 1] (D-15, SOCIAL-07)"
    - "readCalendarPosts() coerces raw AI-generated slot JSON into valid SocialPost[] using readPosts(), assigns each slot a stable id and a computed default posting_time, and drops slots with invalid platform/content_type/week (SOCIAL-03, SOCIAL-04)"
  artifacts:
    - "lib/tools/registry.ts (buildCalendarPrompt, buildSlotCaptionPrompt, buildSlotHookPrompt, SlotCaptionOutput type, exported PLATFORM_CONSTRAINTS text block)"
    - "lib/launchpad/campaigns.ts (PLATFORM_POSTING_DEFAULTS map, computeDefaultPostingTime, readCalendarPosts helpers appended)"
  key_links:
    - "buildCalendarPrompt is invoked by the campaigns generate route (07-03); buildSlotCaptionPrompt/buildSlotHookPrompt by the slot-generate route (07-04)"
    - "computeDefaultPostingTime is used by readCalendarPosts to stamp every AI-generated slot; the same posting_time is what the CSV export (07-04) formats for Buffer"
  prohibitions:
    - "slot-scoped prompts must NOT be registered as a 7th ToolSlug in lib/tools/registry.ts's TOOLS array / getTool() dispatcher (D-12, RESEARCH.md Anti-Patterns) — they are standalone exported functions dispatched directly by the new routes"
    - "the model constant used by any code this plan touches must be 'claude-sonnet-4-6' — never import lib/anthropic/index.ts's stale 'claude-sonnet-4-20250514' (RESEARCH.md Pitfall 4). This plan writes only prompt builders (no Anthropic call yet), but must not introduce the dead constant."
---

<objective>
Add the AI prompt builders and posting-time logic the generation routes need: extend `lib/tools/registry.ts` with a full-calendar prompt (`buildCalendarPrompt`) and two slot-scoped prompts (`buildSlotCaptionPrompt`, `buildSlotHookPrompt`) following the existing `buildDropReadyPrompt`/`buildSoundBaitPrompt` tail convention; and extend `lib/launchpad/campaigns.ts` with the per-platform posting-time defaults (`PLATFORM_POSTING_DEFAULTS`, `computeDefaultPostingTime`) and a `readCalendarPosts()` that turns raw AI JSON into validated `SocialPost[]` (stable ids + default posting times).

Purpose: Isolate all the "what do we ask the model, and how do we default a posting time" logic in pure, testable functions so the routes in wave 3 are thin controllers. Keeping slot-scoped generation OUT of the `ToolSlug` dispatcher (D-12) prevents it from ever being saved to `tool_outputs` or surfaced as a standalone tool.
Output: extensions to two existing files (`lib/tools/registry.ts`, `lib/launchpad/campaigns.ts`) — no new files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-social-campaign-planner/07-CONTEXT.md
@.planning/phases/07-social-campaign-planner/07-RESEARCH.md
@.planning/phases/07-social-campaign-planner/07-PATTERNS.md
@.planning/phases/07-social-campaign-planner/07-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (07-02 subset)

New symbols introduced by this plan (exclude from source-drift verification):
- **lib/tools/registry.ts additions:** `buildCalendarPrompt`, `buildSlotCaptionPrompt`, `buildSlotHookPrompt`, `SlotCaptionOutput`, `PLATFORM_CONSTRAINTS` (exported prompt-text constant)
- **lib/launchpad/campaigns.ts additions:** `PLATFORM_POSTING_DEFAULTS`, `computeDefaultPostingTime(releaseDate, week, platform, sameWeekIndex?)`, `readCalendarPosts(raw, releaseDate)`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend lib/launchpad/campaigns.ts with posting-time defaults + readCalendarPosts</name>
  <files>lib/launchpad/campaigns.ts</files>
  <behavior>
    - computeDefaultPostingTime('2026-07-15', 1, 'instagram') returns an ISO timestamp whose weekday is Wednesday and time is 12:00 local, falling inside [2026-07-15, 2026-07-21].
    - computeDefaultPostingTime for week 2 lands inside [release_date + 7, release_date + 13].
    - Two slots for the same platform in the same week get offset timestamps (not identical), for calendar legibility.
    - readCalendarPosts(rawWithBadRows, releaseDate) drops any AI slot with platform='linkedin' or week=5, assigns a stable id and a computed posting_time to each surviving slot, and sets completed=false / source='ai'.
  </behavior>
  <read_first>
    - lib/launchpad/campaigns.ts (the Task-2/Task-3 output of 07-01 — the SocialPost type, PLATFORM_VALUES, CONTENT_TYPE_VALUES, readPosts to reuse)
    - .planning/phases/07-social-campaign-planner/07-RESEARCH.md ("Per-Platform Posting Defaults" table + the "Default posting-time algorithm (D-15)" paragraph — the exact weekday/time per platform and the week-window math)
  </read_first>
  <action>
    Append to `lib/launchpad/campaigns.ts`. Export `PLATFORM_POSTING_DEFAULTS: Record<Platform, { weekday: number; hour: number; minute: number }>` using the RESEARCH.md "Per-Platform Posting Defaults" table exactly: instagram → Wednesday 12:00 (weekday 3, hour 12), tiktok → Tuesday 19:00 (weekday 2, hour 19), x → Wednesday 13:00 (weekday 3, hour 13), youtube_shorts → Thursday 18:00 (weekday 4, hour 18), facebook → Wednesday 13:00 (weekday 3, hour 13), threads → Thursday 09:00 (weekday 4, hour 9). Use `weekday` in JS `getDay()` semantics (0=Sunday..6=Saturday). Export `computeDefaultPostingTime(releaseDate: string | null, week: 1 | 2 | 3 | 4, platform: Platform, sameWeekIndex = 0): string`: if `releaseDate` is null/unparseable, base the window on `now()`; compute the week window start = `release + (week-1)*7 days` at 00:00 local; scan forward day-by-day within the 7-day window for the first date whose `getDay()` equals the platform's default weekday, set hours/minutes to the platform default; if `sameWeekIndex > 0`, add `sameWeekIndex * 2` days (offset stacked same-platform-same-week slots for legibility, per RESEARCH.md's discretion note — keep within reason, do not push past the window meaningfully); return `.toISOString()`. Export `readCalendarPosts(raw: unknown, releaseDate: string | null): SocialPost[]`: accept the AI's parsed `posts` array (an array under a `posts` key or a bare array), map each raw slot into the shape `readPosts` expects, but BEFORE calling `readPosts`, assign each slot a stable `id` (`crypto.randomUUID()`), default `completed: false`, `completed_at: null`, `source: 'ai'`, and a computed `posting_time` via `computeDefaultPostingTime(releaseDate, week, platform, sameWeekIndex)` (track a per-(platform,week) running index for the offset); then pass the enriched array through `readPosts()` so the same enum/range validation drops any hallucinated platform/content_type/week. Return the validated array. Named exports, 2-space indent, no semicolons.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "campaigns.ts" || echo "OK — campaigns.ts type-checks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `PLATFORM_POSTING_DEFAULTS` has all 6 platform keys with the exact weekday/hour values from the RESEARCH.md table (Instagram Wed 12:00, TikTok Tue 19:00, X Wed 13:00, YouTube Shorts Thu 18:00, Facebook Wed 13:00, Threads Thu 09:00).
    - `computeDefaultPostingTime('2026-07-15', 1, 'instagram')` returns an ISO string whose parsed `Date` has `getDay() === 3` (Wednesday) and `getHours() === 12` (behavior assertion).
    - `computeDefaultPostingTime(null, 2, 'tiktok')` returns a valid ISO string (no throw when release date is absent).
    - `readCalendarPosts` drops a raw slot with `platform: 'linkedin'` and one with `week: 5`, and every surviving slot has a non-empty `id`, `completed === false`, `source === 'ai'`, and a non-empty `posting_time` (behavior assertion).
    - `npx tsc --noEmit` reports no errors in `campaigns.ts`.
  </acceptance_criteria>
  <done>campaigns.ts can turn raw AI calendar JSON into a validated, id-stamped, default-scheduled SocialPost[].</done>
</task>

<task type="auto">
  <name>Task 2: Extend lib/tools/registry.ts with calendar + slot-scoped prompt builders</name>
  <files>lib/tools/registry.ts</files>
  <read_first>
    - lib/tools/registry.ts (buildDropReadyPrompt/buildSoundBaitPrompt — the exact (profile, project) signature + "Respond with ONLY a JSON object..." tail convention to copy; ToolProjectContext type; the TOOLS array + getTool()/buildToolPrompt() dispatcher that these new functions must NOT be added to)
    - .planning/phases/07-social-campaign-planner/07-RESEARCH.md (Pattern 2 <release_data> block + PLATFORM CONSTRAINTS text; "Code Examples" buildSlotCaptionPrompt; Anti-Patterns "do not register a 7th ToolSlug"; Pitfall 4 model constant)
    - .planning/phases/07-social-campaign-planner/07-PATTERNS.md ("lib/tools/registry.ts (extended)" section)
  </read_first>
  <action>
    Append new exported functions to `lib/tools/registry.ts` WITHOUT modifying the `TOOLS` array, the `ToolSlug` union, or `getTool()`/`buildToolPrompt()` (D-12 / RESEARCH.md Anti-Pattern — slot and calendar generation are NOT standalone tools and must never route through `getTool()` or be saved to `tool_outputs`). Export a `PLATFORM_CONSTRAINTS` string constant holding the 6-platform character/content-type limits verbatim from RESEARCH.md Pattern 2 (Instagram ~2200 chars: static image/Stories/Reels; TikTok <150 chars, short-form video only; X <280 chars text/static image; YouTube Shorts <100 char title, short-form video only; Facebook ~500 chars static image/short-form video/text; Threads <500 chars text/static image). Export `buildCalendarPrompt(profile: ArtistProfile, project: ToolProjectContext, collaboratorNames: string[], activePlatforms: string[]): string`: opens with a "You are a social media strategist building a 4-week release campaign calendar" instruction, embeds `PLATFORM_CONSTRAINTS`, isolates user-supplied release data in a `<release_data>` … `</release_data>` block (Title, Genre, Release date, Artist notes as the story, Collaborators = collaboratorNames joined or 'none', Active platforms = activePlatforms joined), and ends with a "Respond with ONLY a JSON object (no markdown, no preamble)" instruction whose stated shape is `{ "posts": [ { "platform": "...", "week": 1-4, "content_type": "...", "caption": "..." } ] }` — explicitly instruct: only the active platforms, weeks 1-4 only, AI decides per-platform pacing (multiple posts per week allowed for high-frequency platforms like TikTok — D-06), AI assigns content_type per slot from the 5 allowed types matching the platform's supported types (D-07), and do not invent release facts not provided. Export `type SlotCaptionOutput = { caption: string }`. Export `buildSlotCaptionPrompt(profile: ArtistProfile, project: ToolProjectContext, slot: { platform: string; week: number; content_type: string; existingCaption: string }): string` exactly per RESEARCH.md's "Code Examples" reference — ARTIST/RELEASE/SLOT sections, ending with `Respond with ONLY a JSON object ... { "caption": "..." }`. Export `buildSlotHookPrompt(...)` with the same signature but framed for a hook (video/stories content types) rather than a caption — same `{ caption: string }` output shape (the field stays `caption`; only the creative framing differs). Copy the existing prompt builders' tail convention exactly. Do NOT construct any Anthropic client or MODEL constant in this file — these are pure string builders. Named exports, 2-space indent, no semicolons.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "registry.ts" || echo "OK — registry.ts type-checks clean"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/tools/registry.ts` exports `buildCalendarPrompt`, `buildSlotCaptionPrompt`, `buildSlotHookPrompt`, `SlotCaptionOutput`, `PLATFORM_CONSTRAINTS`.
    - The `TOOLS` array length and the `ToolSlug` union are unchanged from before this task (source assertion — no new slug added; `getTool`/`buildToolPrompt` untouched).
    - `buildCalendarPrompt(...)` output contains the literal substrings `<release_data>` and `</release_data>` and a JSON shape mentioning `"posts"`, `"platform"`, `"week"`, `"content_type"`, `"caption"` (behavior assertion on returned string).
    - `buildSlotCaptionPrompt(...)` output ends with an instruction to return a JSON object of shape `{ "caption": ... }` and contains no reference to the multi-field standalone shape (`instagram_caption`, `hooks`).
    - The file contains no occurrence of `claude-sonnet-4-20250514` and does not import from `@/lib/anthropic` (source assertion — Pitfall 4).
    - `npx tsc --noEmit` reports no errors in `registry.ts`.
  </acceptance_criteria>
  <done>registry.ts exposes the calendar and slot-scoped prompt builders, dispatched directly by the wave-3 routes, never through the ToolSlug dispatcher.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user release data → AI prompt | Free-text release title/notes/collaborator names are interpolated into the calendar prompt (prompt-injection surface) |
| AI output → posts JSONB | The model's JSON is untrusted input that must be validated before storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering (prompt injection) | buildCalendarPrompt | medium | mitigate | User-supplied release data isolated in a `<release_data>` block with platform constraints in non-user-controllable prompt text (RESEARCH.md Pattern 2) — a malicious note cannot restructure the platform rules; worst case is degraded caption text, not privilege change |
| T-07-05 | Tampering (data integrity) | readCalendarPosts → readPosts | medium | mitigate | AI output routed through readPosts() enum/range validation before it can become a stored slot — a hallucinated platform/content_type/week is dropped, not persisted (defense treats AI output as untrusted input, per RESEARCH.md Security Domain) |
| T-07-06 | Information Disclosure | model constant | low | mitigate | This plan introduces no Anthropic client; acceptance criteria assert no dead `claude-sonnet-4-20250514` constant / no `@/lib/anthropic` import leaks in — keeping the active-convention model in the routes (07-03/07-04) unambiguous |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for both files.
- `TOOLS` array / `ToolSlug` union unchanged (grep-confirm no 7th slug).
- Prompt builders produce the `<release_data>` block and the correct JSON output shapes.
- Posting-time defaults land on the correct weekday/time within the correct week window.
</verification>

<success_criteria>
- Calendar + slot prompt builders ready and isolated from the ToolSlug dispatcher (SOCIAL-03, SOCIAL-05, D-12).
- Posting-time defaults + readCalendarPosts turn AI JSON into validated, scheduled slots (SOCIAL-04, SOCIAL-07 posting-time foundation).
</success_criteria>

<output>
Create `.planning/phases/07-social-campaign-planner/07-02-SUMMARY.md` when done.
</output>
